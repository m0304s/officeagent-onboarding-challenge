# ARCHITECTURE
기술 스택 선택 근거와 설계 의도를 정리한 문서. 이 프로젝트는 채용 과제이지만 실제 서비스에서도 동작할 수 있는 수준의 안정성과 성능을 목표로 구현했습니다. 구체적으로는 아래 네 가지 원칙을 설계 전반에 적용했습니다.

설계 전반에 적용한 네 가지 원칙:

1. **조용한 실패를 관측 가능하게 만든다** — 오류 없이 품질만 나빠지는 자리를 찾아, 서명·응답 필드·경고 로그로 드러냅니다.
2. **결함 격리** — 외부 의존성 하나의 장애가 서비스 전체 장애로 번지지 않게 차단합니다.
3. **경계는 가능한 곳부터 도구로 강제한다** — `core/` 의 서드파티 격리와 `os.environ` 접근 지점은 린트로, 주석 규칙은 스크립트로 막습니다. 도구가 미치지 않는 경계는 그 사실을 적습니다(3절).
4. **상수는 재서 정한다** — 감으로 적은 임계값은 거의 확실히 틀립니다. 못 잰 값은 못 쟀다고 적습니다.


---

## 1. 기술 스택 선택 근거

### 언어 / 프레임워크: Python 3.11 + FastAPI

임베딩·벡터 DB 생태계가 가장 두꺼운 언어가 Python이고, FastAPI는 async/await 네이티브라 LLM 호출처럼 수 초 단위 I/O 바운드 작업에서 스레드 점유 없이 동시성을 냅니다. 과제 기술 조건이기도 합니다.

다만 **블로킹 코드가 이벤트 루프를 막지 않게 하는 책임을 코드 구조로 옮겼습니다.** 파서 프로토콜은 일부러 **동기로 선언**해 호출부가 `asyncio.to_thread` 를 의식하게 만들고, 저장소 어댑터는 반대로 async로 선언해 오프로드를 안에서 처리합니다. 파서는 CPU 바운드 수백 ms~초라 호출부가 알아야 실수를 막을 수 있고, 저장소는 짧은 I/O라 호출부가 매번 감싸면 중복만 늘기 때문입니다.

### 벡터 DB: ChromaDB (서버 모드)

- 메타데이터 필터를 **질의와 함께** 넘길 수 있어 대상 집합 제한을 사후 필터가 아닌 사전 필터로 처리할 수 있습니다.
- 임베디드가 아니라 **서버 모드**입니다. 임베디드는 워커가 둘이면 파일 잠금이 갈리고, 데이터 검사에 별도 도구가 필요합니다.
- **컬렉션 이름에 벡터 차원이 들어갑니다**(`document_chunks_d384`). Chroma는 컬렉션당 차원 하나만 허용하고 그 차원이 컬렉션을 비워도 남아서, 없으면 모델을 바꿨을 때 재업로드가 **영구히** 실패합니다.
- 컬렉션에 임베딩 함수를 붙이지 않습니다. 비워 두면 라이브러리가 자기 기본 모델을 받으러 나가, 색인 서명이 가리키는 모델과 실제로 벡터를 만든 모델이 어긋납니다.

기각: Qdrant·Weaviate·Milvus는 별도 운영 컴포넌트가 늘어 "한 줄 실행" 조건과 맞지 않습니다.

### 어휘 색인: SQLite FTS5 + BM25

하이브리드 검색의 두 번째 신호입니다. **컨테이너가 늘지 않는 것**이 선택 근거입니다 — 파일 하나이고 Python 표준 라이브러리에 들어 있습니다.

레지스트리와 파일을 나눈 이유는 검색용 테이블이 섞이면 백업·삭제 절차가 두 관심사를 함께 다루게 되기 때문입니다.

기각: Elasticsearch·OpenSearch는 서비스 하나가 더 늘고, `rank-bm25` 같은 인메모리 라이브러리는 색인이 프로세스 메모리에 살아 재기동마다 전체 재구축이 필요합니다.

**토큰화는 kiwipiepy(형태소 분석)로 합니다.** 초기에는 순수 파이썬 규칙 토크나이저였고(`add-rrf` 결정 2), konlpy·mecab 을 의존성 비용(JVM·별도 사전·플랫폼 의존)으로 기각한 판단이었습니다. kiwipiepy 는 그 세 비용을 대부분 해소합니다 — 순수 pip 설치, 모델이 wheel 에 번들되어 런타임 다운로드가 없고(오프라인 `--network none` 으로 실측), JVM 이 필요 없습니다. 규칙 기반의 과벗김을 흡수하던 세 장치(원형+어근 병기·희소도 하한·RRF 융합) 중 첫째가 형태소 분석에 흡수되어 사라졌고, 나머지 둘은 그대로 유효합니다. 내용어 태그(명사·동사·형용사·어근·외국어·숫자·한자)만 남기고 조사·어미·기호는 버립니다. 규칙 구현(`RuleTokenizer`)은 지우지 않고 테스트 기본값으로 남깁니다 — 모델 로드가 없어 핵심 3경로 테스트가 kiwipiepy 설치에 묶이지 않습니다. `core/` 서드파티 금지는 kiwipiepy 를 어댑터(`adapters/tokenizer/`)로 격리해 지키고, 상위 계층은 `Tokenizer` 프로토콜만 봅니다. 판 번호와 kiwipiepy 버전을 `signature_material` 로 내보내 색인 서명에 실립니다 — 분석기가 바뀌면 재색인이 필요하다는 사실이 서명으로 표현됩니다.

### 캐시 DB: Redis 8

L1 정확 매치 + L2 유사 매치 + 태그 무효화 + 부정 판정 집합. **8 이상이 필요한 이유는 벡터셋(`VADD`/`VSIM`)** 입니다 — 유사 매치 후보를 "최신 N개"가 아니라 "가장 가까운 N개"로 고르기 위해서고, 그 이유는 5절에 있습니다.

기각한 대안(RediSearch VSS, Chroma에 캐시 컬렉션 추가, 프로세스 메모리, 완전 탐색)과 기각 사유는 5절 끝에 표로 있습니다.

### 임베딩: `intfloat/multilingual-e5-small` (384차원)

| 고려 사항 | 판단 |
|---|---|
| 언어 | 샘플 문서가 한국어라 영어 전용 모델은 검색이 동작하지 않습니다 |
| 입력 창 | **512 토큰.** 한국어 600자 청크가 400~450 토큰이라 여유 있게 덮습니다 |
| 차원 | 384 — L2 캐시 메모리와 배치 임베딩 속도에 유리 |
| 크기 | 파라미터 118M — e5 계열에서 가장 작은 구성(fp32 가중치 약 470MB) |
| 접두사 규약 | `query:` / `passage:` 를 **어댑터 안에 가둡니다** |

**e5 계열의 비대칭 접두사가 이 파일 밖으로 나가지 않습니다.** 상위 계층은 `embed_query` / `embed_documents` 두 메서드만 봅니다. 접두사를 한쪽만 붙이거나 둘 다 빠뜨리면 오류 없이 점수 분포만 이동해 하한이 무의미해지는데, 그것을 호출부가 실수할 수 없게 만든 것입니다. 대신 접두사 규약의 판 번호(`e5-prefix-v1`)를 `signature` 로 내보내 색인 서명에 실립니다 — 규약이 바뀌면 다른 벡터가 나오므로 재색인이 필요하다는 사실이 서명으로 표현됩니다.

**차원과 입력 창은 선언하고, 로딩할 때 실제 모델과 대조합니다.** 선언이 틀리면 서명이 거짓이 되거나 토큰 가드가 상한을 넘겨 통과시킵니다.

### 리랭커: `BAAI/bge-reranker-v2-m3`

기각의 축이 둘입니다.

| 후보 | 기각 사유 |
|---|---|
| `ms-marco-MiniLM-L-6-v2` | **영어 전용.** 가장 흔한 선택이고 가장 흔한 실패 — 한국어에서도 점수가 나오고 순서도 매겨지는데 그 순서가 무작위에 가깝습니다 |
| `mmarco-mMiniLMv2-L12` | 학습 코퍼스(mMARCO 13개 언어)에 **한국어가 없습니다** |
| `bge-reranker-base` | XLM-R 백본이지만 학습은 중국어·영어 |
| ~~`gte-multilingual-reranker-base`~~ | **채택했다가 실물에서 기각했습니다** — 아래 |
| **`bge-reranker-v2-m3` (채택)** | 학습 언어에 **한국어가 있고**, `XLMRobertaForSequenceClassification` 이라 **원격 코드가 없습니다** |

**첫째 축은 한국어가 학습 분포 안에 있는가입니다.** 대상이 한국어 문서인데 영어 전용 크로스인코더를 얹으면 리랭킹은 순위를 **섞는** 단계가 되고, 그 실패가 조용해서 원인을 짚을 수 없습니다.

**둘째 축은 gte를 실제로 얹어 보고 생겼습니다.** 컨테이너에서 `IndexError: index ... is out of bounds` 로 죽었고, `sentence-transformers` 문제가 아니라 원격 코드(2024년판)가 이미지의 `transformers` 와 맞지 않아 초기화되지 않은 버퍼가 인덱스로 쓰인 것이었습니다.

초안은 `trust_remote_code=True` 를 받아들이고 **리비전 고정으로 완화**하는 것이었는데, **그 완화책이 성립하지 않았습니다.** mGTE의 `config.json` 은 `"repo--module"` 형식으로 코드를 가리켜 실행되는 코드가 **두 번째 저장소**에 있고, 모델 저장소에 `revision=` 을 걸어도 그 코드는 각자의 `main` 에서 옵니다. 고정한 것은 가중치뿐입니다. `XLMRobertaForSequenceClassification` 은 `transformers` 안에 있어 원격 코드가 아예 없으므로, 위험 목록에서 항목이 **완화된 것이 아니라 삭제**됐습니다. 대가는 이미지 +2.2GB이고 같은 줄에 적어 두었습니다.

**그래서 채택한 모델에는 리비전 고정이 실제로 성립합니다** — `953dc6f6…` 를 `KNOWN_RERANKER_PROFILES` 에 박아 가중치·설정·토크나이저를 한꺼번에 묶고, 그 값이 `rerank_signature`(`모델@리비전/점수규약`)로 캐시 키에 실립니다. 같은 값이 `Dockerfile` 에도 있어야 빌드가 굽는 커밋과 런타임이 찾는 커밋이 같아지는데, `Dockerfile` 은 앱 패키지를 import 할 수 없어 두 곳에 적힙니다. **그 두 값이 같은지는 테스트가 `Dockerfile` 을 읽어 단언합니다**(`tests/test_boot.py`) — 어긋나면 런타임이 캐시에서 커밋을 찾지 못해 다운로드가 조용히 되살아납니다.

프로필에는 입력 창(8192 토큰)도 함께 둡니다. 모델을 로딩하지 않고 알아야 기동 시점에 "질의 상한 + 청크 크기"가 창을 넘는 구성을 걸러낼 수 있기 때문입니다(3절 「기동을 막는 여섯 가지」).

### LLM: Codex CLI — 단, `codex exec` 가 아니라 `codex app-server`

과제가 허용한 SDK 중 Codex SDK를 사용했습니다.

초안은 `codex exec --output-schema` 였습니다. 컨테이너에서 실측한 결과 이 표면은 **델타를 주지 않습니다** — 4,808자 응답이 이벤트 **하나**로 왔습니다. 이 위에서 SSE를 구현하면 스트리밍하는 겉모습만 얻고 첫 조각 도착 시각은 하나도 앞당겨지지 않습니다.

같은 프롬프트를 `codex app-server` 로 다시 재니 **209자에 델타 112건**, 첫 델타까지 4.5초였습니다. 그래서 변경했습니다. 자세한 통합 구조는 2절에 있습니다.

**Claude Code SDK를 기각한 이유는 실행 조건입니다.** macOS Keychain에 자격증명이 있어 `docker compose` 한 줄로 컨테이너에 넣을 방법이 없었습니다. Codex는 평문 `~/.codex/auth.json` 이라 마운트로 해결됩니다 — 과제의 "한 줄 실행" 조건이 SDK 선택을 결정한 경우입니다.

---

## 2. LLM SDK 통합 방식

### 통합 구조

```
[QaService]                      정책만 — 재시도 횟수·백오프·동시 상한
     ↓ generate(prompt, timeout_seconds) -> AsyncIterator[str]
[CodexAnswerGenerator]           턴 하나의 수명, 델타 파싱, 타임아웃, 인증 판정
     ↓ acquire() / release()
[SessionPool]                    지연 기동, 상한, 죽은 세션 교체
     ↓
[AppServerSession]               프로세스 하나 + stdio 위의 줄 단위 JSON-RPC
     ↓ asyncio.create_subprocess_exec
[codex app-server]               Node.js, @openai/codex
```

**어댑터를 세 부품으로 가른 이유**는 각 층이 다루는 실패가 다르기 때문입니다. `session.py` 는 비동기 흐름이 둘(요청/응답 + 알림 스트림)인 유일한 자리이고, `pool.py` 는 "죽은 세션을 빌려주지 않는다"만 책임지며, `codex.py` 는 턴 하나의 판정을 담습니다. 한 파일이었으면 세션 사망 판정과 턴 종료 판정이 섞였을 것입니다.

### 세션 풀 — 프로세스가 요청보다 오래 산다

`thread/start` 를 실측하니 **첫 호출 8.11초, 이후 0.07초**였습니다. 8초가 프로세스 기동 비용이라, 요청마다 프로세스를 띄우면 그 8초를 매번 냅니다. 그래서 세션을 풀에 두고 재사용합니다.

- **지연 기동** — 기동 경로에서 CLI를 실행하지 않습니다. 자격증명이 없는 환경에서 서비스가 죽으면 안 되기 때문입니다. 첫 `/qa` 요청이 띄웁니다.
- **빌려줄 때 생존을 확인합니다.** 안 하면 사망이 지연으로 둔갑해 기록됩니다.
- **반납 조건은 "확실히 끝났는가"** 하나입니다. `turn/completed` 를 못 받았으면 `turn/interrupt` 를 보내고 유예 안에 완료 알림이 오는지 보며, 안 오면 반납이 아니라 **폐기**합니다. 반쯤 끝난 턴을 반납하면 **다음 요청이 이전 턴의 델타를 받습니다.**
- 종료 훅에서 풀 전체를 회수합니다. 안 하면 컨테이너에 자식 프로세스가 남습니다.

### 프로세스를 좁히는 다섯 가지

`codex` 는 에이전트라 파일을 열고 명령을 실행할 수 있습니다. 답변 생성기로만 쓰기 위해 다섯 겹으로 좁힙니다.

| 조치 | 막는 것 |
|---|---|
| `cwd` = 빈 임시 디렉터리 | 에이전트가 뒤질 대상 자체가 없음 |
| `sandbox: "read-only"` | 쓰기 차단 |
| `approvalPolicy: "never"` | 승인 프롬프트 대기 — **타임아웃의 가장 흔한 원인** |
| `ephemeral: true` | 세션 파일 기록 — 질문·근거가 디스크의 대화 기록으로 남는 것 |
| `env` 를 상속하지 않음 (`HOME`·`PATH`·`CODEX_HOME` 만) | 컨테이너 환경변수의 저장소 접속 정보 유출 |

`env=None` 과 `env={}` 는 다릅니다 — 전자는 부모 환경이 통째로 갑니다. 그래서 `SessionLaunch.env` 에 기본값을 두지 않아 비상속이 **구조로** 강제됩니다.

**stdio 줄 상한을 4MiB로 올려 두었습니다.** 기본값 64KiB로는 부족합니다 — app-server 가 우리 프롬프트를 응답에 되돌려 주는데 그것이 30KB 대라, 근거를 많이 실은 요청에서만 `readline` 이 죽습니다. **큰 문맥에서만 나는 실패**라 작은 질의로 시험하면 발견되지 않습니다. 실패 진단용 stderr 는 마지막 20줄만 들고 있습니다 — 오래 사는 세션에서 전부 들고 있으면 그 자체가 누수입니다.

### 구조화 출력을 JSON Schema로 하지 않은 이유

`codex exec --output-schema` 를 쓰면 JSON을 강제할 수 있지만, **JSON을 토큰 단위로 흘리면 클라이언트는 완성될 때까지 아무것도 표시할 수 없습니다.** 스트리밍이 요구사항인 이상 JSON 강제와 델타 전달은 양립하지 않습니다.

그래서 **"첫 줄에 판정, 둘째 줄부터 본문"** 이라는 줄 단위 규약을 썼습니다. 판정 줄은 정확히 두 문자열 중 하나이고, 그래서 분리기의 **버퍼 상한이 21자로 증명됩니다.** 접두사에서 이탈하는 즉시 확정해 전부 내보내므로, 형식을 어긴 회차에서도 스트리밍이 멎지 않습니다. 자세한 설계는 [`PROMPT_DESIGN.md`](./PROMPT_DESIGN.md)에 있습니다.

### 스트리밍 (SSE)

```
sources → answer* → (done | error)
```

- `sources` 는 **항상 첫 이벤트**입니다. 검색이 생성보다 빨라 먼저 보낼 수 있고, `/search` 와 같은 모양이라 클라이언트 뷰가 하나면 됩니다.
- 판정 줄(`VERDICT:`)을 걷어낸 뒤로는 `answer` 조각을 **서버가 다시 만지지 않습니다.** 쪼개면 연출이고 합치면 도착 시각을 되돌립니다. 판정 줄 확정에 필요한 앞부분만 분리기가 병합합니다.
- **종료 이벤트는 정확히 하나**입니다. 종료 없는 닫힘을 허용하면 클라이언트가 매번 타임아웃으로 추정해야 합니다.
- 조용한 구간에는 SSE **주석**(`: keep-alive`)을 끼웁니다. 이벤트가 아니라 주석이라 클라이언트 파서에 나타나지 않고, 계약을 건드리지 않은 채 프록시의 유휴 연결 종료만 막습니다.
- `X-Accel-Buffering: no` — 중간 계층 버퍼링이 켜져 있으면 스트리밍의 이득이 통째로 사라집니다.

### 상태 코드로 끝나는 실패와 스트림 안으로만 알릴 수 있는 실패

`QaService` 가 `prepare()` 와 `stream()` 으로 갈려 있는 것이 이 어댑터의 첫 계약입니다.

```
prepare()   스트림 밖 — 여기서 난 예외는 HTTP 상태 코드가 된다
   질의 검증 → 캐시 조회 → 검색

stream()    스트림 안 — 여기서 난 실패는 error 이벤트가 된다
   sources → answer* → (done | error)
```

**상태 코드는 첫 바이트와 함께 확정되므로 그 경계가 코드 구조여야 합니다.** 검색 실패를 스트림 안으로 넣으면 저장소 장애가 본문 없는 `200` 이 되고, 같은 장애가 `/search` 와 `/qa` 에서 다르게 보입니다.

### 타임아웃과 재시도 — 나누는 기준

| 항목 | 어디에 | 왜 |
|---|---|---|
| 타임아웃 (기본 60초) | **어댑터** | 중단 대상(턴)과 회수 방법(세션 반납/폐기)을 아는 곳이 거기뿐입니다 |
| 재시도 횟수·백오프 | **서비스** | 정책이지 예산이 아닙니다 |

- **한 시도의 예산 하나를 여러 JSON-RPC 요청이 나눠 씁니다**(`_before(deadline)`). 요청마다 상한을 두면 합이 예산을 넘습니다.
- 기본값 60초는 **실측 최악값(18.9초)의 세 배**입니다. 정상 경로가 닿지 않는 값입니다.
- **조각이 한 번이라도 나간 뒤에는 재시도하지 않습니다.** 재시도는 이어쓰기가 아니라 처음부터 다시 쓰기라, 앞부분이 두 번 적힌 답변이 사용자에게 보입니다.
- **401은 재시도하지 않습니다.** CLI에 맡기면 자체 재시도 10회로 18.6초를 쓰는데, 백오프를 돌아도 자격증명이 생기지 않습니다. 반대로 `willRetry: true` 인 오류는 **예외로 바꾸지 않습니다** — CLI의 재시도와 우리 재시도가 이중으로 걸립니다.
- 지터를 넣지 않았습니다. 몰릴 규모가 없고, 넣으면 대기 순서 단언이 확률적이 됩니다.

### 취소 경로

사용자가 답을 보다 말고 창을 닫으면 정리가 돌아야 합니다. `aclosing` 이 **세 겹**으로 걸려 있습니다(SSE 프레이밍 → 서비스 스트림 → 생성 루프 → 어댑터, 네 층 사이의 경계 셋). `async for` 는 순회를 멈출 때 대상 생성기를 닫아 주지 않기 때문입니다.

어댑터의 `finally` 는 `asyncio.shield` 로 감쌉니다. 취소된 `finally` 안에서 그냥 `await` 하면 **중단 요청을 보내기도 전에 정리가 끝나** 세션 하나가 어중간한 상태로 남습니다.

---

## 3. 레이어 분리 설계

### 경계는 가능한 곳부터 도구로 강제한다

```
api/  ──→  services/  ──→  core/
                ↑             ↑
          adapters/  ─────────┘

core/ 는 아무것도 import 하지 않는다 (표준 라이브러리만)
```

`ruff` 의 `flake8-tidy-imports` 로 `core/` 안에서 서드파티 라이브러리 import를 **금지**합니다(`src/app/core/.ruff.toml` 의 `banned-api`). `config.py` 밖에서 `os.environ` 을 읽는 것도 같은 방식으로 막습니다(`TID251`). 문서로만 적힌 경계는 지켜지지 않습니다.

**상위 계층 import(`core/` → `app.api`·`app.services`·`app.adapters`)를 막는 린트 규칙은 아직 없습니다.** `banned-api` 는 모듈 이름을 하나씩 열거하는 방식이라 계층 방향 규칙을 표현하지 못합니다. 지금은 리뷰로 지키고 있고(현재 `core/` 의 import 는 표준 라이브러리와 `app.core` 뿐), import-linter 류의 도구를 붙이는 것이 남은 빚입니다.

레이어 감사에서 방향을 **뒤집어** 보고 도메인 규칙 유출 4건을 잡은 적이 있습니다 — 서명 비교가 네 곳, 상태 판정이 두 곳에 흩어져 있었고 조합만 다른 한 원자였습니다. `Document.matches_index` / `is_searchable_under` / `is_up_to_date` 로 집결시켰습니다. **값을 통일해도 그 값을 쓰는 규칙이 여러 벌이면 같은 자리가 다시 열립니다.**

### 어댑터는 프로토콜 뒤에 둔다 — 단, 소비자가 생길 때만

`typing.Protocol` 로 계약을 정의하고 구현체를 주입합니다. ABC 상속이 아니라 구조적 타이핑이라 어댑터가 우리 코드를 import하지 않습니다.

**프로토콜을 미리 정의하지 않는 것이 규칙입니다.** 소비처가 생기는 change에서만 정의합니다 — 미리 정한 인터페이스는 사용처가 생기는 순간 거의 틀린 것으로 드러납니다.

시그니처 자체가 결정을 담습니다.

| 시그니처 | 담긴 결정 |
|---|---|
| `VectorStore.query(embedding, ...)` — 문자열이 아니라 벡터를 받음 | 저장소가 임베딩 모델을 알지 않음 |
| `Retriever.retrieve(..., versions, embedding)` | 대상 집합 필터를 **질의 이전에** 걸고, 질의 벡터를 요청당 한 번만 만듦 |
| `DocumentParser.parse()` — 동기 | 호출부가 스레드풀 오프로드를 의식하게 함 |
| `delete_document(id, *, revision=None, index_signature=None)` | 축을 하나만 좁히는 경로가 필요 — 재색인은 `revision` 이 그대로인 채 일어남 |

### 전역 싱글턴을 두지 않는다

DI 컨테이너 라이브러리를 쓰지 않고 **앱 팩토리(`create_app`)가 전부를 주입**합니다. `create_app` 이 모든 어댑터를 선택 인자로 받아 테스트가 어댑터를 갈아끼울 수 있고, 교체 지점이 이 파일 하나로 국한됩니다. 인스턴스는 `app.state` 에 두고 FastAPI `Depends` 가 꺼내 씁니다.

**같은 인스턴스를 두 서비스가 봅니다.** 수집과 검색이 다른 레지스트리를 보면 업로드 직후 문서가 검색되지 않고, 수집과 답변이 다른 캐시 서비스를 보면 차단기 상태가 둘이 되어 무효화가 어느 쪽을 보는지 갈립니다.

### 하나의 서명이 두 색인을 함께 지배한다

`index_signature` 는 **한 곳에서만 유도됩니다**(`main.py`).

```
index_signature = sha256(canonical_json({
    embedder_signature,          # 모델/차원/정규화/접두사 규약
    chunk_strategy, chunk_strategy_version, chunk_size, chunk_overlap,
    tokenizer_signature,         # 어휘 색인의 토큰화 구성
    pdf_extraction_signature,    # 파서와 같은 객체에서 꺼냄
}))
```

- **두 서비스가 각자 유도하면** 한쪽만 고쳐진 순간 방금 올린 문서가 **오류 없이** 검색되지 않습니다.
- **벡터와 어휘를 나누지 않은 이유**는 나누면 문서 하나가 "밀집으로는 검색 가능하고 어휘로는 stale" 인 상태를 가질 수 있게 되기 때문입니다.
- PDF 추출 서명을 **파서와 같은 객체에서** 꺼내는 이유는, 따로 지정하면 마크다운으로 뽑은 청크가 평문 서명으로 저장되어도 아무 데서도 오류가 나지 않기 때문입니다.

서명이 바뀌면 기존 문서는 기동 시 `stale` 이 되고 청크가 제거되며, 재업로드가 필요하다는 사실이 목록 응답의 `index_status` 로 드러납니다. **성능 튜닝용 값(하한·동시성·배치 크기)은 서명에 들어가지 않습니다** — 하한 조정이 전면 재색인을 유발하면 아무도 조정하지 않습니다.

### 기동을 막는 여섯 가지

설정값 하나하나는 유효한데 **조합이 성립하지 않는** 구성이 있습니다. 그런 구성은 대개 오류를 내지 않고 품질만 떨어뜨리므로, `Settings` 의 교차검증(`model_validator`)이 기동 자체를 세웁니다.

| 검사 | 어기면 무슨 일이 일어나는가 |
|---|---|
| `chunk_overlap < chunk_size` | 분할이 진행되지 않아 무한 루프 |
| `retrieval_top_k ≤ retrieval_max_top_k` | 어떤 요청도 상한을 통과하지 못함 |
| `retrievers` 가 비지 않음 | 아무것도 찾지 못하는 배포가 오류 없이 존재 |
| `candidate_depth ≥ retrieval_max_top_k` | 큰 `top_k` 요청에서 후보가 모자라 뒷자리가 빔 |
| `rerank_candidates ≥ retrieval_max_top_k` | 응답 뒷자리가 리랭킹되지 않은 채 채워짐 |
| 리랭커 입력 창 ≥ `retrieval_max_query_chars + chunk_size` | 토크나이저가 뒤에서부터 조용히 자름 — 오류 없이 점수만 틀어짐 |

등록되지 않은 retriever 이름, 표에 없는 임베딩·리랭커 모델도 같은 이유로 기동을 막습니다. **잘못된 구성으로 조용히 뜨는 것보다 뜨지 않는 쪽이 낫습니다** — 앞의 셋은 무한 루프나 빈 결과로 드러나기라도 하지만, 뒤의 셋은 `200` 을 내면서 순서만 나빠집니다.

### 오류 처리

- 도메인 예외(`core/exceptions.py`)를 라우터 밖에서 정의하고 전역 핸들러가 봉투로 변환합니다.
- **어댑터 경계에서 라이브러리 예외를 도메인 예외로 바꿉니다.** `sqlite3.OperationalError` 가 라우터까지 새면 계층 경계가 무의미해지고 내부 경로가 응답에 노출됩니다.
- **요청의 성패를 정하는 경로에서는 `except Exception` 으로 뭉치지 않습니다.** 광범위 캐치는 축소 동작의 경계에만 둡니다 — 캐시 조회·저장(실패 = 미스), 선택 retriever(실패 = 제외), 리랭킹(실패 = 융합 순서), 기동 정리(실패 = 기동 계속). 거기서도 `asyncio.CancelledError` 는 `BaseException` 이라 축소에 걸리지 않습니다 — 취소는 실패가 아닙니다.

---

## 4. RAG 파이프라인 설계

### 전체 흐름

```
[Ingestion]  POST /documents
  파싱 → 청킹 → 토큰 가드 → 배치 임베딩 → 벡터·어휘 두 색인에 쓰기
    → 레지스트리 커밋 ← 이 순간 교체가 확정된다
    → 이전 세대 청크 정리 → 캐시 무효화

[Query]  POST /qa
  질의 검증(문자 + 토큰 두 겹)
    → [L1 정확 매치] ─ Hit ─→ 검색도 하지 않고 재생
         ↓ Miss
    → [후보 수 확인] ─ 0이면 임베딩 자체를 건너뜀
         ↓
    → 질의 임베딩 1회 → [L2 유사 매치] ─ Hit ─→ 현재성 재검증 → 재생
         ↓ Miss
    → 대상 집합(레지스트리) → 밀집·어휘 팬아웃 (asyncio.gather)
         ↓
    → RRF 융합 → 채택(게이트 OR) → 크로스인코더 리랭킹 → 현재성 재검증
         ↓
    → 프롬프트 조립 → codex app-server 턴 하나 → 델타 스트리밍
         ↓
    → 인용 마커 검증 → 캐시 저장 → done
```

### 청킹 — 재귀 분할, 600자 / 겹침 100자

구분자 우선순위로 경계를 고릅니다: 문단 → 줄 → **한국어 종결어미** → 문장부호 → 공백 → 강제. 한국어 문서는 마침표 없이 줄바꿈으로 문장을 끊는 경우가 흔해, 영어용 분리자만으로는 문장 중간에서 잘립니다.

**600자의 근거는 둘입니다.** (a) 출처 표기의 단위이므로 "이 문단에서 왔다"고 말할 수 있을 만큼 작아야 합니다 — 문서 한 쪽을 통째로 인용하면 출처의 의미가 없습니다. (b) 임베딩 입력 창(512토큰) 안에 여유 있게 들어가야 합니다. 한국어 600자는 대략 400~450토큰입니다.

**청크는 페이지 경계를 넘지 않습니다.** 넘으면 인용의 `page` 가 무엇을 가리키는지 말할 수 없게 됩니다.

**크기가 문자 기준인 것은 `core/` 가 임베딩 라이브러리를 알지 않기 위해서**이고, 그래서 토큰 수를 보장하지 못합니다. 그 구멍을 **토큰 가드**가 막습니다 — 수집 시 청크마다 실제 토큰 수를 재고, 상한을 넘으면 문자 크기를 반씩 줄여가며 다시 쪼갭니다. 재분할로도 못 맞추면 실패시키지 않고 **경고를 남기고 진행합니다** — 병적인 텍스트 한 조각이 문서 전체의 수집을 막으면 안 되고, 조용히 잘리는 것과 달리 이건 로그에 남습니다.

### 대상 집합 필터는 질의 **이전에** 건다

저장소에는 **검색되면 안 되는 청크가 남아 있을 수 있습니다** — 되돌리기가 닿지 못한 실패, 확정 뒤의 이전 세대 정리 실패. 그 잔여물이 무해하다는 근거가 이 필터 하나라, 필터는 최적화가 아니라 **지켜야 할 계약**입니다.

검색 서비스가 레지스트리에서 `(document_id, revision, index_signature)` 삼중항 목록을 만들어 질의와 함께 넘깁니다.

| 기각한 대안 | 왜 |
|---|---|
| 사후 필터 | 걸러낸 만큼 결과가 줄어 `top_k` 가 의미를 잃고, 잔여물이 많으면 **결과가 통째로 빌 수 있습니다** |
| `index_signature` 만으로 | 리비전 축이 빠집니다 — **사용자가 지운 문장이 인용됩니다** |
| 필터 없이 전체 검색 | 수집이 잔여물을 허용한 전제가 거짓이 됩니다 |

**빈 목록은 "대상 없음"이지 "조건 없음"이 아닙니다.** 저장소 필터 API에서 "조건 없음 = 전체"가 흔한 관습이라 실수하기 쉬운 자리인데, 여기서 뒤집히면 유효 문서가 하나도 없는 상태에서 잔여 청크만 전체 탐색해 **사용자가 지운 문서가 검색됩니다.**

필터는 조건 **하나**입니다 — 세 축을 합친 `version_key` 를 메타데이터에 함께 저장하고 `{"$in": [...]}` 로 좁힙니다. 축마다 조건을 만들면 조건 노드 수가 문서 수의 세 배가 되고, 그 표현식이 질의마다 조립·직렬화·서버 평가됩니다.

그래서 `version_key` 가 없는 청크는 **어떤 필터에도 걸리지 않아 조용히 검색에서 사라집니다.** 이전 세대 형태로 저장된 청크가 그 상태인데, 기동 정리가 현재 형태로 보정하고(`backfill_version_keys`) 보정하지 못하면 경고를 남깁니다 — 보정 실패를 조용히 두면 목록에는 있는데 검색되지 않는 문서가 생깁니다.

### K와 후보 깊이 — 네 수가 서로를 떠받친다

| 값 | 기본값 | 무엇을 정하는가 |
|---|---:|---|
| `retrieval_top_k` | 5 | 응답에 담기는 청크 수. 요청이 덮어씁니다 |
| `retrieval_max_top_k` | 20 | 요청이 지정할 수 있는 상한. 없으면 `top_k=1000000` 이 저장소를 다 긁습니다 |
| `candidate_depth` (retriever별) | 100 | 각 retriever 가 융합에 실어 보내는 후보 수 |
| `rerank_candidates` | 30 | 리랭커가 다시 읽는 융합 상위 후보 수 |

**상한이 후보 깊이에 가까워질수록 한 retriever 가 자리를 다 채웁니다** — 융합이 교정할 여지가 후보 깊이와 K 상한의 간격에서 나오기 때문입니다. 그래서 세 값의 대소 관계를 기동 시점에 검사합니다(3절).

질의 자체에도 상한이 둘입니다 — 문자 `retrieval_max_query_chars`(1000)와 임베더 입력 창(512 토큰)입니다. 문자 상한이 막는 것은 절단이 아니라 **임의 길이 입력이 토크나이저에 들어가는 것**이고, 절단은 토큰 가드가 따로 막습니다. 문자 수로 절단까지 막으려 하면 상한이 101자쯤으로 내려갑니다.

어휘 색인은 요청받은 **후보 깊이의 10배**(기본 구성에서 100 × 10 = 1000줄)를 읽은 뒤 파이썬에서 자릅니다. SQL 이 깊이만큼만 읽으면 **경계의 동점 항목 중 어느 것이 남을지 정해지지 않아** 같은 질의가 실행마다 다른 결과를 낼 수 있기 때문입니다. 배수를 무한정 키우지 않는 것은 메모리 때문입니다.

### 하이브리드 검색 — 무엇을 실제로 구제하는가

밀집(코사인)과 어휘(BM25)를 `asyncio.gather` 로 겹쳐 돌립니다. 둘 다 블로킹을 스레드로 내보내므로 **지연이 합이 아니라 최댓값에 가깝습니다.**

`sample-docs/` 두 건에 배포 하한(`0.82`)으로 실측한 결과입니다.

| 질의 | 밀집 1위 코사인 | 밀집 단독 | 하이브리드 |
|---|---:|---:|---:|
| `P3` | 0.7969 | **0건** | 1건 |
| `P4` | 0.8015 | **0건** | 1건 |
| `hotfix` | 0.8001 | **0건** | 1건 |
| `develop` | 0.8102 | **0건** | 2건 |
| `release` | 0.8288 | 1건 | 2건 |

**문서에 그 문자열이 그대로 있는데도 밀집 단독은 빈 목록을 냅니다.** 오류가 아니라 근거 0건이라 답변 생성이 `no_evidence` 로 마감하고, 사용자에게는 문서에 있는 사실이 "없다"고 보입니다 — **로그에도 남지 않는 종류의 실패**입니다. 이 표가 하이브리드를 만든 이유이고, 그중 `P3`·`hotfix`·`develop` 세 줄은 회귀 테스트(`tests/test_retrieval_quality.py`)가 붙잡습니다.

동시에 **구제가 보편적이지 않다는 것**도 이 표가 보여 줍니다 — 하한을 넘는 식별자는 밀집도 찾습니다. 갈리는 지점이 하한 `0.82` 라, 하한을 조정하면 이 표를 통째로 다시 재야 합니다.

**가중치는 둘 다 1.0입니다.** `0.5` / `1.0` / `2.0` 으로 바꿔 여덟 질의를 다시 쟀는데 **판정이 세 값에서 모두 같았습니다.** 코퍼스가 작아 두 목록이 대부분 같은 청크를 같은 순서로 꼽고, 그러면 가중치는 분자와 분모에 함께 들어가 상쇄됩니다. **관측되지 않는 차이를 근거로 값을 고르지 않습니다.**

### 융합은 RRF — 점수가 아니라 순위만 쓴다

```
score(d) = Σ_r  w_r / (k + rank_r(d))     k = 60
```

코사인(`[0,1]`)과 BM25(무계 실수)는 척도가 달라 정규화 없이 더할 수 없고, min-max 정규화는 결과 집합에 의존해 같은 문서가 질의마다 다른 값을 받습니다. **순위는 척도가 없어 그 문제가 없습니다.**

결정성을 세 곳에서 고정했습니다.

- **소스를 이름 정렬 순으로 더합니다** — 부동소수점 덧셈에 결합법칙이 없어, 전달 순서대로 더하면 같은 목록 집합이 순서에 따라 마지막 비트가 다른 점수를 냅니다.
- 분모를 `Σ(w/(k+1))` 로 **항 형태까지 맞춥니다** — `(Σw)/(k+1)` 로 접으면 만장일치 1위가 1 ulp 어긋나 `1.0` 에 못 미칩니다.
- 동점 정렬 키가 `(-score, document_id, chunk_index)` 입니다 — 정체성까지 비교해야 딕셔너리 삽입 순서가 순위에 닿지 않습니다.

`contributions` 로 "어느 목록이 몇 위로 올렸고 원점수가 얼마였는가"를 응답에 싣습니다. 융합 뒤의 점수만으로는 "왜 3위인가"에 답할 수 없습니다.

### 하한은 융합 **앞**, 자기 척도로, 자르지 않고 표시로

각 retriever가 자기 단위의 하한을 융합 **전에** 판정합니다(밀집은 코사인 `0.82`, 어휘는 희소도 `0.3`). 융합 뒤의 RRF 점수는 관련성의 크기가 아니라 합의의 정도라 **거기에 임계값을 걸면 뜻이 성립하지 않습니다.**

**그런데 판정과 채택이 다릅니다.** 하한을 넘지 못한 청크도 목록에는 남고, 판정 결과만 플래그로 실려 융합을 지납니다. 채택은 융합 뒤에 한 번, **"자신을 실어 보낸 목록 중 하나라도 하한을 통과했는가"** 로 정해집니다.

게이트가 목록을 **자르던** 동안 두 목록의 교집합은 두 게이트의 교집합으로 좁아져 있었습니다. 순위만 쓰는 융합에서 합의 보너스는 같은 항목이 두 목록에 **모두 등장할 때만** 발동하는데 그 사건이 거의 일어나지 않았고, 그래서 희소 토큰 하나로 우연히 걸린 단독 항목이 코사인 0.85로 통과한 단독 항목과 **같은 융합 점수**를 받았습니다 — 융합이 교정할 것을 애초에 보지 못하는 상태였습니다.

**하한 `0.82` 는 계측값입니다.** 관련 질의 4개·무관 질의 3개의 코사인 분포를 두 청크 구성에서 실제로 쟀습니다.

| 청크 크기 | 관련 질의 1위 | 무관 질의 1위 |
|---|---|---|
| 600 (기본값, 청크 3개) | 0.8511 ~ 0.8710 | 0.8064 ~ 0.8134 |
| 200 (품질 테스트, 청크 10개) | 0.8775 ~ 0.9002 | 0.7907 ~ 0.8019 |

**`0.82` 는 두 구성 모두에서 무관 질의가 전부 0이 되는 가장 낮은 값입니다.** 초안이던 `0.75` 는 아무것도 거르지 못합니다 — e5 계열은 서로 무관한 한국어 문장 사이에서도 0.75~0.81이 흔합니다. **감으로 적은 상수가 거의 확실히 틀리는 자리**라 재서 정했습니다.

표본이 작다는 사실도 함께 적습니다(문서 2개·질의 7개). 완화책은 셋입니다 — 스펙의 단언을 절대값이 아니라 **상대 관계**로 두었고, 하한이 설정 항목이라 재배포 없이 바꿀 수 있으며, 무엇보다 **하한이 유일한 방어선이 아닙니다**(프롬프트가 뒤에 있습니다).

### 리랭킹 — 얹는 단계이고, 실제로 산 것은 3건이다

융합과 현재성 재검증 **사이**에 크로스인코더 한 단계가 들어갑니다. 재검증 뒤에 두면 재검증과 결과 조립 사이에 모델 순전파 한 번이 통째로 끼어들고, 그만큼 넓어진 창으로 새어 나간 리비전은 **어떤 무효화에도 걸리지 않는** 캐시 항목이 됩니다.

골든셋(보험 요약집, 근거 있는 44문항)으로 실측했습니다.

| | @1 | @3 | @5 | @20 | MRR |
|---|---:|---:|---:|---:|---:|
| 융합만 | 27 | 40 | 41 | 44 | 0.761 |
| + 크로스인코더 | 36 | 44 | 44 | 44 | 0.905 |

**이 표를 성과로 읽으면 안 되는 이유를 함께 적습니다.**

- **`@20` 이 양쪽 다 44/44입니다.** 융합이 이미 근거를 전부 상위 20에 넣어 두었고 리랭킹은 재배열만 했습니다. 생성기가 보는 것은 상위 K(기본 5)라, **실제로 산 것은 상위 5 밖에 있다가 안으로 들어온 3건**이지 `@1` 의 +9가 아닙니다.
- 순위가 움직인 19건 중 14건 개선·5건 악화라 부호검정 **`p ≈ 0.064`** 입니다. 문서 1건·44문항으로는 우연을 배제하지 못합니다.
- **평가셋이 "리랭킹의 자리"로 지정한 `distractor-dense` 두 문항이 둘 다 나빠졌습니다.** 이긴 자리는 `semantic-paraphrase` 였는데 그것은 원래 밀집 임베딩의 몫입니다.

답변 채점에서는 기준 답변과 일치가 **45 → 49**, 거절이 8 → 6이었습니다. **순위 지표가 크게 움직인 데 비해 답변은 조금 움직였습니다** — 프롬프트는 상위 다섯 개를 다 읽으므로 상위 5 **안에서** 일어난 재배열은 답변에 닿지 않기 때문입니다.

**분모가 구성마다 다릅니다 — 45/47 과 49/50 입니다.** 검색 게이트가 구성마다 따로 서기 때문입니다(`test_golden_judge.py`). 근거가 오지 않은 문항을 생성 실패로 세면 두 지표가 함께 움직여 어느 단계가 무너졌는지 알 수 없게 되므로, 게이트를 통과한 문항만 채점합니다. 위 순위표의 `@5`(융합 41 · 리랭킹 44)에 근거없음이 정답인 6문항을 더한 값이 그대로 분모입니다. 두 구성의 교집합으로 좁히지 않는 이유는 **리랭킹이 근거를 상위 K 안으로 끌어올린 문항이 통째로 채점에서 빠져, 이 단계가 실제로 값을 만든 자리를 측정이 지우기** 때문입니다. 그래서 분모의 차이(47 → 50) 자체를 결과로 읽습니다.

> 리랭킹의 값어치를 순위 지표가 아니라 여기서 찾는 편이 정직합니다: **답을 더 잘 쓰게 만든 것이 아니라, 답을 쓸 근거가 애초에 도착하게 만들었습니다.**

**리랭킹 점수에는 하한을 걸지 않았습니다.** 리랭킹 점수는 질의마다 범위가 달라, 44건의 분포를 합쳐도 질의 간에 공통으로 성립하는 경계선이 생기지 않습니다. 막는 것은 라벨의 부재가 아니라 **척도**입니다. 설정 항목조차 만들지 않았습니다 — 기본값 0짜리 미측정 임계값이 코드에 있으면 다음 사람이 그것을 켤 근거가 있다고 읽습니다.

**제한 시간은 완주 보장이 아니라 지연 예산입니다.** 15초(실측 최악값 5.13초의 세 배)를 넘기면 융합 순서로 축소되고, 응답의 `ordered_by` / `reranker` 가 그 사실을 밝힙니다. 설정 오타나 모델 로딩 실패로 리랭커가 빠진 배포와 정상 배포는 **둘 다 `200` 을 내고 둘 다 그럴듯한 순서를 돌려줍니다** — 응답이 말해 주지 않으면 구별할 방법이 없습니다.

### 현재성 재검증 — 기준 시점은 결과 조립 시점

레지스트리 읽기와 저장소 질의 사이가 잠겨 있지 않아 창이 생깁니다. 검색이 끝나고 결과를 조립하기 직전에 상위 K의 문서가 **지금도 그 세대인지** 다시 확인하고, 어긋난 것은 버린 뒤 다음 창을 열어 K를 채웁니다.

잠금을 쓰지 않은 이유는 단일 프로세스 배포에서 잠금이 과설계이기 때문이고, **그 창이 넓으면 밀리초짜리 읽기 어긋남이 무기한 캐시 오염으로 증폭되기 때문에** 좁히는 쪽을 택했습니다. 판정은 `Document.is_up_to_date` 가 하며 캐시의 재검증과 **같은 함수**입니다 — 한쪽만 고쳐졌을 때 각자 자기 기준으로는 옳은 채 어긋나는 자리가 열리지 않습니다.

### 수집 — "새로 쓰고 → 커밋 → 지우기"

```
extract → chunk → token guard → embed+store(배치) → registry.commit  ← 여기서 교체 확정
                                                    → 이전 세대 청크 정리
                                                    → 캐시 무효화
```

뒤집으면 중간 실패에 문서가 **통째로 사라집니다.** 실패 시 새 리비전 청크만 같은 요청 안에서 되돌리고, 되돌리기가 실패해도 다음 기동의 정리가 회수합니다. 되돌리기를 색인마다 따로 시도하는 이유는, 한 `try` 로 묶으면 벡터 되돌리기 실패 시 어휘 쪽이 손도 못 댄 채 남기 때문입니다.

- **배치 임베딩** — 메모리가 문서 크기가 아니라 배치 크기에 비례하고, 배치 경계마다 이벤트 루프가 다른 요청으로 넘어갑니다.
- **같은 문서는 직렬화, 다른 문서는 병렬** — 문서 단위 잠금과 총량 상한(`CapacityLimiter`)이 각각 다른 것을 책임집니다.
- **`unchanged` 단축의 조건은 둘 다** — 내용과 색인 구성이 모두 같을 때만 임베딩을 건너뜁니다.
- **`unchanged` 요청은 상한을 점유하지 않습니다** — 조회만 하고 끝나는 요청이 자리를 잡으면 처리량이 이유 없이 떨어집니다.

### 환각 억제 — 검색이 하는 일과 하지 않는 일

| 판정 | 주체 | 근거 |
|---|---|---|
| 이 청크를 다음 단계에 **보여줄 가치가 있는가** | 검색 | 점수만 보면 답할 수 있습니다 |
| 이 근거로 **답할 수 있는가** | 답변 생성 | **본문을 읽어야** 답할 수 있습니다 |

**검색은 근거 없음을 통보하지 않습니다.** 빈 목록으로 드러낼 뿐이고 거절 문구를 만들지 않습니다. 검색이 거절 문구를 만들면 답변 생성의 프롬프트가 그 판정을 뒤집을 수 없게 되고, 과제의 "하드코딩된 응답" 경계에도 닿습니다.

임계값 하나에 환각 억제를 몰아주지 않는 이유는 둘입니다. 임계값은 **의미가 아니라 벡터 거리**를 봅니다 — 질문의 어휘가 문서와 겹치면 답이 없어도 점수가 높습니다("연차를 팔 수 있나요?"는 연차 항목과 가깝습니다). 그리고 임계값이 유일한 방어선이면 모델이나 도메인이 바뀌어 점수 분포가 이동하는 순간 억제 전체가 함께 무너집니다.

나머지 방어선은 [`PROMPT_DESIGN.md`](./PROMPT_DESIGN.md)에 있습니다.

### 넣지 않은 것 — 검색 결과 캐시 (묘비)

설계까지 갔다가 뺐습니다. 프로세스 안 LRU로 융합 결과를 들고 키를 `(질의, 대상 집합 지문, 검색 구성 서명)` 으로 잡는 구성이었습니다.

**빼는 근거는 그 설계 자체에 있었습니다 — "캐시를 꺼도 결과가 같다."** 이 계층이 다루는 것은 지연이지 정확성이 아닙니다. 반면 과제가 요구하는 캐싱은 LLM 응답 캐시이고 그것은 **결과를 바꿉니다.** 남은 시간을 결과가 달라지지 않는 쪽에 쓰지 않았습니다.

대가는 반복 질의도 매번 두 저장소를 친다는 것입니다. 지연이 실제로 문제가 되면 되살리면 됩니다 — **적어 두지 않으면 다음 사람이 빠진 것으로 읽고 다시 설계합니다.**

질의 확장·HyDE·LLM-as-reranker도 넣지 않았습니다.

---

## 5. 캐싱 전략

### 요청 경로의 어디에 있는가

```
prepare()   L1 정확 매치 → (미스면) 후보 수 확인 → 질의 임베딩 → L2 유사 매치 → 현재성 재검증
            히트면 검색도 하지 않는다
stream()    히트: sources(캐시된 근거) → answer(조각 하나) → done
            미스: 평소와 같고, done 직전에 저장
```

- **조회가 검색보다 앞**이라 히트가 생성 비용만이 아니라 **검색까지** 아낍니다.
- **저장이 `done` 직전**이라 클라이언트가 마지막 프레임 직전에 끊어도 이미 저장돼 있습니다.
- **히트는 동시 생성 상한을 잡지 않습니다.** 그 상한이 존재하는 이유는 "생성 하나가 프로세스 하나"이기 때문인데 **히트에는 프로세스가 없습니다.** 잡으면 히트가 미스 뒤에 줄을 서서 캐시가 지연을 줄이지 못합니다.
- **히트 본문은 조각 하나입니다.** 저장해 둔 경계를 재생하는 것은 없던 진행을 연출하는 일입니다. 불변식(`answer` 들을 이어 붙이면 `done.answer`)은 그대로 성립합니다.

### 키가 되는 여섯 재료

```
fingerprint = sha256(canonical_json({
    query:            normalize_query(검색에 실제로 쓰인 질의),
    top_k:            effective_k,
    prompt_version:   PROMPT_VERSION,
    index_signature:  index_signature,
    model:            qa_llm_model,
    rerank_signature: 모델@리비전/점수규약  또는  ""
}))
```

**`effective_k` 와 `index_signature` 를 `QaService` 가 다시 유도하지 않는 것**이 배선의 요점입니다 — 둘 다 `RetrievalService` 에게 물어봅니다. 캐시가 서명을 따로 받으면 **세 번째 유도 지점**이 생기고, 어긋났을 때의 증상이 조용합니다: 히트율이 0이 되거나, 더 나쁘게는 **옛 세대의 답변이 현재 세대의 답으로 나갑니다.**

`rerank_signature` 가 필요한 이유는 리랭킹이 **상위 K에 어떤 청크가 들어오는지**를 바꾸기 때문입니다. 키가 그것을 모르면 리랭커 없이 만든 답변이 켜진 배포에서 그대로 나갑니다.

**이때 응답이 자기모순에 빠지지는 않습니다.** 캐시 항목은 인용이 아니라 **근거 전부**(`CachedAnswer.sources`)를 들고 저장되고 히트는 그것을 그대로 재생하므로, 답변과 `sources` 는 언제나 같은 세대의 짝입니다. 실패의 모양은 **고착**입니다 — 리랭커를 켰는데 답이 이전 구성의 것 그대로이고, TTL이 지나기 전까지 그렇습니다. 증상이 "켰는데 아무 변화가 없다"라서 프롬프트 버전을 올리지 않았을 때와 같은 종류의 조용한 실패이고, 하필 **튜닝의 동기가 된 질문일수록 캐시에 있을 확률이 높아** 검증하려는 표본이 먼저 관측에서 빠집니다.

리랭커를 색인 서명에 **합치지 않은** 이유는 수명이 다르기 때문입니다 — 합치면 리랭커 토글 한 번이 전 문서 재업로드를 요구하게 됩니다.

키가 **해시**인 이유도 둘입니다. 값 경계가 모호한 연결(`q + ":" + str(k)`)은 서로 다른 구성이 같은 키를 받는 자리를 남기고, 키가 질의를 그대로 담으면 `redis-cli KEYS *` 와 로그에 **질문이 복원 가능한 형태로** 노출됩니다.


### L1과 L2를 나눈 이유

- L1만 쓰면 `"연차는 어떻게 발생하나요?"` 와 `"연차가 어떻게 생기나요?"` 가 다른 항목이 되어 LLM이 두 번 호출됩니다.
- L2만 쓰면 동일 질문에도 임베딩과 후보 스캔 비용이 매번 듭니다.
- **L1이 성공하면 임베딩을 만들지 않습니다.** 앞 층이 더 쌉니다.
- **L1 미스여도 후보 집합이 비어 있으면(`VCARD == 0`) 임베딩을 건너뜁니다.** 캐시가 빈 상태(평가자의 첫 실행, 대부분의 테스트)에서 임베딩이 한 번만 돕니다.
- **L1 미스 후 한 번 계산한 임베딩이 L2 조회·L2 저장·밀집 검색 세 곳에서 재사용됩니다.**

`normalize_query` 가 하는 것은 넷뿐입니다 — NFC, 앞뒤 공백 제거, 연속 공백 축약, `casefold`. 어간 추출이나 조사 제거를 넣지 않는 이유는 **정확 매치 층이 "글자가 같다"를 판정하는 층**이기 때문입니다. 이 층이 의미를 판정하기 시작하면 유사 매치의 임계값이 무엇을 뜻하는지 아무도 말할 수 없게 됩니다.

### 임계값 `0.93` — 계측과 그 한계

**이 값은 "얼마나 아끼는가"가 아니라 "얼마나 틀려도 되는가"를 정합니다.** `retrieval_min_score`(0.82)를 그대로 가져올 수 없는 이유가 여기 있습니다 — e5 계열은 무관한 문장끼리도 0.8대에 앉아, 검색용 하한을 쓰면 **서로 다른 질문이 전부 히트**가 됩니다.

| 그룹 | 범위 | `0.93` 에서 |
|---|---|---|
| 같은 뜻, 다른 표현 (7쌍) | 0.8844 ~ 0.9752 | 넷 히트 / 셋 미스 |
| 서로 다른 질문 (5쌍) | 0.7465 ~ 0.8326 | 전부 미스 |
| **부정문 쌍** (4쌍) | **0.9066 ~ 0.9979** | **셋 히트** |

앞의 두 분포는 겹치지 않습니다(0.8326 < 0.8844). 그 사이(0.84~0.88)에 두면 같은 뜻 쌍을 전부 잡을 수 있는데도 **`0.93` 을 고른 것은 여유를 다른 질문 쪽에 몰아 두기 위해서입니다.** 두 실패의 비용이 비대칭이기 때문입니다 — **오탐은 틀린 답이고, 미탐은 그냥 느린 정답입니다.**

### 부정 극성 게이트 — 임계값으로는 막을 수 없는 실패

```
"코드 리뷰 없이 머지할 수 있나요?"  ↔  "코드 리뷰 없이 머지할 수 없나요?"     0.9979
"교육비 지원을 받을 수 있나요?"     ↔  "교육비 지원을 받을 수 없는 경우가?"   0.9773
```

묻는 바가 **정반대**인데 같은 뜻 쌍의 최댓값(0.9752)보다도 가깝습니다. 문장 임베딩이 부정을 사실상 인코딩하지 않는 **구조적 한계**라, 계측을 아무리 정교하게 해도 이 쌍은 임계값 위에 남습니다. 0.998 위로 올려 떨어뜨리면 진짜 같은 질문의 쌍이 먼저 전부 떨어져 **유사 매치 층이 통째로 꺼집니다.**

**척도가 다른 실패라 축이 하나 더 필요합니다.** 질의의 부정 극성을 항목의 속성으로 두고, 극성이 다른 항목은 **유사도를 재기 전에** 후보에서 뺍니다. 후보 집합의 이름을 `{scope}:{aff|neg}` 로 갈라 두면 **게이트가 저장 구조로 강제되어 조회 경로가 필터를 잊는 자리가 없습니다.**

**어떤 부정어인지는 구분하지 않습니다.** 세분할수록 `"안 쓰면"` 과 `"쓰지 않으면"` 처럼 같은 뜻의 두 질의가 갈려 히트를 잃는데, 그 손실은 **미스(다시 생성)** 인 반면 이 게이트가 막으려는 것은 **틀린 답**입니다. 두 오류의 비용이 다르므로 판정도 비대칭으로 둡니다.

`안` 은 낱말 전체로만 셉니다 — `안내`·`안전` 처럼 앞머리만 같은 낱말이 많아, 앞머리로 세면 긍정문이 부정으로 갈려 히트를 잃습니다.

### 후보는 최신순이 아니라 근접순으로 고른다

후보 상한을 "최신 N개"로 채우면 **그 상한이 곧 히트율의 천장**이 됩니다. 캐시가 상한보다 커지는 순간부터, 답이 이미 캐시에 있는 질문이 **그 답을 저장한 지 오래됐다는 이유만으로** 미스가 납니다 — 캐시가 클수록 못 맞히는 구조입니다.

그래서 후보 집합마다 Redis 8 **벡터셋**(`VADD`/`VSIM`)을 둡니다. `VSIM ... COUNT n` 이 가까운 것부터 돌려주고 전수 스캔이 사라집니다.

**임계값 판정은 여전히 우리 코드가 합니다.** `VSIM` 이 주는 점수는 코사인이 아니라 그것의 아핀 변환입니다 — 실측하면 코사인 `0.99388` 인 쌍에 `0.99694`(= `(1+cos)/2`)가 돌아옵니다. 이 값에 임계값을 걸면 **`0.93` 이 실제로는 코사인 `0.86` 을 뜻하게 되고**, 라이브러리가 매핑을 바꾸는 날 조용히 달라집니다. **순서만 저장소에서 받고 임계값은 우리가 보관한 float32로 잽니다** — 순서는 어떤 단조 매핑에서도 같으므로 이 분업이 성립합니다.

### 키 구조

```
name = "{scope}:{aff|neg}"

qa:v2:entry:{fp}      STRING      JSON 페이로드      TTL O
qa:v2:vec:{fp}        STRING      float32 packed     TTL O
qa:v2:vset:{name}     VECTOR SET  fp → 벡터          TTL X (지연 정리)
qa:v2:index:{name}    ZSET        fp → 단조 증가 seq  TTL X (지연 정리)
qa:v2:doc:{doc_id}    SET         fp 들               TTL O (갱신)
qa:v2:negative        SET         fp 들               TTL O (갱신)
qa:v2:scopes          SET         후보 집합 이름들     TTL O (갱신)
qa:v2:seq             STRING      INCR 카운터
```

**벡터를 페이로드에서 뗀 이유** — 합쳐 두면 후보 조회가 답변 본문과 청크 본문까지 전부 끌어옵니다(항목 하나가 3~5KB). 나누면 조회가 `VSIM`(fp 20개) + `MGET`(1.5KB × 20 ≒ 30KB)로 끝나고 **이긴 하나만** 페이로드를 읽습니다.

**나이는 시계가 아니라 `INCR` 카운터입니다.** 시계를 쓰면 같은 밀리초의 두 항목 순서가 정해지지 않아 "가장 최근 N개"가 흔들리고, 그것을 단언하는 테스트가 확률적이 됩니다.

**`top_k` 는 키에는 있고 scope에는 없습니다.** K로 후보 집합까지 쪼개면 같은 질문이 K마다 따로 생성됩니다 — `top_k=5` 로 캐시된 답이 `top_k=10` 요청에 쓸모없어지는데 둘은 **같은 질문**입니다. 대신 히트한 항목이 저장 시점의 K와 근거를 그대로 싣고 나갑니다.

**지연 정리** — 두 색인에 원소 단위 TTL이 없어 만료된 지문이 쌓입니다. 이것은 결함이 아니라 선택입니다: 후보 조회가 `MGET` 결과에서 `None` 을 만나면 그 지문을 `ZREM`/`VREM` 으로 함께 걷어냅니다. 스위퍼 프로세스가 늘지 않고, 용량 상한의 `ZREMRANGEBYRANK` 가 백스톱입니다.

### 낡은 답변을 두 겹으로 막는다

| 겹 | 언제 | 무엇을 잡는가 |
|---|---|---|
| 태그 무효화 | 쓰기 시점(수집·삭제), 레지스트리 커밋 **뒤** | 그 문서를 근거로 삼은 항목 |
| 현재성 재검증 | 읽기 시점(히트 판정 직후) | 무효화가 실패했거나 **경합으로 새어 나간** 항목 |

무효화가 **커밋 뒤**인 것이 계약입니다. 앞에서 부르면 실패해 롤백된 수집이 멀쩡한 캐시를 날립니다. 어댑터 안에 두지 않은 이유는 저장소 둘이 서로를 참조하게 되기 때문입니다 — 그러면 테스트에서 벡터 스토어를 대역으로 갈 때 **무효화가 함께 사라집니다.**

**긍정과 부정은 다른 사건에 반응합니다.**

| 종료 | 주장하는 것 | 무엇이 뒤집나 | 방어 |
|---|---|---|---|
| `stop` | **이 청크들이** 답을 담고 있다 | 그 청크가 바뀌는 것 | 문서 태그 + 재검증 |
| `insufficient_evidence` | **코퍼스 어디에도** 답이 없다 | 코퍼스에 내용이 더해지는 것 | 문서 태그 + 재검증 + 부정 집합 |
| `no_evidence` | **코퍼스 어디에도** 답이 없다 | 코퍼스에 내용이 더해지는 것 | **부정 집합뿐** |

부정 판정은 **없음에 대한 주장**이라 자기가 본 문서만으로는 반증되지 않습니다. `no_evidence` 항목은 근거가 0건이라 태그도 `source_versions` 도 비어 **문서 태그 무효화도 현재성 재검증도 닿지 못합니다.**

그래서 `unchanged` 를 뺀 모든 수집(`created`·`replaced`·`reindexed`)이 부정 집합을 비웁니다. 조건이 문서 **개수**가 아니라 **내용**인 것이 요점입니다 — 교체는 개수를 그대로 둔 채 내용을 바꾸고, 재색인은 하한을 넘는 청크의 집합을 바꿉니다.

**긍정은 코퍼스 변경으로 지우지 않습니다.** 새 문서에 더 나은 답이 있을 수는 있지만 캐시된 답변이 **틀린 것은 아닙니다.** 긍정까지 같은 규칙으로 지우면 업로드 한 번이 캐시 전체 비우기가 됩니다. **삭제는 부정 판정을 비우지 않습니다** — 내용을 지워서 없던 답이 생기지는 않습니다.

**재검증이 잡는 창이 구체적입니다.** 검색이 끝나고 생성이 도는 십수 초 사이에 문서가 바뀌면, 무효화는 *아직 존재하지 않는 항목*을 지우고 그 뒤에 낡은 항목이 저장됩니다. 그 항목은 이후 **어떤 무효화에도 걸리지 않습니다.** 재검증이 그 창을 응답 시점에 닫습니다.

### 장애가 지연을 늘리지 않게

**죽은 캐시는 "미스"가 아니라 "느린 미스"가 됩니다.** 조회 타임아웃이 없으면 매 요청이 연결 타임아웃만큼 멈추고, 그것도 L1·L2 두 번입니다. 지연을 줄이려고 넣은 층이 지연을 늘리는 것은 설계 실패라 두 장치를 답니다.

- **작업 타임아웃 0.2초** — 캐시는 최적화라 200ms 안에 답하지 못하면 도움이 되지 않습니다. 헬스 프로브의 2초와 값이 다른 것은 묻는 것이 다르기 때문입니다 — 프로브는 "살아 있나"를, 조회는 "지금 도움이 되나"를 묻습니다.
- **차단기** — 연속 3회 실패면 30초 동안 저장소를 **아예 부르지 않습니다.** 쿨다운 뒤 첫 요청이 탐침이 되어 회복에 재기동이 들지 않습니다. 상태는 프로세스 메모리입니다 — 공유하려면 저장소가 필요한데 **그 저장소가 지금 죽은 캐시**라 원리적으로 공유할 방법이 없습니다.

차단기가 세는 것은 **저장소 실패뿐**입니다. 재검증 중의 레지스트리 장애나 임베더 실패는 미스로 강등하되 차단기를 건드리지 않습니다 — 캐시가 죽지 않았는데 캐시를 끄면 회복만 늦어집니다.

어댑터는 로그를 남기지 않습니다. 죽은 캐시는 요청마다 실패하므로 어댑터가 매번 찍으면 **진짜 신호가 묻힙니다.** 가용/불가용 **전이 시점**의 관측만 위 계층에서 합니다.

### 기각한 대안

| 후보 | 기각 이유 |
|---|---|
| RediSearch VSS | 인덱스 스키마(차원·거리 함수)가 임베더 서명과 따로 관리되는 **두 번째 진실 원천**이 됩니다 |
| Chroma에 캐시 컬렉션 | **수명이 섞입니다** — TTL이 없어 만료를 직접 구현해야 하고, 문서 코퍼스와 캐시가 같은 백업·삭제 절차를 공유하게 됩니다 |
| 프로세스 메모리 | 워커가 둘이면 캐시가 둘, 재배포마다 소실, 무효화가 프로세스 경계를 넘지 못함 |
| 완전 탐색 | 처음에 골랐던 것 — 그러나 "훑을 N개"를 최신순으로 고르는 한 **히트율이 캐시 크기에 반비례**합니다 |
| **Redis 8 벡터셋 (채택)** | 후보 선택만 저장소에 맡기고 임계값 판정은 우리가 합니다. 스키마가 없어 두 번째 진실 원천이 생기지 않습니다 |

**Redis 장애를 인메모리로 폴백하지 않습니다.** 폴백은 **무효화를 프로세스 경계에서 끊습니다** — Redis가 죽은 동안 워커 A가 근거 없음 답변을 자기 메모리에 캐시하고, 그 뒤 워커 B로 새 문서가 업로드되면 무효화가 A에 닿지 못합니다. **장애 시의 정답은 폴백이 아니라 미스입니다.** 같은 이유로 `cache_enabled=false` 도 인메모리로 잇지 않습니다 — 운영자가 캐시를 끄는 이유는 대개 "캐시를 배제하고 원인을 보겠다"인데, 그 상태에서 히트가 나면 배제가 성립하지 않습니다.

---

## 6. 비동기 처리와 성능 최적화

| 지점 | 기법 | 효과 |
|---|---|---|
| 하이브리드 팬아웃 | `asyncio.gather` | 지연 = max(밀집, 어휘). 합이 아님 |
| 현재성 재검증 | `asyncio.gather` + 창 단위 | 문서 수만큼 동시 연결을 열지 않음 |
| 청크 임베딩 | 배치 `encode` | 개별 호출 대비 수 배, 메모리는 배치 크기에 비례 |
| Redis 저장 | 파이프라인 | 페이로드·벡터·두 색인·태그를 한 왕복에 (순번 채번과 용량 상한 걷어내기는 별도 왕복) |
| 파서 / 토큰 계산 | `asyncio.to_thread` | 큰 PDF 하나가 **헬스 응답까지 멈추지 않게** |
| 저장소 I/O | 어댑터 안 `to_thread` | 호출부가 매번 감싸지 않게 |
| 캐시 후보 스캔 | `VSIM` + `to_thread` 코사인 | 전수 스캔 제거, CPU 바운드는 루프 밖 |
| L1 히트 | 임베딩 계산 생략 | 앞 층이 더 쌈 |
| 빈 후보 집합 | `VCARD` 로 임베딩 생략 | 콜드 캐시에서 임베딩이 한 번만 돎 |

**상한이 있는 곳 셋:**

| 자원 | 상한 | 왜 |
|---|---|---|
| 동시 생성 | `qa_concurrency=2` | **생성 하나가 프로세스 하나**라 없으면 메모리가 동시 요청 수에 비례 |
| 세션 풀 | 같은 값 | 두 곳이 같은 수를 두 번 세지 않게 |
| 동시 수집 | `ingestion_concurrency=2` | 총량만 제한 — 같은 문서의 직렬화는 별도 잠금 |

**상한에 걸린 요청은 실패하지 않고 대기합니다.** `sources` 는 이미 나갔고 클라이언트에게는 느린 생성과 구분되지 않습니다.

**SSE 역압** — 서비스 스트림을 단일 펌프 태스크에서 돌리고 `maxsize=1` 큐로 옮깁니다. 조각마다 태스크를 만들면 `CapacityLimiter` 의 borrower와 반납 태스크가 갈려 반납 시점에 터집니다(실제로 밟은 버그입니다).

---

## 7. 알려진 트레이드오프와 개선 방향

인지하고 있지만 **의도적으로 이 범위에서 제외한 것들**입니다.

### 단일 워커 전제

문서 단위 잠금과 캐시 차단기 상태가 **프로세스 로컬**입니다. `docker-compose.yml` 이 `--workers 1` 을 명시하는 것이 이 전제의 선언입니다. 여러 워커로 올리려면 잠금은 Redis로, 차단기는 워커별로 두되 관측을 합치는 쪽이 맞습니다. 다만 차단기는 **원리적으로 공유가 불가능합니다** — 공유 저장소가 곧 죽은 캐시이기 때문입니다.

### 검색 구성이 캐시 키에 없다 (5절 참조)

`Retriever` 프로토콜에 `gate_signature` 를 더해 `RetrievalService.retrieval_signature` 를 만들면 닫힙니다. `rerank_signature` 가 이미 같은 패턴을 세워 두었습니다.

### `APP_QA_LLM_MODEL` 이 비면 키의 한 재료가 상수가 된다

빈 값은 "CLI 기본 모델"이라는 뜻인데 그 기본값이 CLI 업그레이드로 바뀌어도 키는 `""` 그대로입니다. 컨테이너에서 `model/list` 로 실물 이름을 확인했지만 **설정에 박지 않았습니다** — 어댑터가 그 값을 `thread/start` 에 넘기므로 적는 순간 관측이 **지시**가 되기 때문입니다. 모델을 명시하면 닫히고, 그 방법을 `README.md` 설정 절에 적어 두었습니다. 수명은 TTL 24h입니다.

### 첫 바이트까지의 시간에 리랭킹이 들어 있다

`prepare()` 가 검색과 리랭킹까지 끝낸 뒤 스트림이 열리므로, 리랭킹 순전파(웜 1.0~1.7초, 첫 요청 5.13초)가 첫 바이트 **앞**에 있습니다. 하트비트로 덮을 수 없습니다(스트림이 아직 안 열림).

리랭킹은 이미 실패해도 오류가 아니라 축소이므로 **상태 코드에 영향을 주지 않습니다.** 따라서 이 단계만 `stream()` 안으로 옮기면 계약을 건드리지 않고 TTFB를 줄일 수 있습니다. `prepare`/`stream` 경계의 근거(상태 코드 확정)는 그대로 유지됩니다.

### SQLite 동시성

레지스트리와 어휘 색인이 연산마다 새 연결을 엽니다(`sqlite3.Connection` 은 스레드 간 공유가 안전하지 않습니다). WAL 모드와 `busy_timeout` 을 걸지 않아, 수집 쓰기와 검색 읽기가 겹치면 기본 rollback journal에서 서로를 막습니다. `PRAGMA journal_mode=WAL` + `busy_timeout` 을 연결 시점에 거는 것이 가장 싼 개선입니다.

### 어휘 색인의 대상 필터가 조건 트리다

벡터 스토어는 합성 축 `version_key` 하나로 `$in` 을 쓰는데, 어휘 색인은 `(document_id=? AND revision=? AND index_signature=?) OR ...` 형태라 조건 노드 수가 문서 수의 세 배입니다. **같은 문제를 한쪽에만 적용한 비대칭**입니다. FTS5 테이블에 `version_key UNINDEXED` 컬럼을 더하고 `backfill` 경로로 보정하면 닫힙니다.

### 리랭커에 동시성 상한이 없다

`qa_concurrency` 는 생성기 프로세스에만 걸립니다. 크로스인코더는 기본 스레드풀에서 무제한으로 돌아, 동시 `/search` 가 몰리면 그 풀을 Chroma HTTP·SQLite·임베딩과 함께 나눠 씁니다. 전용 `CapacityLimiter` 와 `torch.set_num_threads` 고정이 필요합니다.

### 캐시 스탬피드

동일 질문이 동시에 도착하면 전부 미스가 나고 전부 생성합니다. `qa_concurrency` 가 피해를 묶지만 중복 생성 자체는 남습니다. `fingerprint → Future` 인플라이트 맵으로 닫히며, 단일 워커 전제 위에서 성립합니다.

---

## 8. 설계 판단 기준

프로젝트 전반에서 반복적으로 의식한 방향입니다.

1. **오류를 내지 않는 실패를 먼저 찾는다.** e5 접두사 누락, Chroma 거리 지표 기본값, 리랭커 미로딩, `version_key` 없는 청크, 부정문 캐시 오탐 — 전부 `200` 을 내면서 답만 틀리는 부류입니다. 이런 자리를 찾으면 **서명·응답 필드·경고 로그 중 하나로 반드시 드러냅니다.**

2. **감으로 적은 상수는 재서 정하거나, 못 쟀다고 적는다.** 하한 `0.75` 는 아무것도 거르지 못했고, 리랭커 타임아웃 `5.0` 은 실측 최악값 `5.13` 보다 작아 **첫 요청이 항상 축소되는 값을 출하 중**이었습니다. 반대로 재고도 바꾸지 않은 값(`rerank_candidates=30`)은 "낮춰서 사는 구간이 없다"를 주석에 남겼습니다.

3. **자기 기능에 불리한 숫자를 지우지 않는다.** 리랭킹의 `@20` 44/44, `p ≈ 0.064`, `distractor-dense` 악화는 전부 리랭킹을 깎는 사실이고 표 바로 아래에 있습니다. 지우면 다음 사람이 그 숫자를 다시 얻으려고 같은 시간을 씁니다.

4. **기각한 설계에 묘비를 남긴다.** 검색 결과 캐시, gte 리랭커, 완전 탐색, 인메모리 폴백 — **다음 사람을 막는 것은 채택 이유가 아니라 기각 이유입니다.** 적어 두지 않으면 빠진 것으로 읽고 다시 설계합니다.

5. **경계는 가능한 곳부터 도구로 강제한다.** `os.environ` 접근 지점, `core/` 의 서드파티 금지, 주석 규칙은 린트나 스크립트의 종료 코드입니다. 레이어 방향은 아직 도구가 없어 리뷰로 지킵니다(3절) — 문장으로만 적힌 규칙은 지켜지지 않는다는 원칙에 비추면 남은 빚입니다.

6. **두 곳에서 같은 값을 유도하지 않는다.** 색인 서명, K의 기본값 규칙, 현재성 판정, 게이트 통과 판정 — 유도 지점이 둘이 되는 순간 한쪽만 고쳐졌을 때 **각자 자기 기준으로는 옳은 채** 어긋납니다.

---

## 부록

- change 단위 제안서·설계 결정·요구사항·작업 분해, 그리고 **되돌린 결정의 기록**: [`openspec/`](./openspec/)
- 테스트가 무엇을 왜 단언하는가: [`tests/README.md`](./tests/README.md)
- 프롬프트 설계와 환각 억제: [`PROMPT_DESIGN.md`](./PROMPT_DESIGN.md)
