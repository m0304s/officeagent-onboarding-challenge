# ARCHITECTURE

> **이 문서는 현재 구현된 것만 기술합니다.** 계획 단계의 결정은 [`openspec/changes/`](./openspec/changes/)의 각 change `design.md`에 있고, 구현되는 시점에 이 문서로 옮겨집니다. 문서와 코드가 어긋나지 않게 하기 위한 규칙입니다.
>
> 현재 구현 범위: 서비스 기동, 헬스 리포팅, 레이어 구조, 어댑터 프로토콜, 설정 로딩, 오류 처리, 구조화 로깅, 테스트 하네스, 컨테이너 실행과 자격증명 주입.

## 기술 스택과 선택 이유

| 계층 | 선택 | 이유 | 현재 사용처 |
|------|------|------|------------|
| 언어 | Python 3.11+ | 과제 고정 조건 | 전체 |
| 웹 프레임워크 | FastAPI | 과제 고정 조건. SSE(`StreamingResponse`)와 async 파이프라인이 네이티브라 스트리밍 요구사항에 별도 계층이 필요 없다 | `api/` |
| LLM SDK | `claude-code-sdk` | 과제가 제시한 두 SDK 중 택 1. API 키 없이 구독으로 동작하고, 파이썬 네이티브라 subprocess 래핑 없이 async 스트리밍·타임아웃 제어가 가능하다. 대안인 Codex CLI는 Node CLI라 스트림 파싱과 프로세스 수명 관리를 직접 구현해야 한다 | **아직 없음** |
| 임베딩 | sentence-transformers (로컬) | 로컬 오픈소스 모델이라 테스트가 LLM 구독 없이 실행된다. 임베딩이 네트워크·과금에 묶이지 않아 검색 테스트를 실물로 돌릴 수 있다 | **아직 없음** |
| 벡터 DB | Chroma (임베디드 퍼시스턴트) | 별도 컨테이너 없이 볼륨 하나로 영속화되어 한 줄 실행이 단순해진다. 메타데이터 필터와 문서 단위 삭제를 지원해 캐시 무효화 연동이 가능하다 | 헬스 프로브 |
| 캐시 DB | Redis | 정확 매치는 키 조회, 유사 질문은 질문 임베딩 유사도로 판정. TTL·태그 기반 무효화가 자연스럽고 캐시 계층이 저장소 수준에서 분리된다 | 헬스 프로브 |
| 테스트 | pytest + httpx | ASGI 앱에 실제 네트워크 없이 직접 요청할 수 있다 | `tests/` |
| 린터 | ruff | 포매팅·린팅을 한 도구로 통일. 계층 경계도 린트 규칙으로 강제한다 | 전체 |

## LLM SDK 통합 방식

> **현재 구현된 것은 실행 환경까지입니다.** CLI 설치와 자격증명 주입 경로는 동작하지만, **컨테이너 안에서 CLI가 실제로 답변을 생성하는지는 아직 호출해 본 적이 없습니다.** SDK를 호출하는 코드는 존재하지 않습니다.

### 왜 이 change에서 다루는가

`claude-code-sdk`는 HTTP 클라이언트가 아니라 **로컬 CLI를 실행합니다.** 컨테이너 안에서 답변을 생성하려면 파이썬만으로 부족하고 Node 런타임·CLI 바이너리·인증 상태가 모두 필요합니다.

이걸 QA change까지 미루면 그 시점에 베이스 이미지와 설치 계층을 다시 짜야 하고, 최악의 경우 "컨테이너 안에서는 SDK가 동작하지 않는다"는 사실을 마감 직전에 발견하게 됩니다. 그래서 **설치와 주입 경로만** 먼저 뚫어두고 호출은 하지 않습니다.

### 자격증명을 이미지에 굽지 않는 이유

이미지에 넣으면 두 가지가 동시에 깨집니다. 자격증명이 빌드 산출물에 남고, 평가자 환경마다 인증 주체가 달라 이미지가 재사용 불가능해집니다. 그래서 **호스트에 이미 있는 것을 실행 시점에 파일로 마운트**합니다.

```
호스트 (macOS)                          컨테이너
  Keychain "Claude Code-credentials"
        │  security find-generic-password
        ▼
  .secrets/claude/.credentials.json ──rw──▶ /home/app/.claude/.credentials.json
        ▲                                             │
        └──── make up 할 때마다 재추출          CLI 가 만료 시 갱신 (사본에만 반영)
```

| 결정 | 이유 |
|------|------|
| 새 토큰을 발급하지 않는다 | `claude setup-token`은 컨테이너 친화적이지만 **재발급**입니다. 기존 인증 상태 재사용이 제약이라 쓰지 않습니다 |
| 환경변수가 아니라 파일로 넣는다 | access token 수명이 실측 약 8시간입니다. 토큰 하나를 박아두면 갱신 경로가 없어 그날 안에 죽습니다 |
| `:ro`가 아니라 rw로 마운트한다 | 읽기 전용이면 갱신에 실패해 같은 이유로 8시간 뒤 인증이 끊깁니다 |
| 기동할 때마다 재추출한다 | 컨테이너가 항상 호스트의 최신 상태로 시작하므로, 사본이 묵는 창이 컨테이너 수명으로 제한됩니다 |

macOS는 Keychain을 컨테이너와 공유할 수 없어 컨테이너가 가진 것은 **사본**입니다. 컨테이너가 갱신해도 호스트 Keychain으로 돌아가지 않습니다. 역방향 동기화는 호스트의 인증 저장소를 직접 쓰는 행위라 호스트 CLI를 망가뜨릴 위험이 더 커서 하지 않습니다.

### 부팅 경로에서 CLI를 실행하지 않는 이유

인증이 없거나 손상된 환경에서 서비스가 기동조차 못 하면 "실행 불가"가 됩니다. 그래서 인증 상태는 기동 조건이 아니며, 헬스도 LLM 제공자를 근거로 불가용을 보고하지 않습니다. LLM 어댑터는 도입될 때 **지연 초기화**로 붙어 첫 호출 시점에만 CLI를 건드립니다.

이 성질은 문서가 아니라 테스트로 고정돼 있습니다 — `tests/test_boot.py`가 자격증명이 **없는** 상태와 **판독 불가능한** 상태 양쪽에서 기동과 `/health` 200을 단언합니다. 누군가 부팅 경로에 자격증명 파싱을 끼워 넣으면 거기서 깨집니다.

### 컨테이너 구성

| 항목 | 값 | 이유 |
|------|-----|------|
| 워커 프로세스 | 1개 고정 | 임베디드 벡터 스토어가 파일 락을 씁니다. 늘리려면 벡터 스토어를 별도 서비스로 분리하는 결정이 먼저입니다 |
| 캐시 컨테이너 | 자체 헬스체크 + `depends_on: service_healthy` | 캐시가 늦게 뜨는 동안 API가 503을 내면 재시작 루프가 됩니다 |
| API 헬스체크 | `start_period: 30s` | 기동 유예 안의 실패는 재시도로 흡수됩니다 |
| 벡터 스토어 | 네임드 볼륨 | 호스트 UID와 무관하게 쓰기 가능합니다 |
| 실행 사용자 | 비루트 (`app`, UID 1000) | — |

**미검증 항목**은 [`openspec/changes/bootstrap-runtime-skeleton/design.md`](./openspec/changes/bootstrap-runtime-skeleton/design.md)의 Risks에 있습니다. 요약하면 세 가지입니다 — 컨테이너 안에서 CLI가 실제로 동작하는지, 마운트한 자격증명을 CLI가 읽는지, refresh token이 회전해 호스트를 로그아웃시키는지.

## 레이어 구조

```
src/app/
  api/          라우터. HTTP 관심사만 — 검증, 상태 코드, 응답 직렬화
    routes/health.py
    errors.py           도메인 예외 → HTTP 변환 (이 계층에서만)
    logging.py          JSON 포매터, 요청 단위 로그 미들웨어
  services/     유스케이스 오케스트레이션
    health.py           프로브를 모아 전체 상태를 판정
  core/         도메인. 프레임워크·외부 라이브러리를 import 하지 않는다
    models.py           상태 값 객체 (Status, ProbeResult, HealthReport)
    exceptions.py       도메인 예외 기반 클래스, ErrorCode
  adapters/     외부 경계
    protocols.py        HealthProbe 프로토콜
    cache/probe.py      캐시 저장소 프로브
    vector_store/probe.py  벡터 스토어 프로브
  config.py     환경변수 → 설정 객체 (환경을 읽는 유일한 곳)
  main.py       앱 팩토리, 의존성 배선
```

상위 레이어는 하위 레이어의 인터페이스만 알고 구현체는 모릅니다.

### 경계를 컨벤션이 아니라 도구로 강제한다

두 규칙을 ruff 린트로 막아둡니다. 문서에만 적힌 규칙은 어겨도 티가 안 나기 때문입니다.

| 규칙 | 강제 방식 | 확인 |
|------|----------|------|
| `core/`는 프레임워크·외부 라이브러리를 import 하지 않는다 | `src/app/core/.ruff.toml`의 `banned-api` (ruff가 파일별로 가장 가까운 설정을 적용) | `core/`에서 `import fastapi` → `TID251` |
| 환경변수는 `config.py`를 통해서만 읽는다 | `pyproject.toml`의 `banned-api` + `config.py`·`tests/`만 예외 | `services/`에서 `os.environ` → `TID251` |

### 어댑터는 프로토콜 뒤에 둔다 — 단, 지금 필요한 만큼만

현재 정의된 계약은 하나뿐입니다.

```python
class HealthProbe(Protocol):
    name: str

    async def check(self) -> ProbeResult: ...
```

`VectorStore`(upsert/query/delete)나 `CacheStore`(get/set/invalidate) 같은 넓은 인터페이스는 **의도적으로 정의하지 않았습니다.** 소비자가 없는 상태에서 정한 인터페이스는 실제 사용처가 생기는 순간 거의 반드시 틀린 것으로 드러나고, 그때는 이미 문서에 적혀 있어 고치는 데 설명이 붙습니다. 각 프로토콜은 그것을 실제로 쓰는 change에서 정의합니다.

이 좁은 경계가 테스트에서 값을 냅니다. 프로브가 프로토콜이므로 대역을 주입해 정상·불능·무응답·예외 네 상태를 결정론적으로 만들 수 있고, 스위트가 외부 서비스 없이 돌아갑니다.

## 헬스 리포팅

`GET /health`는 서비스 자신과 각 의존성의 상태를 함께 보고합니다.

```
정상        200  {"status":"ok",          "dependencies":{...}}
하나라도 불능 503  {"status":"unavailable", "dependencies":{...}}
```

**503일 때도 공통 오류 봉투를 쓰지 않습니다.** 헬스는 오류를 알리는 엔드포인트가 아니라 상태를 보고하는 엔드포인트입니다. 두 코드에서 본문 구조가 갈리면 소비자가 파서를 둘 들고 있어야 하고, "어느 의존성이 문제인가"가 오류 메시지 문자열 안으로 뭉개집니다. `dependencies`의 키 집합은 정상 여부와 무관하게 항상 동일해, 키가 사라지는 것과 값이 비정상인 것을 구분할 필요가 없습니다.

### 의존성마다 "도달 가능"의 의미가 다르다

| | 캐시 저장소 | 벡터 스토어 |
|---|---|---|
| 배치 | 별도 컨테이너 | 프로세스 내부 + 마운트된 경로 |
| 불능의 의미 | 네트워크 도달 불가, 인증 실패 | 볼륨 미마운트, 권한 없음, 디스크 꽉 참 |
| 점검 방법 | 연결 후 ping | 저장 경로에 **실제로 써 보고** 임베디드 클라이언트 heartbeat |

벡터 스토어 프로브가 진짜로 파일을 쓰는 이유가 있습니다. 클라이언트 객체가 만들어졌다는 사실만으로 정상 판정하면 볼륨이 안 붙은 채로 "정상"이 나오고, 그 상태로 문서 수집을 시작하면 첫 업로드에서 터집니다.

### 시간 상한과 실패 격리

- 프로브는 **병렬 실행**하고 **의존성별 상한**과 **전체 상한**을 함께 겁니다. 하나가 매달려도 나머지 결과는 보고됩니다 — 어느 의존성이 문제인지 식별하려면 그래야 합니다.
- 벡터 스토어 프로브는 파일 I/O라 블로킹이므로 **스레드풀로 오프로드**합니다. 이벤트 루프를 막지 않는다는 규약이 여기서 처음 적용됩니다.
- 프로브가 도달 불가가 아닌 **예상치 못한 예외**를 던져도 해당 프로브만 비정상으로 기록하고 나머지는 그대로 보고합니다. 프로브 하나의 버그가 헬스 엔드포인트 전체를 500으로 만들면, 정작 진단해야 할 때 아무것도 못 봅니다.
- 실패 사유 요약에는 접속 문자열·자격증명·스택 트레이스를 담지 않습니다. 원인 추적 정보는 로그로만 남깁니다.

헬스는 LLM 제공자를 의존성으로 보고하지 않습니다 — 이유는 [부팅 경로에서 CLI를 실행하지 않는 이유](#부팅-경로에서-cli를-실행하지-않는-이유)에 있습니다.

## 오류 처리

예외 → HTTP 변환은 **`api/` 계층에서만** 합니다. 어댑터가 던지는 라이브러리 예외가 라우터까지 그대로 새지 않도록, 각 어댑터는 자기 경계에서 도메인 예외로 바꿔 던지고 `api/errors.py`가 HTTP로 옮깁니다.

모든 오류가 같은 봉투를 씁니다.

```json
{ "error": { "code": "not_found", "message": "Not Found" } }
```

`code`는 `core/exceptions.py`의 `ErrorCode` 열거값입니다. 소비자가 메시지 문자열을 파싱하지 않고 분기할 수 있게 하는 것이 목적이라 값은 한 번 정하면 바꾸지 않고, 이후 change는 항목을 **추가**하는 식으로 확장합니다.

| 경로 | 상태 | code |
|------|-----:|------|
| 정의되지 않은 경로 | 404 | `not_found` |
| 지원하지 않는 메서드 | 405 | `method_not_allowed` |
| 요청 검증 실패 | 422 | `validation_error` (+ `fields`) |
| 도메인 예외 | 500 | 예외가 정한 code |
| 미처리 예외 | 500 | `internal_error` |

**프레임워크 기본 응답도 덮어씁니다.** 기본값을 그대로 두면 형식이 두 가지가 되어 소비자가 파서를 둘 들고 있어야 합니다.

검증 실패에는 어떤 입력이 문제인지 알 수 있도록 위치와 사유를 싣되, **입력 값 자체는 싣지 않습니다** — 자격증명이 본문에 실려 오면 그대로 되비치기 때문입니다.

**`/health`는 이 변환의 대상이 아닙니다.** 라우트가 응답을 직접 반환하므로 예외 핸들러를 타지 않고, 503일 때도 상태 보고 본문을 유지합니다.

## 로깅

로그는 JSON 한 줄로 냅니다. 포맷 문자열에 따옴표를 끼워 흉내내면 메시지에 따옴표나 줄바꿈이 섞이는 순간 깨진 JSON이 나오므로, 포매터에서 직렬화합니다.

ASGI 서버는 자기 로거에 평문 핸들러를 붙이고 propagate를 끄기 때문에, 그대로 두면 형식이 두 가지가 됩니다. 핸들러를 걷어내 루트로 흘리고, 서버의 액세스 로그는 끕니다 — 같은 요청이 두 줄로 찍히기 때문입니다.

`RequestLoggingMiddleware`가 요청마다 식별자를 붙이고 메서드·경로·상태 코드·소요 시간을 한 줄로 남기며, `x-request-id`를 응답 헤더로 돌려줍니다. 요청 헤더로 들어온 값이 있으면 그대로 유지합니다. 스트리밍 응답이나 LLM 호출을 디버깅하려면 "어느 요청이 얼마나 걸렸고 무엇을 반환했는가"가 먼저 있어야 하므로, 이후 change의 전제입니다.

스택 트레이스는 로그에만 남고 응답에는 실리지 않습니다.

## 설정

환경변수를 읽는 곳은 `config.py` 하나입니다. 어디서 무엇을 읽는지 흩어지면 문서화가 불가능해지기 때문입니다.

모든 항목에 기본값이 있어 아무것도 주지 않아도 기동됩니다. 반대로 값이 무효하면 **조용히 기본값으로 넘어가지 않고 기동에 실패**하며, 어느 항목이 왜 무효한지 드러납니다. pydantic의 검증 오류는 그대로 흘리지 않고 도메인 예외(`ConfigurationError`)로 바꿔 던집니다 — 프레임워크 예외가 상위 계층으로 새지 않게 하는 규약의 일부입니다.

## 의존성 배선

배선은 앱 팩토리에서 한 번만 합니다. 모듈 전역 싱글턴을 두지 않는 이유는 테스트에서 교체가 어려워지기 때문입니다.

```python
create_app(settings=None, probes=None) -> FastAPI
```

`settings`와 `probes`를 주입할 수 있어 테스트가 의존성 상태를 결정론적으로 구성합니다.

## 테스트 전략

| 대상 | 방식 |
|------|------|
| 헬스 시나리오 | 프로브 대역 주입 — 정상 / 캐시만 불능 / 벡터 스토어만 불능 / 둘 다 불능 / 무응답 / 한쪽만 무응답 / 프로브 예외 |
| 프로브 단위 | 실제 파일시스템(쓰기 가능·불가), 페이크 클라이언트(ping 성공·행), 도달 불가 주소 |
| 설정 | 기본값 기동, 무효값 기동 실패, 기본값 폴백 없음 |
| 오류 응답 | 404 / 405 / 422 / 도메인 예외 / 미처리 예외가 같은 봉투인지, 내부 정보가 새지 않는지, `/health`는 변환되지 않는지 |
| 로깅 | request-id 발급·유지, 액세스 로그의 메서드·경로·상태·소요시간 |
| 기동 | 자격증명 없음 / 판독 불가 양쪽에서 기동과 `/health` 200, 실제 프로브 배선이 외부 서비스 없이 응답 |
| 자격증명 동기화 | 스크립트를 실제로 실행 — 추출 성공 시 퍼미션(0600·0700), 자격증명이 없어도 0으로 종료, 재발급 명령 부재 |
| 하네스 | 픽스처 자체 검증 |

전체 스위트가 **외부 서비스와 자격증명 없이** 통과합니다. 실제 의존성과의 통합 확인은 사람이 1회 수행합니다.
