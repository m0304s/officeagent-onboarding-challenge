# ARCHITECTURE

> **이 문서는 현재 구현된 것만 기술합니다.** 계획 단계의 결정은 [`openspec/changes/`](./openspec/changes/)의 각 change `design.md`에 있고, 구현되는 시점에 이 문서로 옮겨집니다. 문서와 코드가 어긋나지 않게 하기 위한 규칙입니다.
>
> 현재 구현 범위: 서비스 기동, 헬스 리포팅, 레이어 구조, 어댑터 프로토콜, 설정 로딩, 오류 처리, 구조화 로깅, 테스트 하네스, 컨테이너 실행과 자격증명 주입, **문서 도메인 값 객체와 청킹**, **문서 파서(txt·md·pdf)**, **임베딩 어댑터**, **벡터 스토어와 문서 레지스트리**, **수집 파이프라인**, **문서 API(업로드·목록·상세·삭제)**, **검색 서비스와 검색 API(`POST /search`)**, **답변 생성과 SSE 스트리밍(`POST /qa`)**.
>
> 검색(retrieval)은 질의 검증부터 응답 조립까지 구현되어 있습니다 — `VectorStore.query`와 그 Chroma 구현(검색 대상 필터·거리→유사도 변환), 그 위의 `RetrievalService`(질의 임베딩 → 대상 집합 → 저장소 질의 → 현재성 재검증 → 하한), 그리고 `POST /search`가 모두 있습니다.
>
> 답변 생성도 프롬프트 조립부터 SSE 프레이밍까지 구현되어 있습니다 — `codex app-server` 세션 위의 `AnswerGenerator` 구현과 세션 풀, 프롬프트·출력 파싱 순수 함수, 재시도와 인용 검증을 맡는 `QaService`, 그리고 `POST /qa`가 모두 있습니다(아래 "답변 생성"). **캐싱은 아직 없습니다** — 응답에 `cache_hit` 자리를 미리 뚫어 두지도 않았습니다.

## 기술 스택과 선택 이유

| 계층 | 선택 | 이유 | 현재 사용처 |
|------|------|------|------------|
| 언어 | Python 3.11+ | 과제 고정 조건 | 전체 |
| 웹 프레임워크 | FastAPI | 과제 고정 조건. SSE(`StreamingResponse`)와 async 파이프라인이 네이티브라 스트리밍 요구사항에 별도 계층이 필요 없다 | `api/` |
| LLM SDK | **Codex SDK** (`@openai/codex`) | 과제가 제시한 두 SDK 중 택 1. API 키 없이 구독으로 동작한다. `claude-code-sdk`에서 **갈아탔다** — 결정적인 이유는 자격증명이 세 OS 모두 평문 파일이라 컨테이너가 볼륨으로 읽을 수 있다는 것이다 (아래 "LLM SDK 통합 방식") | `adapters/llm/`, `services/qa.py` |
| PDF 파서 | PyMuPDF | 규칙 기반 C 확장이라 모델 가중치 없이 동작해 "한 줄 실행"의 실패 지점이 늘지 않는다. **AGPL-3.0**이므로 상용 배포로 가려면 pypdf(BSD) 교체가 선행되어야 하며, 교체 범위는 `adapters/parsers/pdf.py` 한 파일이다 | `adapters/parsers/` |
| 임베딩 | sentence-transformers (로컬) — `intfloat/multilingual-e5-small` | 로컬 오픈소스 모델이라 테스트가 LLM 구독 없이 실행된다. 다국어·512토큰 창이라 한국어 청크가 잘리지 않는다 (아래 "임베딩") | `adapters/embedding/` |
| 벡터 DB | Chroma (**서버 모드**, 별도 컨테이너) | 메타데이터 필터와 문서 단위 삭제를 지원해 리비전 교체·캐시 무효화 연동이 가능하다. 저장소를 앱 프로세스 밖으로 뺐다 (아래 "임베디드에서 서버 모드로") | `adapters/vector_store/` |
| 캐시 DB | Redis | 정확 매치는 키 조회, 유사 질문은 질문 임베딩 유사도로 판정. TTL·태그 기반 무효화가 자연스럽고 캐시 계층이 저장소 수준에서 분리된다 | 헬스 프로브 |
| 테스트 | pytest + httpx | ASGI 앱에 실제 네트워크 없이 직접 요청할 수 있다 | `tests/` |
| 린터 | ruff | 포매팅·린팅을 한 도구로 통일. 계층 경계도 린트 규칙으로 강제한다 | 전체 |

## LLM SDK 통합 방식

> **CLI 설치·자격증명 주입 경로가 동작하고, 애플리케이션이 그 CLI를 실제로 호출합니다.** 어댑터는 `src/app/adapters/llm/`이고 소비자는 `POST /qa`입니다(아래 "답변 생성").
>
> 아래 수치는 **컨테이너 안에서 실물 CLI를 두 표면(`codex exec --json`·`codex app-server`)으로 돌려** 잰 것입니다 — 이벤트 형식·스트리밍 입자도·인증 실패 형태·소요 시간. 채택한 표면의 출력은 [`tests/fixtures/codex/`](./tests/fixtures/codex/)에 픽스처로 남아 파서 회귀를 고정합니다.

### 왜 이 change에서 다루는가

Codex SDK는 HTTP 클라이언트가 아니라 **로컬 CLI를 실행합니다.** 컨테이너 안에서 답변을 생성하려면 파이썬만으로 부족하고 Node 런타임·CLI 바이너리·인증 상태가 모두 필요합니다.

이걸 QA change까지 미루면 그 시점에 베이스 이미지와 설치 계층을 다시 짜야 하고, 최악의 경우 "컨테이너 안에서는 SDK가 동작하지 않는다"는 사실을 마감 직전에 발견하게 됩니다. 그래서 **설치와 주입 경로만** 먼저 뚫었고, 호출은 그 위에 나중에 얹었습니다 — 순서가 이랬던 덕에 어댑터를 쓸 때는 "무엇이 오는가"가 이미 실측으로 확정돼 있었습니다.

### `claude-code-sdk`에서 Codex SDK로 갈아탄 이유

처음에는 `claude-code-sdk`를 골랐습니다. 파이썬 네이티브라 subprocess 래핑 없이 async 스트리밍을 다룰 수 있다는 것이 근거였습니다. **그 판단은 자격증명이 어디에 저장되는지를 보지 않은 채 내린 것이었고, 실행 경로를 뚫는 단계에서 뒤집혔습니다.**

무엇이 걸렸는지부터 적습니다. Claude Code는 macOS에서 자격증명을 Keychain 항목(`Claude Code-credentials`)에 넣습니다. Keychain은 파일이 아니라 macOS Security 프레임워크가 로그인 세션 키로 여는 암호화 저장소라, **리눅스 컨테이너가 읽을 방법이 없습니다.** 그래서 호스트에서 `security find-generic-password`로 꺼내는 셸 스크립트가 필요했고, `make up`이 그걸 먼저 돌렸습니다. 그 결과 **make가 없는 윈도우에서는 실행 자체가 불가능**했습니다.

`docker compose`에는 호스트 명령을 부르는 훅이 없습니다 — `command`·`entrypoint`·`develop.watch`·`post_start` 모두 컨테이너 안에서 돕니다(있었다면 리포에서 받아온 compose 파일이 곧 임의 코드 실행이 됩니다). 즉 **`docker compose up` 한 줄로 Keychain을 여는 경로는 존재하지 않습니다.**

Codex는 세 OS 모두 자격증명이 **평문 파일**입니다.

| 호스트 | Claude Code | Codex |
|--------|-------------|-------|
| Windows | `%USERPROFILE%\.claude\.credentials.json` (파일) | `%USERPROFILE%\.codex\auth.json` (파일) |
| Linux | `~/.claude/.credentials.json` (파일) | `~/.codex/auth.json` (파일) |
| macOS | **Keychain 항목** — 컨테이너가 읽을 수 없음 | `~/.codex/auth.json` (파일) |

파일이면 볼륨으로 붙습니다. 호스트 단계가 통째로 사라지고 세 OS가 `docker compose up` 하나로 수렴합니다. 이것이 갈아탄 유일한 이유이고, 실행 가능성이 SDK의 API 편의보다 앞선다고 판단했습니다 — 평가 기준에서 "명시된 방법으로 실행 불가"는 감점이 아니라 불합격입니다.

대가는 정직하게 적습니다. **Codex에는 공식 파이썬 SDK가 없습니다.** 파이썬에서 쓰려면 CLI 프로세스를 띄워 stdout을 파싱해야 하므로, 프로세스 수명 관리·타임아웃·재시도를 직접 구현해야 합니다.

### 컨테이너 안에서의 실측

> 측정일 2026-08-03, `api` 이미지 안의 `codex-cli 0.146.0`. **런타임과 같은 이미지에서 잰 값**입니다.
>
> **두 번 쟀고, 두 번째가 첫 번째의 결론을 뒤집었습니다.** 처음 잰 표면은 `codex exec --json`이었고 "이 CLI는 토큰 델타를 주지 않는다"가 결론이었습니다. 그 결론은 `exec`에 대해서는 지금도 사실이지만, `exec`는 CLI의 유일한 표면이 아니었습니다. **`codex app-server`(stdio 위의 JSON-RPC)는 토큰 단위 델타를 냅니다.** 그래서 채택한 표면이 바뀌었고, 아래는 새 표면이 주(主)이고 `exec`는 "왜 이걸 안 쓰는가"의 근거로 남습니다.
>
> 채택한 표면의 stdout 원본은 [`tests/fixtures/codex/`](./tests/fixtures/codex/)에 그대로 있습니다. `exec` 회차의 원본 4건은 읽는 테스트가 없어 지웠고(이유는 같은 디렉터리 README), 결론만 이 문서에 남습니다. CLI 버전이 오르면 이 절과 픽스처를 함께 다시 떠야 합니다.

#### 두 표면의 대비 — 왜 `exec`가 아닌가

| | `codex exec --json` | **`codex app-server`** |
|---|---|---|
| 토큰 델타 | **없음** — 4,808자 답변도 한 이벤트 | **있음** — 77자 답변에 델타 38건 |
| 프로세스 모델 | 요청당 일회성 | 세션 — 핸드셰이크 후 재사용 |
| 요청당 지연 | 12 ~ 19초 | **4.5 ~ 8.0초** (프로세스가 따뜻할 때) |
| 인증 실패 판정 | 종료 코드 1 + **문구 매칭** | `codexErrorInfo.responseStreamDisconnected.httpStatusCode == 401` |
| 인증 실패를 아는 시점 | 19.3초 (CLI 자체 재시도가 끝난 뒤) | **2.2초** (첫 `error` 알림) |
| 성숙도 | 안정 | **`[experimental]` 표기** |

스트리밍은 과제의 하드 요구사항이고 평가 비중 18% 항목입니다. 표면 하나를 바꿔 얻는 것이 스트리밍 하나가 아니라 지연 3분의 1과 구조화된 인증 판정까지 셋이라, `[experimental]`을 감수했습니다. **되돌리기 비용이 낮다는 것이 그 감수의 근거입니다** — `AnswerGenerator` 계약이 표면과 무관하므로 최악의 경우 어댑터 디렉터리 하나를 `exec` 구현으로 되돌리면 됩니다.

#### 주 표면 — `codex app-server`

**호출 형태.** 프로세스는 인자 없이 띄우고, 에이전트를 좁히는 조치는 전부 JSON-RPC 파라미터로 들어갑니다.

```sh
env -i HOME=/home/app PATH=/usr/local/bin:/usr/bin:/bin CODEX_HOME=/home/app/.codex \
  codex app-server        # cwd = 빈 임시 디렉터리
```

| 조치 | 무엇을 위한 것인가 | 실측 결과 |
|------|-------------------|----------|
| 프롬프트를 `turn/start`의 `input`으로 | argv에 싣지 않는다 — 문서 본문이 프로세스 목록에 노출되고 argv 길이 상한이 문맥 크기의 숨은 천장이 된다 | 동작. `[{"type":"text","text":...}]` 배열입니다 |
| `thread/start`의 `cwd` = 빈 디렉터리 | 에이전트가 뒤질 대상 자체를 없앤다 | 동작. **`exec`에 필요했던 `--skip-git-repo-check`가 필요 없습니다** — git 리포가 아닌 디렉터리에서도 그냥 돕니다 |
| `sandbox: "read-only"` | 쓰기 차단 | 파라미터는 받아들여집니다. 효과를 단독으로 확인할 수 없는 것은 `exec` 때와 같습니다 — 아래 "bwrap" 참고 |
| `approvalPolicy: "never"` | 승인 프롬프트를 기다리며 매달리는 것을 막는다 | 매달림 없음 |
| `ephemeral: true` | 세션 파일을 남기지 않는다 | 동작. 응답의 `thread.ephemeral == true` |
| `env -i` + 3개 | 컨테이너 환경변수(저장소 접속 정보 포함)를 에이전트에게 통째로 넘기지 않는다 | `HOME`·`PATH`·`CODEX_HOME`만으로 정상 동작 |

주고받는 메시지는 넷입니다 — `initialize` → `initialized`(알림) → `thread/start` → `turn/start`. 그 뒤로는 서버가 알림을 흘립니다.

**알림 형식과 스트리밍 입자도**

| 확인한 것 | 결과 |
|---|---|
| 토큰 델타 알림 | **있습니다.** `item/agentMessage/delta` `{delta, itemId, threadId, turnId}`. 77자 답변에 **38건**, 209자 답변에 112건 |
| 델타와 완성본의 관계 | 델타를 도착 순서대로 이어 붙이면 같은 아이템 `item/completed`의 `text`와 **바이트 단위로 일치**합니다. 그래서 완성본을 또 내보내면 답변이 두 번 적힙니다 |
| 답변 아이템 식별 | `item.type == "agentMessage"`. **우리가 보낸 프롬프트가 `userMessage` 아이템으로 되돌아오므로** 이 필터가 없으면 프롬프트를 답변으로 착각합니다 |
| 종료 알림 | **`turn/failed`라는 메서드는 없습니다.** 실패해도 `turn/completed`가 오고 성패는 `params.turn.status`(`completed`/`failed`)에 있습니다 |
| 알림 라우팅 | 델타·`item/*`·`error`·`turn/*`은 `params.threadId`를 싣습니다. 다만 **`configWarning`·`remoteControl/status/changed`·`thread/started`·`account/rateLimits/updated` 넷은 싣지 않습니다** — `threadId`로 갈라 큐에 넣는 라우팅은 "세션 수준 알림은 큐에 넣지 않고 로그로만 남긴다"와 함께여야 성립합니다 |
| 사용량 | `thread/tokenUsage/updated`·`account/rateLimits/updated`로 오지만 소비자가 없어 읽지 않습니다 |
| 딸려 오는 것 | `thread/start`마다 번들 MCP 서버(`codex_apps`)가 뜹니다(`starting` → `ready`, 약 0.7초). 우리가 요청한 것이 아니고 끌 인자를 찾지 않았습니다 — 도구 실행 자체가 아래 "bwrap"으로 막혀 있어 위험이 닫혀 있습니다. 스레드마다 뜨는지 세션당 한 번인지는 재지 않았습니다 |

델타가 있다는 사실이 SSE 설계를 좌우합니다. **서버는 그 조각을 다시 합치지도, 더 잘게 쪼개지도 않습니다** — 합치면 실제로 앞당겨진 도착을 되돌리는 일이고, 쪼개면 없는 진행을 만들어 내는 연출입니다. 생성기가 내보내는 단위를 그대로 `answer` 이벤트로 흘립니다.

**소요 시간** — RAG 모양(근거 2건, 짧은 한국어 답변). 프로세스 기동부터의 상대 시각입니다.

| 지점 | 성공 회차 | 401 회차 |
|------|---------:|--------:|
| `initialize` 응답 | 0.25초 | 0.12초 |
| `thread/start` 응답 | **7.87초** | 0.17초 |
| `turn/started` | 8.63초 | 0.18초 |
| 첫 델타 | 15.60초 | — (없음) |
| 첫 401 `error` | — | **2.19초** |
| `turn/completed` | 16.65초 | 18.98초 |

**`thread/start`의 8초가 인증·세션 준비입니다.** 인증이 없는 회차에서 0.17초에 끝나는 것이 그 근거고, 같은 프로세스에서 두 번째 `thread/start`는 **0.07초**입니다. **기동 비용이 첫 요청에만 붙는다**는 이 한 줄이 세션 풀을 정당화합니다 — 턴 자체는 4.5~8.0초입니다.

`qa_llm_timeout_seconds` 기본값 **60초를 유지합니다.** 턴 상한에 거는 값이고 실측 최악값이 8초라 정상 경로가 상한에 닿지 않습니다. 다만 여유가 무한하다는 뜻은 아닙니다 — `exec` 회차에서 근거 제약 없이 장문을 시켰을 때 85.4초가 걸렸습니다. 우리 프롬프트는 답변을 근거 안으로 묶어 그 모양이 나오지 않지만, 문맥이나 지시가 바뀌면 상한이 먼저 걸리는 쪽이 될 수 있습니다.

**인증 부재**

`error` 알림 하나로 판정됩니다. 문구 매칭이 필요 없습니다.

```json
{"method":"error","params":{"error":{"message":"Reconnecting... 2/5",
  "codexErrorInfo":{"responseStreamDisconnected":{"httpStatusCode":401}}},
  "willRetry":true,"threadId":"...","turnId":"..."}}
```

세 가지를 함께 적습니다.

- **첫 401은 2.2초에 오는데 CLI에 맡기면 19.0초입니다.** CLI가 WebSocket 5회 → HTTPS 폴백 5회로 자체 재시도하고, `turn/completed`(`status: "failed"`)는 그 뒤에야 옵니다. 그래서 첫 401을 보는 즉시 `turn/interrupt` 후 실패로 끊습니다.
- **`willRetry`를 401의 판단 근거로 쓰면 안 됩니다.** 실측에서 첫 401은 `willRetry: true`로 왔습니다. "CLI가 알아서 하겠지"로 넘기면 그 19초를 그대로 기다리게 됩니다. `willRetry`는 401이 **아닌** 오류에만 의미가 있습니다.
- **`codexErrorInfo`가 항상 객체는 아닙니다.** 마지막 `error` 알림에서는 문자열 `"other"`로 옵니다. 객체를 가정하고 `.get(...)`을 이어 붙이면 파서가 거기서 죽습니다.

자격증명이 **깨진** 경우(`auth.json`이 유효한 JSON이 아님)는 `exec` 때와 같이 일반 실패로 분류됩니다. 의도한 타협입니다 — 결과가 "재시도 두 번 더 하고 `llm_unavailable`로 끝남"이지 잘못된 성공이 아닙니다.

**stderr는 반드시 읽어야 합니다.** 401마다 한 줄씩 `ERROR ... 401 Unauthorized`가 쌓이고, 매 기동마다 bubblewrap 경고가 옵니다. 응답에는 싣지 않고 로그로만 남기지만, **세션이 오래 사는 표면이라 읽지 않으면 버퍼가 차서 어느 순간 조용히 멎습니다.** 일회성 프로세스였던 `exec`보다 이 규율이 더 중요해졌습니다.

#### 참고 표면 — `codex exec --json` (쓰지 않습니다)

지우지 않는 이유는 위 대비표가 곧 갈아탄 근거이기 때문입니다. 원본 출력은 `git show 8e503fc:tests/fixtures/codex/answerable.jsonl`로 꺼낼 수 있습니다.

```sh
env -i HOME=/home/app PATH=/usr/local/bin:/usr/bin:/bin CODEX_HOME=/home/app/.codex \
  codex exec --json --skip-git-repo-check --cd <빈 임시 디렉터리> --sandbox read-only \
    --ephemeral --ignore-user-config -c approval_policy=never - < prompt.txt
```

| 확인한 것 | 결과 |
|---|---|
| 이벤트 형식 | JSONL 4줄 — `thread.started` → `turn.started` → `item.completed` → `turn.completed` |
| 토큰 델타 | **없음.** 4,808자 답변도 `item.completed` 한 이벤트로 통째로 도착 (장문을 강제해 확인) |
| 한 턴의 `agent_message` 개수 | 1개가 아닐 수 있음 — 도구를 쓰는 회차에서 3건 관측. `answer` 이벤트 계약이 "0회 이상"인 이유가 이것입니다(app-server에서도 그대로 유효) |
| 인증 실패 판정 | **종료 코드로는 불가.** 인증 없음·자격증명 깨짐·신뢰되지 않은 디렉터리가 모두 1이고 잘못된 인자만 2. 문구(`401 Unauthorized`)를 봐야 했습니다 |
| 소요 시간 | 첫 이벤트 1.0~8.5초, 답변 8.0~18.3초, 종료 12.0~18.9초 (6회) |
| `--skip-git-repo-check` | **없으면 아예 못 돕니다.** 빈 임시 디렉터리는 git 리포가 아니라 `Not inside a trusted directory`로 즉시 종료 |
| 구조화 출력 | `--output-schema <JSON Schema 파일>` 옵션이 존재합니다 (쓰지 않는 이유는 change design 결정 4) |

#### bwrap — 이 컨테이너에서는 에이전트가 도구를 아예 못 씁니다

두 표면에 공통으로 해당합니다. `exec` 회차에서 관측했고, app-server도 매 기동마다 같은 원인의 경고(`could not find bubblewrap on PATH ... will use the bundled bubblewrap`)를 stderr에 씁니다.

도구를 쓰라고 대놓고 시키는 프롬프트를 넣어 좁히기가 먹히는지 시험했더니, 모든 셸 실행이 같은 오류로 실패했습니다:

```
bwrap: No permissions to create a new namespace, likely because the kernel does not
allow non-privileged user namespaces.
```

Codex는 샌드박스를 bubblewrap으로 거는데, 비특권 사용자 네임스페이스가 막힌 컨테이너에서는 그 샌드박스를 **만들지 못해 명령 실행 자체가 실패**합니다. 에이전트는 `pwd`조차 돌리지 못했고 `echo hello > ./written-by-agent.txt`도 실패해 작업 디렉터리에 파일이 생기지 않았습니다.

두 가지를 함께 적습니다. 이 환경에서는 "에이전트가 컨테이너 파일시스템을 뒤진다"는 위험이 사실상 닫혀 있습니다. 동시에 **그 차단은 우리 인자가 아니라 커널 설정에서 옵니다** — 읽기 전용 샌드박스가 단독으로 무엇을 막는지는 여기서 관측되지 않았고, 커널이 비특권 네임스페이스를 허용하는 호스트로 옮기면 방어선은 다시 우리 파라미터와 프롬프트 지시로 돌아옵니다. 그래서 좁히는 조치를 하나도 빼지 않습니다.

부수적으로 알게 된 것이 하나 더 있습니다. 에이전트가 요청하지도 않은 스킬 파일(`.codex/plugins/.../SKILL.md`)을 먼저 읽으려 했습니다. 프롬프트에 "제공된 근거 밖을 조회하지 말라"를 넣는 네 번째 방어선이 형식적인 문장이 아닌 이유입니다 — 실제 RAG 프롬프트를 넣은 회차에서는 도구 호출이 한 번도 없었습니다.

### 어댑터를 세 부품으로 가른 이유

`src/app/adapters/llm/`이 `session.py`·`pool.py`·`codex.py` 셋입니다. 한 파일로 두지 않은 근거는 각각 다릅니다.

| 부품 | 맡는 것 | 왜 독립인가 |
|---|---|---|
| `session.py` | 프로세스 하나, 핸드셰이크, JSON-RPC 송수신, 알림 라우팅, 생존 판정, 회수 | **여기에만 비동기 흐름이 둘입니다** — 우리가 보내는 요청과, 우리와 무관하게 흘러드는 알림. 이 라우팅이 어댑터에 섞이면 "델타를 어떻게 텍스트로 바꾸는가"와 "메시지를 누구에게 주는가"가 한 함수에서 얽힙니다 |
| `pool.py` | 지연 기동, 상한, 빌려주기/반납, 죽은 세션 교체 | **이 표면에서 새로 생기는 위험이 전부 여기 있습니다** — 죽은 세션을 빌려주는 경우, 상한 대기, 기동 실패. 요청마다 프로세스를 띄우던 시절에는 없던 것들이라, 가짜 프로세스로 실물 CLI 없이 전부 시험합니다 |
| `codex.py` | 턴 하나의 수명, 델타 파싱, 타임아웃, 인증 판정 | 판정 전부를 `TurnReader`라는 **순수 상태 기계**에 모아 두어, 저장해 둔 실물 알림 샘플(`tests/fixtures/codex/`)을 그대로 먹여 회귀를 고정합니다 |

세션 수준 규칙 셋이 코드 주석에서 문서로 올라온 것들입니다.

- **`threadId` 없는 알림은 큐에 넣지 않습니다.** 실측에서 `configWarning`·`remoteControl/status/changed`·`thread/started`·`account/rateLimits/updated` 넷이 그렇게 옵니다. 아무 큐에나 넣으면 턴 하나가 자기 것이 아닌 알림을 읽고, 빈 큐를 기다리던 쪽은 영원히 기다립니다.
- **stderr를 반드시 읽습니다.** 일회성 프로세스라면 버퍼가 차봐야 그 요청만 멈추지만, 살아 있는 세션은 계속 쌓여 **어느 순간 조용히 멎습니다.** app-server는 401마다 stderr에 한 줄씩 씁니다. 읽되 응답에는 싣지 않습니다.
- **JSON-RPC 한 줄 상한이 4 MiB입니다.** 기본값(64 KiB)으로는 부족합니다 — app-server가 우리 프롬프트를 `userMessage` 아이템으로 되돌려 주는데, `top_k` 상한 50 × 청크 600자면 30 KB대이고 JSON 이스케이프까지 붙으면 기본 상한 근처에서 `readline`이 예외로 죽습니다. 큰 문맥에서만 나는 실패라 평소에 보이지 않습니다.

**에이전트를 텍스트 생성기로 좁히는 조치**는 `thread/start`에 실립니다 — 빈 임시 `cwd`(파일을 뒤질 대상 자체를 없앱니다), `sandbox: read-only`, `approvalPolicy: never`(승인 프롬프트 대기가 타임아웃의 가장 흔한 원인이 됩니다), `ephemeral: true`(요청 사이에 맥락이 새지 않습니다). 프롬프트는 argv가 아니라 `turn/start`의 `input`으로 갑니다 — 문서 본문이 프로세스 목록에 노출되지 않고, argv 길이 상한이 문맥 크기의 숨은 천장이 되지도 않습니다.

### 자격증명을 이미지에 굽지 않는 이유

이미지에 넣으면 두 가지가 동시에 깨집니다. 자격증명이 빌드 산출물에 남고, 평가자 환경마다 인증 주체가 달라 이미지가 재사용 불가능해집니다. 그래서 **호스트에 이미 있는 것을 실행 시점에 파일로 마운트**합니다.

옮기는 일은 compose의 `auth` 서비스가 합니다. 한 번 돌고 끝나는 초기화 컨테이너이고, `api`가 `service_completed_successfully`로 그걸 기다립니다.

```
호스트 (세 OS 동일)                        컨테이너
  ~/.codex/auth.json ──ro──▶ auth 서비스 (alpine)
                                  │  scripts/sync-credentials.sh
                                  ▼
  .secrets/codex/auth.json ──rw──▶ api:/home/app/.codex/auth.json
        ▲                                        │
        └──── compose up 할 때마다 재추출   CLI 가 만료 시 갱신 (사본에만 반영)
```

| 결정 | 이유 |
|------|------|
| 새 토큰을 발급하지 않는다 | `codex login`은 **재발급**입니다. 기존 인증 상태 재사용이 제약이라 쓰지 않습니다 |
| 환경변수가 아니라 파일로 넣는다 | access token은 만료됩니다. 토큰 하나를 박아두면 갱신 경로가 없어 그날 안에 죽습니다 |
| `:ro`가 아니라 rw로 마운트한다 | 읽기 전용이면 갱신에 실패해 같은 이유로 인증이 끊깁니다 |
| 기동할 때마다 재추출한다 | 컨테이너가 항상 호스트의 최신 상태로 시작하므로, 사본이 묵는 창이 컨테이너 수명으로 제한됩니다 |
| 호스트 홈은 **디렉터리째** `:ro`로 붙인다 | 파일 하나를 지정하면, 그 파일이 없는 호스트에서 도커가 그 자리에 **디렉터리를 만들어** 호스트의 `codex login`을 망가뜨립니다 |
| 동기화 스크립트를 이미지에 굽지 않고 마운트한다 | api 이미지 재빌드와 자격증명 로직이 엮이지 않고, 본문을 읽어 제약을 고정하는 테스트가 같은 파일을 봅니다 |

컨테이너가 가진 것은 **사본**입니다. 컨테이너가 갱신해도 호스트로 돌아가지 않습니다. 역방향 동기화는 호스트의 인증 상태를 컨테이너가 덮어쓰는 행위라 호스트 CLI를 망가뜨릴 위험이 더 커서 하지 않습니다.

호스트 홈 경로는 셸에 의존하지 않고 고릅니다. `${CODEX_HOME:-${USERPROFILE:-${HOME:-/nonexistent}}/.codex}` — 윈도우 Git Bash의 `HOME`은 `/c/Users/...`(MSYS 경로)라 도커에 그대로 넘기면 위험하고, `USERPROFILE`은 어느 셸에서든 `C:\Users\...` 네이티브 경로입니다. macOS·리눅스에는 `USERPROFILE`이 없어 `HOME`으로 떨어지고, 비표준 위치를 쓰는 사람은 codex 자신의 `CODEX_HOME`이 존중됩니다.

### 이미지에 Node를 넣는 방법

`codex`는 네이티브 바이너리를 고르는 node 셤이라 Node 런타임이 함께 있어야 합니다. 데비안의 `npm` 패키지는 쓰지 않습니다 — `node-*` 의존성 395개(약 50MB)를 끌고 오고, 실제로 그중 두 개가 미러에서 400을 받아 빌드가 통째로 깨졌습니다. 평가자 환경에서 같은 식으로 깨지면 "실행 불가"가 되므로, 공식 `node:22-slim`에서 설치하고 결과만 `COPY --from`으로 가져와 미러 상태에 걸린 표면을 없앴습니다.

`ca-certificates`는 선택이 아닙니다. 빠뜨리면 codex가 **인증까지 통과하고도** 모든 호출이 `invalid peer certificate: UnknownIssuer`로 죽습니다. 인증 문제로 오인하기 딱 좋은 증상이라 적어둡니다.

### 부팅 경로에서 CLI를 실행하지 않는 이유

인증이 없거나 손상된 환경에서 서비스가 기동조차 못 하면 "실행 불가"가 됩니다. 그래서 인증 상태는 기동 조건이 아니며, 헬스도 LLM 제공자를 근거로 불가용을 보고하지 않습니다. **세션 풀은 지연 기동이라 첫 `/qa` 요청이 오기 전에는 프로세스가 뜨지 않습니다** — 배선은 풀 객체만 만들고, `codex app-server`를 띄우는 것은 첫 대여입니다.

첫 요청이 8초쯤 느려지는 것은 받아들입니다. 기동 시 예열하면 부팅 경로가 CLI를 건드리게 되어, 자격증명이 없는 환경에서 기동이 느려지거나 실패하는 쪽이 훨씬 비쌉니다.

이 성질은 문서가 아니라 테스트로 고정돼 있습니다 — `tests/test_boot.py`가 자격증명이 **없는** 상태와 **판독 불가능한** 상태 양쪽에서 기동과 `/health` 200을 단언하고, 기본 배선에서는 `AppServerSession.start` 자체를 금지한 채 기동시켜 지연 기동을 확인합니다. 누군가 부팅 경로에 자격증명 파싱이나 세션 예열을 끼워 넣으면 거기서 깨집니다.

### 컨테이너 구성

| 항목 | 값 | 이유 |
|------|-----|------|
| 워커 프로세스 | 1개 고정 | 같은 문서에 대한 요청의 직렬화를 **프로세스 안의** `asyncio.Lock` 으로 합니다. 늘리려면 외부 잠금 도입이 먼저입니다 (아래 "같은 문서는 직렬화되고, 다른 문서는 병렬이다") |
| 캐시·벡터 스토어 컨테이너 | 자체 헬스체크 + `depends_on: service_healthy` | 의존성이 늦게 뜨는 동안 API가 503을 내면 재시작 루프가 됩니다 |
| 벡터 스토어 이미지 | `chromadb/chroma:1.5.9` — 클라이언트와 같은 버전 | 서버 모드에서는 둘이 HTTP API로 만납니다. 버전이 어긋나면 기동은 되는데 첫 쓰기에서 실패하는 형태가 됩니다 |
| 벡터 스토어 포트 | 호스트 8001 → 컨테이너 8000 | Chroma 기본 포트(8000)가 API와 겹칩니다. 도커 안팎에서 같은 주소를 쓰게 됩니다 |
| API 헬스체크 | `start_period: 30s` | 기동 유예 안의 실패는 재시도로 흡수됩니다 |
| 영속화 | 호스트 바인드 마운트 — `./data/chroma`, `./data/registry` | 두 서비스가 분리돼 각자의 데이터를 갖습니다. 바인드라 호스트에서 바로 열어 볼 수 있는 대신, **리눅스에서 호스트 UID가 1000이 아니면** 컨테이너의 `app` 사용자가 쓰지 못할 수 있습니다(그 경우 `docker-compose.yml`에 `user:`를 지정합니다). macOS Docker Desktop은 소유권을 매핑해 줍니다 |
| 벡터 스토어 GUI | `vector-store-ui`, **`gui` 프로필** | Chroma 서버에는 자체 UI가 없습니다(루트 경로 404). 비공식 admin UI 이미지가 arm64 전용이라 기본 기동에 넣으면 amd64 환경의 `docker compose up`이 깨집니다 — 프로필로 격리해 `--profile gui`일 때만 뜹니다 |
| 실행 사용자 | 비루트 (`app`, UID 1000) | — |

**미검증 항목은 [`docs/SPIKES.md`](./docs/SPIKES.md)에 모아 두었습니다.** 컨테이너 안에서 CLI가 실제로 동작하는지, 마운트한 자격증명을 CLI가 인증에 쓰는지, refresh token이 회전해 호스트를 로그아웃시키는지, 리눅스 UID 불일치에서 마운트가 읽히는지 — 네 가지이며 각각 확인 방법과 틀렸을 때의 대안이 함께 적혀 있습니다.

## 레이어 구조

```
src/app/
  api/          라우터. HTTP 관심사만 — 검증, 상태 코드, 응답 직렬화
    routes/health.py
    routes/documents.py   업로드·목록·상세·삭제. 크기 상한 검사와 상태 코드 선택
    routes/search.py      질의 검증(내용·문자 수·토큰 수·top_k 경계)과 응답 모양
    errors.py           도메인 예외 → HTTP 변환 (이 계층에서만)
    logging.py          JSON 포매터, 요청 단위 로그 미들웨어
  services/     유스케이스 오케스트레이션
    health.py           프로브를 모아 전체 상태를 판정
    ingestion.py        파싱(오프로드) → 청킹 → 토큰 가드 → 배치 임베딩·저장 → 커밋 → 정리
    retrieval.py        질의 임베딩 → 대상 집합 → 저장소 질의 → 현재성 재검증 → 하한
  core/         도메인. 프레임워크·외부 라이브러리를 import 하지 않는다
    models.py           상태 값 객체 (Status, ProbeResult, HealthReport)
    documents.py        문서 값 객체, 식별자·리비전·색인 서명 유도, 색인 구성 판정
    chunking.py         분할 전략 레지스트리와 recursive 분할, 겹침 보정
    retrieval.py        검색 결과 값 객체 (ScoredChunk)
    exceptions.py       도메인 예외 기반 클래스, ErrorCode
  adapters/     외부 경계
    protocols.py        HealthProbe · DocumentParser · Embedder · VectorStore · DocumentRegistry
    cache/probe.py      캐시 저장소 프로브
    parsers/            문서 파서 (txt·md·pdf) 와 확장자 → 파서 레지스트리
    embedding/          로컬 sentence-transformers 임베더
    vector_store/       Chroma 서버 클라이언트, 쓰기·삭제·집계, heartbeat 프로브
    registry/           SQLite 문서 레지스트리
  config.py     환경변수 → 설정 객체 (환경을 읽는 유일한 곳)
  main.py       앱 팩토리, 의존성 배선
```

상위 레이어는 하위 레이어의 인터페이스만 알고 구현체는 모릅니다.

**레코드가 스스로 답할 수 있는 질문은 레코드에 둡니다.** "이 문서가 현재 색인 구성으로 저장되어 있는가"(`matches_index`), "지금 검색 대상인가"(`is_searchable_under`), "이 내용을 이 구성으로 이미 갖고 있는가"(`is_up_to_date`) — 셋 다 `Document`의 메서드입니다. 수집(재색인 판정·`stale` 처리·기동 정리)과 검색(대상 집합·현재성 재검증)이 **같은 축들을 보기 때문**입니다. 각 서비스가 따로 구현하면 한쪽만 고쳐진 순간 수집이 유효하다고 여기는 문서와 검색이 찾는 문서가 어긋나는데, 그때 **각자 자기 기준으로는 옳아서 어디에도 오류가 남지 않습니다.** 색인 서명을 배선에서 한 번만 유도해 두 서비스에 주입하는 것(아래 "색인 서명")은 절반만 막습니다 — 값을 통일해도 그 값을 쓰는 규칙이 여러 벌이면 같은 자리가 다시 열립니다.

서비스에 남는 것은 **그 판정으로 무엇을 할 것인가**입니다. "이미 최신이면 저장 경로에 진입하지 않는다", "검색 대상이 비면 저장소를 건드리지 않는다" 같은 결정이 유스케이스이고, 그 앞의 사실 판정은 도메인입니다.

### 경계를 컨벤션이 아니라 도구로 강제한다

두 규칙을 ruff 린트로 막아둡니다. 문서에만 적힌 규칙은 어겨도 티가 안 나기 때문입니다.

| 규칙 | 강제 방식 | 확인 |
|------|----------|------|
| `core/`는 프레임워크·외부 라이브러리를 import 하지 않는다 | `src/app/core/.ruff.toml`의 `banned-api` (ruff가 파일별로 가장 가까운 설정을 적용) | `core/`에서 `import fastapi` → `TID251` |
| 환경변수는 `config.py`를 통해서만 읽는다 | `pyproject.toml`의 `banned-api` + `config.py`·`tests/`만 예외 | `services/`에서 `os.environ` → `TID251` |

### 어댑터는 프로토콜 뒤에 둔다 — 단, 지금 필요한 만큼만

현재 정의된 계약은 다섯입니다.

| 프로토콜 | 계약 | 정의된 이유 |
|---|---|---|
| `HealthProbe` | 의존성 하나의 도달 가능 여부 | 프로브 구현이 있다 |
| `DocumentParser` | 바이트 → 텍스트 세그먼트 (아래 "문서 파서") | 파서 구현이 있다 |
| `Embedder` | 텍스트 → 벡터, 토큰 계산, `signature` (아래 "임베딩") | 임베더 구현이 있다 |
| `VectorStore` | 청크 **쓰기·삭제·집계·질의** (아래 "저장소") | 수집이 앞의 셋을, 검색이 질의를 쓴다 |
| `DocumentRegistry` | 문서 현재 상태의 단일 답 (아래 "저장소") | 수집이 쓴다 |

`CacheStore`(get/set/invalidate) 같은 인터페이스는 **의도적으로 정의하지 않았습니다.** 소비자가 없는 상태에서 정한 인터페이스는 실제 사용처가 생기는 순간 거의 반드시 틀린 것으로 드러나고, 그때는 이미 문서에 적혀 있어 고치는 데 설명이 붙습니다. 각 프로토콜은 그것을 실제로 쓰는 change에서 정의합니다.

`VectorStore.query`가 그 규율의 실례입니다. 수집만 있던 동안에는 **일부러 비워 두었고**, 검색이 실제로 소비자가 된 시점에 자기 요구(대상 필터·상위 K·점수 방향)를 알고 붙였습니다. 미리 정했다면 `top_k`와 거리 지표만 있는 인터페이스가 됐을 텐데, 실제로 필요한 것은 **레지스트리가 정한 대상 목록**이었습니다.

이 좁은 경계가 테스트에서 값을 냅니다. 프로브가 프로토콜이므로 대역을 주입해 정상·불능·무응답·예외 네 상태를 결정론적으로 만들 수 있고, 스위트가 외부 서비스 없이 돌아갑니다.

### 어댑터 계약 — 시그니처에 담긴 결정

`src/app/adapters/protocols.py`의 각 프로토콜이 무엇을 왜 그렇게 선언했는지입니다. 코드 주석은 한 줄로 줄이고 근거를 여기 모았습니다.

| 결정 | 이유 |
|---|---|
| `DocumentParser.parse`가 **동기** | 두 구현 모두 CPU 바운드입니다. `async def`로 선언하면 "논블로킹"이라는 거짓말이 되고 구현자가 이벤트 루프 위에서 그냥 돌려도 타입상 문제가 없어 보입니다. 동기면 호출부가 오프로드를 의식할 수밖에 없습니다 |
| 파서가 경로가 아니라 **바이트**를 받음 | 업로드는 크기 상한 안에서 이미 메모리에 있습니다. 경로를 받으면 임시 파일 생성·정리 책임이 생기고 테스트마다 파일을 만들어야 합니다 |
| `formats`가 **복수** | 텍스트 파서 하나가 `.txt`·`.md`를 함께 다룹니다. 추출 방식은 같지만 응답의 `format` 값은 달라야 해서, 단일 `format` 속성을 두면 둘 중 하나가 거짓이 됩니다 |
| `Embedder.count_*`가 **역할별로 둘** | 역할마다 실제로 인코딩되는 문자열이 다릅니다(접두사 길이). 하나로 뭉치면 한쪽 계산이 반드시 틀리고, 그 틀림은 **상한 바로 아래 입력이 조용히 잘리는** 방식으로만 드러납니다 |
| `count_*`만 **동기**, `embed_*`는 async | 토크나이저 호출은 블로킹이라 파서와 같은 이유로 동기입니다. 인코딩은 배치마다 호출되는 자리라 호출부가 매번 감싸면 중복만 늘고, 어댑터가 배치 사이 양보까지 맡는 편이 자연스럽습니다 |
| `dimension`·`signature`를 **모델 로딩 없이** 읽을 수 있어야 함 | 색인 서명이 수집 경로 시작 전에 필요합니다. 서명을 읽는 일이 로딩을 유발하면 선로딩 실패 시의 백스톱(첫 호출 로딩)이 무의미해집니다 |
| `warm_up`이 **프로토콜에** 있음 | 앱 팩토리는 프로토콜만 알고 구현체를 모릅니다. `isinstance`로 구체 어댑터를 확인하고 부르면 배선이 구현체를 알게 됩니다. 실패해도 서비스는 뜨며, 구현체는 실패를 숨기지 않고 그대로 올립니다 |
| `VectorStore.query`가 **벡터**를 받음 | 문자열을 받으면 어댑터가 임베더를 알아야 하고, 둘 중 하나를 갈아끼울 때 다른 하나가 딸려 옵니다 |
| 검색 대상을 **호출자가 삼중항 목록으로** 지정 | 저장소에는 검색되면 안 되는 청크가 남아 있을 수 있어(되돌리기가 닿지 못한 실패), 무엇이 지금 유효한지는 레지스트리를 아는 서비스만 답할 수 있습니다. **빈 목록은 "대상 없음"이지 "조건 없음"이 아닙니다** |
| `add_chunks`가 **원자적이지 않음** | 벡터 스토어 배치 쓰기에 트랜잭션이 없어 계약에 원자성을 적어도 구현이 지킬 수단이 없습니다. 지킬 수 없는 약속 대신 교체 순서를 아는 서비스가 되돌리기를 책임집니다 |
| `AnswerGenerator`가 **문자열 하나**를 받음 | 프롬프트 조립은 도메인 지식이라 `core/prompting.py`에 있습니다. 어댑터가 청크를 받아 조립하면 프롬프트가 구현마다 하나씩 생깁니다 |
| **조각을 yield**, 완성본 반환 아님 | 계약이 "0개 이상"이라 표면이 토큰 델타를 주든 메시지를 통째로 주든 상위 계층이 안 바뀝니다. 실제로 회수했습니다 — 구현을 `codex exec`에서 `codex app-server`로 바꿀 때 이 줄은 그대로였습니다 |
| **취소를 순회 종료로** 표현 (별도 `cancel()` 없음) | 소비자가 `aclose`하면 구현체가 `finally`에서 그 시도의 자원을 정리합니다. 메서드는 호출을 잊을 수 있지만 순회 종료는 잊을 수 없습니다 |
| `timeout_seconds`가 **한 시도의 상한** | 시도 횟수와 백오프는 정책이라 서비스가 듭니다. 여기 있는 것은 "이 시도를 언제 끊는가"뿐입니다 |
| `DocumentRegistry`를 벡터 스토어 메타데이터에서 **유도하지 않음** | 교체 도중에는 두 리비전의 청크가 공존해 답이 둘이 되고, 청크 전체의 메타데이터를 한 번에 바꾸는 원자적 연산도 없습니다. `commit`이 성립하는 순간이 곧 교체가 확정되는 순간입니다 |

## 문서 청킹

> 여기서 만든 청크를 임베딩해 저장하는 구간은 아래 "문서 수집 파이프라인"에 있습니다.

### 전략은 선택지이고, 구현된 것은 하나다

분할 전략은 설정 항목 `APP_CHUNK_STRATEGY`이고 기본값은 `recursive`입니다. **실제로 구현된 전략은 `recursive` 하나뿐입니다.** 다른 값을 넣으면 기동에 실패하고, 오류 메시지에 받아들여지는 값 목록이 나옵니다.

전략을 열거형으로 두는 이유는 다음 후보가 이미 보이기 때문입니다 — **부모-자식 청킹**(작은 자식 청크로 검색하고 답변에는 큰 부모 청크를 넘기는 방식)입니다. 지금 붙이지 않는 이유는 범위가 아니라 순서였습니다. 부모-자식의 이득은 *검색된 자식을 부모로 확장해 답변에 넘기는 단계*에서 나오는데, 청킹을 정할 때는 그 청크를 소비하는 답변 생성이 없었습니다. 확장할 곳이 없는 상태에서 부모 텍스트를 저장하면 인덱스만 배로 커지고 검증할 수 있는 것이 없습니다.

**이제 소비자가 생겼으므로 붙일 자리가 열렸습니다** — `build_prompt`에 넘기는 문맥을 검색된 자식이 아니라 그 부모로 바꾸는 한 지점입니다. 다만 그 이득은 측정해야 알 수 있고, 지금은 답변 품질의 기준선 자체가 없습니다.

붙일 자리는 `core/chunking.py`의 `CHUNK_SPLITTERS` 한 줄입니다. 분할 함수는 전부 `(segments, size, overlap) -> tuple[TextChunk, ...]` 모양을 지키므로, 새 전략이 상위 계층을 건드리지 않습니다. 열거형에 값만 더하고 함수 등록을 빠뜨리는 실수는 테스트가 잡습니다(`set(CHUNK_SPLITTERS) == set(ChunkStrategy)`).

### `recursive` — 구분자 우선순위로 경계를 고른다

```
문단(\n\n) → 줄(\n) → 한국어 문장 종결(다. / 요. ) → 영어 문장 종결(. ) → 공백 → (상한에서 절단)
```

세 단계입니다 — **쪼개고, 합치고, 겹칩니다.**

1. **쪼개기(재귀).** 구간이 예산보다 크면 현재 우선순위의 구분자로 조각냅니다. 그 구분자가 구간에 없으면 다음 우선순위로 내려갑니다. 조각이 여전히 크면 그 조각만 다시 내려갑니다. 구분자를 다 써도 크면 마지막 수단으로 상한에서 그대로 자릅니다 — 공백 없는 긴 토큰이 그 경우입니다.
2. **합치기.** 연속한 조각을 예산까지 이어 붙입니다.
3. **겹치기.** 각 청크의 시작을 `overlap`만큼 뒤로 당깁니다.

**2단계가 없으면 안 됩니다.** 이른 자리에 구조 경계가 하나만 있는 문서 — 짧은 제목 뒤에 문단 구분자가 없는 본문이 오는 흔한 모양 — 에서 그 경계에 끊고 멈추면 10자짜리 청크가 나옵니다. 쪼개기만으로는 상한을 지킬 뿐 청크가 잘게 부서지는 것을 막지 못합니다.

**재귀는 문자열이 아니라 `(start, end)` 구간에 대해 합니다.** 문자열을 잘라 내려가면 각 조각이 원문 어디에서 왔는지 따로 추적해야 하고, 그 추적이 어긋나는 순간 출처 표기가 조용히 틀어집니다. 구간을 넘기면 오프셋이 재귀 인자 그 자체라 어긋날 여지가 없습니다.

**합치기 예산은 `size`가 아니라 `size - overlap`입니다.** 3단계에서 앞으로 늘려 붙일 자리를 남겨야 최종 청크가 상한을 넘지 않습니다. 즉 청크 하나가 새로 담는 내용은 최대 `size - overlap`이고 나머지는 앞 청크와 겹칩니다. 문서 전체가 `size` 안에 들어가면 이 예산 축소를 건너뛰어, 짧은 문서가 불필요하게 둘로 쪼개지지 않습니다.

**겹침은 조각 단위가 아니라 문자 단위**로 당깁니다. 조각 단위로 하면 마지막 조각이 겹침보다 길 때 겹침이 통째로 사라집니다 — 문단 하나가 조각 하나인 문서에서 바로 그렇게 됩니다. 당기는 폭은 앞 청크의 시작을 넘지 못하게 막습니다. 넘으면 앞 청크가 뒤 청크에 통째로 삼켜져 같은 내용이 두 번 저장되고 검색 결과에도 두 번 나옵니다.

| 기각한 대안 | 이유 |
|---|---|
| 고정 길이 분할 | 문장·문단 중간을 자릅니다. 잘린 조각이 검색되면 출처로 보여줄 때 말이 되지 않는 텍스트가 나옵니다 |
| 문장 단위 | 한국어 문장 분리는 규칙만으로 정확하지 않고, 조각이 너무 짧아 임베딩이 주변 맥락을 잃습니다 |
| 시맨틱 청킹 | 수집 비용이 문서당 임베딩 호출 수만큼 늘고 결과가 모델에 종속돼 재현이 어렵습니다. 문서 종류가 두 개뿐인 지금 이득이 비용보다 작습니다 |
| 토큰 기준 크기 | 토크나이저를 알아야 하므로 `core/`가 임베딩 라이브러리에 의존하게 됩니다 — 계층 규칙 위반입니다. 대신 임베딩 직전 토큰 가드가 초과 청크를 `resplit`으로 다시 쪼갭니다 |

### 기본값 600자 / 겹침 100자

**600자**의 근거는 둘입니다. (a) 출처 표기의 단위이므로 "이 문단에서 왔다"고 말할 수 있을 만큼 작아야 합니다 — 문서 한 쪽을 통째로 인용하면 출처의 의미가 없습니다. (b) 임베딩 모델 입력 창(512 토큰) 안에 여유 있게 들어가야 합니다. 한국어 600자는 대략 400~450 토큰입니다.

**겹침 100자**는 문단 경계에 걸친 문장이 양쪽 청크 어디에서도 온전히 읽히게 하는 최소치입니다. 겹침은 **0을 허용하지 않습니다** — 0이면 인접 청크가 겹치지 않아 경계 문장이 어느 쪽에서도 온전하지 않습니다.

0을 막는 곳은 **두 군데**입니다. 설정(`Settings`)과 분할 함수 자신입니다. 분할 함수는 설정을 거치지 않고도 호출되는 공개 계약이라, 설정에서만 막으면 "겹친다"는 성질이 호출 경로에 따라 달라집니다.

두 값 모두 설정 항목입니다. 검색 품질 실측은 질의 경로가 생긴 뒤의 일이고, 그때 재조정될 수 있습니다.

### 청크는 페이지 경계를 넘지 않는다

파서는 문자열 하나가 아니라 `TextSegment` 목록을 돌려줍니다 — PDF 는 쪽, 텍스트·마크다운은 문서 전체가 세그먼트 하나입니다. 분할은 세그먼트 **안에서만** 일어납니다. 두 쪽에 걸친 청크가 생기면 "이 청크는 몇 쪽인가"에 답이 둘이 되어 출처 표기가 모호해집니다. 대가는 짧은 쪽에서 짧은 청크가 나온다는 것인데, 출처 정확성이 더 중요합니다.

오프셋은 **세그먼트 기준**입니다. txt·md 는 문서 전체가 세그먼트 하나라 문서 오프셋과 같고, PDF 는 쪽 안의 오프셋입니다. PDF 에서 문서 전역 오프셋은 실재하지 않는 연결 문자열을 가정해야 나오는 값이라 쓰지 않습니다 — PDF 청크의 주소는 `(page, char_start, char_end)`입니다.

`TextSegment`·`TextChunk` 같은 값 객체는 `core/`에 있고 `DocumentParser` 프로토콜만 `adapters/`에 둡니다. 청킹이 이 타입을 소비하는데 `core/`가 `adapters/`를 import 하면 계층이 역전되기 때문입니다.

### 분할이 지키는 불변식 넷

검색 품질은 여기서 재지 않습니다. 여기서 고정하는 것은 **분할이 원문을 훼손하지 않는다**는 성질입니다.

| 불변식 | 깨졌을 때 |
|---|---|
| `chunk.text == segment_text[char_start:char_end]` | 출처가 원문의 엉뚱한 자리를 가리킵니다 |
| 모든 청크 길이 ≤ 설정 상한 | 임베딩 입력 창을 넘어 뒷부분이 조용히 잘립니다 |
| 원문의 모든 비공백 문자가 어느 청크엔가 포함 | 사라진 내용은 검색되지 않는데 저장은 성공해 아무도 눈치채지 못합니다 |
| 인접 청크 사이에 비공백 문자가 있으면 두 구간이 겹침 | 경계 문장이 양쪽 어디에서도 온전하지 않습니다 |
| 청크가 앞으로 나아감 (앞 청크에 통째로 담기는 청크가 없음) | 같은 내용이 두 번 저장되고 검색 결과에도 두 번 나옵니다 |

넷째가 "비공백 문자가 있으면"으로 조건이 붙는 이유: 경계가 공백뿐이면 걸쳐 있는 문장 자체가 없어 보존할 문맥이 없습니다. 반대로 경계에 글자가 있는데 겹치지 않으면 그 자리의 문맥이 끊기고, 그건 금지입니다. 실측하면 실제 문서에서는 겹침 비율 17%~90% 어느 설정에서도 겹치지 않는 인접 쌍이 나오지 않습니다 — 조건이 걸리는 것은 공백 덩어리가 많은 인공적 입력뿐입니다.

다섯째는 앞의 넷이 잡아 주지 않습니다. `['사내 복리후생 안내', '생 안내', '내']` 같은 결과는 구간 일치·상한·무손실·겹침을 **전부 만족**한 채로 쓸모없는 청크를 인덱스에 넣습니다. 실제로 이 형태의 결함이 한 번 나왔고, 그때 이 불변식을 추가했습니다.

청크 앞뒤 공백은 **구간째로** 덜어냅니다. 본문만 다듬고 오프셋을 그대로 두면 첫째 불변식이 깨집니다.

### `CHUNK_STRATEGY_VERSION`은 손으로 올린다

`core/chunking.py`의 상수입니다. 구분자 우선순위나 겹침 계산처럼 **청크 경계가 달라지는 수정**을 하면 이 값을 올려야 합니다.

이 값은 `index_signature`(저장된 벡터가 어떤 구성으로 만들어졌는지를 나타내는 값)의 재료입니다. 올리면 기존 색인이 현재 구성과 다르다고 판정되고, 올리지 않으면 설정값은 그대로인데 경계만 달라진 청크가 구 경계 청크와 한 인덱스에 섞입니다. 설정값(크기·겹침)만으로는 이 변경을 잡을 수 없어 상수를 따로 둡니다.

자동 유도(소스 해시)를 쓰지 않는 이유는 주석 한 줄 수정에도 반응해 무의미한 전면 재색인을 부르기 때문입니다. **규율에 의존하는 지점임을 인정합니다.**

## 문서 파서

> 이 파서를 호출하는 구간은 아래 "문서 수집 파이프라인"에 있습니다.

### `DocumentParser` 프로토콜

```python
class DocumentParser(Protocol):
    formats: frozenset[DocumentFormat]

    def parse(self, data: bytes) -> ExtractedDocument: ...
```

**동기입니다.** 두 구현 모두 CPU 바운드입니다. `async def`로 선언하면 "이건 논블로킹"이라는 거짓말이 되고, 구현자가 이벤트 루프 위에서 그냥 돌려도 타입상 아무 문제가 없어 보입니다. 동기로 선언하면 호출부가 오프로드를 **의식할 수밖에 없습니다** — 수집 서비스가 `asyncio.to_thread` 로 명시적으로 넘깁니다.

**경로가 아니라 바이트를 받습니다.** 업로드는 크기 상한 안에서 이미 메모리에 올라와 있습니다. 경로를 받게 하면 임시 파일 생성·정리 책임이 생기고, 테스트마다 파일을 만들어야 합니다.

`formats`가 복수인 이유는 텍스트 파서 하나가 `.txt`와 `.md`를 함께 다루기 때문입니다. 추출 방식은 같지만 응답의 `format` 값은 달라야 하므로, 파서에 단일 `format` 속성을 두면 둘 중 하나가 거짓이 됩니다. 어느 포맷으로 들어왔는지는 레지스트리가 판정합니다.

프로토콜이 주고받는 값 객체(`TextSegment`·`ExtractedDocument`)는 `core/`에 있습니다. 청킹이 `TextSegment`를 소비하는데 `core/`가 `adapters/`를 import 하면 계층이 역전됩니다. `HealthProbe`(adapters) ↔ `ProbeResult`(core)와 같은 배치입니다.

### 세그먼트 — 쪽 경계를 살려 두는 이유

파서는 문자열 하나가 아니라 **세그먼트 목록**을 돌려줍니다. 파서마다 자연스러운 단위가 다르기 때문입니다 — PDF는 쪽, 텍스트·마크다운은 문서 전체. 하나로 합쳐 돌려주면 페이지 정보가 그 시점에 소실되고 되살릴 방법이 없습니다. 경계가 남아 있어야 청커가 "청크는 페이지를 넘지 않는다"를 지킬 수 있고, 그래야 "이 청크는 몇 쪽인가"에 답이 하나입니다.

`ExtractedDocument`에는 **텍스트가 있는 세그먼트만** 담고, `page_count`는 따로 보존합니다. 이 조합이 세 상태를 구분합니다.

| 추출 결과 | 뜻 | 이후 처리 |
|---|---|---|
| 세그먼트 있음 | 정상 | 청킹으로 진행 |
| 세그먼트 0 · `page_count` ≥ 1 | 쪽은 있는데 텍스트 레이어가 없다 (스캔본) | `no_extractable_text` — OCR이 필요하다 |
| 세그먼트 0 · 쪽 없음 | 내용 자체가 없다 | `empty_document` — 파일이 잘못됐다 |

두 실패를 뭉개면 클라이언트는 OCR이 필요한 것인지 파일이 잘못된 것인지 알 수 없습니다. 판정 자체는 수집 서비스의 몫이고, 파서는 판정에 필요한 사실만 남깁니다.

같은 이유로 `PdfParser`는 **0바이트를 파싱 실패로 옮기지 않습니다.** PyMuPDF는 빈 스트림에 "파일이 깨졌다" 계열의 예외를 던지는데, 그대로 옮기면 빈 파일이 `document_parse_error`가 되어 위 표의 마지막 행과 뭉개집니다.

### 구현된 파서 두 개

| 파서 | 포맷 | 단위 |
|------|------|------|
| `TextParser` | `.txt` · `.md` | 문서 전체가 세그먼트 하나 |
| `PdfParser` | `.pdf` | 쪽마다 세그먼트 하나 (1-base 페이지 번호) |

**마크다운을 별도 파서로 두지 않았습니다.** 마크다운은 그 자체가 사람이 읽도록 설계된 포맷이라, 문법을 걷어내고 본문만 남기면 오히려 정보가 줍니다 — `## 배포 프로세스`가 `배포 프로세스`가 되면 그것이 제목이었다는 사실이 사라지고, 표는 열 구분이 사라져 행이 뭉갭니다. 검색 대상은 원문 그대로가 낫습니다.

**인코딩**은 BOM이 있으면 그것으로 확정하고, 없으면 UTF-8 → CP949 순으로 시도합니다. CP949는 한국어 문서 때문입니다. 순서를 뒤집으면 안 됩니다 — CP949는 UTF-8로 인코딩된 한글 바이트열도 뜻 없는 글자로 디코딩해내는 경우가 있어, 먼저 걸면 멀쩡한 문서가 깨진 글자로 저장됩니다. 어느 후보로도 읽히지 않으면 `errors="replace"`로 넘기지 않고 거절합니다. 깨진 글자가 색인되면 검색은 되는데 답변에 인용된 근거가 읽을 수 없는 문자열이 됩니다.

**개행은 `\n`으로 통일**합니다. 청커의 최우선 구분자가 `\n\n`이라, 윈도우에서 만든 문서의 `\r\n\r\n`을 그대로 두면 문단 경계에 걸리지 않아 같은 내용이 운영체제에 따라 다르게 잘립니다.

### PDF는 PyMuPDF — 교체 지점은 파일 하나

| 후보 | 성격 | 이번에 선택하지 않은 이유 |
|------|------|--------------------------|
| **PyMuPDF** (선택) | C 확장, 규칙 기반 추출 | — |
| Marker 등 모델 기반 | 딥러닝 레이아웃 분석 → 마크다운. 표·다단·수식 복원 품질이 높다 | 모델 가중치 다운로드와 추론 런타임이 추가로 필요해 "한 줄 실행"의 실패 지점이 늘고, 가중치가 GB 단위라 이미지가 배로 커진다. 샘플 문서(단순 텍스트) 범위에서 얻는 품질 이득은 거의 0이다 |
| pypdf | 순수 파이썬 | 추출 속도와 레이아웃 보존이 떨어진다. 다만 라이선스가 더 느슨하다(아래) |

**모델 기반 파서는 구현하지 않았습니다.** 교체 지점만 남겨 둔 상태이고, 이 문서 어디에서도 "지원한다"고 적지 않습니다.

교체 지점은 `adapters/parsers/pdf.py` **한 파일**입니다. 다른 파서로 바꾸려면 같은 프로토콜을 구현한 클래스를 만들어 `default_parsers()`에 등록하면 되고, `services/`·`core/`·`api/`는 한 줄도 바뀌지 않습니다. 프로토콜이 반환하는 `ExtractedDocument`가 쪽 단위 세그먼트라, 마크다운을 뱉는 파서도 "쪽별 텍스트"로 감쌀 수 있습니다.

**라이선스 주의**: PyMuPDF는 **AGPL-3.0**입니다. 평가용 비공개 리포 범위에서는 문제가 되지 않지만, 상용 배포로 간다면 pypdf(BSD) 교체가 선행되어야 합니다. 교체 비용이 위 한 파일로 국한된다는 점이 이 구조의 값입니다.

파서는 자기 경계에서 라이브러리 예외를 `DocumentParseError`로 바꿔 던집니다. 라우터까지 `pymupdf.FileDataError` 같은 것이 새면 계층 경계가 무의미해지고, 내부 예외 메시지가 응답에 노출됩니다. 원인은 로그로만 남깁니다.

### 지원 포맷 목록의 유일한 근거는 레지스트리다

`ParserRegistry`는 주입받은 파서 목록에서 "확장자 → 포맷 → 파서" 표를 만듭니다. 오류 응답에 싣는 지원 포맷 목록도 `DocumentFormat` 열거형이 아니라 **실제로 등록된 파서**에서 나옵니다. 열거형에서 만들면 파서를 뺀 구성에서도 그 포맷을 지원한다고 응답하게 되고, 그 응답은 허위입니다.

한 포맷에 파서가 둘 등록되면 조용히 덮어쓰지 않고 배선 시점에 실패합니다. 덮어쓰면 어느 파서가 쓰이는지가 등록 순서에 달리고, 실수가 기동이 아니라 업로드 시점에 드러납니다.

확장자가 없으면 내용을 스니핑해 포맷을 추측하지 않고 거절합니다. 추측이 틀리면 사용자가 의도하지 않은 파서가 돌아, 그 실패가 훨씬 설명하기 어려워집니다.

### PDF 테스트 픽스처는 코드로 만든다

바이너리 PDF를 리포에 커밋하지 않습니다. 무엇이 들어 있는지 diff로 볼 수 없고, 텍스트 레이어가 있는지 없는지를 파일 이름으로만 주장하게 되며, 내용을 바꾸려면 매번 외부 도구가 필요합니다. `tests/pdf_fixtures.py`의 `make_pdf(["1쪽 본문", BLANK_PAGE])`처럼 적으면 "2쪽이고 1쪽에만 텍스트가 있다"가 테스트에 그대로 드러납니다.

## 임베딩

### 모델은 `intfloat/multilingual-e5-small`

샘플 문서가 한국어라는 사실 하나가 후보 대부분을 떨어뜨립니다.

| 후보 | 판단 |
|------|------|
| `all-MiniLM-L6-v2` | 기각 — **영어 전용**입니다. 한국어 문서에서 검색이 사실상 동작하지 않습니다 |
| `paraphrase-multilingual-MiniLM-L12-v2` | 기각 — 다국어지만 입력 창이 **128 토큰**입니다. 한국어 600자 청크가 들어가지 않아 뒷부분이 **조용히 잘립니다.** 잘린 부분은 벡터에 반영되지 않으면서 청크 본문에는 남아, "저장은 됐는데 검색되지 않는 텍스트"가 생깁니다 |
| **`intfloat/multilingual-e5-small`** | **선택** — 다국어, 입력 창 512 토큰, 384차원, 118M 파라미터. 창이 청크 크기(600자 ≈ 400~450토큰)를 여유 있게 덮고 모델 크기가 이미지에 부담이 적습니다 |
| `BAAI/bge-m3` | 기각 — 품질은 가장 좋지만 GB 단위라 이미지 크기와 첫 로딩 시간이 감당되지 않습니다 |

### 역할 접두사는 어댑터 안에 갇혀 있다

e5 계열은 입력에 `passage: `(문서) / `query: `(질의) 접두사를 요구합니다. 이 규약은 **모델 고유의 것**이므로 상위 계층은 `embed_documents`/`embed_query` 두 메서드만 보고 접두사의 존재도 문자열도 모릅니다. 상위 계층이 접두사를 알면 모델 교체가 상위 계층까지 번집니다.

그 대가로 밖에서는 접두사가 실제로 붙었는지 확인할 길이 없어집니다. 그래서 통합 테스트가 어댑터를 거친 벡터와 `SentenceTransformer`를 직접 호출한 `"passage: " + 본문`의 벡터가 같은지 비교합니다.

### `signature` — 캡슐화를 유지한 채 서명 재료를 내보내는 통로

"같은 벡터를 낼 것인가"를 결정하는 사실 — 모델 식별자·차원·정규화 여부·접두사 규약 — 을 전부 아는 곳은 어댑터뿐입니다. 그중 접두사 규약은 방금 가둔 것이라, 상위 계층이 서명을 조립하려면 그 캡슐화를 깨야 합니다. 어댑터가 문자열 하나로 요약해 내보냅니다.

```
intfloat/multilingual-e5-small/384/l2norm/e5-prefix-v1
```

사람이 읽을 수 있게 두어, 서명이 달라졌을 때 무엇이 달라졌는지 로그에서 바로 보이게 합니다. 모델 식별자에 `/`가 들어가지만 필드 수가 고정(뒤에서 세 칸)이라 값이 섞이지 않습니다. 조직 접두사(`intfloat/`)를 떼지 않는 이유는 다른 조직의 동명 모델이 같은 서명을 받는 것을 막기 위해서입니다.

이 값은 `index_signature`의 재료입니다. 모델이나 정규화 방식을 바꾸면 서명이 달라지고, 재색인이 강제됩니다.

### 차원은 선언하고, 로딩할 때 확인한다

`dimension`·`max_input_tokens`는 **모델을 로딩하지 않고** 읽을 수 있어야 합니다. 색인 서명은 수집이 시작되기 전에 필요하고, 무엇보다 **선로딩이 실패한 뒤에도** 답할 수 있어야 합니다 — 모델을 못 올린 상태에서 차원을 물으면 또 실패하는 구조라면, 목록·삭제처럼 임베딩과 무관한 경로까지 함께 죽습니다.

그래서 어댑터가 `KNOWN_MODEL_PROFILES` 표로 모델별 차원과 입력 창을 선언합니다. **표에 없는 모델 이름은 기동을 막습니다.** 차원을 추측해 서명에 넣으면 서명이 거짓이 되고, 서명이 거짓이면 재색인 강제라는 목적 자체가 사라집니다. `chunk_strategy`에 미구현 전략 이름을 넣지 못하게 한 것과 같은 규율이고, 오류 메시지에 받아들여지는 값 목록이 실립니다.

선언의 대가는 선언이 틀릴 수 있다는 것입니다. 차원이 틀리면 서명이 거짓이 되고, 입력 창이 틀리면 토큰 가드가 상한을 넘겨 통과시킵니다. 둘 다 조용히 틀리는 종류라 **로딩이 성공한 그 자리에서** 실제 모델과 대조하고, 어긋나면 거기서 멈춥니다.

### 모델은 기동 시 미리 올린다 — 다만 기동 *조건*은 아니다

처음에는 **지연 초기화**였습니다(첫 임베딩 호출에서 로딩). "기동 조건을 늘리지 않는다"는 규약을 따른 결정이었지만, 그 방식은 비용을 없애는 게 아니라 **첫 업로드에게 청구합니다.** 평가자가 처음 눌러 보는 요청이 정확히 그 요청이고, 더 나쁜 것은 실패가 드러나는 시점입니다 — 가중치 부재, 선언한 차원·입력 창과 실물의 불일치, 디스크 부족이 전부 **첫 업로드에서 500**으로 나타납니다. 기동 시 한 번 올려 보면 그 전부가 로그 한 줄로 먼저 드러납니다.

선로딩의 범위는 **가중치 로딩 + 짧은 텍스트 1회 인코딩**입니다. 로딩만으로는 첫 `encode`에 남는 초기화 비용(연산 그래프 준비, 커널 워밍)이 그대로 남아 목적이 절반만 달성됩니다. 실제로 한 번 돌려 보면 "이 모델로 벡터가 나오는가"까지 기동 시점에 확인됩니다.

**실패는 기동을 막지 않습니다.** 경고를 남기고 계속 뜹니다. 근거는 둘입니다 — `openspec/specs/service-health/spec.md`의 "설정을 전혀 제공하지 않아도 기동에 성공한다"가 여전히 유효하고, 기동 정리(`reconcile_storage`)가 이미 같은 규율을 씁니다. 부수 작업의 실패가 서비스 자체를 못 뜨게 하면 안 됩니다.

그래서 **지연 로딩을 걷어내지 않고 백스톱으로 남깁니다.** 선로딩이 실패해도 첫 임베딩 호출이 다시 시도하므로 일시적 실패(디스크 경합, 느린 볼륨)는 스스로 회복됩니다. 선로딩은 "미리 해 두는 일"이지 "유일한 통로"가 아닙니다.

`warm_up()`은 **프로토콜**에 있습니다. 앱 팩토리는 `Embedder`만 알고 구현체를 모르는데, `isinstance`로 구체 어댑터를 확인해 부르면 배선이 구현체를 알게 되어 계층 규약이 깨집니다. 대역은 no-op으로 구현합니다.

**헬스는 모델 상태를 보고하지 않습니다.** 프로브는 캐시·벡터 스토어 둘 그대로입니다. "모델 로딩 중"을 불가용으로 보고하면, 목록·삭제·헬스가 멀쩡한데 서비스 전체가 불가용으로 보입니다.

대가는 기동 시간입니다. 가중치가 로컬에 없으면 기동이 다운로드만큼 길어집니다 — 컨테이너는 빌드 단계에서 굽기 때문에 이 경로가 없고, 도커 없이 로컬로 띄우는 경우에만 해당합니다.

인코딩과 토큰 계산이 서로 다른 워커 스레드에서 동시에 첫 호출될 수 있어 로딩은 잠금으로 감쌉니다 — 잠그지 않으면 모델이 두 벌 올라가 메모리가 두 배가 됩니다.

`count_document_tokens`·`count_query_tokens`만 동기입니다. 토크나이저 호출은 블로킹이고 첫 호출은 로딩까지 유발하므로, `DocumentParser.parse`와 같은 이유로 동기로 선언해 호출부가 오프로드를 의식하게 합니다. 인코딩은 반대로 async인데, 배치마다 호출되는 자리라 호출부가 매번 감싸면 중복만 늘기 때문입니다.

### 토큰 계산은 역할별로 둘이다

`count_document_tokens`/`count_query_tokens`가 `embed_documents`/`embed_query`와 짝을 이룹니다. 처음에는 `count_tokens` 하나였고 그 안에서 `passage: `를 붙였습니다 — 소비자가 수집의 토큰 가드뿐이라 이름에 역할이 없어도 문제가 없었지만, **이름이 이미 사실과 어긋나 있었습니다.** 검색이 같은 계산을 질의 경로에서 쓰기 시작하는 순간 그 어긋남이 버그가 됩니다.

토큰은 **실제로 인코딩되는 문자열 그대로** 셉니다 — 역할 접두사를 포함하고, 토크나이저가 붙이는 특수 토큰(`<s>`·`</s>`)까지 포함합니다. 본문만 세면 그 몫만큼 과소 계산되어 상한 바로 아래 입력이 조용히 잘립니다. 역할마다 접두사 길이가 다르므로(`passage: ` 2토큰 / `query: ` 3토큰) 하나로 뭉치면 둘 중 한쪽이 반드시 틀리고, 그 틀림은 **잘림으로만** 드러납니다. 접두사 문자열 자체는 여전히 어댑터 밖으로 나가지 않습니다 — 역할이 둘이라는 사실만 드러나는데, 그건 `embed_*`가 이미 드러내고 있습니다.

### 질의 길이 상한이 문자 수와 토큰 수 둘인 이유 — 역산 기록

질의에는 재분할이 성립하지 않습니다. 질문 하나를 반으로 잘라 각각 검색하면 그건 다른 질문입니다. 남는 선택지는 자르거나 거부하는 것뿐이고 **조용히 자르는 쪽이 더 나쁩니다** — 잘린 뒷부분이 검색에 반영되지 않는데 사용자는 전부 반영됐다고 믿습니다.

처음에는 문자 상한 하나로 하려 했습니다. 기본 모델의 토크나이저로 역산해 보니 성립하지 않습니다.

| 항목 | 실측값 |
|------|--------|
| 모델 입력 창 | 512 토큰 |
| 특수 토큰(`<s>`·`</s>`) | 2 |
| `query: ` 접두사 | 3 (`passage: `는 2) |
| **본문 가용 토큰** | **507** |
| 최악 비율 — 단일 문자 반복 | **5.016 토큰/문자** (U+331A 계열, 반복 길이에 따라 5.00~5.05) |
| 최악 비율 — 고비용 문자 혼합 | 3.353 토큰/문자 |
| 한국어 평문 질문 | 0.50 ~ 0.57 토큰/문자 |

최악 문자는 **CJK 호환 정사각 문자**입니다. 정규화에서 한 글자가 여러 글자로 펼쳐집니다(`㌚` → `キロメートル`). 반대로 어휘에 없는 문자를 길게 늘어놓는 공격은 오히려 쌉니다 — SentencePiece가 연속된 미지 문자를 `<unk>` 하나로 합쳐 버립니다.

따라서 **절대 안전한 문자 상한은 `507 / 5.016 ≈ 101자`입니다.** 그런데 이 숫자가 곧 문자 상한 단독 설계가 틀렸다는 증거입니다. 실제 한국어와 최악값의 비가 **약 9배**라, 101자로 막으면 51자짜리 정상 질문(27토큰)은 통과하지만 **512토큰 창의 11%만 쓰면서** 150자짜리 정상 질문을 거부합니다. 안전과 실용을 문자 수 하나로 동시에 만족시킬 수 없습니다.

그래서 둘로 나눕니다. **목적이 다르므로 중복이 아닙니다.**

| 상한 | 무엇을 막는가 | 값 | 판정 위치 |
|------|--------------|-----|----------|
| 문자 수 | 임의 길이 입력이 토크나이저에 들어가는 것 | 설정 `retrieval_max_query_chars`, 기본 1000 | API 계층, 토크나이저 이전 |
| 토큰 수 | **조용한 절단** | 임베더가 선언한 입력 창(512), 설정 아님 | 질의 벡터 계산 직전 |

임베딩 어댑터가 제공하는 것은 **계산**(`count_query_tokens`)과 **선언**(`max_input_tokens`)까지입니다. 두 가드를 실제로 거는 자리는 검색 엔드포인트(`POST /search`)입니다 — 문자 수는 토크나이저에 닿기 전에, 토큰 수는 질의 벡터를 계산하기 직전에 판정합니다.

**토큰 상한을 설정 항목으로 두지 않습니다.** 손으로 적을 수 있으면 실제 모델과 어긋난 값을 넣을 수 있고, 그 순간 가드가 보증하려던 것이 사라집니다 — `index_signature`를 설정으로 두지 않은 것과 같은 규율입니다. 모델이 선언한 `max_input_tokens`를 그대로 씁니다.

1000자는 정상 질문(대부분 100자 미만)의 열 배로, 정상 입력을 거부하지 않으면서 토크나이저에 들어가는 양을 묶습니다. 최악 문자로 1000자를 채우면 약 5,000토큰이 되지만, 그건 토큰 가드가 잡습니다.

계산이 실제 인코딩과 일치하는지는 **실물 모델로** 확인합니다(`tests/test_embedding.py`, 가중치가 없으면 스킵). `count_query_tokens`가 모델이 실제로 먹는 토큰 열(`tokenize`)의 길이와 같은지 단언합니다 — 이 일치가 깨지면 가드가 통과시킨 질의가 잘립니다.

### 테스트는 페이크로 돈다 — 통합 테스트 하나만 예외

수집 테스트가 검증하는 것은 **저장 결과의 모양**입니다(청크 수, 위치 정보, 리비전 교체, 오류 경로). 벡터의 *의미*는 하나도 검증하지 않으므로 실제 모델을 돌려도 단언이 강해지지 않습니다. 반면 비용은 큽니다 — 가중치가 캐시되지 않은 환경에서 `pytest` 한 줄이 수백 MB 다운로드에 묶입니다.

그래서 `tests/stubs.py`의 `FakeEmbedder`(텍스트 해시 기반 결정론적 벡터)를 주입합니다. `signature`·`dimension`을 생성 시점에 지정할 수 있는데, 이는 "모델 정체성이 바뀌면 재색인이 강제된다"를 **실제 모델 없이** 재현하는 유일한 수단입니다.

실물이 정말로 붙는지는 `tests/test_embedding.py`의 통합 테스트가 확인합니다. 가중치가 로컬에 없으면 스킵하므로 네트워크 없는 환경에서도 `pytest` 한 줄이 통과하고, 컨테이너 이미지에는 가중치가 구워져 있어 거기서는 실제로 실행됩니다.

## 저장소 — 문서 레지스트리와 벡터 스토어

저장소가 둘입니다. 벡터 스토어는 **검색 단위**(청크와 벡터)를, 레지스트리는 **문서 한 건의 현재 상태**를 보관합니다. 벡터 스토어는 별도 컨테이너(Chroma 서버)이고, 레지스트리는 API 프로세스 옆의 SQLite 파일입니다.

### 임베디드에서 서버 모드로 — 무엇이 바뀌었나

처음에는 Chroma를 **임베디드 퍼시스턴트 모드**로 썼습니다. 컨테이너를 늘리지 않고 볼륨 하나로 영속화되어 "한 줄 실행"이 단순해진다는 이유였습니다. 지금은 **서버 모드**입니다.

바뀐 것과 그 대가를 그대로 적습니다.

| | 임베디드 (이전) | 서버 (현재) |
|---|---|---|
| 배치 | API 프로세스 안 + 마운트된 경로 | 별도 컨테이너, HTTP |
| "도달 가능"의 의미 | 저장 경로에 쓸 수 있는가 | 서버에 닿는가 |
| 저장소 수명 | API 컨테이너와 함께 | API 재배포·재빌드와 무관 |
| 스케일 | 파일 락 때문에 API 워커 1개 강제 | 저장소는 여러 클라이언트를 받는다 |
| 대가 | — | 컨테이너 하나 증가, **실물 어댑터 테스트에 서버 필요** |

**API 워커는 여전히 1개입니다.** 임베디드일 때는 파일 락 때문이었고, 지금은 같은 문서에 대한 요청을 프로세스 안의 `asyncio.Lock`으로 직렬화하기 때문입니다. 제약의 근거가 바뀌었을 뿐 결론은 그대로이며, 늘리려면 외부 잠금이 먼저입니다 — 서버 모드 전환만으로 다중 워커가 열리지 않는다는 뜻입니다.

테스트 쪽 대가가 가장 큽니다. 임베디드일 때는 임시 디렉터리에 실물을 띄워 `pytest` 한 줄로 전부 돌았지만, 이제 `tests/test_vector_store.py`는 **서버가 없으면 건너뜁니다**. 대역으로 대체하지 않은 이유는 그 파일이 확인하려는 것이 "우리 메타데이터·필터·id 규약이 *실제 Chroma*에서 성립하는가"이기 때문입니다 — 대역으로 바꾸는 순간 확인 대상이 사라집니다. 서버까지 포함해 돌리는 명령은 `docker compose run --build --rm test`입니다 — `depends_on`이 벡터 스토어를 함께 띄웁니다.

> **기존 볼륨은 그대로 옮겨지지 않습니다.** 임베디드 모드가 쓰던 볼륨을 서버에 그대로 붙이면 서버가 그 데이터베이스를 읽어 들이는데, 그 안의 컬렉션은 당시 모델의 차원으로 고정돼 있어 첫 쓰기가 실패합니다. 전환 시 볼륨을 비웁니다(`docker compose down -v`). 이 change 이전의 데이터는 재업로드로만 복구됩니다.

### 컬렉션은 벡터 차원마다 나뉜다

**Chroma는 컬렉션 하나에 차원 하나만 허용하고, 그 차원은 컬렉션을 비운 뒤에도 남습니다.** 실측했습니다 — 4차원으로 쓴 컬렉션에서 모든 청크를 지운 뒤 8차원을 넣으면 `Collection expecting embedding with dimension of 4, got 8`으로 거절됩니다.

컬렉션 이름이 `document_chunks_d384`처럼 차원을 달고 있는 이유가 이것입니다. 이름이 하나뿐이면 **차원이 다른 모델로 바꿨을 때 복구가 불가능해집니다.** 기동 정리가 구 서명 청크를 전부 지우고 `stale`로 표시해도, 재업로드가 같은 컬렉션에 새 차원을 넣으려다 영구히 실패합니다 — "재업로드하면 복구된다"는 약속이 그 순간 거짓이 됩니다.

차원이 같은 두 모델은 컬렉션을 공유합니다. 그건 문제가 되지 않습니다 — 모델이 달라지면 `index_signature`가 달라지고, 삭제와 집계가 그 값으로 걸러내기 때문입니다. 여기서 나누는 것은 **Chroma가 강제하는 제약 하나**뿐입니다.

대가는 버려진 컬렉션입니다. 차원을 바꾸면 이전 컬렉션이 남고, 기동 정리는 현재 컬렉션만 보므로 그것을 회수하지 않습니다. 검색에 섞이지는 않지만 디스크는 차지하며, 정리는 수동입니다.

### 왜 레지스트리를 따로 두는가

`openspec/config.yaml`의 스택 표에 없던 구성 요소이므로 추가 이유를 남깁니다.

필요한 것은 "이 문서의 지금 유효한 리비전은 무엇인가"에 대한 **단일 답**입니다. 그 답이 하나여야 리비전 교체가 원자적이 됩니다.

| 대안 | 기각 이유 |
|------|----------|
| 벡터 스토어 메타데이터에서 유도 | "현재 리비전"이 여러 청크에 흩어진 값에서 *추론*됩니다. 교체 도중에는 두 리비전의 청크가 공존하므로 **그 순간 답이 둘**입니다. 청크 전체의 메타데이터를 한 번에 바꾸는 원자적 연산도 없습니다 |
| 캐시 저장소(Redis)에 보관 | 캐시는 축출과 TTL이 있는 저장소입니다. 문서 목록이 조용히 사라질 수 있는 자리에 진실 원천을 두면 안 됩니다 |
| 별도 관계형 DB 컨테이너 | 컨테이너가 하나 늘고 "한 줄 실행"의 실패 지점이 늘어납니다. 저장할 데이터는 문서 수십 건입니다 |
| **SQLite 파일** (선택) | 표준 라이브러리라 의존성·이미지 크기 증가가 **0**입니다. 트랜잭션이 있어 리비전 교체가 원자적이고, 벡터 스토어와 같은 볼륨에 두면 영속화 경로가 하나입니다 |

레코드는 `document_id`(PK)·`filename`·`format`·`revision`·`index_signature`·`index_status`·`chunk_count`·`byte_size`·`ingested_at`입니다. 재커밋은 값을 쌓지 않고 **교체**합니다 — 쌓이면 단일 답이라는 전제가 무너집니다.

`index_status`는 `indexed` | `stale` 둘뿐이고 `stale`은 기동 정리만 붙입니다. `chunk_count: 0` 하나로는 "빈 문서였나, 색인 구성이 바뀐 건가"를 구분할 수 없기 때문입니다.

스키마는 **최초 접근 시** 만듭니다. 마이그레이션 도구도 준비 스크립트도 두지 않습니다 — 평가자가 새 볼륨에서 한 줄로 시작할 수 있어야 합니다.

연결은 연산마다 새로 엽니다. 각 연산이 서로 다른 워커 스레드에서 실행되는데 `sqlite3.Connection`은 스레드 간 공유가 안전하지 않고, 연결을 들고 다니면 스레드 소유권을 직접 관리해야 합니다. 문서 수십 건 규모에서 연결 비용은 무시할 수 있습니다.

### 청크 id에 색인 서명이 들어가는 이유

```
id       = {document_id}:{revision[:12]}:{index_signature[:8]}:{chunk_index:05d}
metadata = document_id, revision, index_signature, filename, format,
           chunk_index, char_start, char_end, page(PDF 만)
본문      = 청크 텍스트
```

**재색인은 `revision`이 그대로인 채 일어납니다.** id가 `document_id:revision:index`뿐이면 새 청크가 이전 청크의 id를 그 자리에서 덮어써, "새로 쓰고 → 커밋 → 지우기" 순서가 성립하지 않습니다. 쓰는 도중에 이미 이전 벡터가 사라지고, 실패해도 되돌릴 원본이 없습니다. 서명을 붙이면 두 세대가 서로 다른 id를 가져 순서가 그대로 지켜집니다. 이 성질은 `tests/test_vector_store.py`가 "같은 리비전의 두 서명이 공존한다"로 고정합니다.

`page`는 값이 있을 때만 키를 넣습니다. Chroma가 널 메타데이터를 허용하지 않고, 센티널 값(-1 같은)을 쓰면 소비자가 그 규약을 알아야 합니다.

### 삭제는 세 축으로 좁힌다

`delete_document(document_id, revision=…, index_signature=…)` 하나가 세 경우를 덮습니다 — 이전 리비전 정리(교체), 이전 서명 정리(재색인), 문서 삭제(조건 없음). 지운 개수를 돌려주므로 호출자가 "정말 지워졌는가"를 확인할 수 있습니다. 라이브러리의 조건 삭제는 개수를 알려주지 않아, id를 먼저 모은 뒤 지웁니다.

### 컬렉션에 임베딩 함수를 붙이지 않는다

임베딩 함수를 컬렉션에 붙이지 않습니다(`embedding_function=None`). 비워 두면 라이브러리가 자기 기본 모델로 가중치를 받으러 나가고, 그러면 **색인 서명이 가리키는 모델과 실제로 벡터를 만든 모델이 어긋납니다.** 거리 지표는 코사인으로 명시합니다 — 벡터를 항상 정규화해 넣기도 하고, 명시하지 않으면 라이브러리 기본값이 바뀔 때 거리 의미가 조용히 달라집니다.

같은 이유로 **`query`는 벡터를 받습니다, 질의 문자열이 아니라.** 문자열을 받게 하면 어댑터가 임베더를 알아야 하고, 저장소가 임베딩 모델에 의존하는 순간 둘 중 하나를 갈아끼울 때 다른 하나가 딸려 옵니다. `add_chunks`가 텍스트가 아니라 벡터를 받는 것과 같은 규율입니다.

### 검색 대상 필터는 질의 **이전에** 건다

```python
query(embedding, *, top_k, versions: Sequence[StoredIndexVersion]) -> list[ScoredChunk]
```

`versions`는 `(document_id, revision, index_signature)` 삼중항 목록이고, 어댑터는 이것을 `$or`/`$and` 필터로 조립해 **질의와 함께** 저장소에 넘깁니다. 무엇이 지금 유효한지는 레지스트리를 아는 서비스만 답할 수 있으므로, 목록을 만드는 쪽은 검색 서비스입니다.

저장소에는 **검색되면 안 되는 청크가 남아 있을 수 있습니다** — 되돌리기가 닿지 못한 실패, 확정 뒤의 이전 세대 정리 실패. 그 잔여물이 무해하다는 근거가 이 필터 하나뿐이라, 필터는 최적화가 아니라 지켜야 할 계약입니다.

기각한 대안:

| 대안 | 왜 안 되는가 |
|------|-------------|
| **사후 필터** (상위 K를 받고 걸러낸다) | 걸러낸 만큼 결과가 줄어 `top_k`가 의미를 잃고, 잔여 청크가 많으면 유효한 청크가 K 밖으로 밀려 **결과가 통째로 빌 수 있습니다.** 검색되면 안 되는 청크가 결과 수에 영향을 주는 것 자체가 계약 위반입니다 |
| **`index_signature`만으로 필터** | 리비전 축이 빠집니다. 교체는 재색인이 아니라 내용 변경이라 이전 리비전 청크의 서명이 현재와 같고, 그대로 통과합니다 — 사용자가 지운 문장이 인용됩니다 |
| **필터 없이 전체 검색** | 잔여 청크가 결과에 섞여, 수집이 잔여물을 허용한 전제가 거짓이 됩니다 |

**빈 목록은 "대상 없음"이지 "조건 없음"이 아닙니다.** 저장소 필터 API에서 "조건 없음 = 전체"가 흔한 관습이라 실수하기 쉬운 자리인데, 여기서 뒤집히면 유효한 문서가 하나도 없는 상태에서 **잔여 청크만 남은 저장소를 전체 탐색해 사용자가 지운 문서가 검색됩니다.** 어댑터는 빈 목록을 받으면 저장소를 아예 건드리지 않고 빈 결과를 돌려주며, `tests/test_vector_store.py`가 "청크를 저장해 둔 채 빈 목록으로 질의하면 결과가 비어 있다"로 이 성질을 고정합니다.

비용은 질의당 필터 표현식의 크기입니다 — 문서 수에 비례해 커집니다. 이번 규모(수십 건)에서는 문제가 아니고, 재고 시점은 목록 조립이 검색 지연에서 눈에 띄기 시작할 때입니다.

### 거리는 어댑터가 유사도로 바꿔 내보낸다

저장소는 코사인 **거리**(작을수록 가깝다)를 돌려주고, 어댑터가 `유사도 = 1 - 거리`로 바꿔 `[0, 1]`로 클램프한 뒤 `ScoredChunk.score`에 싣습니다. 상위 계층은 "클수록 가깝다"만 압니다.

거리를 그대로 흘리면 "점수가 낮은 것이 좋다"는 규약이 API 응답까지 새고, 저장소를 바꿔 거리 지표가 달라지면 **응답의 의미가 조용히 뒤집힙니다.** 임계값 비교 방향도 함께 뒤집히는데 타입은 그대로라 컴파일도 테스트도 통과합니다.

**클램프에는 대가가 있습니다.** 정규화된 벡터의 코사인 유사도는 이론상 음수가 될 수 있고, 그런 값이 나온다는 것은 모델 교체 등으로 점수 분포가 이동했다는 신호입니다. 조용히 0으로 만들면 그 신호가 사라지므로, **클램프가 실제로 발동하면 경고 로그를 남깁니다.** 범위 자체는 `ScoredChunk`가 생성 시점에 다시 검증해, 변환이 어긋나면 상위 계층의 임계값 비교를 무의미하게 만들기 전에 값 객체에서 걸립니다.

### 오프로드는 어댑터 안에서 한다

레지스트리도 벡터 스토어도 파일 I/O라 블로킹입니다. 프로토콜은 async로 선언하고 구현체 안에서 스레드풀로 내보냅니다 — 파서와 **반대 방향**인데 이유가 다릅니다. 파서는 CPU 바운드에 수백 ms~초라 호출부가 오프로드를 의식해야 실수를 막을 수 있고, 저장소는 짧은 I/O라 호출부가 매번 감싸면 중복만 늘어납니다.

같은 경계에서 라이브러리 예외를 `StorageUnavailable`로 바꿉니다. `sqlite3.OperationalError`가 라우터까지 새면 계층 경계가 무의미해지고 내부 경로가 응답에 노출됩니다.

### `add_chunks`는 원자적이지 않다 — 계약에 적지 않는 이유

벡터 스토어의 배치 쓰기에는 트랜잭션이 없습니다. 프로토콜에 "원자적이어야 한다"고 적어도 **구현이 지킬 수단이 없습니다.** 지킬 수 없는 약속을 계약에 적는 대신, 교체 순서를 아는 서비스가 되돌리기를 책임집니다 (아래 "문서 수집 파이프라인").

## 어휘 색인

벡터 검색만으로는 **문서에 글자 그대로 적힌 짧은 식별자**(`P1`, `v2`, `200만원`)를 문맥 없이 물었을 때 닿지 못합니다. 임베딩은 의미를 잡지만 그런 토큰은 의미가 거의 없고 표기가 전부입니다. 그래서 BM25 어휘 검색을 두 번째 retriever로 두고, 두 목록을 RRF로 합칩니다(`core/fusion.py`).

### 저장소는 SQLite FTS5 — 파일 하나, 컨테이너 0개

FTS5는 파이썬 표준 `sqlite3`에 컴파일되어 들어 있고 `bm25()` 순위 함수를 내장합니다. 저장소는 `./data/lexical.sqlite3` 파일 하나이며, 레지스트리(`./data/registry.sqlite3`)가 이미 같은 방식으로 붙어 있어 볼륨·백업·삭제 절차가 늘지 않습니다.

| 대안 | 왜 아닌가 |
|------|-----------|
| Chroma의 `where_document` | `$contains` 부분 문자열 **필터**일 뿐 순위가 없습니다. 어휘 검색이 아닙니다 |
| Elasticsearch / OpenSearch | 컨테이너 + JVM 힙. 샘플 문서 2건 규모에 대한 과잉이고 기동 시간이 "한 줄 실행"을 위협합니다 |
| `rank_bm25` 인메모리 | 영속성이 없어 기동마다 전 청크 본문을 Chroma에서 끌어와 재구축해야 합니다. 워커마다 사본이 생기고 메모리가 코퍼스 크기에 비례합니다 |
| 레지스트리와 **같은** 파일 | 한 트랜잭션으로 묶을 수 있을 것 같지만 착시입니다 — 벡터 쓰기는 다른 프로세스라 어차피 원자성이 성립하지 않습니다. 얻는 것 없이 레지스트리 파일에 검색용 테이블이 섞입니다 |

FTS5 테이블은 외부 콘텐츠 없는 독립 테이블이고, 삼중항·`chunk_index`·본문·출처는 전부 `UNINDEXED` 컬럼입니다. 색인되는 것은 토큰 문자열 컬럼 하나뿐입니다. 대상 집합 필터는 벡터 쪽과 같은 삼중항 `WHERE` 조건이고, 동기 API라 `asyncio.to_thread`로 내보냅니다 — Chroma 어댑터와 같은 규약입니다.

FTS5가 꺼진 파이썬 빌드도 있을 수 있어 기동 시 `sqlite_compileoption_used('ENABLE_FTS5')`로 확인합니다. **없어도 기동을 세우지 않고** 경고만 남깁니다 — 밀집 검색은 그대로 돌고, 어휘 색인은 첫 사용에서 `StorageUnavailable`로 드러납니다. 실행 가능성이 런타임 빌드 옵션에 묶이는 쪽이 더 나쁩니다.

### `bm25()`의 부호는 어댑터가 뒤집는다

FTS5의 `bm25()`는 관련도가 높을수록 **더 작은 음수**를 돌려줍니다(내부 BM25 값에 -1을 곱한 값). 정렬은 `ORDER BY bm25(...) ASC`, 노출 점수는 `-bm25(...)`입니다.

**이 변환을 빠뜨리면 오류가 나지 않고 순위만 뒤집힙니다.** 원값을 그대로 실으면 점수가 전부 음수라 값 객체에서 걸리지만, 원값에 `DESC`를 걸면 아무 데서도 걸리지 않고 가장 안 맞는 청크가 1위가 됩니다. 그래서 `tests/test_lexical_index.py`의 「점수는 음수가 아니고 앞이 더 크다」가 이 파일에서 가장 값비싼 단언입니다. 뒤집은 값이 음수가 되지 않는 것은 fts5가 `idf <= 0`을 `1e-6`으로 눌러 두기 때문입니다.

### 토큰화는 우리가 한다 — 원 토큰과 어근을 둘 다 색인한다

FTS5 기본 토크나이저(`unicode61`)는 공백·기호로만 자릅니다. 한국어에서 이것은 어절 단위 색인을 뜻하고, `"교육비는"`으로 물으면 `"교육비"`를 담은 청크에 **닿지 않습니다.** 오류 없이 그냥 안 찾습니다.

그래서 쓰기와 읽기 양쪽에서 같은 순수 파이썬 토크나이저(`core/lexical.py`)를 돌려 공백으로 이어 붙인 토큰 문자열을 넣고, 질의도 같은 토큰들의 `OR`로 만듭니다. 규칙은 넷입니다.

1. **정규화** — NFKC 후 소문자화. 전각·반각과 대소문자 차이가 여기서 사라집니다.
2. **경계 분할** — 한글 / 라틴·숫자 경계에서 자릅니다. 라틴·숫자 연속(`P1`, `v2`)은 쪼개지 않습니다.
3. **혼합 토큰 보존** — `200만원`은 원형과 조각(`200`, `만원`)을 **모두** 색인합니다. 원형만 넣으면 조각으로 못 찾고, 조각만 넣으면 `200`의 변별력이 사라집니다.
4. **접미 벗기기** — 조사·어미를 최장 일치로 한 번 벗겨 **원 토큰과 어근을 함께** 넣습니다. 남는 어근이 2글자 미만이면 벗기지 않습니다.

원형과 어근을 둘 다 넣는 것이 요점입니다. 어근만 넣으면 벗기기가 과하게 동작한 경우(`"수도"` → `"수"`)에 원문이 사라지고, 원형만 넣으면 조사 붙은 질의가 닿지 않습니다. 둘 다 있으면 잘못 벗겨도 원형이 살아 있어 최악이 "재현율이 조금 는다"로 끝납니다.

FTS5 쪽은 `tokenize = 'unicode61 remove_diacritics 0'`입니다. 우리 토큰은 이미 정규화되어 들어가므로, fts5가 그것을 다시 손대지 않아야 `fts5vocab`의 term이 우리 토큰과 글자 그대로 같아집니다 — 아래 문서 빈도 조회가 그 동일성에 기댑니다.

**형태소 분석(`konlpy`·`mecab`)은 쓰지 않습니다.** JVM 또는 C 확장과 사전 데이터를 요구해 이미지가 수백 MB 늘고 빌드가 플랫폼을 탑니다. **`trigram` 토크나이저도 아닙니다** — 한국어 부분 일치는 잘 되지만 3글자 미만 질의어를 아예 검색할 수 없고(`P1`이 검색 불가), 어휘 검색을 넣는 첫 번째 이유가 짧은 식별자라 탈락했습니다.

`TOKENIZER_VERSION`과 접미 목록은 `derive_index_signature`의 재료입니다. 규칙을 고치면 서명이 달라지고 기존 문서는 `stale`이 되어 재업로드를 요구합니다 — 이미 있는 기동 정리 경로가 그대로 처리합니다.

### 변별력 없는 겹침만으로는 결과에 오르지 않는다

이 가드가 없으면 문서와 아무 상관없는 일상 질문이 `"오늘"`·`"어때"` 같은 흔한 토큰 하나로 청크를 끌어올립니다. 그 청크는 어휘 목록 1위가 되어 융합에 정상 입력으로 들어가고, 근거 없는 질문에 근거가 붙습니다. 관련성 판정은 융합 **이전에** 각자의 단위로 끝나야 합니다.

BM25 원점수에는 하한을 걸 수 없습니다 — 값이 코퍼스 크기와 질의 길이에 따라 몇 배씩 움직여서 고정 임계값이 의미를 갖지 않습니다. 대신 **토큰 하나하나의 희소도**를 재고, 질의 토큰 중 충분히 드문 것이 하나라도 겹치는 청크만 남깁니다.

```
idf(t)    = ln(1 + (N - df(t) + 0.5) / (df(t) + 0.5))    # BM25 표준, df=0 에서도 유한
rarity(t) = idf(t) / idf(df=0)                            # 같은 코퍼스의 최대 IDF 로 정규화
통과 조건  = max{ rarity(t) : t 가 이 청크에 있는 질의 토큰 } >= APP_LEXICAL_MIN_TOKEN_RARITY
```

`df`는 `fts5vocab` 가상 테이블에서 읽고 질의당 토큰 수만큼만 조회합니다. 정규화가 하한을 코퍼스 크기에서 뗍니다 — `idf` 원값은 `N`이 커지면 같은 비율의 `df`에서도 함께 커져 고정 임계값이 뜻을 잃습니다. 이 판정에 걸려 결과가 하나도 남지 않는 것은 오류가 아니라 빈 목록입니다.

**처음에는 커버리지 비율**(매칭된 토큰의 IDF 합 ÷ 전체 질의 토큰의 IDF 합)**로 설계했고, 구현 중 실측에서 두 군데가 깨져 위 규칙으로 바꿨습니다.**

- 비율은 **토큰 하나짜리 질의를 원리적으로 걸러내지 못합니다** — 분자와 분모가 같아 항상 `1.0`입니다. `"승인"` 하나로 물으면 그 토큰이 아무리 흔해도 통과합니다.
- 토크나이저가 원형과 어근을 둘 다 내므로 질의 하나가 토큰 여럿이 되고, 그중 색인에 없는 표층형·의문사(`교육비는`, `얼마인가요`)가 `df=0`이라 **최대 IDF를 받아 분모를 부풀립니다.** `sample-docs/` 두 건(200자 청킹, `N=10`)에서 `"교육비는 얼마인가요"`의 커버리지가 **0.244**로 하한 `0.3`에 못 미쳐, 조사 붙은 질의가 빈 목록이 됐습니다.

### 하한 `0.3`의 실측

`sample-docs/` 두 건을 배포 구성(recursive, 200자/40자 겹침)으로 색인하면 `N = 10`, 최대 IDF = `ln(22) ≈ 3.091`입니다. 희소도는 `df`의 함수라 코퍼스 하나에 표 하나가 나옵니다.

| `df` | 1 | 2 | 3 | 4 | 5 | 8 | 10 |
|---|---|---|---|---|---|---|---|
| `rarity` | 0.645 | 0.479 | 0.370 | 0.289 | 0.224 | 0.083 | 0.015 |

하한별로 네 회귀 질의(`retrieval` 스펙)와 무관 질의를 재면:

| 하한 | 교육비 | 재택근무 | 코드리뷰 | 장애대응 | `"오늘 서울 날씨 어때?"` |
|---|---|---|---|---|---|
| 0.2 | 1건 | 3건 | 3건 | 2건 | 0건 |
| **0.3** | **1건** | **3건** | **3건** | **2건** | **0건** |
| 0.4 | 1건 | 3건 | **0건** | 2건 | 0건 |
| 0.5 | 1건 | **0건** | **0건** | 1건 | 0건 |

`0.4`부터 코드리뷰 질의(`승인`·`pr`이 `df=3`)가 죽고 `0.5`에서는 재택근무까지 죽습니다. `0.2`는 `0.3`과 결과가 같아 얻는 것이 없습니다. 그래서 **`0.3`**이고, 이 값은 `N=10`에서 `df >= 4`(청크의 40%)를 "흔하다"로 판정하는 지점입니다. 무관 질의가 모든 하한에서 0건인 것은 네 토큰이 전부 색인에 없어 `MATCH` 자체가 걸리지 않기 때문입니다 — 가드가 실제로 일하는 것은 「흔한 토큰만 겹치는 청크」 쪽이고, 그 성질은 `tests/test_lexical_index.py`가 흔한 토큰을 12개 청크 전부에 심어 확인합니다.

**표본이 작다는 것은 그대로 적어 둡니다.** `N=10`에서는 `"수"`(`df=2`)조차 희소도 `0.479`라 드문 토큰으로 판정됩니다. 코퍼스가 커지면 그런 토큰의 `df`가 함께 올라 자연히 걸러지지만, 지금 값은 문서 두 건으로 잰 값입니다. 재는 절차는 위 표 그대로이고, 값을 바꿀 때 코드는 손대지 않습니다(`APP_LEXICAL_MIN_TOKEN_RARITY`).

### 어휘 색인의 수명은 벡터와 같은 코드 경로에 얹는다

`LexicalIndex` 프로토콜은 `VectorStore`와 **같은 축**을 씁니다 — `add_chunks` / `delete_document` / `count_chunks` / `list_stored_versions`의 시그니처가 임베딩 인자만 빼고 같습니다. 축이 어긋나면 재색인(같은 `revision`, 다른 `index_signature`)에서 한쪽만 지워지고, 어휘 검색은 지워진 세대의 문장을 계속 근거로 내놓습니다.

`search`만 다릅니다. BM25 점수는 코사인 유사도와 척도가 달라 `[0, 1]`에 들지 않으므로 `ScoredChunk`가 아니라 `RetrievedChunk`(`native_score`에 범위 불변식이 없는 값 객체)를 돌려줍니다. 척도가 다른 점수를 `score` 자리에 담으면 그 불변식이 뜻을 잃습니다.

## 문서 수집 파이프라인

```
파서 선택 → 파싱(스레드풀) → 청킹 → 토큰 가드 → 배치 임베딩·벡터 쓰기·어휘 쓰기 → 커밋 → 이전 청크 정리
                                                    └ 실패하면 같은 요청 안에서 양쪽을 되돌린다
```

서비스는 어댑터의 **프로토콜만** 압니다. 파서 레지스트리·임베더·벡터 스토어·어휘 색인·문서 레지스트리를 전부 생성자로 주입받으므로, 구현을 갈아끼워도 이 파일은 바뀌지 않습니다. 배선은 앱 팩토리 한 곳이며 `create_app(parsers=…, embedder=…, vector_store=…, lexical_index=…, registry=…)`가 그 교체 지점입니다.

### 두 색인은 한 트랜잭션의 두 면이다

청크는 벡터 스토어와 어휘 색인에 **같은 삼중항으로** 들어갑니다. 두 색인이 어긋나도 오류는 나지 않습니다 — 검색은 계속 `200`을 내고, 다만 retriever마다 다른 세대의 문장을 근거로 올립니다. 융합은 그 둘을 정상 입력으로 받으므로 **한 응답 안에 두 세대가 섞입니다.**

원자성을 저장소에서 얻을 수 없으므로(서로 다른 프로세스입니다) 순서와 되돌리기로 만듭니다 — 이미 벡터 하나에 대해 세워 둔 그 순서입니다. 쓰기는 배치마다 **벡터 다음에 어휘**이고, 되돌리기와 이전 세대 정리는 두 색인을 **각각의 `try`로** 지웁니다. 한 번의 `try`로 묶으면 벡터 되돌리기가 실패했을 때 어휘 쪽이 손도 대지 못한 채 남아, 응답 시점의 "양쪽 0개"가 깨집니다.

기동 정리는 두 색인의 `list_stored_versions()` **합집합**을 봅니다. 한쪽에만 남은 잔여물이 가장 흔한 어긋남인데, 교집합으로 보면 그것이 영원히 회수되지 않습니다.

### 하나의 서명이 두 색인을 함께 지배한다

토큰화 구성을 별도 서명으로 두지 않고 `index_signature`에 합쳤습니다. 정밀하게 하려면 어휘 색인용 서명을 따로 두어야 하고(임베딩 모델을 바꿔도 어휘 색인은 다시 만들 필요가 없습니다), 그 순간 문서 하나가 "밀집으로는 검색 가능하고 어휘로는 stale"인 상태를 가질 수 있게 됩니다. 그러면 검색의 대상 집합이 retriever마다 갈라지고, 기동 정리도 판정도 두 벌이 됩니다.

대가는 임베딩 모델을 바꿀 때 어휘 색인도 함께 다시 만들어야 한다는 것이고, 그 비용은 재업로드 한 번입니다. 검색 가능성이 두 축으로 쪼개지는 비용보다 훨씬 쌉니다.

### 순서가 "새로 쓰고 → 커밋 → 지우기"인 이유

역순(먼저 지우고 새로 쓰기)을 쓰면 중간 실패 시 문서가 **통째로 사라집니다.** 답할 수 있던 질문에 답할 수 없게 되는 것이, 잔여 청크가 잠시 남는 것보다 훨씬 나쁩니다.

| 실패 지점 | 결과 |
|---|---|
| 파싱·청킹 | 저장소 무변경. 이전 리비전 그대로 |
| 임베딩·청크 쓰기 | 같은 요청 안에서 새 리비전 청크를 되돌리고 `503 storage_unavailable`. 이전 리비전 그대로 |
| 커밋 | 위와 동일. 커밋이 성립하는 순간이 곧 교체가 확정되는 순간입니다 |
| 되돌리기마저 실패 | 응답은 동일하게 `503`, 로그에 경고. 잔여 청크는 레지스트리가 가리키지 않아 검색 대상이 아니며 다음 기동의 정리가 회수합니다 |
| 이전 청크 정리 | **교체는 이미 성립.** 요청은 성공하고 경고만 남습니다 — 이미 성립한 교체를 되돌릴 수 없으면서 호출자만 혼란스러워집니다 |

되돌리기를 어댑터가 아니라 **서비스**에 둔 이유는 위의 "`add_chunks`는 원자적이지 않다"와 같습니다. 배치로 나뉘면서 부분 기록의 폭이 커졌으므로 이 되돌리기는 선택이 아닙니다.

이전 세대를 지울 때는 **축을 하나만** 고릅니다. 내용이 바뀌었으면 이전 `revision` 전체를(그 리비전의 서명이 무엇이든), 내용이 같으면(재색인) 이전 `index_signature` 전체를 지웁니다. 정확한 짝만 지우면 다른 축에 남은 이전 세대가 살아남습니다.

### `unchanged` 단축의 조건은 둘 다다

`revision`만 보고 단축하면 모델이나 청킹을 바꾼 뒤 같은 파일을 다시 올려도 **아무 일이 일어나지 않습니다.** 구 구성으로 만든 벡터가 그대로 남고 사용자는 재색인했다고 믿습니다. 그래서 조건은 `revision`과 `index_signature` 둘 다입니다.

| 상황 | `status` |
|---|---|
| 처음 보는 문서 | `created` |
| 내용이 다름 | `replaced` (+ `previous_revision`) |
| 내용은 같고 색인 구성이 다름 | `reindexed` |
| 둘 다 같음 | `unchanged` — 파싱도 임베딩도 하지 않습니다 |

`reindexed`를 `replaced`로 뭉개지 않는 이유는 재색인이 `revision`이 그대로인 채 일어나기 때문입니다. `previous_revision`이 현재 `revision`과 같아져 응답이 자기모순이 됩니다.

### 토큰 가드 — 조용한 절단을 막는다

청크 크기는 **문자 기준**입니다(계층 규칙상 `core/`가 토크나이저를 알 수 없습니다). 그래서 토큰 수를 보장하지 못하고, 상한을 넘는 청크를 그대로 임베딩에 넘기면 뒷부분이 조용히 잘립니다 — 잘린 텍스트는 벡터에 반영되지 않으면서 청크 본문에는 남아, **저장은 됐는데 검색되지 않는 텍스트**가 생깁니다.

임베딩 직전에 `count_document_tokens`로 확인하고, 넘는 청크는 문자 크기를 반씩 줄여가며 다시 쪼갭니다. 토큰 계산은 토크나이저를 아는 어댑터가, 재분할은 라이브러리를 모르는 `core/`가 맡습니다.

바닥(32자)까지 내려가도 못 맞추면 **경고를 남기고 진행합니다.** 여기서 요청을 실패시키면 병적인 텍스트 한 조각이 문서 전체의 수집을 막습니다. 조용히 잘리는 것과 달리 이건 로그에 남습니다.

### 배치 — 메모리는 문서 크기가 아니라 배치 크기에 비례한다

청크 전체를 한 번에 인코딩하지 않고 `embedding_batch_size`(기본 64)씩 끊어, 배치마다 인코딩하고 씁니다. 업로드 상한(20 MiB) 안의 문서도 청크가 수만 개일 수 있고, 한 번에 인코딩하면 결과 벡터만 수십 MB에 모델 내부 버퍼가 더해집니다.

배치마다 인코딩과 쓰기가 각각 스레드풀을 거치므로 그 지점에서 이벤트 루프가 다른 요청으로 넘어갑니다. 임베딩 단계도 다른 요청을 굶기지 않습니다.

대가는 부분 기록의 폭이 커진다는 것이고, 그래서 위의 되돌리기가 필수가 됩니다.

### 같은 문서는 직렬화되고, 다른 문서는 병렬이다

같은 `document_id`의 요청 둘이 겹치면 `get → 저장 → 커밋 → 삭제`가 인터리빙되어, 마지막 커밋이 아닌 쪽의 청크가 잔여물이 되거나 삭제가 방금 쓴 새 청크를 지웁니다. **전역 동시성 상한은 이를 막지 못합니다** — 총량만 제한할 뿐 같은 문서 두 건을 그대로 통과시킵니다.

그래서 둘을 나눠 둡니다.

| 장치 | 막는 것 |
|---|---|
| `document_id`별 `asyncio.Lock` | 같은 문서의 인터리빙. 잠금 구간은 레지스트리 조회부터 이전 청크 정리 완료까지 |
| `anyio.CapacityLimiter` (기본 2) | 동시 수집 **총량**. 상한에 걸린 요청은 실패하지 않고 대기합니다 |

리미터는 실제로 일하는 구간에만 겁니다. 조회만 하고 끝나는 `unchanged` 요청이 상한을 점유하면 처리량이 이유 없이 떨어집니다.

**다중 워커로 가는 순간 이 잠금이 가장 먼저 깨집니다.** 인메모리 잠금이 프로세스 로컬이기 때문입니다. 벡터 스토어를 서버 모드로 옮긴 뒤 워커 1 프로세스를 강제하는 것은 이제 이 잠금 하나뿐이고, 그 전환은 외부 잠금 도입이라는 별도 작업이 됩니다 — 둘은 같이 다뤄야 합니다.

### 기동 정리 — 규칙 둘

기동 시 벡터 스토어를 레지스트리에 맞춥니다. 두 규칙의 성격이 다릅니다.

**규칙 1 (크래시 백스톱)** — 레지스트리의 어떤 문서도 현재 값으로 가리키지 않는 `(document_id, revision, index_signature)` 청크를 지웁니다. 정합성 수단이 **아닙니다.** 요청 안의 되돌리기가 닿지 못하는 실패(프로세스 강제 종료, 되돌리기 자체의 실패)가 남긴 잔여물만 회수합니다.

**규칙 2 (구성 변경 처리)** — 기록된 `index_signature`가 현재 구성과 다른 문서는 청크를 전부 지우고 `chunk_count = 0`, `index_status = "stale"`로 커밋합니다.

규칙 2가 필요한 이유는 `unchanged` 단축이 **재업로드가 있을 때만** 작동하기 때문입니다. 모델을 바꾸고 아무도 아무것도 올리지 않으면, 구 구성 벡터는 그대로 살아서 검색되고 아무도 그 사실을 모릅니다.

**원본 바이트를 보관하지 않으므로 자동 재색인은 불가능합니다.** 선택지는 셋뿐이었습니다 — 구 서명 벡터를 그대로 두거나(다른 의미 공간의 벡터가 검색에 섞입니다), 기동을 실패시키거나(평가자가 설정을 한 번 만져 본 순간 "한 줄 실행"이 깨집니다), 지우고 그 사실을 드러내거나. 셋째를 택했습니다. 청크가 실제로 없으므로 **검색 계층이 별도 필터를 두지 않아도** 낡은 벡터가 답변에 섞이지 않고, `index_status`가 무엇을 다시 올려야 하는지 알려 줍니다. 대가는 재업로드해야만 복구된다는 것입니다.

원자성은 여기서도 같은 순서입니다 — **청크를 지우고 나서 레코드를 커밋합니다.** 반대로 하면 커밋 후 죽었을 때 `chunk_count: 0`인데 청크가 남아 있는, 아무도 지우지 않는 상태가 됩니다.

`stale` 문서의 `index_signature`는 **바꾸지 않습니다.** 현재 구성 값으로 갱신하면 재업로드가 `unchanged` 단축에 걸려 영영 복구되지 않습니다. 대신 이미 처리된 문서는 다음 기동에서 다시 지우거나 커밋하지 않습니다 — 아무것도 바뀌지 않은 기동이 쓰기를 만들 이유는 없습니다. 경고는 계속 남습니다. 여전히 다시 올려야 하는 문서이기 때문입니다.

정리는 **기동을 실패시키지 않습니다.** 실패하면 경고만 남기고 서비스는 뜹니다.

### 업로드 상한이 실제로 막는 것

본문 파싱은 프레임워크에 맡깁니다(`UploadFile` 주입). 상한은 그 뒤에 파일 크기로 겁니다.

```python
if file.size and file.size > settings.max_upload_bytes:
    raise DocumentTooLarge(..., max_upload_bytes=...)
```

**이 검사가 막는 것은 수신이 아니라 파싱과 임베딩입니다.** 핸들러가 불리는 시점에 본문은 이미 수신된 뒤입니다 — Starlette이 1 MiB를 넘는 파트를 디스크로 스풀하므로 메모리는 본문 크기에 묶이지 않지만, 바이트 자체는 일단 받습니다. 21 MB 요청은 9 ms 만에 413으로 끝나고 파서까지 가지 않습니다. 수신 단계에서 끊으려면 리버스 프록시의 `client_max_body_size` 같은 **앞단 상한**이 함께 있어야 합니다.

상한을 재는 대상은 요청 본문이 아니라 **파일**입니다. 본문에 걸면 상한에 꼭 맞는 파일이 multipart 봉투 몇백 바이트 때문에 거절되어, 응답이 알려주는 상한과 실제 동작이 어긋납니다.

프레임워크의 multipart 파트 기본 상한(1 MiB)은 파일 파트에 적용되지 않습니다. 적용된다면 설정을 20 MiB로 올려도 2 MiB 파일이 설정과 무관한 이유로 거절되므로, 그 경계는 테스트로 고정해 두었습니다.

### 로그는 무엇이 어떻게 잘렸는지만 남긴다

```json
{"message":"문서 업로드 완료","request_id":"df09ee03...","document_id":"b166d4ad-...",
 "document_filename":"handbook.pdf","format":"pdf","revision":"662b78b2c395",
 "byte_size":15013,"page_count":3,"chunk_count":5,"ingestion_status":"created"}
```

**문서 본문과 청크 내용은 싣지 않습니다** — 로그로 새어 나가면 그 자체가 유출입니다.

키가 `filename`이 아니라 `document_filename`인 이유는 `filename`이 `LogRecord`가 이미 쓰는 이름(로그를 남긴 소스 파일)이라, `extra`로 넘기면 로깅이 `KeyError`로 거절하기 때문입니다.

### 오프로드 지점은 코드에서 눈에 보인다

```python
extracted = await asyncio.to_thread(parser.parse, data)
```

파싱은 CPU 바운드이고 문서 크기에 비례합니다. 워커가 1개 프로세스로 고정돼 있어(문서 단위 잠금이 프로세스 로컬입니다) 이벤트 루프를 막으면 서비스 전체가 멈춥니다 — 20 MiB PDF 하나가 헬스 응답까지 세웁니다.

`DocumentParser`를 동기로 선언한 이유가 정확히 이것입니다. 프로토콜이 async였다면 이 줄이 그냥 `await parser.parse(data)`가 되어, 오프로드가 빠졌다는 사실이 코드에서 보이지 않습니다. 테스트는 느린 파서를 주입하고 그동안 다른 코루틴이 실제로 진행하는지를 셉니다.

### "내용이 없다"는 세 갈래로 갈린다

| 상황 | 예외 | 클라이언트가 할 일 |
|---|---|---|
| 지원 목록에 없는 확장자 / 확장자 없음 | `UnsupportedDocumentFormat` (지원 포맷 목록 동봉) | 다른 포맷으로 변환 |
| 바이트가 없거나 추출 결과가 공백뿐 | `EmptyDocument` | 파일을 확인 |
| 쪽은 있는데 텍스트 레이어가 없음 (스캔본) | `NoExtractableText` (쪽 수 동봉) | OCR을 거쳐 다시 업로드 |

뒤 둘을 한 코드로 뭉개면 클라이언트는 파일이 잘못된 것인지 OCR이 필요한 것인지 구분할 수 없습니다. HTTP 상태 코드로의 변환은 `api/errors.py`의 코드별 상태 표가 담당합니다 (아래 "도메인 오류 코드 → HTTP 상태").

## 문서 API

| 메서드 | 경로 | 성공 | 하는 일 |
|---|---|---:|---|
| `POST` | `/documents` | 201 / 200 | 업로드 한 건을 끝까지 수집 |
| `GET` | `/documents` | 200 | 목록 (최근 수집 순) |
| `GET` | `/documents/{document_id}` | 200 | 문서 한 건 |
| `DELETE` | `/documents/{document_id}` | 204 | 문서와 그 청크 제거 |

라우터가 하는 판단은 셋뿐입니다 — 크기 상한, 상태 코드 선택, 응답 모양. 무엇을 저장하고 무엇을 지우는지는 전부 `IngestionService`가 정합니다.

### 상태 코드는 "무엇이 새로 생겼는가"로 갈린다

`201`은 최초 수집뿐입니다. 교체·재색인·무변경은 이미 있던 리소스를 갱신하거나 그대로 두는 것이라 `200`이고, 무엇이 일어났는지는 본문의 `status`(`created`·`replaced`·`reindexed`·`unchanged`)가 말합니다.

`reindexed`를 `replaced`로 뭉개지 않는 이유는 응답이 자기모순이 되기 때문입니다. 재색인은 `revision`이 그대로인 채 일어나므로, `replaced`로 답하면 `previous_revision`이 현재 `revision`과 같아집니다. 그래서 `previous_revision`은 **내용이 바뀐 교체에서만** 값이 있습니다.

### 응답에 청크 본문을 싣지 않는다

저장 단계가 없던 시절에는 청크 목록을 응답으로 돌려주는 것 말고 결과를 보여줄 방법이 없었습니다. 이제 청크는 벡터 스토어에 있고, 업로드 응답·목록·상세가 **같은 `DocumentView`** 를 씁니다(업로드만 `status`와 `previous_revision`을 덧붙입니다). 같은 리소스를 두 모양으로 보여주면 소비자가 모델을 둘 들어야 합니다.

같은 이유로 `page_count`도 응답에 없습니다. 레지스트리가 보관하지 않는 값이라 상세 조회에서는 채울 수 없고, 업로드 응답에만 나타나면 그 자체가 두 모양이 됩니다. 진단에 필요한 값이므로 로그에는 남습니다.

목록은 배열이 아니라 `{"documents": [...], "count": n}` 봉투입니다. 페이지네이션이나 집계가 붙을 때 최상위 타입이 바뀌지 않게 하기 위해서입니다. 정렬(최근 수집 순)은 서비스가 정합니다 — 레지스트리 구현이 바뀌었다고 응답 순서가 바뀌면 그건 구현 세부가 API로 샌 것입니다.

### 미지원 포맷은 잠금을 잡기 전에 걸러낸다

`ingest()`가 가장 먼저 하는 일이 포맷 판정입니다. 수집되지 않을 파일이 문서 단위 잠금과 동시성 상한을 점유할 이유가 없고, 파일명이 비어 있으면 `document_id` 유도 자체가 성립하지 않습니다 — 그 경우도 "확장자가 없다"와 같은 사건이므로 같은 `415`로 끝나야 합니다.

### 삭제는 청크를 먼저 지운다

레지스트리 레코드를 먼저 지우면, 그 뒤 청크 삭제가 실패했을 때 **"삭제 성공 응답 + 잔여 청크"** 가 관측됩니다. spec이 금지한 상태입니다. 청크를 먼저 지우면 그 단계의 실패가 곧 요청의 실패라 그런 상태가 존재하지 않습니다.

## 검색 파이프라인

```
                        ┌─→ 밀집 retriever (질의 임베딩 → 코사인 → 자체 하한) ─┐
대상 집합 확정(레지스트리) ─┤                                                  ├─→ RRF 융합
                        └─→ 어휘 retriever (BM25 → 희소도 하한) ────────────┘        │
                                                                                    ↓
                                                    조립 ← 상위 K 절단 ← 현재성 재검증
```

`RetrievalService`가 **retriever 목록**과 레지스트리를 주입받아 이 순서를 지킵니다. 대상 집합 확정·현재성 재검증·상위 K 절단은 retriever가 몇 개든 **요청당 한 번**이고, 팬아웃은 가운데 한 자리에만 들어갑니다. 대상 집합을 저장소 질의 **이전에** 거는 이유와 빈 목록이 전체 검색이 아닌 이유는 위 "검색 대상 필터는 질의 이전에 건다"에 적었습니다. 여기서는 그 위에 얹힌 정책 — K, 하한, 융합, 현재성 — 을 다룹니다.

**모든 retriever가 하나의 같은 대상 집합을 공유합니다.** 각자 레지스트리를 읽으면 그 사이의 커밋 때문에 retriever마다 다른 세대를 보게 되고, 서로 다른 세대의 순위를 융합한 결과는 그 순간 의미를 잃습니다.

**검색은 답변을 만들지 않습니다.** 이 단계의 산출물은 근거 후보 목록이고, 그 근거로 답할 수 있는지의 판정은 답변 생성의 몫입니다.

### 상위 K 기본값은 5, 상한은 20, 후보 깊이는 100

| 설정 | 값 | 무엇을 정하는가 |
|---|---:|---|
| `retrieval_top_k` | 5 | 요청이 `top_k`를 주지 않았을 때의 K |
| `retrieval_max_top_k` | 20 | 요청이 지정할 수 있는 K의 최댓값 |
| `candidate_depth` (retriever별) | 100 | 융합 **전에** 각 retriever에게 받아 오는 후보 수 |

기본값 5는 청크 겹침 설계에서 나온 값입니다. 청크는 100자씩 겹치도록 잘려 있고 그 겹침의 목적이 "경계에 걸친 문장이 양쪽에서 온전히 읽히게" 하는 것이므로, K=1이면 답이 절 경계에 걸렸을 때 반쪽만 옵니다. 샘플 문서의 한 절이 청크 1~2개에 걸치므로 관련 절(1~2) + 인접 문맥(1) + 검색이 완벽하지 않을 때의 여유(1~2) = 4~6이고, 600자 × 5 ≈ 3,000자는 뒤에 붙을 프롬프트 지시문과 함께 넣기에 부담 없는 크기입니다.

5는 **출발점이지 결론이 아닙니다.** 요청이 `top_k`로 덮어쓸 수 있게 열어 두어, 답변 품질을 보며 조정할 때 재배포가 필요 없게 했습니다.

**상한이 필요한 이유가 바로 그 열어 둔 문입니다.** `top_k`를 요청이 정할 수 있으면 그 값은 클라이언트가 정하는 응답 크기이자 저장소 부하입니다. 상한이 없으면 `top_k=1000000` 한 줄이 저장소에서 전부를 긁어 와 전부 직렬화합니다 — 업로드 크기와 질의 길이에 상한을 둔 것과 같은 종류의 구멍입니다.

**상한을 20으로 정한 것은 응답 크기가 아니라 융합 여지 때문입니다.** 초안은 50이었고 근거도 응답 크기(600자 × 50 ≈ 30 KB)였는데, 하이브리드로 바뀌면서 그 근거가 무의미해졌습니다 — 청크 50개는 어차피 프롬프트 예산상 문맥에 들어가지 못해 상한 50에 실효가 없습니다. 지금 상한을 정하는 것은 **후보 깊이와의 비율**입니다. 기동 검증이 요구하는 것은 `깊이 >= 상한`이라는 하한뿐이라 둘을 같게 두어도(50/50) 기동은 통과하지만, `top_k=50` 요청에서 한 retriever가 K칸을 혼자 채워 오면 다른 쪽의 발견이 들어올 자리가 남지 않습니다. 그래서 **깊이 100 · 상한 20, 다섯 배**로 벌렸습니다. 배수를 기동 규칙으로 못 박지는 않았습니다 — 표본이 문서 2건(배포 구성에서 청크 3개)이라 어느 배수가 옳은지 관측할 수 없고, 근거 없는 상수를 검증으로 굳히면 바꿀 때 코드를 고쳐야 합니다.

상한도 설정 항목입니다. 코드에 박으면 K를 실험할 때마다 재배포가 필요해져 요청별 지정을 열어 둔 이유가 사라집니다. 대신 설정 둘이 서로 모순될 수 있는 자리가 생기므로 — 기본 K가 상한보다 크면 **어떤 요청도 통과하지 못합니다** — `chunk_overlap ≥ chunk_size`와 같은 자리에서 같은 방식으로, 기동을 막습니다.

### 유사도 하한 0.82는 계측값이다 — 그리고 이제 밀집 retriever 안에 있다

**적용 지점이 옮겨졌습니다.** 예전에는 검색 마지막 단계에서 결과 전체에 걸었고, 지금은 `DenseRetriever`가 융합 **전에** 자기 코사인에 겁니다. 서로 단위가 다른 점수(코사인과 BM25)에 임계값 하나를 걸 수 없고, 융합 뒤의 RRF 점수는 관련성의 크기가 아니라 순서만 담아서 임계값의 뜻이 성립하지 않기 때문입니다. 그래서 어휘 쪽은 자기 단위의 하한(희소도 `0.3`, 위 "어휘 색인")을 따로 갖습니다. 아래 계측표는 **여전히 유효합니다** — 재는 대상이 코사인 분포이고 그 분포는 하한이 어디서 걸리든 같습니다.

`sample-docs` 두 문서를 실물 임베더(`intfloat/multilingual-e5-small`)로 색인하고, spec이 지정한 관련 질의 4개와 무관 질의 3개의 점수를 실제로 쟀습니다.

| 청크 크기 | 관련 질의 1위 점수 | 무관 질의 1위 점수 |
|---|---|---|
| 600 (기본값, 청크 3개) | 0.8511 ~ 0.8710 | 0.8064 ~ 0.8134 |
| 200 (품질 테스트, 청크 10개) | 0.8775 ~ 0.9002 | 0.7907 ~ 0.8019 |

후보 하한별로 남는 결과 수(상위 K = 5, 질의별):

| 하한 | 관련 질의 (600) | 무관 질의 (600) | 관련 질의 (200) | 무관 질의 (200) |
|---:|---|---|---|---|
| 0.75 | 3, 3, 3, 3 | **3, 3, 3** | 5, 5, 5, 5 | **5, 5, 5** |
| 0.80 | 1, 3, 3, 3 | **1, 1, 2** | 5, 5, 5, 5 | **0, 0, 2** |
| 0.81 | 1, 2, 2, 3 | **0, 1, 0** | 4, 5, 4, 5 | 0, 0, 0 |
| **0.82** | 1, 2, 2, 3 | 0, 0, 0 | 4, 5, 3, 5 | 0, 0, 0 |
| 0.84 | 1, 1, 1, 1 | 0, 0, 0 | 2, 2, 2, 1 | 0, 0, 0 |

**0.82는 두 구성 모두에서 무관 질의가 전부 0이 되는 가장 낮은 값입니다.** 더 올리면 관련 질의의 인접 문맥까지 잘려 K=5를 둔 이유가 사라지고, 더 내리면 무관 질의가 근거를 얻습니다.

**초안이던 0.75는 아무것도 거르지 못합니다.** e5 계열은 정규화 코사인이라 서로 무관한 한국어 문장 사이에서도 0.75~0.81이 흔합니다. 감으로 적은 상수가 거의 확실히 틀리는 자리라, 이 값은 재서 정했습니다.

**표본이 작다는 사실을 함께 적습니다.** 문서 2개·질의 7개뿐이라 통계적 근거는 약합니다. 완화책은 셋입니다 — spec의 단언을 절대값이 아니라 상대 관계로만 두어(반환된 결과가 모두 하한 이상 / 무관 질의의 최고 점수가 관련 질의보다 낮음) 임계값이 틀려도 spec이 거짓이 되지 않게 했고, 하한은 설정 항목이라 재배포 없이 바꿀 수 있으며, 무엇보다 하한이 **유일한 방어선이 아닙니다**(바로 아래).

### 하한이 판정하는 것과 하지 않는 것

| 판정 | 주체 | 근거 |
|---|---|---|
| 이 청크를 다음 단계에 **보여줄 가치가 있는가** | 검색 | 점수만 보면 답할 수 있습니다. 본문을 읽을 필요가 없습니다 |
| 이 근거로 **답할 수 있는가** | 답변 생성 (아래 "답변 생성") | 본문을 읽어야 답할 수 있습니다. 점수만으로는 알 수 없습니다 |

검색은 근거 없음을 **통보하지 않습니다.** 빈 목록이나 낮은 점수로 드러낼 뿐이고 거절 문구를 만들지 않습니다. 하한에 걸려 결과가 하나도 남지 않는 것도 오류가 아니라 빈 결과입니다.

임계값 하나에 환각 억제를 몰아주지 않는 이유는 둘입니다.

- 임계값은 **의미가 아니라 벡터 거리**를 봅니다. 질문의 어휘가 문서와 겹치면 답이 문서에 없어도 점수가 높고("연차를 팔 수 있나요?"는 연차 항목과 가깝습니다), 표현이 다르면 답이 있어도 점수가 낮습니다. 어느 쪽이든 **본문을 읽어야만** 판정되는 종류입니다.
- 임계값이 유일한 방어선이면, 모델이나 문서 도메인이 바뀌어 점수 분포가 이동하는 순간 환각 억제 전체가 함께 무너집니다. 프롬프트가 뒤에 있으면 임계값이 헐거워도 답변은 여전히 거절할 수 있습니다.

검색이 거절 문구를 만들면 답변 생성의 프롬프트가 그 판정을 뒤집을 수 없게 되고, PRD의 "하드코딩된 응답" 경계에도 닿습니다.

### 질의는 질의용 경로로 임베딩한다 — 틀려도 오류가 나지 않는 자리

검색은 `embed_query`를 씁니다. `embed_documents([query])[0]`으로도 벡터는 나오고 **오류도 나지 않습니다.** 역할에 따라 입력을 다르게 다루는 모델(`query: `/`passage: ` 접두사)에서는 점수 분포가 통째로 이동할 뿐입니다. 그런데 하한 0.82가 바로 그 분포를 재서 정한 값이라, 경로가 어긋나면 **하한이 조용히 무효가 됩니다** — 검색은 계속 무언가를 반환하고, 무엇이 나빠졌는지는 어디에도 드러나지 않습니다.

그래서 검증을 둘로 나눴습니다.

| 확인할 것 | 어디서 | 임베더 |
|---|---|---|
| 검색이 **질의 경로를 부르는가** | 검색 서비스 테스트 | 페이크 (경로별 호출을 기록만 합니다) |
| 두 경로가 **실제로 다른 벡터를 내는가** | 임베딩 어댑터 테스트 | 실물 (가중치 없으면 스킵) |

**페이크 임베더는 두 경로에서 같은 벡터를 냅니다 — 버그가 아니라 결정입니다.** 해시 기반 벡터에는 의미가 없으므로 순위 단언은 "질의 문자열을 청크 본문과 똑같이 두면 벡터가 일치해 1위"라는 성질에 기댑니다. 페이크를 비대칭으로 바꾸면 그 성질이 사라져 정렬·필터 단언이 전부 임의값이 됩니다. 비대칭성은 페이크가 아니라 **실물 모델의 성질**이므로 실물로 확인하는 것이 맞습니다 — 페이크에 흉내 내게 하면 페이크가 검증 대상이 되고, 정작 확인하려던 성질은 여전히 미확인으로 남습니다. 대칭 모델도 정당한 구현이라 "두 벡터가 달라야 한다"를 모든 임베더에 거는 계약으로 두지도 않았습니다.

### 현재성의 기준 시점은 결과 조립 시점이다

대상 집합 필터는 "레지스트리를 읽는다 → 저장소에 질의한다"는 두 단계이고, **그 사이는 잠겨 있지 않습니다.** 수집의 교체 순서가 `저장 → 커밋 → 이전 세대 정리`이므로, 그 틈에 커밋이 끼면 검색은 방금 밀려난 리비전을 필터에 넣고 정리가 아직 안 돌았으면 그것이 그대로 결과가 됩니다.

**삭제 경로는 이미 안전합니다.** 삭제는 반대 순서(`청크 삭제 → 레지스트리 삭제`)라, 검색이 청크를 받았다면 그 시점에 삭제가 아직 성공 응답을 내지 않았습니다. 노출되는 것은 교체·재색인 경로뿐입니다.

**노출의 크기와 실제 손해는 다릅니다.** 반환된 텍스트는 검색이 시작된 시점에는 현재였던 내용이고 창은 저장소 왕복 한 번이라, 잠깐 직전 리비전이 보이는 것 자체는 스냅숏 격리가 없는 읽기의 정상 동작에 가깝습니다. 손해는 결과에 실려 나가는 `revision`에 있습니다 — 다음 change의 캐시가 그 값으로 항목을 만들면 무효화가 이미 지나간 뒤라 **그 항목은 어떤 무효화에도 걸리지 않습니다.** 밀리초짜리 읽기 어긋남이 무기한 캐시 오염으로 증폭됩니다.

그래서 질의 뒤에 레지스트리를 한 번 더 봅니다. **결과에 실제로 기여한 문서만** 다시 읽어(보통 한두 건) 현재 `(revision, index_signature)`가 필터에 쓴 삼중항과 다르면 **그 문서의 결과를 버립니다.**

- **재시도가 아니라 드롭인 이유**: 재시도는 지속적인 업로드 아래에서 끝나지 않을 수 있어 횟수 한계와 그 한계에 걸렸을 때의 동작을 또 정해야 합니다. 반면 드롭의 관측 결과는 "이전 세대 정리가 이미 돌았다"와 **완전히 같습니다** — 그 문서가 이번 검색에 기여하지 않습니다. 이미 존재하는 결과 모양이라 경로가 둘로 갈리지 않습니다.
- **잠금을 공유하지 않는 이유**: 검색은 특정 문서가 아니라 전체를 대상으로 하므로 문서 단위 잠금으로는 성립하지 않고 전역 읽기/쓰기 잠금이 됩니다. 그러면 검색 지연이 진행 중인 업로드의 임베딩 시간에 묶여 "검색 중에도 다른 요청에 응답한다"가 반대편에서 깨집니다. 무엇보다 기존 잠금은 프로세스 로컬이라 다중 워커에서 성립하지 않습니다. 레지스트리는 공유 SQLite라 재검증은 다중 워커에서도 성립합니다.
- **창을 좁힐 뿐 닫지는 못합니다.** 조립 뒤 응답이 전달되기까지 사이의 커밋은 어떤 방법으로도 막을 수 없습니다(클라이언트가 응답을 읽는 순간에는 이미 낡았을 수 있습니다). 그래서 계약은 "결과를 조립하는 시점"까지만 보장하고, `revision`을 근거로 **영속화**하는 소비자가 그 시점에 현재성을 다시 확인해야 한다는 것을 명시적으로 다음 change에 넘깁니다. 닫았다고 적으면 캐싱 change가 그 거짓 위에 설계됩니다.

### 질의마다 레지스트리를 두 번 읽는 비용

대상 집합 확정에 한 번(전체 목록), 현재성 재검증에 한 번(기여 문서만). SQLite 파일 조회이고 문서가 수십 건 규모라 캐시하지 않습니다 — 캐시하면 삭제·재업로드 직후 검색이 낡은 대상 집합을 보게 되고, 그 무효화가 또 하나의 캐시 무효화 문제가 됩니다.

**재고 시점은 명확합니다**: 문서 수가 커져 목록 조립과 저장소 필터 표현식이 검색 지연에서 눈에 띄기 시작할 때입니다. 그때 붙일 것은 대상 집합 캐시가 아니라 레지스트리 쪽 인덱스이거나, 필터를 문서 단위가 아니라 서명 단위로 좁히는 방법입니다.

### 융합은 RRF — 점수가 아니라 순위만 쓴다

두 목록을 합치는 방법은 셋이었습니다.

| 방법 | 왜 아닌가 |
|---|---|
| 점수 정규화 후 가중합 | 코사인과 BM25는 **분포가 다릅니다**. BM25는 상한이 없고 코퍼스 크기·질의 길이에 따라 몇 배씩 움직이므로, min-max로 눌러도 그 스케일이 질의마다 달라 가중치가 뜻을 잃습니다 |
| 한쪽을 후보 생성, 다른 쪽을 필터 | 필터 쪽이 못 찾으면 상대의 발견이 통째로 사라집니다. 어휘 색인이 구제하려던 자리가 바로 "한쪽만 찾는" 경우라 목적과 반대로 동작합니다 |
| **RRF** | 순위만 쓰므로 척도를 맞출 필요가 없습니다. 두 목록이 같은 것을 꼽으면 오르고, 한쪽만 꼽으면 덜 오릅니다 |

    raw(d)   = Σ_r  w_r / (RRF_K + rank_r(d))    # r 은 이름표 정렬 순서
    max      = Σ_r  w_r / (RRF_K + 1)            # 같은 항 형태, 같은 순서
    score(d) = raw(d) / max                      # (0, 1]

`RRF_K = 60`은 원 논문(Cormack et al., 2009)이 실험으로 고른 값이자 업계 관례입니다. 우리 코퍼스로 다시 재려면 정답 라벨이 필요한데 그것이 없습니다 — 근거 없이 다른 값을 쓰는 것보다 관례값을 쓰고 그 사실을 적는 편이 정직합니다.

**정규화 분모를 `(Σ w_r) / (RRF_K + 1)`로 접지 않습니다.** 두 식은 실수에서 같지만 부동소수점에서는 다르고(가중치 `1.0`·`2.0`에서 `fl(1/61) + fl(2/61) ≠ fl(3/61)`), 그 차이가 만장일치 1위를 `0.9999999999999999`로 만듭니다. 누적 순서를 이름표 정렬로 고정하는 것도 같은 이유입니다 — 전달 순서로 더하면 순서가 마지막 비트를 바꿔 "전달 순서가 결과에 닿지 않는다"가 깨집니다.

**동점은 `(-score, document_id, chunk_index)` 전순서로 깹니다.** RRF는 구조적으로 동점이 잦고, 동점 처리가 딕셔너리 순회 순서에 기대면 "같은 질의는 같은 결과"가 깨집니다. 그것이 깨지면 다음 change의 캐시 전제까지 함께 무너집니다.

**빈 목록과 없는 목록은 다릅니다.** 어휘 retriever가 하한에서 전부 걸러 빈 목록을 내면 그 가중치는 분모에 남고(한쪽만 찾은 청크는 `0.5`), 선택 retriever가 **실패해 목록에서 빠지면** 분모에서도 빠져 살아남은 쪽 기준으로 척도가 다시 잡힙니다. 앞은 "못 찾았다"는 판정이고 뒤는 판정 자체가 없는 상태라, 어휘 색인이 죽어 있는 동안 점수가 통째로 절반이 되는 일은 생기지 않습니다.

### 점수는 유사도가 아니라 융합 점수다 (BREAKING)

응답의 `score`는 코사인 유사도였고, 지금은 RRF 융합 점수입니다. 의미가 이렇게 바뀝니다.

| | 예전 | 지금 |
|---|---|---|
| 무엇을 재는가 | 질의와 청크의 의미 거리 | 여러 retriever가 얼마나 **같은 것을 꼽았는가** |
| 범위 | `[0, 1]` (코사인) | `(0, 1]` (이론 최댓값으로 정규화) |
| `1.0`의 뜻 | 사실상 같은 문장 | 전달된 **모든** 목록에서 1위 |

**`1.0`이 흔한 것은 버그가 아닙니다.** 정규화 분모가 "모든 목록이 이 항목을 1위로 꼽았을 때의 점수"라, 두 retriever가 같은 청크를 1위로 꼽으면 정의상 최댓값입니다. 값 크기로 관련성을 비교하려던 소비자를 위해 각 결과에 **retriever별 기여 내역**(`contributions` — 이름·순위·그 retriever의 원래 점수)을 함께 싣습니다. 융합 뒤에는 그것 없이 "왜 이 순위인가"에 답할 수 없습니다.

바꾼 대가는 API 표면 한 줄이었습니다. `score`를 읽는 곳이 `api/queries.py`의 응답 뷰뿐이고 `core/prompting.py`도 `services/qa.py`도 점수를 보지 않아, 파급이 그 자리에 갇혀 있었습니다.

### 하이브리드가 실제로 무엇을 구제하는가 — 실측

**재는 절차**는 품질 테스트(`tests/test_retrieval_quality.py`)와 같습니다: `sample-docs/` 두 건을 배포 구성(recursive 200자/40자 겹침, `N=10`)으로 **실물 임베더 + 실물 FTS5**에 색인하고, 같은 저장소 위에 설정만 다른 검색 서비스를 세워(`searching_with`) 비교합니다. 저장소를 새로 만들면 비교가 성립하지 않습니다.

**문맥 없는 식별자 질의에서 밀집 단독은 근거를 0건 냅니다.** 배포 하한 `0.82`에서 잰 값입니다.

| 질의 | 밀집 1위 코사인 | 밀집 단독 결과 | 하이브리드 결과 |
|---|---:|---:|---:|
| `P3` | 0.7969 | **0건** | 1건 |
| `P4` | 0.8015 | **0건** | 1건 |
| `hotfix` | 0.8001 | **0건** | 1건 |
| `develop` | 0.8102 | **0건** | 2건 |
| `release` | 0.8288 | 1건 | 2건 |
| `HR팀` | 0.8426 | 3건 | 4건 |

아래 두 줄은 **구제가 보편적이지 않다는 것**을 함께 보여 줍니다 — 코사인이 하한을 넘는 식별자는 밀집도 찾고, 어휘는 근거를 하나 더 얹을 뿐입니다. 갈리는 지점은 하한 `0.82`이고, 그래서 이 표는 하한을 조정하면 통째로 다시 재야 합니다.

문서에 그 문자열이 **그대로 있는데도** 밀집 단독은 빈 목록을 냅니다. 오류가 아니라 근거 0건이라 답변 생성이 `no_evidence`로 마감하고, 사용자에게는 문서에 있는 사실이 "없다"고 보입니다 — 로그에도 남지 않는 종류의 실패입니다. 이 change를 만든 이유가 이 표이고, 위 네 줄은 `tests/test_retrieval_quality.py`가 회귀로 붙잡습니다(그중 셋).

**기존에 맞히던 것은 그대로 맞힙니다.** 네 회귀 질의는 하이브리드에서도 1위가 같습니다.

| 질의 | 1위 | 기여 |
|---|---|---|
| 교육비는 얼마까지 | `company-policy.txt` 청크 0 | dense#1 `0.8917` + lexical#1 `2.3798` |
| 재택근무 주 몇 회 | `company-policy.txt` 청크 1 | dense#1 `0.9002` + lexical#1 `1.9604` |
| PR 승인 몇 명 | `development-guide.md` 청크 0 | dense#1 `0.8924` + lexical#1 `2.0796` |
| P1 장애 몇 분 | `development-guide.md` 청크 4 | dense#1 `0.8775` + lexical#1 `7.5576` |

### 가중치는 둘 다 1.0 — 바꿀 근거를 만들지 못했다

`lexical` 가중치를 `0.5` / `1.0` / `2.0`으로 바꿔 위 네 회귀 질의와 식별자 질의 넷을 다시 쟀습니다. **여덟 질의의 판정이 세 값에서 모두 같습니다** — 1위도, 근거 유무도 바뀌지 않습니다.

코퍼스가 청크 10개뿐이라 두 목록이 대부분 같은 청크를 같은 순서로 꼽고, 그러면 가중치는 분자와 분모에 함께 들어가 상쇄됩니다. **관측되지 않는 차이를 근거로 값을 고르지 않습니다.** 정답 라벨이 있는 평가 셋이 생기기 전까지 `1.0`이고, 바꿀 때 코드는 손대지 않습니다(`APP_RETRIEVERS`).

희소도 하한 `0.3`도 같은 절차로 다시 확인했고 위 "하한 `0.3`의 실측" 표와 결과가 일치합니다 — `0.4`에서 코드리뷰 질의가, `0.5`에서 재택근무까지 죽습니다. **`0.3`은 회귀 질의를 모두 살리는 가장 높은 값입니다.**

### 검색 결과 캐시는 넣지 않았다

설계까지 갔다가 뺐습니다. 프로세스 안 LRU로 융합 결과를 들고, 키를 `(질의, 대상 집합 지문, 검색 구성 서명)`으로 잡아 무효화 호출 없이 문서 변경을 흡수하는 구성이었습니다.

**빼는 근거는 그 설계 자체에 있었습니다 — "캐시를 꺼도 결과가 같다."** 이 계층이 다루는 것은 지연이지 정확성이 아닙니다. 반면 PRD가 요구하는 캐싱은 LLM 응답 캐시이고 그것은 결과를 바꿉니다. 남은 시간을 결과가 달라지지 않는 쪽에 쓰지 않았습니다. `retrieval_cache_size` 설정 항목이 한때 남아 있었으나 소비처 없이 죽어 있어 함께 지웠습니다.

**대가는 반복 질의도 매번 두 저장소를 친다는 것입니다.** 두 retriever가 `asyncio.gather`로 겹쳐 도니 지연은 합이 아니라 최댓값에 가깝고, 그 상태에서도 헬스 응답이 막히지 않는 것은 동시성 요구사항이 이미 강제합니다. 지연이 실제로 문제가 되면 그때 되살리면 됩니다 — 결과가 달라지지 않는 계층이라 나중에 얹어도 늦지 않습니다.

**적어 두지 않으면 다음 사람이 빠진 것으로 읽고 다시 설계합니다.** 그래서 묘비를 남깁니다.

### 재랭킹·질의 확장은 여전히 없다

재랭킹(cross-encoder)·질의 확장·HyDE는 넣지 않았습니다. 재랭킹 모델은 **두 번째 모델**이라 이미지 크기·기동 시 선로딩·별도 임계값이 따라오고, 융합의 효과를 재기 전에 얹으면 둘 중 무엇이 기여했는지 알 수 없습니다.

**붙일 자리는 명확합니다** — 융합과 현재성 재검증 사이입니다. 대상 집합 확정과 결과 조립은 그대로 두고 그 사이에 한 단계가 들어갑니다.

## 답변 생성

`POST /qa`는 질문 하나를 받아 **서버 전송 이벤트(SSE)** 로 답변을 흘려보냅니다. 검색이 고른 근거로 프롬프트를 조립하고, `codex app-server` 세션에서 턴 하나를 돌리고, 도착하는 델타를 그대로 이벤트로 내보내고, 마지막에 인용을 검증해 닫습니다.

```
POST /qa ──▶ enforce_query_limits ──▶ QaService.prepare ──▶ StreamingResponse
             (422)                    (검색 · 503)          │
                                                            ▼
   sources ──▶ build_prompt ──▶ CodexAnswerGenerator.generate ──▶ answer* ──▶ done | error
                                (세션 풀 → thread/start → turn/start)
```

### 이벤트 시퀀스가 계약이다

| 이벤트 | 횟수 | 위치 | 데이터 |
|---|---|---|---|
| `sources` | 정확히 1회 | **항상 첫 이벤트** | `results`·`count`·`top_k`·`target_documents`. `results`는 `/search`와 **같은 모양**입니다 |
| `answer` | 0회 이상 | 그 뒤 | `text` — 생성기가 내보낸 조각 그대로 |
| `done` | 0 또는 1회 | **마지막** | `finish_reason`·`answer`·`citations`·`dropped_markers`·`elapsed_ms` |
| `error` | 0 또는 1회 | **마지막** | `code`·`message`·`attempts`·`reason` |

**스트림은 `done` 또는 `error` 정확히 하나로 닫힙니다.** 종료 이벤트 없이 연결이 닫히는 경우를 허용하면 클라이언트가 모든 스트림에 대해 "정상 종료인가 끊긴 것인가"를 타임아웃으로 추정해야 합니다.

이름 있는 SSE 이벤트(`event:` 필드)를 쓰는 이유는 브라우저 `EventSource`가 `addEventListener("sources", ...)`로 바로 분기하기 때문입니다. 데이터 안의 `type` 필드로 구분하면 모든 소비자가 파싱 후 분기를 다시 만들어야 합니다.

`done`이 답변 전문을 **다시** 싣는 것은 의도한 중복입니다. 조각을 이어 붙이는 책임을 모든 클라이언트에 지우지 않으면서, "이어 붙인 결과 = 서버의 최종본"이라는 불변식을 테스트가 검증할 수 있게 만듭니다. 전체 답변만 필요한 소비자는 `done` 하나만 읽으면 되므로 **비스트리밍 엔드포인트를 따로 두지 않았습니다** — 표면이 둘이면 거절 두 갈래·인용 검증·오류 세 종류가 각 표면에서 각각 표현되고, 다음 change의 캐시도 두 경로에 붙어야 합니다.

**서버는 조각을 다시 만지지 않습니다.** 쪼개면 "스트리밍처럼 보이는 화면"만 얻고 첫 글자의 도착 시각은 1밀리초도 앞당겨지지 않으며, 합치면(예: 문장 단위로 모으면) 실제로 앞당겨진 도착을 되돌리는 일이 됩니다. 유일한 예외가 판정 줄 제거입니다(아래).

### 상태 코드로 끝나는 실패와 스트림 안으로만 알릴 수 있는 실패

**상태 코드는 첫 바이트와 함께 확정됩니다.** 스트림을 연 뒤에 발견한 실패는 어떤 방법으로도 상태 코드로 알릴 수 없습니다. 그래서 서비스가 두 단계로 갈려 있습니다.

```python
context = await qa_service.prepare(question, top_k=...)   # 예외가 그대로 올라간다 → 422 / 503
return StreamingResponse(qa_service.stream(context), media_type="text/event-stream")
```

| 실패 | 어디서 | 결과 |
|---|---|---|
| 빈 질문 · 문자 수/토큰 창 초과 · `top_k` 상한 초과 · `top_k < 1` | `prepare` 이전 | `422` + `/search`와 **같은 오류 코드** |
| 저장소 접근 실패 | `prepare` 안 | `503 storage_unavailable` |
| 생성 시도 소진 · 인증 부재 | `stream` 안 | `200` + `error` 이벤트 |

두 단계를 한 async generator에 넣으면 첫 `yield` 전에 던진 예외가 프레임워크 내부 사정에 달리게 되고, 실제로는 이미 `200`과 `text/event-stream` 헤더가 나간 뒤에 예외가 터져 **본문 없는 200**이 됩니다. 그 형태는 어떤 시나리오로도 잡히지 않습니다.

검색을 스트림 밖에서 끝내는 부수 효과가 하나 더 있고 그게 오히려 이득입니다 — `sources`에 실을 값이 스트림이 시작되기 전에 이미 손에 있어 첫 이벤트가 지연 없이 나갑니다. **`sources`가 생성보다 먼저 나가는 것이 스트리밍이 실제로 앞당기는 것의 전부입니다**(생성 지연 전체만큼). 답변을 기다리는 동안 클라이언트는 "무엇을 근거로 답하려는지"를 이미 보여줄 수 있습니다.

같은 이유로 저장소 장애를 `error` 이벤트로 내지 않습니다. 그러면 같은 장애가 `/search`(503)와 `/qa`(200 + 이벤트)에서 다르게 보여 모니터링이 규칙을 둘 들어야 합니다.

### 거절은 두 갈래이고, 판정 주체가 다르다

| 사건 | 판정 주체 | 생성기 호출 | `finish_reason` | `answer` |
|---|---|---|---|---|
| 근거 0건 | 서비스 | **안 한다** | `no_evidence` | **빈 문자열** |
| 근거는 있으나 무관 | 생성기(프롬프트) | 한다 | `insufficient_evidence` | 생성기가 쓴 사유 |
| 답변 | 생성기 | 한다 | `stop` | 생성기가 쓴 답변 |

**근거가 0건일 때 부르지 않는 이유**는 문맥이 빈 프롬프트에서 모델이 쓸 재료가 학습된 지식뿐이기 때문입니다. "문서에 없으면 답하지 마라"는 지시로 막는 것보다 애초에 묻지 않는 편이 확실하고 빠르고 쌉니다.

**그 대신 서비스가 문구를 쓰지도 않습니다.** `answer` 이벤트는 한 번도 나가지 않고 `done`의 `answer`는 빈 문자열입니다. 근거가 없다는 **사실**은 `finish_reason`이 나르고, 사용자에게 보일 **표현**은 소비자가 정합니다. 이 선택으로 "하드코딩된 응답" 경계가 해석 문제가 아니라 구조가 됩니다 — **답변 문자열을 만드는 곳은 생성기뿐이고 예외가 없습니다.**

그 규칙이 성립하려면 빈 `answer`의 의미가 하나여야 합니다. 그래서 **본문 없는 생성 출력을 성공으로 받지 않습니다** — 판정 줄만 내고 본문을 못 낸 회차는 생성 실패로 다뤄 재시도에 태우고, 소진되면 `error`로 끝냅니다. 받아 주면 그 장애가 사용자 화면에서 "근거가 없습니다"(정상)와 똑같은 빈 화면이 되어 조용해집니다.

### 모델 출력은 판정 줄 + 마커 산문이다

```
VERDICT: ANSWERABLE
교육비는 연 200만원까지 지원됩니다 [1]. 신청은 부서장 승인 후 가능합니다 [1][2].
```

CLI가 지원하는 `--output-schema`(JSON Schema 강제 출력)를 **쓰지 않습니다.**

- **JSON은 스트리밍과 원리적으로 충돌합니다.** 부분 JSON은 파싱할 수 없어 답변 문자열을 꺼내려면 전체를 받아야 합니다. 스키마를 쓰는 순간 스트리밍이 형식 때문에 불가능해지고, 나중에도 열리지 않습니다.
- **스키마가 검증 단계를 없애 주지 못합니다.** `cited_chunks: [7]`의 형식은 보증해도 근거가 2건일 때 `7`이 없다는 사실은 보증하지 않습니다. 범위 검증은 어느 형식을 쓰든 우리가 합니다.
- **판정 줄을 첫 줄에 두면 스트리밍과 판정이 양립합니다.** 첫 줄만 버퍼링하면 판정을 즉시 알 수 있고 나머지는 그대로 흘려보냅니다.

구조화 출력을 포기한 것이 아니라 **보증하는 주체를 모델에서 서버로 옮긴 것**입니다. `done`의 `citations`는 검증을 통과한 항목만 담긴 배열이고 그 모양은 pydantic 모델이 강제합니다 — 모델이 형식을 어겨도 소비자가 받는 구조는 흔들리지 않습니다.

| 입력 | 처리 |
|---|---|
| 첫 줄 `VERDICT: ANSWERABLE` | 나머지가 답변, 마커 검증 진행 |
| 첫 줄 `VERDICT: INSUFFICIENT` | 나머지가 거절 문구, `citations` 비움 |
| 판정 줄 없음 | `ANSWERABLE`로 보고 **경고 로그**. 답변을 버리지 않습니다 |
| 본문이 비어 있음 | 생성 실패 (재시도 대상) |

판정 줄이 없을 때 답변을 버리지 않는 이유는, 그 본문이 이미 근거만 주어진 프롬프트에서 나온 것이라 형식 위반이 곧 환각은 아니기 때문입니다. 마커 검증은 그대로 돌아 출처 보증도 그대로입니다.

### 판정 줄 분리 — 버퍼 상한이 21자다

**실측이 이 부품을 필수로 만들었습니다.** 판정 줄 하나가 델타 **7조각**에 걸쳐 옵니다 — `VER`·`DICT`·`:`·`' ANSW'`·`ER`·`ABLE`·`\n`. `core/prompting.py`의 `VerdictSplitter`가 그 경계를 흡수합니다.

- 개행이 오면 → 첫 줄을 판정으로 파싱. 형식이 맞으면 그 뒤가 본문, 안 맞으면 **첫 줄까지 통째로 본문**입니다.
- 누적 문자열이 `VERDICT: ANSWERABLE`·`VERDICT: INSUFFICIENT` 어느 쪽의 접두사도 아니게 되는 순간 → 판정 없음으로 확정하고 **즉시 전부 내보냅니다.**

두 번째 규칙이 없으면 판정 줄을 쓰지 않은 출력에서 첫 줄 전체(짧은 답이면 답변 전부)를 붙들게 됩니다 — **하필 형식을 어긴 회차에서만 스트리밍이 죽습니다.** 이 규칙 덕에 버퍼가 21자를 넘지 않고, 판정이 확정된 뒤의 조각은 변형 없이 그대로 나갑니다.

이 상태 기계가 `services/`가 아니라 `core/`에 있는 이유는 프롬프트 조립·파싱과 같습니다 — 순수 함수여야 비동기도 페이크도 없이 문자열 단언만으로 회귀를 고정할 수 있습니다. 이 change에서 가장 틀리기 쉬운 로직인데 서비스에 두면 그 테스트가 페이크 생성기와 이벤트 시퀀스 위에 서게 됩니다.

### 인용은 마커를 검증해 조립한다

`\[(\d+)\]`로 답변 본문을 훑어 등장 순서대로 번호를 모으고, 중복을 제거하고, `1..len(sources)` 밖은 버리고, 남은 번호를 `sources`의 해당 결과에 대응시켜 `citations`를 만듭니다.

- **본문의 마커는 지우지 않습니다.** 지우면 스트림으로 흘러간 문장과 `done.answer`가 달라져, 둘 다 받은 클라이언트가 무엇을 표시할지 알 수 없습니다.
- **버린 개수를 응답에 싣습니다**(`dropped_markers`). 숨기면 "인용이 왜 하나뿐인가"를 로그 없이 설명할 수 없고, 그 수가 늘어나는 것이 **프롬프트가 나빠졌다는 가장 이른 신호**입니다.
- **판정이 마커를 이깁니다.** `INSUFFICIENT`면 마커를 무시합니다 — 거절문에 출처가 붙으면 답변처럼 보입니다.
- 인용 항목의 값은 `ScoredChunk`에서 그대로 복사합니다. 인용은 그 스트림이 이미 보여 준 근거를 가리킬 뿐 새 사실을 만들지 않습니다.

존재하지 않는 번호를 가리키는 마커는 **환각의 한 형태이고, 겉보기에는 가장 그럴듯한 형태**입니다. 번호가 붙어 있으니 검증된 것처럼 보이는데 가리키는 곳이 없습니다. 조용히 통과시키면 출처 표기가 오히려 신뢰를 만들어 내는 장치가 됩니다.

마커가 하나도 없는 답변도 유효한 답변입니다. 그때 `citations`는 비고 `finish_reason`은 `stop`이며, 이 조합이 "답은 했는데 근거를 특정하지 못했다"를 유일하게 식별합니다.

### 재시도는 서비스가, 타임아웃은 어댑터가

| 관심사 | 소유 | 이유 |
|---|---|---|
| 한 시도의 시간 상한, 턴 중단과 자원 회수 | 어댑터 | 중단할 대상(턴)과 회수 방법을 아는 곳이 거기뿐입니다 |
| 시도 횟수, 백오프, 재시도 가부 | 서비스 | 정책입니다. 어댑터에 두면 생성기를 바꿀 때 정책이 따라 이사합니다 |

| 사유 | 재시도 |
|---|---|
| 시간 초과 (`LlmTimeout`) | 한다 |
| 세션 사망 · 핸드셰이크 실패 · 파싱 실패 · 빈 본문 (`LlmGenerationFailed`) | 한다 |
| 인증 부재·만료 (`LlmUnauthenticated`) | **안 한다** |
| 이미 조각을 내보낸 뒤의 모든 실패 | **안 한다** |

어댑터가 실패를 이 **세 예외로 정규화**하므로 생성 표면을 바꿔도 이 표와 서비스 코드는 그대로입니다.

**조각이 나간 뒤에는 재시도하지 않는다는 규칙이 스트리밍 때문에 생깁니다.** 재시도는 답변을 이어 쓰는 것이 아니라 처음부터 다시 쓰는 것이라, 이미 절반이 도착한 상태에서 두 번째 시도의 출력을 이어 붙이면 사용자는 앞부분이 두 번 적힌 답변을 봅니다. 스트리밍이 없었다면 존재하지 않았을 제약입니다.

**타임아웃 그 자체는 스트림을 끝내지 않습니다.** 스트림을 끝내는 것은 "시도가 다 떨어졌다"는 사건이고, 마지막 실패가 무엇이었는지는 `error` 이벤트의 `reason`(`timeout`·`generation_failed`·`unauthenticated`)이 말합니다. 그래서 타임아웃에 별도 오류 코드를 주지 않았습니다 — **코드는 종료 사건을, `reason`은 그 원인을 가리킵니다.**

백오프는 `base * 2^(n-1)`(1초 → 2초)이고 지터를 넣지 않습니다. 워커 1개에 동시 생성 상한이 2라 동기화된 재시도가 몰릴 규모 자체가 없고, 지터를 넣으면 "두 번째 대기가 첫 번째보다 길다"는 단언이 확률적이 됩니다.

**인증 부재는 즉시, 그 사실 그대로 실패합니다.** 백오프를 몇 번 돌아도 자격증명이 생기지 않습니다. `llm_unauthenticated`와 `llm_unavailable`을 나누는 이유는 소비자가 할 일이 다르기 때문입니다 — 앞은 `.secrets/codex/` 주입 경로 확인이고 뒤는 재시도나 상한 조정입니다. 뭉개면 평가자가 자격증명을 볼 이유를 못 찾습니다.

### 세션 풀 — 프로세스가 요청보다 오래 산다

기동 비용이 첫 요청에만 붙기 때문입니다. `thread/start` 첫 응답이 **7.87초**인데 같은 프로세스의 두 번째는 **0.07초**입니다(그 8초가 인증·세션 준비입니다 — 인증이 없는 회차에서는 0.17초에 끝납니다). 요청마다 프로세스를 띄우면 `exec`보다 빨라지지 않고 델타만 얻습니다.

| 규칙 | 이유 |
|---|---|
| 세션당 활성 턴은 **하나** | 둘이면 `turnId` 없는 알림의 주인이 모호해집니다 |
| thread는 요청마다 새로 (`ephemeral: true`) | 대화 맥락이 없고 이전 요청의 문서 본문이 다음 문맥에 남지 않습니다. 0.07초라 비용이 없습니다 |
| 반납 조건은 `turn/completed` 하나뿐 | **애매하면 버립니다.** 반쯤 끝난 턴을 반납하면 다음 요청이 이전 턴의 델타를 받습니다. 8초가 아깝지만 답변이 섞이는 것보다 훨씬 쌉니다 |
| 풀 상한 = `qa_concurrency` (기본 2) | 생성 하나가 프로세스 하나라, 상한이 없으면 컨테이너 메모리가 동시 요청 수에 비례합니다. 세마포어와 풀이 같은 수를 두 번 세지 않게 값을 공유합니다 |
| 상한에 걸린 요청은 **대기**한다 | 수집이 이미 같은 규율을 씁니다. 대기 중에도 `sources`는 이미 나갔고 하트비트가 연결을 유지하므로 사용자에게는 느린 생성과 구분되지 않습니다 |

**타임아웃과 취소는 프로세스가 아니라 턴을 끊습니다** — 프로세스가 풀 자산이기 때문입니다. `turn/interrupt`를 보내고 유예(`qa_llm_interrupt_grace_seconds`, 기본 2초) 안에 `turn/completed`가 오면 세션을 반납하고, 오지 않으면 폐기합니다. 시도가 만든 것은 thread와 turn이고 프로세스는 그 전부터 있다가 그 뒤에도 삽니다 — **둘 다 정리하므로 "중단된 시도의 자원을 회수한다"는 요구는 만족됩니다.** 이 독법을 적어 두는 이유는 나중에 누가 "프로세스를 죽이라는 뜻"으로 읽고 풀을 해체하는 일을 막기 위해서입니다.

에이전트를 텍스트 생성기로 좁히는 조치들은 위 "어댑터를 세 부품으로 가른 이유"에 있습니다.

### 하트비트와 계층 배치

```
api/routes/qa.py     요청 모델, prepare 호출(예외 → 상태 코드), StreamingResponse 조립
api/sse.py           SSE 프레이밍 — event:/data: 직렬화, 하트비트 주석, 펌프
api/queries.py       `/search`와 공유하는 상한 판정·근거 뷰·의존성 조회
services/qa.py       프롬프트 조립 → 생성 → 재시도 → 이벤트 시퀀스 산출
core/answers.py      FinishReason, Citation, 조립된 답변 값 객체
core/prompting.py    build_prompt / parse_answer / VerdictSplitter (표준 라이브러리만)
adapters/llm/        session.py · pool.py · codex.py
```

**SSE 프레이밍이 `api/`에 있는 이유**는 그것이 전송 형식이기 때문입니다. 서비스는 `QaEvent` 값 객체를 yield 하고, `event:`/`data:`/빈 줄로 바꾸는 일은 전송 계층이 합니다. 서비스가 `"event: done\n\n"`을 만들면 서비스 테스트가 SSE 문법을 단언하게 되어 전송 형식을 바꿀 때 함께 깨집니다.

**하트비트는 SSE 주석(`: keep-alive`)입니다.** 이벤트가 아니므로 클라이언트 파서에 나타나지 않고, 계약을 건드리지 않은 채 프록시의 유휴 연결 종료만 막습니다. 기본 간격 15초는 흔한 프록시 유휴 상한(60초)의 4분의 1입니다.

**그 하트비트를 끼우는 방식이 한 번 잘못돼 실물이 잡았습니다.** 처음에는 "다음 이벤트를 기다리되 상한이 지나면 주석을 낸다"를 `__anext__`마다 태스크를 만들어 구현했는데, 서비스가 든 `anyio.CapacityLimiter`가 **빌린 태스크에 묶여 있어** 반납하는 태스크가 달라지자 터졌습니다(`this borrower isn't holding any of this CapacityLimiter's tokens`). 전송 계층이 서비스의 실행 문맥을 쪼갠 대가입니다. 지금은 서비스 스트림 전체를 **한 태스크**에서 돌려 큐로 옮기고, 소비 쪽이 그 큐를 상한과 함께 기다립니다.

**클라이언트가 끊으면 그 펌프를 취소하는 것이 곧 생성 중단입니다.** 취소는 `aclosing`을 타고 어댑터의 `finally`까지 내려가 턴을 끊고 세션을 회수합니다. 정리하지 않으면 취소된 요청 하나가 프로세스 하나씩을 남기는데, 답이 마음에 안 들어 새 질문을 보내는 것은 흔한 조작이라 이 누수는 정상 사용에서 쌓입니다.

### 로그에 남기는 것과 남기지 않는 것

질문 문자열·근거 청크 본문·답변 본문을 로그에 쓰지 않습니다. 질문 자체가 개인정보일 수 있고 청크 본문은 문서 내용 그 자체이며, 답변은 그 둘을 합친 것이라 가장 민감합니다. 검색이 이미 같은 규율을 지킵니다.

```json
{"level":"INFO","logger":"app.services.qa","message":"답변 생성 요청을 처리했습니다","request_id":"...",
 "source_count":2,"target_documents":2,"finish_reason":"stop","citation_count":1,
 "dropped_markers":0,"attempts":1,"elapsed_ms":16650}
```

`dropped_markers`와 `attempts`가 여기 있는 이유가 위에 적은 그것입니다 — 프롬프트 열화와 생성 불안정이 각각 이 두 숫자로 먼저 드러납니다.

## 데모 UI

`demo-ui/`에 Vite + React + TypeScript 단일 페이지 앱이 있습니다. **서버 코드는 한 줄도 바뀌지 않았습니다** — 이 화면은 공개 계약(`/documents`·`/search`·`/qa`·`/health`)의 소비자일 뿐입니다. 레이어 밖에 있고, 안쪽 어느 것도 이 화면을 알지 못합니다.

만든 이유는 스트리밍이 **관측 가능해야** 하기 때문입니다. `curl -N`으로는 조각이 도착한 시각이 화면에 남지 않아, 답변을 모아서 한 번에 보내는 구현과 조각으로 흘리는 구현이 눈으로 구분되지 않습니다.

### CORS를 열지 않고 dev 프록시를 썼다

Vite dev 서버가 `/api`를 API로 넘깁니다. 브라우저는 5173 한 출처만 보므로 프리플라이트도 CORS 헤더도 필요 없습니다.

- 서버에 `CORSMiddleware`를 붙이는 대안은 **데모 하나 때문에 API의 공개 표면과 미들웨어 체인을 영구히 넓힙니다.** 허용 출처를 잘못 열면 그건 데모가 아니라 서버의 결함이 됩니다.
- 대가는 `npm run build` 산출물을 정적으로 열면 API 호출이 실패한다는 것입니다. 데모는 dev 서버로 도는 것을 전제로 두고 `README.md`도 그 경로만 안내합니다.

### `EventSource`를 쓸 수 없다

`/qa`는 `POST` + JSON 본문인데 `EventSource`가 만드는 요청은 **method가 GET으로 고정**입니다. body도 커스텀 헤더도 넣을 자리가 없습니다.

`GET /qa?question=...`을 따로 두는 대안은 셋 다 걸립니다. (1) 서버를 고쳐야 하고, (2) **질의 문자열이 로그·프록시 로그·브라우저 히스토리에 남습니다** — 이 코드베이스가 질의를 로그에 싣지 않기로 한 결정(`_log_search`)이 무력화됩니다. (3) `EventSource`의 `onerror`는 상태 코드도 본문도 노출하지 않아 422의 상한 값(`max_query_chars`)을 화면이 읽을 수 없습니다.

그래서 `fetch` + `ReadableStream`으로 프레임을 직접 팹니다. 디코딩에 `stream: true`가 없으면 한글이 UTF-8 3바이트라 **청크 경계에서 글자가 깨집니다** — 짧은 답변에서는 재현되지 않아 늦게 발견되는 종류의 결함입니다. `": keep-alive"` 주석 프레임은 파서가 버립니다.

자동 재연결을 얹는 라이브러리(`fetch-event-source` 등)를 쓰지 않은 것도 계약 때문입니다. 이 서버는 질문 하나가 스트림 하나이고, 자동 재연결은 같은 질문을 두 번 생성시킵니다.

### 화면이 새 사실을 만들지 않는다

- 답변 본문의 `[n]`은 **`done`이 실어 온 `citations`에 있는 번호만** 링크가 됩니다. 스트리밍 중에는 전부 평문입니다 — 검증 전 마커를 링크로 만들면 근거가 아닌 것이 근거인 척합니다.
- 검증되지 않은 마커를 본문에서 지우지 않습니다. 지우면 흘러간 문장과 `done.answer`가 달라집니다. 대신 `dropped_markers`를 그대로 표시합니다.
- 캐시는 아직 없으므로 `cache_hit` 자리를 화면에 두지 않았습니다. 서버가 주지 않는 값을 화면이 지어내지 않습니다.

시각·상호작용 규칙, 토큰 실측 대비율, QA 체크리스트는 [`demo-ui/DESIGN.md`](./demo-ui/DESIGN.md)에 있습니다.

## 헬스 리포팅

`GET /health`는 서비스 자신과 각 의존성의 상태를 함께 보고합니다.

```
정상        200  {"status":"ok",          "dependencies":{...}}
하나라도 불능 503  {"status":"unavailable", "dependencies":{...}}
```

**503일 때도 공통 오류 봉투를 쓰지 않습니다.** 헬스는 오류를 알리는 엔드포인트가 아니라 상태를 보고하는 엔드포인트입니다. 두 코드에서 본문 구조가 갈리면 소비자가 파서를 둘 들고 있어야 하고, "어느 의존성이 문제인가"가 오류 메시지 문자열 안으로 뭉개집니다. `dependencies`의 키 집합은 정상 여부와 무관하게 항상 동일해, 키가 사라지는 것과 값이 비정상인 것을 구분할 필요가 없습니다.

### 의존성마다 "도달 가능"의 의미가 다르다

| | 캐시 저장소 | 벡터 스토어 |
|---|---|---|
| 배치 | 별도 컨테이너 | 별도 컨테이너 |
| 불능의 의미 | 네트워크 도달 불가, 인증 실패 | 네트워크 도달 불가, 주소 오설정 |
| 점검 방법 | 연결 후 ping | 연결 후 heartbeat |

둘 다 **실제로 물어봅니다.** 클라이언트 객체가 만들어졌다는 사실만으로 정상 판정하면 서버가 없는 채로 "정상"이 나오고, 그 상태로 문서 수집을 시작하면 첫 업로드에서 터집니다.

임베디드 모드였을 때 이 프로브는 저장 경로에 실제로 파일을 써 보는 것이었습니다. 서버 모드로 옮기면서 "도달 가능"의 의미가 캐시와 같아졌고, 프로브도 같은 모양이 됐습니다.

### 시간 상한과 실패 격리

- 프로브는 **병렬 실행**하고 **의존성별 상한**과 **전체 상한**을 함께 겁니다. 하나가 매달려도 나머지 결과는 보고됩니다 — 어느 의존성이 문제인지 식별하려면 그래야 합니다.
- 벡터 스토어 프로브는 Chroma 파이썬 클라이언트가 동기 HTTP라 블로킹이므로 **스레드풀로 오프로드**합니다. 이벤트 루프를 막지 않는다는 규약이 여기서 처음 적용됩니다.
- 다만 상한이 걸리는 것은 **응답**입니다. 클라이언트가 자체 타임아웃을 노출하지 않아, 매달린 요청을 보낸 워커 스레드 자체는 라이브러리·OS 타임아웃까지 남습니다. 여기서 지켜야 할 성질은 헬스 응답 시간이 그것에 끌려가지 않는다는 것입니다.
- 프로브가 도달 불가가 아닌 **예상치 못한 예외**를 던져도 해당 프로브만 비정상으로 기록하고 나머지는 그대로 보고합니다. 프로브 하나의 버그가 헬스 엔드포인트 전체를 500으로 만들면, 정작 진단해야 할 때 아무것도 못 봅니다.
- 실패 사유 요약에는 접속 문자열·자격증명·스택 트레이스를 담지 않습니다. 원인 추적 정보는 로그로만 남깁니다.

헬스는 LLM 제공자를 의존성으로 보고하지 않습니다 — 이유는 [부팅 경로에서 CLI를 실행하지 않는 이유](#부팅-경로에서-cli를-실행하지-않는-이유)에 있습니다.

## 오류 처리

예외 → HTTP 변환은 **`api/` 계층에서만** 합니다. 라이브러리 예외가 라우터까지 그대로 새면 계층 경계가 무의미해지므로, 어댑터는 자기 경계에서 예외를 처리하고 `api/errors.py`가 HTTP로 옮깁니다.

> 헬스 프로브는 예외를 **던지지 않고** `ProbeResult`로 보고합니다 — 하나의 프로브 실패가 헬스 전체를 500으로 만들면 안 되기 때문입니다. 반대로 파서는 던집니다. `pymupdf.FileDataError`를 `DocumentParseError`로 바꿔 던지고, 아래 표가 그것을 422로 옮깁니다.

모든 오류가 같은 봉투를 씁니다.

```json
{ "error": { "code": "not_found", "message": "Not Found" } }
```

`code`는 `core/exceptions.py`의 `ErrorCode` 열거값입니다. 소비자가 메시지 문자열을 파싱하지 않고 분기할 수 있게 하는 것이 목적이라 값은 한 번 정하면 바꾸지 않고, 이후 change는 항목을 **추가**하는 식으로 확장합니다.

| 경로 | 상태 | code |
|------|-----:|------|
| 정의되지 않은 경로 | 404 | `not_found` |
| 지원하지 않는 메서드 | 405 | `method_not_allowed` |
| 요청 검증 실패 | 422 | `validation_error` (+ `fields`) |
| 도메인 예외 | 아래 표 | 예외가 정한 code |
| 미처리 예외 | 500 | `internal_error` |

**프레임워크 기본 응답도 덮어씁니다.** 기본값을 그대로 두면 형식이 두 가지가 되어 소비자가 파서를 둘 들고 있어야 합니다.

### 도메인 오류 코드 → HTTP 상태

`AppError`에는 상태 코드 개념이 없습니다. `code`만 있고, 그것을 몇 번으로 옮길지는 `api/errors.py`의 표가 정합니다.

| code | 상태 | 부가 항목 |
|------|-----:|---------|
| `unsupported_document_format` | 415 | `supported_formats` |
| `document_too_large` | 413 | `max_upload_bytes` |
| `empty_document` · `no_extractable_text` · `document_parse_error` | 422 | (스캔본은 `page_count`) |
| `not_found` | 404 | — |
| `storage_unavailable` | 503 | — |

**표를 도메인이 아니라 API 계층에 둔 이유**는 도메인이 HTTP를 알면 같은 예외를 CLI나 배치에서 재사용할 때 의미 없는 숫자를 들고 다니게 되기 때문입니다. **표에 없는 코드는 500**이라, 상태를 정하지 않은 새 예외가 들어와도 봉투는 그대로입니다.

부가 항목은 `AppError.extra`가 나르고 봉투에 **평평하게** 실립니다. 중첩 `details` 객체를 만들지 않은 것은 검증 오류의 `fields`가 이미 평면이기 때문입니다 — 같은 봉투에 형식이 둘이면 소비자가 파서를 둘 들어야 합니다. 값은 포맷 목록·바이트 수 같은 도메인 사실뿐이라 `core/`가 HTTP를 알게 되는 것은 아닙니다.

`storage_unavailable`은 저장소 어댑터가 라이브러리 예외를 옮길 때와, 수집 서비스가 실패한 교체를 되돌린 뒤 던집니다. 업로드 요청에서는 이것이 `503`으로 나갑니다 — 저장이 실패했는데 2xx로 답하면 그 응답이 거짓이 되기 때문입니다.

검증 실패에는 어떤 입력이 문제인지 알 수 있도록 위치와 사유를 싣되, **입력 값 자체는 싣지 않습니다** — 자격증명이 본문에 실려 오면 그대로 되비치기 때문입니다.

**`/health`는 이 변환의 대상이 아닙니다.** 라우트가 응답을 직접 반환하므로 예외 핸들러를 타지 않고, 503일 때도 상태 보고 본문을 유지합니다.

## 로깅

로그는 JSON 한 줄로 냅니다. 포맷 문자열에 따옴표를 끼워 흉내내면 메시지에 따옴표나 줄바꿈이 섞이는 순간 깨진 JSON이 나오므로, 포매터에서 직렬화합니다.

ASGI 서버는 자기 로거에 평문 핸들러를 붙이고 propagate를 끄기 때문에, 그대로 두면 형식이 두 가지가 됩니다. 핸들러를 걷어내 루트로 흘리고, 서버의 액세스 로그는 끕니다 — 같은 요청이 두 줄로 찍히기 때문입니다.

`RequestLoggingMiddleware`가 요청마다 식별자를 붙이고 메서드·경로·상태 코드·소요 시간을 한 줄로 남기며, `x-request-id`를 응답 헤더로 돌려줍니다. 요청 헤더로 들어온 값이 있으면 그대로 유지합니다. 스트리밍 응답이나 LLM 호출을 디버깅하려면 "어느 요청이 얼마나 걸렸고 무엇을 반환했는가"가 먼저 있어야 하므로, 이후 change의 전제입니다.

스택 트레이스는 로그에만 남고 응답에는 실리지 않습니다.

## 설정

환경변수를 읽는 곳은 `config.py` 하나입니다. 어디서 무엇을 읽는지 흩어지면 문서화가 불가능해지기 때문입니다.

모든 항목에 기본값이 있어 아무것도 주지 않아도 기동됩니다. 반대로 값이 무효하면 **조용히 기본값으로 넘어가지 않고 기동에 실패**하며, 어느 항목이 왜 무효한지 드러납니다. pydantic의 검증 오류는 그대로 흘리지 않고 도메인 예외(`ConfigurationError`)로 바꿔 던집니다 — 프레임워크 예외가 상위 계층으로 새지 않게 하는 규약의 일부입니다.

## 의존성 배선

배선은 앱 팩토리에서 한 번만 합니다. 모듈 전역 싱글턴을 두지 않는 이유는 테스트에서 교체가 어려워지기 때문입니다.

```python
create_app(settings=None, probes=None, parsers=None, embedder=None,
           vector_store=None, registry=None, generator=None) -> FastAPI
```

인자 하나하나가 **교체 지점**입니다. PDF 파서든 임베딩 런타임이든 벡터 스토어든 답변 생성기든, 갈아끼울 때 바뀌는 곳은 여기 한 줄이고 `services/`·`core/`·`api/`는 그대로입니다. 같은 통로로 테스트가 대역을 넣어 의존성 상태를 결정론적으로 구성합니다.

팩토리가 세우는 서비스는 넷입니다 — `HealthService`·`IngestionService`·`RetrievalService`·`QaService`. 가운데 둘은 **같은 벡터 스토어와 같은 레지스트리 인스턴스**를 봅니다. 검색의 대상 집합은 수집이 방금 커밋한 값을 읽어야 하므로, 여기서 하나로 묶어 두지 않으면 업로드 직후의 문서가 검색되지 않습니다. `QaService`는 그 `RetrievalService`를 그대로 받습니다 — `/qa`와 `/search`가 같은 검색을 보는 것이 "답이 이상할 때 원인을 가르는 관측 지점"이 성립하기 위한 조건입니다.

**`generator`를 주지 않으면 세션 풀과 Codex 어댑터가 배선되고, 그 풀은 첫 대여까지 프로세스를 띄우지 않습니다.** 주면(테스트) 풀 자체가 만들어지지 않습니다 — 대역을 꽂는 통로가 곧 "CLI를 아예 배선하지 않는" 통로입니다. 종료 시 회수는 lifespan의 `finally`가 합니다.

### 색인 서명은 배선이 유도해 주입한다

`derive_index_signature(...)` 호출은 **팩토리에 한 번만** 있고, 결과 문자열이 서비스에 주입됩니다. 처음에는 `IngestionService`가 생성자에서 스스로 유도했습니다 — 수집이 유일한 소비자였을 때는 그것으로 충분했습니다.

검색이 같은 값을 필요로 하면서 조건이 달라졌습니다. 검색은 "지금 유효한 청크"만 보기 위해 현재 `index_signature`로 필터하는데, **두 서비스가 각자 유도하면 재료 목록이 두 곳에 생깁니다.** 한쪽만 고쳐진 순간 수집이 청크에 찍는 서명과 검색이 찾는 서명이 어긋나고, 그러면 방금 업로드한 문서가 검색되지 않으면서 **어디에도 오류가 남지 않습니다** — 두 값이 각자 자기 기준으로는 옳기 때문입니다. 저장은 성공하고 검색은 빈 결과를 돌려주므로, 로그로도 상태 코드로도 드러나지 않습니다.

기각한 대안 둘입니다. 검색 서비스가 수집 서비스에서 값을 꺼내 오는 방식은 서비스가 서비스를 참조하게 만들어 계층이 옆으로 얽힙니다. 유도 로직을 양쪽에 복제하는 방식은 문제 자체를 그대로 둡니다.

대가는 `IngestionService` 생성자 시그니처 변경과, 그것을 부르는 테스트 하네스(`tests/ingestion_harness.py`) 수정입니다. **관측 가능한 수집 동작은 바뀌지 않습니다** — 같은 재료로 같은 값이 나오고, 여전히 기동 시점에 한 번 고정됩니다(요청마다 다시 유도하면 요청 중간에 구성이 바뀐 것처럼 보일 수 있고, 무엇보다 이 값은 요청과 무관한 상수입니다). 기존 수집 테스트가 전부 그대로 통과하는 것이 이 변경의 합격 조건이었습니다.

기동 시 실행되는 것은 둘입니다(FastAPI lifespan) — **임베딩 모델 선로딩**과 **저장소 정합 정리**. 선로딩이 먼저인 이유는 순서 의존이 아니라 관측성입니다: 오래 걸리는 쪽을 먼저 두어야 기동 로그가 무엇을 기다리는 중인지 순서대로 말합니다.

**둘 다 기동 조건이 아닙니다.** 실패하면 경고만 남기고 서비스는 뜹니다. 평가자가 처음 실행하는 한 줄이 부수 작업 하나 때문에 실패하면 안 되고, "설정을 전혀 제공하지 않아도 기동에 성공한다"는 요구사항이 그대로 유효합니다.

## 테스트 전략

| 대상 | 방식 |
|------|------|
| 헬스 시나리오 | 프로브 대역 주입 — 정상 / 캐시만 불능 / 벡터 스토어만 불능 / 둘 다 불능 / 무응답 / 한쪽만 무응답 / 프로브 예외 |
| 프로브 단위 | 실제 파일시스템(쓰기 가능·불가), 페이크 클라이언트(ping 성공·행), 도달 불가 주소 |
| 설정 | 기본값 기동, 무효값 기동 실패, 기본값 폴백 없음 |
| 오류 응답 | 404 / 405 / 422 / 도메인 예외 / 미처리 예외가 같은 봉투인지, 내부 정보가 새지 않는지, `/health`는 변환되지 않는지 |
| 청킹 | 불변식 넷(상한·겹침·포함 금지·누락 금지)을 문서 여러 종류에 대해 |
| 파서 | txt·md·PDF 실물 추출, 쪽 번호, 텍스트 레이어 없는 PDF, 깨진 바이트, 미지원 확장자 |
| 임베딩 | 페이크로 서명·차원·배치·토큰 가드. 인코딩 오프로드와 선로딩(로딩 + 인코딩 1회, 두 번 불러도 한 벌, 실패를 삼키지 않음)은 느린 모델 대역으로 가중치 없이. 실제 모델 계약은 가중치가 있을 때만 도는 테스트 5건 |
| 벡터 스토어 | **실물 Chroma 서버**로 메타데이터 왕복, 삭제 축 셋, 같은 리비전의 두 서명 공존, 컬렉션 차원 고정. 서버가 없으면 파일 전체를 건너뜁니다 — `docker compose run --build --rm test`가 함께 띄웁니다 |
| 문서 레지스트리 | 실물 SQLite(임시 파일)로 트랜잭션·영속성·스키마 자동 생성 |
| 수집 파이프라인 | 대역에 **실패를 주입**해 순서를 고정 — 부분 기록 되돌리기, 커밋 실패, 되돌리기까지 실패, 이전 청크 정리 실패, 기동 정리 규칙 둘, 문서별 잠금과 병렬성 |
| 오프로드·동시성 | 지연을 주입한 파서·임베더로 수집을, 지연을 주입한 임베더·저장소 질의·**토큰 계산**으로 검색을 진행시킨 채 `GET /health` 소요 시간을 재고 **주입한 지연보다 짧은지** 단언. 서로 다른 문서의 병렬성, 같은 문서의 직렬화, 재업로드-삭제 경합의 최종 상태. 시간 단언은 전부 주입한 지연 대비 **상대값**입니다 — 절대 시간은 느린 CI에서 무관한 이유로 깨집니다. 토큰 계산의 지연만 **블로킹**인 이유는 그 오프로드 책임이 어댑터가 아니라 라우터에 있어서입니다(프로토콜상 동기) — async 로 자면 `asyncio.to_thread` 를 걷어내도 루프가 멈추지 않아 아무것도 확인되지 않습니다 |
| 검색 서비스 | 페이크 임베더로 **순서와 판정** — 질의용 경로가 정확히 한 번(문서용은 0번), 대상 집합(문서 없음·삭제·이전 리비전 잔여·`stale`·다른 서명), 동시 변경(대상 집합 확정 뒤 교체 / 저장소 질의 뒤 삭제 / 한 문서 두 리비전 금지), 순서·K·결정성, 하한. 하한은 **점수 분포에서 유도**합니다 — 상수로 박으면 분포가 조금만 움직여도 단언이 공허해집니다 |
| 검색 API | 봉투 네 필드와 `count` 일치, 점수 범위·내림차순, `top_k` 지정, 출처(텍스트 오프셋이 원문의 부분 문자열인지 / PDF 쪽 번호 범위 / 문서 상세와의 대조), 거부 경로 일곱, 저장소 장애 503. **거부된 모든 경우에 질의 벡터가 계산되지 않고 저장소 질의가 호출되지 않았는지**를 대역의 호출 기록으로 단언합니다 — 프레임워크가 핸들러 진입 전에 끝내는 경로도 포함해서입니다(자동으로 성립하지만, 고정해 두지 않으면 검증이 핸들러 안으로 옮겨졌을 때 조용히 깨집니다) |
| 검색 품질 | **로컬 임베딩 실물**과 완전 탐색 스텁 저장소로 `sample-docs` 두 문서를 색인해, spec의 질의 4개가 각각 자기 주제 문서의 청크를 **1위**로 받는지와 무관 질의의 최고 점수가 그 넷보다 낮은지. 저장소를 실물로 두지 않는 이유는 검증 대상이 "임베딩이 의미를 잡는가"이지 "Chroma가 HNSW를 제대로 도는가"가 아니어서입니다 — 완전 탐색이면 근사 검색의 흔들림이 사라져 임베딩의 성질만 남습니다. 청크 크기는 기본값(600)이 아니라 200입니다: 기본값으로는 두 문서가 청크 셋이라 상위 5가 전부를 반환해 순위 단언이 무의미해집니다. 가중치가 없으면 파일 전체를 건너뛰고, **이미지에는 구워져 있어 `docker compose run --build --rm test`에서는 실행됩니다** |
| 문서 API | 상태 코드·`status` 네 값·저장 결과 대조(응답의 `chunk_count`만큼 실제로 저장됐는지), 거부된 업로드가 흔적을 남기지 않는지, 실제 Chroma 서버·SQLite 배선 1건 |
| 프롬프트·출력 파싱 | `core/`의 순수 함수라 **LLM도 비동기도 페이크도 없이 문자열 단언만**으로 — 문맥에 모든 근거가 번호와 함께 들어가는지, 판정 두 값·판정 줄 누락·마커 없음/중복/범위 밖·빈 본문, `VerdictSplitter`의 조각 경계(실측 모양 `VER`·`DICT`·`: ANSW`·`ERABLE`·`\n` 포함) |
| 답변 시퀀스·정책 | **스크립트된 페이크 생성기**로 이벤트 순서, 거절 두 갈래, 인용 검증, 재시도·오류 3종. 스펙의 "생성기 호출 횟수가 `2`"·"한 번도 호출되지 않는다"가 그 대역의 카운터로만 관측됩니다. 재시도 대기는 실제로 자지 않고 기록만 합니다 |
| 알림 파싱 | **저장해 둔 실물 알림 샘플**(`tests/fixtures/codex/`)로 델타 → 텍스트, 401 판정, 종료 판정. 손으로 지어낸 샘플은 우리가 상상한 형식을 검증할 뿐입니다 |
| 세션·풀 수명 | **가짜 app-server 스크립트**로 타임아웃·턴 도중 사망·취소·핸드셰이크 무응답·`turn/interrupt` 무시·상한 대기. 실물 CLI로는 인위적으로 만들 수 없는 실패들이라, 이 층은 실물 층의 대체재가 아니라 **실물 층이 닿지 못하는 곳**입니다 |
| 답변 API | `200`·`text/event-stream`·이벤트 이름 순서·`sources`가 `/search` 응답과 같은 모양, 스트림 **밖** 실패 여섯(전부 생성기 호출 0), 그리고 **도착 순서와 연결 종료**. 뒤의 둘은 `httpx.ASGITransport`가 앱이 끝난 뒤에야 응답을 돌려줘 관측되지 않으므로, ASGI 프로토콜을 직접 몰아 `http.disconnect`까지 보냅니다 |
| 로깅 | request-id 발급·유지, 액세스 로그의 메서드·경로·상태·소요시간 |
| 기동 | 자격증명 없음 / 판독 불가 양쪽에서 기동과 `/health` 200, 실제 프로브 배선이 외부 서비스 없이 응답, **선로딩이 실패해도 기동 성공 + 첫 업로드 성공**(백스톱) |
| 자격증명 동기화 | 스크립트를 실제로 실행 — 추출 성공 시 퍼미션(0600·0700), 자격증명이 없어도 0으로 종료, 재발급 명령 부재 |
| 하네스 | 픽스처 자체 검증 |

전체 스위트가 **외부 서비스와 자격증명 없이** 통과합니다. 환경에 없는 것이 필요한 테스트는 건너뛰되, 건너뛴 이유가 실행 결과에 남습니다.

| 명령 | 무엇을 더 덮는가 | 건너뛰는 것 |
|---|---|---|
| `docker compose run --build --rm test` | **전부** — 구조 층 + 실물 Chroma 어댑터 + 실물 임베딩(검색 품질) | 없음 |
| 호스트 `pytest` | 구조 층 전부 | 실물 Chroma(서버가 없으면), 실물 임베딩(가중치가 없으면) |

예전에는 `make test`·`make test-all`·`make test-container` 세 갈래였습니다. 각 층이 요구하는 것이 달라서였는데 — 구조 층은 아무것도 요구하지 않아야 하고(`pytest` 한 줄이 네트워크에 묶이면 요구사항 위반입니다), 실물 Chroma는 서버가, 실물 임베딩은 가중치가 필요합니다 — **compose 서비스 하나가 셋을 다 만족시킵니다.** 서버는 `depends_on`이 띄우고, 가중치는 **이미지에 이미 구워져 있습니다**(런타임의 선로딩이 다운로드를 기다리지 않게 하려던 결정의 부산물입니다). 컨테이너 안에서 `localhost:8001`이 벡터 스토어가 아니라는 문제는 `APP_VECTOR_STORE_URL`로 서비스 이름을 넘겨 해소했습니다.

테스트 스테이지는 런타임 이미지를 덧쌓아 만들어, 품질을 재는 모델과 배포되는 모델이 같은 것임을 구조적으로 보장합니다. 호스트에서 직접 `pytest`를 돌리는 경로도 그대로 살아 있고, 그때는 없는 것만 건너뜁니다.

실제 의존성과의 통합 확인(업로드 → 검색 왕복)은 사람이 1회 수행합니다.
