# Retrobot 회고 — 2026-08-04 22:41 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 20 (구현 6 · Retrobot 훅 11 · 서브에이전트 3) |
| 총 프롬프트 | 32 (실질 사용자 요청 15 · 훅 자동 실행 11 · 서브에이전트 지시 4 · 중단 2) |
| 에러 발생 | 60건 (**46건, 77%가 Retrobot 훅 자신**) |
| 창 안 커밋 | 38건 (기능·문서 19 · 회고 19) |
| 전체 테스트 | **933 passed, 23 deselected** · `pytest -m redis` 21 passed |

## 세션별 활동

### 세션 `75cd4368` (08-04 20:49 ~ 22:38) — 이번 회차의 본체

`add-response-cache` 3~6장을 한 세션에서 마감. 도구 1,009엔트리(Bash 110 · Edit 94 · Read 29 · Write 15), 에러 7건.

**프롬프트:**
1. (21:45) `23건 테스트 왜 생략된거지?`
2. (22:18) `음...CLI 실행해서 기본 모델 측정할 수 있나요? 만약, 측정 가능하다면, 기본 모델값을 해당 내용으로 넣어서 세팅값 넣으면 안되나요`

커밋 흐름: `b07c66d`(3장 Redis) → `5ea81bc`(4장 오케스트레이션) → `fe4bcfa`(5장 `/qa` 배선) → **`2336861`(6장 무효화, 7파일 +404/-38)**.

**발생한 에러 (7건, 전부 자체 게이트가 낸 것):**
- 21:10 `E501 Line too long (118 > 100)` — `tests/test_response_cache.py:101`
- 21:11 / 21:18 `src/app/config.py:91 인라인 주석 3줄 (상한 2)`
- 21:55 `ruff format` unformatted — `src/app/services/cache.py:28`
- 22:32 `E501 (103 > 100)` — `tests/test_cache_invalidation.py:11` (docstring 안 표)
- 22:28 **`859 passed, 23 deselected, 24 errors`** — `IngestionService` 시그니처 변경이 하네스 3개에 반영되지 않음

### 세션 `45f0bdd9` (08-04 20:13 ~ 20:48) — 캐시 설계

`add-response-cache` 6아티팩트 작성(Edit 22 · Write 6), 에러 0건. 사용자 지적 4건이 전부 설계에 반영됨:

1. `사용자가 질문을 했을 시, 질문에 대한 캐시를 설명하는 것이 맞나요?`
2. `상황/원하는 동작` 표 — 운영자가 껐다 → 항상 미스 / Redis 없이 테스트 → 정상 히트 / Redis 죽음 → 항상 미스 + 관측 가능
3. `수정해주세요.`
4. **`1. REPLACED 가 no-evidence 미삭제하는 것 확인해주세요. 2. insufficient_evidence 무효화 누락된 것 확인해주세요.`**

### 세션 `8ccc760d` (08-03 22:38 ~ 23:42) — SSE 마감

`스트리밍은 어떤 방식 쓰고 있지?` 한 줄로 시작해 `src/app/api/sse.py`·`tests/test_qa_api.py`·`ARCHITECTURE.md`(16회 수정)를 닫음. `.secrets/codex/auth.json`을 일부러 치우고 `-m llm` 층이 건너뛰는지 실측(에러 2건은 그 실측 과정).

### 세션 `724d2218` (08-03 22:47 ~ 08-04 00:31) — demo UI

프롬프트 8건, Write 56 · Agent 3 · Chrome MCP 22. `openspec new change add-demo-ui` → 5아티팩트 → 구현 → `demo-ui/DESIGN.md` 기반 재작업. 서브에이전트 셋이 리포 관례·API 계약·구현 설계를 병렬 조사.

### 세션 `58b22a1f` (08-04 00:31 ~ 00:51) / `2d412d02` (08-04 00:51 ~ 19:24)

전자는 `add-multiturn-qa` 4아티팩트, 후자는 `add-rrf-algorithm-spec` 7아티팩트. 둘 다 에러 0건. **전자의 산출물은 22시간째 커밋되지 않았습니다.**

### Retrobot 훅 세션 11건

`9d06bafd` · `d0c01226` · `cc79ede2` · `e4260f90` · `a2542476` · `115a7e25` · `e27b60d4` · `7a93d660` · `4e480d75` · `8486165b` · `25644f92`.
11세션 중 10세션이 **같은 두 줄**에서 죽음: `command -v python3 jq` 거부 → `/tmp` Write 거부. 에러 46건.

## KPT 회고

### Keep (좋았던 것)

- **핵심 3경로의 마지막 칸이 찼습니다 — `2336861`이 캐시 무효화 테스트 274줄을 올렸습니다.** 과제가 불합격 기준으로 못 박은 세 경로(ingestion · retrieval · 캐시 무효화) 중 마지막이고, 이 창 안에서 SSE → RRF → 캐시 6장이 순서대로 닫혔습니다. 부분 구현이 아니라 각 장이 커밋 경계와 일치한 채 닫힌 것이 중요합니다 — 되돌릴 자리가 장 단위로 남습니다.

- **무효화를 "긍정과 부정은 다른 사건에 반응한다"는 축으로 갈랐고, 그 축이 사용자 지적에서 왔습니다.** 20:43의 두 줄(`REPLACED가 no-evidence 미삭제`, `insufficient_evidence 무효화 누락`)이 설계 단계에서 들어왔고, 6장이 그것을 코드로 닫았습니다. `no_evidence` 항목은 근거가 0건이라 태그도 `source_versions`도 비어 있어 **문서 태그 무효화도 현재성 재검증도 닿지 못합니다** — 부정 집합이 유일한 방어선이라는 결론이 문서에 근거째 남았습니다. 조건을 문서 *개수*가 아니라 *내용*으로 잡아 `REINDEXED`까지 포함시킨 것도 같은 판단선상입니다.

- **무효화의 위치를 두 번 부정하고 정했습니다** — 커밋 *앞*이면 실패해 롤백된 수집이 멀쩡한 캐시를 날리고, 어댑터 *안*이면 테스트에서 벡터 스토어를 대역으로 갈 때 무효화가 함께 사라집니다. 그래서 `IngestionService`의 커밋 **뒤**. 고른 자리뿐 아니라 **버린 자리 둘과 그 이유**가 문서에 있어, 다음 사람이 "왜 어댑터가 아니지"를 다시 묻지 않습니다.

- **22:26의 `--build`가 실제로 결함을 잡았습니다.** `859 passed, 24 errors` — `IngestionService` 시그니처 변경이 `ingestion_harness` · `qa_harness` · `retrieval_harness` 셋에 반영되지 않은 것으로, 이미지를 다시 굽지 않았으면 초록불로 지나갔을 회귀입니다. 단언을 푸는 대신 하네스 셋을 함께 고쳐 933으로 복구했고, 그 셋이 한 커밋에 함께 들어갔습니다. `--build` 준수 **여덟 세션 연속**.

- **탐침을 리포에 남기지 않았습니다.** 22:19 `probe_model.py`(CLI 기본 모델 확인)와 22:39 `measure_threshold.py`(임계값 계측) 둘 다 루트에서 사라졌고, 후자는 `tests/test_cache_threshold.py`로 승격 중입니다. 일회용 스크립트가 리포에 눌러앉으면 다음 사람이 그것을 계약으로 읽습니다.

### Problem (나빴던 것)

- **무효화를 설명하는 `ARCHITECTURE.md` 21줄이 커밋에서 빠졌습니다.** `2336861`은 코드·테스트·`tasks.md`만 담았고, 문서 수정은 워킹트리에 `M ARCHITECTURE.md`로 남았습니다. **커밋된 문서는 지금도 "문서 변경 시 무효화는 아직 붙지 않았습니다"라고 말합니다** — 구현이 문서를 앞질렀고, 이 리포에서 문서-코드 불일치는 감점 항목입니다. 같은 모양의 누락이 3장(`b07c66d`에 `tasks.md` 빠짐)에서 한 번 있었고 4장에서 회수됐는데, 6장에서 대상만 바꿔 재발했습니다.

- **`README.md:532`가 세 번째로 낡았습니다** — `861 passed, 23 deselected`인데 실제는 **933**입니다. 21:49에 `660`→`861`로 고친 바로 그 줄이 한 시간 만에 다시 틀렸습니다. 손으로 적는 한 네 번째가 옵니다.

- **`src/app/config.py:91` 인라인 주석 위반이 일곱 회차째 살아 있습니다.** `src` 한정 위반은 **딱 1건**이라 닫는 비용이 사실상 0인데, 세션이 21:11 · 21:18 · 22:34 세 번 직접 관측하고도 지나갔습니다. 게이트가 빨간 채로 있으면 새 위반이 신호가 되지 못합니다 — 실제로 전체 위반이 501 → **527건**으로 늘었습니다.

- **`ruff format --check` 10건이 여덟 번째 재현입니다.** 22:32에 새 테스트 파일이 `E501 (103 > 100)`으로 걸린 것도 같은 뿌리입니다 — 코드는 88자로 손수 줄바꿈돼 있는데 `pyproject.toml:45`는 `line-length = 100`이라, `ruff check`는 100 기준으로 재고 **"All checks passed!"를 내놓습니다. 초록불이 거짓입니다.** 닫는 비용은 `.venv/bin/ruff format src tests` 한 줄 10초이고, 그 명령은 `README.md:319`가 평가자에게 시키는 것입니다.

- **`add-multiturn-qa` 4아티팩트가 22시간째 untracked입니다 — 다섯 번째 계수.** `58b22a1f`(08-04 00:31)이 `proposal`·`design`·`tasks`·`specs/` 셋을 다 만들었고, 그 사이 change 둘(`add-rrf-algorithm-spec`, `add-response-cache`)이 커밋됐습니다. **168시간 창은 커밋만 셉니다** — 트리에만 있는 산출물은 평가에서 존재하지 않습니다.

- **미푸시 32건. `add-response-cache` 브랜치가 origin에 아예 없습니다.** 원격에는 `add-rrf-algorithm`까지만 올라가 있어 **평가자가 지금 리포를 보면 캐싱은 착수조차 안 한 것으로 보입니다** — 비중 18%이자 불합격 기준(캐시 DB 미구현)입니다. 지난 회고에서 7건 → 13건 → 지금 32건으로 계속 벌어지는 중입니다.

- **외부 적대적 검토 0회, 열두 세션 연속.** 이 창에서 설계가 사람 지적으로 **다섯 번** 뒤집혔고(POST+SSE, `scope` 도입, 임계값, `REPLACED`/`insufficient_evidence` 무효화 누락) `openspec validate --strict`는 그중 한 건도 잡지 못했습니다. 형식 검증은 이 결함군을 구조적으로 못 봅니다.

- **Retrobot이 창 전체 에러의 77%(46/60)를 냅니다 — 12회차.** 훅 11세션 중 10세션이 같은 두 줄에서 죽었고, 원인은 `retrobot/SKILL.md` 자신인데 `afa9794`(08-03 22:23) 이후 **24시간 무수정**이며 회고 일곱 편이 같은 진단만 반복했습니다. 실측으로 확정된 사실: `command -v`는 **단독도 거부** · `/tmp` Write 10/10 거부 · **`python3 -c` 단독은 통과하고 `>`·`;`를 붙이는 순간 거부** · **`python3 - <<'PY'` stdin heredoc은 통과**(이 회고 전체가 그것으로 집계됐고, `75cd4368`도 같은 창에서 다섯 번 성공시켰습니다). `SKILL.md:54`의 heredoc 금지는 **Windows에서 참인 사실인데 호스트를 구분하지 않아** macOS에서 되는 유일한 경로를 막습니다. 대가는 `115a7e25`가 치렀습니다 — 두 번 막힌 뒤 python3를 재시도하지 않고 **Grep 30회**로 우회했습니다.

### Try (개선해야 할 것)

1. **`ARCHITECTURE.md`를 지금 커밋합니다** — 21줄이 이미 쓰여 있고 `git add ARCHITECTURE.md` 한 번입니다. 구현이 문서를 앞지른 상태로 다음 장에 들어가면, 7장 커밋에서 이 diff가 섞여 "언제 무엇이 참이었는지"가 사라집니다.
2. **`add-multiturn-qa`를 같은 커밋 또는 바로 다음 커밋에 넣습니다.** 다섯 회고 연속 이월이고, 미완성 상태의 커밋도 괜찮다고 과제 규칙이 명시합니다.
3. **`add-response-cache`를 push합니다.** 32건이 로컬에만 있고, 그중 8건이 비중 18%짜리 캐싱 전부입니다. 평가자가 볼 수 없는 작업은 하지 않은 작업입니다.
4. **`.venv/bin/ruff format src tests` + `config.py:91` 주석 2줄로 줄이기 — 합쳐 1분.** 7.7 태스크가 이미 이 둘을 요구하므로 새 일이 아니라 **지금 하면 되는 일**입니다. 게이트가 초록이 되어야 새 위반이 신호가 됩니다.
5. **`README.md`의 테스트 수를 숫자로 적지 않습니다.** 세 번 낡았고 손으로 고치는 한 네 번째가 옵니다 — "전체 스위트가 통과합니다(실물 CLI·Redis 층은 마커로 제외)"처럼 **변하지 않는 사실만** 적고, 정확한 수가 필요하면 명령 출력을 그대로 인용하게 둡니다.
6. **7장(임계값 계측) 전에 Codex 검토를 겁니다.** 남은 8태스크가 전부 계측·검증이고, 여기서 정한 `cache_semantic_threshold` 기본값은 **부정문 쌍이 임계값 위에 남는다**는 알려진 한계 위에서 골라야 합니다. 사람 지적으로 다섯 번 뒤집힌 설계에 외부 검토가 열두 세션째 0회입니다.
7. **`retrobot/SKILL.md` 세 줄을 고칩니다** — ① "먼저 `command -v python3 jq`를 쳐라"를 지우거나 호스트별로 가릅니다(이 호스트에서 첫 두 에러를 매 회차 만듭니다), ② "하지 말 것"의 heredoc 금지에 **Windows 한정**을 명시합니다, ③ 통하는 경로(`python3 - <<'PY'`)를 권장 절차로 올립니다. 일곱 편의 회고가 같은 진단을 냈고 실측도 끝났습니다 — 남은 것은 파일을 여는 일뿐입니다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 08-03 22:38~23:42 | `8ccc760d` | SSE 구현 마감 — `sse.py`·`test_qa_api.py`·`ARCHITECTURE.md`(16회), 자격증명 치우고 `-m llm` 건너뜀 실측 → `346c6df`·`dcd6bd7` |
| 08-03 22:47~00:31 | `724d2218` | `add-demo-ui` 5아티팩트 → Vite/React 구현 → `DESIGN.md` 기반 재작업, 서브에이전트 3 병렬 조사 → `f392b24` |
| 08-04 00:31~00:51 | `58b22a1f` | `add-multiturn-qa` 4아티팩트 작성 (**커밋되지 않음, 22시간째**) |
| 08-04 00:51~19:24 | `2d412d02` | `add-rrf-algorithm-spec` 7아티팩트 → RRF·BM25 구현 6커밋 → 아카이빙(`d320d8d`) |
| 08-04 20:13~20:48 | `45f0bdd9` | `add-response-cache` 6아티팩트, 사용자 지적 4건 반영(`REPLACED`/`insufficient_evidence` 무효화 누락 포함) → `f07ed41` |
| 08-04 20:49~21:59 | `75cd4368` | 캐시 1~4장 — 도메인(`23acd3a`) · 저장소(`a466d72`) · `scope` 스펙(`3eda547`) · Redis(`b07c66d`) · 오케스트레이션(`5ea81bc`) |
| 08-04 22:11~22:23 | `75cd4368` | 5장 `/qa` 배선 — 919 passed, CLI 기본 모델 실측 후 **설정에 박지 않기로 결정** → `fe4bcfa` |
| 08-04 22:23~22:38 | `75cd4368` | **6장 무효화** — 하네스 3개 회귀 잡고(24 errors → 0) `test_cache_invalidation.py` 274줄, **933 passed** → `2336861` |
| 08-04 22:41 | (이 회고) | 63/71 태스크. 남은 8건은 전부 7장 계측·검증 |
