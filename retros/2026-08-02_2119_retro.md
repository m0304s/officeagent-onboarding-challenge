# Retrobot 회고 — 2026-08-02 21:19 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 2 (구현 1 + Retrobot 1) |
| 총 프롬프트 | 1 (`/opsx:apply add-qa-retrieval`) |
| 에러 발생 | 9 (구현 세션 1 + Retrobot 세션 8) |

직전 회고(21:05) 이후 창(21:05~21:19)에 새로 생긴 활동만 다룹니다. 지정된 로그 14개 중
나머지 12개는 16:07·17:47·21:05 회고에서 이미 다뤘습니다.

## 세션별 활동

### 세션 `5f796aaf` (21:05 ~ 21:12) — 2장 구현

**프롬프트:**
1. `/opsx:apply` 재호출 — `add-qa-retrieval` 2장(태스크 2.1~2.5) 구현

**한 일:**

| 태스크 | 내용 |
|--------|------|
| 2.1 | `IngestionService` 생성자에서 `derive_index_signature` 호출 제거, `index_signature`를 인자로 수령 |
| 2.2 | `create_app`이 서명을 한 번 유도해 주입 |
| 2.3 | `tests/ingestion_harness.py`의 `make_service`가 서명을 유도해 전달 — 개별 테스트 무수정 |
| 2.4 | 수집 스위트 전체 재실행 — **340 passed (변경 전후 동일)**, 서명 관련 6건 개별 실행 전부 통과 |
| 2.5 | `ARCHITECTURE.md`에 "색인 서명은 배선이 유도해 주입한다" 절 추가 (+10줄) |

진행률 9/66 → **14/66**. 커밋 `8cdf619` (+56/-19, 5파일).

**발생한 에러:** 1건
- `ruff check .` 실패 — `Found 15 errors.` 전부 **`retrobot/_extract.py`** 에서 발생.
  `ruff check src tests`로 범위를 좁혀 통과시키고 넘어감.

### 세션 `90e8e04a` (21:05 ~) — Retrobot 자동 회고 생성

**발생한 에러:** 8건 — 전부 권한 거부
- `/tmp/retro_extract.py` 쓰기 거부 (훅 허용 목록은 `Edit(retros/**)` 뿐)
- `python3 retrobot/_extract.py ...` 실행 거부 (`Bash(git:*)` 만 허용)
- 이후 동일 계열 거부 반복

이 세션이 남긴 `retrobot/_extract.py`(git 미추적)가 곧바로 구현 세션의 `ruff check .`를
깨뜨렸습니다. 그리고 이번 회고 세션(`62f70e7c`)도 **같은 거부를 3건 반복**했습니다.

## KPT 회고

### Keep (좋았던 것)

- **동작 불변 리팩터링의 합격 조건을 숫자로 못 박고 지킴.** tasks.md가 "기존 수집 테스트가
  전부 그대로 통과하는 것이 합격 조건"이라고 미리 적어 두었고, 실제로 340 passed가 변경
  전후 동일했습니다. 리팩터링에서 테스트 수가 늘면 그건 리팩터링이 아니라는 판단 기준이
  코드가 아니라 계획에 먼저 있었던 점이 좋았습니다.
- **서명 관련 테스트 6건을 전체 스위트와 별도로 한 번 더 돌림.** 전체가 초록이어도 이 변경이
  겨냥한 불변식이 실제로 실행됐는지는 따로 확인해야 합니다 — 스킵이나 선택 누락은 전체
  카운트에 묻힙니다.
- **2.5(문서 갱신)를 다음 회차로 미루지 않고 같은 커밋에 넣음.** "수집 서비스가 기동 시점에
  고정한다"는 기존 서술이 코드와 어긋난 채로 남을 뻔했고, 이는 과제 기준에서 감점이 아니라
  불합격 사유(허위 기재)입니다. 직전 회고의 Try("설계 근거를 값 옆에")를 이번에도 이행.
- **기각한 대안 둘을 문서에 남김.** 검색이 수집에서 값을 꺼내오는 방식·유도 로직 복제 —
  왜 안 골랐는지가 있어야 나중에 같은 자리를 다시 뒤집지 않습니다.
- 7분에 5태스크. 계획이 확정된 뒤 구현이 기계적으로 흘러가는 상태가 두 회차 연속 유지됨.

### Problem (나빴던 것)

- **`make lint`가 지금 로컬에서 깨져 있습니다.** Makefile의 lint 타깃은 `ruff check .`인데
  `retrobot/_extract.py`에서 15건이 잡힙니다. 구현 세션은 `ruff check src tests`로 범위를
  좁혀 통과시키고 지나갔습니다 — 초록을 본 게 아니라 초록으로 보이는 범위를 고른 것입니다.
  (git 미추적 파일이라 평가자의 깨끗한 clone에서는 재현되지 않지만, 로컬에서 `make lint`가
  빨간 상태로 방치되면 다음에 진짜 lint 실패가 나도 눈에 띄지 않습니다.)
- **Retrobot 실행이 매번 같은 권한 벽에 부딪힙니다.** 훅의 허용 목록은
  `Bash(git:*)` · `Read` · `Glob` · `Grep` · `Edit(retros/**)` 인데, 로그(JSONL 수 MB)를
  파싱하려면 python이나 jq가 필요합니다. 90e8e04a가 8건, 이번 세션이 3건을 같은 이유로
  거부당했습니다. **회차마다 같은 시행착오를 처음부터 반복**하고 있습니다.
- **그 시행착오가 리포에 쓰레기를 남겼습니다.** `retrobot/_extract.py`는 실행 권한이 없어
  쓰이지도 못한 채 남아 lint를 깨뜨렸습니다. 실패한 우회 시도의 잔해가 본 작업을 방해한
  형태입니다.
- **동점 결정성(medium 지적)·검색 점수 하한 계측은 세 회차째 이월입니다.** 17:47·21:05에
  이어 이번에도 손대지 않았습니다. 미루는 이유가 난이도가 아니라 "지금 진행 중인 장에 없어서"
  라면, 이월 항목은 장 경계에서 매번 밀려나기만 합니다.

### Try (개선해야 할 것)

1. **`retrobot/_extract.py`를 지우거나, 살릴 거면 제대로 살린다.** 지금 상태(미추적 + lint
   위반 + 실행 불가)가 최악입니다. 다음 커밋 전에 삭제하고, `make lint`가 `ruff check .`
   그대로 초록인지 한 번 확인.
2. **Retrobot 훅의 허용 목록에 로그 파싱 수단을 넣는다.** `.githooks/post-commit`의
   `--allowedTools`에 `Bash(python3:*)`(또는 `Bash(jq:*)`)와 `Write(/tmp/**)` 대신
   `Edit(retros/**)`만 있는 현 구성이 원인입니다. 회고 품질이 아니라 회고 **비용**의 문제이며,
   훅 파일 한 줄 수정입니다.
3. **이월 2건(동점 결정성·하한 계측)을 3장 착수 전에 처리하거나, tasks.md에 명시적으로
   태스크로 박는다.** 회고 Try 칸에만 있으면 네 번째 이월이 됩니다. 어느 쪽이든 "회고 문서
   바깥"으로 옮기는 것이 이번 회차의 조건.
4. **범위를 좁혀 통과시킨 검사는 그 사실을 남긴다.** `ruff check src tests`로 바꿔 통과한
   것은 판단으로서 타당했지만(잡힌 15건이 과제 코드가 아니므로), 원인을 처리하지 않고
   좁힌 범위만 보고하면 다음 사람은 lint가 초록인 줄 압니다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 21:05 | `5f796aaf` | `/opsx:apply add-qa-retrieval` — 2장 선택, 컨텍스트·대상 코드 읽기 |
| 21:05 | `90e8e04a` | Retrobot 21:05 회고 생성 시작 (권한 거부 8건, `retrobot/_extract.py` 잔해) |
| 21:07 | `5f796aaf` | 2.1 서명 유도 제거 → 2.2 `create_app` 주입 → 2.3 하네스 수정 |
| 21:09 | `5f796aaf` | 2.4 전체 스위트 340 passed(불변), 서명 6건 개별 통과 |
| 21:10 | `5f796aaf` | `ruff check .` 15건 실패(`retrobot/_extract.py`) → `src tests`로 좁혀 통과 |
| 21:11 | `5f796aaf` | 2.5 `ARCHITECTURE.md` "색인 서명은 배선이 유도해 주입한다" 절 추가 |
| 21:14 | — | 커밋 `8cdf619` refactor: 서명 생성을 앱 팩토리로 이동 (14/66) |
| 21:15 | `62f70e7c` | Retrobot 이번 회고 생성 (같은 권한 거부 3건 반복) |
