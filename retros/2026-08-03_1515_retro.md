# Retrobot 회고 — 2026-08-03 15:15 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 3 (직전 회고 이후 새 작업은 1개) |
| 총 프롬프트 | 9 (새 세션 기준) |
| 에러 발생 | 5 (새 세션) + 11 (Retrobot 실행 세션) |
| 커밋 | `441a03f` (+183/-16) · `9bbe7db` (+342) |
| 테스트 | **418 passed** (직전과 동일 — 이번 회차는 실측·문서만) |

## 세션별 활동

### 세션 `1f98f356` (14:10 ~ 15:12 KST) — 이번 회차의 실체

**프롬프트:**
1. `/opsx:apply add-answer-generation` — "Task 1 작업 진행해주세요."
2. `/superpowers:brainstorming` — "codex 응답을 streaming으로 오게 할 수 없나?"
3. "C로 가겠습니다."
4. "진행해주세요."
5. "맞습니다. 그 다음 작업 진행해주세요"
6. "진행해주세요"
7. "작업 진행햊수에ㅛ."
8. "수정 진행해주세요."
9. `/opsx:update add-answer-generation` — "Codex Streaming 형식에 맞게 스펙 문서 업데이트해주세요."

**전반부 (14:10~14:33) — 1장 실측 8태스크 마감**

컨테이너 안에서 실물 `codex exec --json`을 돌려 형식·입자도·실패 형태·시간을 쟀습니다.
호스트가 아니라 **`api` 이미지 안**(`codex-cli 0.146.0`)에서 잰 값입니다.

| 측정 항목 | 결과 |
|-----------|------|
| 이벤트 형식 | JSONL 4줄 — `thread.started` → `turn.started` → `item.completed` → `turn.completed` |
| 답변 텍스트 위치 | `item.completed` 중 **`item.type == "agent_message"`** 의 `item.text` |
| 토큰 델타 | **없음.** 4,808자 답변도 한 이벤트로 통째로 |
| 한 턴의 `agent_message` | **1건이 아닐 수 있음** — 도구 사용 회차에서 3건 관측 |
| 소요 시간 (RAG 모양 6회) | 첫 이벤트 1.0~8.5초 · 답변 8.0~18.3초 · 종료 12.0~18.9초 → `qa_llm_timeout_seconds` **60초 유지** |
| 인증 실패 | 종료 코드로 구분 불가(모두 1). 판별은 문구 — 다행히 **stdout JSONL 안**에 `401 Unauthorized` |
| 뜻밖의 필수 인자 | **`--skip-git-repo-check` 없으면 빈 디렉터리에서 아예 못 돈다** (`Not inside a trusted directory`) |

stdout 원본은 `tests/fixtures/codex/` 4건(`answerable` · `insufficient` · `unauthenticated` ·
`tool_use`)으로 남기고, 채취 조건과 "파서가 이 파일들에서 읽어야 할 것"을 같은 디렉터리
`README.md`에 적었습니다. `docs/SPIKES.md` S-2의 남은 조각도 여기서 닫혔습니다.

**후반부 (14:35~15:12) — 사용자 질문 하나가 설계를 뒤집음**

"codex 응답을 streaming으로 오게 할 수 없나?" — 30분 전에 "델타 없음"으로 확정한 직후였습니다.
`codex exec`는 CLI의 유일한 표면이 아니었습니다. **`codex app-server`(stdio 위 JSON-RPC)는
토큰 단위 델타를 냅니다.** 실측으로 확인했습니다.

| | `codex exec --json` | `codex app-server` |
|---|---|---|
| 토큰 델타 | 없음 (4,808자도 한 이벤트) | **있음** (209자에 112건) |
| 요청당 지연 | 12~19초 | **4.5초** (프로세스가 따뜻할 때) |
| 인증 실패 판정 | 종료 코드 1 + 문구 매칭 (19.3초) | `httpStatusCode == 401` **구조화** (약 2초) |
| 성숙도 | 안정 | `[experimental]` |

`thread/start`가 첫 턴 8.11초 → 두 번째 턴 0.07초라는 것을 따로 재서, 8초가 기동 비용이며
**프로세스를 살려 두면 사라진다**는 것을 세션 풀 결정의 근거로 삼았습니다.
설계 문서 342줄을 `docs/superpowers/specs/2026-08-03-codex-app-server-streaming-design.md`로
커밋하고, 스펙 문서 갱신(`/opsx:update`)은 이 회고 시점에 진행 중입니다.

`specs/answer-generation/spec.md`는 **한 글자도 바뀌지 않습니다** — 스펙이 `exec`·JSONL·델타·
`codex`를 한 번도 언급하지 않는 고도로 쓰여 있었기 때문입니다.

**발생한 에러 (5건, 전부 셸/경로 혼용):**
- `sh: 0: cannot open C:/Users/SSAFY/AppData/Local/Temp/probe.sh` — 윈도우 경로를 컨테이너 셸에 그대로 넘김
- `Python was not found; run without arguments to install from the Microsoft Store` — 호스트에 `python3` 없음
- `docker compose cp /tmp/checkfix.py …` → `GetFileAttributesEx C:\tmp: The system cannot find the file specified` — Git Bash의 `/tmp`를 Docker CLI가 `C:\tmp`로 해석
- `json.decoder.JSONDecodeError: Expecting value: line 1 column 1 (char 0)`
- `FileNotFoundError: '/tmp/schema/AgentMessageDeltaNotification.json'`

에러로 잡히지 않은 같은 종류 1건 — **커밋 메시지에 PowerShell here-string 문법이 그대로
들어가 제목이 `@ docs: …`가 되었고**, 푸시 전에 발견해 `a87bf4e` → `441a03f`로 다시 썼습니다
(내용 차이 0).

### 세션 `076cba3d` (~14:09 종료) — 직전 회고에 반영됨

Codex 적대적 리뷰 → `ARCHITECTURE.md` 정정(`bd59e24`). 2026-08-03 14:16 회고가 다룬 내용입니다.

### 세션 `cd30845c` (~14:09 ~ 14:16) — Retrobot 실행 자체

회고 생성 세션. **에러 11건이 전부 샌드박스 거부**입니다 —
`ls` 작업 디렉터리 밖 차단 · `python3` 부재 · `Temp\retro_parse.ps1` 쓰기 미허용 ·
`Command contains subexpressions $()` · `Command invokes .NET methods` ·
`Command contains script block that may execute arbitrary code`.
이번 회고 실행에서도 같은 벽에 그대로 부딪혔습니다.

## KPT 회고

### Keep (좋았던 것)

- **실측을 표면 단위로 한정해 적었다.** `ARCHITECTURE.md`의 문장이 "Codex는 델타를 주지 않는다"가
  아니라 "`codex exec --json`은 델타를 주지 않는다"였기 때문에, 새 표면이 나타났을 때 **지워야 할
  값이 하나도 없었습니다.** 오히려 두 표면 대비표가 그대로 갈아탄 근거가 됐습니다. 측정값에
  측정 대상을 붙여 두는 것이 값 자체를 지키는 방법이었습니다.
- **픽스처를 런타임과 같은 이미지에서 떴다.** 손으로 지어낸 샘플이었다면 도구 사용 회차의
  `agent_message` 3건과 `command_execution` 혼입을 **상상하지 못했을 것**이고, 파서는
  `item.completed`만 보고 셸 명령 기록을 답변으로 착각한 채 통과했을 겁니다. 채취 조건과
  다시 뜨는 방법까지 같은 디렉터리에 남겨 CLI 버전이 오를 때 무엇이 낡는지 표시했습니다.
- **계약을 구현 세부보다 높은 고도에 두었던 것이 이번에 값을 치렀다.** `answer` 이벤트를
  "0회 이상"으로 열어 둔 덕에 표면 교체가 spec 0줄 수정으로 끝났습니다. 열어 둔 이유(한 턴에
  메시지가 여럿일 수 있음)와 이번에 도움이 된 이유(델타는 N건)가 서로 다른데도 같은 문장이
  둘 다 흡수했습니다.
- **성능 주장을 평균이 아니라 구간별로 쪼개 쟀다.** "4.5초"만 봤다면 `thread/start` 8초를
  요청 비용으로 오해했을 것이고, 세션 풀이 아니라 요청당 프로세스로 갔을 겁니다.
- **커밋을 관심사별로 둘로 나눴다** — 실측(`441a03f`)과 설계(`9bbe7db`). 훅이 `nohup`으로
  분리돼 커밋을 막지 않는지 먼저 확인한 뒤 커밋한 것도 포함.

### Problem (나빴던 것)

- **1장을 "실측 완료"로 커밋한 지 30분 만에 표면 선택 자체가 뒤집혔다.** 실측 계획이
  `codex exec` 하나만 후보로 두고, **이 CLI가 어떤 표면을 더 가지고 있는지 훑는 항목이
  없었습니다.** 더 나쁜 것은 발견의 계기가 절차가 아니라 **사용자 질문**이었다는 점입니다 —
  8/3 01:37 회고의 "레이어 감사가 9장이 아니라 사용자 지적에서 나왔다"와 정확히 같은 모양이고,
  같은 자리가 이틀 만에 다시 열렸습니다.
- **셸 혼용이 세 세션 연속.** 12:31 회고가 셸 혼용 4건을 Problem으로 적었는데 이번에 5건,
  거기에 **커밋 메시지에 PowerShell here-string이 새어 제목이 `@ docs: …`가 되어 커밋을 다시
  쓰는 일**까지 더해졌습니다. 문제는 매번 다른 명령이 아니라 매번 같은 경계입니다 —
  호스트(Git Bash/PowerShell)와 컨테이너 사이에서 `/tmp`·`C:\`가 뒤섞입니다.
- **README `/search` 예시의 손으로 쓴 `0.8734`·`chunk_index: 1`이 다섯 번째 이월.** 이번에도
  파일을 열지 않았습니다. `chunk_index: 1`은 청크 1개짜리 문서에 나올 수 없는 값이므로
  단순 미갱신이 아니라 **문서-코드 불일치**입니다.
- **app-server 결정을 1턴·2턴 수준의 실측 위에서 확정했다.** `[experimental]` 표기이고,
  `turn/failed`라는 알림이 없어 **메서드 이름만 보면 실패를 성공으로 세는** 함정이 있는데,
  프로세스 사망 후 재기동 경로와 동시 2턴이 한 프로세스에서 섞이는지는 재지 않았습니다.
  세션 풀은 그 둘이 실패 경로의 전부인 구조입니다.
- **`retrobot/parse_sessions.py` 다섯 회차째 미작성, 샌드박스 벽 15번째 재발.** 이번 실행에서도
  `/tmp` 쓰기·`python3`·`node` 실행·PowerShell 스크립트 블록이 전부 거부되어 Grep·Read로만
  로그를 훑었습니다.

### Try (개선해야 할 것)

- **다음 change의 `tasks.md` 1장 첫 항목을 "표면 목록"으로 둔다.** `codex --help`의 서브커맨드를
  전부 적고, 각각이 우리 요구(토큰 델타 · 지연 · 오류 판정 · 구조화 출력)에 대해 무엇을 주는지
  한 줄씩. 후보를 하나로 정한 뒤 재는 것이 아니라, **재기 전에 후보를 세는 것**이 이번에
  빠졌던 단계입니다.
- **커밋 메시지는 here-string이 아니라 파일로 넘긴다** — 임시 파일에 쓰고 `git commit -F`.
  그리고 컨테이너 안에서 쓸 경로는 `docker compose exec`로 컨테이너 안에서 만들고,
  호스트의 `/tmp`를 경유시키지 않습니다.
- **README `/search` 예시는 회고 Try가 아니라 `tasks.md` 항목으로 박는다.** 다섯 번 이월한
  항목이 회고에만 있으면 여섯 번째가 됩니다. `add-answer-generation` tasks에
  "실제 `/search` 요청 1회를 쏘고 응답을 README에 붙여넣는다"를 추가합니다.
- **app-server 3장 태스크에 실패 경로 두 개를 명시한다** — (a) 프로세스를 강제 종료한 뒤
  다음 요청이 재기동·재핸드셰이크로 복구되는지, (b) 동시 2턴이 `threadId` 라우팅으로
  섞이지 않는지. 둘 다 통과하기 전에는 `[experimental]` 채택을 되돌릴 수 있게
  `exec` 어댑터 복귀 경로를 문서에 남겨 둡니다.
- **Retrobot 파서는 포기를 확정하고 스킬에서 지운다.** 열다섯 번 막힌 우회를 다섯 회차째
  Try로 옮겨 적는 것이 회고를 오염시키고 있습니다. `retrobot/SKILL.md`의 "python3/jq로
  집계" 지침을 "Grep 도구로 필드 단위 추출" 절차로 바꿔 쓰는 편이 정직합니다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| ~14:09 | `076cba3d` | Codex 적대적 리뷰 반영 · `ARCHITECTURE.md` 정정 커밋 (`bd59e24`) |
| 14:09~14:16 | `cd30845c` | Retrobot 회고 생성 (샌드박스 거부 11건) |
| 14:10 | `1f98f356` | `/opsx:apply` — `add-answer-generation` 1장 실측 착수 |
| 14:10~14:33 | `1f98f356` | 컨테이너 안 `codex exec --json` 실측 8태스크 · 픽스처 4건 채취 · `ARCHITECTURE.md` 실측 절 신설 |
| 14:33 | `1f98f356` | `418 passed` · `openspec validate --strict` 통과 |
| ~14:35 | `1f98f356` | 사용자 질문 — "codex 응답을 streaming으로 오게 할 수 없나?" → 브레인스토밍 진입 |
| ~14:40 | `1f98f356` | `codex app-server` 실측 — 209자에 델타 112건 확인 |
| 14:48 | `1f98f356` | 결정 ①: 지금 app-server로 간다 (exec 완주·양쪽 병행 기각) |
| 14:51 | `1f98f356` | 결정 ②: 지연 기동 세션 풀, 상한 `qa_concurrency` |
| 15:03~15:06 | `1f98f356` | 설계 문서 342줄 작성 · 기존 코드와 대조 |
| 15:07 | `1f98f356` | 실측 커밋 — 제목에 `@` 유출 발견, `a87bf4e` → `441a03f` 재작성 |
| 15:08 | `1f98f356` | 설계 문서 커밋 (`9bbe7db`) |
| 15:09~15:12 | `1f98f356` | `/opsx:update` — 스펙 문서 갱신 착수 (회고 시점 진행 중) |
