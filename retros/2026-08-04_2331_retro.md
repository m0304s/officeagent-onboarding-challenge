# Retrobot 회고 — 2026-08-04 23:31 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 18 (실작업 6 · Retrobot 훅 12) |
| 총 프롬프트 | 42 (실작업 30 · 훅 12) |
| 에러 발생 | 58 (그중 **45건(78%)이 Retrobot 자신**) |
| 창 내 커밋 | 39 (feat/docs 20 · 회고 19) |
| 최종 테스트 | `951 passed, 23 deselected` (23:25·23:28 `--build` 2회 확인) |

**분석 범위 구멍**: 지정된 18개 파일은 08-03 23:29 ~ 08-04 23:29 을 덮지만 **02:08 ~ 19:24 구간의 실작업 세션이 목록에 없습니다.** 그 사이 RRF 커밋 6건(`35b9c3a` 09:42 ~ `d320d8d` 17:29)이 있었으므로, 아래 KPT 는 그 작업을 포함하지 않습니다.

## 세션별 활동

### 실작업 세션

| 세션 | 시각 | 프롬프트 | 에러 | 주요 도구 |
|---|---|---:|---:|---|
| `8ccc760d` | 08-03 23:29~23:42 | 0 (이월) | 0 | Bash 16 · Edit 13 |
| `724d2218` | 23:43~00:31 | 6 | 3 | Write 52 · Bash 46 · Edit 19 · chrome MCP 21 |
| `58b22a1f` | 00:31~00:51 | 2 | 0 | Bash 17 · Write 6 |
| `2d412d02` | 00:51~19:24 | 2 | 0 | Bash 24 · Write 7 |
| `45f0bdd9` | 20:13~20:48 | 7 | 0 | Bash 30 · Edit 22 |
| `75cd4368` | 20:49~23:29 | 13 | 10 | **Bash 195 · Edit 96 · Read 29 · Write 16** |

**`75cd4368` 프롬프트 (이 창의 본체):**

1. 20:50 `/opsx:apply add-response-cache` — 캐시 도메인 구현
2. 21:00 2장 캐시 저장소
3. 21:16 `/opsx:update` — `scope` 반영
4. 21:22 3장 Redis 어댑터
5. 21:45 "23건 테스트 왜 생략된거지?"
6. 21:47 4장 캐시 오케스트레이션
7. 21:59 5장 + 2~4장 미완 태스크
8. 22:18 "CLI 실행해서 기본 모델 측정할 수 있나요?"
9. 22:23 6장 무효화
10. 22:38 7장
11. 22:55 "나중에 작업 진행하겠습니다. 명시해주세요"
12. 22:58 7.7 — `src`는 위반 없음, `tests` 폴더 정리
13. 23:29 `/opsx:apply add-response-cache`

**`45f0bdd9` (20:13~20:48) — 설계 검토 세션:**

- 20:15 `/opsx:propose` L1 Exact / L2 Semantic 캐시
- 20:31 "사용자가 질문을 했을 시, 질문에 대한 캐시를 설명하는 것이 맞나요?"
- 20:34 캐시 OFF / Redis 없는 테스트 / Redis 죽음 — 3상황 표 검토 요청
- 20:43 **"REPLACED 가 no-evidence 미삭제하는 것 확인해주세요 / insufficient_evidence 무효화 누락"**

**`724d2218` (23:43~00:31) — SSE + 데모 UI:**

- 23:44 프론트엔드 데모 UI 완성 요청 → 00:13 `demo-ui/DESIGN.md` 기반 수정 → 00:18 UI 통한 업로드·삭제 실동작

### 발생한 에러 (실작업 13건)

| 시각 | 세션 | 내용 |
|---|---|---|
| 23:58 | `724d2218` | 프록시 헬스체크 Traceback |
| 00:17 | `724d2218` | `cd: no such file or directory: demo-ui` |
| 00:18 | `724d2218` | chrome MCP 대상 탭 판정 실패 |
| 21:02 | `75cd4368` | 문서 문자열 파싱 exit 1 |
| 21:10 | `75cd4368` | `E501 (118>100)` — `test_response_cache.py:101` |
| 21:10 | `75cd4368` | `ruff format` 재포맷 요구 (`ruff check` 는 통과) |
| 21:11 | `75cd4368` | `config.py:91` 인라인 주석 3줄 |
| 21:33 | `75cd4368` | Edit 다중 매치 |
| 21:55 | `75cd4368` | `ruff format` 재포맷 (`cache.py:28`) |
| 22:32 | `75cd4368` | `E501 (103>100)` — `test_cache_invalidation.py` |
| 22:42 | `75cd4368` | `E501 (102>100)` — `test_cache_threshold.py` |
| 23:01 | `75cd4368` | `TypeError: unsupported operand type(s) for \|` |
| 23:11 | `75cd4368` | `49개 파일, 위반 187건` — 주석 정리 착수 시점 |

Retrobot 훅 45건은 전부 같은 두 줄입니다 — 별도 항목으로 뒤에 씁니다.

## KPT 회고

### Keep (좋았던 것)

- **`add-response-cache` 71/71 완주 — 20:49 착수, 23:28 마감, 2시간 39분에 8커밋.**
  `f07ed41`(설계) → `23acd3a`(도메인) → `a466d72`(저장소) → `3eda547`(scope 스펙, 코드 0줄) →
  `b07c66d`(Redis) → `5ea81bc`(오케스트레이션) → `fe4bcfa`(`/qa` 배선) → `2336861`(무효화) →
  `4425f5f`(임계값) → `1638473`(주석). 커밋 경계가 장 단위로 살아 있어 각 커밋이 무엇을
  더했는지 로그만 보고 읽힙니다.

- **주석 게이트가 187건에서 0건으로 닫혔습니다 — 일곱 회차 이월 항목의 종결.**
  23:11 에 `49개 파일, 위반 187건` 으로 시작해 23:27 `112개 파일, 위반 0건`. `config.py:91`
  인라인 주석은 여섯 회고가 연속으로 지적하고 지나간 항목이었습니다. 중요한 건 **어떻게**
  닫았는가입니다 — 밀려난 설명을 지우지 않고 `tests/README.md` 831줄로 옮겼고(`CLAUDE.md`
  의 "근거가 사라지면 다음 사람이 같은 함정을 다시 밟는다" 규칙), 규칙 자체도 `CLAUDE.md`
  에 +11줄로 명문화했습니다. 게이트를 통과시키려 근거를 버리는 쪽이 훨씬 쌌는데 그 길로
  가지 않았습니다.

- **문서-코드 불일치 두 건이 같은 커밋에서 닫혔습니다 — 3회차 재발의 첫 종결.**
  `b07c66d`(tasks.md 누락) · `2336861`(ARCHITECTURE.md 누락) · `4425f5f`(문서 3개 누락)로
  세 번 반복된 "구현이 커밋되고 문서는 워킹트리에 남는" 패턴이, `1638473` 에서
  `ARCHITECTURE.md`·`README.md`·`CLAUDE.md`·`tests/README.md` 를 코드와 함께 올리며
  끊겼습니다. `README.md:535` 의 테스트 수도 `660` → `861` → `933` 으로 세 번 낡았던 줄인데,
  이번엔 **23:25·23:28 에 `--build` 로 실측한 `951 passed, 23 deselected` 를 그대로** 적었습니다.
  허위 기재는 감점이 아니라 불합격인 항목입니다.

- **`--build` 30회 — 아홉 세션 연속 준수, 그리고 실제로 값을 했습니다.**
  22:28 재검증이 `36 failed, 859 passed, 24 errors`(`IngestionService` 시그니처가 하네스에
  미반영) 회귀를 잡았고, 단언을 푸는 대신 하네스 3개를 고쳐 `933 passed` 로 닫았습니다.
  `CLAUDE.md` 가 적어 둔 함정("`--build` 없으면 직전 이미지를 검사한다")이 문서로만 남지
  않고 실행 습관이 됐습니다.

- **사용자 지적 두 줄이 6장 전체의 설계 축이 됐습니다.**
  20:43 의 `REPLACED가 no-evidence 미삭제` · `insufficient_evidence 무효화 누락` 이 두 시간 뒤
  `2336861` 로 구현됐습니다. `no_evidence` 항목은 근거가 0건이라 태그도 `source_versions` 도
  비어 **문서 태그 무효화가 원리적으로 닿지 못하므로** 부정 집합이 유일한 방어선이고,
  조건을 문서 *개수* 가 아니라 *내용* 으로 잡아 `REINDEXED` 까지 포함시켰습니다.
  이로써 과제가 불합격 기준으로 못 박은 핵심 3경로(ingestion·retrieval·캐시 무효화)가 찼습니다.

- **22:18 "CLI로 기본 모델 재서 설정에 넣자" 를 실측한 뒤 거절했습니다.**
  컨테이너에서 `model/list`·`thread/start` 를 직접 쳐 `gpt-5.6-sol` 을 확인하고도 설정에 박지
  않았습니다 — 어댑터가 빈 값을 `thread/start` 에 넘기므로(`codex.py:178`) 적는 순간 관측이
  지시가 됩니다. 남는 구멍(CLI 업그레이드 시 옛 모델의 답이 새 모델의 답인 척 남음)은
  범위·수명(TTL 24h)·닫는 법까지 문서 양쪽에 남겼고, 탐침 스크립트는 리포에 안 남겼습니다.

### Problem (나빴던 것)

- **`add-response-cache` 브랜치가 origin 에 아예 없습니다 — 미푸시 36건, 다섯 회차 이월.**
  `git branch -r` 에 `add-response-cache` 가 없습니다. `f07ed41` 부터 `1638473` 까지 캐싱 전부가
  로컬에만 있고, 원격에는 RRF 까지(`origin/add-rrf-algorithm`)만 올라가 있습니다.
  **평가자가 지금 리포를 열면 캐싱은 착수조차 안 한 것으로 보입니다** — 비중 18% 이자
  "캐시 DB 미구현" 이라는 불합격 기준입니다. 이 창에서 가장 값이 큰 두 시간 반의 작업이
  통째로 안 보이는 상태이고, 7·13·32·34 에 이어 **36건** 으로 계속 커지고 있습니다.

- **`ruff format --check` 13건 — 열 번째 재현, 11 → 13 으로 증가.**
  같은 순간 `ruff check` 는 `All checks passed!` 입니다(컨테이너 23:25 검증도 마찬가지).
  `line-length = 100` 설정과 손수 88자로 접은 줄이 어긋나는 것이라 **초록불이 거짓** 이고,
  이 창에서 `E501` 로 세 번(21:10 · 22:32 · 22:42) 걸린 것도 같은 뿌리입니다.
  닫는 비용은 `.venv/bin/ruff format src tests` 한 줄 10초인데 열 회차째 방치됐습니다.

- **`add-multiturn-qa` 4아티팩트가 23시간째 untracked — 일곱 번째 계수.**
  00:37 에 `/opsx:propose` 로 만들어 놓고 그 사이 change 둘(`add-rrf-algorithm` 아카이브,
  `add-response-cache` 완주)이 커밋됐는데 이것만 워킹트리에 있습니다. 커밋하지 않으면
  168시간 창 안의 산출물로 계수되지 않습니다.

- **`tasks.md` 71/71 체크가 스테이지에만 있고 커밋에 없습니다.**
  `1638473` 이후 `git add` 만 된 상태라, 다음 커밋(이 회고)에 딸려 들어갑니다. 완주
  표시가 기능 커밋이 아니라 회고 커밋에 붙으면 `git log` 로 읽는 진행 이력이 어긋납니다.
  `b07c66d` 의 tasks.md 누락과 같은 계열의 미세 재발입니다.

- **외부 적대적 검토 0회 — 열네 세션 연속.**
  이 창에서 설계가 **사람의 지적으로 다섯 번** 뒤집혔습니다(20:31 캐시 대상 · 20:34 3상황
  분기 · 20:43 부정 집합 두 건 · 21:16 `scope` · 21:45 테스트 23건 누락). `openspec validate
  --strict` 는 그중 **한 건도** 못 잡았습니다. 형식 검증이 이 결함군에 무력하다는 것이
  다섯 번 확인됐는데도 Codex 검토는 한 번도 걸지 않았습니다.

- **Retrobot 이 이 창 에러의 78%(45/58) — 14회차, 훅 12세션이 전부 같은 두 줄에서 죽었습니다.**
  `command -v python3 jq` **12/12 거부**, `/tmp` Write **11/12 거부**. 원인은
  `retrobot/SKILL.md` 자신인데 `afa9794`(08-03 22:23) 이후 **25시간 무수정** 이고, 회고
  아홉 편이 같은 진단만 반복했습니다. 대가를 실제로 치른 사례가 있습니다 — `115a7e25` 는
  두 번 막힌 뒤 python3 재시도 없이 **Grep 30회** 로 우회했습니다.
  **실측으로 확정된 사실** (같은 창의 `75cd4368` 이 `python3 - <<'PY'` 를 **72회** 성공시켰습니다):

  | 수단 | macOS 이 호스트 | `SKILL.md` 현재 서술 |
  |---|---|---|
  | `command -v python3 jq` | 단독도 **거부** | 맨 먼저 치라고 지시 |
  | `/tmp` 에 스크립트 Write | **거부** | 스크립트는 `/tmp` 에 쓰라고 지시 |
  | `python3 -c '...'` 단독 | **통과** | — |
  | `python3 -c` + `>`·`;`·`&&` | **거부** | — |
  | `python3 - <<'PY'` heredoc | **통과** | "하지 말 것" 으로 금지 |

  heredoc 금지는 Windows 에서 참인 사실인데 호스트 구분이 없어, macOS 에서 **되는 수단을
  막고 안 되는 수단을 지시** 하고 있습니다.

### Try (개선해야 할 것)

1. **지금 즉시 push.** 다른 무엇보다 먼저입니다.
   ```bash
   git push -u origin add-response-cache
   ```
   36건이 origin 에 올라가야 캐싱(18%)이 평가 대상이 됩니다. 이후 각 장 마감마다 push 를
   커밋과 한 묶음으로 묶습니다.

2. **`ruff format` 을 열 번째 회고에서 끝냅니다.**
   ```bash
   .venv/bin/ruff format src tests && .venv/bin/ruff format --check src tests
   ```
   그리고 `docker-compose.yml` 의 `test` 서비스 검증 명령에 `ruff format --check` 를 넣어
   `ruff check` 만 보고 초록으로 오판하는 경로를 막습니다.

3. **`add-multiturn-qa` 를 커밋하거나 지웁니다.** 일곱 회차 이월은 "나중에" 가 아니라
   "안 함" 입니다. 착수 의사가 있으면 `docs: add-multiturn-qa 스펙 제안` 으로 지금 커밋하고,
   없으면 삭제해 워킹트리를 깨끗이 둡니다.

4. **`tasks.md` 를 지금 회고 커밋 전에 분리 커밋합니다.**
   ```bash
   git commit -m "docs: add-response-cache 71/71 완료" openspec/changes/add-response-cache/tasks.md
   ```

5. **`retrobot/SKILL.md` 를 실측대로 고칩니다** — 25시간·아홉 회고 동안 진단만 반복한
   항목입니다. 세 곳:
   - "먼저 `command -v python3 jq` 를 쳐라" → **삭제**. 대신 `python3 -c "print(1)"` 단독 호출로 판정.
   - "`/tmp` 에 스크립트를 쓴다" → **삭제**. `python3 -c` 단독 또는 heredoc 으로 인라인 처리.
   - "하지 말 것: heredoc" → **`uname -s` 로 호스트 분기**. Windows 에서만 금지, macOS 에서는 권장 경로.

6. **다음 change 착수 전에 Codex 검토를 한 번 겁니다.** 다섯 번 뒤집힌 것이 전부 사람의
   눈이었고 `validate --strict` 는 0건이었습니다. `add-multiturn-qa` 는 세션 상태가 캐시 키에
   섞이는 구조라 같은 계열의 결함이 나올 자리이고, 코드가 붙기 전이 반영 비용이 가장 쌉니다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 08-03 23:29 | `8ccc760d` | SSE 로직 테스트 마무리 → `dcd6bd7` |
| 23:43 | `724d2218` | 데모 UI 착수, chrome MCP 로 실동작 확인 |
| 08-04 00:13 | `724d2218` | `demo-ui/DESIGN.md` 기반 UI 수정 |
| 00:18 | `724d2218` | UI 통한 업로드·삭제 실행 검증 → `f392b24`·`2d215cf` |
| 00:37 | `58b22a1f` | `/opsx:propose add-multiturn-qa` (이후 23시간 untracked) |
| 00:54 | `2d412d02` | `/opsx:propose add-rrf-algorithm-spec` → `ff7a5ad` |
| 09:42~17:29 | *(로그 미제공)* | RRF 구현 6커밋 — `35b9c3a` ~ `d320d8d` |
| 20:15 | `45f0bdd9` | `/opsx:propose` L1/L2 캐시 설계 |
| 20:34 | `45f0bdd9` | 캐시 OFF·Redis 부재·Redis 사망 3상황 분기 검토 |
| 20:43 | `45f0bdd9` | **`no_evidence` 무효화 누락 지적** → `f07ed41` |
| 20:50 | `75cd4368` | 1장 캐시 도메인 → `23acd3a` |
| 21:00 | `75cd4368` | 2장 저장소 → `a466d72` |
| 21:16 | `75cd4368` | `scope` 스펙 수정 (코드 0줄) → `3eda547` |
| 21:22 | `75cd4368` | 3장 Redis 어댑터 → `b07c66d` |
| 21:45 | `75cd4368` | "23건 테스트 왜 생략?" → README 낡은 수치 발견 |
| 21:47 | `75cd4368` | 4장 오케스트레이션 → `5ea81bc` |
| 21:59 | `75cd4368` | 5장 `/qa` 배선 → `fe4bcfa` |
| 22:18 | `75cd4368` | CLI 모델 실측 후 설정 하드코딩 **거절** |
| 22:23 | `75cd4368` | 6장 무효화 → `2336861` (핵심 3경로 완성) |
| 22:28 | `75cd4368` | `--build` 재검증이 회귀 24 errors 포착 → 하네스 3개 수정 |
| 22:38 | `75cd4368` | 7장 임계값 계측 → `4425f5f` |
| 23:11 | `75cd4368` | 주석 정리 착수 — `49개 파일, 위반 187건` |
| 23:25 | `75cd4368` | `--build` 최종 검증 `951 passed, 23 deselected` |
| 23:28 | `75cd4368` | `tests/README.md` 831줄 + 문서 4종 → `1638473`, **위반 0건 · 71/71** |
