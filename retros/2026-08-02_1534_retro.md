# Retrobot 회고 — 2026-08-02 15:34 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

지정된 로그 파일 11개 중 최근 24시간(08-01 15:34 이후) 엔트리를 가진 것은 11개 전부이나,
08-01 저녁 구간은 [17:40](./2026-08-01_1740_retro.md)·[18:25](./2026-08-01_1825_retro.md)·
[21:25](./2026-08-01_2125_retro.md)·[13:02](./2026-08-02_1302_retro.md) 회고가 이미 다루었습니다.
**이 회고는 직전 회고 이후의 델타(08-02 13:03 ~ 15:35 KST)를 대상으로 합니다.**

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 3 (`f2564d3d` 구현 · `abf1f969` 회고 훅 · `18b09f4b` 조사) |
| 총 프롬프트 | 6 (실질 요청만; 훅 세션은 자동 주입) |
| 에러 발생 | 도구 에러 6건 · 진짜 테스트 실패 3건 · **의도한 변이 실패 6건** |
| 테스트 | 291 → 310 → 319 → **326 passed** (스킵 0) |
| 커밋 | `7782154` feat: API 동시성 검증 (+1087/-199, 29 파일) |
| change 진행률 | 78/87 → **82/90** (남은 8건 전부 섹션 9·10) |

## 세션별 활동

### 세션 `f2564d3d` (13:03 ~ 15:14) — 구현

도구: Bash 89 · Edit 60 · Read 7 · Write 4 · AskUserQuestion 1

**프롬프트:**
1. ChromaDB를 임베디드 모드가 아닌, docker-compose 파일 기반 **서버 모드**로 진행
2. `/opsx:apply 8장 진행해주세요` (오프로드·동시성 검증)
3. `/opsx:update add-document-ingestion 임베딩 모델을 애플리케이션 시작 시 자동 로딩하도록 수정`
4. 경고만 남기고 기동 계속, 실제로 요청을 한번 해보는 경우까지 진행 (AskUserQuestion 응답)
5. `/opsx:apply 4.3 4.8 4.9 4.10 작업 진행해주세요`

**발생한 에러:**
- `NameError: name 'needs_weights' is not defined` — `test_embedding.py` 수집 단계 실패.
  마커 정의를 옮기는 편집이 사용 블록과 어긋났습니다.
- `KeyError: 'document_id'` (`test_documents_api.py:432`) — 1 failed / 307 passed
- `AssertionError: 무엇이 준비되지 않았는지가 로그에 남지 않았다` — `caplog`이 경고를 못 잡음.
  원인은 `create_app`의 `configure_logging`이 **루트 핸들러를 비우는** 것이었고, 같은 테스트를
  두 번 재실행해 규명했습니다.
- `Blocked: sleep 25 followed by: ...` — 포그라운드 `sleep`이 하네스에 차단되어 호출 1건 소모
- `Exit code 1` × 2 (grep 무매치), `Exit code 7` (curl), 사용자 도구 거부 1건

### 세션 `abf1f969` (13:02 ~ 13:09) — Retrobot 훅

직전 커밋(`8466509`)의 회고를 생성하고 `0305693`으로 커밋. 사용자 프롬프트 없음.

### 세션 `18b09f4b` (15:27 ~ 15:35) — 조사 (진행 중)

도구: Bash 20 · Read 9 · Skill 1 (`systematic-debugging`)

**프롬프트:**
1. ChromaDB를 GUI로 연결해 전체 문서 업로드 → 임베딩 → DB Insert가 정상 동작하는지 확인 중.
   `document_chunks`·`document_chunks_d8`은 생성됐고 나머지 차원 컬렉션은 없다. SQLite에는
   문서 데이터가 정상. ChromaDB에 왜 데이터가 안 보이는지 체크

**재현 결과(15:35, 결론 전):**

```
document_chunks_d8   dim=8    02d71477-...
document_chunks      dim=8    4e7962c7-...
document_chunks_d384 dim=384  6067743d-...   → count now = 0
```

d384 컬렉션은 **존재하는데 비어 있습니다.** 원인 규명은 이 시점에 미완입니다.

## KPT 회고

### Keep (좋았던 것)

- **변이 테스트가 절차로 굳었다.** 8장에서 3종(`to_thread` 제거 → 8.1·8.4 실패, 문서별 잠금 →
  전역 잠금 → 8.4 실패, 잠금 무력화 → 8.5 실패), 4.9에서 2종(선로딩 호출 제거 → 2건 실패,
  `try/except` 제거 → 실패-내성 테스트 실패). 단언이 초록이라는 사실만으로는 테스트가 일하는지
  알 수 없다는 것을 매번 실측으로 닫았습니다. 배점표의 "테스트 의도의 타당성"에 직접 대응하는
  증거가 로그에 남았습니다.
- **테스트를 쓰다 커버리지 구멍 자체를 찾았다.** 8.2를 작성하면서 `FakeEmbedder`가
  `asyncio.sleep`으로 기다리니 루프를 놓아 주는 게 당연하고, 따라서 **실제 어댑터의
  `to_thread` 오프로드는 어디서도 검증되지 않았다**는 사실이 드러났습니다(태스크 4.4의 산출물에
  대응 테스트 부재). 느린 인코더를 끼우는 테스트를 추가해 막았습니다 — 실물 모델은 너무 빨라
  이 회귀를 드러내지 못한다는 판단까지 붙였습니다.
- **서버 모드 전환이 진짜 버그를 드러냈다.** Chroma는 컬렉션당 차원이 하나이고 **비운 뒤에도 그
  차원이 남습니다.** `4-dim add → delete all → count 0 → 8-dim add → FAILED`로 직접
  재현했습니다. 이대로면 `APP_EMBEDDING_MODEL`을 e5-small(384) ↔ e5-base(768)로 바꿨을 때
  기동 정리가 청크를 지우고 `stale`로 표시해도 **재업로드가 영구히 실패**하고, spec·README에 적힌
  "stale 문서는 재업로드로 복구된다"가 그 순간 거짓이 됩니다. 컬렉션 이름에 차원을 붙여
  (`document_chunks_d384`) 고치고, 버려지는 컬렉션을 기동 정리가 회수하지 않는다는 대가까지
  적었습니다. 임베디드 모드에도 있던 결함이라 전환이 **만든** 게 아니라 **드러낸** 것입니다.
- **문서-코드 불일치를 같은 턴에 닫았다.** 선로딩을 도입하면 ARCHITECTURE의 「모델 로딩은 첫
  호출까지 미룬다」가 허위 기재가 된다는 것을 스스로 지적하고 4.10에서 교체했으며, **바꾼 이유**
  (비용이 첫 업로드에 청구된다 / 실패가 첫 업로드에서야 500으로 드러난다)를 남겼습니다.
  CLAUDE.md가 가산점으로 명시한 형태입니다.
- **spec을 건드릴지가 갈리는 분기에서 임의로 정하지 않았다.** 선로딩 실패 시맨틱을
  AskUserQuestion으로 세 선택지(경고 후 계속 / fail-fast + MODIFIED Requirement / 설정 토글)의
  대가와 `preview`까지 붙여 제시했습니다. fail-fast를 골랐으면 `service-health` spec의 "설정
  없이 기동 성공"을 고쳐야 했다는 점을 미리 계산해 둔 것이 판단 비용을 줄였습니다.
- **시간 단언을 상대값과 조건 폴링으로 썼다.** 절대 시간은 느린 CI에서 무관한 이유로 깨지고, 그때
  지연을 늘려 무마하면 검증하려던 성질이 사라집니다. 고정 대기 대신 `until(lambda: parser.calls > 0)`로
  측정 시점을 잡고, `assert not ingesting.done()`을 함께 걸어 **수집이 끝난 뒤 잰 측정은 실패로**
  만들었습니다.
- **`/compact` 0회.** 직전 회고 Try 중 하나가 처음 지켜졌습니다. `f2564d3d`가 2시간 11분을
  compact 없이 마쳤고(누적 5회에서 증가 없음), 15:27 조사는 새 세션(`18b09f4b`)으로 열었습니다.
- **린트가 마지막에 터지지 않았다.** 이틀 연속 `E501`·`I001`이었던 패턴이 이번엔 없었습니다
  (매 블록 "ruff·`openspec validate --strict` 통과").

### Problem (나빴던 것)

- **섹션 9를 또 미뤘다 — 직전 회고 Try의 정반대.** [13:02 회고](./2026-08-02_1302_retro.md)에
  "섹션 9를 섹션 8보다 먼저 당긴다. 동시성 검증은 정확성 보강이지만 컨테이너 실행은 **불합격
  방어**"라고 적었습니다. 실제로는 8장을 먼저 했습니다. 세션이 스스로 "다음은 8장일까요, 아니면
  9.1·9.2를 먼저 해서 `docker compose up`을 닫을까요?"라고 물었고 8장이 선택됐습니다.
  9.1(CPU 전용 torch 휠)·9.2(가중치 굽기)·9.7(`docker compose up` 1회)·9.8(문서-코드 대조)이
  그대로 남았고, **"명시된 방법으로 실행 불가"는 이 과제의 1번 불합격 기준**입니다.
  326 passed는 이 위험을 조금도 줄이지 않습니다.
- **또 한 덩어리 커밋.** +1087/-199·29 파일에 서버 모드 전환 / 8장 / 4.3~4.10 세 블록이 뭉쳤고,
  커밋 메시지는 "API 동시성 검증" 하나입니다(셋 중 하나만 가리킴). 자연 경계는 로그에 **테스트
  수로 세 번** 찍혀 있습니다 — 310 / 319 / 326. "tasks.md 섹션 번호를 커밋 경계로 쓴다"는
  두 회고 연속 미이행입니다. 다만 15시간·+3943에서 2.5시간·+1087로 줄었습니다.
- **미해결 조사 중에 커밋했다.** 15:27에 "ChromaDB에 데이터가 안 보인다" 조사가 시작되고 15:34에
  커밋이 들어갔습니다. 15:35 재현 결과는 `document_chunks_d384` **count 0** — 방금 커밋한 서버
  모드 경로가 실제로 벡터를 쓰고 있는지가 아직 확인되지 않았습니다. **로컬 테스트가 초록인데 GUI에
  데이터가 없다**는 조합은 테스트가 실물 경로를 밟지 않고 있을 가능성을 가리키며, 그렇다면
  "310 passed / 326 passed"의 의미가 달라집니다.
- **`make verify`를 만들지 않았다.** 직전 회고 Try였고, 이번 커밋에서 Makefile을 실제로 고쳤는데
  (`vector-store`·`test-all` 추가) 그 자리에서 넣지 않았습니다. 검증은 여전히 `make test` +
  `make lint` 두 줄입니다. 이번에 린트가 안 터진 것은 운이고 구조가 아닙니다.
- **개발용 Chroma 서버에 8차원 컬렉션 둘이 쌓였다.** `document_chunks`(dim 8)와
  `document_chunks_d8`(dim 8) — 대역 임베더 차원이 그대로 누적됐습니다. 기동 정리가 버려진
  컬렉션을 회수하지 않는다는 사실은 이미 문서화해 두었으니 설계 결함은 아니지만, "왜 데이터가
  안 보이나"라는 조사에서 원인을 흐리는 노이즈를 스스로 만들었습니다.
- **파일 단위 수정을 부분 편집으로 처리해 두 번 손해 봤다.** `needs_weights` `NameError`는 마커
  정의와 사용이 서로 다른 편집에 걸쳐 있었고, `test_boot.py`의 `caplog` 문제는 같은 테스트를
  두 번 돌려서야 원인(루트 핸들러 비우기)에 닿았습니다.
- **하네스가 막는 명령을 던졌다.** 포그라운드 `sleep 25`가 차단되어 호출 하나가 버려졌습니다.

### Try (개선해야 할 것)

- **다음 작업은 섹션 9로 고정한다. 예외 없다.** 세 회고 연속 미룬 항목입니다. 순서는
  9.1(CPU 전용 torch 휠) → 9.2(가중치 굽기) → 9.7(`docker compose up` 1회) → 9.8(문서-코드
  대조). **`docker compose up` 1회가 초록이 되기 전에는 정확성 작업을 더 얹지 않습니다.**
- **조사 중인 결함이 있으면 커밋하지 않거나, 커밋 메시지에 미해결 항목을 남긴다.** 지금의
  "d384 count 0"은 이번 커밋 절반(서버 모드 전환)의 정당성을 흔듭니다. 조사가 끝난 뒤 결론을
  별도 커밋으로 남겨, 회고 타임라인에서 두 사건이 구분되게 합니다.
- **`make verify: test lint` 한 줄을 지금 넣는다.** 다음에 Makefile을 열 이유가 생기기를
  기다리지 않습니다. 두 회고 연속 Try로 적혔다는 사실 자체가, 계기를 기다리는 방식이 작동하지
  않는다는 증거입니다.
- **커밋 경계를 "테스트 수가 바뀌는 지점"으로 읽는다.** 섹션 번호는 두 번 실패한 기준입니다.
  이번엔 310 / 319 / 326이 로그에 남았고, 이 숫자가 오른 순간이 곧 "확인된 블록이 하나 닫혔다"는
  신호였습니다. 숫자가 오르면 그 자리에서 커밋합니다.
- **조사 재현은 깨끗한 볼륨에서 시작한다.** 8차원 컬렉션 둘을 지우고, 테스트가 쓰는 서버와 GUI로
  들여다보는 개발용 서버를 분리합니다. 지금 조사의 절반은 스스로 만든 노이즈를 걷어내는 데 쓰였습니다.
- **마커·픽스처 정의를 옮기는 편집은 파일 전체를 다시 쓴다.** 정의와 사용이 서로 다른 편집에
  걸치면 수집 단계에서 깨지고, 그때 실패는 검증이 아니라 순수한 손실입니다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 08-02 13:02 | — | 커밋 `8466509` (Ingest API) + 회고 `0305693` |
| 08-02 13:02~13:09 | `abf1f969` | Retrobot 회고 자동 생성 |
| 08-02 13:03 | `f2564d3d` | ChromaDB 임베디드 → **서버 모드** 전환 요청 |
| 08-02 13:0x~14:5x | `f2564d3d` | 전환 완료. 컬렉션 차원 고정 결함 재현·수정(`_d384`), 전용 `registry` 볼륨 분리, 프로브 테스트 전량 대역화 — `make test` 291 passed / `make test-all` 310 passed |
| 08-02 14:56 | `f2564d3d` | `/opsx:apply` 8장 — 오프로드·동시성 검증 착수 |
| 08-02 ~15:04 | `f2564d3d` | 테스트 9개 추가, 변이 3종으로 검증. `to_thread` 미검증 구멍 발견 — **319 passed** (79/87) |
| 08-02 15:05 | `f2564d3d` | `/opsx:update` — 임베딩 선로딩으로 계획 산출물 수정 (78/90, 코드 무변경) |
| 08-02 15:09 | `f2564d3d` | 실패 시맨틱 결정: **경고 후 기동 계속** + 지연 초기화를 백스톱으로 존치 |
| 08-02 15:14 | `f2564d3d` | 4.3·4.8·4.9·4.10 구현. `caplog` 루트 핸들러 문제 규명 — **326 passed** (82/90) |
| 08-02 15:27 | `18b09f4b` | `systematic-debugging` — "ChromaDB에 데이터가 안 보인다" 조사 착수 |
| 08-02 15:34 | — | 커밋 `7782154` **feat: API 동시성 검증** (+1087/-199, 29 파일) |
| 08-02 15:35 | `18b09f4b` | 재현 완료 — `document_chunks_d384` 존재하나 **count 0**. 원인 규명 진행 중 |
