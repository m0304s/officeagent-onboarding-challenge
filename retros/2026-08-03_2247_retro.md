# Retrobot 회고 — 2026-08-03 22:47 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 15 (구현·정리 7 · Retrobot 7 · 서브에이전트 1) |
| 총 프롬프트 | 25 |
| 에러 발생 | 39 (이 중 **30건(77%)이 Retrobot 세션**) |

직전 회고(22:23)가 22:22까지를 덮었으므로, **새로운 것은 세션 `8ccc760d`(22:23~22:46) 하나**입니다.
나머지 13개 세션은 이미 보고된 구간이라 재발 횟수만 갱신합니다.

## 세션별 활동

### 세션 `8ccc760d` (22:23 ~ 22:46) — 5장 API·SSE 엔드포인트

**프롬프트:**
1. `/opsx:apply add-answer-generation` — 5장, API 구현 및 SSE 엔드포인트 작업 진행해주세요.
2. 스트리밍은 어떤 방식 쓰고 있지?

**결과:** `346c6df`(+1032/-132, 10파일), 622 → **640 passed**, 진행 61 → **73/87**.
새 파일 `src/app/api/sse.py`·`routes/qa.py`·`queries.py`·`tests/qa_harness.py`·`tests/test_qa_api.py`.

**검증 경로:** `docker compose run --build --rm test` **3회** — 11 failed → 1 failed → 640 passed.

**발생한 에러:**
- `check_comments.py` exit 1 (신규 파일 위반, `src/` 분만 정리)

### 세션 `12eec827` (22:23 ~ 22:30) — Retrobot 직전 회차

**에러 9건** — `/tmp` 쓰기 거부 2건, `command -v python3 jq` 다중 연산 거부, `sed` 거부 등.
회고 본문에 우회로를 적었으나 `SKILL.md`는 고치지 않아 이번 회차가 같은 벽을 다시 만남.

### 이미 보고된 세션 (23:36 ~ 22:22)

`7e054299`·`cadbfc2d`·`8ccbec39`·`b42efae1`(add-qa-retrieval 4~9장·아카이브·레이어 감사·리팩토링),
`38dcf8a2`(4장 QA 서비스), `4f55c9d8`(회고 Try 이행 · 주석 규칙 · LLM 5파일), `31165d64` 외 Retrobot 6회.

## KPT 회고

### Keep (좋았던 것)

- **컨테이너 실행이 초록 테스트로는 안 잡힐 결함을 잡았다.** 첫 `--build` 회차에서 11 failed.
  원인은 하트비트를 위해 `iterator.__anext__()`를 매번 `ensure_future`로 감싼 것 — `anyio.CapacityLimiter`의
  borrower는 **획득한 태스크**인데 반납이 다른 태스크에서 일어나 `RuntimeError`가 났다. 서비스 스트림 전체를
  단일 펌프 태스크(`_pump` + `maxsize=1` 큐)로 옮겨 해결. 이 결함은 동시 상한이 걸린 경로에서만 드러나므로
  4장이 상한을 **관측 가능하게** 만들어 두지 않았다면 API 층에서 조용히 지나갔을 모양이다.
- **`--build`를 세 번 다 붙였다 — 두 세션 연속.** 17:41이 지적한 누락이 22:23에 이어 이번에도 0회.
  `CLAUDE.md`에 적힌 항목이 반복해서 지켜지는 첫 연속 구간이다.
- **유출 단언을 고칠 때 `/search`의 같은 자리를 먼저 읽고 판단했다.** `"주입된" not in response.text`가
  실패한 것은 유출이 아니라 **주입한 예외가 도메인 예외**(`tests/stubs.py:322` `StorageUnavailable("주입된 질의 실패")`)여서
  그 문구가 곧 우리 봉투의 `message`였기 때문. 테스트를 약화하는 대신 `/search`와 같은 단언
  (`set(error) == {"code","message"}` · `"Traceback" not in`)으로 바꾸고, 근거를 그 자리 주석에 남겼다.
- **경계 모듈을 먼저 뽑고 두 라우트가 공유하게 했다.** `queries.py`(`SearchResultView` 등)를 신설해
  `search.py`를 124줄 줄이고 `sse.py`가 같은 뷰를 쓰게 함 — "`sources` 이벤트가 `/search` 응답과 같은 모양"이라는
  계약이 **문서가 아니라 import로** 강제된다.

### Problem (나빴던 것)

- **23분 만에 `src/` 주석 위반 0건이 깨졌다 (0 → 1).** `src/app/api/sse.py:146 _pump docstring 4줄(상한 3)`.
  22:38에 `check_comments.py | grep "^src/"`로 0을 확인했고, **그 뒤 22:41의 버그 수정이 `_pump`를 새로 만들었는데
  재검사를 안 했다.** 커밋 전 마지막 검증은 `ruff check`와 테스트뿐. 610 → 0을 만든 커밋의 바로 다음 커밋이다.
- **`README.md:319`가 평가자에게 시키는 명령이 계속 실패하고, 이번에 악화됐다.** `ruff format --check .`
  실패 파일이 **2 → 5개** (`docs/superpowers/specs/...md`·`scripts/check_comments.py` + **이번 커밋의
  `src/app/api/queries.py`·`src/app/api/sse.py`·`tests/test_qa_api.py`**). 22:37에 `ruff format --diff . | grep "^---"`로
  **다섯 줄을 화면에 띄우고도** 고치지 않았다. 17:41 → 20:31 → 22:23에 이은 **네 번째 재현**.
  (호스트 `.venv/bin/ruff` 실측. 컨테이너 실측은 이번에도 승인 거부로 미확인 — 관측 위치를 명시함.)
- **`tests/` 주석 위반이 402 → 422로 늘었다.** 신규 `qa_harness.py`·`test_qa_api.py`·`test_concurrency_api.py`가
  규칙 도입 23분 뒤에 위반을 20건 더 얹었다. `CLAUDE.md:74`는 "판정은 `python3 scripts/check_comments.py`가 한다"고
  적지만 인자 없이 돌리면 실패한다 — 21:26에 택한 "`src/` 먼저"라는 범위가 **문서 문장에 안 실린 것**이 그대로 남아,
  새 파일이 계속 규칙 밖에서 태어난다.
- **4B.1 적대적 리뷰는 여전히 `[ ]` — 외부 검토 0회 다섯 세션 연속.** 미검토 누적이 4,321 + 2,543줄에
  이번 1,032줄이 더해졌고, 그 안에는 방금 실물 실행으로만 드러난 **태스크 경계·취소 정리** 코드가 있다.
  20:40 서브에이전트는 인용 부호 하나(`(eval):34: unmatched "`)로 1분 만에 죽었고 재시도가 없었다.
- **Retrobot 샌드박스 벽 22번째.** 이번 회차 거부 5건 — `command -v python3 jq`(다중 연산),
  `Write /tmp/retro_agg.py`, `python3 --version && ls`(다중 연산), heredoc 안의 `"$@"`(simple_expansion).
  22:23 회고가 **정확한 우회로를 이미 적어 두었는데**(`/tmp` 파일 생성은 막히고 `python3 -` stdin은 통한다)
  `retrobot/SKILL.md` 본문은 여전히 "파싱 스크립트가 필요하면 `/tmp` 아래에 쓴다"고 지시한다.
  **런타임이 읽는 파일은 `SKILL.md`이고 회고는 아무도 안 읽는다** — 20:31의 진단("이행된 Try는 tasks.md에 있던 것뿐")과 같은 모양.

### Try (개선해야 할 것)

1. **`retrobot/SKILL.md` 2단계의 "`/tmp` 아래에 쓴다"를 지우고 통하는 형태를 그대로 박는다** —
   `python3 - <<'PY' … PY` (파일 생성 없음), 그리고 **금지 목록에 `&&`·`;`로 명령 잇기와 heredoc 안의 `"$@"`·`$VAR`**를 추가.
   이번 회차가 성공한 정확한 형태이므로 다음 회차부터 거부 5건이 0이 된다. 이걸 안 고치면 23번째가 온다.
2. **`scripts/check_comments.py`를 커밋 직전 검증에 붙인다** — `ruff check` 다음 줄에 `python3 scripts/check_comments.py src`.
   `sse.py:146 _pump` 한 건이 지금 열려 있으니 먼저 닫는다(docstring 4 → 3줄).
3. **`ruff format .`를 실제로 실행해 5개 파일을 정리하고, `README.md:319`가 통과하는지 컨테이너에서 확인한다.**
   `--diff`로 목록만 보는 것을 세 회차 반복했다 — 목록을 보는 것과 고치는 것은 다른 행동이다.
   `docs/superpowers/specs/*.md`가 범위에 들어가는 게 맞는지도 이번에 결정한다(맞다면 포맷, 아니라면 `pyproject.toml`에서 제외).
4. **`CLAUDE.md:74`에 판정 범위를 적는다** — "`src/`가 판정 대상이고 `tests/`는 422건이 남아 있다"를 명시.
   범위가 문장에 없으면 새 파일이 매번 규칙 밖에서 태어난다.
5. **4B.1을 `codex exec` 한 번으로 좁혀 재시도한다** — 서브에이전트 디스패치는 두 번 다 실패했으므로 경로를 바꾼다.
   대상에 `346c6df`의 `sse.py`(펌프 태스크·`aclosing`·취소 시 좀비 태스크)를 추가.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 08-02 23:36 | `7e054299` | add-qa-retrieval 4장 — 저장소 임베딩 벡터 검색 |
| 23:49 | `e6173212` | Retrobot (에러 4) |
| 23:58 | `cadbfc2d` | 검색 서비스 구현 |
| 08-03 00:15 | `8ccbec39` | add-qa-retrieval API 계층 |
| 00:40 | `b42efae1` | 7·8·9장 → 아카이브 → 레이어 감사 → 리팩토링 |
| 01:16 / 01:37 | `6ef28f9b` `06eb7705` | Retrobot (에러 3 / 2) |
| 20:08 | `38dcf8a2` | 4장 QA 서비스 (`5f5d93f`) |
| 20:27 | `4f55c9d8` | 회고 Try 6건 이행 → 4B.1 리뷰 시도 실패 → 주석 규칙 도입 → LLM 5파일 정리 (`afa9794`) |
| 20:40 | `agent-a49a…` | 적대적 리뷰 서브에이전트 — 1분 만에 인용 부호 오류로 사망 |
| 22:23 | `12eec827` | Retrobot (에러 9) → `cde0f87` |
| **22:23** | **`8ccc760d`** | **5장 API·SSE — `queries.py`/`sse.py`/`routes/qa.py` 신설, 컨테이너 3회로 11 failed → 640 passed (`346c6df`)** |
| 22:45 | `8ccc760d` | 스트리밍 방식 설명 (app-server 델타 → 서비스 → SSE) |
| 22:47 | (이번) | Retrobot — 거부 5건, heredoc stdin으로 우회 |
