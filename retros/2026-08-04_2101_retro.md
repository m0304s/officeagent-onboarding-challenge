# Retrobot 회고 — 2026-08-04 21:01 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 16 (본 세션 13 + 서브에이전트 3) |
| 총 프롬프트 | 34 (사용자 실제 입력 기준) |
| 에러 발생 | 47 (이 중 34건 = 72%가 Retrobot 훅) |
| 커밋 (창 내) | 3 신규 — `f07ed41` · `0991010` · `23acd3a` |

분석 범위는 2026-08-03 20:50 ~ 2026-08-04 21:01 KST. **이 창은 직전 회고
(`2026-08-04_2050_retro.md`)와 거의 전부 겹친다.** 지정된 16개 파일 중 14개는 20:50 회고가
이미 다뤘고, 그 이후 새로 생긴 것은 두 개뿐이다 — 캐시 도메인을 구현한 `75cd4368`과,
20:50 회고 자신을 생성한 훅 세션 `a2542476`. 아래는 그 두 개를 중심으로 적고, 겹치는 부분은
직전 회고에서 미해결로 남은 항목만 다시 짚는다.

## 세션별 활동

### 세션 `75cd4368` (08-04 20:49 ~ 21:01) — 이번 창의 유일한 신규 구현

**프롬프트:** 2건

1. `/opsx:apply add-response-cache` — "캐시 도메인을 구현한다"
2. `/opsx:apply add-response-cache` — "2장 작업(캐시 저장소 구현) 진행해주세요"

**도구:** Read 18 · Bash 13 · Write 2 · Edit 1 · Skill 1

**발생한 에러:** 0건.

**산출물:** `23acd3a feat: 캐시 도메인 구현` — `src/app/core/cache.py` 183줄,
`tests/test_cache.py` 311줄, 총 +499/-5. `tasks.md`의 1장 다섯 항목(1.1~1.5)이 전부 `[x]`.
`37 passed in 0.03s`, `ruff check` · `ruff format` · `check_comments.py` 통과.

순수 함수 계층만 먼저 닫은 구조다 — `normalize_query`(NFC → strip → 공백 축약 → casefold),
`derive_cache_key`(다섯 재료 정규 JSON → SHA-256), 값 객체 `CachedAnswer`/`CacheLayer`/
`CacheLookup`, 코사인 유사도와 임계값 판정. I/O가 없으니 테스트가 0.03초에 끝난다.
임계값 판정을 어댑터가 아니라 도메인에 둔 것은 설계 결정 그대로다.

2장(캐시 저장소 구현)은 착수 직후 — `Dockerfile`의 test 스테이지와 `test_boot`의
리포 경로 관례를 읽는 데서 로그가 끝난다. **아직 커밋되지 않았다.**

### 세션 `a2542476` (08-04 20:49 ~ 20:50) — 직전 회고를 만든 훅 세션

**프롬프트:** 1건 (`SKILL.md` 전문)

**도구:** Bash 19 · Write 2

**발생한 에러:** 4건. 그중 두 건이 이번 회고의 핵심이다.

```
Error: This Bash command contains multiple operations.
       The following part requires approval: command -v python3 jq
Error: This command requires approval          ← 재시도한 `command -v python3` 단독
```

직전 회고가 Try 1순위로 적은 처방은 "`command -v python3 jq`를 **두 줄로 분리**하라"였다.
`a2542476`은 실제로 분리해서 재시도했고, **분리해도 거부됐다.** 처방이 틀렸다.

산출물은 `0991010 docs(retro): 자동 회고 생성 by Retrobot`.

### 나머지 14개 파일

직전 회고(`2026-08-04_2050_retro.md`)가 다뤘다. 요약만 옮긴다.

| 세션 | 시각 (KST) | 활동 | 에러 |
|---|---|---|---|
| `4f55c9d8` | 08-03 20:53~22:22 | 주석 규칙 반영, LLM 어댑터 5파일 → `afa9794` | 5 |
| `8ccc760d` | 08-03 22:23~23:42 | SSE 구현·테스트 → `346c6df` · `dcd6bd7` | 3 |
| `724d2218` + 서브 3 | 08-03 22:47~08-04 00:31 | 데모 UI → `f392b24` · `2d215cf` | 5 |
| `58b22a1f` | 08-04 00:31~ | `add-multiturn-qa` 4아티팩트 제안 | 0 |
| `2d412d02` | 08-04 01:12~ | `add-rrf-algorithm-spec` → `ff7a5ad` · `35b9c3a` | 0 |
| `45f0bdd9` | 08-04 20:13~20:48 | `add-response-cache` 설계 → `f07ed41` | 0 |
| 훅 5개 | 08-03 22:23~08-04 20:31 | 회고 생성 | 30 |

## KPT 회고

### Keep (좋았던 것)

- **설계에서 구현으로 넘어가는 데 12분, 에러 0건.** `45f0bdd9`가 20:48에 설계를 커밋했고
  `75cd4368`이 20:49에 구현을 시작해 21:00에 커밋했다. 그 사이 Read 18회로 기존 코드
  관례(`derive_index_signature`의 직렬화 규약, `ScoredChunk` 테스트 헬퍼, `PROMPT_VERSION`
  위치)를 먼저 확인한 뒤 썼기 때문에 되돌린 자리가 없다. 태스크 문장이 "`derive_index_signature`
  와 같은 직렬화 규약을 쓴다"처럼 참조 대상을 못 박아둔 것이 그대로 효과를 냈다.
- **순수 함수 계층을 I/O보다 먼저 닫았다.** 1장 전체가 `core/cache.py` 한 파일에
  외부 의존 없이 들어갔고, 테스트 311줄이 0.03초에 돈다. Redis도 컨테이너도 필요 없으니
  2장에서 저장소 세 구현(`Null`/`InMemory`/`Redis`)이 어떻게 흔들리든 이 층의 회귀는
  즉시 잡힌다. 과제 평가 기준의 "테스트 실행 용이성"에도 직접 맞는다.
- **커밋 직전 검증 3종을 빠뜨리지 않았다.** `ruff check` · `ruff format` ·
  `check_comments.py`를 컨테이너 안에서 돌린 뒤 커밋했다. 08-03 `4f55c9d8`에서 주석 규칙
  위반 4건을 사후에 처리했던 것과 대비된다 — 같은 검사를 앞으로 당겼다.
- **훅 세션이 거부당한 뒤 포기하지 않고 다른 경로를 찾아 회고를 완성했다.**
  `a2542476`은 두 번의 승인 거부 후 Bash 19회로 우회로를 찾아 `0991010`을 남겼다.

### Problem (나빴던 것)

- **직전 회고의 Try 1순위가 이행되지 않았고, 이행됐어도 틀렸을 처방이었다.**
  `retrobot/SKILL.md`의 마지막 수정은 `afa9794`(08-03 22:14)로, 20:50 회고 이후 손대지
  않았다. 게다가 그 회고가 제시한 "`command -v python3 jq`를 두 줄로 분리" 처방은
  `a2542476`이 실측으로 반증했다 — 분리한 `command -v python3` 단독도 승인 요구로 막혔다.
  **이 호스트에서 막히는 것은 multi-operation이 아니라 `command -v` 자체다.**
  회고가 실측 없이 원인을 추정해서 처방을 적었고, 그 처방이 다음 회차에 재검증 없이
  이월될 뻔했다. 샌드박스 벽 자체는 23회째 반복이다.
- **`add-multiturn-qa` 4개 아티팩트가 20시간째 커밋되지 않았다.**
  `58b22a1f`(08-04 00:31)이 `proposal.md` · `design.md` · `tasks.md` · `specs/`를 전부
  만들었는데 `git status`에 아직 `?? openspec/changes/add-multiturn-qa/`로 남아 있다.
  168시간 창은 커밋 기준이므로 **트리에만 있는 산출물은 평가에 존재하지 않는다.**
  직전 회고도 이 세션을 `add-rrf-algorithm-spec` 작업으로 잘못 적어(실제 rrf는 `2d412d02`)
  누락을 짚지 못했다.
- **`add-response-cache` 67태스크가 여전히 외부 검토 0회인 채로 구현이 시작됐다.**
  직전 회고의 Try가 "구현 착수 전에 외부 검토에 건다"였는데, 검토 없이 1장이 커밋됐다.
  세 번 뒤집힌 설계라는 사정은 그대로다. 특히 검토 대상으로 지목됐던 결정 12(구현 3분리)는
  지금 착수한 2장의 주제다 — 검토를 붙일 마지막 시점을 지나가는 중이다.
- **회고 창이 겹쳐 같은 내용을 두 번 적고 있다.** 이번 실행 대상 16개 중 14개가 12분 전
  회고와 동일하다. 훅이 커밋마다 도는데 창은 고정 24시간이라, 커밋 간격이 짧으면 회고가
  직전 회고의 재작성이 된다. 신규 구간(12분·2세션)에 비해 재분석 비용이 압도적이다.

### Try (개선해야 할 것)

- **`retrobot/SKILL.md`의 "집계 도구가 없거나 막힌 환경" 절을 이번 실측으로 다시 쓴다.**
  직전 회고의 처방(두 줄 분리)은 반증됐으니 그대로 옮기지 말 것. 실제로 확인된 사실만 적는다:
  1. macOS 호스트에서 `command -v` 는 **분리해도 승인 요구로 막힌다.** 가용성 확인을
     Bash로 하려는 시도 자체를 첫 단계에서 뺀다.
  2. 대신 **`Glob` → `Grep`(count) → `Grep`(`-o` + `head_limit`) 순으로 바로 들어간다.**
     이번 세션이 이 경로만으로 16개 파일의 프롬프트 수·에러 수·타임스탬프·도구 분포를
     전부 뽑았다. 프롬프트 본문은 `"role":"user","content":"[^"]{0,220}` 를 `-o` 로 자르면
     한 줄이 수 MB여도 안전하다.
  3. `git log` · `git show --stat` 같은 구체적 Bash는 통과한다. 막히는 것과 통하는 것을
     명령 단위로 나열해 다음 회차가 다시 탐색하지 않게 한다.
- **처방을 회고에 적을 때 "실측함 / 추정함"을 구분해 표시한다.**
  이번에 반증된 항목은 추정이었는데 실측처럼 적혀서 1순위 Try가 됐다. 검증하지 않은
  처방에는 그 표시를 남겨, 다음 회차가 먼저 재현부터 하게 한다.
- **`add-multiturn-qa` 를 지금 커밋한다.** 미완성 상태여도 무방하다 —
  `CLAUDE.md`가 명시한 규칙이고, 168시간 창은 커밋만 센다. 다음 작업 시작 전에 처리한다.
- **2장 착수 상태에서 `add-response-cache` 결정 12를 외부 검토에 건다.**
  세 구현 분리(`Null`/`InMemory`/`Redis`)와 차단기(타임아웃 0.2초·연속 3회·30초)는
  아직 코드가 없어 검토 반영 비용이 0이다. 2장이 커밋되면 그 비용이 생긴다.
- **훅 창을 "직전 회고 이후"로 좁히는 것을 검토한다.** `--since 24h` 고정 대신
  `retros/` 의 최신 파일 시각을 하한으로 쓰면, 이번처럼 14/16이 중복되는 재분석이 사라진다.
  겹치는 세션은 표 한 줄로 참조만 남기면 타임라인의 연속성은 유지된다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 (KST) | 세션 | 주요 활동 |
|------|------|----------|
| 08-03 20:53~08-04 20:48 | 14개 세션 | 직전 회고 `2026-08-04_2050_retro.md` 참조 |
| 08-04 00:31 | `58b22a1f` | `add-multiturn-qa` 4아티팩트 생성 — **미커밋 상태로 잔류** |
| 08-04 20:49 | — | `f07ed41 docs: 응답 레이어 L1/L2 캐시 설계` |
| 08-04 20:49 | `a2542476` | Retrobot 훅 — `command -v` 승인 거부 2건(분리 후에도) |
| 08-04 20:49 | `75cd4368` | `/opsx:apply add-response-cache` 1장 착수, 기존 관례 Read 18회 |
| 08-04 20:50 | — | `0991010 docs(retro): 자동 회고 생성 by Retrobot` |
| 08-04 20:5x | `75cd4368` | `core/cache.py` 183줄 · `test_cache.py` 311줄, `37 passed in 0.03s` |
| 08-04 21:00 | — | `23acd3a feat: 캐시 도메인 구현` (+499/-5), 1장 5/5 완료 |
| 08-04 21:01 | `75cd4368` | 2장(캐시 저장소) 착수 — Dockerfile test 스테이지 확인 중 |
