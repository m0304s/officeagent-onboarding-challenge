# Retrobot 회고 — 2026-08-05 10:05 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 13 (지시된 로그 파일 전부 — 작업 7 · Retrobot 훅 6) |
| 총 프롬프트 | 36 (사용자 문자열 엔트리 기준. 훅 6건은 `SKILL.md` 본문 하나씩) |
| 에러 발생 | 56건 (작업 28 · **훅 28 = 50%**) |
| 증분 (직전 회고 01:22 이후) | 세션 `81ff2e29` 28분, 5장 완료 **31→36/65**, 커밋 `173779b` |

지시된 13개 파일 중 **`81ff2e29` 하나만 직전 회고(01:22) 이후의 활동**을 담고 있습니다.
나머지 12개는 08-04 저녁~심야 KST 구간이고 그날의 회고 스무 편이 이미 다뤘으므로,
이 회고는 증분 세션을 자세히 보고 나머지는 집계로만 셉니다.

**시각 표기** — 로그 JSONL 의 `timestamp` 는 UTC(`Z`)이고 회고 파일명은 KST 입니다.
아래 시각은 전부 +9 변환한 KST 입니다.

## 세션별 활동

### 세션 `81ff2e29` (09:33 ~ 10:01) — 이번 증분의 전부

Read 22 · Edit 20 · PowerShell 13 · Grep 7 · Bash 3 · Write 1 = 도구 66회.

**프롬프트:**
1. (09:33) `/opsx:apply add-cross-encoder` — `5장 작업해서 /qa API 연결 진행해주세요.`
2. (10:01) `커밋은 제가 직접 하겠습니다.`

**증분 타임라인:**

| 시각 | 활동 |
|---|---|
| 09:33 | `openspec status`·`instructions apply` — 진행 **31/65**, 5장(5.1~5.5) 착수 |
| 09:34~09:42 | 컨텍스트 Read 후 Edit 11연속 — `api/queries.py`·`api/routes/search.py`·`api/sse.py`·`services/qa.py` |
| 09:43 | `docker version` → **데몬 다운**(`dockerDesktopLinuxEngine` 파이프 없음) |
| 09:43~09:44 | `Start-Process` 로 Docker Desktop 기동 + `until docker version ...; do sleep 3; done` 로 대기 — 사람 개입 0 |
| 09:44 | `docker compose run --build --rm test pytest tests/test_search_api.py tests/test_qa_api.py -q` |
| 09:47~09:54 | 전체 스위트 → `test_concurrency_api.py::test_different_documents_are_not_serialized` **1건 실패** |
| 09:54~09:58 | 같은 테스트만 재실행 → 동일 실패 |
| 09:58~09:59 | **`git stash push` 로 변경을 걷어내고 재실행 → 여전히 실패** = 이번 변경의 회귀가 아님을 확정, `git stash pop` |
| 09:59 | `docker compose run --build --rm test ruff check .` |
| 10:00 | `git add -A; git commit` 시도 → **사용자 거부** |
| 10:01 | `memory/user-commits-themselves.md` 신설 + `MEMORY.md` 색인, `tasks.md` **36/65** 갱신 후 종료 |

커밋 `173779b`(`feat: 리랭킹 순위 신호를 API 표면에 노출`)는 사용자가 직접 만들었습니다.

**5장에서 붙인 것:**

- **5.1** `SearchResultView.rerank_score: float | None` — `score` 의 `gt=0, le=1` 은 그대로 두고,
  리랭커 점수에는 범위를 걸지 않았습니다(척도가 질의마다 다름). 그 이유를 필드 설명에 적었습니다.
- **5.2** `SearchResponse` 에 `ordered_by`·`reranker` — 둘 다 `RetrievalResult` 에서 그대로 옵니다.
- **5.3** `SourcesPayload`(`api/sse.py`)·`SourcesEvent`(`services/qa.py`)에 같은 두 필드 —
  `/qa` 의 `sources` 이벤트가 `/search` 응답과 **같은 봉투**를 냅니다.
- **5.4** 켠 구성 / 끈 구성(`fusion`·`null`·`null`) / 리랭커가 실패한 구성(이름을 남기지 않음) 세 테스트.
- **5.5** 기존 모양 테스트에 두 필드 비교를 더하고, **리랭킹이 실제로 도는** 구성에서
  `/qa` 와 `/search` 가 같은지 재는 테스트를 새로 추가.

**발생한 에러:** 5건 — Docker 데몬 다운 1 · 동일 테스트 실패 3 · 커밋 거부 1.

### 나머지 12세션 (08-04 구간, 집계만)

| 세션 | 종류 | 프롬프트 | 에러 |
|---|---|---:|---:|
| `2b0b7d11` | 작업 (캐시 키 유도·`adapters/cache/` 배치) | 7 | 2 |
| `976130a0` | 작업 (`/opsx:apply` 재호출, lexical-index 점검) | 6 | 8 |
| `b2ecdd45` | 작업 | 5 | 4 |
| `ce0bf3da` | 작업 | 4 | 5 |
| `c4df5f7c` | 작업 | 2 | 3 |
| `dd40c3ba` | 작업 (프롬프트 1건이 596엔트리를 끌고 감) | 1 | 1 |
| `01643357`·`269378f4`·`4fcddd90`·`a449f215`·`c26e011b`·`d9537656` | Retrobot 훅 | 각 1 | 8·3·5·7·2·3 |

훅 6세션의 에러 28건은 **전부 `requires approval` 거부**입니다(같은 파일들에서
승인 거부 문자열 33건 관측). 작업 내용의 상세는 08-04 회고들에 있습니다.

## KPT 회고

### Keep (좋았던 것)

- **실패한 테스트를 세 번 다시 돌리는 대신 `git stash` 로 갈랐습니다.** 09:54·09:58 에 같은
  실패를 두 번 본 뒤, 세 번째는 재시도가 아니라 **변경을 걷어낸 트리에서의 실행**이었습니다.
  거기서도 실패했으므로 이번 5장 변경의 회귀가 아니라는 것이 30초에 확정됐습니다.
  실패를 "플래키 같다"로 넘기지 않고 **선재라는 사실을 실측으로 만든** 것이 차이입니다.

- **데몬이 죽은 것을 알아보고 자동으로 되살렸습니다.** `CLAUDE.md` 가 예고한 그 함정
  (`dockerDesktopLinuxEngine` 파이프 없음)을 09:43 에 그대로 만났는데, 사용자에게 묻지 않고
  `Start-Process` → `until docker version` 대기 루프로 통과했습니다. 문서에 적힌 환경 사실이
  실제로 시간을 아낀 회차입니다.

- **필드를 추가하고 끝내지 않고 두 표면의 일치를 불변식으로 고정했습니다.** 5.5 의
  "리랭킹이 **실제로 도는** 구성에서 `/qa` 와 `/search` 가 같은지" 테스트가 그것입니다.
  `sources` 이벤트와 `/search` 응답이 각자 필드를 싣는 구조라, 한쪽만 고치는 다음 변경은
  이 테스트가 없으면 조용히 갈라집니다.

- **범위를 안 건 이유를 필드에 적었습니다.** `rerank_score` 에 `score` 와 같은 `gt=0, le=1` 을
  걸지 않은 것은 누락이 아니라 판단인데, 그 판단(척도가 질의마다 다름)이 코드에 남아 있어
  다음 사람이 "제약이 빠졌다"고 읽고 채워 넣는 일이 없습니다.

- **거부당한 커밋을 다시 시도하지 않고 규칙으로 승격했습니다.** 10:00 커밋 거부 →
  10:01 `memory/user-commits-themselves.md` + `MEMORY.md` 색인. 같은 거부를 다음 세션이
  반복하지 않게 하는 유일한 방법이 저장이고, 실제로 저장까지 갔습니다.

- **마지막 검증 이후의 편집이 `tasks.md` 체크뿐입니다.** 09:59 `ruff check --build` 뒤에 남은
  Edit 하나는 진행률 갱신이라, 커밋 시점의 코드는 검증된 그대로입니다.

### Problem (나빴던 것)

- **`test_different_documents_are_not_serialized` 를 선재로 판정하고 그대로 두었습니다.**
  벽시계 기반 동시성 테스트라 재발이 예정돼 있는데, 이슈도 마커도 남지 않았습니다.
  다음 회차가 같은 실패를 만나면 **또 stash 로 가르는 30초**를 씁니다. 판정은 잘했지만
  판정 결과가 코드에도 문서에도 없습니다.

- **전체 스위트 두 번(09:47·09:54)이 `--build` 없이 돌았습니다.** 09:44 빌드 이후 편집이
  없었으므로 이번 결과는 유효했지만, `CLAUDE.md` 가 "수정 직후 재검증이 같은 결과로
  돌아왔다면 `--build` 를 빠뜨린 것"이라고 못 박은 바로 그 상황(같은 실패 두 번)에서
  플래그가 빠져 있었습니다. **결과가 우연히 맞았다고 절차가 맞은 것은 아닙니다.**

- **직전 회고 Try 6건의 이행 여부를 이 창에서 확인하지 못했습니다.** `git push` ·
  `ruff format src tests` · 이미지 증가분 실측 · `add-multiturn-qa` 처분 — 전부 셸이
  필요한데, 이 훅 실행은 `Bash`·`PowerShell` 이 모두 승인 게이트에 막혀 있습니다.
  회고가 스스로 낸 처방의 이행을 **회고가 검증할 수 없는 구조**입니다.

- **Retrobot 이 창 에러의 50%(28/56) — 18회차.** 직전 79% 보다는 낮지만 절반입니다.
  훅 6세션이 **6/6** 승인 거부로 시작했고, `retrobot/SKILL.md` 는 `afa9794`(08-03) 이후
  무수정입니다. 회고 열두 편이 같은 진단을 반복했습니다.

- **이번 실행에서 셸이 통째로 막혔습니다 — `SKILL.md` 의 1순위 절차가 또 첫 줄에서 죽었습니다.**
  `command -v python3 jq` 거부, `py.exe -3 -c` 거부. 직전 회고가 "`python3 -c` 단독은 통과"를
  확정 사실로 적었는데 이 실행에서는 그 경로도 없습니다. 결국 `Glob`·`Grep`·`Read` 만으로
  집계했고 그것으로 충분했는데, `SKILL.md` 는 그 경로를 **차선책**으로만 적어 둡니다.

- **`81ff2e29` 의 커밋 시도가 거부된 것은 규칙 부재였습니다.** 10:01 에 메모리로 남겼지만,
  그 규칙은 리포가 아니라 개인 메모리에 있습니다. 다른 호스트·다른 에이전트(Codex)는
  `AGENTS.md`/`CLAUDE.md` 만 읽으므로 같은 거부를 다시 받습니다.

### Try (개선해야 할 것)

1. **플래키 테스트를 명시적으로 처분.** `test_different_documents_are_not_serialized` 에
   마커를 달거나 벽시계 단언을 이벤트 기반으로 바꾸거나, 최소한 `tests/README.md` 에
   "선재 실패, 원인 미규명" 한 줄. 판정을 두 번 하지 않게 하는 것이 목적입니다.
2. **`docker compose run` 에는 예외 없이 `--build`.** 이번엔 우연히 맞았습니다.
3. **`retrobot/SKILL.md` 2단계를 뒤집기 — 열여덟 번째 진단 대신.** `Glob`→`Grep(count)`→
   `Grep(-o + head_limit)`→`Read(offset/limit)` 를 **1순위**로 올리고, 셸 집계를
   "가능하면" 으로 내리세요. 이 회고 전체가 그 넷만으로 집계됐고 셸은 한 번도 통하지
   않았습니다. `command -v python3 jq` 줄은 지우는 것이 맞습니다 — 이 호스트에서
   **성공한 적이 한 번도 없습니다.**
4. **"커밋은 사용자가 직접 한다"를 `CLAUDE.md` 에 한 줄로 옮기기.** 개인 메모리에만 있으면
   `AGENTS.md` 를 읽는 다른 에이전트에는 전달되지 않습니다.
5. **직전 회고 Try 6건을 사람이 한 번 확인.** push · `ruff format src tests` ·
   `결정 11` 이미지 증가분 · `add-multiturn-qa` — 회고가 확인할 수 없는 항목들입니다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 08-04 저녁~심야 | 작업 6세션 | 캐시 키 유도·`adapters/cache/` 배치 등 (에러 23) — 상세는 08-04 회고 |
| 08-04 저녁~심야 | 훅 6세션 | Retrobot 회고 6편 (에러 28, 전부 승인 거부) |
| 08-05 09:33 | `81ff2e29` | `/opsx:apply add-cross-encoder` 5장 착수 — 31/65 |
| 08-05 09:34~09:42 | `81ff2e29` | `queries.py`·`routes/search.py`·`sse.py`·`services/qa.py` 에 세 필드 배선 |
| 08-05 09:43 | `81ff2e29` | Docker 데몬 다운 → `Start-Process` + `until` 대기로 자동 복구 |
| 08-05 09:44~09:47 | `81ff2e29` | `--build` 부분 스위트 통과 |
| 08-05 09:47~09:58 | `81ff2e29` | 전체 스위트 1건 실패 ×2 (`test_concurrency_api`) |
| 08-05 09:58~09:59 | `81ff2e29` | `git stash` 로 **선재 실패 확정**, `stash pop`, `ruff check --build` |
| 08-05 10:00 | `81ff2e29` | 커밋 시도 → 사용자 거부 |
| 08-05 10:01 | `81ff2e29` | `memory/user-commits-themselves.md` 신설, `tasks.md` **36/65** |
| 08-05 10:0x | — | 커밋 `173779b` (사용자 직접) · 이 회고 |

## 지표

| 지표 | 직전 (01:22) | 현재 | 추이 |
|---|---|---|---|
| `add-cross-encoder` tasks | 16/65 | **36/65** | +20 |
| 훅 에러 비중 | 49/62 (79%) | **28/56 (50%)** | 하락 (훅 세션 수 13 → 6) |
| 증분 세션 도구 호출 | — | 66 (Read 22·Edit 20·PowerShell 13) | — |
| 증분 세션 에러 | — | 5 | — |

## 이 세션에서 확정된 환경 사실

- **이번 훅 실행에서는 `Bash`·`PowerShell` 호출이 전부 `requires approval` 로 거부됐습니다.**
  `command -v python3 jq` 도, `py.exe -3 -c "..."` 단독도 막혔습니다. 승인 게이트는
  실행마다 다르므로 **셸이 있다고 가정하는 절차는 매번 첫 줄에서 죽습니다.**
- `Glob` → `Grep(count)` → `Grep(-o + head_limit)` → `Read(offset/limit)` 넷만으로
  32개 파일의 프롬프트·에러·도구 분포·타임스탬프를 전부 집계했습니다. 이 회고가 그 산물입니다.
- **로그 JSONL 의 `timestamp` 는 UTC(`Z`)이고 회고 파일명·본문은 KST 입니다** — +9 변환이
  필요합니다. 변환을 빼면 세션 순서가 커밋 순서와 어긋나 보입니다.
- 이 호스트의 로그 디렉터리에는 **다른 호스트(macOS)에서 돈 세션이 없습니다.** 08-04
  회고들이 인용한 `34e9f10c`·`75cd4368` 같은 세션 ID 는 여기 없으니, 회차 간 비교를
  세션 ID 로 하면 어긋납니다.
- Docker Desktop 데몬이 꺼져 있어도 `Start-Process` 로 띄우고 `until docker version` 으로
  기다리면 사람 개입 없이 복구됩니다.
