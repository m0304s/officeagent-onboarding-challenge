# Retrobot 회고 — 2026-08-06 21:42 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 9 (실작업 5 · Retrobot 훅 4) |
| 총 프롬프트 | 46 |
| 도구 호출 | 714 (Bash 406 · Edit 127 · Read 58 · Write 51 · Grep 31) |
| 에러 발생 | 30 (이 중 18건 = 60% 가 Retrobot 훅 자신) |
| 창 안 커밋 | 8 (기능 4 · 회고 3 · 스펙 1) |

이 창(08-05 21:42 ~ 08-06 21:42)의 앞 세 덩어리는 이미 회고가 있습니다 —
`add-pymupdf4llm-parser` 스펙(00:12·00:20), OCR 왕복과 파서 마감(13:10), 근거 펼치기 UI(13:19).
**새 증분은 21:05~21:42 한 덩어리**이므로 아래는 거기에 무게를 둡니다.

## 세션별 활동

### 세션 `f4deea06` (08-06 21:05 ~ 21:42) — 새 증분

**프롬프트:**
1. `/opsx:propose bug-fix` — BM25가 희소 토큰 하나로 우연히 건진 청크가 코사인 0.85로 통과한
   청크와 융합 점수가 같다 / 시맨틱 캐시가 부정문을 통과시킨다("연차를 이월할 수 있나요?" 와
   "연차를 이월할 수 없나요?")
2. `/opsx:apply fix-retrieval-cache-defects 1장 및 2장 작업 진행해주세요.`

**산출:** `170b056` 8파일 `+574` — proposal · design · tasks(8장 47태스크) · 델타 스펙 4건
(`retrieval` 107 · `response-cache` 108 · `document-ingestion` 52 · `lexical-index` 33).
1장 적용분(`docker-compose.yml` redis:7→8, `pyproject.toml` redis>=5.2→>=6.0, `README.md`)은
**워킹트리에만** 있고, 1.3(redis-py 헬퍼 확인) 진행 중에 커밋 훅이 떴습니다.

**발생한 에러:** 1건 — `cd openspec/specs` 실패(디렉터리 없음, 즉시 복구)

### 세션 `8f673272` (10:43 ~ 13:07) · `bc5f4779` (13:07 ~ 13:18) · `c69d384c`·`f13eeb41` (23:09 ~ 00:49)

각각 13:10 · 13:19 · 00:12/00:20 회고가 다룹니다. 요약만 이월 —
OCR 왕복으로 86분이 코드 0줄을 남기고 되돌아갔고, 파서 교체는 아카이브까지 닫혔으며,
근거 펼치기 UI는 실물 `/qa` 검증까지 밟았으나 유일한 로직(`joinCitations`)에 테스트가 없습니다.

### 훅 세션 `e1686297` · `e76cada6` · `0e8a88f5` · `7e429c14`

Retrobot 실행 4회. 각 실행이 같은 순서로 막혔습니다 — `command -v python3 jq` 거부 →
`/tmp` Write 거부 → `>` 리다이렉트 거부. 창 에러 30건 중 **18건(60%)** 이 여기서 나왔습니다.

## KPT 회고

### Keep (좋았던 것)

- **두 결함이 코드가 아니라 사람의 독해에서 나왔고, 코드 0줄 시점에 스펙으로 굳었습니다.**
  RRF 대칭성 결함은 "게이트가 목록을 자르니 교집합이 안 생긴다"는 인과가 핵심인데,
  이건 테스트가 초록불인 채로도 참입니다 — 융합 점수는 계산되고 결과도 K개 나오니까요.
  2장이 그 인과를 그대로 뒤집어 **"자르지 말고 `gate_passed`로 표시하고, 융합 뒤 채택 규칙을
  적용한다"** 로 설계했습니다. 지금 고치면 어댑터 두 곳 + 서비스 한 곳이고, 코드가 붙은 뒤였다면
  회귀 테스트까지 딸려 옵니다. 08-04 `3eda547`(scope 지적이 코드 6분 전에 통과)와 같은 모양.

- **1.2를 문서 주장이 아니라 컨테이너 실측으로 닫았습니다.** Redis 8 벡터셋은 이 change 전체가
  올라탄 전제인데, `docker compose up -d --wait cache` 뒤 `COMMAND INFO VADD`가 비지 않음을 보고
  `VADD`/`VSIM`/`VCARD`/`VDIM`/`VREM`을 직접 쳤습니다 — `VSIM ... WITHSCORES`가
  `a 0.99999 / b 0.99694`, 없는 키의 `VSIM`은 빈 결과·`VCARD`는 0, 마지막 원소를 `VREM`하면
  키가 사라짐까지 확인. **후보 선택을 근접 이웃으로 바꾸는 7장이 이 성질들 위에 서 있으므로,
  틀렸다면 7장을 다 쓴 뒤에 알게 됐을 겁니다.**

- **대비책을 태스크에 미리 적었습니다.** 1.3은 "`redis-py`에 `vadd` 헬퍼가 있는지 확인하고,
  없으면 `execute_command`로 보내기로 확정"이라 **확인 결과와 무관하게 진행 경로가 있습니다.**
  라이브러리 지원 여부에 설계가 인질로 잡히지 않습니다.

- **부정 극성을 판정 위치가 아니라 후보 구성 단계에 넣었습니다.** 6.5가 "극성이 다른 항목을
  **후보에서 제외**"인 것은 임계값 조정으로는 부정문 쌍을 못 가른다는 08-04 7장 계측
  (`add-response-cache`, "부정문 쌍이 임계값 위에 남는다")의 결론을 반영한 것입니다 —
  같은 벽에 두 번 부딪히지 않았습니다.

### Problem (나빴던 것)

- **`tasks.md`가 0/47인데 1.1은 워킹트리에서 이미 끝났습니다.** `docker-compose.yml`·
  `pyproject.toml`·`README.md`가 고쳐져 있는데 `1.1`은 `[ ]`이고, 1.2 실측 결과를
  design.md 「위험」에 반영하라는 지시(1.2 후단)도 `design.md +2`가 그것인지 커밋으로는
  구분되지 않습니다. 08-04 `b07c66d`가 3장 체크 10개를 빠뜨린 것과 같은 모양의 재발입니다.

- **1장 적용분이 커밋 밖이라, 지금 체크아웃하면 스펙은 Redis 8을 요구하는데 컴포즈는 7입니다.**
  `170b056`은 순수 문서 커밋이고 인프라 상향은 워킹트리에만 있습니다.
  08-06 00:20 회고가 `da889dc`("pymupdf4llm 동작 확인"인데 버전 핀이 미커밋이라 재현 불가)에
  대해 적은 지적과 **같은 문장이 그대로 성립합니다.**

- **브랜치 이름이 `debugging`입니다.** 다른 브랜치는 전부 change 이름(`add-*`·`fix-*`)을 따르는데
  이것만 작업 성격을 이름으로 씁니다. change는 `fix-retrieval-cache-defects` 하나로 명확한데
  `git log --all`에서 어느 브랜치가 무슨 change인지 이 하나만 안 읽힙니다.

- **미푸시 2건, 그리고 `add-cross-encoder` 15커밋이 아직 `main`에 없습니다.**
  `main`은 이번 창에서 움직였지만(`fix-pdf-parser` 병합) 리랭킹 브랜치는 `main` 기준 15커밋
  미병합이고 `main`에만 있는 커밋도 10건이라, **격차가 양쪽으로 벌어졌습니다.**
  지금 이 change가 손대는 `services/retrieval.py`는 리랭킹이 만지는 바로 그 파일입니다.

- **`add-multiturn-qa` 4아티팩트가 69시간째 untracked — 열두 번째 계수.**
  08-04 00:48 이후 change 넷이 그 옆을 지나갔습니다.

- **`ruff format --check` 13건 — 열두 번째 재현, 한 건도 안 줄었습니다.**
  같은 순간 `check_comments.py`는 114파일 위반 0건이라 다른 게이트는 초록입니다.
  닫는 비용은 `.venv/bin/ruff format src tests` 한 줄 10초.

- **외부 적대적 검토 0회 — 열일곱 세션 연속.** 이번 change는 `openspec validate --strict`를
  통과했지만, 창 안에서 설계를 실제로 뒤집은 것은 전부 사람의 지적이었습니다
  (이번 회차의 두 결함도 `validate`가 아니라 사용자가 찾았습니다).

- **Retrobot 25회차, 창 에러의 60%(18/30).** 훅 4세션이 전부 같은 세 지점에서 죽었고,
  이번 실행도 **네 번 막힌 뒤**에야 통했습니다 — `command -v python3 jq`(단독도 거부) →
  `/tmp` Write 거부 → 중괄호+따옴표 딕셔너리 리터럴 거부 → `>` 리다이렉트 거부 →
  `python3 - <<'PY'` 성공. `SKILL.md`는 `afa9794`(08-03 22:14) 이후 **71시간 무수정**이라
  회고 열두 편이 같은 진단을 반복했습니다.

### Try (개선해야 할 것)

1. **1장 적용분과 `tasks.md` 체크를 같은 커밋에 올린다.** `docker-compose.yml`·`pyproject.toml`·
   `README.md` + `1.1`~`1.3` 체크 + 1.2 실측 한 줄이 한 커밋이면, 체크아웃한 사람이
   "스펙은 Redis 8을 요구하는데 컴포즈는 7"인 상태를 만나지 않습니다.
2. **브랜치를 `fix-retrieval-cache-defects`로 옮긴다** (`git branch -m`). 커밋 2건뿐이라
   지금이 가장 쌉니다.
3. **2장에 들어가기 전에 `add-cross-encoder`와의 관계를 정한다.** 병합하든 리베이스하든,
   `services/retrieval.py::search`를 두 브랜치가 각자 고치면 나중에 손으로 합쳐야 합니다.
4. **`.venv/bin/ruff format src tests` 를 지금 한 번 돌린다** (10초, 열두 번째 이월).
5. **7장 착수 전에 Codex 검토를 한 번 건다.** 7장은 캐시 키를 `qa:v1`→`qa:v2`로 올리고
   후보 선택 방식을 통째로 바꾸는데(비중 18%·불합격 기준 영역), 이 창에서 `validate --strict`가
   잡아낸 설계 결함은 0건입니다.
6. **`retrobot/SKILL.md`의 "하지 말 것" 목록에서 heredoc 금지에 호스트 조건을 붙인다.**
   macOS에서는 `python3 - <<'PY'`가 유일하게 통하는 경로이고 그것을 금지가 막습니다.
   확정 사실 — 이 호스트에서 막히는 것은 **허용 디렉터리 밖 쓰기 · `>` 리다이렉트 ·
   `;`/`&&` 다중 연산 · 따옴표를 품은 중괄호 리터럴**이며, `python3` 자체는 3.9.6으로 멀쩡합니다.
7. **`add-multiturn-qa`를 커밋하거나 지운다** (열두 번째 계수, 69시간).

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 08-05 23:09 ~ 08-06 00:07 | `c69d384c` | `add-pymupdf4llm-parser` 스펙 확정 (`88dc607`) |
| 08-06 00:07 ~ 00:49 | `f13eeb41` | 1·2장 적용, OCR 처리 방식 검토 (`da889dc`) |
| 08-06 10:43 ~ 13:07 | `8f673272` | OCR 도입 → 전면 철회, 파서 3~8장 마감·아카이브 (`9eb2976`) |
| 08-06 13:07 ~ 13:18 | `bc5f4779` | 데모 UI 근거 펼치기 (`56341eb`·`5c49e34`) |
| 08-06 21:05 ~ 21:38 | `f4deea06` | `fix-retrieval-cache-defects` 제안·설계·태스크 (`170b056`, `+574`) |
| 08-06 21:38 ~ 21:42 | `f4deea06` | 1장 적용 — Redis 8 상향, 컨테이너에서 벡터셋 실측, 1.3 진행 중 |
