# Retrobot 회고 — 2026-08-04 22:23 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 21 (실작업 4 · 서브에이전트 3 · Retrobot 훅 12 · 기타 2) |
| 총 프롬프트 | 44 |
| 에러 발생 | 67 (이 중 **53건(79%)이 Retrobot 훅 자신**) |
| 창 | 최근 24시간 (08-03 22:23 ~ 08-04 22:23 KST) |

## 세션별 활동

### 세션 `75cd4368` (08-04 20:49 ~ 22:23) — 이번 커밋의 주역

**프롬프트:**
1. 20:50 `/opsx:apply add-response-cache` — 캐시 도메인을 구현한다
2. 21:00 `/opsx:apply` — 2장 캐시 저장소
3. 21:16 `/opsx:update` — scope를 반영해주세요
4. 21:22 `/opsx:apply` — Redis 어댑터
5. 21:45 23건 테스트 왜 생략된거지?
6. 21:47 `/opsx:apply` — 4장 캐시 오케스트레이션
7. 21:59 `/opsx:apply` — 섹션 5, 그리고 2~4장 미완료 태스크도 함께
8. 22:18 CLI 실행해서 기본 모델 측정할 수 있나요? 측정 가능하다면 그 값을 세팅에 넣으면 안 되나요
9. 22:23 `/opsx:apply` — 6장 무효화

**도구:** Bash 94 · Edit 84 · Read 28 · Write 14 · AskUserQuestion 2 · Skill 1

**발생한 에러:** 6건 — 전부 게이트가 먼저 잡은 것(`E501` 118자, `check_comments` `config.py:91`, `ruff format` 미포맷, `Edit` 중복 매치 2건)이고 런타임 실패는 0건

### 세션 `45f0bdd9` (20:13 ~ 20:48) — 설계와 사용자 검토

`/opsx:propose`로 L1/L2 캐시 4아티팩트를 세우고, 사용자가 `cache_enabled` 배선·`REPLACED` 무효화·`insufficient_evidence` 부정집합 3건을 뒤집음. 도구 에러 0건.

### 세션 `724d2218` (08-03 22:47 ~ 08-04 00:31) — 데모 UI

서브에이전트 3개(`agent-a6`·`agent-ad`·`agent-a7`)로 `ErrorCode` 계약을 선확정한 뒤 브라우저 MCP로 실화면 검증.

### Retrobot 훅 12세션 (`9d06bafd`·`d0c01226`·`cc79ede2`·`e4260f90`·`a2542476`·`115a7e25`·`e27b60d4`·`7a93d660`·`4e480d75`·`8486165b`·`25644f92`·`12eec827`)

**발생한 에러:** 53건. 12세션 중 11세션이 **똑같은 두 줄**에서 죽음.

- `command -v python3 jq` → `This Bash command contains multiple operations` (11/12)
- 분리한 `command -v python3` 단독 → `This command requires approval` (11/12)
- `/tmp/retro_agg.py` Write → `you haven't granted it yet` (8/12)
- `python3 -c '...' > file` / `; wc -c` → multi-operation 거부 (3/12)

## KPT 회고

### Keep (좋았던 것)

- **5장이 K와 색인 서명을 "다시 유도하지 않고 읽는다"로 닫혔습니다.** `RetrievalService.index_signature` 프로퍼티와 `resolve_top_k()`를 만들고 `search` 자신이 그 메서드를 쓰게 했습니다. 캐시 키의 재료를 두 곳에서 각자 계산하면 언젠가 갈라지고, 갈라지는 순간 히트가 틀린 답을 냅니다 — 계산 지점을 하나로 만들어 그 가능성을 구조에서 없앴습니다.
- **사용자 제안을 실측으로 검증한 뒤 거절했고, 거절한 자리에 한계를 남겼습니다.** 22:18 "CLI로 기본 모델을 재서 설정에 넣으면 안 되나"에 대해 컨테이너에서 `model/list`·`thread/start`를 직접 쳐서 `gpt-5.6-sol`이라는 실물 이름을 확인했습니다. 그러고도 박지 않은 이유가 정확합니다 — `qa_llm_model`이 비어 있지 않으면 어댑터가 그 값을 `thread/start`에 **넘기므로**(`codex.py:178`), 설정에 적는 순간 관측이 아니라 지시가 됩니다. 대신 남는 구멍(CLI 업그레이드로 기본 모델이 바뀌어도 키는 `""` 그대로 → 옛 모델의 답이 새 모델의 답인 척 남음)을 범위·수명(TTL 24시간)·닫는 법까지 `ARCHITECTURE.md:1429`와 `README.md:595` 양쪽에 적었습니다. **모르는 것을 아는 척하지 않고, 아는 것을 문서에 두는 것**이 이 프로젝트에서 반복적으로 통한 패턴입니다.
- **직전 회고가 지적한 미체크 태스크를 회수했습니다.** 2.7·4.8·4.10이 5장 작업과 함께 닫혀 49/71이 됐습니다. 21:18 회고가 "이유가 `tasks.md`에 없으면 다음 세션이 빠뜨린 것으로 읽는다"고 적은 자리가 실제로 회수됐습니다.
- **`ARCHITECTURE.md` 9~11줄의 거짓이 정정됐습니다.** "캐싱은 아직 없습니다"가 배선된 실물(도메인·구현 셋·Redis 키 구조·`CacheService`) 설명으로 바뀌었고, **아직 없는 것(문서 변경 시 무효화)은 없다고 명시**했습니다. 있는 것을 있다고, 없는 것을 없다고 적는 것이 이 과제에서 불합격을 가르는 축입니다.
- **탐침 스크립트가 리포에 남지 않았습니다.** 22:19에 `./probe_model.py`를 만들어 컨테이너에서 돌린 뒤 지웠습니다 — `git status`에 흔적이 없습니다.
- **`--build` 준수 일곱 세션 연속.** 22:15 최종 검증도 `docker compose run --build --rm test`였고 결과는 **919 passed, 23 deselected**, `pytest -m redis` **21 passed**.

### Problem (나빴던 것)

- **`README.md:532`가 다시 낡았습니다 — 21:49에 한 번 고친 그 자리입니다.** 지금 `861 passed, 23 deselected`라고 적혀 있고 실제는 **919 passed**입니다. 21:45의 "23건 테스트 왜 생략된거지?"가 드러낸 것이 정확히 이 줄의 `660 passed`였고, `b07c66d`가 861로 고쳤습니다. 한 시간 만에 같은 방식으로 다시 어긋났습니다 — **테스트를 늘리는 커밋마다 이 줄이 낡습니다.** 손으로 적는 한 세 번째, 네 번째가 옵니다.
- **`src/app/config.py:91` 인라인 주석 위반이 여섯 회차째 살아 있습니다.** 21:11에 검출된 뒤 21:18·21:25·21:49·21:59 회고가 모두 지적했고, 지금도 `62개 파일, 위반 1건`입니다. `src` 한정 **딱 1건**이라 닫는 비용이 사실상 0인데, 게이트가 빨간 채로 있으니 **새 위반이 섞여 들어와도 구분이 안 됩니다**(21:59 회고에서 실제로 2건까지 올랐다가 되돌아왔습니다).
- **`add-multiturn-qa` 4아티팩트가 22시간째 untracked입니다 — 네 번째 계수입니다.** `58b22a1f`(08-04 00:31)가 `proposal`·`design`·`tasks`·`specs/`를 다 만들었고, 그 사이 change 둘이 커밋됐습니다. **168시간 창은 커밋만 세므로 트리에만 있는 산출물은 존재하지 않습니다.**
- **원격에 없는 커밋이 7건 → 13건으로 늘었습니다.** `f07ed41`부터 `fe4bcfa`까지 캐싱 작업 전부가 로컬에만 있습니다. **평가자가 지금 리포를 보면 캐싱(비중 18%, 동시에 불합격 기준)은 착수조차 안 한 것으로 보입니다.** 21:25 회고가 이 상태를 지적한 뒤 여섯 커밋이 더 쌓였습니다.
- **외부 적대적 검토 0회, 열한 세션 연속.** `add-response-cache`는 설계 단계에서 사용자 지적으로 네 번 뒤집혔습니다(`cache_enabled` 배선 / `REPLACED` 무효화 / `insufficient_evidence` 부정집합 / `scope`). 네 번 뒤집힌 설계가 지금 `/qa`에 직접 붙는 코드로 1,700줄 이상 쌓였고, 다섯 번째가 없다는 보장은 없습니다. 남은 6·7장이 **핵심 3경로 중 하나(캐시 무효화)** 라 지금이 반영 비용이 가장 싼 마지막 구간입니다.
- **이 회고 도구가 24시간 에러의 79%를 만듭니다 — 그리고 원인은 `retrobot/SKILL.md` 자신입니다.** `afa9794`(08-03 22:23) 이후 **정확히 24시간 무수정**이고, 그 사이 여섯 회고가 같은 진단을 반복했습니다. 세 줄이 매 회차 에러를 재생산합니다:
  - `SKILL.md`가 "**먼저 확인할 것**"이라며 시킨 `command -v python3 jq`가 **12세션 중 11번 거부**됐습니다. 진단하라는 줄이 진단을 막습니다.
  - "파싱 스크립트가 필요하면 `/tmp` 아래에 쓴다"가 8회 전부 쓰기 거부를 냈습니다.
  - "**하지 말 것**"이 heredoc을 금지하는데, `75cd4368`은 같은 시간대에 `python3 - <<EOF`를 **네 번 성공**시켰습니다(21:26·21:34·21:43·22:16). Windows 사실이 호스트 구분 없이 적혀, macOS에서 되는 수단을 막고 있습니다.
  - 대가는 `115a7e25`가 치렀습니다 — 두 번 막힌 뒤 python3를 재시도하지 않고 **Grep 30회**로 우회했습니다. 그 호스트에서 `python3 -c`는 멀쩡히 돕니다(이 회고가 그것 6번으로 집계됐습니다).
- **6장이 커밋 없이 워킹트리에 있습니다.** `fe4bcfa` 직후 22:24부터 `src/app/services/ingestion.py`에 6.1~6.4 편집이 들어갔고 지금 ` M` 상태입니다. 커밋 경계가 장 단위로 살아 있던 흐름(2장 `a466d72` → 3장 `b07c66d` → 4장 `5ea81bc` → 5장 `fe4bcfa`)이 유지되는지는 다음 커밋에서 확인됩니다.

### Try (개선해야 할 것)

1. **`README.md:532`의 숫자를 지금 고치고, 손으로 적는 것을 그만둡니다.** 당장은 `919 passed, 23 deselected`로 정정. 재발을 막으려면 숫자 대신 "제외된 23건은 실물 CLI 2건 + 실물 Redis 21건"처럼 **변하지 않는 구조만 적고** 총계는 빼거나, 커밋 전 체크리스트에 이 줄을 넣습니다. 세 번 어긋난 줄은 네 번째도 어긋납니다.
2. **`src/app/config.py:91`을 이번 커밋에 같이 닫습니다.** 인라인 3줄을 2줄로 줄이거나 근거를 `ARCHITECTURE.md`로 옮기면 끝입니다. 게이트를 초록으로 되돌려야 다음 위반이 신호가 됩니다.
3. **`add-multiturn-qa`를 커밋합니다.** `git add openspec/changes/add-multiturn-qa && git commit -m "docs: 멀티턴 QA 설계"` 한 줄. 네 회고가 같은 말을 했습니다.
4. **6장을 닫는 커밋과 함께 push합니다.** 13커밋이 로컬에만 있는 상태에서 평가 리포는 "캐싱 미착수"로 읽힙니다. 무효화는 과제의 핵심 3경로 중 하나라, 이것이 원격에 없는 것과 있는 것의 차이가 가장 큽니다.
5. **6장 착수 전 `/codex:rescue`로 무효화 설계를 검토받습니다.** 뒤집기 네 번이 전부 사람의 지적에서 왔고 `openspec validate --strict`는 한 건도 못 잡았습니다. 검토 대상은 6.7·6.11 짝(같은 사건이 긍정은 살리고 부정은 지운다)과 6.12(서명이 같은 `REINDEXED`가 `no_evidence`를 지워야 하는가) — 둘 다 판정 규칙이라 코드보다 설계에서 갈립니다.
6. **`retrobot/SKILL.md` 세 줄을 고칩니다.** 여섯 회고가 진단만 반복했고 파일은 24시간 그대로입니다. 실측으로 확정된 사실을 그대로 적으면 됩니다:
   - `command -v` 확인 지시를 **삭제**하고 "`python3 -c '...'`를 바로 시도한다. 실패하면 내장 도구(Glob·Grep)로 간다"로 교체
   - `/tmp` 언급을 삭제하고 "**리다이렉트·파이프·`;` 없이** `python3 -c` 단독 호출로 stdout에 출력한다"로 교체 — `>`나 `;`를 붙이는 순간 multi-operation 판정으로 거부됩니다
   - heredoc 금지에 **호스트 조건을 붙입니다**(Windows 한정). macOS에서는 `python3 - <<'EOF'`가 통합니다
7. **회고 창을 `retros/` 최신 파일 시각으로 좁히는 안을 검토합니다.** 고정 24시간 창이 커밋 간격(10~30분)보다 훨씬 길어, 이 회고 내용의 상당 부분이 직전 여섯 회고의 재작성입니다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 08-03 22:24 | `12eec827` | 주석 정리(`afa9794`) 후속 검증 — 이후 `SKILL.md`는 24시간 무수정 |
| 08-03 22:45 | `8ccc760d` | 스트리밍 방식 확인 → 6~8장 SSE 작업 |
| 08-03 22:48 | `724d2218` | 데모 UI 제안 → 서브에이전트 3개로 `ErrorCode` 계약 선확정 |
| 08-04 00:13 | `724d2218` | `DESIGN.md` 기반 UI 수정, 브라우저 MCP 실화면 검증 |
| 08-04 00:37 | `58b22a1f` | `add-multiturn-qa` 4아티팩트 생성 — **이후 22시간 untracked** |
| 08-04 00:54 | `2d412d02` | `add-rrf-algorithm-spec` 제안 (하이브리드 검색·BM25) |
| 08-04 20:15 | `45f0bdd9` | L1/L2 캐시 설계 → 사용자 검토가 3건 뒤집음 (`f07ed41`) |
| 08-04 20:50 | `75cd4368` | 1장 캐시 도메인 (`23acd3a`) |
| 08-04 21:00 | `75cd4368` | 2장 저장소 계약·구현 셋 (`a466d72`) |
| 08-04 21:16 | `75cd4368` | `scope` 도입 — 네 번째 설계 뒤집기 (`3eda547`, 코드 0줄) |
| 08-04 21:22 | `75cd4368` | 3장 Redis 어댑터 (`b07c66d`) |
| 08-04 21:45 | `75cd4368` | "23건 왜 생략?" → `README` 숫자 정정 (660 → 861) |
| 08-04 21:47 | `75cd4368` | 4장 오케스트레이션 (`5ea81bc`) |
| 08-04 21:59 | `75cd4368` | 5장 `/qa` 배선 + 2·4장 미완료 회수 |
| 08-04 22:15 | `75cd4368` | 전체 검증 — **919 passed, 23 deselected** / redis 21 passed |
| 08-04 22:18 | `75cd4368` | 모델 식별자 실측(`gpt-5.6-sol`) → 설정에 박지 않고 한계를 문서화 |
| 08-04 22:23 | `75cd4368` | `fe4bcfa` 커밋(10파일 +756/-44, 49/71) → 6장 무효화 착수 |
