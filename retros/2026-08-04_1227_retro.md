# Retrobot 회고 — 2026-08-04 12:27 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 3 (구현 1 · Retrobot 훅 2) |
| 총 프롬프트 | 5 (구현 세션) |
| 에러 발생 | 23 (구현 4 · Retrobot 19) |
| 커밋 | `426917f` — 10파일 **+260/-55** |
| 테스트 | 772 → **776 passed, 2 deselected** (컨테이너 `--build` 3회) |
| 진행 | `add-rrf-algorithm-spec` 36 → **41/55** |

## 세션별 활동

### 세션 `b2ecdd45` (11:37 ~ 12:19 KST, 42분)

**프롬프트:**

1. `/opsx:apply add-rrf-algorithm` — "API 로직 구현작업 진행해주세요."
2. ".env를 사용하지 않고, config.py에서 바꿀 수 있도록 작업 진행해주세요."
3. "현재 작업에선.. BM25로 키워드 검색하는 것이랑, Dense로 의미가 유사한 질의 검색 한 뒤, 랭킹을 다시 매겨서 상위 N개만 맥락으로 같이 들어가는 로직이 되어있나요?"
4. "2번 문제를 해결하려면 어떤 방식이 있는지 확인해주세요"
5. "작업 진행해주세요"

**발생한 에러:** 4건

- 11:38:54 · 11:39:00 — `openspec status/instructions --change add-rrf-algorithm` → `Change 'add-rrf-algorithm' not found. Available changes: add-answer-generation / add-demo-ui / add-rrf-algorithm-spec`. **2회 연속**, 첫 실패가 후보 셋을 인쇄했는데 같은 인자로 재시도.
- 11:59:37 — 사용자 거부. `cp .env .env.bak …; echo 'APP_RETRIEVERS=…' > .env` (프롬프트 2가 이 거부에서 나옴).
- 12:19:37 — 사용자 거부. `src/app/core/retrieval.py` Edit. 38초 뒤 커밋.

### 세션 `c26e011b` · `01643357` (Retrobot 훅)

**발생한 에러:** 13건 — 전부 승인 거부/샌드박스 차단.

## KPT 회고

### Keep (좋았던 것)

- **의미가 바뀐 자리에서 README 예시를 다시 쟀다.** `score`가 코사인 유사도에서 RRF 융합 점수로 바뀌자 예시 JSON·로그 한 줄·필드 표를 **실측값**으로 교체했다(`score: 1.0`, `contributions` `0.86091968`/`0.6117184199523331`, `revision 5baf958963e1…`, `request_id 212efc995dd4…`). 이 리포가 열 회고에 걸쳐 못 닫았던 실패(손으로 쓴 `0.8734`)와 정확히 반대 방향이다. 나아가 `1.0`이 버그로 읽힐 것을 예상해 "합의의 정도라서 활성 retriever 둘이 모두 1위로 꼽으면 정의상 최댓값"이라는 문단을 값 옆에 붙였다 — 값을 고치는 것과 값이 오해받을 자리를 막는 것은 다른 일이다.
- **사용자 거부를 설정 이동으로 바꾸고 그 판단을 세션 밖으로 내보냈다.** `.env` 쓰기가 거부되자 구성을 `config.py`의 `_default_retrievers()` 한 곳으로 옮겼고, 같은 결정을 메모리 파일 하나로 기록했다. 다음 세션이 같은 제안을 다시 하지 않게 하는 자리는 대화가 아니라 파일이라는 인식이 행동으로 나왔다.
- **사용자 질문 하나가 죽은 기본값 조합을 드러냈고, 고치면서 근거까지 남겼다.** `candidate_depth`(50)와 `retrieval_max_top_k`(50)가 같으면 기동 검증(`깊이 >= 상한`)은 통과하지만 `top_k=50` 요청에서 한 retriever가 K칸을 다 채워 **융합할 재료가 남지 않는다.** 100/20(다섯 배)으로 벌리고, 배수를 기동 규칙으로 못 박지 **않은** 이유(표본이 문서 2건·청크 3개뿐이라 깊이의 비용도 효과도 관측되지 않는다)까지 README에 적었다. 관측하지 않은 것을 제약으로 만들지 않는 절제다.
- **하이브리드가 실제로 돌았는지를 응답과 로그 양쪽에서 볼 수 있게 만들었다.** 응답에 `retrievers`·`contributions`를, 로그에 `top_score` → `top_fusion_score` + `contributing_retrievers`를 넣었다. 근거는 코드 주석에 그대로 있다 — "설정 오타 하나로 어휘 색인이 빠진 배포와 정상 배포는 **둘 다 `200`을 내고 둘 다 그럴듯한 근거를 돌려준다**". 테스트도 그 형태로 갔다(`test_every_result_says_who_put_it_there`, `test_a_failed_optional_retriever_never_claims_a_contribution`, 점수 하한을 `0 <=`에서 `0 <`으로 조여 "어느 목록에도 없는 항목"을 불가능하게).
- **`--build` 준수와 실물 기동 검증이 함께 돌아왔다.** `docker compose run --build --rm test` 3회로 776 passed를 재확인했고, `docker compose down` → `rm -rf data` → `up -d --build --wait` → 샘플 2건 업로드 → `/search` 실측까지 갔다. 직전 회고가 Problem으로 적은 "Bash 37건에 `docker compose up` 0회"가 이번엔 이행됐다.
- **config.py에서 지운 주석의 근거가 실제로 다른 곳에 살아 있다.** 겹침 0 금지 → `ARCHITECTURE.md:391`, 8001 포트 → `:252`, 프로브 상한 → `:1355`, `candidate_depth`·`required` → README 신설 절. `CLAUDE.md`의 "밀려난 근거는 지우지 말고 옮긴다"가 지켜진 상태다.

### Problem (나빴던 것)

- **`LLM 응답 캐싱`이 여전히 코드에 없다 — 여섯 회고 연속.** `services/qa.py`에 `cache` 0회, `adapters/cache/`는 `probe.py`(헬스체크)뿐, specs·changes 어디에도 capability가 없다. 비중 18%이자 **불합격 기준**이다. 168시간 창은 오늘 15:11 마감이고 지금 12:27 — 남은 것은 약 2시간 45분인데, 이번 42분도 전부 요구사항 **밖** 확장인 하이브리드 검색에 갔다.
- **change 이름 실수가 네 세션 연속, 이번에도 2회 연속.** 다만 성격이 이전과 다르다: 이번엔 첫 프롬프트의 `<command-args>`가 `add-rrf-algorithm`(브랜치명)이었고, 사람이 준 값을 검증 없이 그대로 통과시킨 형태다. 11:03 회고가 이 문제를 Keep으로 올렸고 11:45가 재발을 Problem으로 적었는데 또 났다 — 회고에 적는 것으로는 네 번 다 실패했다.
- **검증 게이트 셋이 0회 실행.** `scripts/check_comments.py` · `ruff format --check .` · `openspec validate --strict` 모두 이 세션에서 한 번도 안 돌았다(로그의 12건 언급은 전부 `tasks.md` 9장의 `[ ]` 항목 본문이다). 이번 커밋은 `config.py`에서 주석 8블록을 지우고 `search.py`에 새 주석 2줄을 넣었는데, 커밋 전 판정은 `ruff check .` 뿐이었다. 훅에서 직접 재보려 했으나 승인 거부로 **미확인** — 관측 위치를 밝혀 둔다.
- **`/qa`는 `retrievers` 신호를 받지 못한다.** `sse.py:70`의 sources 이벤트는 `results: list[SearchResultView]`뿐이라 `contributions`는 따라 들어가지만 최상위 기여 목록은 없다. README는 그 목록을 "하이브리드가 꺼진 배포를 구별하는 **유일한** 신호"라고 적었는데 정작 답변 경로에는 그 자리가 없고, `tests/test_qa_api.py`에 `contributions` 단언도 0건이다.
- **`retrieval_cache_size`가 두 회고 연속 죽은 손잡이.** `src/`에 소비처 0(선언 한 줄), README·ARCHITECTURE 언급 0. 이번 커밋이 `config.py`를 두 번 고치면서도 지나쳤다.
- **미push 2건.** `origin/add-rrf-algorithm`은 `a4be966`, HEAD는 `426917f` — ahead 2. push는 세 회고 만에 11:45에 한 번 됐다가 곧바로 다시 벌어졌다.
- **`src.zip` 203KB가 리포 루트에 untracked.** `.gitignore`·`.dockerignore` 어디에도 `*.zip`이 없어 `--build`마다 빌드 컨텍스트로 딸려간다 — `demo-ui/`를 `.dockerignore`에 넣은 것과 같은 이유가 걸리는 자리다.
- **demo-ui 자동화 테스트 0건 — 다섯 회차째.** `demo-ui/src`에 `*.test.*`가 하나도 없다. 이번 커밋이 `format.ts`·`types.ts`·`SourceList.tsx`의 점수 표기를 유사도에서 융합으로 바꿨는데 유일한 게이트는 `tsc -b`다.
- **외부 검토 0회 — 열세 세션 연속.** `4B.1`은 여전히 `[ ]`.
- **Retrobot이 이번 창 에러의 83%(19/23).** 훅 두 회차 13건 + 이번 회차 6건. `SKILL.md`가 "먼저 쳐라"고 지시하는 `command -v python3 jq`가 또 거부됐고, 이번엔 `ls -lt`·`head`·PowerShell `Get-ChildItem` 글롭까지 전부 막혀 **로그 디렉터리에 대한 셸 접근이 사실상 전면 차단**임이 확정됐다. 통한 것은 내장 도구뿐이다 — `Glob`(파일 목록), `Grep -o` + `head_limit`(프롬프트·도구·에러), `Grep`의 `offset` 이분 탐색(세션 시작·끝 시각).

### Try (개선해야 할 것)

1. **캐싱을 지금 착수한다.** 남은 시간 2시간 45분, `add-rrf-algorithm-spec`은 41/55다. rrf 8·9장을 뒤로 미루고 최소 경로만 코드로 만든다 — 질의 정규화 키 → 저장 → 응답의 `cache_hit` 필드 → 문서 변경 시 무효화. 여섯 회고가 같은 문장을 적었으므로 이번에는 회고가 아니라 `tasks.md` **최상단**에 적는다.
2. **`retrobot/SKILL.md` 2단계를 셸 없는 절차로 교체한다.** 첫 지시 `command -v python3 jq`를 지우고 "로그 디렉터리에는 `Glob`·`Grep`만 쓴다"로 바꾸며, 이번에 통한 형태를 그대로 적는다(위 Problem 마지막 항목). `head`/`tail`/`ls` 표 항목은 삭제한다 — 이번에 전부 차단을 확인했다. 훅은 **커밋된 판**을 넘기므로 같은 커밋에 넣는다.
3. **`openspec` 호출 전 `openspec list`를 먼저 친다.** 다섯 회차째 같은 실패다. `CLAUDE.md`에 한 줄로 박는다 — "브랜치명 `add-rrf-algorithm` ≠ change 명 `add-rrf-algorithm-spec`". 회고 Keep은 네 번 다 실패했으므로 자리를 옮긴다.
4. **커밋 전 게이트를 하나로 묶어 실제로 돌린다.** `docker compose run --build --rm test` · `ruff check .` · `ruff format --check .` · `python3 scripts/check_comments.py src` · `openspec validate <change> --strict`. 이번 회차는 앞의 둘만 돌았다.
5. **`/qa` sources 이벤트에 `retrievers`를 싣고 `test_qa_api.py`에 `contributions` 단언을 더한다.** README가 그 목록을 유일한 신호라고 적어 둔 이상, 답변 경로에 없는 것은 문서-코드 불일치에 가깝다.
6. **`retrieval_cache_size`를 지우거나 소비처를 만든다.** 두 회차 방치했고, 1번의 캐싱 착수와 같은 자리라 함께 처리한다.
7. **`src.zip`을 지우고 `.gitignore`·`.dockerignore`에 `*.zip`을 넣는다.**
8. **push를 다음 작업보다 먼저 한다.** ahead 2. 창 판정은 committer date지만 평가자가 여는 것은 원격이다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 11:37 | `b2ecdd45` | `/opsx:apply add-rrf-algorithm` — change 이름 오류 2회 연속(`not found`), `-spec`으로 정정 |
| 11:39~11:52 | `b2ecdd45` | 7장 API 표면 — `ContributionView` 신설, `SearchResponse.retrievers`, 로그 필드 개명, `test_search_api.py` 확장 → **776 passed** |
| 11:53~11:58 | `b2ecdd45` | `demo-ui` 점수 표기 전환 + `npm ci` · `tsc -b`, `./data` 비우고 `up -d --build --wait` → 샘플 2건 업로드 → `/search` 실측 |
| 11:59 | `b2ecdd45` | `.env` 쓰기 시도 **사용자 거부** → 프롬프트 2 |
| 12:00~12:07 | `b2ecdd45` | 구성을 `config.py` `_default_retrievers()`로, 메모리 파일 기록, README "retriever 구성" 절 신설 |
| 12:08~12:14 | `b2ecdd45` | 사용자 질문(BM25+Dense 재랭킹 확인) → `candidate_depth == max_top_k` 로 융합 여지 0 인 것을 규명 |
| 12:15~12:19 | `b2ecdd45` | 깊이 100 · 상한 20 으로 분리, 776 passed 재확인, 마지막 `retrieval.py` Edit **사용자 거부** |
| 12:20 | — | `426917f feat: API 로직 연결` |
| 12:22~12:27 | Retrobot 훅 | 로그 집계 — 셸 경로 전면 차단 확인 후 `Glob`·`Grep`만으로 우회 |
