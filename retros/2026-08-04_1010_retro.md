# Retrobot 회고 — 2026-08-04 10:10 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 2 (이번 창 09:42~10:09) |
| 총 프롬프트 | 3 (슬래시 1 + 직접 입력 2) — 작업 세션 기준 |
| 에러 발생 | 12 (작업 세션 5 · Retrobot 훅 세션 7) |
| 커밋 | `f731869` (6파일 +553/-10) — **코드 244줄 + 테스트 299줄** |
| 미push | **3건** (`origin/add-rrf-algorithm` 은 아직 `dfbdb8d`) |
| 도구 | Bash 31 · Read 12 · Edit 5 · Write 4 |
| 검증 | 컨테이너 `700 passed, 2 deselected` · `ruff check` 통과 · `ruff format --check` 통과 |

직전 회고(09:43) 이후의 창입니다. 세션 `b1757e3d` 는 직전 회고를 만든 Retrobot 훅이고,
실제 작업은 `ce0bf3da` 한 세션입니다.

## 세션별 활동

### 세션 `b1757e3d` (09:42 ~ 09:53, 11분) — Retrobot 훅

`35b9c3a` 커밋 후 자동 실행되어 `2026-08-04_0943_retro.md` 를 만들고 `2986202` 로 커밋했습니다.

**발생한 에러:** 7건 — 전부 샌드박스 거부입니다.

- `command -v python3 jq` 승인 요구 (2회), PowerShell "script block" 거부, Glob 심볼릭 링크 미검증 거부, `$()` 부분식 거부
- 로그 파일 1.1MB `Read` 시도 → 256KB 초과 거부

`retrobot/SKILL.md` 가 "둘 다 없으면 내장 도구만 쓰라"고 적어둔 경로로 넘어가 완료했습니다.

### 세션 `ce0bf3da` (09:43 ~ 10:09, 26분) — 브랜치 `add-rrf-algorithm`

**프롬프트:**

1. `/opsx:apply add-rrf-algorithm` — "2장 어휘 토큰화까지 작업 진행해주세요." (09:43)
2. "도커 데몬 켰습니다. 실제 테스트 진행해주세요." (10:01)
3. "check_comments.py 결과는 test 폴더만 있나?" (10:07)

**발생한 에러:** 5건.

- `openspec status --change add-rrf-algorithm` → `Change 'add-rrf-algorithm' not found` (**2회 연속**). 실제 이름은 `add-rrf-algorithm-spec` 이고, `add-rrf-algorithm` 은 브랜치 이름입니다
- 호스트 파이썬 탐색 중 `python`·`python3` exit 2 — `WindowsApps` 별칭
- `ruff` UP033: `@lru_cache(maxsize=None)` → `@cache`
- `/tmp/cc.txt` 쓰기 `FileNotFoundError` — Windows에 `\tmp` 가 없음

**흐름:** 태스크 1.1~2.4를 구현했습니다. `core/fusion.py`(106줄)에 `FusionInput`·`FusedItem`·
`Contribution` 값 객체와 `fuse()`, `core/lexical.py`(138줄)에 NFKC 정규화·한글/라틴 경계 분할·
접미 최장일치 벗김 토크나이저와 `TOKENIZER_VERSION`. 테스트는 `test_fusion.py`(211줄) ·
`test_lexical_tokenizer.py`(88줄), 합쳐 **40 passed**.

첫 컨테이너 시도(09:47)에서 Docker 데몬이 꺼져 있어 막히자, 호스트에서 실물 파이썬을 찾아
`py.exe -3`(3.15.0b3)로 스크래치 venv를 만들고 `--noconftest` 로 40건을 먼저 돌렸습니다.
사용자가 데몬을 켠 뒤 컨테이너에서 `docker compose run --build --rm test` → **700 passed,
2 deselected (69.82s)**, `ruff check .` 통과, `ruff format --check` 통과.

마지막 프롬프트에는 `check_comments.py` 위반 432건을 디렉터리·종류별로 집계해 답했습니다 —
`tests/` 432 · `src/` 0 · `scripts/` 0, 종류는 강조 마크업 199 · docstring 길이 180 ·
선언조 35 · 인라인 3줄 이상 17 · 주석>코드 1.

## KPT 회고

### Keep (좋았던 것)

- **직전 창에서 조인 스펙이 코드에서 그대로 값을 했습니다.** 09:38에 사용자가 지정한
  "원점수와 같은 항 형태·같은 누적 순서" 계약이 `fusion.py:77-79` 의 루프 누적으로 내려왔고,
  `(Σw)/(RRF_K+1)` 로 접지 않은 이유가 바로 위 주석에 1 ulp 어긋남으로 적혀 있습니다.
  스펙 정교화가 문서 안에서 끝나지 않고 실행되는 코드와 테스트로 닫힌 첫 사례입니다.
- **막힌 경로를 우회하되 최종 판정은 정본으로 받았습니다.** 데몬이 꺼진 동안 스크래치 venv로
  40건을 돌린 것은 사전 점검이고, 데몬이 살아난 뒤 `--build` 붙은 컨테이너로 700건을 다시
  돌렸습니다. 호스트 결과를 최종 근거로 삼지 않았습니다.
- **알아낸 환경 사실을 `CLAUDE.md` 에 되돌려 썼습니다.** "Windows에는 파이썬이 없다"는 기존
  기술이 틀렸다는 것(`Launcher\py.exe -3` 는 실물 3.15.0b3)과 `pytest`·`ruff`·`fastapi` 부재,
  `conftest.py` 때문에 `--noconftest` 가 필요하다는 것, 데몬이 꺼져 있을 수 있다는 것까지
  같은 커밋에 넣었습니다. `CLAUDE.md` 가 "환경 사실은 이 파일에만 남긴다"고 요구하는 그대로입니다.
- **`ruff format` 이월이 끝났습니다.** 여덟 창을 넘어온 `ruff format --check .` 6 files 가
  이번 컨테이너 실행에서 "already formatted" 로 통과했습니다.
- **범위를 넘지 않았습니다.** `check_comments.py` 위반 432건이 전부 기존 `tests/` 파일이고
  태스크 9.6의 몫이라는 것을 확인한 뒤 손대지 않고 보고만 했습니다. 신규 4파일은 0건입니다.

### Problem (나빴던 것)

- **`LLM 응답 캐싱` 은 여전히 코드에 없습니다 — 불합격 기준 항목입니다.**
  `services/qa.py` 에 `cache` 문자열 **0회**, `adapters/cache/` 는 아직 `probe.py` 하나.
  직전 회고의 Try 2번이 "남은 시간을 캐싱에 쓴다" 였고, 이번 창 26분은 전부 RRF 하이브리드
  검색에 갔습니다. 하이브리드 검색은 과제 요구사항 밖 확장이고 캐싱은 요구사항 3번입니다.
  **세 회고 연속** 같은 항목이 Problem에 남았습니다.
- **push를 또 하지 않았고, 미push가 1건에서 3건으로 늘었습니다.** 직전 회고의 Try **1번**이
  "다른 무엇보다 먼저 `git push`" 였습니다. 168시간 창이 **오늘 15:11 KST**에 닫히므로
  10:09 기준 약 **5시간**이 남았고, 원격에는 `dfbdb8d`(01:15) 이후 아무것도 없습니다.
  로컬에만 있는 커밋은 평가자가 볼 수 없습니다.
- **change 이름을 틀린 채로 두 번 연속 호출했습니다.** `openspec status --change add-rrf-algorithm`
  이 사용 가능한 이름 셋을 출력하며 실패했는데, 다음 호출에서 같은 이름을 다시 넣었습니다.
  에러 메시지에 답이 인쇄돼 있었습니다. 브랜치명(`add-rrf-algorithm`)과 change명
  (`add-rrf-algorithm-spec`)이 한 글자 차이인 것이 원인이고, 앞으로도 계속 부딪힐 모양입니다.
- **`docker compose run` 을 `--build` 없이 한 번 돌렸습니다.** 10:05의 "두 테스트 파일만
  자세히" 실행이 그렇습니다. 직전 `--build` 실행 이후 편집이 없어 결과는 유효했지만,
  `CLAUDE.md` 가 "반드시 `--build`" 라고 못 박은 규칙에서 벗어난 실행입니다. 편집이 끼었다면
  구세대 이미지를 검사하고도 통과로 읽었을 것입니다.
- **`/tmp` 에 쓰려다 실패했습니다.** `retrobot/SKILL.md` 가 "Bash heredoc·`/tmp` Write는
  샌드박스가 막는다"고 이미 적어둔 항목인데, Windows에는 `\tmp` 자체가 없다는 쪽으로
  다시 부딪혔습니다. 회고 훅에만 적혀 있고 `CLAUDE.md` 의 Windows 행에는 없습니다.
- **열린 change는 그대로 셋입니다** (`add-answer-generation` 86/87 · `add-demo-ui` ·
  `add-rrf-algorithm-spec`). 직전 Try 6번이 "`add-answer-generation` 을 닫는 것 외에는
  새 스펙 작업을 하지 않는다" 였는데, 이번 창은 `add-rrf-algorithm-spec` 의 구현으로 갔습니다.
- **외부 검토 0회, 열 세션 연속.**

### Try (개선해야 할 것)

1. **먼저 `git push` 합니다.** 두 회고 연속 1번 자리에 같은 문장이 있습니다. 3건이 로컬에만
   있고 창은 5시간 뒤 닫힙니다. 다음 편집을 시작하기 전에 끝냅니다.
2. **남은 시간은 `LLM 응답 캐싱` 최소 구현에 씁니다.** 스펙을 새로 열지 않습니다 —
   질문 정규화 키 → `adapters/cache/` 실제 저장소 어댑터 → `services/qa.py` 조회·기록 →
   응답에 캐시 히트 필드 → 문서 변경 시 무효화. `add-rrf-algorithm-spec` 의 3장 이후는
   "미구현 부분은 설계 문서로 대체 가능" 규정에 기댑니다.
3. **`openspec` 호출 전에 change 이름을 목록에서 확인합니다.** 브랜치명을 그대로 넣지 않습니다.
   실패 메시지가 후보를 인쇄하면 **다음 호출에서 그 후보를 씁니다** — 같은 인자로 재시도하지
   않습니다.
4. **`docker compose run` 에 `--build` 를 예외 없이 붙입니다.** "직전에 빌드했으니 괜찮다"는
   판단을 하지 않습니다. 판단이 맞았는지는 결과가 같아서 확인되지 않고, 틀렸을 때만 조용히
   틀립니다.
5. **`/tmp` 를 쓰지 않습니다.** 이 호스트에서 스크래치가 필요하면 리포 밖의 실재하는 경로를
   쓰거나, 파일 없이 파이프로 끝냅니다. 이 사실을 `CLAUDE.md` 의 Windows 행에 옮겨 적습니다
   (지금은 `retrobot/SKILL.md` 에만 있습니다).

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 (KST) | 세션 | 주요 활동 |
|------|------|----------|
| 09:42~09:53 | `b1757e3d` | Retrobot 훅 — 09:43 회고 생성, 샌드박스 거부 7건을 내장 도구로 우회, `2986202` 커밋 |
| 09:43 | `ce0bf3da` | `/opsx:apply add-rrf-algorithm` — "2장까지 진행" |
| 09:43~09:45 | `ce0bf3da` | `openspec status/instructions` 두 번 실패(change 이름 오류) 후 `-spec` 으로 정정, 아티팩트 정독 (Read 12) |
| 09:45~09:47 | `ce0bf3da` | `core/fusion.py` · `core/lexical.py` · 테스트 2종 작성 (Write 4) |
| 09:47 | `ce0bf3da` | 컨테이너 실행 시도 → Docker 데몬 꺼짐 |
| 09:48~09:52 | `ce0bf3da` | 호스트 파이썬 탐색 — `python`/`python3` exit 2, `py.exe -3`(3.15.0b3) 발견, 스크래치 venv 생성 |
| 09:52 | `ce0bf3da` | `--noconftest` 로 **40 passed**, `ruff` UP033 지적 → `@cache` 로 수정 후 재실행 40 passed |
| 09:55 | `ce0bf3da` | `check_comments.py` 신규 4파일 0건, 태스크 1.1~2.4 체크, `CLAUDE.md` Windows 행 갱신 |
| 10:01 | `ce0bf3da` | 사용자 "도커 데몬 켰습니다" |
| 10:02~10:04 | `ce0bf3da` | `docker compose run --build --rm test` → **700 passed, 2 deselected (69.82s)**, `ruff check .` 통과 |
| 10:05 | `ce0bf3da` | 신규 2파일 재확인 40 passed (`--build` 누락 실행) |
| 10:07 | `ce0bf3da` | 사용자 "check_comments.py 결과는 test 폴더만 있나?" |
| 10:07~10:09 | `ce0bf3da` | `/tmp/cc.txt` 실패 후 우회 집계 — 432건 전부 `tests/`, 종류별 분해 보고 |
| 10:09 | — | `f731869` 커밋 (6파일 +553/-10). push 안 함 (누적 3건) |
