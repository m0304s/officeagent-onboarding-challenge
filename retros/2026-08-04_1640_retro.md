# Retrobot 회고 — 2026-08-04 16:40 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 3 (구현 1 · 신규 착수 1 · Retrobot 훅 1) |
| 총 프롬프트 | 7 (구현 세션) |
| 도구 호출 | 약 112 (Edit 51 · Bash 30 · Read 21 · Grep 9 · Skill 1) |
| 에러 발생 | 7 (구현 2 · 신규 세션 2 · Retrobot 3) |
| 커밋 | `9ad7dd8` — 6파일 **+47/-105**, 전부 `openspec/` |
| 테스트 | 실행 0회 (코드 변경 0줄) |
| 진행 | `add-rrf-algorithm-spec` 41/55 → **41/50** (분모가 줄어듦) |

## 세션별 활동

### 세션 `2b0b7d11` (14:29 ~ 16:35 KST, 2시간 06분)

**프롬프트:**

1. 14:29 — `/opsx:propose add-rrf-algorithm` — "8번 작업 진행하려고 합니다. 8.2에 Retriever 프로토콜에 `signature: str` 을 추가하고, 검색 구성 서명을 (이름, 가중치, 깊이, retriever.signature) 목록 + rrf_k 로 유도하는 방향에 대해 검토해주세요. 하한을 아는 곳이 하한을 가진 객체가 되고, 서비스는 나열하지 않도록"
2. 14:34 — "A안으로 진행해주세요."
3. 14:36 — "진행해주세요."
4. 14:39 — "`‖` 연결은 경계가 모호합니다. `json.dumps(..., sort_keys=True, separators=(...))` …"
5. 14:41 — "8.2 의 유도 함수는 순수·표준 라이브러리 전용이고 `derive_index_signature` 와 같은 종류입니다 → `core/` 입니다. `adapters/cache/` 에는 구현체만 두고, 이름은 계층(`retrieval.py`)이 아니라 기구(`memory.py`)로 붙이세요. 캐시 히트를 로그에 남기는 항목이 없습니다."
6. 14:57 — `/opsx:update` — "검색 캐시는 구현하지 않습니다."
7. 16:16 — "작업 진행해주세요."

**발생한 에러:** 2건

- `openspec status --change add-rrf-algorithm` → `Change 'add-rrf-algorithm' not found. Available changes: add-answer-generation / add-demo-ui / add-rrf-algorithm-spec`. **다섯 세션 연속** 같은 실수. 다만 이번엔 바로 다음 호출이 `ls openspec/changes/` 였고 그다음이 `-spec` 정정이라 **재시도 없이 1회로 끝났다**.
- `sed: can't read openspec/changes/add-rrf-algorithm-spec/proposal.md: No such file or directory` (exit 2). 파일은 있다 — 앞선 호출 `cd openspec/changes/add-rrf-algorithm-spec && grep …` 가 남긴 작업 디렉터리 때문이다.
- (에러로 집계되진 않았으나) Edit `String to replace not found` 1건 — 이미 다른 Edit 이 지운 문장을 다시 지우려 했다.

### 세션 `976130a0` (16:36 ~, 진행 중)

커밋 직후 시작된 새 세션. 첫 명령이 또 `/opsx:apply add-rrf-algorithm` — **change 이름 실수 여섯 회차 연속**. 에러 2건.

### 세션 `269378f4` (Retrobot 훅)

에러 3건 — 전부 승인 거부(`command -v python3 jq`, `ls -la`, `py.exe -c`).

## KPT 회고

### Keep (좋았던 것)

- **사용자의 설계 지적을 계층 규칙에서 이름 규칙까지 밀어붙였다.** 프롬프트 4·5 두 건이 각각 (a) `‖` 문자열 연결은 구분자가 데이터에 섞이면 경계가 모호해지므로 `json.dumps(sort_keys=True, separators=…)`, (b) 순수·표준 라이브러리 전용 유도 함수는 `core/`, `adapters/cache/` 에는 구현체만, 그 파일 이름은 계층(`retrieval.py`)이 아니라 기구(`memory.py`)를 가리켜야 한다는 것이었고, 아티팩트가 그대로 그 형태로 갔다. `derive_index_signature` 라는 **기존 함수를 근거로 지목한 지적**이라 리포 안의 선례에 맞추는 교정이었다.
- **change 이름 실수의 복구 비용이 절반으로 줄었다.** 직전 회차는 첫 실패가 후보 셋을 인쇄했는데 **같은 인자로 재시도**했다. 이번엔 실패 → `ls openspec/changes/` → `-spec` 으로 1회 정정이다. 실수 자체는 다섯 회차째 남았지만, 실패 출력을 읽고 행동을 바꾼 것은 처음이다.
- **되돌린 결정의 이유를 남길 자리를 미리 만들었다.** "검색 캐시는 구현하지 않습니다"로 8장 캐시 스펙을 통째로 지우면서, 태스크 8.4 에 "검색 결과 캐시를 넣지 않은 이유(결과가 아니라 지연만 다룬다)도 한 줄 남긴다 — **적지 않으면 다음 사람이 빠진 것으로 읽고 다시 설계한다**"를 넣었다. 이 리포가 `CLAUDE.md` 에 적어 둔 "밀려난 근거는 옮긴다"를 삭제 방향에도 적용한 것이다.
- **두 회고를 방치했던 죽은 손잡이가 처음으로 실행 목록에 올랐다.** `retrieval_cache_size` 제거가 태스크 5.8 로 들어갔고, `tests/test_config.py` 의 관련 단언까지 함께 지우라는 범위까지 적혔다. 회고 Problem 에만 있던 항목이 `tasks.md` 로 옮겨간 첫 사례다.
- **`openspec validate --strict` 를 5회 돌렸다.** 직전 회차가 "게이트 셋 0회 실행"이었던 것과 대비된다.
- **`src.zip` 203KB 가 사라졌다.** 직전 회고 Try 7번의 절반이 이행됐다.

### Problem (나빴던 것)

- **커밋이 168시간 창 **밖**이다.** 첫 커밋 `1313b0a` 가 2026-07-28 15:11:44 KST 이므로 창은 **2026-08-04 15:11:44 에 닫혔다.** `9ad7dd8` 의 committer date 는 **16:36:40 — 1시간 25분 초과**다. `CLAUDE.md` 는 "창 밖 커밋은 채점에서 제외되며 README 정리·오탈자 수정도 예외가 없습니다"라고 적었다. 세션은 14:29 에 시작해 마감까지 **42분**을 갖고 있었는데 그 사이 커밋이 없었고, **14:57 → 16:16 의 79분 공백이 마감선을 그대로 통과했다.** 창 안 마지막 커밋은 `bd61cd8`(12:33, 회고)이고, 실질 산출물 기준으로는 `426917f`(12:20)다.
- **`LLM 응답 캐싱`이 창이 닫힐 때까지 코드에 없었다 — 일곱 회고 연속.** `services/qa.py` 에 `cache` 0건이고, `api/routes/qa.py` 의 유일한 한 건은 HTTP 응답 헤더 `Cache-Control: no-cache` 다. 비중 18%이자 **불합격 기준**이다. 직전 회고 Try 1번이 "남은 2시간 45분에 캐싱을 착수한다"였는데, 실제로 쓴 2시간은 **캐시를 스펙에서 지우는 데** 갔다. 지운 것은 검색 결과 캐시(요구사항 밖)이지 LLM 응답 캐시(요구사항 안)가 아니라는 점에서 방향이 어긋난 것은 아니지만, 요구사항 안쪽은 이번에도 0줄이다.
- **40분 쓴 설계를 16분 뒤에 되돌렸다.** 14:36~14:57 사이 Edit 22회로 8장 검색 캐시 스펙(`derive_search_signature`, `adapters/cache/memory.py`, 캐시 히트 로그)을 세웠고, 14:57 `/opsx:update 검색 캐시는 구현하지 않습니다` 로 전부 삭제했다. 순 산출물은 **+47/-105**. 특히 프롬프트 5(14:41)에서 사용자가 "캐시 히트를 로그에 남기는 항목이 없습니다"라며 **보강을 요구한 지 16분 만의 반전**이라, 확장을 시작하기 전에 "이 캐시를 만들 것인가"를 먼저 물었어야 했다.
- **이번 창의 코드가 0줄이다.** 6파일 전부 `openspec/changes/add-rrf-algorithm-spec/` 밑 문서이고 `src/`·`tests/`·`demo-ui/` 는 손대지 않았다. 창이 닫히는 회차에 실행 가능한 산출물이 늘지 않았다.
- **검증 게이트 다섯 중 하나만 돌았다.** `openspec validate --strict` 5회, 나머지는 0회 — `docker compose run --build --rm test` · `ruff check .` · `ruff format --check .` · `scripts/check_comments.py`. 직전 회고 Try 4번이 다섯을 묶으라 했다. 코드 변경이 없어 위험은 낮지만, "묶어서 한 번에 돌린다"는 절차 자체는 또 안 만들어졌다.
- **`cd` 가 살아남아 뒤의 상대 경로를 깼다.** Bash 도구는 호출 사이에 작업 디렉터리를 유지한다. `cd openspec/changes/add-rrf-algorithm-spec && grep …` 다음의 `sed -n … openspec/changes/add-rrf-algorithm-spec/proposal.md` 가 exit 2 로 죽었고, 원인 진단에 한 번 더 왕복했다.
- **직전 회고 Try 8건 중 이행 2.5건.**
  - #1 캐싱 착수 → 반대 방향
  - #2 `retrobot/SKILL.md` 를 셸 없는 절차로 교체 → **미이행**. 이번 훅도 첫 지시 `command -v python3 jq` 에서 거부됐다
  - #3 `CLAUDE.md` 에 "브랜치명 `add-rrf-algorithm` ≠ change 명 `add-rrf-algorithm-spec`" 못박기 → **미이행**(`CLAUDE.md` 에 해당 문자열 0건). 그 결과가 이번 두 세션의 재발이다
  - #4 게이트 묶음 → 1/5
  - #5 `/qa` sources 에 `retrievers` + `test_qa_api.py` 의 `contributions` 단언 → **미이행**(`sse.py`·`services/qa.py` 에 두 단어 모두 0건)
  - #6 `retrieval_cache_size` → 코드는 그대로, 태스크만 생김 (절반)
  - #7 `src.zip` → 파일은 삭제, `*.zip` 은 `.gitignore`·`.dockerignore` 어디에도 없음 (절반)
  - #8 push → **미이행**
- **미push 4건.** `origin/add-rrf-algorithm` 은 아직 `a4be966`(11:36) 이다. 창이 닫힌 지금은 "원격에 없다"가 곧 "평가자에게 없다"이고, `426917f`·`bd61cd8` 두 **창 안** 커밋까지 원격에 없다.
- **Retrobot 셸 차단 스물여섯 번째.** `SKILL.md` 가 "먼저 쳐라"고 지시하는 `command -v python3 jq` 가 또 거부됐고, `ls -la` 와 `py.exe -c` 도 막혔다. 직전 회고 Try 2번이 정확히 이 교체였는데 미이행이라 같은 우회로(내장 `Glob`·`Grep -o`+`head_limit`·`Grep offset` 이분 탐색)를 처음부터 다시 찾았다.
- **외부 검토 0회 — 열네 세션 연속.** `4B.1` 은 여전히 `[ ]`.
- **demo-ui 자동화 테스트 0건 — 여섯 회차째.**

### Try (개선해야 할 것)

1. **창 초과를 문서에 명시한다.** `9ad7dd8` 이후의 커밋은 채점 대상이 아니다. `README.md` 나 PR 본문에 "168시간 창은 2026-08-04 15:11:44 에 닫혔고, 이후 커밋은 스펙 정리다"를 한 줄 적는다 — 적지 않으면 평가자가 커밋 목록을 창 안 산출물로 읽을 수 있고, 이 리포에서 그 종류의 오독은 허위 기재에 가깝다.
2. **push 를 다음 무엇보다 먼저 한다.** ahead 4 이고 그중 둘(`426917f`·`bd61cd8`)은 **창 안**이다. 원격에 올리지 않으면 창 안에 만든 것도 평가되지 않는다. 이 항목은 네 회고 연속 Try 이고 네 번째로 미이행이다.
3. **`retrobot/SKILL.md` 2단계를 셸 없는 절차로 교체한다.** 두 회고 연속 Try 이고 두 번 다 미이행이라 같은 거부가 스물여섯 번 반복됐다. 첫 지시 `command -v python3 jq` 를 지우고 `Glob`·`Grep -o`+`head_limit`·`Grep offset` 만 남긴다. `head`/`tail`/`ls` 표 항목도 삭제한다.
4. **`CLAUDE.md` 에 change 이름을 못박는다.** 두 회고 연속 Try, 두 번 다 미이행, 그사이 여섯 세션 연속 재발. 회고에 적는 것이 여섯 번 실패했으므로 자리를 옮긴다 — "브랜치 `add-rrf-algorithm` ≠ change `add-rrf-algorithm-spec`, openspec 호출 전 `openspec list`".
5. **`LLM 응답 캐싱` 최소 경로를 만든다.** 창 밖이라 점수는 없지만 **불합격 기준**이라 없는 채로 두면 창 안 산출물까지 함께 떨어진다. 질의 정규화 키 → 저장 → 응답 `cache_hit` → 문서 변경 시 무효화, 넷이면 된다.
6. **되돌릴 수 있는 확장 앞에 확인 한 번을 넣는다.** 이번 40분은 "만들 것인가"를 묻지 않고 "어떻게 만들 것인가"부터 시작해서 버려졌다. 스펙을 새로 세우기 전에 그 capability 가 요구사항 안인지 밖인지를 한 줄로 확인한다.
7. **Bash 에서 `cd` 를 쓰지 않는다.** 작업 디렉터리가 호출 사이에 유지되므로 절대 경로나 `git -C` / `sed … <경로>` 형태를 쓴다.
8. **`*.zip` 을 `.gitignore`·`.dockerignore` 에 넣는다.** 파일만 지웠고 재발 방지는 안 됐다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 14:29 | `2b0b7d11` | `/opsx:propose add-rrf-algorithm` — change 이름 오류 1회, `ls` 로 후보 확인 후 `-spec` 정정. 8.2 `Retriever.signature` 방향 검토 |
| 14:34~14:36 | `2b0b7d11` | A안 확정, 8장 검색 캐시 스펙 착수 |
| 14:39 | `2b0b7d11` | 사용자 지적 — `‖` 대신 `json.dumps(sort_keys=True, separators=…)` |
| 14:41~14:57 | `2b0b7d11` | 사용자 지적 — 유도 함수는 `core/`, `adapters/cache/memory.py`, 캐시 히트 로그 누락. Edit 22회로 반영 |
| 14:57 | `2b0b7d11` | `/opsx:update` — **"검색 캐시는 구현하지 않습니다"**, 직전 40분치 스펙 철거 시작 |
| **15:11:44** | — | **168시간 창 마감** (첫 커밋 `1313b0a` 07-28 15:11:44 + 168h) |
| 15:15 | `2b0b7d11` | `cd openspec/changes/… && grep` — 이후 상대 경로 `sed` 가 exit 2 |
| 16:16~16:35 | `2b0b7d11` | 6파일 철거 마무리, `openspec validate --strict` 통과 |
| 16:36 | — | `9ad7dd8 docs: search 단계 캐시 삭제` — **창 밖 1시간 25분** |
| 16:36 | `976130a0` | 새 세션 `/opsx:apply add-rrf-algorithm` — change 이름 오류 여섯 회차 연속 |
| 16:36~16:40 | Retrobot 훅 | 로그 집계 — `command -v python3 jq` 재거부, 내장 도구로 우회 |
