# Retrobot 회고 — 2026-08-04 22:59 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 21 (실작업 8 · Retrobot 훅 12 · 서브에이전트 3 중복 포함) |
| 총 프롬프트 | 32 (그중 12건이 훅이 자기 자신에게 넣은 `SKILL.md` 본문) |
| 에러 발생 | 59 (그중 **45건(76%)이 Retrobot 자신**) |
| 커밋 | 38 (회고 커밋 포함) — `dcd6bd7`(SSE) ~ `4425f5f`(캐시 계측) |
| 도구 호출 | Bash 587 · Edit 183 · Write 111 · Read 102 · Grep 30 |

24시간 창은 **SSE 스트리밍 마감 → 데모 UI → RRF 하이브리드 검색 → 응답 캐싱 7장 완주**로
과제 요구사항 다섯 중 셋(RAG 스트리밍 · 캐싱 · 무효화)이 이 창 안에서 닫혔습니다.

## 세션별 활동

### 세션 `724d2218` (08-03 22:58 ~ 08-04 00:31) — 데모 UI + 스트리밍

**프롬프트:**
1. `자, 그렇다면 다른 세션에서 change를 완료하고 나서 프론트엔드 데모 UI를 붙여보겠습니다.`
2. `현재, change 완성했습니다.(SSE) / 이어서 프론트엔드 데모 UI 완성해주세요`
3. `add-demo-ui 완성됐습니다.`
4. `demo-ui/DESIGN.md 파일 기반으로 UI 수정해주세요.`
5. `중단 및 UI를 통한 업로드 및 삭제 작업 진행했습니다.`

서브에이전트 셋(`agent-adbc9bff`·`agent-a6b691ee`·`agent-a7e649b8`)을 동시에 띄워
**백엔드 API 계약 문자열 실측 · 리포 관례 조사 · Vite 구현 설계**로 갈랐습니다.
TypeScript 프론트가 백엔드 enum 값을 추측하지 않게 "정확한 리터럴을 인용하라"고 지시한 것이
이 갈래의 요점입니다. 이후 `claude-in-chrome`으로 실물 브라우저에서 업로드·삭제까지 눌러 확인.

**발생한 에러:**
- `(eval):cd:1: no such file or directory: demo-ui` — 데모 UI 생성 전에 `cd` 시도
- `Couldn't determine which page this action targets` — 브라우저 탭 컨텍스트 미갱신

### 세션 `8ccc760d` (08-03 23:06 ~ 23:42) — SSE 마감

Bash 51 · Edit 44. `=== health === 000`(exit 56)이 나온 뒤 컨테이너를 다시 세워 통과.

### 세션 `2d412d02` (08-04 00:51 ~ 19:24) · `58b22a1f` (00:31 ~ 00:51)

`git checkout add-answer-generation`으로 시작해 RRF 스펙·구현 구간으로 이어졌습니다.
**다만 09:42~17:29의 커밋 8건(RRF 융합·토크나이저·어휘 색인 통합·팬아웃·API 배선·BM25·아카이빙)에
대응하는 로컬 로그가 거의 없습니다** — 이 리포는 호스트 둘에서 작업되므로 그 구간은 다른 호스트의
로그일 가능성이 높습니다. 회고가 이 창의 작업량을 구조적으로 과소 집계합니다.

### 세션 `45f0bdd9` (08-04 20:13 ~ 20:48) — 캐시 설계 검토

**프롬프트:**
1. `해당 내용이 / 사용자가 질문을 했을 시, 질문에 대한 캐시를 설명하는 것이 맞나요?`
2. `상황 / 운영자가 캐시를 껐다 → 항상 미스 / 테스트가 Redis 없이 돈다 → 정상 캐시 동작(히트 포함) / Redis가 죽었다 → 항상 미스 + 관측 가능 / 이거 검토해주세요.`
3. `수정해주세요.`
4. `1. REPLACED 가 no-evidence 미삭제하는 것 확인해주세요. / 2. insufficient_evidence 무효화 누락된 것 확인해주세요.`

**네 번째 프롬프트가 이 창에서 가장 값이 큰 두 줄입니다.** 두 시간 뒤 `2336861`(무효화)의
설계 축이 정확히 이 지적이었습니다 — 긍정 답변과 부정 답변은 서로 다른 사건에 반응하고,
`no_evidence` 항목은 태그가 비어 문서 태그 무효화가 닿지 못합니다.

### 세션 `75cd4368` (08-04 20:49 ~ 22:56) — 응답 캐싱 7장 완주

Bash 135 · Edit 96 · Read 29 · Write 16, 1,135 엔트리. 창에서 가장 무거운 세션입니다.

**프롬프트:**
1. `23건 테스트 왜 생략된거지?` → `README.md:505`의 낡은 `660 passed, 2 deselected`가 드러나 같은 커밋에서 정정
2. `음...CLI 실행해서 기본 모델 측정할 수 있나요? / 만약, 측정 가능하다면, 기본 모델값을 해당 내용으로 넣어서 세팅값 넣으면 안되나요` → 실측 후 **거절**(어댑터가 빈 값을 `thread/start`에 그대로 넘기므로 설정에 적는 순간 관측이 지시로 바뀜)
3. `나중에 작업 진행하겠습니다. 명시해주세요` → 태스크 7.7을 닫지 않고 선택지·수치와 함께 명시적 미결로 남김

**발생한 에러(8건, 전부 게이트가 잡아준 것):**
- `E501 (118>100)`·`(103>100)`·`(102>100)` — 새 테스트 세 곳
- `ruff format`이 `cache.py`·`sqlite.py`를 재포맷 요구
- `src/app/config.py:91 인라인 주석 3줄 (상한 2)` — 21:02·21:11에 검출, **이 창 마지막 커밋에서 닫힘**
- `Found 2 matches of the string to replace` — Edit 중복 매치

### Retrobot 훅 세션 12건 (`d0c01226`·`cc79ede2`·`e4260f90`·`a2542476`·`115a7e25`·`e27b60d4`·`7a93d660`·`4e480d75`·`8486165b`·`25644f92`·`cf9d24e5`·`3f5b5605`)

**프롬프트:** 12건 모두 동일 — `retrobot/SKILL.md` 본문 전체.

**발생한 에러(45건):** 같은 두 지점의 반복입니다.
- `command -v python3 jq` 거부 — **12/12**. 단독(`command -v python3`)도, 세미콜론으로 이은 형태도 거부
- `/tmp/*.py` Write 거부 — **11/12**
- 나머지는 그 두 벽 이후의 우회 시도(파이프·`&&`·f-string 백슬래시 문법 오류)

## KPT 회고

### Keep (좋았던 것)

- **사용자 지적이 설계를 바꾼 자리마다 커밋 경계가 살아 있습니다.** 20:43의 두 줄
  (`REPLACED가 no-evidence 미삭제`·`insufficient_evidence 무효화 누락`)이 `2336861`의
  설계 축이 됐고, 21:16의 `scope` 지적은 코드가 붙기 6분 전인 `3eda547`(코드 0줄, 문서만)로
  들어갔습니다. **설계 수정을 코드 비용이 0인 시점에 통과시킨 것**이 이 창의 가장 좋은 패턴입니다.
- **실측한 뒤 사용자 제안을 거절했습니다.** 22:18의 "CLI로 기본 모델 재서 설정에 넣자"는
  컨테이너에서 `model/list`를 직접 쳐 `gpt-5.6-sol`을 확인하고도 박지 않았습니다 —
  어댑터가 빈 값을 그대로 넘기므로 적는 순간 *관측*이 *지시*가 되기 때문이고,
  대신 남는 구멍을 범위·수명·닫는 법까지 문서 양쪽에 남겼습니다. **거절에 근거가 붙어 있습니다.**
- **`config.py:91` 주석 위반이 일곱 회차 만에 닫혔습니다.** 지금 `check_comments.py src`는
  **62개 파일, 위반 0건**입니다. 여섯 편의 회고가 같은 한 줄을 지적했고, 이번 창의 마지막
  커밋이 그것을 회수했습니다.
- **`--build` 준수가 창 내내 유지됐고 실제로 회귀를 잡았습니다.** 22:26의 재검증이
  `859 passed, 24 errors`(`IngestionService` 시그니처 미반영)를 드러냈고, 단언 완화가 아니라
  하네스 세 개 수정으로 닫았습니다.
- **테스트가 실물 없이 도는 층으로 갈려 있습니다.** `redis`·`llm` 마커로 기본 실행은
  구독·서버 없이 통과합니다(과제의 무구독 가산점 조건과 같은 자리). 핵심 3경로
  (ingestion · retrieval · **캐시 무효화**)가 이 창에서 전부 채워졌습니다.
- **서브에이전트 셋을 축이 겹치지 않게 갈랐습니다**(계약 실측 / 관례 조사 / 구현 설계).
  프론트가 백엔드 enum을 추측하지 않게 "정확한 리터럴 인용"을 명시한 것이 특히 유효했습니다.

### Problem (나빴던 것)

- **Retrobot이 창 에러의 76%(45/59)를 자기 손으로 만들었고, 이번이 열세 번째입니다.**
  훅 12세션이 전부 같은 두 줄에서 죽습니다 — `command -v python3 jq`(12/12 거부) →
  `/tmp` Write(11/12 거부). **원인은 `retrobot/SKILL.md` 자신입니다.** 본문이
  "먼저 `command -v python3 jq`를 쳐라"고 지시해 매 회차 첫 에러를 만들고, "하지 말 것"이
  heredoc과 `/tmp` Write를 동시에 막습니다. `afa9794`(08-03 22:14) 이후 **25시간 무수정**이고
  회고 여덟 편이 같은 진단만 반복했습니다.
- **대가를 치른 세션이 있습니다.** `115a7e25`는 두 번 막힌 뒤 python3를 재시도하지 않고
  **Grep 30회**로 우회했습니다 — 이 호스트에서 `python3 -c`는 멀쩡히 돕니다
  (**이 회고 전체가 `python3 -c` 단일 호출 한 번으로 집계됐습니다**).
- **`ruff format --check` 위반이 아홉 번째 재현이고 10 → 11건으로 늘었습니다.**
  같은 시각 `ruff check`는 `All checks passed!`를 냅니다 — 코드가 88자(black 기본값)로 손수
  줄바꿈돼 있는데 `pyproject.toml:45`는 `line-length = 100`이라 **E501을 100으로 재기 때문**입니다.
  **초록불이 거짓이고**, 닫는 비용은 `.venv/bin/ruff format src tests` 한 줄 10초입니다.
- **구현이 또 문서를 앞질렀습니다.** `ARCHITECTURE.md`·`README.md`·`CLAUDE.md`가
  `+71/-9`로 워킹트리에만 있습니다. **커밋된 `README.md`는 지금도 "응답 캐싱, 캐시 무효화 — 미구현",
  "하이브리드 검색 — 미구현"이라고 말합니다.** 방향이 반대라 허위 기재는 아니지만,
  `b07c66d`의 `tasks.md` 누락 · `2336861`의 `ARCHITECTURE.md` 누락에 이은 **세 번째 재발**입니다.
- **미푸시 34건, `add-response-cache` 브랜치가 origin에 아예 없습니다.**
  `f07ed41`부터 `4425f5f`까지 캐싱 9커밋 전부가 로컬에만 있어 **평가자가 지금 리포를 보면
  캐싱(비중 18%·불합격 기준)은 착수조차 안 한 것으로 보입니다.** 원격에는 RRF까지만 올라가 있습니다.
- **`add-multiturn-qa` 4아티팩트가 23시간째 untracked — 여섯 번째 계수입니다.**
  그 사이 change 둘이 커밋됐습니다. 168시간 창에서 커밋되지 않은 작업은 존재하지 않는 작업입니다.
- **외부 적대적 검토 0회, 열세 세션 연속.** 이 창에서 설계가 사람의 지적으로 **다섯 번** 뒤집혔고
  (`scope` 분리 · `no_evidence` 무효화 · `REINDEXED` 포함 · Redis 부재 시 동작 · 모델명 하드코딩),
  `openspec validate --strict`는 그중 한 건도 잡지 못했습니다.

### Try (개선해야 할 것)

- **`retrobot/SKILL.md`를 세 줄 고칩니다 — 25시간 미룬 그 세 줄입니다.**
  1. `command -v python3 jq` 지시를 지우고 **`python3 -c "print(1)"` 단일 호출**로 대체
     (`command -v` 자체가 이 샌드박스에서 거부됩니다)
  2. `/tmp` 스크립트 Write 지시를 지우고 **`python3 -c '<본문>'` 단일 인자 인라인**을 표준 경로로 명시
     (`>`·`;`·`&&`·파이프를 붙이는 순간 거부되므로 **단일 명령**이어야 합니다)
  3. heredoc 금지를 **"Windows 호스트에 한함"**으로 좁힙니다 — macOS에서는 통하는 수단입니다
- **`.venv/bin/ruff format src tests`를 지금 돌립니다**(10초). 그다음
  `pyproject.toml`의 `line-length`를 88로 내리거나 코드를 100자에 맞추거나 **하나를 고릅니다** —
  둘이 어긋난 채로는 `ruff check`의 초록불이 계속 거짓입니다.
- **문서 3개를 `add-response-cache` 마무리 커밋에 함께 올립니다.** 세 번 연속 같은 모양으로
  빠졌으므로, "구현 커밋에 그 구현을 설명하는 문서가 포함되었는가"를 커밋 전 확인 목록에 넣습니다.
- **`git push -u origin add-response-cache`를 이번 세션에 칩니다.** 34건이 로컬에만 있는 상태는
  회고 세 편 연속 지적됐고, 평가자에게는 캐싱이 없는 리포로 보입니다.
- **`add-multiturn-qa`를 커밋하거나 지웁니다.** 여섯 번째 계수입니다 — 둘 중 무엇이든
  다음 회고에서 이 항목이 사라지는 쪽으로 결정합니다.
- **7장을 닫기 전 Codex 적대적 검토를 한 번 겁니다.** 남은 태스크는 7.7 하나뿐이고,
  이 창에서 사람 지적이 다섯 번 유효했던 것에 비해 형식 검증은 0건이었습니다.
  캐시가 `/qa`에 직접 붙어 있으므로 **틀린 히트는 틀린 답을 냅니다.**

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 08-03 22:58 | `724d2218` | 데모 UI 착수 — 서브에이전트 3갈래(계약 실측·관례 조사·구현 설계) |
| 08-03 23:06 | `8ccc760d` | SSE 스트리밍 마감, `dcd6bd7` 커밋 |
| 08-03 23:43 | `d0c01226` | Retrobot #1 — `command -v`·`/tmp` 벽 |
| 08-04 00:30 | `724d2218` | `f392b24` 스트리밍 Q&A 데모 UI, `2d215cf` 병합 |
| 08-04 00:13 | `724d2218` | `DESIGN.md` 기반 UI 수정, 브라우저로 업로드·삭제 실물 확인 |
| 08-04 01:15 | `e4260f90` | `ff7a5ad` RRF 알고리즘 스펙 정의 |
| 08-04 09:42~17:29 | (로그 결손) | RRF 구현 8커밋 — 토크나이저·어휘 색인·팬아웃·API 배선·BM25·아카이빙 |
| 08-04 20:13 | `45f0bdd9` | 캐시 설계 검토 — Redis 부재/장애 동작, `no_evidence` 무효화 지적 |
| 08-04 20:49 | `75cd4368` | `f07ed41` L1/L2 설계 → `23acd3a` 도메인 → `a466d72` 저장소 |
| 08-04 21:22 | `75cd4368` | `3eda547` scope 스펙(코드 0줄) → `b07c66d` Redis 어댑터 |
| 08-04 21:45 | `75cd4368` | "23건 왜 생략?" → 낡은 테스트 수 정정, `5ea81bc` 캐시 서비스 |
| 08-04 22:18 | `75cd4368` | 모델명 실측 후 하드코딩 거절, `fe4bcfa` `/qa` 배선 |
| 08-04 22:38 | `75cd4368` | `2336861` 무효화 — 핵심 3경로 마지막 칸 |
| 08-04 22:57 | `75cd4368` | `4425f5f` 임계값 계측 + `config.py` 주석 위반 회수 (70/71) |
| 08-04 22:59 | `3f5b5605` | Retrobot #13 — 같은 두 벽, `python3 -c` 단일 호출로 우회 성공 |
