# Retrobot 회고 — 2026-08-04 11:03 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 2 (이번 창 10:09~11:03) |
| 총 프롬프트 | 1 (슬래시 1) — 작업 세션 기준 |
| 에러 발생 | 4 (작업 세션 1 · Retrobot 훅 세션 3) |
| 커밋 | `4d370fc` (20파일 +1537/-75) — src 약 552줄 · 테스트 약 877줄 · 문서 약 183줄 |
| 미push | **5건** (`origin/add-rrf-algorithm` 은 아직 `dfbdb8d`) |
| 도구 | Edit 68 · Bash 46 · Read 29 · Write 7 |
| 검증 | 컨테이너 `749 passed, 2 deselected` · `ruff check .` 통과 · `check_comments.py src` 0건 · 실물 API 기동 검증 |

직전 회고(10:10) 이후의 창입니다. 세션 `d9537656` 은 그 회고를 만든 Retrobot 훅이고,
실제 작업은 `dd40c3ba` 한 세션(10:09~10:59, 50분)입니다.

## 세션별 활동

### 세션 `d9537656` (10:09 ~ 10:15, 6분) — Retrobot 훅

`f731869` 커밋 후 자동 실행되어 `2026-08-04_1010_retro.md` 를 만들고 `c48f1df` 로 커밋했습니다.

**발생한 에러:** 3건 — 전부 샌드박스 거부입니다.

- `command -v python3 jq` 승인 요구 (2회), PowerShell "script block" 거부

집계 도구 탐색에서 다시 막혔고, `retrobot/SKILL.md` 의 내장 도구 경로로 넘어가 완료했습니다.

### 세션 `dd40c3ba` (10:09 ~ 10:59, 50분) — 브랜치 `add-rrf-algorithm`

**프롬프트:**

1. `/opsx:apply add-rrf-algorithm` — "어휘 색인 어댑터 및 색인 서명과 수집 통합 까지 작업 진행해주세요." (10:09)

**발생한 에러:** 1건.

- `Edit` 의 `String to replace not found` 1회 (`# 문서 수집` 주석 블록) — 즉시 정정

**흐름:** `add-rrf-algorithm-spec` 의 3장·4장을 구현해 진행률을 **9/55 → 24/55** 로 올렸습니다.

3장은 `adapters/protocols.py` 에 `LexicalIndex` 프로토콜과 `core/retrieval.py` 에
`RetrievedChunk` 값 객체를 얹고, `adapters/lexical/sqlite.py`(353줄)에 FTS5 스키마·쓰기·
bm25 검색·IDF 커버리지 하한·FTS5 가용성 확인을 넣었습니다. `test_lexical_index.py`(455줄)가
lexical-index 스펙 시나리오 전체를 실물 SQLite 로 덮습니다.

4장은 `core/documents.py` 의 색인 서명을 고치고 호출부 3곳(`main.py`·`ingestion_harness`·
`retrieval_harness`)을 맞춘 뒤, `services/ingestion.py` 의 쓰기 순서·되돌리기·삭제·기동 정리를
벡터·어휘 두 색인 모두에 걸었습니다. `config.py` 에 `lexical_index_path`·
`lexical_idf_coverage_floor`, `create_app` 배선, compose 환경변수까지 이어집니다.
`StubLexicalIndex` 와 하네스 배선으로 `test_ingestion_pipeline.py`(+204) ·
`test_documents.py`(+44) 를 확장했습니다.

검증은 컨테이너에서 `docker compose run --build --rm test` → **749 passed, 2 deselected
(74.92s)**, 재실행 77.81s, `ruff check .` → All checks passed, `check_comments.py src` 0건,
`openspec validate` 통과, 이미지 안 FTS5 가용성 실측 `1`. 마지막으로 `./data` 를 비우고
`docker compose up -d --build --wait` 로 실물 API 를 띄워 샘플 문서 2건 업로드 → 검색 →
삭제가 두 색인에 모두 닿는 것까지 확인하고 스택을 내렸습니다.

## KPT 회고

### Keep (좋았던 것)

- **스펙의 공식이 실측에서 깨지는 것을 잡고, 고친 값을 다시 실측으로 받았습니다.**
  계획대로 "질의 토큰 전체에 대한 커버리지 비율"을 쓰면 `교육비는 얼마인가요` 가 **0.244**
  로 하한 0.3 에 걸려 탈락하고, 반대로 흔한 토큰 하나짜리 `승인` 은 **1.0** 으로 통과합니다.
  둘 다 스펙이 요구하는 것과 반대입니다. 비율식이 단일 토큰 질의를 원리적으로 막을 수 없다는
  것을 확인하고 희소성(rarity) 규칙으로 바꾼 뒤, 회귀 질의 4건과 스펙 시나리오가 하한 `0.3`
  에서 모두 통과하는 것을 다시 재서 기본값을 근거와 함께 확정했습니다(경계 df ≈ 10청크 중 4).
  구현이 스펙을 그대로 옮기는 대신 **스펙을 반증하고 고친** 사례입니다.
- **직전 회고의 Try 3·4 를 그대로 이행했습니다.** change 이름을 브랜치명으로 넣지 않고
  `openspec list` 로 먼저 확인해 두 번째 발화("The change is named `add-rrf-algorithm-spec`.
  Using that.")에서 정정했습니다 — 직전 창에서 같은 인자로 두 번 실패했던 지점입니다.
  컨테이너 실행도 전부 `--build` 가 붙었습니다.
- **테스트가 코드보다 많고, 실물 계층을 덮습니다.** 이번 커밋은 src 약 552줄 대 테스트 약
  877줄입니다. `test_lexical_index.py` 는 스텁이 아니라 실물 SQLite 를 씁니다. 전체는
  700 → **749 passed** 로 49건 늘었습니다.
- **통과한 테스트에서 멈추지 않고 실물 기동까지 갔습니다.** `./data` 를 비운 상태에서
  API 를 띄워 업로드·검색·삭제를 실제 요청으로 확인했습니다. `CLAUDE.md` 가 "`./data` 가
  이전 실행의 벡터를 들고 있으면 조용히 0건이 된다"고 경고한 함정을 선제적으로 피한 실행입니다.
- **50분·프롬프트 1건에 에러 1건.** 중단 없이 3장과 4장을 닫았습니다.

### Problem (나빴던 것)

- **`LLM 응답 캐싱` 은 여전히 코드에 없습니다 — 불합격 기준 항목입니다.**
  `services/qa.py` 의 `cache` 문자열 **0회**, `adapters/cache/` 는 아직 `probe.py` 하나.
  **네 회고 연속** 같은 항목이 Problem 에 남았습니다. 직전 회고 Try **2번**이 "남은 시간을
  캐싱 최소 구현에 쓴다" 였고, 이번 창 50분은 전부 RRF 하이브리드 검색에 갔습니다.
  하이브리드 검색은 과제 요구사항 밖 확장이고, 캐싱은 요구사항 3번이자 평가 비중 **18%** 입니다.
- **push 를 또 하지 않았고, 미push 가 3건에서 5건으로 늘었습니다.** 직전 두 회고의 Try
  **1번**이 "다른 무엇보다 먼저 `git push`" 였습니다. 원격은 `dfbdb8d`(02:08)에 멈춰 있고,
  168시간 창은 **오늘 15:11 KST**(첫 커밋 `1313b0a` 07-28 15:11 기준)에 닫힙니다.
  11:03 기준 약 **4시간**입니다. 로컬에만 있는 커밋은 평가자가 볼 수 없고, 창이 닫히면
  그 뒤의 push 로도 되살릴 수 없습니다.
- **작업 시간이 요구사항이 아닌 확장으로 계속 흐릅니다.** 진행률 24/55 는 `add-rrf-algorithm-spec`
  안에서의 숫자입니다. 이 change 를 끝까지 밀면 나머지 31개 태스크가 남고, 그 시간에 캐싱은
  시작조차 못 합니다. 남은 4시간을 어디에 쓸지는 이제 선택이 아니라 마감 문제입니다.
- **열린 change 는 그대로 셋입니다** (`add-answer-generation` 86/87 · `add-demo-ui` ·
  `add-rrf-algorithm-spec` 24/55). 86/87 짜리가 한 태스크 남기고 서 있는 채로 새 change 가
  깊어졌습니다.
- **회고 훅이 매 회차 같은 승인 거부에 부딪힙니다.** `command -v python3 jq` 승인 요구가
  이번에도 2회입니다. `retrobot/SKILL.md` 는 "먼저 이 한 줄을 쳐라"고 지시하는데 그 한 줄
  자체가 이 샌드박스에서 거부됩니다. 지시와 환경이 어긋난 채로 방치돼 있습니다.
- **외부 검토 0회, 열한 세션 연속.**

### Try (개선해야 할 것)

1. **지금 `git push` 합니다.** 세 회고 연속 1번 자리에 같은 문장입니다. 5건이 로컬에만 있고
   창은 약 4시간 뒤 닫힙니다. 다음 편집·다음 태스크보다 먼저입니다.
2. **`add-rrf-algorithm-spec` 구현을 여기서 멈추고 남은 시간을 캐싱에 씁니다.** 3장·4장까지가
   커밋됐으니 중단점으로 깨끗합니다. 5장 이후는 "미구현 부분은 설계 문서로 대체 가능" 규정에
   기댑니다. 캐싱 최소 구현은 질문 정규화 키 → `adapters/cache/` 실제 저장소 어댑터 →
   `services/qa.py` 조회·기록 → 응답의 캐시 히트 필드 → 문서 변경 시 무효화, 다섯 단계입니다.
3. **마감 전 마지막 검증은 `docker compose up -d --build --wait` 로 시작합니다.** 이번 창에서
   실제로 밟은 경로이므로 재현 비용이 낮습니다. 평가자가 1회 수행하는 것과 같은 경로를
   같은 방식으로 밟아 둡니다.
4. **`retrobot/SKILL.md` 의 "먼저 `command -v python3 jq`" 지시를 이 호스트 기준으로 고칩니다.**
   이 샌드박스에서 그 한 줄은 승인 거부로 끝납니다. 첫 단계를 내장 도구(`Glob`·`Grep`) 집계로
   바꾸고, 셸 탐색은 선택지에서 내립니다. 매 회차 같은 2회를 태우고 있습니다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 (KST) | 세션 | 주요 활동 |
|------|------|----------|
| 10:09~10:15 | `d9537656` | Retrobot 훅 — 10:10 회고 생성, 샌드박스 거부 3건 우회, `c48f1df` 커밋 |
| 10:09 | `dd40c3ba` | `/opsx:apply add-rrf-algorithm` — "어휘 색인 어댑터·색인 서명·수집 통합까지" |
| 10:09~10:12 | `dd40c3ba` | `openspec list` 로 change 이름 `-spec` 확인 후 아티팩트·구현 파일 정독 (Read 29) |
| 10:13 | `dd40c3ba` | 구현 계획 수립, `check_comments.py` 로 `src/` 사전 확인 (위반 0건) |
| 10:14~10:20 | `dd40c3ba` | `LexicalIndex` 프로토콜 · `RetrievedChunk` · `adapters/lexical/sqlite.py`(353줄) 작성 |
| 10:21 | `dd40c3ba` | `교육비는 얼마인가요` 가 커버리지 하한에서 탈락 — 실물 코퍼스로 재측정 |
| 10:22~10:25 | `dd40c3ba` | 비율식 결함 규명(0.244 탈락 / `승인` 1.0 통과) → 희소성 규칙으로 전환, 회귀 4건 + 스펙 시나리오 재측정해 하한 `0.3` 확정 |
| 10:26~10:28 | `dd40c3ba` | `test_lexical_index.py`(455줄) — 로컬 **33 passed** |
| 10:29~10:38 | `dd40c3ba` | 4장 — `documents.py` 색인 서명, 호출부 3곳, `ingestion.py` 쓰기·되돌리기·삭제·기동 정리, `config`·`main.py` 배선 |
| 10:39~10:44 | `dd40c3ba` | `StubLexicalIndex` · conftest · 하네스 · `test_documents.py` · `test_ingestion_pipeline.py` 확장 |
| 10:45 | `dd40c3ba` | 로컬 사전 실행 — `anyio` 설치 후 **115 passed** |
| 10:46~10:50 | `dd40c3ba` | `ARCHITECTURE.md` 어휘 색인 절(+110) · `README.md` 설정·재업로드 안내(+15) |
| 10:48~10:51 | `dd40c3ba` | 컨테이너 **749 passed, 2 deselected (74.92s)** |
| 10:52~10:54 | `dd40c3ba` | `ruff check .` All checks passed · `check_comments.py src` 0건 · `openspec validate` 통과 · FTS5 실측 `1` |
| 10:55~10:58 | `dd40c3ba` | `./data` 비우고 `up -d --build --wait` → 샘플 2건 업로드 · 검색 · 삭제가 두 색인에 도달 확인 후 스택 내림 |
| 10:59 | — | `4d370fc` 커밋 (20파일 +1537/-75), 24/55 태스크. push 안 함 (누적 5건) |
