# Retrobot 회고 — 2026-08-04 09:43 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 1 (이번 창 09:13~09:43) |
| 총 프롬프트 | 4 (슬래시 1 + 직접 입력 3) |
| 에러 발생 | **0** |
| 커밋 | `35b9c3a` (6파일 +101/-30) — 전부 `openspec/`, 코드 0줄 |
| 미push | `35b9c3a` 1건 (`origin/add-rrf-algorithm` 보다 1 앞섬) |
| 도구 | Edit 38 · Read 9 · PowerShell 7 · Grep 5 · Glob 2 |

직전 회고(01:18) 이후 01:18~09:13 사이 로그 활동은 없습니다. 이번 창은 세션 하나입니다.

## 세션별 활동

### 세션 `548929a4` (09:13 ~ 09:43, 30분) — 브랜치 `add-rrf-algorithm`

**프롬프트:**

1. `/superpowers:brainstorming rank-fusion 스펙에 모슨이 없는지 확인해주세요.` (09:13)
2. `1번 진행해주세요.` (09:22)
3. 정규화 분모 규칙을 문장으로 지정 — "빈 목록도 분모에 포함(MUST) / 전달되지 않은 목록은 제외(MUST NOT)" + 시나리오 2개 (09:30)
4. 분모 문장 재지정 — "각 입력 목록에 대한 `가중치/(상수+1)`의 **합**, 원점수와 같은 항 형태·같은 누적 순서, 비트 단위 동일(MUST)" + "FTS5 `bm25()`는 음수를 반환하는데 스펙은 점수 0 이상(MUST)" 지적 (09:38)

**발생한 에러:** 없음.

**흐름:** `add-rrf-algorithm-spec` 의 7개 아티팩트를 서로 대조해 **모순 4건 + 스코프·표기 3건 + 인접 change 파급 3건**을 뽑았고(09:19, 11,329 출력 토큰 한 턴), 사용자가 ①(문언·배치 먼저, 분모는 논의로 남김)을 택해 6건을 고쳤습니다.

찾아낸 진짜 모순:

- **Req 1과 Req 3의 융합 점수 정의가 둘** — `Σ w/(c+rank)` 합(상수 60·가중치 1·1이면 `2/61`)과 "모든 목록 1위 = 정확히 1"이 동시에 참일 수 없음 → Req 1의 합을 "정규화 **전** 원점수"로 명시
- **정규화 분모가 스펙에 없고 design과 갈림** — 스펙은 "모든 입력 목록", design은 "활성 retriever 전체". 선택 retriever 실패 시 `1.0` 대 `0.5`로 답이 갈림
- **"입력 목록 길이 ≥ K (MUST)" 가 바로 아래 시나리오(항목 2개, K=5 정상)와 충돌** → 하한이 융합 **앞**에 걸리므로 제약을 뒤집음
- **design 결정 7의 "하한 적용"이 `retrieval` 의 "융합 뒤 하한 MUST NOT"을 어김** → 문장에서 제거

문언·배치:

- 기동 검증이 순수 함수인 `rank-fusion` 에 있었음(저장소·임베더에 닿지 않는다고 스스로 못 박은 capability) → `retrieval` 로 이관
- 검증 기준이 K **기본값**이라 `top_k=상한` 요청이 런타임에 뚫림 → 상한 기준으로 교체
- `K` 가 RRF 상수 60 · 응답 `top_k` · 후보 깊이 셋을 가리킴 → `RRF_K` / `top_k` / `candidate_depth` 로 분리, 캐시 키를 이름순 정렬한 `(retriever, 깊이)` 쌍으로 통일

`openspec validate --strict` 5회 전부 통과, 마지막 확인 후 `35b9c3a` 로 커밋.

## KPT 회고

### Keep (좋았던 것)

- **스펙을 코드로 옮기기 전에 스펙끼리 대조한 것.** 4건은 전부 "구현하면 둘 중 하나는 반드시 깨지는" 종류였습니다. `Σ w/(c+rank)` 가 `2/61` 인데 "1위는 정확히 1" 이라는 두 MUST는 코드를 쓰는 순간 테스트 두 개가 서로를 부정합니다. 09:13의 질문 한 줄이 그 비용을 구현 전으로 당겼습니다.
- **범위를 넘을 때 넘었다고 먼저 말한 것.** 새 분모가 `Σw/(상수+1)` 이라 가중치 합 0이면 0으로 나누기가 되므로 "가중치는 양수(SHALL)"를 ① 범위 밖인데도 넣었고, 그 사실과 되돌리는 방법("그 한 문장만 빼면 됩니다")을 같이 보고했습니다. 승인 없이 조용히 늘리는 것과 다릅니다.
- **에러 0건, 셸 혼용 0건.** PowerShell 7회 전부 성공했고 Bash와 섞지 않았습니다. 8/3에 세 세션 연속으로 났던 셸 혼용 에러가 이번 창에 없습니다.
- **고칠 때마다 `validate --strict` 를 다시 돌린 것** (5회). 마지막 한 번만 도는 대신 중간 상태를 계속 확인해, 델타 형식이 깨진 채로 다음 편집이 쌓이지 않았습니다.

### Problem (나빴던 것)

- **168시간 창이 오늘 15:11 KST에 닫히는데, 이번 창도 코드 0줄입니다.** 첫 커밋이 `1313b0a` 2026-07-28 15:11이므로 판정 대상 커밋은 오늘 15:11까지입니다 — 이 세션이 끝난 09:43 기준 **약 5시간 28분**. 회고에 "코드 0줄"이 적힌 것은 01:18에 이어 **두 창 연속**입니다.
- **`LLM 응답 캐싱`이 코드에 없습니다 — 불합격 기준 항목입니다.** `src/app/adapters/cache/` 에는 `probe.py`(51줄, 연결성 헬스체크) 하나뿐이고 `services/qa.py` 에는 `cache` 문자열이 **0회** 나옵니다. `openspec/specs/` 에도 `openspec/changes/` 에도 해당 capability가 없습니다. 평가 비중 18%이고 "벡터 검색 또는 캐시 DB 미구현"은 감점이 아니라 불합격입니다. 01:18 회고가 같은 것을 적었고 그 뒤 8시간 동안 이 항목에 쓴 시간은 0입니다.
- **모순 탐지가 두 가지를 놓쳤고, 둘 다 사용자가 잡았습니다.**
  - **부동소수점 비트 동일성** — 09:30에 "분모 = 가중치 합 ÷ (상수+1)"로 반영했지만, 09:38에 사용자가 "**원점수와 같은 항 형태·같은 누적 순서**"로 다시 지정했습니다. 값은 같아도 `fl(1/61)+fl(2/61) ≠ fl(3/61)` 이라 "정확히 1.0"이 깨집니다. "정확히"라고 쓴 MUST를 검토하면서 그것이 부동소수점 계약이라는 것을 읽지 못했습니다.
  - **`bm25()` 부호** — FTS5는 음수를 반환하는데 `lexical-index` 스펙은 "점수는 0 이상(MUST)", `tasks 3.3`에 부호 변환이 없었습니다. 그대로 두면 스펙 위반이면서 정렬도 반전됩니다. **이 요구사항에는 MUST만 있고 시나리오가 없어** 잘못 구현해도 테스트가 침묵할 구조였는데, 시나리오 부재를 훑는 검토 항목이 없었습니다.
  - 같은 모양이 8/3 15:15·01:37 회고에도 적혀 있습니다 — **발견이 절차가 아니라 사용자 지적에서 나옴**. 세 회차째입니다.
- **커밋을 push하지 않았습니다.** `origin/add-rrf-algorithm` 은 `dfbdb8d`(01:15)에 멈춰 있고 `35b9c3a` 는 로컬에만 있습니다. `CLAUDE.md` 는 "작업한 날은 하루 1회 이상 커밋·**push**"라고 적었고, 창이 오늘 닫히므로 push 누락은 이월할 여유가 없습니다.
- **검증 명령 실행 0회.** `docker compose` · `pytest` · `ruff` · `check_comments.py` 가 세션 로그에 명령으로 등장하지 않습니다(전부 스펙·태스크 **문서 본문** 속 문자열). 편집이 `.md` 뿐이면 파이썬 게이트가 통째로 건너뛰어지는 구조가 01:18·00:30에 이어 **세 창 연속**입니다. `ruff format --check .` 6 files는 여덟 번째 이월입니다.
- **열린 change가 이 브랜치에만 셋** (`add-answer-generation` · `add-demo-ui` · `add-rrf-algorithm-spec`). 어느 것도 닫히지 않았고, `add-rrf-algorithm-spec` 은 이번 창에도 스펙만 더 정교해졌습니다 — 하이브리드 검색은 과제 요구사항 밖 확장입니다.
- **외부 검토 0회, 아홉 세션 연속.**

### Try (개선해야 할 것)

1. **지금 즉시 `git push`.** 창이 15:11에 닫히고 `35b9c3a` 는 로컬에만 있습니다. 다른 무엇보다 먼저.
2. **남은 시간을 `LLM 응답 캐싱` 코드에 씁니다.** 스펙을 새로 쓰지 말고 최소 구현으로 갑니다 — 질문 정규화 키 + `adapters/cache/` 에 실제 저장소 어댑터 + `services/qa.py` 에 조회·기록 + 응답에 캐시 히트 여부 필드 + 문서 변경 시 무효화. 불합격 기준을 벗어나는 것이 스펙 정교화보다 먼저입니다. `add-rrf-algorithm-spec` 은 지금 상태로 두고 "설계 문서로 대체 가능" 규정에 기댑니다.
3. **`MUST` 에 시나리오가 붙어 있는지를 검토 항목으로 고정합니다.** 이번에 놓친 `bm25()` 부호는 "MUST는 있는데 Scenario가 없는 요구사항"이라 잡히지 않았습니다. 스펙 대조 시 *요구사항 목록과 시나리오 목록을 좌우로 놓고 빈 칸을 먼저 세는* 단계를 넣습니다.
4. **"정확히" · "동일" · "같다"가 수치에 붙으면 부동소수점 계약으로 읽습니다.** 값의 동치가 아니라 **연산 순서**까지 지정해야 하는지 매번 확인합니다. 이번 Req 3이 그 예입니다.
5. **문서만 고친 창에도 `docker compose run --build --rm test` 를 한 번 돌립니다.** `.md` 만 만졌다는 이유로 게이트를 건너뛴 것이 세 창 연속이고, 그 사이 `ruff format` 이월이 여덟 번째가 됐습니다.
6. **change를 새로 열지 않습니다.** 열린 셋 중 `add-answer-generation`(86/87)을 닫는 것 외에는 창이 닫힐 때까지 새 스펙 작업을 하지 않습니다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 (KST) | 세션 | 주요 활동 |
|------|------|----------|
| 09:13 | `548929a4` | `/superpowers:brainstorming` — rank-fusion 스펙 모순 점검 요청 |
| 09:13~09:19 | `548929a4` | 7개 아티팩트 대조 (Read 9 · Grep 5 · Glob 2), `git show --stat ff7a5ad` |
| 09:19 | `548929a4` | 모순 4 + 스코프·표기 3 + 인접 3 보고, 진행 방향 ①②③ 제시 (5분 25초 턴) |
| 09:22 | `548929a4` | 사용자 ① 선택 |
| 09:22~09:27 | `548929a4` | 1·3·4·5·6·7 반영 (Edit 16), `validate --strict` 통과, 2번은 미결로 남김 |
| 09:30 | `548929a4` | 사용자가 분모 규칙 문장·시나리오 2개 지정 (빈 목록 포함 / 미전달 제외) |
| 09:30~09:33 | `548929a4` | Req 3 신설 + `retrieval` 실패 요구사항 + design 결정 4·6 + tasks 4건, 범위 초과 1건 자진 보고 |
| 09:38 | `548929a4` | 사용자가 분모 문장 재지정(항 형태·누적 순서·비트 동일) + `bm25()` 음수 지적 |
| 09:38~09:42 | `548929a4` | Req 3 교체 + 이름표 정렬 누적 MUST + 시나리오 2개, design 결정 1에 `-bm25()` · `ORDER BY ... ASC`, `tasks 3.3`·`3.6`·`lexical-index` 시나리오 추가 |
| 09:42 | — | `35b9c3a` 커밋 (6파일 +101/-30). push 안 함 |
| 09:43 | `548929a4` | 세션 종료 |
