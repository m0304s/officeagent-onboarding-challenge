# Retrobot 회고 — 2026-08-06 23:26 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 10 (최근 24시간 활동분) |
| 총 프롬프트 | 47 |
| 에러 발생 | 36 (이 중 23건이 Retrobot 자체 실행 실패) |
| 주요 도구 | Bash 624 · Edit 230 · Read 72 · Write 49 |
| 커밋 | `170b056`(21:38) · `44df3aa`(23:24) — 그 외는 Retrobot 회고 커밋 |

직전 회고(21:42)가 21:44까지를 덮었으므로, 새로 관측된 실질 작업은 **`fix-retrieval-cache-defects`
한 change의 전 주기(21:05~23:25)** 입니다. 나머지 세션은 이전 회고에서 다룬 구간이라 요약만 싣습니다.

## 세션별 활동

### 세션 `f4deea06` (21:05 ~ 23:25) — 이번 회차의 본체

RRF·시맨틱 캐시 결함 5건을 하나의 change로 묶어 propose → apply(1~8장) → archive까지 2시간 20분에
완주했습니다. 프롬프트 6개로 Bash 241 · Edit 105회를 끌어냈습니다.

**프롬프트:**
1. (21:15) `/opsx:propose bug-fix` — 결함 5건을 한 번에 서술
   - BM25가 희소 토큰 하나로 건진 청크와 코사인 0.85 청크의 융합 점수가 같음. 게이트 비대칭 탓에
     RRF의 원래 의도인 "두 목록 교집합 보너스"가 생기지 않음
   - 시맨틱 캐시가 부정문을 통과시킴 — "연차를 이월할 수 있나요?" / "…없나요?"가 L2 히트
   - `top_k`가 scope에 들어가 있음 → scope에서 빼고 히트 시 `entry.top_k`를 응답에 실을 것
   - L2 후보 집합이 "최신순 200"이라 캐시가 커질수록 안 맞음 → Redis 8 Vector Sets / RediSearch HNSW 검토
   - 질의 임베딩이 요청당 2회 계산됨
2. (21:39) `/opsx:apply` 1장 및 2장
3. (22:01) `/opsx:apply` 3장 및 4장
4. (22:19) `/opsx:apply` 5장 및 6장
5. (22:57) `/opsx:apply` 7장 및 8장
6. (23:24) `/opsx:archive fix-retrieval-cache-defects`

**손댄 파일:** `src/app` 14개(`core/retrieval.py`·`core/cache.py`·`services/cache.py`·
`services/retrieval.py`·`adapters/cache/probe.py`·`adapters/vector_store/chroma.py` 등),
테스트 11개, 문서 3개(`ARCHITECTURE.md`·`README.md`·`docker-compose.yml`), 스펙 6개.

**발생한 에러:** 3건
- `(eval):cd:1: no such file or directory: openspec/specs` — 이전 `cd`가 남긴 작업 디렉토리 오해
- `jq: parse error: Invalid numeric literal at line 1, column 5` — `/qa` 응답(SSE)을 그대로 `jq`에 물림
- 인라인 `python3` 프로브의 `AssertionError` (설계 가정 확인 중 의도된 실패)

### 세션 `8f673272` (10:43 ~ 13:07)

`add-pymupdf4llm-parser` 2~8장 + 아카이브. OCR을 넣었다가(RapidOCR + Tesseract 하이브리드)
"텍스트 레이아웃만" → "OCR 전면 제거, `to_markdown()`만"으로 두 번 되돌린 구간입니다. 프롬프트 20개.

### 세션 `f13eeb41` (00:07 ~ 00:49) · `c69d384c` (23:25 ~ 00:07)

pymupdf4llm 어댑터 설계 브레인스토밍, 버전 핀 `0.3.4` → `1.28.0` 정정, `to_markdown` 파라미터
공식 문서 확인. 사용자 인터럽트 4회 · 도구 거부 4건.

### 세션 `bc5f4779` (13:07 ~ 13:18)

demo-ui 인용 청크 클릭 시 근거 노출. 선택지 제시 후 사용자가 C안 선택.

### 세션 `7e429c14`·`e1686297`·`e76cada6`·`0e8a88f5`·`a60a7617` — Retrobot 자체 실행

5회 실행, 에러 23건. 내용은 아래 Problem에서 다룹니다.

## KPT 회고

### Keep (좋았던 것)

- **설계 전에 런타임 능력을 실측했습니다.** Redis 8 Vector Sets를 쓰기로 정하기 전에
  `redis-cli COMMAND INFO VADD`로 서버 지원을, 컨테이너 안에서 `hasattr(redis.Redis, 'vadd')`와
  async 클라이언트의 `vset` 접근으로 클라이언트 지원을 각각 확인했습니다(21:40~21:42, 4회).
  "문서에 있으니 되겠지"로 진행했다면 8장에서 되돌아왔을 경로입니다.
- **`--build` 함정을 이번엔 처음부터 피했습니다.** 세션 내 `docker compose run` 20여 회 중
  코드 수정 직후의 재검증은 전부 `--build`가 붙어 있었고, 실물 확인용 `docker compose up`도
  `-d --build --wait`였습니다. CLAUDE.md에 적어둔 항목이 실제로 회수됐습니다.
- **린트를 호스트 `.venv/bin/ruff`로 먼저 치고 컨테이너로 확정했습니다.** 컨테이너 빌드 없이
  초를 재는 사전 점검이 12회, 그 뒤 컨테이너 전체 실행. 피드백 루프를 두 단계로 쪼갠 덕에
  ruff 위반이 테스트 실행까지 흘러가지 않았습니다.
- **아카이브 전에 실물 `/qa`를 돌려 확인했습니다.** 기존 문서를 지우고 `company-policy.txt`를
  다시 올린 뒤(23:15), 밀집 검색이 0건인 상태 → `api` 재기동 → 보정 후 검색으로
  기동 시 backfill이 실제로 동작하는지 눈으로 봤습니다(23:18). 스펙에 적은 동작과 실행 결과를
  같은 세션 안에서 맞춰본 셈입니다.
- **결함 5건을 쪼개지 않고 한 change로 묶은 판단이 맞았습니다.** RRF 게이트·캐시 scope·L2 후보
  집합·임베딩 중복이 전부 `services/retrieval.py`와 `services/cache.py` 위에서 만나기 때문에,
  나눴다면 같은 파일을 네 번 열고 네 번 충돌시켰을 것입니다.

### Problem (나빴던 것)

- **Retrobot이 오늘만 5번 돌았고, 5번 다 같은 순서로 막혔습니다.** 26회차인 이번 실행도
  `command -v python3 jq`(복합 명령 거부) → `/tmp` Write 도구 거부 → `>` 리다이렉트 차단 →
  heredoc stdin 성공, 정확히 네 단계였습니다. 창 전체 에러 36건 중 23건(64%)이 여기서 나옵니다.
  `SKILL.md`는 이 우회로를 이미 알고 있는데도 "먼저 `command -v python3 jq`를 쳐라"고
  지시하고 있어서, 스킬 본문이 매 회차 첫 번째 실패를 만들어내고 있습니다.
- **`/qa`는 SSE인데 `jq`를 붙였습니다.** 22시대 실물 확인에서 `jq: parse error` 2연발.
  스트리밍 응답을 JSON으로 취급하는 실수는 이 프로젝트 구조상 반복될 수밖에 없는 형태입니다.
- **`cd`가 남긴 작업 디렉토리를 다음 명령이 물려받는다고 착각했습니다.** 21:20에 `cd openspec/specs`,
  21:16에 `ls openspec/specs/` — 두 호출 사이에 셸이 유지되지 않아 `no such file or directory`.
- **`add-multiturn-qa`가 71시간째 미추적입니다.** 08-04 00:37 생성, 지금까지 `??` 상태.
  직전 회고에서 "열두 번째 계수(69시간)"로 셌던 항목이 두 시간 더 늘었습니다.
- **브랜치가 `debugging`이고 업스트림이 없습니다.** change 단위로 이름 붙인 다른 11개 브랜치와
  규칙이 어긋나며, `44df3aa`를 포함해 이 브랜치 커밋은 아직 원격에 없습니다.
- **`add-pymupdf4llm-parser`에서 OCR을 넣었다 뺐습니다.** 11:19 "OCR 통합 진행" → 11:48
  "pymupdf4llm에만" → 12:06 "텍스트 레이아웃만" → 12:09 "OCR 모두 제거". 세 번 좁히다 지웠고,
  그 사이 4장 작업이 두 번 수행됐습니다.

### Try (개선해야 할 것)

- **`retrobot/SKILL.md`의 "먼저 `command -v python3 jq`를 쳐라"를 호스트별 분기로 바꿉니다.**
  macOS에서는 `python3`가 항상 있으므로 탐지를 건너뛰고 곧장
  `python3 - <파일들> <<'PY' … PY`(stdin heredoc, 파일 생성 없음)로 들어가게 하고,
  `/tmp`에 스크립트를 쓰라는 문장은 삭제합니다. 이 한 번의 수정이 회차당 4번의 실패를 없앱니다.
- **`SKILL.md`의 "하지 말 것" 목록에 두 줄을 추가합니다** — `command -v A B` 형태의 복합 명령
  (승인 규칙에 걸림), `>`·`>>`로 작업 디렉토리 밖에 쓰기(샌드박스가 차단).
- **`/qa` 확인용 셸 함수를 `README.md`나 `scripts/`에 한 줄로 박아둡니다.** SSE를 `curl -N`으로 받아
  `data:` 라인만 뽑은 뒤 `jq`에 넣는 형태로. 매 세션 즉석에서 다시 만드느라 두 번 깨졌습니다.
- **한 Bash 호출 안에서 `cd`의 효과가 끝난다고 전제합니다.** 디렉토리 이동이 필요하면
  `cd X && …`로 한 호출에 묶거나 절대 경로를 씁니다.
- **다음 커밋 전에 `add-multiturn-qa`를 처리합니다.** 살릴 거면 `git add`, 아니면 삭제.
  71시간 동안 "나중에"였던 항목이므로 판단을 미루는 것 자체가 결정이 되고 있습니다.
- **브랜치를 `fix-retrieval-cache-defects`로 개명하고 push합니다.** 커밋 2건뿐인 지금이
  가장 싼 시점이고, 168시간 창 안에 산출물이 원격에 남아야 평가 대상이 됩니다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 23:25~00:07 | `c69d384c` | pymupdf4llm 어댑터 설계 검토, 버전 핀 0.3.4 → 1.28.0 정정 |
| 00:07~00:49 | `f13eeb41` | 1~2장 apply, PDF 내 이미지 처리 브레인스토밍, `to_markdown` 파라미터 문서 확인 |
| 00:07 / 00:17 | `e1686297`·`e76cada6` | Retrobot 실행 — 각 4·5건 에러 |
| 10:43~13:07 | `8f673272` | `add-pymupdf4llm-parser` 2~8장 + 아카이브, OCR 도입 후 전면 철회 |
| 13:05 / 13:18 | `0e8a88f5`·`7e429c14` | Retrobot 실행 — 각 5·4건 에러 |
| 13:07~13:18 | `bc5f4779` | demo-ui 인용 청크 클릭 시 근거 노출(C안) |
| 21:05~21:38 | `f4deea06` | `fix-retrieval-cache-defects` propose — 결함 5건 스펙화, `170b056` |
| 21:38~21:44 | `a60a7617` | Retrobot 실행 — 5건 에러 |
| 21:39~22:19 | `f4deea06` | 1~4장 apply — RRF 게이트 대칭화, Redis 8 Vector Sets 실측 |
| 22:19~23:12 | `f4deea06` | 5~8장 apply — 캐시 scope에서 `top_k` 제거, 임베딩 중복 제거, 문서 갱신 |
| 23:12~23:19 | `f4deea06` | 실물 검증 — 문서 재업로드, `api` 재기동 전후 검색 비교로 backfill 확인 |
| 23:24~23:25 | `f4deea06` | archive, `44df3aa` 커밋 |
