# Retrobot 회고 — 2026-08-06 23:33 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 11 (지정된 로그, 최근 24시간 활동분) |
| 총 프롬프트 | 41 |
| 에러 발생 | 42 (이 중 29건이 Retrobot 자체 실행 실패) |
| 주요 도구 | Bash 646 · Edit 223 · Read 73 · Write 51 · Grep 31 |
| 커밋 | `3b6f921`(23:31 아카이빙) · `1921214`(23:32 `debugging` → `main` 병합) |

직전 회고(23:26)가 `fix-retrieval-cache-defects` 본체를 덮었으므로, 새로 관측된 구간은 **7분**입니다.
그 7분에 두 가지가 있었습니다 — 아카이빙이 델타를 메인 스펙에 먼저 동기화하고 실행됐고, 그 직후
`debugging`이 `main`에 병합·푸시됐습니다. 나머지 세션은 앞선 회고에서 다룬 구간이라 요약만 싣습니다.

## 세션별 활동

### 증분 (23:26 ~ 23:33)

**`3b6f921` "fix-retrieval-cache-defects 스펙 아카이빙"** — 12파일 `+150/-21`. 이동된 8개 아티팩트는
전부 `0` 변화이고, 실제 증분은 메인 스펙 4개에 있습니다: `retrieval` `+68`, `response-cache` `+61`,
`lexical-index` `+25`, `document-ingestion` `+17`. **델타가 메인으로 옮겨간 뒤에 change 디렉터리가
움직였다**는 뜻입니다. 08-04 23:33 회고가 특정한 손실 경로(동기화 없이 archive하면 델타가 아카이브
안으로 사라짐)를 이번엔 밟지 않았습니다.

**`1921214` "fix: retrieval-cache-defects"** — 병합 커밋(부모 `5c49e34` + `3b6f921`), `origin/main`에
푸시 완료. 이 커밋으로 `ARCHITECTURE.md` `+124`, `src/app` 캐시·검색 어댑터, 회고 3편
(`1319`·`2142`·`2326`), `TIMELINE.md` 3행이 한꺼번에 `main`에 들어갔습니다.

**발생한 에러:** 0건 (증분 구간)

### 세션 `f4deea06` (21:05 ~ 23:30)

`fix-retrieval-cache-defects` propose → apply 1~8장 → archive. 프롬프트 6개로 Bash 253 · Edit 106.
23:26 회고에서 상세히 다뤘습니다. 에러 3건.

### 세션 `8f673272` (10:43 ~ 13:07) · `f13eeb41` (00:07 ~ 00:49) · `c69d384c` (23:32 ~ 00:07)

`add-pymupdf4llm-parser` 스펙 → 구현 → 아카이브. OCR을 넣었다 세 번 좁힌 뒤 전면 철회
("RapidOCR만" → "텍스트 레이아웃만" → "OCR 모두 제거, `to_markdown()`만"). 13:10 회고 참조.

### 세션 `bc5f4779` (13:07 ~ 13:18)

demo-ui 근거 청크 클릭 시 본문 노출. 프롬프트 2개(선택지 중 C안). 13:19 회고 참조.

### Retrobot 훅 세션 6개 (`e1686297`·`e76cada6`·`0e8a88f5`·`7e429c14`·`a60a7617`·`13315412`)

전부 회고 생성 실행. **창 전체 에러 42건 중 29건(69%)이 여기서 나왔습니다.**

## KPT 회고

### Keep (좋았던 것)

- **아카이빙이 동기화를 먼저 했습니다.** `3b6f921`의 `+150/-21`이 전부 `openspec/specs/` 아래에 있고
  이동된 아티팩트는 `0` 변화입니다. `/opsx:archive`는 되돌릴 수 없는 이동이라 순서가 뒤집혔으면
  `response-cache` 61줄을 포함한 150줄이 아카이브 안에서만 살아남았고, 손실은 커밋 이후에야
  드러났을 것입니다. 08-04 23:33에 사람이 물어서 알아낸 성질이 이번엔 절차로 굳었습니다.
- **`debugging` 브랜치가 7분 만에 `main`으로 들어갔습니다.** 23:26 회고가 지적한 두 항목
  (`업스트림 없음`·`미푸시 2건`)이 `1921214` 하나로 닫혔고 `origin/debugging`도 생겼습니다.
  08-04에 다섯 회차 이월됐던 "작업이 원격에 없어 평가자 눈에는 미착수" 패턴이 이번 change에는
  재발 0입니다.
- **주석 게이트가 114파일 위반 0건**으로 유지됩니다. 창 안에서 `src/app` 14개 파일과 테스트 11개가
  바뀌었는데도 새 위반이 들어오지 않았습니다.
- **이번 Retrobot은 금지 항목으로 우회하지 않았습니다.** `/tmp` Write가 거부된 뒤 heredoc을 쓰지 않고
  `python3 -c` 단일 호출 4번으로 12개 로그(5,000+ 엔트리)를 전량 집계했습니다.

### Problem (나빴던 것)

- **`ruff format --check`가 13건 → 17건으로 늘었습니다.** 열세 번째 재현이자 두 회차 연속 증가
  (`16 → 13 → 17`). 같은 순간 `check_comments.py`는 위반 0건이라, **게이트 하나가 초록인 것이
  포맷이 맞다는 뜻으로 읽힙니다.** 닫는 비용은 `.venv/bin/ruff format src tests` 한 줄입니다.
- **`openspec/changes/add-multiturn-qa/` 4아티팩트가 여전히 untracked — 열네 번째 계수입니다.**
  파일 mtime이 `08-04 00:47`이니 **71시간**째이고, 그 사이 change 셋(`add-cross-encoder`,
  `add-pymupdf4llm-parser`, `fix-retrieval-cache-defects`)이 제안부터 아카이브까지 지나갔습니다.
- **`add-cross-encoder` 15커밋이 아직 `main` 밖입니다** (`git log main..add-cross-encoder` = 15).
  `main`은 방금 `1921214`로 전진했으므로 지금이 격차가 가장 좁은 시점이고, 이 브랜치가 만지는
  리랭킹은 방금 병합된 `services/retrieval.py`와 같은 파일입니다. **`retros/`에 08-05 회고 파일이
  0건**인 것도 같은 뿌리 — 그날 회고 6편이 `add-cross-encoder` 안에만 있어 `main`을 여는 사람에게
  08-05는 없는 하루입니다(다섯 번째 계수).
- **Retrobot 27회차, 창 에러의 69%(29/42).** 훅 6세션이 **전부 같은 순서로** 죽었습니다 —
  `command -v python3 jq`(복합 명령 거부) → 단독 재시도도 거부 → `/tmp` Write 거부. 즉 실패 3건이
  `SKILL.md`의 첫 두 지시를 그대로 따른 결과입니다. `retrobot/SKILL.md`는 `afa9794`(08-03 22:14)
  이후 **72시간 무수정**이고, 회고 열두 편이 같은 진단을 반복했습니다.
- **외부 적대적 검토 0회 — 열여덟 세션 연속.** 이번 창에서 설계가 사람 지적으로 다섯 번 뒤집혔고
  (`add-pymupdf4llm-parser` OCR 3회 · 버전 핀 정정 · 게이트 비대칭), `openspec validate --strict`는
  그중 한 건도 잡지 못했습니다.

### Try (개선해야 할 것)

1. **`retrobot/SKILL.md` 3줄 수정** — ① `command -v python3 jq` 복합 형태를 지우고 `python3 --version`
   단독으로 교체 ② `/tmp` 언급을 세션 스크래치패드(`/private/tmp/claude-501/<프로젝트>/<세션-id>/`)로
   교체하거나, 아예 "스크립트 파일 없이 `python3 -c` 한 번으로 집계"를 기본 경로로 명시
   ③ heredoc 금지에 `(Windows 한정)` 호스트 조건을 붙일 것. 이번 회차가 ②만으로 4회 성공했으므로
   비용은 3줄입니다.
2. **`.venv/bin/ruff format src tests` 실행 후 커밋** — 17건, 10초. 다음 change 착수 전에 닫지 않으면
   열네 번째 재현이 옵니다.
3. **`add-multiturn-qa`를 커밋하거나 지울 것.** 71시간 방치는 "설계했지만 안 했다"와 "쓰다 말았다"를
   구분할 수 없게 만듭니다. 착수 안 할 거면 삭제가 정직합니다.
4. **`add-cross-encoder`를 `main`에 병합** — `main`이 방금 전진해 격차가 가장 좁고, 병합하면 08-05
   회고 6편도 함께 `main` 타임라인에 들어옵니다. 다음 change가 `services/retrieval.py`를 또 만지면
   충돌 비용이 커집니다.
5. **다음 change의 설계 확정 전에 Codex 검토 1회** — 사람이 읽어서 다섯 번 뒤집은 것을 `--strict`는
   0건 잡았습니다. 코드가 붙기 전이 반영 비용이 가장 싼 시점입니다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 08-05 23:32 ~ 00:07 | `c69d384c` | `add-pymupdf4llm-parser` 제안, 어댑터 교체 방식 브레인스토밍, 버전 핀 `0.3.4` → `1.28.0` 정정 |
| 00:07 ~ 00:49 | `f13eeb41` | 1~2장 적용, PDF 이미지 처리 조사, `to_markdown` 파라미터 공식 문서 확인 |
| 10:43 ~ 13:07 | `8f673272` | 2~8장 + 아카이브. OCR 도입 → 세 번 축소 → 전면 철회 |
| 13:07 ~ 13:18 | `bc5f4779` | demo-ui 근거 청크 펼치기 (`56341eb`·`5c49e34`) |
| 21:05 ~ 23:25 | `f4deea06` | `fix-retrieval-cache-defects` 전 주기 (`170b056`·`44df3aa`) |
| 23:31 | `f4deea06` | `3b6f921` 아카이빙 — 메인 스펙 4개 `+150/-21` 선행 동기화 |
| 23:32 | — | `1921214` `debugging` → `main` 병합·푸시 |
