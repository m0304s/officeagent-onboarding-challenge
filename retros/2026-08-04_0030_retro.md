# Retrobot 회고 — 2026-08-04 00:30 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 17 (주 세션 13 + 서브에이전트 4) |
| 총 프롬프트 | 46 |
| 에러 발생 | 51 |

24시간 창(08-03 00:30 ~ 08-04 00:31 KST) 전체 수치입니다. 직전 회고(23:46) 이후의 **새 작업은
`724d2218` 한 세션**이며, 이 회차의 커밋은 `f392b24 feat: 스트리밍 Q&A 데모 UI`
(61파일 **+4,690**) 하나입니다. 에러 51건 중 **35건(69%)이 Retrobot 자신의 7개 회차**에서
나왔습니다.

## 세션별 활동

### 세션 `724d2218` (08-03 22:47 ~ 08-04 00:31) — 이번 회차의 실작업

**프롬프트 13건 · 에러 3건 · 도구 호출 176회**
(`Write` 56 · `Bash` 71 · `Edit` 19 · `Agent` 3 · 브라우저 도구 22 · `Skill` 2 · `TaskStop` 2)

**프롬프트:**
1. `/opsx:propose add-demo-ui` — "질의-응답 결과 확인용 프론트엔드 데모 페이지"
2. POST + SSE라서 `EventSource`를 못 쓰는 이유는?
3. `/opsx:apply add-demo-ui 전체 작업 진행해주세요`
4. 자, 그렇다면 **다른 세션에서 change를 완료하고 나서** 프론트엔드 데모 UI를 붙여보겠습니다
5. (39분 후) 현재 change 완성했습니다(SSE). 이어서 프론트엔드 데모 UI 완성해주세요
6. Claude in Chrome 연결 완료 — 브라우저 도구 사용 가능
7. `add-demo-ui` 완성됐습니다
8. `demo-ui/DESIGN.md` 파일 기반으로 UI 수정해주세요
9. 중단 및 UI를 통한 업로드 및 삭제 작업 진행했습니다
10. 인터럽트 3회

**발생한 에러:**
- `Blocked: sleep 45 followed by: wc -c ...` — 서브에이전트 대기용 복합 명령이 가드에 걸림
- `dev server: 200 / (eval):cd:1: no such file or directory: demo-ui` — 이미 `demo-ui` 안에 있는데 또 `cd demo-ui`
- `Couldn't determine which page this action targets` — 브라우저 탭 컨텍스트 소실

### Retrobot 회차 7건 (`9d06bafd`·`12eec827`·`96832dcb`·`06eb7705`·`6ef28f9b`·`d0c01226`·`31165d64`)

프롬프트 각 1건, **에러 합계 35건**. 일곱 회차가 전부 같은 순서로 실패했습니다 —
`command -v python3 jq` → "multiple operations" 거부 → `/tmp` 스크립트 `Write` 거부 → 우회로 재탐색.

### 나머지 (직전 회고들이 이미 다룬 구간)

`b42efae1`(레이어 감사·리팩터링) · `4f55c9d8`(주석 규칙 도입) · `38dcf8a2`(4장 QA 서비스) ·
`8ccc760d`(5~8장 SSE) · 서브에이전트 4건.

## KPT 회고

### Keep (좋았던 것)

- **사용자의 23:04 중단 판단이 옳았음이 39분 만에 증명됨.** 23:00에 `/opsx:apply`로 서브에이전트
  3개를 띄웠는데, 사용자가 "다른 세션에서 change를 완료하고 나서 붙이겠다"며 세웠습니다
  (`TaskStop` ×2). 그 사이 세션 `8ccc760d`가 `sse.py`와 API 계약 문서 227줄을 고쳤고, 23:43에
  재개했을 때 프론트엔드는 **완성된 백엔드 계약 위에서** 시작했습니다. 직전 회고가 "낡은
  컨텍스트 위에서 쓰인 미수"라고 적은 그 실패가 이번엔 발생하지 않았습니다 — `design.md`·
  `proposal.md` 어디에도 `claude-code-sdk` 참조가 없습니다(실측 0건). **순서 판단이 다시
  에이전트가 아니라 사람에게서 나왔다**는 점은 Problem 쪽에 그대로 남습니다.

- **화면을 실제로 렌더해서 보고 고쳤음.** `npm run build`가 초록인 상태에서 멈추지 않고,
  Claude in Chrome으로 `localhost:5173`을 띄워 스크린샷 → 질문 입력 → 스트리밍 수신 →
  콘솔 메시지 판독까지 22회 돌렸습니다. 그 결과 **빌드로는 절대 잡히지 않는 3건**을 회수했습니다
  (`global.css`·`AnswerStream.module.css`·`format.ts`). 타입 체커는 "레이아웃이 깨졌다"를
  모릅니다.

- **브라우저 이전에 프로토콜을 먼저 실측했음.** 23:58~00:02에 Vite 프록시 너머로 `curl -N`을
  직접 쏴서 `/api/qa` SSE 이벤트를 눈으로 확인하고, `/api/search` 점수를 4개 질의로 재고,
  Chroma 컬렉션의 실제 문서 수까지 확인했습니다. UI가 "답이 안 나온다"고 할 때 그것이 UI
  문제인지 검색 0건인지를 **미리 갈라 놓은** 것입니다 — `CLAUDE.md`가 경고한 `./data` 잔여
  벡터 함정이 바로 이 자리입니다.

- **미완료 태스크 7건을 체크하지 않고, 무엇을 못 봤는지까지 적었음.** 48/55로 커밋하면서
  `4.7`에 "재업로드 무변경·미지원 포맷·빈 상태 3건이 남았다", `5.11`에 "미인증·서버 미도달·
  연결 끊김·진행 중 새 질문 4건이 남았다"고 명시했습니다. 이 리포에서 미검증을 완료로
  적는 것은 감점이 아니라 불합격입니다.

- **계획에 없던 항목을 발견 즉시 태스크로 승격.** `1.5`에 `.dockerignore`에 `demo-ui/`를
  넣는 이유가 "상시 실행하는 `docker compose run --build --rm test`의 빌드 컨텍스트에
  `node_modules`가 딸려간다"로 적혔고, 발견 경위("계획에 없던 항목 — 조사 중 발견")까지
  남았습니다. 커밋 직전 `git diff --cached --name-only | grep -E "node_modules|dist/"`로
  스테이징까지 확인했습니다.

- **`openspec validate --strict` 두 번 실행**(22:56 · 00:28). 3·4장에서 두 회차 연속
  누락됐던 항목이 끊겼습니다.

- **직전 회고 Try #6 이행.** `.gitignore`·`.dockerignore`·아티팩트 4건이 전부 `f392b24`에
  들어갔습니다.

### Problem (나빴던 것)

- **`ruff format --check .`가 이번 세션 0회 — 여섯 번째 이월.** 직전 회고 Try #1이
  "**지금** 돌리고 `README.md:319`를 통과시켜라"였는데, 이 세션이 실행한 검증은
  `npm run build` 4회와 `openspec validate` 2회가 전부입니다. 호스트 실측 결과 지금도
  **6 files would be reformatted**입니다. 이 명령은 평가자가 README를 따라 **1회** 수행합니다.
  demo-ui가 TypeScript라 파이썬 게이트가 심리적으로 "내 범위 밖"이 된 것으로 보이지만,
  같은 커밋이 `README.md`와 `ARCHITECTURE.md`를 고쳤습니다.

- **직전 회고 Try 6건 중 이행 1건(#6)** — #1(ruff format)·#2(`CLAUDE.md:74` 판정 범위)·
  #3(`SKILL.md` 수정)·#4(`4B.1` 결정) 전부 미이행. `4B.1`은 여전히 `[ ]`이고 보류 사유도
  `tasks.md`에 안 적혔습니다. **첫 프롬프트가 `/opsx:propose` 한 줄**이었고, 이 회차에도
  "태스크 번호를 받은 것만 이행된다"는 진단(17:41 이후 세 번째 기록)이 그대로 재현됐습니다.
  `python3 scripts/check_comments.py`는 인자 없이 돌리면 지금도 exit 1(`tests/` 432건)입니다.

- **Retrobot 샌드박스 벽 24번째 — 직전 회고가 정확히 예언한 횟수.** 23:46 회고 Try #3이
  "`retrobot/SKILL.md`를 고쳐라 — 회고에 적는 것으로는 **24번째가 옵니다**"라고 썼고,
  `SKILL.md`가 안 고쳐진 채로 이번 회차 첫 명령 `command -v python3 jq`가 그대로 거부됐습니다.
  런타임이 읽는 파일은 회고가 아니라 `SKILL.md`입니다. 이번 회차에서 **통하는 형태를 새로
  확정**했습니다: `python3 - <<'PY' ... PY` **heredoc이 통합니다**(따옴표 이스케이프 문제가
  없어 `python3 -c`보다 낫습니다). `/tmp`에 파일을 만드는 것은 여전히 막힙니다.

- **demo-ui에 자동화 테스트 0건.** `package.json`의 스크립트는 `dev`·`build`·`preview`뿐이고
  vitest 계열 의존성이 없습니다. `tasks.md` 머리에 "백엔드 핵심 3경로를 건드리지 않으므로
  pytest 태스크가 없다"고 **범위를 명시한 것은 옳지만**, 그것이 정당화하는 것은 pytest 부재이지
  프런트 로직 부재가 아닙니다. `sse.ts`(84줄)와 `useQaStream.ts`는 **SSE 프레임을 손으로
  파싱하는 상태 기계**이고, 백엔드에서 같은 종류의 로직(`VerdictSplitter`)은 "가장 틀리기 쉽다"고
  자평해 출력 12종 × 전 분할로 단언했던 자리입니다. 지금 그 층의 유일한 게이트는 `tsc -b`입니다.

- **서브에이전트 대기에 맹목 `sleep` 175초.** 23:01~23:04에 `sleep 45`·`sleep 40`·`sleep 90`을
  차례로 쳤고, 첫 건은 복합 명령 가드에 막혔습니다. 완료 알림이 오는 구조인데 폴링으로 태운
  것이고, 그렇게 기다린 3개 중 설계 담당(`a7e649b8`)은 23:05에 `TaskStop`으로 버려졌습니다.

- **외부 검토 0회 — 일곱 세션 연속.** 미검토 누적에 이번 4,690줄이 더해졌습니다.
  `4B.1`은 5회차째 `[ ]`입니다.

- **`f392b24`가 `add-demo-ui`에 있고 이 회고는 `add-answer-generation`에 실립니다.**
  훅이 도는 사이 사용자가 브랜치를 되돌렸습니다(reflog `HEAD@{0}`). 유실은 없지만,
  회고가 설명하는 커밋이 회고와 다른 브랜치에 있다는 점은 기록으로 남깁니다.

- **`cd` 중복으로 인한 실패 1건, 그리고 브라우저 탭 컨텍스트 소실 1건.** 둘 다 즉시 회복됐으나,
  `cd demo-ui`는 이미 그 디렉터리 안에서 친 것이라 경로 상태를 안 보고 명령을 조립한 사례입니다.

### Try (개선해야 할 것)

1. **`retrobot/SKILL.md`를 지금 고쳐라 — 25번째를 막을 마지막 기회.** 세 곳:
   (가) "파싱 스크립트가 필요하면 `/tmp` 아래에 쓴다"를 **"스크립트 파일을 만들지 마라"**로,
   (나) `command -v python3 jq` → **`which python3 jq`**(복합 명령 가드에 안 걸림),
   (다) 통하는 형태를 본문에 박아라 — **`python3 - <<'PY' … PY` heredoc**. 이번 회차의
   모든 집계가 이 형태로 통과했습니다. 회고에 적는 것으로는 24회차가 왔습니다.

2. **커밋 전 검증 묶음을 파일 하나로 만들어라.** `ruff check` · `ruff format --check .` ·
   `python3 scripts/check_comments.py src/` · `pytest`를 한 스크립트에 넣고 `README.md:319`가
   그것을 가리키게 하라. 프런트엔드 작업이라 파이썬 게이트를 건너뛰는 일이 이번에 실제로
   일어났고, 여섯 회차 이월된 6개는 `ruff format .` 한 번이면 닫힙니다.

3. **`demo-ui/src/api/sse.ts`의 프레임 파싱에 테스트를 붙여라.** 백엔드 픽스처
   (`tests/fixtures/codex/`)에 실물 SSE 바이트가 이미 있으므로 지어낼 필요가 없습니다.
   최소 3케이스: 이벤트가 청크 경계에서 쪼개진 경우 · `sources`가 첫 이벤트로 오는 경우 ·
   중간에 연결이 끊긴 경우. `tasks.md` 머리의 범위 노트도 "프런트 테스트는 왜 없는가"로
   갱신하라.

4. **`4B.1`을 이번 주 안에 붙일지 지금 결정해 `tasks.md`에 쓰라.** 5회차째 `[ ]`이고
   "보류"라는 판단조차 회고에만 있습니다. 붙일 수 없다면 **`[~] 보류 — 사유**"로 마킹하라.

5. **남은 7태스크의 검증을 사람 손에 넘길지 명시하라.** `4.7`·`5.11`·`6.6`·`6.8`·`6.9`·
   `8.1`·`8.2` 중 브라우저 자동화로 가능한 것(360px 뷰포트·키보드 전 과정·`prefers-reduced-motion`)과
   사람이 밟아야 하는 것(`docker compose down -v` 후 README 처음부터)을 갈라 적으라.
   지금은 둘이 같은 목록에 섞여 있습니다.

6. **서브에이전트를 `sleep`으로 기다리지 마라.** 완료 알림이 오는 구조이므로 대기 명령
   자체가 불필요하고, 이번엔 그중 하나가 가드에 막혀 에러로도 남았습니다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 22:47 | `724d2218` | `/opsx:propose add-demo-ui` — brainstorming으로 실행 방식·범위·프레임워크 3문 |
| 22:51 | `724d2218` | proposal · spec(273줄) · design(212줄) · tasks 4아티팩트, `validate --strict` 통과 |
| 22:57 | `724d2218` | 사용자 질문 — POST + SSE라 `EventSource`를 못 쓰는 이유 |
| 23:00 | `724d2218` | `/opsx:apply` — 서브에이전트 3 fan-out(백엔드 계약 / 리포 관습 / 구현 설계) |
| 23:01 | `724d2218` | `sleep 45`·`sleep 40`·`sleep 90` 맹목 대기, 첫 건은 가드에 막힘 |
| 23:04 | `724d2218` | **사용자 중단** — "다른 세션에서 change 완료 후 붙이겠다", `TaskStop` ×2 |
| 23:43 | `724d2218` | 재개 — `git log`로 SSE change 완료 확인 후 착수 |
| 23:45 | `724d2218` | `demo-ui/` 스캐폴딩(Vite + React + TS), `npm install` |
| 23:48 | `724d2218` | 토큰·타입·훅·컴포넌트 40여 파일 작성 (~23:57) |
| 23:57 | `724d2218` | 빌드 실패 → `@types/node` 추가 → 통과 |
| 23:58 | `724d2218` | Vite 프록시 너머로 `/health`·`/documents` 실측, dev 서버 200 확인 |
| 23:59 | `724d2218` | **`curl -N`로 실물 `/api/qa` SSE 수신**, `/api/search` 점수 4질의 실측 |
| 00:00 | `724d2218` | Chroma 컬렉션 문서 수 직접 확인 — 검색 0건인지 UI 문제인지 분리 |
| 00:02 | `724d2218` | Claude in Chrome 연결 |
| 00:05 | `724d2218` | 브라우저에서 질문 입력 → 스트리밍 수신 → 스크린샷 → 콘솔 판독 |
| 00:06 | `724d2218` | 렌더로만 보이는 3건 수정(`global.css`·`AnswerStream.module.css`·`format.ts`) |
| 00:10 | `724d2218` | 사용자 인터럽트 — "add-demo-ui 완성됐습니다" |
| 00:13 | `724d2218` | `demo-ui/DESIGN.md` 기반 UI 정합 — 토큰 밖 원시 색상·간격 스케일 색출 |
| 00:24 | `724d2218` | `outline: none` 0건 확인(접근성), `global.css` 포커스 링 정리 |
| 00:25 | `724d2218` | `README.md`·`ARCHITECTURE.md`에 demo-ui 절 추가 |
| 00:27 | `724d2218` | `src/app` 무변경 확인 → 백엔드 회귀 없음 |
| 00:28 | `724d2218` | `tasks.md` 재작성(48/55, 미검증 7건에 사유 명기), `validate --strict` |
| 00:29 | `724d2218` | `git checkout -b add-demo-ui`, `node_modules`·`dist` 미스테이징 확인 |
| 00:30 | `724d2218` | **`f392b24`** — 61파일 +4,690 |
| 00:31 | — | 사용자가 `add-answer-generation`으로 브랜치 복귀 |
