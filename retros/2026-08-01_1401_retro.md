# Retrobot 회고 — 2026-08-01 14:01 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

트리거 커밋: *feat: 마무리 검증* (작성 2026-08-01 13:59:54, 당시 해시 `452ca03`)

## 요약

| 항목 | 값 |
|------|-----|
| 신규 세션 | 2 (`41a00411` 후반부, Retrobot 실행 `d3e1ad56`) |
| 신규 프롬프트 | 8 (직접 실행한 `git` 명령 3건 포함) |
| 에러 발생 | 6 (push 403 ×2 · 커맨드 오타 1 · 비대화식 push 실패 1 · 회고 실행의 권한 거부 2) |

직전 회고([13:43](./2026-08-01_1343_retro.md)) 이후 새로 쌓인 구간은 **`41a00411`의 13:44~14:00** 입니다.
`bootstrap-runtime-skeleton`이 **47/52 → 52/52**로 끝났습니다.

## 세션별 활동

### 세션 `41a00411` (13:44 ~ 14:00) — push 인증 복구 + 그룹 7 마무리 검증

**프롬프트:**
1. `git push -u origin bootstrap-runtime-skeleton` (직접 실행) → 403
2. (도구 사용 중 인터럽트 1회)
3. `재생성 진행했습니다.` — PAT 재생성 후 알림
4. `printf ... | git credential-osxkeytchain erase` (직접 실행, 오타)
5. `git push -u origin bootstrap-runtime-skeleton` (직접 실행) → Device not configured
6. `/opsx:apply push 진행했습니다. 이제 마무리 작업 진행해주세요.`
7. `커밋 진행해주세요. feat: 마무리 검증`

**만든 것:** `docs/SPIKES.md`(신규), `Makefile`에 `test`·`lint` 타깃, `README.md`·`ARCHITECTURE.md` 정정,
`tasks.md` 7.1~7.5 완료 — 5개 파일 +83/-13.

**진행 경과:**

- **push 403 원인 규명(13:44~13:46).** "커밋 이메일 때문 아니냐"는 흐름을 먼저 끊고 — 인증은 이메일이 아니라
  자격증명으로 한다 — 토큰이 어떤 계정으로 인증되는지부터 확인. 계정은 `m0304s`로 정확했고 리포 조회가 **404**.
  프라이빗 리포에 권한이 없으면 GitHub이 403이 아니라 404를 준다는 점에서 **fine-grained PAT의 리포 선택 누락**으로 확정.
- 재생성한 토큰이 반영되지 않아 keychain의 옛 자격증명을 삭제. 사용자가 입력한 `credential-osxkeytchain` 오타를
  바로잡아 대신 실행(삭제는 입력이 필요 없으므로).
- **7.1 (13:51~13:52).** 커밋된 트리만으로 되는지 보려고 **별도 위치로 clone**해 문서에 적힌 `make up` 그대로 실행 → 200.
- **7.2에서 실제 결함 2건(13:53).** ① 깨끗한 체크아웃에는 `.venv`가 없어 README의 `.venv/bin/pytest`가 성립하지 않음.
  ② 이 맥의 `python3`는 **3.9.6**이라 README 한 줄의 `python3 -m venv`로는 `requires-python >= 3.11` 때문에 설치 실패.
  → `make test`·`make lint` 타깃을 만들고, 3.11+ 인터프리터를 후보에서 골라 없으면 **조용히 낮은 버전으로 돌지 않고 멈추게** 처리.
  깨끗한 clone에서 venv 생성(python3.14) 후 46 passed, 컨테이너를 모두 내리고 재실행해 외부 의존 없음까지 확인.
- **7.3 문서-코드 대조(13:55~13:56).** ARCHITECTURE의 오류 처리 문장이 코드보다 앞서 있었음 — 현재 어댑터인 헬스 프로브는
  예외를 **던지지 않고** `ProbeResult`로 보고한다. 문장을 현재 구현에 맞추고, 도메인 예외 경로가 테스트로 덮여 있다는 근거를 병기.
- **7.4 (13:56~13:57).** 미검증 가정을 `docs/SPIKES.md`로 분리. change가 archive되면 사라지는 design의 Risks 대신
  리포 레벨 문서로 두고, S-1~S-4 각각에 확인 방법과 **틀렸을 때의 대안**까지 적음.
- **7.5 (13:58).** `openspec validate --strict` + 전체 검증 → **52/52**, 커밋.

### 세션 `d3e1ad56` (13:41 ~ 13:45) — Retrobot 자기 실행

13:43 회고를 만든 백그라운드 훅 실행. 트리거 커밋 대비 **커밋 시각 13:45:31**로 약 4분. 백그라운드 분리 효과는 세 번째 실행에서도 유지.

## KPT 회고

### Keep (좋았던 것)

- **"깨끗한 체크아웃"을 말이 아니라 실제 clone으로 검증했습니다.** 현재 작업 디렉토리에서 `make up`·`pytest`를
  돌리면 둘 다 통과합니다 — `.venv`도 있고 이미지도 캐시돼 있으니까요. 별도 위치로 clone해서 돌렸기 때문에
  **README의 두 줄이 평가자 환경에서 깨진다는 사실**이 드러났습니다. 불합격 기준 1번(명시된 방법으로 실행 불가)에
  정확히 해당하는 결함을, 평가자가 아니라 이쪽에서 먼저 찾았습니다.
- **결함을 문서 수정이 아니라 실행 경로 수정으로 막았습니다.** README에 "먼저 venv를 만드세요"를 덧붙이는 게
  더 쉬웠지만, `make test` 한 줄이 스스로 venv를 만들도록 바꿨습니다. 실행 조건을 사람 기억에 맡기지 않는 쪽입니다.
- **3.11 미만 인터프리터에서 조용히 도는 대신 멈추게 했습니다.** 시스템 `python3`가 3.9인 맥이 흔한데,
  낮은 버전으로 venv를 만들면 실패가 설치 단계까지 밀려 원인이 흐려집니다. 후보를 훑고 없으면 명시적으로 멈춥니다.
- **push 실패에서 증상이 아니라 신호를 읽었습니다.** 403이 떴을 때 토큰을 다시 만들어보는 대신
  "권한 없는 프라이빗 리포는 404"라는 GitHub의 동작으로부터 fine-grained PAT의 리포 선택 누락을 특정했습니다.
  같은 세션 전반부에서 모델 이름을 6번 찍어본 것과 정반대의 접근입니다.
- **문서가 코드보다 앞서 있는 문장을 스스로 잡았습니다.** ARCHITECTURE의 오류 처리 서술이 실제 어댑터 동작과
  달랐고, 항목별 대조를 했기 때문에 발견됐습니다. CLAUDE.md에서 문서-코드 불일치는 감점이 아니라 불합격입니다.
- **미검증 가정을 change 밖으로 꺼냈습니다.** design의 Risks에 두면 archive와 함께 사라집니다.
  `docs/SPIKES.md`는 각 항목에 확인 방법과 틀렸을 때의 후퇴 경로까지 적어 다음 change의 첫 태스크로 넘어갑니다.

### Problem (나빴던 것)

- **이미 push된 회고 커밋 하나가 옛 이메일을 달고 있습니다.** `58e7ca0`은 post-commit 훅이
  트리거 커밋의 `GIT_AUTHOR_*` 환경을 물려받아 만들어져, 13:44의 작성자 리라이트를 **비껴갔습니다**
  (작성자 날짜도 트리거 커밋과 같은 13:41:08). 리라이트 시점에 훅이 이미 돌고 있었기 때문입니다.
  로컬 7개는 고쳤는데 훅이 만든 8번째가 그대로 원격에 올라갔고, 되돌리려면 force-push가 필요합니다.
- **push 인증 복구에 5분, 왕복 4회를 썼습니다.** 403 → 재생성 → keychain 삭제(오타로 1회 헛돎) →
  비대화식 셸에서 `Device not configured`. 마지막은 자격증명 입력이 필요한 명령을 도구로 돌릴 수 없어
  사용자가 직접 실행해야 했던 건인데, 그 사실을 시도한 뒤에 알았습니다.
- **회고 실행 자체가 매번 같은 권한 벽에 부딪힙니다.** 이번 실행에서 `simple_expansion` 거부, `/tmp` 쓰기 거부,
  `python3` 실행 거부가 연달아 났고 — **직전 실행(`d3e1ad56`)의 에러 4건도 정확히 같은 세 가지**였습니다.
  매번 우회 경로를 새로 찾느라 회고 1건당 서너 번의 헛도는 도구 호출이 붙습니다.
- **회고가 임시 스크립트를 또 리포 루트에 만들었습니다.** `/tmp` 쓰기가 막혀 `.retro_scan.py`를 루트에 만들었습니다
  (이번엔 종료 전에 지웠습니다). 13:43 회고의 Try가 "회고 실행이 만든 임시 파일은 스스로 지운다"였는데,
  **애초에 리포 안에 만들지 않아도 되는 경로**가 없다는 게 진짜 문제입니다.
- **`.venv/bin/pytest`가 README에 들어간 채 두 커밋을 지났습니다.** 13:09·13:41 커밋 모두 이 줄을 담고 있었고
  깨끗한 체크아웃 검증(7.1·7.2)에 와서야 드러났습니다. 태스크에 검증 단계가 있었기에 잡혔지만,
  없었다면 그대로 제출됐을 줄입니다.

### Try (개선해야 할 것)

- **`.githooks/post-commit`에서 `GIT_AUTHOR_*`·`GIT_COMMITTER_*`를 명시적으로 지웁니다.** 훅이 트리거 커밋의
  identity를 물려받지 않고 현재 `git config`를 쓰게 합니다. 한 줄이면 `58e7ca0` 같은 커밋이 다시 나오지 않습니다.
- **`58e7ca0`의 작성자 정정은 다음 push 때 함께 처리할지 결정합니다.** 지금은 원격 HEAD라 force-push가 필요하고,
  혼자 쓰는 브랜치라 비용은 낮습니다. 그냥 두기로 한다면 그 판단을 어딘가에 남겨야 나중에 다시 고민하지 않습니다.
- **회고용 임시 파일 자리를 `.gitignore`된 경로 하나로 고정합니다.** `.retrobot/` 같은 디렉토리를 `.gitignore`에
  넣어두면 `/tmp` 권한과 무관하게 매번 같은 곳에 쓰고, 리포를 더럽히지도 린트를 깨지도 않습니다.
- **회고 실행이 쓰는 도구 패턴을 허용 목록에 넣습니다.** 두 번 연속 같은 세 가지(셸 확장·임시 파일 쓰기·`python3` 실행)에서
  막혔습니다. `retrobot/SKILL.md`에 "이 경로/명령만 쓴다"를 못박고 그에 맞춰 권한 규칙을 한 번 정리하면
  회고당 서너 번의 헛도는 호출이 사라집니다.
- **다음 change의 첫 태스크는 `docs/SPIKES.md`의 S-1입니다.** 컨테이너 안에서 CLI를 **한 번 호출해 보는 것**이
  이 프로젝트에서 가장 값싼 고위험 검증입니다. 여기가 틀리면 실행 경로 문서 전체를 다시 써야 하는데,
  마감에 가까울수록 되돌릴 시간이 없습니다.
- **`openspec archive bootstrap-runtime-skeleton`으로 정리하고 다음 change(문서 수집)로 넘어갑니다.**
  52/52로 끝났으니 미완 change를 남기지 않고 닫습니다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 13:41–13:45 | `d3e1ad56` | Retrobot 백그라운드 실행 — 13:43 회고 생성 (약 4분) → `58e7ca0` |
| 13:44 | `41a00411` | 미푸시 7개 커밋 작성자 리라이트(`filter-branch`) 직후 push → 403 |
| 13:44–13:46 | `41a00411` | 인증 계정은 `m0304s`로 정확, 리포 조회 404 → fine-grained PAT 리포 선택 누락으로 원인 확정 |
| 13:47–13:49 | `41a00411` | PAT 재생성 → keychain 옛 자격증명 삭제(오타 1회) → 비대화식 push 실패, 사용자가 직접 push |
| 13:50 | `41a00411` | push 확인 (원격 `58e7ca0`), 회고 커밋이 옛 이메일을 단 사실 인지 — 이미 push되어 보류 |
| 13:51–13:52 | `41a00411` | **7.1** 별도 위치로 clone → 문서의 `make up` 그대로 실행 → 200 |
| 13:53 | `41a00411` | **7.2** 결함 2건 — 깨끗한 체크아웃에 `.venv` 없음, 시스템 `python3`가 3.9.6 |
| 13:53–13:54 | `41a00411` | `make test`·`make lint` 신설(3.11+ 인터프리터 선택, 없으면 중단) → clean clone 46 passed, 컨테이너 0개로 재확인 |
| 13:55–13:56 | `41a00411` | **7.3** 문서-코드 대조 — ARCHITECTURE 오류 처리 서술이 코드보다 앞서 있어 정정 |
| 13:56–13:57 | `41a00411` | **7.4** `docs/SPIKES.md` 신설 (S-1~S-4, 확인 방법과 대안 포함) |
| 13:58 | `41a00411` | **7.5** `openspec validate --strict` + 전체 검증 → **52/52** |
| 13:59 | `41a00411` | 커밋 `452ca03 feat: 마무리 검증` (5파일 +83/-13) |
