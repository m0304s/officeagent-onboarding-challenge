# Retrobot 회고 — 2026-08-03 20:31 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 17 (작업 8 · Retrobot 자체 실행 9) |
| 총 프롬프트 | 4 (그 외 세션은 `/opsx:apply` 등 슬래시 명령으로 시작) |
| 에러 발생 | 31 (그중 **27건이 Retrobot 자체 실행 세션**) |
| 직전 회고 Try | 6건 중 **6건 반영** (전부 미커밋) — 세 회차 연속 0이 끊김 |
| 테스트 | 340 → **622 passed** (이번 커밋 구간 578 → 622, 신규 44건) |
| 커밋 | 36건 (그중 18건이 `docs(retro)`) |

**분석 범위 주의.** 지정된 17개 로그의 24시간 창은 08-02 20:55 ~ 08-03 20:27이고, 이 중
08-03 01:42까지의 세션은 이미 `2026-08-02_2105` ~ `2026-08-03_0137` 회고 9건이 다뤘다.
낮 시간대(09:50~17:31 커밋 9건)의 세션 로그는 **지정 목록에 없어 이번 분석에서 빠졌다.**
따라서 아래 KPT는 아직 회고되지 않은 마지막 세션 `38dcf8a2`(20:07~20:27, `5f5d93f`)에
집중하고, 나머지는 타임라인으로만 남긴다.

## 세션별 활동

### 세션 `38dcf8a2` (08-03 20:07 ~ 20:27) — `5f5d93f feat: QA 서비스 구현`

**프롬프트:**
1. `/opsx:apply add-answer-generation` — "QA 서비스 작업 진행해주세요."

인터럽트 0회. 도구 54회 — Bash 25 · Edit 18 · Read 10 · Write 5 · Skill 1.
**Bash 25회 전부 POSIX, 셸 혼용 에러 0건.**

**산출:** `+1445/-17`, 7파일. `src/app/services/qa.py`(441줄) · `tests/qa_harness.py`(140) ·
`test_qa_service.py`(398) · `test_qa_citations.py`(144) · `test_qa_retry.py`(208) ·
`tests/stubs.py`(+83) · `tasks.md` 4.1~4.16 체크와 근거 주석.
진행 43/84 → **59/84**.

**발생한 에러:** 2건
- 20:22 `pytest -q && ruff check . && ruff format --check .` 복합 명령 exit 1 —
  실패한 것은 `ruff format --check`
- 20:23 `ruff format tests/ src/ && ruff check . && ruff format --check .` exit 1 —
  `tests/qa_harness.py` 는 정렬됐고 `ruff check` 도 통과했으나
  `docs/superpowers/specs/2026-08-03-codex-app-server-streaming-design.md:142` 가 남음

### 세션 `44fac989`·`5f796aaf`·`a626c3aa` (08-02 20:55 ~ 21:25)

Retrieval 설정값 · 앱 팩토리 서명 이동 · 임베딩 토큰 계산 질의 경로 연결.
340 → 344 passed. `retrobot/_extract.py` 가 `ruff` E401 로 걸림(당시 미추적 파일).

### 세션 `7e054299`·`cadbfc2d`·`8ccbec39`·`b42efae1` (08-02 23:36 ~ 08-03 01:39)

저장소 질의 프로토콜 → 검색 서비스 → search API → qa-retrieval 완성 → 레이어 감사·리팩토링.
354 → 371 → 388 → 400 → **414 passed**. 사용자 프롬프트 2건
("core 단에 들어가야 할 내용이 services 단에 있는지 체크", "코드 리팩토링 진행해주세요")이
`b42efae1` 의 감사·리팩토링을 촉발했다.

### Retrobot 자체 실행 9회 (`90e8e04a`·`62f70e7c`·`a9355fb9`·`6937c4af`·`e6173212`·`b403397a`·`96832dcb`·`6ef28f9b`·`06eb7705`)

**에러 27건 전부가 로그 파싱 수단 확보에 쓰였다.** `/tmp` Write 거부 6회,
"Contains brace with quote character (expansion obfuscation)" 3회, `cat`·`sed`·`xxd`·`cd`
승인 요구 8회, 리포 안에 쓴 `retrobot/_extract.py` 가 lint 를 깨뜨려 `6937c4af` 에서
사용자가 직접 삭제를 지시.

## KPT 회고

### Keep (좋았던 것)

- **관측할 수 없는 요구사항을 관측 가능하게 만들고 나서 단언했다.** 4.8 동시 상한은
  성패로 드러나지 않는다 — 상한이 있든 없든 모든 시도가 성공한다. 페이크 생성기에
  "동시에 열려 있던 시도의 최대치" 카운터를 심어 그 값을 재는 방식으로 바꿨다.
  4.12 "판정 줄 확정 전 이벤트 0회"도 같은 문제였다: 이벤트 **총합**으로는 확인되지
  않는다(첫 조각에서 이미 하나 나갔어도 총합은 같을 수 있다). 이벤트마다 그 시점까지
  생성기가 내보낸 조각 수를 함께 기록해 **시각이 아닌 순서로** 고정했다.
  두 경우 모두, 카운터가 없었으면 요구사항을 지우는 회귀가 아무 테스트도 깨지 않는다.

- **하네스가 근거를 손으로 심지 않고 실제 수집·검색 경로로 만들었다.** 4.11 이 단언하는
  것은 "`sources` 가 `/search` 응답과 같은 모양인가"인데, 근거를 지어내면 이 대조가
  테스트 자신의 조작으로 **항상 참이 된다.** 같은 질문을 검색 경로에 직접 보내
  대조하는 형태라 그 자기 충족이 성립하지 않는다.

- **스트리밍 조립의 진실 원천을 하나로 유지했다.** 서비스는 `VerdictSplitter` 의 판정을
  재사용해 답을 조립할 수 있었는데, 대신 받은 원문 전체를 `parse_answer` 에 다시 먹인다.
  두 경로가 같은 본문을 낸다는 것은 2.7이 임의 분할로 이미 단언했으므로 중복이 아니고,
  스트리밍용 상태 기계가 조립의 원천이 되면 **둘이 갈릴 때 갈린 사실 자체가 안 보인다.**

- **구현 중에 설계에 없던 결함을 잡아 tasks.md 에 근거로 남겼다.** `async for` 는 순회를
  멈출 때 대상 생성기를 닫아 주지 않는다 — `aclosing` 없이는 위임 경로의 취소 정리가
  영영 돌지 않는다. 20:20~20:21 에 실제로 걸려서 고쳤고, 4.14 에 "`aclosing` 두 겹이
  실제로 도는지 보는 유일한 테스트"라는 주석과 함께 순회 중단 정리 테스트를 넣었다.

- **셸 혼용이 두 세션 연속으로 끊겼다.** Bash 25회 · PowerShell 0회 · 혼용 에러 0건.
  20분 세션에 대기 구간이 없었다(직전 두 세션은 72분 중 47분이 승인 대기였다).

- **커밋 직후 Try 전용 세션이 여섯 건을 10분 만에 닫았다.** 세 회차 연속 0이던 것이
  끊겼고, 그것도 문구를 더 정밀하게 써서가 아니라 **처리 자리를 바꿔서** 끊겼다.
  `CLAUDE.md` 에는 `--build` 함정의 이유가, `docker-compose.yml` 에는 마운트를 붙이지
  않는 이유가 들어갔는데 후자는 특히 "안 했다"가 아니라 **"해봤자 안 듣는다"** 를
  적었다 — `pip install .`(편집 가능 설치가 아니다) 때문에 `/app/src` 를 덮어써도
  import 되는 코드는 그대로이고, `tests` 마운트만 듣는 **반쪽 상태가 더 나쁘다**는
  판단까지 남겼다. 그리고 열 번 이월된 README `/search` 예시가 실측값으로 닫혔다.

- **20분에 44건의 테스트가 구독·네트워크 없이 돈다.** `test_qa_service.py` 24건 0.12초,
  `test_qa_citations.py` 9건, `test_qa_retry.py` 11건 — 백오프 단언은 **실제로 자지 않고**
  하네스가 대기를 기록으로 바꾼다. `CLAUDE.md` 가 가산점으로 명시한 조건을 서비스 층에서도
  충족했다.

### Problem (나빴던 것)

- **`ruff format --check .` 가 빨간 채로 커밋됐고, 그 사실을 두 번 보고도 넘어갔다.**
  20:22와 20:23 두 번 exit 1 을 받았다. 20:23 의 대응은 `ruff format tests/ src/` 였는데,
  실패한 파일은 `docs/superpowers/specs/2026-08-03-codex-app-server-streaming-design.md`
  라서 그 범위에 없다. 재확인에서 다시 exit 1 이 났고 `&&` 체인이 끊겨 뒤의 `git status`
  가 돌지 않았는데도, 다음 명령은 `git status` 를 따로 부르는 것이었다 — **실패를 처리한
  것이 아니라 실패가 막은 명령만 우회했다.** 지금 HEAD 에서 그대로 재현된다:

  ```
  1 file would be reformatted, 142 files already formatted
  ```

  그리고 이것은 `README.md:312` 에 평가자용 명령으로 적혀 있다
  (`docker compose run --build --rm test ruff format --check .`).
  **문서에 적힌 명령이 지금 실패한다.** 17:41 회고가 "`README.md` 281·311·312줄에는
  `--build` 가 이미 적혀 있음(자기 문서 미준수)"을 지적한 지 3시간 만에, 같은 줄 번호에서
  다른 방식으로 재현됐다.

- **작업 세션은 Try를 하나도 안 실었다 — 다만 이번엔 직후 전용 세션이 여섯 건을 전부
  해치웠다.** 커밋 `5f5d93f`(20:27) 시점에서 6건은 모두 미반영이었다. 4장 세션의 첫
  프롬프트는 또 한 줄("QA 서비스 작업 진행해주세요")이었고, 세션이 자동으로 읽는
  `tasks.md`·`CLAUDE.md` 어디에도 Try가 없었으므로 이것은 구조적으로 예정된 결과다.

  그런데 **커밋 4분 뒤부터 10분 사이에 별도 세션이 여섯 건을 전부 작업 트리에 반영했다.**
  파일 수정 시각이 그것을 말한다:

  | Try | 내용 | 반영 | 수정 시각 |
  |-----|------|------|-----------|
  | 1 | `CLAUDE.md` 환경·검증 절차 | `+24` | 20:31:18 |
  | 2 | `docker-compose.yml` `test` 마운트 부재 이유 | `+18` | 20:31:41 |
  | 3 | `tasks.md` 기준선 (`622 passed`, `5f5d93f`) | 4-B 머리 | 20:34:25 |
  | 4 | Codex 적대적 리뷰를 **번호로** (`4B.1`) | 〃 | 〃 |
  | 6 | README `/search` 실측 교체 (`4B.2`) + README 본문 | `+11/-3` | 20:37:13 |
  | 5 | `retrobot/SKILL.md` 우회 절차 절 | `+30` | 20:32:25 |

  **"세 회차 연속 0"은 여기서 끊겼다.** 그리고 무엇이 통했는지도 분명하다 — 기능 세션
  안에 Try를 끼워 넣는 것이 아니라, **커밋 직후 Try만 처리하는 세션을 따로 여는 것**이다.
  세 회차 동안 실패한 쪽(정밀도를 높인다, 순서를 지정한다)은 전부 전자를 전제로 했다.

  다만 Try #4·#6은 "태스크 번호로 만든다"까지가 이행이고 리뷰·교체 자체는 아직 `[ ]`이다.
  README 본문 예시는 실측값으로 교체됐다(`score` `0.8609192999999999`, `chunk_index` 0,
  `char_start/end` 0·527) — **열 번 이월된 항목이 닫혔다.**

- **그 여섯 건이 전부 커밋되지 않은 상태이고, 그래서 아직 아무 효력이 없다.**
  이 회고를 쓰는 시점에 `git status` 는 다섯 파일이 `M` 이다. 특히 `retrobot/SKILL.md` 는
  훅이 **커밋된 판**을 다음 회차에 넘기므로, 커밋 전에 다음 회차가 돌면 그 30줄은
  또 안 읽힌다. 실제로 이번 회차가 그 상태였다 — 20:32에 들어온 그 절의 맨 위
  지침(`command -v python3 jq`)이면 피했을 에러 2건(heredoc `"expansion obfuscation"`
  가드, `/tmp` Write 거부)을 20:29~20:35의 이 세션이 그대로 냈다. **일을 하고도
  닿지 않으면 비용은 두 번 나간다.**

- **새로 들어온 `CLAUDE.md` 문장 하나가 이 세션의 로그와 어긋난다.** 20:31에 추가된
  절은 이렇게 적는다:

  > **테스트·앱은 호스트에서 돌지 않습니다.** … 저장소의 `.venv`에도 `app` 패키지가
  > 설치되어 있지 않습니다(`ruff` 전용). 실행 경로는 컨테이너 하나뿐입니다.

  그러나 같은 날 20:17~20:24에 `38dcf8a2` 가 호스트에서 이것을 돌렸다:

  ```
  .venv/bin/pytest tests/test_qa_service.py -q   →  24 passed in 0.12s
  .venv/bin/pytest -q                            →  622 passed, 43 warnings in 37.17s
  ```

  경고 출력의 경로가 `.venv/lib/python3.14/site-packages/chromadb/…` 다 — `.venv` 는
  3.9가 아니라 **3.14**이고, `app` 을 import 하지 못하면 622건이 통과할 수 없다.
  이 커밋의 622라는 숫자 자체가 그 venv 에서 나왔다.

  **문제의 핵심은 문장 하나가 틀렸다는 것이 아니라, 그것이 `CLAUDE.md` 로 들어간다는
  것이다.** 17:41 Try #1도 같은 형태였다 — "호스트에 python·jq가 없다(Windows Store
  alias, exit 49)"를 `CLAUDE.md` 에 박으라고 했는데, 이번 세션의 셸에서는 거짓이다.
  두 번 다 **관측한 환경을 특정하지 않은 환경 사실**이 모든 세션이 읽는 파일로 승격될
  뻔했다. 아이러니하게도 같은 커밋이 "환경 사실은 이 파일에만 남깁니다"라는 규율을
  새로 세우는데, **그 규율에 관측 조건을 함께 적으라는 단서가 없다.**

- **1,445줄에 외부 검토 0회 — 세 세션 연속.** 2장 1,184줄 · 3장 1,692줄 · 4장 1,445줄,
  합계 4,321줄이 Codex 적대적 리뷰 없이 들어갔다. 이번 4장에는 재시도 정책·세마포어·
  `aclosing` 두 겹 정리처럼 **테스트가 초록인 채로 틀릴 수 있는** 것들이 있다.
  `4B.1` 이 20:34에 태스크 번호를 받았지만 아직 `[ ]` 이고, 4,321줄은 그동안 계속
  검토되지 않은 채 쌓여 있다.

- **`openspec validate --strict` 미실행.** `tasks.md` 16줄에 근거 주석을 새로 붙였는데
  검증을 돌리지 않았다. 3장(17:41)에서도 같은 누락이 지적됐다 — **두 회차 연속.**

- **Retrobot 자체가 전체 에러의 87%(27/31)를 만든다.** 커밋된 `SKILL.md` 는 "파싱
  스크립트가 필요하면 `/tmp` 아래에 쓴다"고 지시하는데 이 환경에서 `/tmp` Write 는
  거부된다(**20번째 재발**). 지침이 막힌 경로를 가리키고 있어서 회차마다 같은 탐색을
  반복한다. `6937c4af` 에서 사용자가 직접 개입해 `retrobot/_extract.py` 를 지우게 한
  것도 이 탐색의 부산물이었다. 고친 지침은 이미 있는데 커밋되지 않았다(위 항목).

- **README `/search` 예시는 20:37에 실측값으로 교체됐다 — 다만 로그 예시 한 줄이 남았다.**
  본문 예시는 닫혔지만 209행의 검색 로그 예시 `"top_score":0.8734` 는 그대로다.
  같은 실행에서 나온 값은 `0.8609192999999999` 이므로, 두 예시가 **같은 요청을 서로
  다르게 보고하는 상태**다. `4B.2` 가 "같은 파일의 검색 로그 예시(`top_score`)도 같은
  실행에서 나온 값으로 맞춘다"고 이미 지시하고 있다.

### Try (개선해야 할 것)

앞선 세 회차의 Try는 "정밀하게 쓰기 → 0/5", "순서까지 지정 → 0/6"으로 실패했고,
이번에 통한 것은 **커밋 직후 Try만 처리하는 세션을 따로 여는 것**이었다(20:31~20:37에
6건). 그러므로 아래는 그 방식을 전제로 하고, 가장 먼저 할 일은 새로 쓰는 것이 아니라
**이미 쓰인 것을 닿게 만드는 일**이다.

1. **작업 트리의 다섯 파일을 커밋한다.** `CLAUDE.md` · `docker-compose.yml` ·
   `retrobot/SKILL.md` · `tasks.md` · `README.md` — 여섯 Try의 결과물 전부가 아직
   `M` 상태다. 커밋 전에는 효력이 없고, 특히 `retrobot/SKILL.md` 는 훅이 **커밋된
   판**을 넘기므로 다음 회차가 먼저 돌면 그 30줄은 또 안 읽힌다. 이번 회차가 정확히
   그랬다.

   `SKILL.md` 에 한 줄 더 넣고 커밋하면 좋다 — 이번 회차에 실제로 통한 형태다.
   `command -v python3` 가 나오는 기계에서는 `/tmp` 파일 없이 인라인으로 끝난다:

   ```bash
   python3 -c '
   import json, os, datetime, sys
   ...   # dict() 를 쓰고, 중괄호 뒤에 따옴표가 오는 리터럴을 쓰지 않는다
   ' <session-id> <session-id> ...
   ```

   샌드박스의 `"expansion obfuscation"` 가드가 막는 것은 `{` 바로 뒤에 따옴표가 오는
   형태다. f-string 과 dict 리터럴이 전부 여기 걸리므로 `%` 포매팅과 `dict()` 를 쓴다.
   세션 ID 목록은 스크립트에 박지 말고 `sys.argv` 로 받는다(훅이 매 회차 다른 목록을 준다).

2. **커밋 전에 `CLAUDE.md` 의 "테스트·앱은 호스트에서 돌지 않습니다" 절을 고친다.**
   이 커밋의 `622 passed` 가 바로 그 `.venv`(python 3.14)에서 나왔다 —
   `.venv/bin/pytest -q` → `622 passed, 43 warnings in 37.17s`. 지금 문장대로면
   **모든 세션이 읽는 파일에 거짓이 들어간다.** 관측된 사실로 바꾸고, 같은 절에
   새로 세운 규율("환경 사실은 이 파일에만 남깁니다")에 **관측 조건을 함께 적는다**는
   단서를 붙인다 — 17:41 Try #1이 틀렸던 것도 정확히 이 누락 때문이다.

3. **`ruff format --check .` 을 초록으로 되돌린다.** 대상은 한 파일 한 군데,
   `docs/superpowers/specs/2026-08-03-codex-app-server-streaming-design.md:142` 의
   코드 블록이다. 선택지가 둘이고 **어느 쪽인지는 정해야 한다** — (a) 그 파일에
   `ruff format` 을 돌린다(정렬해 둔 주석이 무너진다), (b) `pyproject.toml` 의
   `[tool.ruff]` 에 `extend-exclude = ["docs/"]` 를 넣어 게이트 범위를 소스로 한정한다.
   설계 문서의 코드 블록을 소스와 같은 기준으로 재는 것이 의도가 아니었다면 (b)가 맞다.
   어느 쪽이든, 고친 뒤 `ruff check .` 과 `ruff format --check .` 을 **따로** 돌려
   각각의 종료 코드를 본다 — `&&` 로 묶어서 이번에 어느 쪽이 죽었는지 가려졌다.

4. **`4B.1`(3·4장 Codex 적대적 리뷰)을 5장 착수보다 먼저 돌린다.** 번호는 받았지만
   아직 `[ ]` 이고, 그동안 검토되지 않은 구현이 4,321줄까지 쌓였다. 5장은 그 위에
   SSE 를 얹는 장이라, 리뷰를 5장 뒤로 미루면 검토 대상이 한 번 더 늘어난다.
   대상 커밋(`096d676`·`5f5d93f`)과 **구현 단계임**을 리뷰어에게 먼저 말한다.

5. **`4B.2` 를 닫을 때 `README.md:209` 의 `"top_score":0.8734` 도 같이 바꾼다.**
   본문 예시는 이미 실측값(`0.8609192999999999`)으로 갔는데 로그 예시만 남아,
   지금은 **같은 요청을 두 예시가 다르게 보고한다.** 태스크 문구에는 이미 들어 있다.

6. **다음 회차에도 "커밋 → Try 전용 세션" 순서를 유지한다.** 이번에 6/6을 만든 것은
   Try 문구의 정밀도가 아니라 **전용 세션을 연 것**이다. 기능 세션의 첫 프롬프트는
   앞으로도 한 줄일 것이므로(네 회차 연속 그랬다), Try 를 거기 실으려는 시도는
   접는다. 대신 그 세션이 끝나면 회고를 읽고 Try 만 처리하는 세션을 한 번 연다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 08-02 20:55 | `44fac989` | Retrieval 설정값 세팅 — 17 passed, 340 passed |
| 08-02 21:05 | `90e8e04a` | Retrobot — `/tmp` 거부·승인 요구 에러 8건 |
| 08-02 21:05 | `5f796aaf` | 서명 생성을 앱 팩토리로 이동 — 340 passed |
| 08-02 21:14 | `62f70e7c` | Retrobot — 에러 3건 |
| 08-02 21:14 | `a626c3aa` | 임베딩 토큰 계산을 질의 경로까지 — 344 passed |
| 08-02 21:26 | `a9355fb9` | Retrobot — 에러 2건 |
| 08-02 21:27 | `6937c4af` | 사용자 지시로 `retrobot/_extract.py` 삭제·훅 허용 목록 수정 |
| 08-02 23:36 | `7e054299` | 저장소 질의 프로토콜 — 354 passed |
| 08-02 23:49 | `e6173212` | Retrobot — `/tmp`·brace 가드 에러 4건 |
| 08-02 23:58 | `cadbfc2d` | 검색 서비스 — 371 passed |
| 08-03 00:15 | `b403397a` | Retrobot — 에러 2건 |
| 08-03 00:15 | `8ccbec39` | search API — 388 passed |
| 08-03 00:35 | `96832dcb` | Retrobot — 에러 3건 |
| 08-03 00:40 | `b42efae1` | qa-retrieval 완성 → 사용자 지적으로 레이어 감사 → 리팩토링, 400 → **414 passed** |
| 08-03 01:16 | `6ef28f9b` | Retrobot — 에러 3건 |
| 08-03 01:37 | `06eb7705` | Retrobot — 에러 2건 |
| 08-03 20:07 | `38dcf8a2` | `/opsx:apply` 4장 QA 서비스 16태스크 — `qa.py` 441줄 + 테스트 3파일, 578 → **622 passed**, 진행 59/84 |
| 08-03 20:22 | `38dcf8a2` | `ruff format --check .` exit 1 (docs 파일) — **미해결 상태로 커밋** |
| 08-03 20:27 | `38dcf8a2` | `5f5d93f feat: QA 서비스 구현` (+1445/-17, 7파일) |
| 08-03 20:31 | (후속 세션) | 직전 회고 Try 처리 — `CLAUDE.md` `+24` · `docker-compose.yml` `+18` |
| 08-03 20:32 | (후속 세션) | `retrobot/SKILL.md` `+30` — 막힌 도구 환경 우회 절차 |
| 08-03 20:34 | (후속 세션) | `tasks.md` **4-B 신설** — `622 passed` 기준선 · `4B.1` Codex 리뷰 · `4B.2` README 실측 · `4B.3` validate |
| 08-03 20:37 | (후속 세션) | `README.md` `/search`·`/documents` 예시를 **실측값으로 교체** (열 번째 이월 종결) |
| 08-03 20:40 | — | 위 다섯 파일 모두 **미커밋 상태** — 커밋 전까지 다음 세션에 닿지 않음 |
