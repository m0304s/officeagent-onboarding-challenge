# Retrobot 회고 — 2026-08-02 23:53 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 3 (Retrobot 수정 1 + 구현 1 + Retrobot 1) |
| 총 프롬프트 | 2 (Retrobot 결함 3건 지시 1 · `/opsx:apply add-qa-retrieval — 4장` 1) |
| 에러 발생 | 5 (구현 세션 **0** + Retrobot 수정 1 + 이번 Retrobot 4) |

직전 회고(21:29) 이후 창(21:27~23:53)의 활동만 다룹니다. 지정된 로그 18개 중 나머지
15개는 13:02·15:34·16:07·16:35·17:47·21:05·21:19·21:29 회고에서 이미 다뤘습니다.

## 세션별 활동

### 세션 `6937c4af` (21:27 ~ 21:30) — Retrobot 결함 정리

**프롬프트:**
1. "Retrobot 회고 관련해서 1. `retrobot/_extract.py`를 지우거나 살릴 거면 제대로 살린다
   2. 훅의 허용 목록에 로그 파싱 수단을 넣는다 — 회고 품질이 아니라 회고 **비용**의 문제"

**한 일:** 커밋 `ae57939` (+15/-2, 2파일)

| 대상 | 내용 |
|------|------|
| `.githooks/post-commit` | `--allowedTools` 에 `Bash(python3:*)` · `Bash(jq:*)` · `Edit(/tmp/**)` · `Edit(/private/tmp/**)` 추가, 왜 필요한지 주석으로 |
| `retrobot/SKILL.md` | "JSONL을 `Read`로 통째로 읽지 말고 `python3`/`jq`로 집계하라", "파싱 스크립트는 `/tmp`에 써라" 를 스킬 본문에 명시 |

### 세션 `7e054299` (23:36 ~ 23:48) — 4장 저장소 질의

**프롬프트:**
1. `/opsx:apply add-qa-retrieval` — "4번 저장소 질의 진행해서, 저장소에서 임베딩 벡터
   검색하는 파트까지 구현 진행해주세요"

**한 일:**

| 태스크 | 내용 |
|--------|------|
| 4.1 | `VectorStore` 프로토콜에 `query(embedding, *, top_k, versions) -> list[ScoredChunk]`. "`query`가 없다 — retrieval change가 붙인다"는 예약 문구를 붙인 내용으로 교체 (`adapters/protocols.py`) |
| 4.2 | Chroma 어댑터 `_query` — 삼중항을 `$and` 셋으로 만들고 여럿이면 `$or` 로 묶음, `n_results=top_k`, 기존 `_offload` 재사용으로 `StorageUnavailable` 변환 규약 유지 |
| 4.3 | 빈 `versions` 는 **저장소를 건드리지 않고** 빈 목록 반환 — "조건 없음 = 전체" 관습으로 뒤집히면 지워진 문서가 검색된다는 이유를 코드 주석으로 |
| 4.4 | `_similarity()` — `1 - 거리`, `[0, 1]` 클램프, **발동 시 경고 로그**. 모듈 docstring을 "거리 지표는 이 파일 밖으로 나가지 않는다"로 교체 |
| 4.5 | `_scored_chunk()` — `page` 없는 포맷(txt·md)은 `metadata.get("page")` 로. 수집이 값 없는 키를 넣지 않는 규약의 반대편 |
| 4.6 | 실물 Chroma 질의 테스트 **10건** 추가 (`tests/test_vector_store.py` +163) |
| 4.7 | `StubVectorStore` 에 **완전 탐색 코사인** 질의 + 지연·실패 주입을 질의 경로에도 (`tests/stubs.py` +84) |
| 4.8 | `ARCHITECTURE.md` 검색 대상 필터 절 (+42) — 기각한 대안(사후 필터·서명만 필터·필터 없음) 포함 |

진행률 22/66 → **27/66**. 커밋 `e43b795` (+438/-29, 6파일).
전체 스위트 344 → **354 passed**, 벡터 스토어 스위트 28 passed(실물 Chroma, 스킵 0).

**발생한 에러:** 0건 — 도구 실패도 권한 거부도 없습니다. 되돌린 것은
`ruff format --check` 가 잡은 1파일 미포맷뿐이고, 바로 다음 명령에서 재포맷했습니다.

### 세션 `e6173212` (23:49 ~) — Retrobot 이번 회고 생성

**발생한 에러:** 4건 — `/tmp` 쓰기 거부 2건(`Write` 툴 1 · 리다이렉션 1), 히어독의
`{"key":` 를 셸 확장 난독화로 오인한 차단 1건, `sed` 파이프 승인 요구 1건.

## KPT 회고

### Keep (좋았던 것)

- **12분짜리 세션이 에러 0건으로 끝났습니다.** 4장은 태스크 8개에 6파일 438줄인데도
  시행착오가 한 건도 없습니다. 앞선 세션들과 다른 점은 tasks.md가 "무엇을"이 아니라
  "왜 그래야 하는지"(빈 목록이 전체 검색이 아닌 이유, 거리를 어댑터 안에서 바꾸는 이유)까지
  적혀 있었다는 것입니다. 계획에 근거가 들어 있으면 구현 중에 되짚을 일이 없습니다.
- **빈 `versions` 를 "저장소를 건드리지 않는다"로 못 박았습니다.** 이건 성능 최적화가
  아니라 안전 계약입니다. Chroma를 포함해 대부분의 필터 API가 "조건 없음 = 전체"로
  동작하므로, 빈 목록을 그대로 흘려보내면 **유효 문서가 하나도 없을 때 잔여 청크 전체가
  검색되는** 방향으로 조용히 뒤집힙니다. 코드 주석·프로토콜 docstring·테스트
  (`test_an_empty_target_set_returns_nothing_rather_than_everything`) 세 곳에 같은
  이유를 남겨서, 어느 하나를 지워도 나머지가 버팁니다.
- **클램프를 조용히 하지 않았습니다.** `[0, 1]` 밖이면 잘라내되 경고를 남깁니다. 클램프는
  본래 "이상치를 정상값으로 위장"하는 연산이라, 로그가 없으면 모델 교체로 점수 분포가
  이동한 사실이 영영 드러나지 않습니다. 하한 설정의 정의역을 `[0, 1]` 로 고정하려고
  클램프를 두면서 그 대가를 함께 처리했습니다.
- **스텁을 근사가 아니라 완전 탐색 코사인으로 만들었습니다.** 7장 품질 테스트가 재는 것은
  임베딩 품질이지 ANN 인덱스의 흔들림이 아닙니다. 스텁이 근사를 흉내내면 실패가 임베딩
  탓인지 인덱스 탓인지 갈라낼 수 없습니다 — `FakeEmbedder` 를 대칭으로 남긴 결정과 같은
  논리입니다.
- **직전 Try 2가 닫혔습니다.** `.githooks/post-commit` 의 `--allowedTools` 에
  `Bash(python3:*)` 가 들어갔고, 세 회차 연속 이월되던 한 줄이 사라졌습니다.
- **첫 명령부터 `.venv/bin/...` 을 썼습니다.** 직전 회고의 Problem이었던 시스템
  `python3` 오용이 이번엔 한 번도 없습니다 — 검증용 프로토콜 확인(23:48)까지
  `.venv/bin/python` 입니다. 지침으로 남기진 못했지만(Try 3 미이행) 행동은 교정됐습니다.
- **실물 Chroma 테스트 10건이 스킵 없이 돌았습니다.** 필터 조립·메타데이터 왕복·`page`
  부재는 라이브러리 동작에 의존하는 부분이라 목으로 덮으면 "통과"가 아무것도 보장하지
  않습니다.

### Problem (나빴던 것)

- **Retrobot 훅 수정이 절반만 맞았습니다.** `Edit(/tmp/**)` 을 넣었는데 이번 회차도
  `/tmp` 쓰기가 거부됐습니다. 거부 사유가 다릅니다 — `--allowedTools` 가 아니라
  **작업 디렉토리 샌드박스**("Claude Code may only write to files in the allowed working
  directories for this session")입니다. 허용 목록으로는 이 벽을 넘을 수 없고,
  `--add-dir` 이 필요합니다. 원인을 한 층 얕게 짚어서 고쳤고, 증상은 그대로입니다.
- **그 결과 워크스페이스에 다시 스크립트를 썼습니다.** `/tmp`가 막히니 리포 루트에
  `.retrobot_parse.py` 를 만들었습니다 — 두 회차 전 `retrobot/_extract.py` 가
  `make lint` 를 깨뜨린 것과 **똑같은 상황**입니다(이번에는 회고 커밋 전에 지웠습니다).
  훅 주석이 "워크스페이스 안에 쓰면 미추적 파일이 남는다"고 경고하는 바로 그 일을,
  그 훅이 만든 제약 때문에 하게 됐습니다.
- **`동점 결정성` 이 다섯 번째 이월입니다.** 17:47 · 21:05 · 21:19 · 21:29에 이어
  이번에도 `openspec/changes/add-qa-retrieval/` 전체에 "동점"이 없습니다. 직전 Try 1이
  "4장 착수 전 1분이면 된다"고 적었는데 4장이 끝났습니다. **5장은 정렬·하한이 실제로
  붙는 장이고, 다음 세션이 바로 5장입니다** — 여기서 놓치면 구현이 끝난 뒤 되짚습니다.
- **직전 Try 3·4도 미이행입니다.** `.venv/bin/python` 지침은 `CLAUDE.md`·`AGENTS.md`
  어디에도 없고, 3.5의 "가드는 6장에서 붙는다"를 9.5 체크리스트에 거는 일도 tasks.md에
  "6장"이라는 표기가 없어 그대로입니다. 4장 세션이 에러 0건으로 깨끗했던 것과 별개로,
  **회고가 낸 액션 4건 중 닫힌 것은 1건뿐**입니다.

### Try (개선해야 할 것)

1. **`.githooks/post-commit` 의 `claude -p` 에 `--add-dir /tmp` 를 붙이거나, 스크립트를
   `.git/retrobot/` 에 쓰게 한다.** 후자가 낫습니다 — 이미 훅이 `WORK_DIR` 로 만들어
   두는 디렉토리이고, 리포 안이라 샌드박스를 통과하면서 `.git` 아래라 `git status` 에도
   `ruff check .` 에도 잡히지 않습니다. `--allowedTools` 에 넣은 `/tmp` 규칙 세 줄은
   그때 지웁니다(효과가 없는 규칙이 남아 있으면 다음 사람이 또 그걸 원인으로 봅니다).
2. **5장 착수 프롬프트에 `동점 결정성`을 태스크로 넣는 일을 먼저 시킨다.** 다섯 번 연속
   "다음에"로 밀린 항목이 여섯 번째로 밀리는 것을 막는 방법은 회고에 또 적는 게 아니라
   **5장 `/opsx:apply` 를 부르기 전에 tasks.md를 먼저 고치는 것**입니다. 회고를 읽는
   시점과 계획을 고치는 시점이 붙어 있어야 합니다.
3. **직전 회고의 미이행 Try 2건(`\.venv` 지침 · 9.5 체크리스트)을 5장 커밋에 끼워 넣는다.**
   둘 다 한 줄 편집입니다. 이월 회차가 아니라 "한 줄인데도 안 한 이유"가 문제입니다 —
   커밋 단위로 묶이지 않아서입니다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 21:27 | `6937c4af` | Retrobot 결함 2건 지시 — 잔해 파일·훅 허용 목록 |
| 21:29 | `6937c4af` | `--allowedTools` 에 `python3`·`jq`·`/tmp` 추가, SKILL.md에 집계 방식 명시 |
| 21:30 | — | 커밋 `ae57939` fix: Retrobot 오류 수정 |
| 23:37 | `7e054299` | `/opsx:apply add-qa-retrieval` — 4장 지정, 컨텍스트·대상 코드 읽기 |
| 23:39 | `7e054299` | 4.1 프로토콜 `query` 추가, 예약 docstring 교체 |
| 23:41 | `7e054299` | 4.2~4.5 Chroma 어댑터 — `_version_filter`·`_first_batch`·`_scored_chunk`·`_similarity` |
| 23:44 | `7e054299` | 4.6 실물 Chroma 질의 테스트 10건, 4.7 `StubVectorStore` 완전 탐색 코사인 |
| 23:45 | `7e054299` | `ruff format --check` 가 1파일 미포맷 검출 → 재포맷, 벡터 스토어 스위트 28 passed |
| 23:46 | `7e054299` | 4.8 `ARCHITECTURE.md` 검색 대상 필터 절(+42) |
| 23:47 | `7e054299` | `ruff check .` · `format --check` · 전체 스위트 **354 passed** |
| 23:49 | — | 커밋 `e43b795` feat: 저장소 질의 프로토콜 구현 (27/66) |
| 23:49 | `e6173212` | Retrobot 이번 회고 생성 — `/tmp` 쓰기 거부 2건, 워크스페이스로 우회 |
