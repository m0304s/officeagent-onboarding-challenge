# Retrobot 회고 — 2026-08-04 20:50 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 14 (본 세션 11 + 서브에이전트 3) |
| 총 프롬프트 | 36 |
| 총 로그 엔트리 | 4,407 |
| 에러 발생 | 42 (이 중 30건 = 71%가 Retrobot 훅) |
| 커밋 (창 내) | 26 (구현·문서 13 + 회고 13) |

분석 범위는 2026-08-03 20:50 ~ 2026-08-04 20:48 KST. 이 창 안에서 주석 규칙 정리 → SSE 스트리밍
→ 데모 UI → RRF 스펙 → BM25 연동 → 응답 캐시 설계까지 여섯 덩어리가 지나갔다. 08-04 낮의
BM25 구현 세션들(`4d370fc`~`d320d8d`)은 이번 실행 대상 파일 목록에 없어 커밋으로만 보인다.

## 세션별 활동

### 세션 `4f55c9d8` (08-03 20:53 ~ 22:22)

**프롬프트:** 2건

1. 주석 규칙(모듈 docstring 5줄, 함수 1~3줄, 인라인 1~2줄 "왜"만, 강조 마크업 금지, 주석 줄 수 > 코드 줄 수 실패) 반영 요청
2. "LLM 5개 파일 작업 진행해주세요"

**도구:** Bash 87 · Edit 41 · Read 28 · Write 6 · AskUserQuestion 2

**발생한 에러:** 4건 — 전부 `scripts/check_comments.py`의 exit 1. 위반이 파일·줄·사유로
나열되어 그대로 다음 작업 목록이 됐다. 1건은 `Write` 전 `Read` 누락.
결과물은 `afa9794 refactor: 주석 정리`.

### 세션 `8ccc760d` (08-03 22:23 ~ 23:42)

**프롬프트:** 3건. 시작은 "스트리밍은 어떤 방식 쓰고 있지?"

**도구:** Bash 73 · Edit 72 · Read 38 · Write 6 · Skill 1

**발생한 에러:** 3건 — `ruff` 위반, `.secrets/codex/`·`openspec/project.md` 부재(경로 가정 오류),
`curl` exit 56(health 000, 컨테이너 미기동). 커밋 `346c6df feat: SSE 로직 구현`,
`dcd6bd7 feat: SSE 로직 테스트`.

### 세션 `724d2218` + 서브에이전트 3개 (08-03 22:47 ~ 08-04 00:31)

**프롬프트:** 11건 (본 세션) + 4건 (서브에이전트)

1. "POST + SSE라서 EventSource 못쓰는 이유는?"
2. "다른 세션에서 change를 완료하고 나서 프론트엔드 데모 UI를 붙여보겠습니다"
3. "현재, change 완성했습니다.(SSE) 이어서 프론트엔드 데모 UI 완성해주세요"
4. "demo-ui/DESIGN.md 파일 기반으로 UI 수정해주세요"
5. "중단 및 UI를 통한 업로드 및 삭제 작업 진행했습니다"
6. 중단 2회 (`[Request interrupted by user]`)

**도구:** Bash 71 · Write 56 · Edit 19 · `claude-in-chrome` computer 10 · Read 8 ·
`browser_batch` 8 · Agent 3 · ToolSearch 3

서브에이전트 3개를 병렬로 띄워 백엔드 API 계약(`ErrorCode` 전 멤버, 필드명, 기본값)과 리포
관례를 먼저 확정한 뒤 프론트를 붙였다. 브라우저 MCP로 실물 화면까지 확인.

**발생한 에러:** 3건 — 프록시 헬스 확인 중 Python traceback, `cd demo-ui` 디렉토리 부재
(생성 전 진입), 브라우저 MCP 탭 컨텍스트 미해결. 커밋 `f392b24`, `2d215cf`.

### 세션 `58b22a1f` · `2d412d02` (08-04 00:31 ~ 19:24)

**프롬프트:** `git checkout add-answer-generation`, `/exit`

**도구:** Bash 41 · Write 13 · Read 9 · Skill 1 · AskUserQuestion 1

**발생한 에러:** 0건. `add-rrf-algorithm-spec` 4/4 아티팩트 `--strict` 통과 →
`ff7a5ad`, `35b9c3a`. 이후 낮 시간대 BM25 구현은 다른 로그 파일에 있고, 커밋으로만 보인다
(`f731869`·`4d370fc`·`a4be966`·`426917f`·`9ad7dd8`·`1df833b`·`d320d8d`).

### 세션 `45f0bdd9` (08-04 20:13 ~ 20:48) — 이번 창의 중심

**프롬프트:** 7건

1. (세션 시작) 응답 캐시 설계 착수
2. "해당 내용이 사용자가 질문을 했을 시, 질문에 대한 캐시를 설명하는 것이 맞나요?"
3. "상황 / 원하는 동작" 3행 표 제시 — 운영자가 캐시를 껐다 → 항상 미스 / 테스트가 Redis 없이
   돈다 → 정상 캐시 동작 / Redis가 죽었다 → 항상 미스 + 관측 가능. "이거 검토해주세요"
4. "수정해주세요"
5. "1. REPLACED가 no-evidence 미삭제하는 것 확인해주세요. 2. insufficient_evidence 무효화
   누락된 것 확인해주세요"

**도구:** Bash 30 · Edit 22 · Write 6 · Read 5 · Skill 1 · AskUserQuestion 1

**발생한 에러:** 0건.

**산출물:** `openspec/changes/add-response-cache/` 4개 아티팩트, `--strict` 통과,
`f07ed41 docs: 응답 레이어 L1/L2 캐시 설계`. 태스크 47 → 67로 증가.

**사용자 지적으로 뒤집힌 설계 3건:**

| # | 지적 | 결과 |
|---|---|---|
| 1 | `cache_enabled=false`인데 인메모리로 배선되면 히트가 계속 난다 | 구현을 셋으로 분리 — `NullResponseCache`(꺼짐, 항상 미스) / `InMemoryResponseCache`(테스트가 명시 주입할 때만) / `RedisResponseCache`. 배선에 인메모리 분기가 없음을 태스크 2.8이 테스트로 고정. Redis 장애 시 인메모리 폴백은 **무효화가 프로세스 경계를 못 넘는다**는 이유로 폐기, 대신 타임아웃 0.2초 + 연속 3회 실패 시 30초 차단기 |
| 2 | `REPLACED`가 `no_evidence` 캐시를 안 지운다 | `design.md:136`의 근거("교체는 문서 집합의 **크기**를 바꾸지 않는다")가 틀렸음을 확인 — 판정을 뒤집는 건 크기가 아니라 **내용**. 비우는 조건을 `CREATED`만 → `UNCHANGED`를 뺀 전부로 확대 |
| 3 | `insufficient_evidence` 무효화 누락 | 부정 판정인데 부정 집합에 없었음. 키를 `qa:v1:no-evidence` → `qa:v1:negative`로 바꾸고 `no_evidence`·`insufficient_evidence` 둘 다 수용 |

### Retrobot 훅 세션 5개 (`9d06bafd`·`12eec827`·`cc79ede2`·`d0c01226`·`e4260f90`)

**프롬프트:** 각 1건 (`SKILL.md` 전문)

**발생한 에러:** 30건 — 전체 42건의 71%. 반복 패턴 세 가지:

- `command -v python3 jq` → "multiple operations, requires approval" (5회 전부)
- `/tmp/retro_*.py` `Write` 또는 `>` 리다이렉션 차단 (5회 전부)
- 인라인 파이썬 문법 실패 3종 — `SyntaxError: Non-UTF-8 code starting with '\xeb'`
  (stdin 파이프에 한글), `f-string expression part cannot include a backslash`,
  TIMELINE 행 문자열 안의 따옴표 충돌

## KPT 회고

### Keep (좋았던 것)

- **사용자 검토가 스펙 결함 3건을 잡아냈고, 세 건 다 실재했다.** `45f0bdd9`에서
  `openspec validate --strict`는 통과한 상태였다. 검증기가 잡는 것은 아티팩트의 *형식*이지
  design과 spec의 *주장이 서로 맞는지*가 아니다. 사람이 3행 표 하나로 그 틈을 정확히 짚었고,
  구현 시작 전에 잡혔으므로 코드 수정 비용은 0이었다.
- **틀린 쪽을 방어하지 않고 즉시 뒤집었다.** "표가 맞고, 제 design.md가 틀렸습니다",
  "`design.md:136`의 근거 자체가 틀렸습니다"처럼 어디가 왜 틀렸는지를 줄 번호로 특정한 뒤
  고쳤다. 2번 지적에서는 원래 근거(크기)와 옳은 근거(내용)를 나란히 적어, 같은 오판이
  재발할 자리를 문서에 남겼다.
- **설계 결정에 실패 경로를 붙여 적었다.** Redis 폴백을 뺀 이유가 "관측이 안 돼서"가 아니라
  "워커 A가 캐시한 `no_evidence`를 워커 B의 무효화가 못 지운다"는 구체적 시나리오로 적혔다.
  결정 4의 비대칭 표(`stop`은 "이 청크들이" 주장 / `no_evidence`는 "코퍼스 어디에도" 주장)도
  같은 성격 — 다음 사람이 근거를 재구성하지 않아도 된다.
- **데모 UI에서 서브에이전트 3개를 계약 확정에 먼저 썼다.** 백엔드 `ErrorCode` 전 멤버와
  필드 기본값을 문자 그대로 뽑아온 뒤 프론트를 붙였고, 서브에이전트 3개의 에러는 합쳐서
  2건(전부 `ls`/`wc`의 디렉토리 오류)이었다.
- **`45f0bdd9`은 Bash 30 · Edit 22 · Write 6을 돌리면서 도구 에러 0건.** `--build` 누락도
  경로 오타도 없었다.

### Problem (나빴던 것)

- **Retrobot 샌드박스 벽 22번째 — 그리고 이번엔 원인이 `SKILL.md` 자신이다.**
  전체 에러의 71%(30/42)가 여기서 나왔다. `SKILL.md`의 "먼저 확인할 것" 절이 지시하는 첫
  명령 `command -v python3 jq`가 **multi-operation 판정으로 매번 거부된다.** 진단하라고
  넣은 줄이 진단을 막고 있다. 본문도 여전히 "파싱 스크립트가 필요하면 `/tmp` 아래에 쓴다"고
  적혀 있어 5회 전부 `/tmp` 쓰기 거부를 재생산했다. 08-03 22:23 회고가 확정한 우회로
  (`/tmp` 파일 생성은 막히고 stdin 파이프는 통한다)는 **`SKILL.md`에 반영되지 않았다** —
  회고에만 적힌 발견은 다음 회차에 읽히지 않는다.
- **stdin 파이프 우회로도 한글에서 깨진다.** `12eec827`이
  `SyntaxError: Non-UTF-8 code starting with '\xeb'`로 죽었다. 직전 회고가 "통한다"고
  기록한 경로가 이 리포에서는(프롬프트·회고가 전부 한글) 조건부로만 통한다. 우회로를
  기록할 때 **어떤 입력에서 통했는지**를 같이 적지 않아 생긴 재실패다.
- **`openspec validate --strict`가 통과한 change에 실질 결함 3건이 남아 있었다.**
  design.md:177이 자기 스펙의 시나리오("모든 요청이 미스로 처리된다")와 정면으로 어긋났는데
  검증기는 통과시켰다. 이번엔 사람이 잡았지만, 사람이 그 표를 안 만들었으면 67개 태스크가
  틀린 설계 위에서 구현됐을 것이다.
- **`add-response-cache`에 적대적 외부 검토가 0회.** 결함 3건이 전부 대화 중 사용자
  지적으로만 발견됐고, Codex 등 외부 검토는 붙지 않았다. 이 창에서 만들어진 스펙
  (`add-rrf-algorithm-spec`, `add-response-cache`) 둘 다 미검토 상태로 남았다.
- **인라인 파이썬 따옴표 사고 3회.** f-string 안의 백슬래시, TIMELINE 행 문자열의 따옴표
  충돌, UTF-8 선언 누락 — 전부 "한 줄로 밀어넣기" 때문에 났고, 매번 처음부터 다시 고쳤다.

### Try (개선해야 할 것)

- **`retrobot/SKILL.md`를 이번 실측으로 갱신한다.** 22회 반복된 항목이므로 다음 작업의
  맨 앞에 둔다. 고칠 세 곳:
  1. `command -v python3 jq` → `command -v python3` 와 `command -v jq` **두 줄로 분리**
     (한 줄이면 거부된다).
  2. "`/tmp` 아래에 쓴다" → "**`python3 -c '...'` 한 번의 호출로 끝낸다. 리다이렉션(`>`),
     파이프, `&&`, `;`를 붙이면 그 순간 승인 요구로 막힌다**". 이번 세션이 실측으로 확인한
     경로다 — `python3 -c`는 통했고, 뒤에 `> /tmp/out.json; wc -c`를 붙인 순간 거부됐다.
  3. 함정 절 신설 — f-string 안 백슬래시 금지, 한글을 stdin으로 넘기지 말 것,
     출력이 크면 `[:400]` 절단.
- **우회로를 기록할 때 성공 조건을 같이 적는다.** "stdin 파이프는 통한다"가 아니라
  "ASCII 전용 스크립트면 통한다 / 한글이 섞이면 인코딩 선언이 필요하다" 수준으로.
  조건 없는 우회로는 다음 회차에 다시 깨진다.
- **change 완료 선언 전에 design ↔ spec 대조를 한 번 넣는다.** `--strict` 통과는 형식
  검사일 뿐이다. spec의 시나리오 문장과 design의 결정을 짝지어 읽는 패스를 태스크로
  명시한다 — 이번 3건 중 1번(`cache_enabled=false`)은 **스펙이 이미 옳았고 design만
  어긋난** 경우라, 이 대조만으로 잡혔을 것이다.
- **`add-response-cache` 67태스크를 구현 착수 전에 외부 검토에 건다.** 세 번 뒤집힌
  설계이므로 네 번째가 없다는 보장이 없다. 검토 대상은 특히 결정 4(부정 판정 비대칭)와
  결정 12(세 구현 분리) — 둘 다 이번에 급히 다시 쓴 자리다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 (KST) | 세션 | 주요 활동 |
|------|------|----------|
| 08-03 20:53 | `4f55c9d8` | 주석 규칙 반영 착수, `check_comments.py` exit 1을 작업 목록으로 |
| 08-03 22:14 | `4f55c9d8` | LLM 어댑터 5파일 정리 → `afa9794` |
| 08-03 22:23 | `12eec827` | Retrobot — 거부 8건, UTF-8 SyntaxError |
| 08-03 22:45 | `8ccc760d` | "스트리밍은 어떤 방식 쓰고 있지?" → SSE 구현 `346c6df` |
| 08-03 22:57 | `724d2218` | "POST + SSE라서 EventSource 못쓰는 이유는?" |
| 08-03 23:00 | 서브에이전트 3 | 백엔드 API 계약·리포 관례 병렬 조사 |
| 08-03 23:24 | `8ccc760d` | health 000 (컨테이너 미기동) |
| 08-03 23:43 | `d0c01226` | Retrobot — f-string 백슬래시 SyntaxError |
| 08-03 23:44 | `724d2218` | SSE change 완료 → 데모 UI 착수 |
| 08-04 00:13 | `724d2218` | `DESIGN.md` 기반 UI 수정, 브라우저 MCP로 실화면 확인 |
| 08-04 00:30 | — | `f392b24` · `2d215cf` 데모 UI 커밋 |
| 08-04 00:31 | `58b22a1f` | `add-answer-generation` 브랜치로 이동 |
| 08-04 01:12 | `2d412d02` | `add-rrf-algorithm-spec` 4/4 `--strict` 통과 → `ff7a5ad` |
| 08-04 09:42~17:29 | (범위 밖 로그) | BM25 구현 6커밋 → `1df833b` · `d320d8d` 아카이빙 |
| 08-04 20:13 | `45f0bdd9` | 응답 캐시 설계 착수 — Redis는 헬스 프로브뿐임을 확인 |
| 08-04 20:29 | `45f0bdd9` | `add-response-cache` 4아티팩트 완료, `--strict` 통과 |
| 08-04 20:34 | `45f0bdd9` | 사용자 3행 표 → design.md 오류 인정, 구현 3분리 + 차단기, 태스크 47→67 |
| 08-04 20:43 | `45f0bdd9` | 사용자 지적 2건 → `qa:v1:no-evidence` → `qa:v1:negative`, 무효화 범위 확대 |
| 08-04 20:49 | — | `f07ed41 docs: 응답 레이어 L1/L2 캐시 설계` |
