# Retrobot 회고 — 2026-08-02 17:47 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 3 (실작업 1 + Retrobot 자동 실행 1 + 빈 세션 1) |
| 총 프롬프트 | 8 (창 내 **신규** 지시 기준) |
| 에러 발생 | 2 (사용자 중단 1 · 도구 실패 1) |
| 커밋 | `7804f85` 병합 · `a802efc` 스펙 확정(+714) · `ab458b0` · `1d99301` |

직전 회고([16:35](./2026-08-02_1635_retro.md)) 이후의 신규 활동은 **16:44~17:47, 약 1시간**이다.
`document-ingestion` 브랜치를 `main`에 병합하고, 새 브랜치 `add-qa-retrieval`에서 **검색(RAG의 R) change의 계획 산출물 4개를 확정**했다. 코드는 한 줄도 쓰지 않았다 — 전부 스펙·설계·태스크다.

## 세션별 활동

### 세션 `9df9a4d6` (16:44 ~ 17:47 KST) — `add-qa-retrieval` 스펙 정의

**프롬프트:**

1. `/opsx:propose` — (중단 후 재입력) `add-qa-retrieval`: 질의 임베딩 → 유사도 검색 → 상위 K 청크 반환까지. LLM 답변 생성은 다음 change로 미룬다
2. `/opsx:propose` — "질의와 청크는 같은 임베딩 공간에서 비교된다" 요구사항 추가 (질의용/문서용 임베딩 분리, 색인 서명 연동)
3. `/opsx:propose` — `top_k` 범위 요구사항과 "상한을 넘는 K" 시나리오 추가
4. "하한도 pydantic에 두고 코드 갈리는 쪽으로 되돌려주세요."
5. `/codex:adversarial-review` — "스펙을 분석하고, 논리 상 맞지 않거나 수정해야 하는 요인이 있으면 알려주세요"
6. "TOCTOU 지적부터 검토해서 반영해주세요"
7. `/opsx:propose` — "질의 길이 상한 지적건에 대해 최악 케이스에 대해 **역산**하여 질의 문자 상한을 계산해주세요"
8. "현재까지의 스펙을 확정하겠습니다."
9. (직전 회고 Try 인용) "이것도 반영해주세요" — 고아 볼륨·compose 개행

**도구 사용:** Bash 56 · Edit 48 · Write 6 · Read 3 · Skill 1 · AskUserQuestion 2

**병합과 분기 (16:42)**

- `document-ingestion` → `main` 병합 (`7804f85`, 65 파일 +10077/-185). 브랜치 하나가 통째로 들어간 커밋이라 diff로 읽을 수 있는 단위는 아니지만, 병합 자체는 change 경계와 일치한다
- `main`에서 `add-qa-retrieval` 브랜치 분기

**계획 산출물 4개 — `add-qa-retrieval` (+714줄)**

| 파일 | 줄 |
|---|---:|
| `proposal.md` | 55 |
| `specs/retrieval/spec.md` | 283 |
| `design.md` | 278 |
| `tasks.md` | 96 |

주요 결정 13건 중 굵직한 것:

- **검색 대상은 레지스트리가 정하고 필터는 질의 *이전에* 건다** — 문서별 현재 `(revision, index_signature)` 삼중항에 속한 청크만 본다. 수집이 "검색이 현재 값으로 필터한다"고 전제해 둔 것을 여기서 실제로 지킨다
- **`VectorStore.query`는 문자열이 아니라 벡터를 받는다** — 저장소 어댑터가 임베딩 모델을 알게 되면 둘 중 하나를 갈아끼울 때 다른 하나가 딸려 온다
- **거리 → 유사도 변환을 어댑터 안에 가둔다** — 거리를 흘리면 저장소 교체 시 "점수가 클수록 좋다"가 조용히 뒤집히는데 타입도 테스트도 그대로 통과한다. 클램프가 발동하면 경고 로그
- **하한(threshold)은 두되 "답할 수 있는가"는 판정하지 않는다** — 그건 다음 change의 프롬프트 몫. 검색은 근거 없음을 *통보*하지 않고 빈 목록/낮은 점수로 *드러낼* 뿐이다
- **`POST /search`를 답변 엔드포인트와 별도로 남긴다** — 검색과 생성을 분리해 관측하는 진단 지점
- **색인 서명 유도를 앱 팩토리로 올린다** — 수집과 검색이 각자 유도하면 진실 원천이 둘이 된다 (동작 불변, 생성자만 바뀜)

**Codex 적대적 리뷰 (17:22~17:24, 백그라운드)**

`needs-attention` — high 2건 · medium 1건.

| 심각도 | 지적 | 처리 |
|---|---|---|
| high | 검색과 교체/삭제 사이 **TOCTOU** — 레지스트리를 읽은 뒤 저장소에 질의하는 사이 재업로드가 커밋되면 폐기된 리비전이 반환된다 | **반영** (결정 13) |
| high | **1000자 상한이 512토큰 무음 절단을 막지 못한다** | **반영** (결정 6) |
| medium | 결정성 요구에 **동점 처리 계약**과 실물 저장소 검증이 없다 | 미반영 |

**TOCTOU — 잠금이 아니라 재검증을 택했다 (결정 13)**

리뷰어의 권고는 "공유 read/write 동기화 또는 재검증 후 재시도"였는데, 채택한 것은 **재검증 후 드롭**이다. 결과를 받은 뒤 *실제로 기여한 문서만* 레지스트리에서 다시 읽고(보통 한두 건), 삼중항이 다르면 그 문서의 결과를 버린다. 재시도하지 않는다. 현재성의 기준 시점을 "결과 조립 시점"으로 계약에 명시하고, **응답 전달 사이의 커밋은 막지 못한다**는 한계를 Risks에 남긴 뒤 캐싱 change로 넘겼다.

**질의 길이 — 역산이 원래 설계를 기각했다 (결정 6)**

토크나이저를 실측해 표를 만들었다.

| 항목 | 실측값 |
|---|---|
| 모델 입력 창 | 512 토큰 |
| 특수 토큰 + `query: ` 접두사 | 5 |
| 본문 가용 | **507** |
| 최악 비율 | **5.016 토큰/문자** (CJK 호환 정사각 문자 — `㌚` → `キロメートル`로 펼쳐짐) |
| 한국어 평문 | 0.53 ~ 0.56 토큰/문자 |

절대 안전한 문자 상한은 **101자**. 그런데 이 숫자가 곧 "문자 상한 하나로 하려던 원래 설계가 틀렸다"는 증거였다 — 최악값과 실제 한국어의 비가 약 9배라, 101자면 512토큰 창의 11%만 쓰면서 150자짜리 정상 질문을 거부한다. 그래서 **문자 수(임의 길이 입력이 토크나이저에 닿는 것을 막음)와 토큰 수(조용한 절단을 막음)로 역할을 나눴다.** 토큰 상한은 설정 항목이 아니라 임베더가 선언한 값을 그대로 쓴다 — 손으로 적을 수 있으면 어긋난 값이 들어가고 그 순간 가드가 사라진다.

여기서 **직전 설계의 근거를 스스로 기각한 기록도 남겼다**: "거부 경로가 모델 로딩을 기다리면 안 된다"던 원래 논리가 약했다는 것(정상 경로는 어차피 임베딩하고 기동 훅이 선로딩한다)을 design에 적었다.

**이월 3건 전부 처리 (17:44~17:47)**

직전 회고 Try를 사용자가 그대로 붙여 넣었고, 실제로 닫혔다.

- `ab458b0` — `docker-compose.yml` 끝 개행 복구(**세 번째 이월**), 고아 볼륨 처분을 tasks.md **0장**(= "이 change의 첫 커밋에 포함")으로 태스크화
- `1d99301` — `officeagent-onboarding-challenge_registry`(16KB)·`_vector-store`(3.0MB) 삭제. `AskUserQuestion`으로 확인받고 진행했고("삭제는 되돌릴 수 없어서 임의로 진행하지 않았습니다"), 삭제 후 현행 바인드 마운트 데이터가 무사한지 확인

**발생한 에러:**

- **`[Request interrupted by user for tool use]` (16:46:37)** — 첫 `/opsx:propose`를 change 이름 없이 "왜 여기서 끊는가"부터 시작해 입력했다. 14초 뒤 이름(`add-qa-retrieval`)을 앞에 붙여 재입력
- **`Error: Exit code 1` — `openspec change list` (16:46:56)** — `changes: []`. 새 브랜치에 진행 중인 change가 없는 정상 상태였고, 뒤 단계에 영향 없음

### 그 밖 (창 내)

- Retrobot 자동 실행 1회 — `2e6eb0ed`(16:26)
- `6766b765` — "폴더 구조 변경"으로 이름만 붙고 프롬프트 없이 종료된 빈 세션

## KPT 회고

### Keep (좋았던 것)

- **세 번 이월된 항목이 이번엔 커밋 두 개로 닫혔다.** [16:07](./2026-08-02_1607_retro.md)·[16:35](./2026-08-02_1635_retro.md) 회고가 "이월되는 이유는 어려워서가 아니라 '다음에'라고 적기만 해서다"라고 진단했는데, 그 진단대로 **tasks.md의 0장**에 올려 구조로 강제했다. 회고 → 다음 세션의 작업 순서 변경이 두 회차 연속으로 성립했다.
- **리뷰 지적을 그대로 받지 않고 더 싼 해법을 골랐다.** TOCTOU에 대해 리뷰어는 프로세스 외부 락을 권했지만, 채택한 것은 "결과에 기여한 문서만 재검증하고 어긋나면 드롭"이다. 락은 검색 경로 전체를 직렬화하는데 이 규모에서 그 대가는 과하고, 재검증은 보통 한두 건의 `get`이다. **그리고 이 선택이 무엇을 못 막는지(응답 전달 사이의 커밋)를 Risks에 적고 캐싱 change로 명시적으로 넘겼다** — 해결한 척하지 않았다.
- **"1000자면 여유 있다"는 감을 실측으로 반증했다.** 리뷰가 지적하자 추정으로 답하지 않고 토크나이저를 직접 돌려 최악 5.016 토큰/문자를 뽑았고, 그 숫자가 **원래 설계(문자 상한 단독)를 기각**하는 데까지 갔다. 게다가 최악 문자가 왜 최악인지(정규화에서 펼쳐지는 CJK 호환 문자)와 반대 방향 공격이 왜 싼지(`<unk>` 병합)까지 적어, 다음 사람이 상한을 "적당히" 올리는 것을 막는다.
- **잘못된 이전 근거를 삭제하지 않고 기각 사유와 함께 남겼다.** "거부 경로가 모델 로딩을 기다리면 안 된다"던 논리가 약했다는 것을 design에 적었다. CLAUDE.md가 "바꾼 이유를 문서에 남기는 쪽이 오히려 가산점"이라고 한 자리다.
- **되돌릴 수 없는 작업 앞에서 멈추고 물었다.** 볼륨 삭제를 `AskUserQuestion`으로 확인받았고, 선택지에 "그대로 둔다 — 다만 이게 네 번째 이월이 됩니다"까지 정직하게 적었다. 삭제 후 현행 데이터 무사 여부도 확인했다.
- **커밋을 작업 단위로 잘랐다.** [16:35](./2026-08-02_1635_retro.md) Try의 "커밋을 change 단위가 아니라 작업 단위로" 지적을 반영해 스펙 확정(`a802efc`, openspec 5파일만) · 개행+태스크화(`ab458b0`) · 볼륨 삭제(`1d99301`)를 나눴다. 네 회차 연속 미이행이던 항목이다.

### Problem (나빴던 것)

- **Codex의 medium 지적(결정성 동점 처리)이 반영도 기각도 되지 않은 채 남았다.** 스펙 51행은 여전히 "두 응답의 결과 개수·순서·`chunk_index`·점수가 모두 같다"를 요구하는데, 설계에는 tie-breaker 계약이 없고 저장소에 `n_results=top_k`만 요청한다. ANN 저장소가 동점·근접 점수에서 어떤 후보를 K개에 넣고 어떤 순서로 내는지는 보장되지 않는다. high 2건은 반영하고 medium은 **말없이 지나간 것**이 문제다 — 기각이라면 근거가 design에 있어야 한다. 게다가 이 요구사항은 스펙 21행이 직접 적었듯 **다음 change의 캐싱이 성립하기 위한 전제**다.
- **첫 `/opsx:propose`를 change 이름 없이 보내 중단이 났다.** 14초 손실 자체는 작지만, 슬래시 커맨드 인자 형식을 확인하지 않고 본문부터 쓴 것이다.
- **병합 커밋 `7804f85`가 65파일 +10077줄이다.** 브랜치 병합이라 성격상 불가피하지만, 커밋 메시지가 "Document Ingestion 파이프라인 완성" 한 줄뿐이라 평가자가 이 커밋에서 무엇이 들어왔는지 알려면 브랜치 로그를 따로 봐야 한다. 병합 커밋 본문에 change 링크나 요약이 없다.
- **하한 기본값(0.75)이 아직 초안이다.** proposal은 "계측으로 정하고 `ARCHITECTURE.md`에 남긴다"고 적었고 tasks 5.3이 이를 담고 있지만, 계측 표본은 문서 2개·질의 5개다. Risks에 과적합 위험을 적어 뒀으므로 은폐는 아니나, **이 change의 핵심 상수가 근거 없이 코드에 들어갈 위험**이 실행 단계에 그대로 남아 있다.
- **한 시간을 전부 계획에 썼고 실행 코드는 0줄이다.** 계획 품질은 높지만, 남은 평가 비중은 여전히 **72%**(RAG·스트리밍·캐싱·프롬프트)이고 그중 이 change가 덮는 것은 검색뿐이다. 하드 마감(08-09)까지 일주일이나, LLM 실호출·SSE·유사 질문 캐시는 아직 한 줄도 시작하지 않았다.

### Try (개선해야 할 것)

- **구현 착수 전에 결정성 요구사항을 결론 낸다.** 둘 중 하나다 — ① `(점수 내림차순, 안정적 청크 ID 오름차순)` tie-breaker를 API 계약에 넣고 후보를 K보다 넉넉히 받아 서비스에서 정렬하거나, ② 스펙 51행의 MUST를 "점수 집합이 같다"로 완화하고 **왜 순서까지는 보장하지 않는지**를 design에 적는다. 어느 쪽이든 캐싱 change가 이 답에 의존하므로 미루면 두 번 고친다.
- **적대적 리뷰 지적은 심각도와 무관하게 전부 명시적으로 닫는다.** high만 반영하고 medium을 흘리면 "검토했는데 기각"과 "못 보고 지나감"이 구분되지 않는다. tasks.md에 리뷰 지적 대조표를 두거나 design 각 결정 아래 "이 지적을 여기서 처리함"을 적는다.
- **하한 계측(tasks 5.3)은 구현 순서에서 앞당긴다.** 임계값이 API 응답 모양과 품질 테스트 단언을 동시에 결정하므로, 나중에 재면 그 위에 쌓은 테스트를 다시 고친다. 표본이 작다는 사실은 숫자와 **같은 문단**에 적는다.
- **다음 세션은 `/opsx:apply`로 시작해 코드를 낸다.** 계획 4개 아티팩트가 확정됐고 0장 이월도 비었다. 남은 일정에서 계획에 다시 한 시간을 쓰면, 미지의 영역(SSE 계약·claude-code-sdk 실호출)이 뒤로 밀린다.
- **병합 커밋에는 한 줄 요약 대신 change 이름과 아카이브 경로를 본문에 적는다.** 평가자가 `git log`만으로 "이 병합이 어느 스펙의 완료인가"를 알 수 있어야 한다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 (KST) | 세션 | 주요 활동 |
|------|------|----------|
| 08-02 16:26 | `2e6eb0ed` | Retrobot 자동 회고 생성 → [16:35 회고](./2026-08-02_1635_retro.md) |
| 08-02 16:42 | — | `document-ingestion` → `main` 병합 (`7804f85`, 65파일 +10077/-185), `add-qa-retrieval` 브랜치 분기 |
| 08-02 16:44~16:47 | `9df9a4d6` | `/opsx:propose` 첫 시도 중단(change 이름 누락) → 재입력. 기존 코드·아카이브 스펙 탐색 |
| 08-02 16:47~17:00 | `9df9a4d6` | proposal·spec·design·tasks 4개 생성 (`retrieval` capability) |
| 08-02 17:08~17:20 | `9df9a4d6` | 질의/문서 임베딩 경로 분리 요구사항, `top_k` 범위·422 시나리오 추가. "하한도 pydantic으로" 되돌림 |
| 08-02 17:22~17:24 | `9df9a4d6` + Codex | `/codex:adversarial-review` 백그라운드 실행 → `needs-attention` (high 2 · medium 1) |
| 08-02 17:27~17:32 | `9df9a4d6` | **TOCTOU 반영** — 질의 뒤 현재성 재검증, 재시도 아닌 드롭 (결정 13). 한계는 Risks로 캐싱 change에 인계 |
| 08-02 17:33~17:41 | `9df9a4d6` | **질의 길이 역산** — 507 가용 토큰 / 최악 5.016 토큰·문자 → 안전 상한 101자. 문자·토큰 두 겹 설계로 전환 (결정 6) |
| 08-02 17:43 | `9df9a4d6` | `openspec validate --strict` 통과 → `a802efc feat: add-qa-retrieval 스펙 확정` (+714, 5파일) |
| 08-02 17:44~17:46 | `9df9a4d6` | 직전 회고 Try 반영 — compose 개행 복구, 고아 볼륨 처분을 tasks 0장으로 (`ab458b0`) |
| 08-02 17:46~17:47 | `9df9a4d6` | `AskUserQuestion` 확인 후 고아 볼륨 2개 삭제, 현행 데이터 무사 확인 (`1d99301`) |
