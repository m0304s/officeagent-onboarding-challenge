# Retrobot 회고 — 2026-08-03 17:41 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 2 (작업 1 + Retrobot 훅 1) |
| 총 프롬프트 | 1 |
| 에러 발생 | 4 (작업 세션) + 4 (Retrobot 훅) |

직전 회고(17:10) 이후 새로 들어온 구간만 다룹니다. 목록의 나머지 로그 파일은 이전 회고에
이미 반영되어 있습니다.

## 세션별 활동

### 세션 `20a4b397` (17:03:48 ~ 17:31:50 KST, 28분) — `add-answer-generation` 3장

**프롬프트:**
1. `/opsx:apply` **"add-answer-generation 3장 LLM 어댑터구현 작업 진행해주세요."**

인터럽트 0회. 도구 57회 — Read 20 · Edit 16 · PowerShell 13 · Write 7 · Skill 1.
**Bash 0회.**

**결과:** `096d676` (+1692/-20, 11파일) — `adapters/llm/session.py`(396) · `codex.py`(336) ·
`pool.py`(114) · `__init__.py`(31) · `protocols.py`·`core/exceptions.py`·`api/errors.py` ·
`tests/fake_app_server.py`(188) · `test_llm_pool.py`(282) · `test_llm_session.py`(219) ·
`tasks.md`. 3장 19태스크 **전부 `[x]`**. 549 → **578 passed in 48.21s** (신규 29건,
구독·네트워크 없이 3.51초), `ruff check`·`format --check` 통과.

**발생한 에러:**
- `Exit code 49` (17:22:26) — 호스트 `python --version` → `Python was not found`.
  **직전 세션이 47분을 태워 얻은 결론을 40분 만에 다시 밟았다.**
- `Exit code 1` (17:24:56) — `2 failed, 576 passed`. `test_핸드셰이크_실패는_생성_실패다`,
  `test_순회를_중간에_멈추면_정리된다`
- `Exit code 1` (17:26:58) — `ruff check` **`Found 4 errors.`** (E501 109·108·103자)
- `Exit code 1` (17:27:30) — **같은 `Found 4 errors.` 재발.** 3초 만에 돌아왔다

### 세션 `5d2c4c70` (17:03:43 ~ 17:10:46 KST, 7분) — Retrobot 훅

직전 회고(`3495494`) 생성. 에러 4건 — 직전 훅(13건)보다 줄었다.

## KPT 회고

### Keep (좋았던 것)

- **셸 혼용이 일곱 세션 만에 끊겼다.** Bash 0회 · PowerShell 13회. 8/2 21:05부터 여섯 세션
  연속으로 매번 나던 `cd` 접두사 승인 프롬프트도, `MSYS_NO_PATHCONV` 경로 변환 사고도 이번엔
  없다. 직전 회고가 47분(세션의 65%)을 태운 원인으로 지목한 바로 그 형태다. **다만 규칙은
  여전히 `CLAUDE.md`에 없다** — 이번 세션이 우연히 한 쪽만 쓴 것인지 학습한 것인지 구분할
  근거가 로그에 없고, 다음 세션이 자동으로 읽는 파일에는 아무것도 안 적혀 있다.

- **가짜 app-server가 첫 실행에서 실패 2건을 냈다.** `test_핸드셰이크_실패는_생성_실패다`와
  `test_순회를_중간에_멈추면_정리된다` — 둘 다 **실물 CLI로는 재현할 수 없는 경로**다.
  `initialize`에 응답하지 않는 서버, 소비자가 순회를 중간에 버리는 상황은 정상 동작하는
  Codex에 요청해서는 만들 수 없다. 15:29 설계가 `tests/fake_app_server.py`를 "실물이 닿지
  못하는 곳"이라 규정하며 6개 고장 플래그를 판 근거가, 만든 지 30분 만에 실패 2건으로
  회수됐다. 스펙에 적힌 이유가 실행되는 증거가 된 회차다.

- **29건 전부가 구독·네트워크 없이 3.51초에 돈다.** `CLAUDE.md`가 "LLM 호출은 목/스텁으로
  대체 가능하며, **구독 없이 실행되면 가산점**"이라 적은 조건을, 어댑터 층 — 즉 구독이 가장
  필요해 보이는 층 — 에서 실측으로 충족했다. 평가자가 인증 없이 `docker compose run --build
  --rm test` 한 줄만 쳐도 LLM 어댑터의 수명·타임아웃·401 판정까지 초록을 본다.

- **환경 격리를 말이 아니라 단언으로 고정했다.** 3.4의 "환경은 상속하지 않고 `HOME`·`PATH`·
  `CODEX_HOME`만 넘긴다"는 결정을, 가짜 서버가 `initialize` 응답에 **자기 환경을 되비쳐 주고**
  부모에 심어 둔 센티널이 거기 없음을 단언하는 방식으로 검증했다. 환경 누수는 오류를 내지
  않는다 — 로컬에서는 잘 돌고 컨테이너에서만 다르게 도는 형태로만 드러난다.

- **타임아웃 회차는 프로세스가 살아 있어야 통과하게 짰다.** "상한이 세션을 죽이지 않는다"는
  3.11의 결정을 뒤집으면 테스트가 빨개진다. 상한 초과 시 `turn/interrupt` 후 유예 안에
  `turn/completed`가 오면 반납한다는 규칙은, 반대 방향(무조건 폐기)으로 구현해도 사용자에게
  보이는 증상이 같기 때문에 단언으로 못 박지 않으면 다음 리팩터링에서 조용히 뒤집힌다.

- **28분에 19태스크.** 프롬프트 1건·인터럽트 0회, 대기 구간 없음. 2장(72분 중 47분 대기)과
  같은 분량인데 시간은 3분의 1이다. 차이는 전부 셸 쪽에서 났다.

### Problem (나빴던 것)

- **`--build` 없는 검증 2회가 이미 고친 결함을 다시 보고했다.** 17:26:47 `ruff check` →
  `Found 4 errors`. 17:27:03~17:27:24에 `codex.py`·`session.py`·`fake_app_server.py`·
  `test_llm_session.py` 4곳을 고쳤다. 17:27:27 재실행 → **같은 `Found 4 errors`, 3초 만에.**
  그리고 소스는 한 글자도 안 고친 채 17:27:34에 `--build`만 붙였더니 통과했다.
  `docker-compose.yml`의 `test` 서비스에는 **소스 볼륨 마운트가 없다**(93~105줄) — 코드는
  빌드 때 이미지에 구워진다. `--build` 없는 `docker compose run`은 **이전 이미지의 코드를
  검사한다.** 더 나쁜 것은 `README.md` 281·311·312줄에 `--build`가 **이미 적혀 있다**는
  점이다. 자기가 쓴 실행 절차를 자기가 안 따라서 2분을 태웠고, 3초 만에 돌아온 결과를
  의심하지 않았다. 이 함정은 구조적이라 다음에도 똑같이 밟는다.

- **직전 회고 Try 5건 이행 0건 — 두 회차 연속 0.** 직전 회고는 "회고에만 적힌 Try는 여덟 번
  연속 이행되지 않았고 `tasks.md`에 적힌 것만 이행됐다"고 진단한 뒤 **모든 항목에 "어느
  파일의 어느 줄"까지 지정**했다. 그래도 0건이다.
  - #1 `CLAUDE.md` 셸 규칙 4줄 → 미이행 (파일 무변경)
  - #2 README `/search` 예시 교체를 `tasks.md` 3장 태스크로 → 미이행 (3장은 3.19에서 닫힘)
  - #3 `tasks.md` 3장 머리에 `549 passed` 기준선 → **다섯 회차째 미이행**
  - #4 3장 직후 Codex 적대적 리뷰를 `tasks.md` 3.17로 → 미이행 (3.17은 `test_llm_session.py`)
  - #5 `retrobot/SKILL.md` 우회 절차 신설 → 미이행

  **지정을 더 정밀하게 하는 것으로는 안 고쳐진다는 것이 이번에 증명됐다.** 남은 변수는 하나다
  — 첫 프롬프트에 실리는가. 이번에도 첫 프롬프트는 "3장 LLM 어댑터구현 작업 진행해주세요."
  한 줄이었다.

- **1,692줄 구현에 외부 검토 0회 — 두 세션 연속.** 그 안에 `session.py` 396줄이 있고,
  거기 담긴 것이 3.6(사망 판정과 `terminate`→유예→`kill`→**반드시 `wait()`**)·3.7(stderr
  미소비로 **어느 순간 조용히 멎음**)·3.14(애매한 세션을 반납하면 **다음 요청이 이전 턴의
  델타를 받는다**)다. 셋 다 **테스트가 초록인 채로 통과할 수 있는 종류**다 — 좀비 프로세스,
  버퍼 포화, 세션 오염은 스위트가 끝난 뒤에 나타난다. 14:16 회고가 세운 Codex 적대적 리뷰
  관행은 스펙 단계에만 붙고 구현 단계에는 여전히 한 번도 안 붙었다.

- **`openspec validate --strict`를 안 돌리고 커밋했다.** 2장 세션(`371350c`)은 돌렸다.
  이번엔 `tasks.md` 19줄 전부에 근거 주석(`→ ...`)을 새로 붙였는데 — 즉 아티팩트를 가장 많이
  건드린 회차인데 — 검증 없이 17:31에 커밋했다. `tasks.md` 갱신(17:30:53) 이후 남은 도구
  호출은 `openspec instructions apply` 하나뿐이다.

- **호스트 `python --version`을 또 쳤다** (17:22:13). 직전 세션이 이 한 줄에 47분을 태우고
  `Python was not found`(Windows Store alias)를 받았다. 40분 뒤 같은 세션 계열에서 같은 줄이
  다시 나왔고 같은 exit 49를 받았다. 이번엔 즉시 실패해서 13초만 잃었지만, **환경 사실이
  세션 사이에 전달되지 않는다**는 것은 그대로다. 이것도 `CLAUDE.md`에 한 줄이면 끝난다.

- **README `/search` 예시의 손으로 쓴 `0.8734`·`chunk_index: 1`이 아홉 번째 이월.** 이번
  세션은 README를 열지도 않았다. 8/2 21:14부터 아홉 회차 연속이고, 직전 회고가 "파일을 여는
  것으로는 안 고쳐진다"고 진단한 뒤 이번엔 열지도 않는 쪽으로 갔다. `CLAUDE.md`가 문서-코드
  불일치를 감점이 아니라 **불합격**으로 규정한 항목에 해당할 수 있는 유일한 잔여물이다.

- **Retrobot 샌드박스 벽 19번째 재발.** 이 회고를 만드는 동안 거부 13건 — `/tmp`·`%TEMP%`
  Write 2건, Bash heredoc, `python -c` 3건(Bash·PowerShell 양쪽), PowerShell 965바이트 제한,
  `ls`/`cd` 디렉터리 접근 2건. 이번에 **새로 확정된 사실 하나**: 호스트에 python이 아예 없다
  (`py`·`python3` 모두 Store alias, `jq`도 없음). 결국 Glob + Grep `-o`만으로 집계했다 —
  15:52 회고가 "우회로 소멸"이라 적고 17:10 회고가 "`SKILL.md`에 적기로" 한 그 절차를,
  **19번째로 처음부터 다시 찾았다.**

### Try (개선해야 할 것)

앞선 두 회차의 Try는 "어느 파일 어느 줄"까지 지정하고도 0/5, 0/6이었다. 지정 정밀도는 원인이
아니다. **따라서 아래는 순서를 지정한다 — 1번은 4장 착수 프롬프트보다 먼저 끝낸다.**

1. **다음 프롬프트를 치기 전에 `CLAUDE.md`에 다섯 줄을 박는다.** 세 회차 연속 미이행 항목이고,
   다음 세션이 자동으로 읽는 파일은 이것뿐이다.
   - Bash는 POSIX(Git Bash), PowerShell은 5.1 — 한 명령에 섞지 않는다
   - **호스트에 python·jq가 없다** (`python`/`py`/`python3`는 Windows Store alias, exit 49)
   - 컨테이너 검증은 **반드시 `--build`** — `test` 서비스에 소스 마운트가 없어 없으면 이전
     이미지를 검사한다
   - PowerShell에서 네이티브 exe에 `2>&1` 금지
   - 컨테이너 경로 인자에는 `MSYS_NO_PATHCONV=1`

   Problem 1·3과 Keep 1의 지속성을 한 번에 친다.

2. **`docker-compose.yml`의 `test` 서비스에 `- ./src:/app/src`·`- ./tests:/app/tests`를
   추가하거나, 없다면 그 이유를 주석으로 남긴다.** 이번 2분은 우연이 아니라 구조가 만든
   것이다. 마운트가 평가 환경(`--build` 1회)에 무해하다면 붙이는 쪽이 맞고, 굽는 것이
   의도라면 그 의도가 파일에 적혀 있어야 다음에 `--build`를 빠뜨리지 않는다.

3. **`tasks.md` 4장 머리에 `578 passed (2026-08-03 17:31, 096d676)` 한 줄.** 여섯 회차째다.
   4장은 QA 서비스 오케스트레이션이라 기존 테스트 무영향이 다시 합격 조건이 된다 — 기준선이
   없으면 그 조건을 판정할 수 없다.

4. **`tasks.md` 4장에 "3장 구현 Codex 적대적 리뷰" 태스크를 번호로 추가한다.** 대상은 이미
   커밋된 `session.py`·`pool.py`·`codex.py`의 3.6·3.7·3.14 — 좀비 회수, stderr 미소비,
   반납 불변식. 리뷰어에게 **"구현 단계이고 대상은 커밋 `096d676`"** 임을 먼저 말한다
   (14:16 회고에서 단계를 안 밝혀 지적 2건이 헛돌았다). Try에만 있으면 열 번째 미이행이 되므로
   4장 태스크 목록 안에 넣는 것이 조건이다.

5. **`retrobot/SKILL.md`에 "이 환경의 우회 절차" 절을 신설한다.** Windows + 샌드박스에서
   Bash·PowerShell·python·node·jq가 전부 막히거나 없으므로 **Glob으로 파일을 찾고 Grep의
   `-o`·`head_limit`·`offset`으로만 집계한다**. 타임스탬프는 `head -n 8` / `tail -c 130`으로
   양 끝만 뽑는다. 19회 반복된 탐색을 지침 한 절로 끝낸다.

6. **README `/search` 예시 교체를 4장 태스크로 옮긴다.** 아홉 번 이월된 유일한 이유는 회고에만
   있었기 때문이다. 4장은 `/qa` 경로를 만드는 장이라 README를 어차피 연다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 17:03~17:10 | `5d2c4c70` | Retrobot 훅 — 직전 회고 생성 (`3495494`), 에러 4건 |
| 17:03:48 | `20a4b397` | 세션 시작 (직전 커밋 `371350c`와 같은 분) |
| 17:04 | `20a4b397` | `/opsx:apply` 3장 LLM 어댑터 구현 요청 (프롬프트 1건, 이후 인터럽트 0) |
| 17:05~17:12 | `20a4b397` | 정찰 Read 20회 — `protocols.py`·`exceptions.py`·`errors.py`·design·tasks, 1B 픽스처 `app_server_answerable.jsonl` 38~57줄 확인 |
| 17:13~17:15 | `20a4b397` | 3.1~3.3 Edit — `AnswerGenerator` 계약, `LLM_UNAVAILABLE`·`LLM_UNAUTHENTICATED`, `_CODE_TO_STATUS` 503 |
| 17:16~17:20 | `20a4b397` | 3.4~3.14 Write — `session.py`(396) · `pool.py`(114) · `codex.py`(336) |
| 17:19~17:22 | `20a4b397` | 3.16~3.19 Write — `fake_app_server.py`(188) · `test_llm_session.py`(219) · `test_llm_pool.py`(282) |
| 17:22:13 | `20a4b397` | 호스트 `python --version` → **exit 49** `Python was not found` (직전 세션과 동일) |
| 17:23:04 | `20a4b397` | `docker compose run --build --rm test` → 17:24:56 **2 failed, 576 passed** |
| 17:25~17:26 | `20a4b397` | `fake_app_server.py` 3회 · `test_llm_pool.py` 수정 → `--build` 재실행 → **29 passed in 3.51s** |
| 17:26:47 | `20a4b397` | `ruff check`(**`--build` 없음**) → `Found 4 errors` (E501) |
| 17:27:03~24 | `20a4b397` | `codex.py`·`session.py`·`fake_app_server.py`·`test_llm_session.py` 수정 |
| 17:27:27 | `20a4b397` | 재검증(**`--build` 없음**) → **3초 만에 같은 `Found 4 errors`** — 이전 이미지의 코드 |
| 17:27:34 | `20a4b397` | 소스 무변경 + `--build`만 추가 → 17:29:23 **578 passed in 48.21s**, ruff 통과 |
| 17:30:53 | `20a4b397` | `tasks.md` 3.1~3.19 전부 `[x]` + 근거 주석 19줄 (`validate --strict` 미실행) |
| 17:31 | — | 커밋 `096d676` "feat: LLM 어댑터 생성" (+1692/-20, 11파일) |
