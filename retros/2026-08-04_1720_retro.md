# Retrobot 회고 — 2026-08-04 17:20 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 15 (작업 8 · Retrobot 훅 7) |
| 총 프롬프트 | 37 |
| 에러 발생 | 70 (그중 Retrobot 훅 44건 = 63%) |
| 이번 커밋 | `1df833b feat: BM25 연동` (8파일 +178/-38, 17:17:16) |
| 테스트 | 776 → **779 passed, 2 deselected** (71.93s) |
| 진행 | `add-rrf-algorithm-spec` 41/50 → **50/50** |

16:36 이전 세션(`20a4b397`·`ce0bf3da`·`dd40c3ba`·`c4df5f7c`·`b2ecdd45`·`548929a4`·`2b0b7d11`)은
직전 회고들이 이미 다뤘습니다. 이번 회고는 **`976130a0`**(16:36~, 커밋 17:17)을 중심에 둡니다.

## 세션별 활동

### 세션 `976130a0` (16:36 ~ 17:17 KST, 41분)

**프롬프트:**
1. `/opsx:apply add-rrf-algorithm` — "검색 단계에서 사용되는 캐시 로직을 삭제 진행할 예정입니다.
   해당 작업은 추후 질의 단계에서 사용될 …"
2. (재호출 1건 — 인자 전달)
3. 168시간 창 기준에 대한 정정 — "창은 첫 실제 커밋부터다"

**한 일:** 태스크 5.8 + 8장 전체(8.1~8.8)를 마감해 change 를 50/50 으로 닫음.

- `retrieval_cache_size` 제거 — `config.py`·`test_config.py`·`test_documents.py` docstring 세 곳.
  두 회고 연속 Problem 이던 죽은 손잡이.
- `tests/test_retrieval_quality.py` 를 **실물 FTS5 를 쓰는 하이브리드 구성**으로 전환.
  `harness` 가 `SqliteLexicalIndex` 를 실제로 세우고, 밀집 단독은 같은 저장소 위에
  `searching_with(retrievers=("dense",))` 로 따로 세워 비교.
- 신규 테스트 3건 — `P3`·`hotfix`·`develop` 처럼 문맥 없는 식별자 질의가
  배포 하한 `0.82` 에서 밀집 단독은 **0건**, 하이브리드는 찾는다는 것을 회귀로 고정.
- `ARCHITECTURE.md` +113/-9 — 파이프라인 다이어그램 재작성, RRF 선택 근거(가중합·필터 기각),
  정규화 분모를 접지 않는 부동소수점 근거, 점수 의미 변경(BREAKING) 표,
  구제 실측표 6행, 가중치 실측, 검색 결과 캐시 묘비.
- 실물 기동 검증 — `docker compose up -d --build --wait` → `./data` 비우고 `sample-docs/` 2건 업로드
  → `/search` → `/qa` SSE(`event: sources` + `answer` 델타 다수) 확인.
- `CLAUDE.md` +9 — `docker compose run` 에 넘긴 인자가 `CMD` 를 통째로 갈아치운다는 함정 기록.

**발생한 에러:** 7건

- `check_comments.py` Exit 1 ×3 — `src/app/config.py:91` 인라인 주석 3줄, `tests/api_harness.py:1`
  docstring 6줄, `tests/test_retrieval_quality.py` 강조 마크업 다수. 자기 추가분 +4 는 커밋 전에 0 으로 복구
- `/usr/bin/bash: here-document at line 5 delimited by end-of-file (wanted 'PY')` —
  `SKILL.md` 가 "하지 말 것"으로 명시한 heredoc 에 또 부딪힘
- `Exit code 1` ×2 (openspec 상태 조회), 위반 총계 확인 1건

## KPT 회고

### Keep (좋았던 것)

- **스텁과 실물의 방향이 반대인 이유를 그 자리에 적었다.** 벡터 저장소는 스텁, 어휘 색인은 실물
  SQLite 다. 근거는 "검증 대상이 BM25 순위와 희소도 하한 자체라, 대역이 토큰 교집합으로 흉내 내면
  재는 것이 대역이 된다" — 저장소의 근사 검색은 잡음이지만 어휘 순위는 검증 대상 그 자체이기 때문.
  같은 파일 안에서 정반대 선택을 하면서 그 비대칭을 설명하지 않으면 다음 사람이 하나로 통일한다.

- **구제 사례 테스트가 전제가 깨질 때 빨개진다.** `alone.count == 0` 을 단언해
  밀집 단독이 식별자를 **찾으면 실패**하게 만들었다. 임베딩 모델이 바뀌거나 하한이 내려가면
  "이건 더 이상 구제 사례가 아니다"를 테스트가 먼저 말한다. 하이브리드 쪽만 단언했다면
  전제가 사라진 뒤에도 조용히 초록이었을 것이다.

- **가중치를 실측하고 "바꿀 근거를 못 만들었다"를 결론으로 적었다.** `lexical` 가중치
  `0.5`/`1.0`/`2.0` 에서 여덟 질의의 판정이 **전부 같았고**, 그 이유(청크 10개뿐이라 두 목록이 같은
  순서를 꼽고 가중치가 분자·분모에서 상쇄)까지 문서에 남겼다. 관측되지 않는 차이로 값을 고르지 않은 것.

- **직전 회고의 Problem 두 건이 이행됐다.** "계획에 적어 둔 실물 기동 검증 미실행"은
  `up -d --build --wait` + `/search`·`/qa` 실측으로, "`retrieval_cache_size` 죽은 손잡이"는 삭제로.
  둘 다 `tasks.md` 에 번호를 갖고 있던 항목이었다 — 회고에만 있던 Try 와 다른 점.

- **삭제한 설계에 묘비를 남겼다.** 검색 결과 캐시를 "왜 안 넣었나"로 `ARCHITECTURE.md` 에 남기고,
  근거를 그 설계 자체에서 끌어왔다 — "캐시를 꺼도 결과가 같다. 이 계층은 지연이지 정확성이 아니다."

- **새 함정을 발견한 자리에서 `CLAUDE.md` 에 넣었다.** `docker compose run --build --rm test <경로>` 가
  `exec format error` 를 내는 이유(`CMD` 를 인자가 갈아치움)와 올바른 형태를 함께.

### Problem (나빴던 것)

- **168시간 창 계산이 틀렸고, 네 회고가 그 위에 세워졌다.** 기준으로 삼은
  `1313b0a Initial commit`(2026-07-28 15:11:44)은 GitHub 템플릿 생성 커밋이고
  (author `MinSeok_Choi <120546936+m0304s@users.noreply.github.com>`), 실제 작업 첫 커밋은
  `a88295c`(2026-08-01 12:32:50, `최민석 <cms8955@naver.com>`)다. 창은 **2026-08-08 12:32 KST 마감**,
  지금 약 **3일 19시간** 남았다.
  11:03·11:45·12:27·16:40 **네 회고 연속** 잘못된 "오늘 15:11 마감"을 실었고,
  특히 16:40 회고는 **"커밋이 창 밖으로 1시간 25분 초과"를 헤드라인으로** 삼아 전체 구성을
  그 위에 세웠다. 그 마감선은 존재하지 않았다. 정정은 절차가 아니라 사용자 지적에서 나왔다.
  덧붙여 이 해석은 아직 **확정된 것이 아니다** — `CLAUDE.md` 는 "첫 커밋으로부터 168시간,
  판정은 committer date 기준"이라 적었고 `1313b0a` 가 문자 그대로 첫 커밋이다.

- **`LLM 응답 캐싱` 이 여전히 코드에 없다 — 여덟 회고 연속.**
  `services/qa.py` 에 캐시 0건, `adapters/cache/` 는 `probe.py` 뿐. 평가 비중 18% 이자
  불합격 기준("벡터 검색 또는 캐시 DB 미구현")이다. 이번 41분도 전부 요구사항 밖 확장인
  하이브리드 검색의 마무리에 갔다.
  **창이 3일 19시간 남았다는 정정은 오히려 이 항목을 더 아프게 만든다** — 여덟 회차 동안
  착수하지 못한 원인이 시간 부족이 아니라 우선순위였음이 드러났기 때문이다.

- **커밋 메시지가 내용과 어긋난다.** `1df833b` 는 `feat: BM25 연동` 인데 `src/` 변경은
  `retrieval_cache_size` **삭제 2줄뿐**이고 나머지는 문서(+113)·테스트(+61)다. BM25 구현 자체는
  `4d370fc`(11:03)에 들어갔다. 게다가 커밋은 사람이 쳤고 에이전트는 나중에 발견했다 —
  로그에 `"1df833b feat: BM25 연동 exists and I didn't create it"`.

- **change 이름 실수 일곱 회차 연속.** 첫 프롬프트가 또 `add-rrf-algorithm`(브랜치명)이고
  실제 change 는 `add-rrf-algorithm-spec`. 직전 회고가 Keep 으로도 Problem 으로도 적었지만
  `CLAUDE.md` 에는 여전히 이 한 줄이 없다.

- **미push 6건.** origin 은 `a4be966`(11:36) 그대로다. push 는 세 회고 연속 Try 였다가 11:45 에
  한 번 이행됐고, 이후 다시 6건이 쌓였다. 창이 열려 있다는 정정이 압력을 낮춘 형태다.

- **`check_comments.py` 리포 전체 459건(97파일)이 기준선으로 방치돼 있다.** 이번 추가분 +4 는
  커밋 전에 0 으로 되돌렸으나, `CLAUDE.md` 는 **인자 없는 실행**을 판정으로 규정한다.
  즉 문서에 적힌 게이트가 지금 통과하지 않는다 — `README.md:312` 가 실패했던 것과 같은 모양.

- **Bash 60건이 전부 `cd ` 접두사다.** 직전 회고가 "`cd` 가 살아남아 뒤의 상대 경로를 깼다"를
  Problem 으로 적었는데 형태가 그대로다. 이번엔 사고로 이어지지 않았을 뿐이다.

- **외부 검토 0회 — 열다섯 세션 연속.** `ARCHITECTURE.md` +113 에 RRF 정규화·부동소수점 동치·
  동점 전순서 같은 "틀려도 오류가 안 나는" 설명이 들어갔는데 아무도 읽지 않았다.

- **Retrobot 훅이 이번 24시간 에러의 63%(44/70).** heredoc·`command -v`·`ls -la` 등이 반복 거부되고,
  통한 것은 `Glob`·`Grep -o`+`head_limit`·`Grep offset` 이분 탐색뿐. 회차마다 같은 우회로를
  처음부터 재발견하고 있다.

### Try (개선해야 할 것)

1. **다음 세션의 첫 번째이자 유일한 작업을 `LLM 응답 캐싱` 으로 고정한다.**
   `add-llm-response-cache` change 를 새로 열고, 하이브리드·검색 쪽 작업은 열지 않는다.
   여덟 회차 미착수의 원인이 시간이 아니라 우선순위였음이 확정됐으므로, 고칠 것은 일정이 아니라 순서다.
2. **168시간 창 기준을 `CLAUDE.md` 에 커밋 해시와 함께 못 박는다** —
   "`a88295c`(2026-08-01 12:32:50 KST)부터 168시간, **2026-08-08 12:32 마감**.
   `1313b0a` 는 템플릿 생성 커밋이라 제외." 동시에 **이 해석이 평가자 기준과 같은지 확인이 필요하다**는
   사실도 같은 줄에 적는다 — 확정된 것처럼 적으면 같은 종류의 오류가 반대 방향으로 반복된다.
3. **`CLAUDE.md` 에 "브랜치명 ≠ change 이름 — `add-rrf-algorithm-spec`" 한 줄을 넣는다.**
   일곱 회차 연속이고 회고 Keep/Problem 은 이 행동을 바꾸지 못했다. 회고가 아니라 세션이 읽는 파일에 넣는다.
4. **`git push` 를 먼저 한다.** 미push 6건. 창이 남았어도 원격에 없는 산출물은 평가자에게 없는 것이다.
5. **커밋 메시지를 diff 와 맞춘다.** 코드 순증이 0 이면 `docs:`/`test:` 를 쓴다. `1df833b` 의 `feat:` 는
   "구현했다고 적었으나 코드가 없다"와 같은 방향의 오해를 만든다.
6. **`check_comments.py` 판정 문장을 실행 가능한 형태로 고친다.** 리포 전체 459건을 0 으로 만들 계획이
   없다면 `CLAUDE.md` 의 판정을 `python3 scripts/check_comments.py src tests` 로 바꾼다.
   지금은 문서에 적힌 명령이 항상 실패하는 상태다.
7. **`SKILL.md` 의 "먼저 확인" 절을 이 샌드박스에서 실제로 통하는 절차로 교체한다** —
   `command -v` 도 heredoc 도 막히므로, `Glob` → `Grep count` → `Grep -o` + `offset` 이분 탐색을
   1순위 절차로 올린다. 세 회고 연속 미이행 항목이다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 (KST) | 세션 | 주요 활동 |
|------|------|----------|
| 08-03 17:03 | `20a4b397` | `add-answer-generation` 3장 LLM 어댑터 |
| 09:13 | `548929a4` | rank-fusion 스펙 모순 점검 (브레인스토밍) |
| 09:43 | `ce0bf3da` | `add-rrf-algorithm-spec` 2장 어휘 토큰화 |
| 10:09 | `dd40c3ba` | 어휘 색인 어댑터 · 색인 서명 · 수집 통합 |
| 10:59 | `c4df5f7c` | Retriever 계약 · 구현 · 검색 서비스 팬아웃 |
| 11:37 | `b2ecdd45` | API 로직 연결, `.env` → `config.py` 전환 |
| 14:28 | `2b0b7d11` | 8장 캐시 스펙 설계 → 16분 뒤 철거 |
| **16:36** | **`976130a0`** | **5.8 + 8장 8건 마감, 하이브리드 회귀·실측·실물 기동 검증** |
| **17:17** | — | **`1df833b feat: BM25 연동` — 50/50, 779 passed** |
| 17:17~ | `976130a0` | 168시간 창 기준 정정, `/opsx:archive` 착수 |
