# Retrobot 회고 — 2026-08-03 15:29 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 9 (작업 5 + 서브에이전트 1 + Retrobot 3) |
| 총 프롬프트 | 24 (사용자 발화 기준, 슬래시 명령 래퍼·시스템 리마인더 제외) |
| 에러 발생 | 35 (작업 세션 22 + Retrobot 세션 13) |
| 커밋 | 8 (구현/문서 4 + Retrobot 자동 회고 4) |
| 창 | 최근 24시간 = 08-02 15:29 ~ 08-03 15:29 KST. **로그 엔트리는 전부 08-03 09:52 이후에 몰려 있다** |

> 이 창은 직전 세 회고(12:31 · 14:16 · 15:15)와 겹친다. 겹치는 구간은 요약만 하고,
> **직전 회고 이후의 증분(15:07~15:26, 커밋 `8e503fc`)** 을 KPT의 중심에 둔다.

## 세션별 활동

### 세션 `a58ce1f7` (09:52 ~ 11:24) — Makefile 제거와 Codex SDK 전환

**프롬프트:**
1. (superpowers 스킬 진입) 실행 경로를 `docker compose up` 한 줄로
2. "docker daemon 켰습니다. 실제로 검증 진행해줏에ㅛ."
3. "자격 증명은 자동으로 세팅이 되어야 합니다.(사용자가 스크립트 실행하면 안됩니다.[MUST-NOT])"
4. "사용자는 단순히 docker-compose up 을 진행하되, 해당 설정 파일 내에서 스크립트를 실행하는 방식은 없나요?"
5. "현재 macOS에 접속하여 확인해본 결과, `~/.codex/auth.json` 파일이 존재합니다. … Windows/Linux/MacOS 전부 마운트하여 인증하는 방식으로"
6. "스트리밍 이벤트는 구현할 필요가 없습니다. 지금 세션에서 원하는 것은 claude-code-sdk·Makefile로 되어있는 내용을 docker-compose.yml·Codex SDK 로 전환하는 것입니다."

**발생한 에러:** 13건
- Docker 데몬 미기동(`npipe:////./pipe/dockerDesktopLinuxEngine`)
- `sh: 1: Syntax error: Unterminated quoted string` / `sh: 1: Syntax error: "(" unexpected` — PowerShell·sh 혼용
- `Python was not found` (Windows 앱 실행 별칭)
- `stream disconnected before completion` (Codex `chatgpt.com/backend-api`) ×2
- `2 failed, 415 passed` → `test_boot.py` · `test_harness.py` 회귀 2회 반복
- `1 file would be reformatted` (포맷 exit 1)
- 업로드 왕복 중 `unsupported_document_format` / `internal_error: There was an error parsing the body` — 셸 인용부호가 깨진 curl

**결과:** `f1cc26f`·`e0da77e` "fix: Docker Compose 전환"(+419/-264), 414→**418 passed**

### 세션 `02b371b1` (11:29 ~ 12:26) — `add-answer-generation` 스펙 4개 아티팩트

**프롬프트:**
1. `/opsx:propose add-answer-generation` — "검색된 청크를 근거로 LLM이 답변을 생성하는 것까지. PRD §2의 생성 부분과 §4 프롬프트 설계. LLM 활용(18%)과 프롬프트 엔지니어링(13%)이 이 change에 걸려 있다."
2. "spec에 요구사항 추가 —"
3. (중간 인터럽트 1회 후 재실행)

**발생한 에러:** 4건 — `TaskCreate` 스키마 위반, `openspec validate --change` 옵션 오인(`--changes`), 권한 거부 2건

**결과:** `78e4ac4` "docs: add-answer-generation 스펙 분석"(+870)

### 세션 `0d343c07` (12:26 ~ 12:32) — 훅 상태 확인

**프롬프트:**
1. "`git config core.hooksPath .githooks` (Retrobot 활성화) 되어있나요?"
2. "지금 커밋 했는데, 회고작업 진행되고 있는지 확인햊수에ㅛ"

**발생한 에러:** 0건. **작업 세션이 아니라 훅이 조용해서 생긴 확인용 세션이다.**

### 세션 `076cba3d` + 서브에이전트 `agent-a7c4bdc3` (13:57 ~ 14:10) — Codex 적대적 리뷰

**프롬프트:**
1. "codex 모델을 사용하여 add-answer-generation change에 대한 적대적 리뷰 진행해주세요."
2. "Minor1은 진짜입니다. 수정 진행해주세요. Major2는 절반만 맞습니다. 캐싱이 PRD 필수이지만, 해당 change에선 일부러 제외했습니다. … answer-generation 기능이 브랜치에 통째로 없다(는 지적은 스펙 확정 단계라 정상)"

**발생한 에러:** 0건

**결과:** `bd59e24` "docs: ARCHITECTURE 문서 업데이트"(+4/-4)

### 세션 `1f98f356` (14:10 ~ 15:26) — 1장 실측 → 표면 전환 → 스펙 정리

**프롬프트:**
1. `/opsx:apply Task 1 작업 진행해주세요.`
2. `/superpowers:brainstorming` **"codex 응답을 streaming으로 오게 할 수 없나?"**
3. "C로 가겠습니다." (선택지 중 app-server 실측안)
4. "진행해주세요." / "맞습니다. 그 다음 작업 진행해주세요" / "진행해주세요" / "작업 진행햊수에ㅛ." / "수정 진행해주세요."
5. `/opsx:update add-answer-generation` **"Codex Streaming 형식에 맞게 스펙 문서 업데이트해주세요."**
6. "수정해주세요"

**발생한 에러:** 5건
- `sh: 0: cannot open C:/Users/SSAFY/AppData/Local/Temp/probe.sh: No such file` — 윈도우 경로를 컨테이너 sh에 그대로
- `Python was not found` (호스트 python 별칭)
- `docker cp` 시 `GetFileAttributesEx C:\tmp: The system cannot find the file specified`
- `json.decoder.JSONDecodeError: Expecting value: line 1 column 1`
- `FileNotFoundError: '/tmp/schema/AgentMessageDeltaNotification.json'`

**도구 사용:** Bash 60 · Edit 45 · Write 7 · Read 6 · TaskUpdate 7 · TaskCreate 4 · AskUserQuestion 2 · Grep 1 · ToolSearch 1

**결과:** `441a03f`(+183/-16) · `9bbe7db`(+342) · **`8e503fc`(+178/-59)**

### Retrobot 세션 `10c7baad`(12:26) · `cd30845c`(14:09) · `26fb8d3e`(15:07)

**발생한 에러:** 각 17 · 11 · 13건 — 대부분 `/tmp` 쓰기·`python3`·`node`·PowerShell 스크립트 블록 거부.
**이번 회차(`318fd856`)도 동일**: `cat`+heredoc 거부, `python -c` 거부, `PowerShell` 스크립트 블록 거부, 로그 디렉터리 `ls` 거부 → 결국 **Glob·Grep만으로 집계**.

## KPT 회고

### Keep (좋았던 것)

- **계약을 "0회 이상"으로 열어 둔 덕에 표면을 통째로 갈아치우고도 `spec.md`가 0줄 바뀌었다.**
  `8e503fc`는 `design.md`·`proposal.md`·`tasks.md` 셋만 건드렸다. `codex exec` → `codex app-server`는
  프로세스 모델·프로토콜·인증 판정·타임아웃 처리가 전부 바뀌는 교체인데, `AnswerGenerator.generate()`가
  `AsyncIterator[str]`이고 SSE `answer` 이벤트가 "0회 이상"이라 **어댑터 안쪽에서 끝났다.**
  좋았던 이유는 유연해서가 아니라, 그 유연함이 **1장 실측(“exec은 델타를 안 준다”)의 결과로 의도해서 열린 문**이었기 때문이다.

- **뒤집힌 1장을 지우지 않고 `1-B`로 이어 붙였다.**
  `tasks.md`에 "**1장을 지우지 않는 이유는 exec 숫자가 곧 갈아탄 근거이기 때문이다**"를 명시했다.
  실측 회차를 덮어쓰면 다음 사람은 "왜 app-server인가"에 답할 수 없다. 12~19초 vs 4.5초, 1이벤트 vs 112델타는
  **비교 대상이 남아 있어야만 근거**가 된다.

- **되돌리기 비용을 미리 값으로 적었다.** `app-server`가 `[experimental]` 표기라는 대가를 숨기지 않고,
  "계약이 안 바뀌므로 되돌리기는 어댑터 디렉터리 하나"라고 proposal에 썼다. 실험적 API 채택을
  낙관이 아니라 **철수 경로가 계산된 결정**으로 만든 것.

- **새 실패 모드를 태스크로 먼저 못 박았다.** 세션이 오래 살면 생기는 문제들 —
  `stderr` 미소비로 조용히 멎음(3.7), 애매한 세션 반납 시 **다음 요청이 이전 턴의 델타를 받음**(3.14),
  `turn/failed`라는 알림이 **없음**(3.13) — 이 셋은 코드를 쓰다 만나면 재현이 어려운 종류다.
  `tests/fake_app_server.py`(`--no-handshake`·`--die-mid-turn`·`--hang`·`--ignore-interrupt`·`--auth-401`)를
  **실물 CLI의 대체재가 아니라 "실물이 닿지 못하는 곳"으로 규정**한 문장이 특히 정확하다.

- **판정 줄 분리를 `core/`의 동기 상태 기계로 내렸다(2.4/2.7).**
  델타가 오면 `VERDICT: ANSWERABLE`이 `VER`·`DICT`·`: ANSW`·`ERABLE`로 쪼개져 온다.
  이걸 서비스의 async 흐름 안에서 다루면 테스트가 페이크·이벤트 루프에 묶이는데,
  순수 함수로 빼서 **"비동기도 페이크도 없이 표 하나로" 덮게** 만들었다.

- **적대적 리뷰의 지적을 사실로 갈랐다.** 4건 중 Minor1만 수용하고 Major 3건은
  "스펙 확정 단계라 코드 부재가 정상"·"캐싱은 의도적 범위 제외"로 기각했다. 수용/기각 둘 다 근거가 있었다.

### Problem (나빴던 것)

- **표면 선택을 뒤집은 것이 또 사용자 질문 하나였다.**
  1장은 `codex exec --json`을 8태스크에 걸쳐 정밀하게 쟀지만, **"다른 표면이 있는가"를 묻는 항목이 없었다.**
  `codex --help` 한 번이면 `app-server`가 보였을 자리다. 01:37(레이어 감사)·15:15 회고와 **세 번째 같은 모양** —
  절차가 깊이는 파고 넓이는 훑지 않는다.

- **직전 회고(15:15)의 지적을 14분 만에 다시 반복했다.** 15:15 회고가 "표면 목록을 훑는 실측 항목이 없었다"를
  Problem으로 적었는데, `8e503fc`의 `1-B`에도 **"app-server 말고 또 다른 표면이 있는가"를 확인하는 태스크는 없다.**
  같은 함정에 두 번째로 들어가 있는데 회고를 읽고도 못 봤다.

- **셸·경로 혼용이 네 세션 연속.** 이번엔 5건: 윈도우 임시 경로를 컨테이너 `sh`에,
  `docker cp` 소스에 `C:\tmp`, 호스트 `python` 별칭. 오전 세션(`a58ce1f7`)까지 합치면 하루에 9건이다.
  매번 "다음엔 조심"으로 끝나고 **규칙이 어디에도 안 적혀 있다.**

- **`pytest`를 한 번도 돌리지 않고 하루를 닫았다.** 14:09 이후 커밋 3건이 전부 문서라
  418 passed는 11:27 이후 재확인된 적이 없다. 문서 change라 정당하지만, `tasks.md`가
  **19개 태스크짜리 3장**으로 불어난 상태에서 초록의 마지막 확인이 4시간 전이라는 건 기록해 둘 값이다.

- **README `/search` 예시의 손으로 쓴 값이 여섯 번째 이월.**
  `score: 0.8734`(167·209행)·`chunk_index: 1`(167행)은 여전히 미측정값이다.
  12:31 회고에서 **같은 질의의 실측값 `0.861`을 화면에 띄우고도** 안 고쳤고, 그 뒤 두 세션은 파일을 열지도 않았다.

- **Retrobot 샌드박스 벽 16번째 재발.** 이번 회차도 `cat` heredoc → `python -c` → PowerShell → `ls` 순으로
  네 번 거부당하고 Glob·Grep으로 우회했다. 15:15 회고에서 `parse_sessions.py`를 포기하기로 확정했으므로
  **이건 이제 "미완 과제"가 아니라 "상시 비용"** 이다 — 회차마다 5~10분과 에러 10여 건이 여기 든다.

- **인터럽트 4회.** `a58ce1f7` 3회 · `02b371b1` 1회. 특히 오전 세션은 자격 증명 자동화 방향을
  세 번 되물어 잡았다(스크립트 실행 금지 → compose 내부 실행 가능한가 → `~/.codex/auth.json` 마운트).
  **첫 프롬프트에 "사용자는 스크립트를 실행하지 않는다"는 제약이 없어서** 두 번 헛돌았다.

### Try (개선해야 할 것)

1. **`1-B`에 표면 탐색 태스크를 지금 추가한다.**
   `1B.0` — `codex --help` · `codex app-server --help`의 서브커맨드와 옵션을 전부 나열하고,
   "델타를 낼 수 있는 표면"이 app-server 말고 또 있는지(예: `codex mcp`, `--experimental-json`) 판정한다.
   **결론이 "없다"여도 그 문장이 파일에 남아야** 세 번째 뒤집기를 막는다.
   → 실측 장 템플릿에 **"경쟁 표면 열거"를 1번 태스크로 고정**한다.

2. **셸 규칙을 `CLAUDE.md`에 세 줄로 박는다.**
   - 컨테이너 안에서 도는 명령은 호스트 경로를 넘기지 않는다 — 파일은 `docker compose exec -T … sh -c 'cat > /tmp/x'`로 표준입력으로 넣는다.
   - `python`이 아니라 `docker compose exec api python`을 쓴다(호스트에 파이썬 없음).
   - 다중 인용부호가 필요한 명령은 PowerShell here-string이 아니라 **Bash 도구의 heredoc**을 쓴다.
   네 세션 연속이면 개인 주의력이 아니라 **문서에 없다는 것이 원인**이다.

3. **3장 착수 전에 `pytest`를 한 번 돌려 418을 재확인하고 그 숫자를 `tasks.md` 3장 머리에 적는다.**
   3장은 프로세스·서브프로세스·풀이라 회귀가 조용히 섞이기 쉽다. **출발선 숫자가 없으면 무엇이 깨졌는지 못 가른다.**

4. **README 예시를 3장 착수 전에 실측으로 교체한다 — 이번 회차 안에.**
   `docker compose up` → `curl -sS -X POST localhost:8000/search` 한 번의 출력을 그대로 붙인다.
   여섯 번 이월됐으므로 **"다음에"가 아니라 3장 첫 태스크 앞에 끼운다.** 미측정값이 README에 있는 것은
   과제 불합격 기준("문서에 적었으나 코드가 없음")과 같은 종류의 위험이다.

5. **`1B.6`(exec 픽스처 4건 삭제)를 `1B.7`(ARCHITECTURE 재작성)과 같은 커밋에 묶는다.**
   픽스처만 지우고 문서를 안 고치면 "왜 없어졌는가"가 사라지고, 문서만 고치고 픽스처를 남기면
   다음 사람이 죽은 샘플을 현재 형식으로 읽는다. **둘은 한 원자다.**

6. **긴 세션의 첫 프롬프트에 MUST-NOT을 먼저 쓴다.**
   오전 세션의 "사용자가 스크립트를 실행하면 안 된다"는 제약은 세 번째 프롬프트에야 나왔다.
   제약을 뒤늦게 주면 이미 만든 것을 버리게 된다.

7. **Retrobot 우회 절차를 `retrobot/SKILL.md`에 명문화한다.**
   "이 환경에서는 스크립트를 쓸 수 없다. Glob으로 대상 파일을 잡고, Grep `-o` + `head_limit`/`offset`으로
   타임스탬프·프롬프트·`is_error`를 뽑는다"를 절차로 적어 **회차마다 네 번씩 거부당하는 왕복을 없앤다.**
   16번 반복된 실패는 우회법을 기록하지 않은 대가다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 09:52 | `a58ce1f7` | Makefile 제거·Codex SDK 전환 착수, Docker 데몬 미기동으로 첫 검증 실패 |
| 10:2x | `a58ce1f7` | 자격 증명 자동화 방향을 세 번 되물어 `~/.codex/auth.json` 마운트로 확정 |
| 11:25 | — | `f1cc26f`·`e0da77e` "fix: Docker Compose 전환"(+419/-264), 414→418 passed |
| 11:29 | `02b371b1` | `/opsx:propose add-answer-generation` — 아티팩트 4종 생성 |
| 12:26 | — | `78e4ac4` "docs: add-answer-generation 스펙 분석"(+870) |
| 12:26 | `10c7baad` | Retrobot 12:31 회고 (에러 17건 — 샌드박스 거부) |
| 12:26 | `0d343c07` | 훅이 조용해 상태 확인용 세션 (에러 0건) |
| 13:57 | `076cba3d` | Codex 적대적 리뷰 — 지적 4건 중 Minor1만 수용 |
| 14:09 | — | `bd59e24` "docs: ARCHITECTURE 문서 업데이트"(+4/-4) |
| 14:09 | `cd30845c` | Retrobot 14:16 회고 (에러 11건) |
| 14:10 | `1f98f356` | `/opsx:apply` 1장 — 컨테이너 안 `codex exec --json` 실측 8태스크 |
| 15:07 | — | `441a03f` "1장 실측 — codex exec"(+183/-16), 픽스처 4건 채취 |
| 15:07 | `26fb8d3e` | Retrobot 15:15 회고 (에러 13건) |
| 15:1x | `1f98f356` | **"codex 응답을 streaming으로 오게 할 수 없나?"** → app-server 실측: 209자에 델타 112건, 지연 4.5초, `thread/start` 8.11초→0.07초 |
| 15:08 | — | `9bbe7db` "Codex app-server 델타 스트리밍 설계"(+342) |
| 15:2x | `1f98f356` | `/opsx:update` — 표면 전환을 스펙에 반영. 1장은 남기고 `1-B` 신설, 3장 11→19태스크, `spec.md`는 **0줄** |
| 15:24 | — | **`8e503fc` "docs: Codex Streaming 형태로 스펙 정리"(+178/-59)** |
| 15:29 | `318fd856` | Retrobot 이번 회고 — 샌드박스 거부 4종, Glob·Grep으로만 집계 |
