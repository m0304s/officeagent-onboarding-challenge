# Retrobot 회고 — 2026-08-05 14:55 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 3 (스펙 1 · Retrobot 2) |
| 총 프롬프트 | 2 (사람이 친 것 기준) + 선택 응답 2 |
| 에러 발생 | 8 (**전부 Retrobot** — 스펙 세션 0) |
| 커밋 | `4904c19` — 3파일 **+69/-10** |
| 태스크 | `add-cross-encoder` **51/74** (65 → 74 로 늘고 체크는 그대로) |

집계 창은 직전 회고(14:32) 이후 **14:32~14:55 KST**(로그의 UTC `05:32~05:55`)입니다. 증분 23분입니다.

## 세션별 활동

### 세션 `f2287e68` (14:32 KST ~ 진행 중) — LLM-Judge 스펙

직전 회고가 "이 회고와 동시에 도는 중"이라고 적은 그 세션입니다. 도구 60회:

| 도구 | 횟수 |
|---|---:|
| Edit | 28 |
| Bash | 13 |
| Read | 13 |
| Grep | 3 |
| AskUserQuestion | 2 |
| Write | 1 |

**프롬프트:**

1. `14:32` `/opsx:update add-cross-encoder` — 해당 명세서에서 크로스인코더 실행 내용이 존재합니다. 골든셋이 존재하므로, LLM-Judge를 실행하는 방식으로 스펙을 수정해주세요.
2. `14:49` `/opsx:apply` — 8장, 9장 작업 진행해주세요.

**에러: 0건.**

`14:49` `4904c19` **docs: llm-judge 판정 스펙 추가** — `proposal.md`·`design.md`·`tasks.md` 3파일. 커밋 직후 §9 구현에 들어가 `tests/llm_judge.py`(신규)와 `tests/golden_harness.py`(수정)가 **지금 워킹트리에** 있습니다.

### 세션 `f27b92f6` (14:31 ~ 14:43 KST) — Retrobot 훅

직전 회고(`2026-08-05_1432_retro.md`, 커밋 `074ad70`)를 만든 실행. 에러 5건 — 전부 승인 거부입니다.

### 이 Retrobot 실행 (14:55 KST)

에러 3건 (아래 Problem).

## KPT 회고

### Keep (좋았던 것)

- **모호한 지시를 실행 전에 갈랐습니다.** "LLM-Judge를 실행하는 방식으로 스펙을 수정해주세요"는 두 갈래로 읽힙니다 — **품질 평가**를 judge로 하는 것과, **리랭커 자체**를 LLM-as-reranker로 갈아치우는 것. 후자는 이 change가 `proposal.md`·`design.md` 두 곳에서 명시적으로 범위 밖이라고 못 박은 항목이라, 잘못 읽었으면 745줄 스펙의 축이 통째로 뒤집혔습니다. 첫 `AskUserQuestion`이 세 선택지(품질 평가 / 리랭커 자체 / 둘 다)를 던졌고, 그 답이 정해진 뒤에야 파일에 손이 닿았습니다. 편집 28건이 전부 확정된 방향 위에서 일어났습니다.

- **두 번째 질문이 "게이트로 쓸 것인가"를 별도 선택지로 꺼냈습니다.** "세 파일 전부 적용(권장) / design + tasks 만 / **judge를 게이트로**". 가장 유혹적인 오답을 선택지로 올려놓고 기각한 셈이라, 기각이 누락이 아니라 판단이라는 것이 기록에 남습니다.

- **`판정불가`를 오답으로 세지 않습니다.** 형식 위반·파싱 실패는 `일치/불일치`와 다른 칸으로 갑니다 — 오답으로 세면 **판정자가 흔들린 것이 품질이 나빠진 것으로 보입니다.** 여기에 판독 규칙까지 붙었습니다: 판정불가가 과반이면 그 회차의 통과 수는 읽지 않습니다. 숫자를 만드는 규칙과 그 숫자를 언제 믿지 않을지가 같은 자리에 적혔습니다.

- **judge를 회귀 게이트로 만들지 않은 근거가 기존 결정에서 나왔습니다.** "끈 구성보다 몇 이상"류의 단언을 두고 싶은 자리인데, 그 임계값은 재지 않은 값이라 **결정 5(리랭킹 점수 하한 거절)와 같은 이유로** 거절했습니다. 새 결정이 새 논리를 만들지 않고 이미 선 논리를 재사용했습니다 — 두 자리가 나중에 따로 흔들리지 않습니다.

- **사실이 바뀌자 결론이 아니라 근거를 고쳤습니다.** 결정 5는 "**잴 수 없다** — 정답 라벨이 없다"였는데, 골든셋이 생겨 `(질의, 관련 청크)` 44쌍이 확보되면서 그 문장이 거짓이 됐습니다. 지우거나 뭉개지 않고 "**재도 임계값이 나오지 않는다** — 막는 것은 라벨의 부재가 아니라 **척도**다(질의마다 범위가 달라 44건을 합쳐도 공통 경계선이 안 생긴다)"로 갈아 끼웠습니다. 결론은 유지, 이유는 교체입니다.

- **채점 순서 자체가 결정이 됐습니다.** ① 검색 게이트 → ② 결정적 문자열 검사 → ③ LLM-Judge, 그리고 **②와 ③을 합산하지 않습니다.** 합치면 흔들리는 쪽이 결정적인 쪽을 오염시키므로 나란히 적고 **어긋나는 문항을 눈으로 봅니다** — 그 어긋나는 자리가 곧 판정자를 의심할 자리라고 적혔습니다. 골든셋 README가 명령형으로 적어 둔 "검색 실패와 생성 실패를 섞지 말 것"이 스펙 조항이 됐습니다.

- **판정자에게 근거 청크를 넘기지 않습니다.** 넘기면 판정이 "근거에 있으니 맞다"로 흐르는데 그것은 이미 ①이 판정한 것입니다. ③이 묻는 질문을 하나로 좁혔습니다.

- **자기 채점 위험을 위험 목록에 스스로 올렸습니다.** 판정자와 답변 생성기가 같은 CLI라 같은 실패를 공유하면 틀린 답을 틀렸다고 못 합니다. 완화가 "다른 모델을 쓴다"(범위 밖)가 아니라 "②를 나란히 두고 어긋나는 문항을 본다"입니다 — 지금 할 수 있는 것으로 적혔습니다.

- **직전 회고의 Keep이 다음 회차 태스크로 승격됐습니다.** 14:32 회고가 칭찬한 "자기 기능에 불리한 사실을 문서에 남긴 것"이 `10.5`에서 **"여전히 말할 수 없는 것 세 줄을 나란히 적는다"**로 태스크가 됐습니다(문서 한 건 위의 값 / 판정자 자신이 검증되지 않았다 / 작은 차이는 신호가 아니다). 습관이 규칙이 되는 방향입니다.

- **평가 한 바퀴의 비용을 먼저 세고 기본 실행에서 뺐습니다.** 44문항 × 두 구성 × LLM 한 턴(5~17초) + 리랭킹입니다. `-m "llm and slow"`로 격리해 PRD §5가 요구하는 한 줄(`docker compose run --build --rm test`)은 **구독 없이** 그대로 돕니다. 불합격 기준이 "명시된 방법으로 실행 불가"인 과제에서 매번 지켜지고 있는 규율입니다.

- **스펙 세션 에러 0건.** 60회 도구 호출에 실패가 없습니다.

### Problem (나빴던 것)

- **Retrobot이 이 창 에러의 100%(8/8) — 20회차.** 스펙 세션은 0건이고 8건 전부가 훅 두 실행입니다. `retrobot/SKILL.md`는 `afa9794` 이후 무수정이라 이번에도 첫 줄 `command -v python3 jq`가 첫 에러였고, 이어 `py.exe -3` PowerShell 호출도 거부됐습니다. **회고 열네 편이 같은 진단을 반복했고, 고치는 비용은 여전히 한 블록 이동입니다.**

- **직전 회고 Try 6건, 이 창에서 이행 0건.** 창이 23분이고 스펙 작업이라 기회가 적었다는 것은 사실이지만, **#6(`SKILL.md` 첫 줄)은 이 실행에서 그대로 다시 터졌습니다** — 이행 대상이 회고를 만드는 도구 자신인데도 회차를 넘겼습니다.

- **`distractor-dense` 두 문항이 스펙 갱신에도 안 들어갔습니다.** 직전 회고 Try #3이 "리랭킹을 넣은 명분이 그 자리였는데 거기서만 졌다, 지금 안 보면 9장 회귀가 틀린 기준선 위에 앉는다"고 적었습니다. 그런데 이번에 다시 쓴 `9.1`은 "순위가 움직인 문항을 `probe`와 함께 찍는다"까지만 적고 **그 두 문항을 이름으로 지정하지 않았습니다.** 9장을 여는 커밋이 바로 그 회귀 층인데, 경고를 받은 자리가 태스크 문장에 실리지 않았습니다.

- **태스크가 65 → 74로 늘고 체크는 51 그대로입니다.** 남은 23건 중 **11건이 이번에 새로 생긴 것**입니다(`8.6`, `9.6`~`9.11`, `10.9`~`10.12`). 하드 마감이 8/9인데 남은 일이 줄지 않고 늘어난 회차입니다.

- **`reranker_enabled` 기본값 근거는 두 번째 이월입니다.** 직전 회고 Try #1 — 코드는 `True`로 출하하는데 근거가 코드 옆에 없고 문서에는 못 믿는 이유만 있습니다. 이번 커밋이 `design.md`를 54줄 고치면서도 그 한 줄은 안 붙었습니다.

- **`ARCHITECTURE.md`·`README.md`·`PROMPT_DESIGN.md`가 아직 안 따라왔습니다.** `proposal.md`가 "`PROMPT_DESIGN.md`에 **판정 프롬프트 절 하나가 는다**"고 커밋됐고 태스크(`10.10`·`10.11`)도 있지만 문서는 그대로입니다. 지금은 proposal이 미래형이라 허위 기재는 아니지만, 이 리포에서 문서-코드 불일치는 감점이 아니라 불합격 항목이라 **여는 순서가 이미 위험한 쪽**입니다.

- **워킹트리가 다시 갈렸습니다.** 직전 회고가 "완전히 깨끗"이라고 적은 지 23분 만에 `tests/llm_judge.py`(신규)·`tests/golden_harness.py`(수정)가 미커밋 상태입니다. 진행 중이라 정상이지만, 3·6·7장에서 세 번 반복된 "구현만 커밋되고 문서는 워킹트리에 남는" 패턴이 시작되는 자리와 모양이 같습니다.

- **외부 적대적 검토 0회 — 열아홉 세션 연속.** 이번에 들어온 것은 기능이 아니라 **평가 방법론**입니다. 자기 CLI가 자기 답을 채점하는 구조를 스스로 위험 목록에 적었는데, 그 구조를 반박해 본 사람도 도구도 없습니다. 검토가 가장 값싼 대상(결정 13 한 절, 58줄)인데 여전히 안 붙었습니다.

### Try (개선해야 할 것)

1. **`retrobot/SKILL.md` 2단계의 첫 줄을 바꿉니다 — 20회차입니다.** `command -v python3 jq`를 지우고, 그 아래 이미 있는 내장 도구 표(`Glob` → `Grep(count)` → `Grep(-o + head_limit)` → `Read(offset/limit)`)를 첫 줄로 올립니다. 이 회고도 그 표만으로 완성됐습니다. 블록 하나를 위로 옮기는 편집입니다.

2. **`9.1`에 `distractor-dense` 두 문항을 이름으로 박습니다.** "순위가 움직인 문항을 찍는다"는 결과를 보고 나서야 무엇을 볼지 정하는 문장입니다. 리랭킹의 명분이었던 자리는 **재기 전에 지정돼야** 사후 해석이 아닌 예측이 됩니다.

3. **판정자를 한 번 검증하는 태스크를 9장에 넣습니다.** `design.md`가 "판정자 자신이 검증되지 않았다"고 스스로 적었습니다. 44문항 중 10건 정도를 사람이 직접 채점해 judge 판정과 대조하면, "우리 judge가 그렇게 봤다"가 "우리 judge가 사람과 N/10 일치한다"가 됩니다. 그 숫자 없이는 통과 수 차이를 읽을 기준이 없습니다.

4. **`reranker_enabled = True`의 근거를 `config.py` 옆에 한 줄로 적거나 `false`로 내립니다.** 두 번째 이월입니다. 문서는 "근거가 표보다 약하다"고 말하고 코드는 "켠다"고 말하는 상태가 회차를 넘겼습니다.

5. **`tests/llm_judge.py`·`golden_harness.py`를 `PROMPT_DESIGN.md` 갱신과 같은 커밋에 넣습니다.** proposal이 이미 그 절의 존재를 약속했으므로, 코드만 먼저 들어가면 문서 쪽이 다시 뒤처집니다.

6. **결정 13 한 절(58줄)만 외부 검토에 겁니다.** 전체 스펙이 아니라 이 절입니다 — 자기 채점 구조, 판정불가 처리, ②/③ 비합산 셋만 물으면 되고, 열아홉 세션 연속 0회를 가장 싸게 끊는 지점입니다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 (KST) | 세션 | 주요 활동 |
|------|------|----------|
| 14:31~14:43 | `f27b92f6` | Retrobot — 14:32 회고 생성, `074ad70` (에러 5, 전부 승인 거부) |
| 14:32 | `f2287e68` | `/opsx:update` — "골든셋이 존재하므로 LLM-Judge를 실행하는 방식으로" |
| 14:32~14:36 | `f2287e68` | 붙일 자리 확인 — 골든셋 README·`golden_harness.py`·`test_llm_live.py` 읽기 |
| ~14:38 | `f2287e68` | **선택 질문 ①** 품질 평가를 judge로 / 리랭커 자체를 LLM-as-reranker로 / 둘 다 |
| ~14:40 | `f2287e68` | **선택 질문 ②** 세 파일 전부(권장) / design+tasks 만 / judge를 게이트로 |
| 14:40~14:48 | `f2287e68` | 편집 28건 — 결정 13 신설, 결정 5·11·12 개정, 태스크 11개 추가 |
| 14:49 | — | `4904c19` **docs: llm-judge 판정 스펙 추가** (3파일 +69/-10, 51/74) |
| 14:49 | `f2287e68` | `/opsx:apply` 8·9장 착수 → `9.6 tests/llm_judge.py` 작성 |
| 14:55 | — | 이 Retrobot 실행 (에러 3) |

## 신규 확정 사실 (환경)

- **`Bash` 도구는 파라미터 확장(`${f:0:8}`)이 든 명령을 `Contains simple_expansion`으로 거부합니다.** `for` 루프로 파일 목록을 순회하며 앞뒤 타임스탬프를 뽑으려던 시도가 여기서 죽었습니다. 세션 시각은 아래 `Grep` 기법으로 대신합니다.
- **`tail` 없이 파일의 마지막 매치를 뽑는 방법** — `Grep(output_mode: "count")`로 총 개수 N을 먼저 재고, 같은 패턴에 `offset: N-1, head_limit: 1`을 주면 마지막 한 건이 나옵니다. 세션 종료 시각을 셸 없이 얻는 유일한 경로였고, 이 회고의 세션 10개 시각이 전부 이것으로 나왔습니다.
- **`retros/TIMELINE.md`는 이제 `Read`로 통째로 못 읽습니다** — 63줄인데 **32,305 토큰**이라 한도(25,000)를 넘깁니다. 한 줄이 수천 토큰인 파일이라 `offset`/`limit`이 필수입니다.
- **셸 승인 게이트는 직전 회차(14:32)와 같은 상태입니다** — `git log`·`git show`·`git status`·`date`·`wc` 단독은 통과, `command -v python3 jq`·`py.exe -3`(PowerShell)·`;`나 `for`로 이어 붙인 복합 명령은 거부입니다.
