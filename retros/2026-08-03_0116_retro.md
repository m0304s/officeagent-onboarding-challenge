# Retrobot 회고 — 2026-08-03 01:16 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 24 (구현·조사 13 · Retrobot 훅 11) |
| 총 프롬프트 | 35 (구현 24 · 훅 11) |
| 에러 발생 | 66 (구현 16 · 훅 50) |
| 커밋 | `69082de` qa-retrieval 로직 완성 (+701/-47) |
| 테스트 | 388 → **400 passed**, `ruff` clean, `openspec validate --strict` valid |

직전 회고(00:40)가 6장까지 다뤘으므로, 아래 KPT는 **7·8·9장과 아카이브**에 무게를 둡니다.
`add-qa-retrieval`은 **66/66 태스크로 종료**되었고 `openspec/changes/archive/2026-08-03-add-qa-retrieval/`로
옮겨졌습니다. 메인 스펙 `openspec/specs/retrieval/spec.md`(283줄, 요구사항 9 · 시나리오 36)가 새로 생겼습니다.

## 세션별 활동

### 세션 `b42efae1` (00:40 ~ 01:15) — 7·8·9장 + 아카이브

**프롬프트:**
1. `/opsx:apply add-qa-retrieval Task 8, 9 작업 진행해주세요.` → **즉시 중단**
2. `/opsx:apply add-qa-retrieval Task 7, 8, 9 작업 진행해주세요.` (7장을 되살려 재입력)
3. `/opsx:archive`

**활동:** `Bash`×62 · `Edit`×28 · `Read`×14 · `Write`×1. 35분, 태스크 16개(7.1~9.7) 마감.

- `tests/test_retrieval_quality.py` 174줄 신설 — **실물 `SentenceTransformerEmbedder` + 완전 탐색 스텁**.
  `sample-docs/` 실제 파일을 청크 200자로 색인. spec 질의 4건 전부 1위 적중,
  실측 1위 **0.8775~0.9002** vs 무관 질의 **0.7928**, 배포 기본 하한 `0.82`가 그 사이
- `tests/test_concurrency_api.py` +98줄 — 질의 임베딩·저장소 질의·**토큰 계산** 세 갈래 오프로드
- `Dockerfile` `test` 스테이지 신설 + `docker-compose.yml`의 `api`에 `target: runtime` 명시
- `tests/conftest.py`·`test_harness.py`·`stubs.py` — `needs_weights`를 `needs_vector_store` 옆으로 통합,
  스텁의 `query` 계약 준수 단언 추가
- `ARCHITECTURE.md` — 검색 API·품질 층·세 실행 경로 커버리지 표 보강

**세 실행 경로 실측:**

| 명령 | 결과 | 덮는 것 |
|------|------|--------|
| `pytest` (외부 서비스·네트워크 없음) | 357 passed, 43 skipped | 기본 층 |
| `make test-container` (신규) | 371 passed, 29 skipped | + 품질 층 8건 |
| `make test-all` | **400 passed** | 전부(호스트에 가중치·Chroma가 있을 때) |

**발생한 에러:** 도구 차원 실패 0건. 대신 **의도한 실패 1건**(변이 검사)과
**환경 결함 3건**(컨테이너 안 `test_boot.py` 자격증명 동기화 — `FileNotFoundError`)을 만나 후자는
`Dockerfile` 수정으로 01:02 → 01:04 사이에 해소했습니다.

### 구현 세션 요약 (창 전체)

| 세션 | 시각 | 활동 |
|------|------|------|
| `f2564d3d` | 12:32~16:26 | Chroma 서버 모드 전환·동시성 (Bash 150, Edit 87) |
| `18b09f4b` `1679c0c5` | 15:27~16:06 | 컬렉션 차원 조사, GUI compose 연결 |
| `9df9a4d6` | 16:44~17:47 | `add-qa-retrieval` 스펙 확정 (TOCTOU·질의 길이) |
| `44fac989` `5f796aaf` `a626c3aa` `6937c4af` | 20:55~21:30 | 1~3장 + Retrobot 잔해 정리 |
| `7e054299` `cadbfc2d` `8ccbec39` | 23:36~00:33 | 4·5·6장 |
| **`b42efae1`** | **00:40~01:15** | **7·8·9장 + 아카이브 → `69082de`, 400 passed** |

### Retrobot 훅 세션 (11건)

`abf1f969` `ad268012` `5720fb57` `2e6eb0ed` `c8e49521` `90e8e04a` `62f70e7c` `a9355fb9` `e6173212`
`b403397a` `96832dcb` — 에러 50건. 이번 회차(12번째)도 같은 순서로 세 번 막혔습니다:
`/tmp` Write 거부 → heredoc 재작성 시 "multiple operations" 거부 → 단일 `python3 - <<PY`로 재작성,
그 뒤 `sed` 승인 요구.

## KPT 회고

### Keep (좋았던 것)

- **단언이 공허하지 않은지 변이로 검사했다.** 8장의 오프로드 테스트는 아무것도 오프로드하지 않아도
  통과하기 쉬운 종류입니다 — `/health`는 원래 빠르니까요. 그래서 라우터의 `asyncio.to_thread`를
  실제로 걷어낸 상태로 돌려 `test_health_answers_while_a_query_is_being_counted`가 **FAILED로 바뀌는지**
  확인하고(00:55), 원복 후 11건 재통과까지 봤습니다. 세 갈래(질의 임베딩·저장소 질의·토큰 계산) 중
  **토큰 계산만 오프로드 책임이 라우터에 있었고 아무 테스트도 잡고 있지 않았습니다** — 태스크에 없던
  경로를 스스로 찾아 덮은 것이 이 묶음에서 가장 값어치 있는 추가입니다.

- **품질 테스트를 통과시키려고 단언을 약화하지 않았다.** 7.5가 "떨어지면 청크 크기·K·하한을 조정하기
  전에 원인을 기록하라"고 미리 못 박아 뒀는데, 첫 실행에 통과해 그 조항을 쓸 일이 없었습니다.
  5장에서 하네스로 잰 하한 `0.82`가 **구성이 완전히 다른 자리**(실물 임베더 · 청크 200자 · 실제 파일)에서도
  관련/무관 분포 사이에 그대로 놓였다는 뜻이라, 5장 회고에서 스스로 지적했던 "표본이 작다"는 약점이
  독립 표본 하나로 보강됐습니다.

- **테스트 설계의 전제를 숫자로 방어했다.** 청크 200자는 취향이 아니라 계산입니다 — 기본값 600자면
  두 문서가 청크 서넛이라 `top_k=5`가 **전부를 반환**해 "1위가 맞는 문서인가"라는 단언이 항상 참이
  됩니다. 순위 테스트가 순위를 재도록 만들려면 후보가 K보다 많아야 한다는 것을 코드 옆에 적었습니다.

- **테스트 이미지를 만들면서 배포 이미지가 오염되지 않게 못 박았다.** 멀티스테이지에서 `--target` 없는
  빌드는 **마지막 스테이지**를 고릅니다. `test` 스테이지를 `runtime` 위에 덧쌓은 뒤 compose의 `api`에
  `target: runtime`을 명시하고, 서비스 이미지에 `pytest`가 없는지 실제로 확인했습니다. 안 했으면
  운영 이미지에 테스트 의존성이 조용히 딸려 들어갔을 구조입니다.

- **컨테이너 첫 실행의 3건 실패를 "환경 탓"으로 넘기지 않았다.** `test_boot.py`의 자격증명 동기화 3건이
  `FileNotFoundError`로 깨졌을 때 스킵 조건을 붙여 가리는 대신 `Dockerfile`을 고쳐 통과시켰습니다(01:03).
  "컨테이너에서는 원래 안 되는 테스트"를 한 번 허용하면 그 목록은 계속 자랍니다.

- **메인 스펙 동기화를 임의로 하지 않고 물었다.** `retrieval` capability는 메인 스펙이 아예 없는
  상태였고, 동기화는 요구사항 9개·시나리오 36개를 새로 만드는 일이었습니다. 델타↔메인 대조표를
  먼저 보여주고 확인을 받은 뒤 실행했으며, 옮긴 직후 **누락 없음을 재비교로 검증**했습니다.

### Problem (나빴던 것)

- **README `/search` 예시가 여전히 허구인데, 이번엔 진짜 값이 손에 있었다.** 01:07~01:08에 컨테이너를
  띄워 실제 업로드 → `POST /search` 왕복을 돌렸고 진짜 응답을 받았습니다
  (`document_id: 0de0c0a1-...`, `company-policy.txt`의 `chunk_count: 1`). 그리고 01:09에 9.5 문서-코드
  대조로 **README의 그 예시 블록을 화면에 띄웠습니다.** 그런데도 `README.md:161`의
  `"document_id":"b166d4ad-..."` · `"score":0.8734` · `"chunk_index":1`과 `README.md:201` 로그 예시의
  `"top_score":0.8734`가 그대로 남았습니다. 기본 설정에서 `company-policy.txt`는 청크가 **1개**이므로
  `chunk_index: 1`은 나올 수 없는 값이고, 6.7이 바로 `chunk_index ∈ [0, chunk_count)`를 단언합니다.
  두 회고 연속 같은 지적이며, 이번에는 "몰라서"가 아니라 **실측값을 보고도 옮겨 적지 않아서**입니다.
  문서-코드 불일치는 감점이 아니라 불합격 사유입니다.

- **동점 결정성 — 여덟 번째 이월이고, 이제 창이 닫혔다.** `src/`·`tests/`·`ARCHITECTURE.md`·
  `openspec/specs/` 어디에도 "동점"이라는 단어가 없습니다. 스펙 확정 회차(17:47)의 Codex 리뷰에서
  나온 medium 지적이 여섯 회고를 건너뛴 끝에, `add-qa-retrieval`이 **아카이브되면서 이 change 안에서
  처리할 자리가 사라졌습니다.** 이월 항목이 change 경계를 넘으면 "다음에 하자"가 아니라 "안 하기로
  했다"가 됩니다 — 그 결정을 명시적으로 내린 적은 없습니다.

- **어느 한 명령도 400건 전부를 덮지 못한다.** `make test-container`가 스킵한 29건의 이유가
  로그에 그대로 남아 있습니다 — *"Chroma 서버(http://localhost:8001)가 떠 있지 않습니다"*. 테스트
  컨테이너가 compose 네트워크 밖에서 돌아 `localhost`가 자기 자신을 가리킵니다. 400 전부가 도는
  경로는 `make test-all`뿐인데 그건 **호스트에 로컬 가중치가 있을 때**의 이야기입니다. 평가자가
  둘 중 무엇을 돌리든 한쪽 층은 스킵으로 지나가고, 스킵은 초록으로 보입니다.

- **Retrobot 훅 샌드박스 벽 12번째 재발.** 훅 세션 11개가 이 창에서 에러 50건을 냈고 대부분이 같은
  벽입니다. 두 회고 전에 나온 Try(`retrobot/parse_sessions.py`를 리포에 두고 훅이 그것만 실행)는
  아직 파일조차 없습니다. 회고를 만드는 도구가 회고에서 지적한 문제를 회차마다 재생산하고 있습니다.

### Try (개선해야 할 것)

- **README 예시를 실제 응답으로 교체한다 — 다음 커밋에.** 절차까지 고정합니다:
  `docker compose up` → `sample-docs/company-policy.txt` 업로드 → `POST /search` 1회 →
  **응답 JSON을 그대로 붙여넣는다.** 손으로 다듬지 않습니다. 로그 예시의 `top_score`도 같은 실행의
  값으로 맞춥니다. 이번 회차에 이미 왕복을 돌렸으므로 추가 비용은 붙여넣기 한 번입니다.
  (`README.md:161`, `README.md:201`)

- **동점 결정성을 "안 한다"로 닫거나 다음 change의 태스크 0으로 올린다.** 여덟 번 미룬 항목에
  필요한 건 아홉 번째 알림이 아니라 **결정**입니다. 둘 중 하나를 이번 주 안에 씁니다 —
  (a) `ARCHITECTURE.md`에 "동점 시 순서는 저장소 반환 순서를 따르며 보장하지 않는다"를 명시하고
  닫거나, (b) 답변 생성 change의 `## 0. 이월 정리`에 `(-score, document_id, chunk_index)` 정렬로
  올립니다. 이 change가 `## 0. 이월 정리`로 두 건을 실제로 마감한 전례가 있으니 (b)는 검증된 자리입니다.

- **테스트 컨테이너를 compose 네트워크에 붙인다.** `make test-container`를 `docker run` 단독이 아니라
  `docker compose run --rm`으로 바꾸고 `VECTOR_STORE_URL`을 `http://vector-store:8000`으로 주면
  한 명령이 400건 전부를 덮습니다. 그때까지는 README의 커버리지 표에 **"이 경로는 Chroma 실물 29건을
  스킵한다"**를 숫자와 함께 적어 스킵이 초록으로 읽히지 않게 합니다.

- **`retrobot/parse_sessions.py`를 만든다 — 세 번째 요구다.** 훅이 `python3 retrobot/parse_sessions.py --since 24h`
  한 줄만 실행하면 `/tmp` 쓰기도 heredoc도 필요 없어집니다. 21:10에 `_extract.py`가 lint를 깨뜨린 전례가
  있으므로 **처음부터 `ruff check .` 범위 안에 두고 추적 파일로** 커밋합니다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 00:40 | `b42efae1` | `/opsx:apply 8,9` 입력 직후 중단 → `7,8,9`로 재입력 (7장 되살림) |
| 00:42 | `b42efae1` | 기준선 확인 — 388 passed |
| 00:47~00:49 | `b42efae1` | 7장 품질 테스트 — 실물 임베더로 8건, 질의 4건 전부 1위 적중 |
| 00:53~00:55 | `b42efae1` | 8장 오프로드 3갈래 + **변이 검사**(`to_thread` 제거 시 FAILED 확인 후 원복) |
| 00:57~00:58 | `b42efae1` | 9.1 — 외부 서비스 없이 357 passed / 43 skipped |
| 00:59~01:01 | `b42efae1` | Dockerfile `test` 스테이지 · compose `target: runtime` · `.dockerignore` |
| 01:02 | `b42efae1` | 컨테이너 첫 실행 3 failed (`test_boot` 자격증명 `FileNotFoundError`) |
| 01:03~01:04 | `b42efae1` | Dockerfile 수정 → 371 passed / 29 skipped (Chroma 미도달) |
| 01:05 | `b42efae1` | `make test-all` **400 passed** · README 실행 경로 갱신 |
| 01:06 | `b42efae1` | 9.3 ruff 통과 · `core/` 외부 import 없음 확인 |
| 01:07~01:08 | `b42efae1` | 9.4 `docker compose` 기동 → 업로드 → `POST /search` 왕복 확인 |
| 01:09~01:10 | `b42efae1` | 9.5 문서-코드 대조 — 하한 0.82 계측표 재현 확인, `ARCHITECTURE.md` 보강 |
| 01:11 | `b42efae1` | 9.6 spec 시나리오 ↔ 테스트 대조 · 9.7 `openspec validate --strict` |
| 01:12~01:13 | `b42efae1` | 최종 400 passed · 66/66 보고 · `/opsx:archive` |
| 01:14~01:15 | `b42efae1` | 델타 → 메인 스펙 동기화(요구사항 9 · 시나리오 36) 확인 후 실행, 아카이브 완료 |
| 01:16 | — | `69082de` qa-retrieval 로직 완성 (+701/-47) |
