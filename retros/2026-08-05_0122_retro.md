# Retrobot 회고 — 2026-08-05 01:22 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 19 (창: 08-04 02:06 ~ 08-05 01:22) |
| 총 프롬프트 | 23 (실질 작업 지시 9건, Retrobot 훅 13건, `/exit` 1건) |
| 에러 발생 | 62건 (그중 Retrobot 훅 49건 = **79%**) |
| 증분 (직전 회고 00:54 이후) | 28분, 커밋 1건 `ba4e9d6`, 프롬프트 1건, 에러 1건 |

이 창의 산출물은 `add-response-cache` 71/71 완주·아카이빙(`75cd4368`, 20:49~23:35)과
`add-cross-encoder` 제안 745줄(`0145a27`) 그리고 이번 증분의 어댑터 구현
`ba4e9d6`(14파일 **+787/-50**)입니다. 직전 회고가 끊긴 지점부터가 새 내용입니다.

## 세션별 활동

### 세션 `34e9f10c` (08-05 00:16 ~ 01:21) — 이번 증분의 전부

1447→ 이어지는 최장 세션. Bash 62 · Edit 32 · Read 25 · Write 11.

**프롬프트:**
1. (01:11) `bge-reranker-v2-m3가 된다면, 해당 모델로 교체해주세요.`

**증분 타임라인:**

| 시각 | 활동 |
|---|---|
| 00:54~00:57 | 1장 잔여 — `core/reranking.py`·`test_reranking_core.py` 마감, `--build` 로 **968 passed, 23 deselected** |
| 00:58 | `/opsx:apply add-cross-encoder` 2장 착수 |
| 00:58~01:01 | HF API 로 `gte-multilingual-reranker-base` 의 `config.json`·`tokenizer_config.json` 조회, 컨테이너에서 `CrossEncoder` 시그니처 직접 inspect |
| 01:02 | `adapters/protocols.py` 확장 + `adapters/reranking/local.py` 167줄 신규 |
| 01:03~01:05 | `conftest.py`·`stubs.py` 하네스 확장, `test_reranking.py` 222줄 |
| 01:06~01:08 | 스크래치패드에 가중치를 **실제로 내려받아** gte 로딩 시도 → 실패 확인 |
| 01:09 | `bge-reranker-v2-m3`·`bge-reranker-base` 대안 실측 착수 (백그라운드) |
| 01:09 | `sleep 90; tail` **차단** → `ToolSearch` 로 `Monitor` 를 찾아 전환 |
| 01:11 | 사용자 지시 도착 — 이미 진행 중이던 방향과 일치 |
| 01:12~01:15 | 모델 교체를 `local.py`·`conftest.py` 와 **`design.md`·`proposal.md`·`tasks.md` 에 동시 반영** |
| 01:16~01:20 | 재검증, `tests/README.md` 갱신, `openspec validate --strict` |
| 01:21 | 커밋 `ba4e9d6` — **986 passed, 2 skipped, 23 deselected** |

**발생한 에러:** 1건
- `Blocked: sleep 90 followed by: tail ...` — 포그라운드 `sleep` 후속 명령 차단. `Monitor` 로 전환해 해결.

### 세션 `45f0bdd9` (08-04 20:13 ~ 20:48)

**프롬프트:** 4건 — `캐시를 설명하는 것이 맞나요?` · 캐시 3상태 표 검토 · `수정해주세요` ·
`REPLACED 가 no-evidence 미삭제 / insufficient_evidence 무효화 누락 확인`.
이 넷이 두 시간 뒤 6장 무효화 설계의 축이 됐습니다. 에러 0건.

### 세션 `75cd4368` (08-04 20:49 ~ 23:35)

Bash 207 · Edit 96. `add-response-cache` 2~7장을 장 단위 `/opsx:apply` 로 닫고 아카이빙까지.
프롬프트 3건 중 21:45 **"23건 테스트 왜 생략된거지?"** 가 초록불의 숫자가 아니라
**빠진 숫자**를 본 질문. 에러 10건(주석 게이트·`E501`·`ruff format`).

### 세션 `7656b215` (08-04 23:41 ~ 23:46)

`현재까지 RAG 파이프라인 어떤 로직 구현되어있는지 확인 해주세요.` 에러 1건.

### Retrobot 훅 세션 13건 (`a2542476`·`115a7e25`·`e27b60d4`·`7a93d660`·`4e480d75`·`8486165b`·`25644f92`·`cf9d24e5`·`3f5b5605`·`62743af5`·`ed436f8b`·`5db6209d`·`408546e5`)

프롬프트는 전부 `SKILL.md` 본문 하나. 합계 에러 **49건**.
**13/13 세션이 `command -v python3 jq` 에서 죽고, 12/13 이 이어서 `/tmp` Write 에서 죽었습니다.**

## KPT 회고

### Keep (좋았던 것)

- **스펙이 실측에 의해 뒤집혔고, 뒤집힌 자리가 문서에 남았습니다.** `design.md 결정 1` 은
  `Alibaba-NLP/gte-multilingual-reranker-base` 였습니다. 01:06~01:08 에 컨테이너에서
  가중치를 실제로 내려받아 얹어 보니 **로딩 자체가 되지 않았습니다.** 표에서 조용히
  갈아치우지 않고 `~~취소선~~ + "처음 채택했다가 실물에서 기각했다"` 로 남겼습니다 —
  다음 사람이 같은 모델을 다시 후보에 올리지 않게 하는 것은 채택 이유가 아니라 **기각 이유**입니다.

- **완화책을 버리고 위험 자체를 없앤 판단.** 초안의 `결정 8` 은
  "`trust_remote_code=True` 를 받아들이되 리비전을 고정한다" 였습니다. mGTE 의 `config.json`
  이 저장소 코드를 가리키므로 **리비전 고정으로는 임의 코드 실행 표면이 남습니다.**
  `bge-reranker-v2-m3` 는 `XLMRobertaForSequenceClassification` 이라 `transformers` 안에 있고
  원격 코드가 필요 없습니다. 위험 목록에서 항목이 **완화된 게 아니라 삭제**됐고
  (`~~trust_remote_code~~ → 사라졌다`), 그 대가인 이미지 +1GB(1.2 → **2.2GB**)를
  같은 줄에 적었습니다. 완화책을 붙이는 것보다 위험이 없는 선택지를 고르는 쪽이 언제나 쌉니다.

- **01:11 사용자 지시가 이미 진행 중이던 방향과 일치했습니다.** 01:08 에 gte 실패를 확인하고
  01:09 에 `bge-reranker-v2-m3`·`bge-reranker-base` 실측에 들어간 상태에서 지시가 왔습니다.
  사용자가 방향을 틀어준 게 아니라 **같은 결론에 독립적으로 도달**했다는 뜻이라,
  이 회차의 스펙 수정은 지시 대기로 멈춰 있던 시간이 0입니다.

- **근거의 크기를 과장하지 않았습니다.** 위험 항목에 "한국어 성능이 기대에 못 미친다" 를 남기고,
  가진 근거를 `학습 언어에 한국어가 있다는 것과 **예시 한 건**의 점수 차(0.9826 대 0.0)가
  전부이고, 우리 도메인 전체에서의 성능은 아니다` 라고 적었습니다. 실측 하나를 벤치마크로
  부풀리지 않은 문장입니다. 되돌리기가 `APP_RERANKER_ENABLED=false` 한 줄이라는 것도 함께.

- **코드와 스펙이 같은 커밋에 들어갔습니다.** `ba4e9d6` 14파일에 `local.py`(구현)와
  `design.md`·`proposal.md`·`tasks.md`(스펙)가 함께 있습니다. 3장 `b07c66d`(tasks.md 누락)·
  6장 `2336861`(ARCHITECTURE.md 누락)·7장(문서 3개 누락)에서 **세 번 반복됐던** "구현만 커밋되고
  문서는 워킹트리에 남는" 패턴이 `1638473` 이후 두 회차 연속 재발하지 않았습니다.

- **차단당한 뒤 같은 명령을 다시 치지 않았습니다.** 01:09 `sleep 90; tail` 이 막히자
  `ToolSearch` 로 `Monitor` 를 찾아 전환했습니다. 이 회고의 훅 세션 13건이 전부
  `command -v` 를 두 번씩 다시 친 것과 대조됩니다.

- **주석 게이트 0건 유지 — 파일이 112 → 117 로 늘었는데도.** 신규 5파일
  (`adapters/reranking/*`, `test_reranking*.py`)이 규칙 아래로 들어왔고,
  밀려난 설명은 지우지 않고 `tests/README.md` 로 옮겼습니다(`+20`).

### Problem (나빴던 것)

- **`add-cross-encoder` 가 origin 에 없습니다. 미푸시 3건.** `origin` 에는 브랜치 10개가
  있는데 `add-cross-encoder` 는 그중에 없습니다. 스펙 745줄(`0145a27`)과 구현 787줄(`ba4e9d6`)이
  전부 로컬에만 있습니다. **직전 회고(00:54)가 정확히 이 항목을 지적했고 28분이 지났습니다.**
  더 앞선 회차에서 `add-response-cache` 가 같은 상태로 다섯 회차를 이월하다 23:36 `git push`
  한 줄로 닫힌 적이 있는데, 브랜치가 바뀌자 **같은 모양이 처음부터 다시** 돌고 있습니다.

- **`ruff format --check` 13건, 열세 번째 재현.** 직전 16건에서 줄었지만 고쳐서가 아니라
  이번 신규 파일들이 처음부터 포맷을 맞춰 들어와 분모가 커진 결과입니다. 기존 13건은
  그대로입니다. 같은 시점 `.venv/bin/ruff check src tests` 는 통과하므로
  **초록불이 거짓**이고, 닫는 비용은 여전히 `ruff format src tests` 한 줄입니다.

- **`add-cross-encoder` 65태스크 중 16개만 체크(49개 미완).** 특히 `결정 11` 계측 표의
  **이미지 크기 증가 칸이 아직 `아직` 으로 비어 있습니다** — 가중치가 1.2GB → 2.2GB 로
  바뀐 직후라 이 숫자는 `README.md` 의 빌드 시간 안내와 직결되는데, 재지 않은 채
  "+2.2GB" 라고 적혀 있습니다(가중치 크기지 이미지 증가분이 아닙니다).

- **`add-multiturn-qa` 가 25시간 넘게 untracked — 열 번째 계수.** 그 사이 change 셋이
  제안→아카이빙을 완결했습니다. 쓸 것이면 커밋하고 아니면 지워야 하는데,
  회고가 열 번 세는 동안 둘 다 일어나지 않았습니다.

- **Retrobot 이 창 에러의 79%(49/62) — 17회차.** `SKILL.md` 는 `afa9794`(08-03 22:23) 이후
  **약 27시간 무수정**입니다. 훅 13세션이 **13/13** `command -v python3 jq` 거부로 시작하고
  **12/13** 이 `/tmp` Write 거부로 이어집니다. 회고 열한 편이 같은 진단을 반복했고
  아무것도 바뀌지 않았습니다. 대가를 실제로 치른 사례도 그대로입니다 — `115a7e25` 는
  두 번 막힌 뒤 `python3` 재시도 없이 **Grep 30회**로 우회했습니다.
  (이 회고는 `python3 -c` **단일 호출 3번**으로 집계됐습니다.)

- **외부 적대적 검토 0회 — 열일곱 세션 연속.** 이 창에서 설계가 사람 지적으로 다섯 번
  뒤집혔고 `openspec validate --strict` 는 한 건도 잡지 못했습니다. 그런데 지금 붙이는
  리랭킹은 상위 K 의 **내용**을 바꾸는 단계입니다 — 틀리면 테스트가 초록인 채로
  답변 품질만 조용히 나빠집니다.

### Try (개선해야 할 것)

1. **지금 push.** `git push -u origin add-cross-encoder` 한 줄. 커밋 3건, 스펙+구현 1532줄이
   평가 대상에 들어갑니다. 다음 회고가 이 항목을 세 번째로 세게 두지 마세요.
2. **`ruff format src tests` 를 다음 커밋 전에 실행.** 13건이 열세 회차를 넘기는 것은
   난이도가 아니라 우선순위의 문제입니다. 닫은 뒤 `.githooks/pre-commit` 에
   `ruff format --check` 를 넣어 열네 번째가 없게 하세요.
3. **이미지 증가분을 실측해 `결정 11` 을 채우기.** `docker image ls` 전후 차이를 재고,
   `design.md` 의 `아직` 과 `README.md` 의 빌드 시간 안내를 같은 숫자로 맞추세요.
   가중치 2.2GB 는 이미지 증가분이 아닙니다.
4. **`SKILL.md` 를 고치기 — 열일곱 번째 진단 대신.** 세 줄이면 끝납니다.
   (a) `command -v python3 jq` 를 지우고 `python3 --version` 단독으로 대체(이 세션에서 통과 확인),
   (b) `/tmp` Write 절차를 지우고 `python3 -c` 단일 호출을 1순위로 승격
       (`>`·`;` 를 붙이는 순간 거부되므로 리다이렉트 금지를 명시),
   (c) heredoc 금지에 **호스트 조건을 달기** — Windows 사실인데 구분이 없어 macOS 의
       되는 경로를 막고 있습니다.
5. **`add-multiturn-qa` 처분.** 커밋하거나 지우거나. 열 번을 셌으면 판단할 재료는 충분합니다.
6. **리랭킹 구현에 외부 검토 1회.** 상위 K 의 내용을 바꾸는 단계라 지금이 반영 비용이
   가장 싼 구간입니다. 회귀 질의 4개가 "나빠졌다" 를 실제로 붙잡는지가 검토 대상입니다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 08-04 02:06~02:08 | `e4260f90` | 회고 TIMELINE 갱신 (에러 1) |
| 08-04 19:24 | `2d412d02` | `/exit` |
| 08-04 20:13~20:48 | `45f0bdd9` | 캐시 3상태 검토, `REPLACED`·`insufficient_evidence` 지적 (에러 0) |
| 08-04 20:49~23:35 | `75cd4368` | `add-response-cache` 2~7장 완주 + 아카이빙, Bash 207 (에러 10) |
| 08-04 20:49~00:57 | 훅 12세션 | Retrobot 회고 12편 (에러 45) |
| 08-04 23:41~23:46 | `7656b215` | RAG 파이프라인 현황 점검 (에러 1) |
| 08-05 00:16~00:57 | `34e9f10c` | 1장 코어 리랭킹 마감, **968 passed** |
| 08-05 00:58~01:08 | `34e9f10c` | 2장 어댑터 — gte 실측 로딩 실패 확인 |
| 08-05 01:09~01:11 | `34e9f10c` | bge 대안 실측, `sleep` 차단 → `Monitor` 전환, 사용자 지시 도착 |
| 08-05 01:12~01:20 | `34e9f10c` | 모델 교체를 코드·스펙 5파일에 동시 반영, `validate --strict` |
| 08-05 01:21 | `34e9f10c` | `ba4e9d6` 커밋 — 14파일 **+787/-50**, **986 passed, 2 skipped** |
| 08-05 01:22 | `518ba2d1` | 이 회고 |

## 지표

| 지표 | 직전 (00:54) | 현재 | 추이 |
|---|---|---|---|
| 테스트 | 968 passed, 23 deselected | **986 passed, 2 skipped, 23 deselected** | +18 |
| `check_comments.py` | 112파일 0건 | **117파일 0건** | 유지 |
| `ruff format --check` | 16건 | **13건** | 신규 파일이 분모를 키운 결과 |
| `add-cross-encoder` tasks | 0/65 | **16/65** | +16 |
| 미푸시 커밋 | 1 | **3** | origin 에 브랜치 없음 |
| `add-multiturn-qa` | untracked (9회차) | **untracked (10회차)** | 25시간 초과 |

## 이 세션에서 확정된 환경 사실

- `python3 --version` **단독은 통과**합니다. `command -v python3` 은 **단독이어도 거부**됩니다.
- `python3 -c "..."` 단독 호출은 통과하나, `> file` 리다이렉트나 `;` 연결을 붙이면 거부됩니다.
- `/tmp` 에 `Write` 는 거부됩니다(창 누적 **13/14**).
- 포그라운드 `sleep N; <cmd>` 는 차단됩니다 — `Monitor` 를 쓰세요.
