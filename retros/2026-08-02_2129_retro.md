# Retrobot 회고 — 2026-08-02 21:29 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 2 (구현 1 + Retrobot 1) |
| 총 프롬프트 | 1 (`/opsx:apply add-qa-retrieval — TASK 3장`) |
| 에러 발생 | 3 (구현 세션 2 + Retrobot 세션 1) |

직전 회고(21:19) 이후 창(21:16~21:29)에 새로 생긴 활동만 다룹니다. 지정된 로그 16개 중
나머지 15개는 16:07·17:47·21:05·21:19 회고에서 이미 다뤘습니다.

## 세션별 활동

### 세션 `a626c3aa` (21:16 ~ 21:25) — 3장 구현

**프롬프트:**
1. `/opsx:apply add-qa-retrieval` — "TASK 3장에 있는 내용을 구현한다"

**한 일:**

| 태스크 | 내용 |
|--------|------|
| 3.1 | `Embedder` 프로토콜 `count_tokens` → `count_document_tokens` 개명 + `count_query_tokens` 추가 (`adapters/protocols.py:109`) |
| 3.2 | `SentenceTransformerEmbedder` 두 메서드가 각자 역할 접두사를 붙여 `_count()`에 위임, 특수 토큰 포함 실제 인코딩 길이 반환 (`adapters/embedding/local.py:147`) |
| 3.3 | 호출부 갱신 — `IngestionService._guard_tokens`(2곳) · `FakeEmbedder` · `test_ingestion_pipeline.py`. 수집 스위트 전부 통과 |
| 3.4 | 임베더 테스트 3건 추가 — 접두사 인코딩 여부 / 두 값의 차이가 접두사 길이 / **실물 모델로 `count_query_tokens`가 `model.tokenize()` 실제 길이와 일치** |
| 3.5 | `ARCHITECTURE.md`에 토큰 예산 역산 기록 + 스테일해진 `count_tokens` 서술 2곳 정정 |

진행률 14/66 → **19/66**. 커밋 `73c821f` (+182/-26, 8파일).
전체 스위트 340 → **344 passed** (실물 임베딩 테스트 5건 스킵 없이 실행),
`ruff check .` · `ruff format --check .` 모두 통과.

**발생한 에러:** 2건 (둘 다 즉시 자기교정)
- `ModuleNotFoundError: No module named 'huggingface_hub'` — 시스템 `python3`로 실측을
  시도. `.venv/bin/python`으로 바꿔 해결.
- `ls -d .venv venv` 종료코드 1 — `venv`가 없어서. 실패가 아니라 탐색 결과.

### 세션 `a9355fb9` (21:26 ~) — Retrobot 자동 회고 생성

**발생한 에러:** 1건 — `python3 -c` 권한 거부.

## KPT 회고

### Keep (좋았던 것)

- **설계 문서에 이미 적혀 있던 숫자를 그대로 옮기지 않고 다시 쟀습니다.** design 결정 6이
  "최악 5.016 토큰/문자"라고 못 박아 두었는데, 3.5를 쓰기 전에 실물 모델을 다시 로드해
  `㌚`(U+331A) 반복 20자 5.05 · 100자 5.01 · 1000자 5.001, 한국어 평문 0.50~0.57,
  `507/5.016 = 101.07`을 재확인했습니다. 문서에 적히는 순간 그 숫자는 검증 대상이 아니라
  근거가 되므로, 옮겨 적기 전에 한 번 더 재는 것이 맞습니다.
- **실물 일치 테스트를 페이크로 대체하지 않았습니다.** 3.4의 핵심은 `count_query_tokens`가
  `model.tokenize()`가 **실제로 모델에 먹이는** 토큰 열 길이와 같은지입니다. 이게 어긋나면
  가드는 통과시키는데 입력은 잘리는, 오류 없이 조용히 틀리는 형태가 됩니다. 가중치가 로컬에
  캐시돼 있어 5건이 스킵 없이 실행됐고, 스킵됐다면 "통과"가 아무것도 보장하지 않았을
  자리입니다.
- **`FakeEmbedder`가 두 경로에 같은 수를 돌려주게 두었습니다.** 접두사가 없는 페이크에
  인위적 차이를 만들면 검증 대상이 페이크가 됩니다 — 벡터를 대칭으로 남긴 결정 12와 같은
  논리를 일관되게 적용.
- **문서가 미구현을 구현으로 말하지 않도록 경계를 명시했습니다.** 3.5의 두 상한 표는
  계산·선언까지이고 **실제 가드를 거는 자리(`POST /search`)는 6장**이라고 적어 두었습니다.
  과제 기준에서 허위 기재는 감점이 아니라 불합격이므로, 9.5 대조 때 이 문장이 여전히 맞는지
  보면 됩니다.
- **직전 회고의 Try 1·4가 실제로 닫혔습니다.** `retrobot/_extract.py` 잔해가 사라졌고,
  이번 세션은 범위를 좁히지 않은 `ruff check .` 를 그대로 돌려 통과했습니다. "초록으로 보이는
  범위를 고른" 상태가 한 회차 만에 해소됐습니다.
- **개명(3.1)을 이름만 바꾸고 끝내지 않았습니다.** 왜 역할별로 메서드가 둘이어야 하는지
  (역할마다 인코딩되는 문자열이 다르므로 하나로 뭉치면 한쪽이 반드시 틀리고, 그 틀림이 조용한
  절단으로만 드러난다)를 docstring에 남겼습니다.

### Problem (나빴던 것)

- **실측을 시스템 `python3`로 먼저 시도했습니다.** 이 프로젝트는 `.venv`에 의존성을 깔고
  Makefile도 전부 `$(VENV)/bin/...` 을 부르는데, 첫 시도가 시스템 인터프리터였습니다.
  같은 실수를 이전 세션들에서도 반복한 흔적이 있습니다 — 한 번 물어보고 아는 게 아니라
  매번 처음부터 부딪히고 있습니다.
- **`동점 결정성`(Codex medium 지적)이 네 번째 이월입니다.** 17:47 · 21:05 · 21:19에
  이어 이번에도 tasks.md에 태스크로 박히지 않았습니다. `openspec/changes/add-qa-retrieval/`
  전체에 "동점"이라는 단어가 없습니다. 직전 Try 3이 "회고 문서 바깥으로 옮기는 것이 이번
  회차의 조건"이라고 못 박았는데 그대로 회고 문서 안에 남았습니다. (`하한 계측`은 5.3으로
  이미 계획에 있으므로 이월이 아닙니다 — 두 건을 묶어 세던 지난 회고들의 계산이 부정확했습니다.)
- **Retrobot 훅의 허용 목록은 여전히 그대로입니다.** `.githooks/post-commit:78` 은
  `Bash(git:*)` · `Read` · `Glob` · `Grep` · `Edit(retros/**)` 뿐입니다. 이번 회차가
  거부 1건으로 끝난 것은 훅을 고쳐서가 아니라 프로젝트 권한 설정이 `sed`·`cut` 같은 읽기
  명령을 따로 허용해 준 덕입니다. 원인은 그대로 있고 증상만 가벼워졌습니다.

### Try (개선해야 할 것)

1. **`동점 결정성`을 지금 tasks.md 5장에 태스크로 넣는다.** 4장(저장소 질의) 착수 전
   1분이면 됩니다. 5장은 정렬·하한이 실제로 붙는 장이라 여기서 놓치면 구현이 끝난 뒤
   되짚어야 합니다. 다섯 번째 이월을 막는 유일한 방법은 회고가 아니라 계획에 적는 것입니다.
2. **`.githooks/post-commit:78` 의 `--allowedTools` 에 `Bash(python3:*)` 를 추가한다.**
   세 회차 연속 Try에 올라온 한 줄 수정입니다. 이번엔 우연히 싸게 끝났지만 프로젝트 권한
   설정이 바뀌면 다시 8건짜리 회차로 돌아갑니다.
3. **실측·스크립트 실행은 `.venv/bin/python` 으로 시작한다.** CLAUDE.md나 tasks.md 어디에도
   이 사실이 없어 매 세션 처음부터 부딪힙니다. 한 줄이면 프로젝트 지침에 남길 수 있습니다.
4. **3.5가 "6장에서 붙는다"고 적은 문장을 9.5 체크리스트에 명시적으로 건다.** 6장이 실제로
   가드를 붙이면 그 문장은 스테일해집니다. 지금은 정직한 서술이지만, 고치는 것을 잊으면
   방향만 반대인 같은 종류의 불일치가 됩니다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 21:16 | `a626c3aa` | `/opsx:apply add-qa-retrieval` — 3장 지정, 컨텍스트·대상 코드 읽기 |
| 21:17 | `a626c3aa` | 시스템 `python3` 실측 실패(`huggingface_hub` 없음) → `.venv/bin/python` 으로 전환 |
| 21:18 | `a626c3aa` | 실물 모델 로드, 특수 토큰 2 · `query: ` 3 · `passage: ` 2 · 창 512 재측정 |
| 21:19 | `a626c3aa` | 3.1 프로토콜 개명·추가 → 3.2 어댑터 구현 → 3.3 호출부(`_guard_tokens`·`FakeEmbedder`) 갱신 |
| 21:21 | `a626c3aa` | 3.4 테스트 3건 추가, 임베더 스위트 30 passed / 실물 5 passed(스킵 0) |
| 21:22 | `a626c3aa` | 최악 문자 `㌚` 5.05 재확인, `507/5.016 = 101.07` 역산 |
| 21:23 | `a626c3aa` | 3.5 `ARCHITECTURE.md` 토큰 예산 절 추가(+45), 스테일 서술 2곳 정정 |
| 21:24 | `a626c3aa` | `ruff check .` · `format --check` · 전체 스위트 **344 passed** |
| 21:26 | — | 커밋 `73c821f` feat: 임베딩 토큰 계산을 질의 경로까지 연결 (19/66) |
| 21:26 | `a9355fb9` | Retrobot 이번 회고 생성 (권한 거부 1건) |
