# Retrobot 회고 — 2026-08-04 21:49 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 19 (실작업 7 · Retrobot 훅 9 · 서브에이전트 3) |
| 총 프롬프트 | 사람이 직접 친 것 15 (중단 2건 별도) |
| 에러 발생 | 58 (그중 **45건이 Retrobot 훅 자신**) |
| 커밋 | 3 (`a466d72` · `3eda547` · `b07c66d`) — 회고 커밋 2건 제외 |
| 창 범위 | 08-03 21:48 ~ 08-04 21:47 |

## 세션별 활동

### 실작업 세션

| 세션 | 시각 | 도구 | 에러 | 활동 |
|------|------|------|-----:|------|
| `4f55c9d8` | 08-03 21:48~22:22 | Bash 55 · Read 10 · Edit 1 | 0 | "LLM 5개 파일 작업 진행해주세요" |
| `8ccc760d` | 08-03 22:23~23:42 | Bash 73 · Edit 72 · Read 38 · Write 6 | 3 | `/opsx:apply` — SSE 스트리밍 구현. "스트리밍은 어떤 방식 쓰고 있지?" |
| `724d2218` | 08-03 22:47~08-04 00:31 | Write 56 · Bash 71 · Edit 19 · chrome 20 · Agent 3 | 3 | `add-demo-ui` 프런트엔드. 서브에이전트 3인 조사, 브라우저로 업로드·삭제 실측 |
| `58b22a1f` | 08-04 00:31~00:51 | Bash 17 · Write 6 | **0** | `add-multiturn-qa` 제안 |
| `2d412d02` | 08-04 00:51~19:24 | Bash 24 · Write 7 | **0** | `add-rrf-algorithm-spec` 제안 |
| `45f0bdd9` | 08-04 20:13~20:48 | Bash 30 · Edit 22 · Write 6 | **0** | `add-response-cache` 제안 + 사용자 검토 4연속 |
| `75cd4368` | 08-04 20:49~21:47 | Bash 52 · Edit 42 · Read 21 · Write 10 | 5 | 캐시 1·2·3장 구현, `scope` 수정, README 숫자 정정 |

### Retrobot 훅 세션 (9회)

`12eec827`(22:23) · `9d06bafd`(22:46) · `d0c01226`(23:43) · `cc79ede2`(00:30) · `e4260f90`(01:15) ·
`a2542476`(20:49) · `115a7e25`(21:00) · `e27b60d4`(21:14) · `7a93d660`(21:22)

에러 45건. **9회 중 8회가 같은 두 줄에서 막혔습니다** — `command -v python3 jq` 승인 거부 → `/tmp` Write 거부.
예외는 `115a7e25` 하나로, Bash를 포기하고 `Grep` 30회로만 집계했습니다.

## KPT 회고

### Keep (좋았던 것)

- **캐시 3장을 한 시간에 마감했고, 장 단위로 커밋 경계가 살아 있습니다.**
  `a466d72`(저장소, +732/-27) → `3eda547`(scope 스펙 수정, 코드 0줄) → `b07c66d`(Redis 어댑터, +1,128/-9).
  중간에 스펙만 고친 커밋이 따로 서 있는 것이 중요합니다 — `qa:v1:index` 단일 ZSET이 `top_k`를 도로 합치는
  결함을 코드로 덮지 않고 설계로 되돌린 흔적이 커밋 이력에 남았습니다.

- **`redis` 마커로 실행 층을 갈랐습니다.** 기본 `docker compose run --build --rm test`는 Redis 없이
  `861 passed, 23 deselected`, 실물 층은 `pytest -m redis`로 `21 passed`. 과제가 "구독 없이 실행되면 가산점"이라
  적은 것과 같은 모양이고, `llm` 마커에서 이미 쓴 방식을 재사용해 새 개념을 늘리지 않았습니다.

- **사용자 검토가 스펙을 뒤집은 회차가 또 나왔습니다.** 20:31~20:43에 네 번 연속으로
  ("질문 캐시가 맞나" → 껐을 때/Redis 죽었을 때 동작표 → "수정해주세요" → REPLACED·`insufficient_evidence` 무효화 확인)
  `--strict` 통과 뒤에 사람이 결함을 잡았습니다. 20:50 회고가 지적한 것과 같은 패턴이고, 이번에도 통했습니다.

- **문서 숫자를 코드와 같은 커밋에서 맞췄습니다.** 21:45 "23건 테스트 왜 생략된거지?" 질문이
  `README.md:505`의 `660 passed, 2 deselected`가 낡았다는 것을 드러냈고, 같은 커밋에서
  `861 passed, 23 deselected — 제외된 23건은 실물 CLI 2건과 실물 Redis 21건`으로 고쳤습니다.
  `CLAUDE.md`가 **허위 기재는 감점이 아니라 불합격**이라고 적은 항목입니다.

- **`--build` 준수 다섯 세션 연속.** 21:37·21:43 두 번의 전체 검증 모두 `--build`가 붙어 있습니다.

### Problem (나빴던 것)

- **Retrobot 샌드박스 벽이 이 창에서만 9회 중 8회 재발했습니다.** 창 전체 에러 58건 중 **45건(78%)이
  과제 코드가 아니라 회고 도구 자신**입니다. 매 회차가 `command -v python3 jq`로 시작해 거부당하고,
  `/tmp`에 스크립트를 쓰려다 또 거부당한 뒤에야 우회로를 찾습니다.
  `SKILL.md:25`는 여전히 "`/tmp` 아래에 쓴다"라고 적혀 있고, `:54`는 heredoc을 **금지 목록에** 둡니다.

- **그런데 그 금지 목록이 이 호스트에서는 틀렸습니다.** 같은 시간대 실작업 세션 `75cd4368`은
  `python3 - <<'EOF'`를 21:26·21:34·21:43 **세 번 성공적으로** 썼습니다. 금지는 Windows 호스트에서
  관측된 사실인데 `SKILL.md`가 호스트를 구분하지 않고 적어, macOS에서 되는 수단이 막힌 것으로 읽힙니다.
  규칙이 틀린 채로 남아 있으니 매 회차가 우회로를 처음부터 다시 탐색합니다.

- **3장 완료 체크 10개가 커밋에서 빠졌습니다.** `b07c66d`는 13파일 +1,128을 담았지만
  `openspec/changes/add-response-cache/tasks.md`가 빠져 `3.1`~`3.10`이 전부 `[ ]`인 채 워킹트리에만 있습니다.
  2장 커밋 `a466d72`에는 `tasks.md`가 들어 있었으므로 **이번 회차에서 퇴행한 것**입니다.

- **`add-multiturn-qa`가 21시간째 untracked입니다.** 01:18 회고가 "168시간 창 판정은 committer date 기준이라
  지금 상태로는 어디에도 안 잡힌다"라고 적은 그 디렉터리가 지금도 `??`입니다. 그 사이에
  `add-rrf-algorithm-spec`·`add-response-cache` 두 change가 커밋됐으므로 잊은 것이 아니라 **건너뛰고 지나간** 것입니다.

- **주석 게이트가 창 내내 exit 1입니다.** `src`만 돌려도 `config.py:91` 인라인 주석 3줄 위반이
  21:11·21:37·21:44 세 번 관측되고 한 번도 고쳐지지 않았습니다. 인자 없는 전체 실행은
  **107개 파일, 위반 484건**(직전 회고의 432건에서 늘었습니다). `CLAUDE.md:74`가 판정 기준으로 지정한 명령입니다.

- **`openspec validate --change`로 한 번 헛돌았습니다**(21:19, `unknown option '--change'`).
  CLI 인자 형태를 세션마다 다시 더듬는 패턴이 반복됩니다.

- **외부 검토 0회가 아홉 세션 연속입니다.**

### Try (개선해야 할 것)

1. **`retrobot/SKILL.md`를 이번 창의 실측으로 고칩니다.** 세 곳입니다 —
   `:25` "`/tmp` 아래에 쓴다" → "**`python3 -c '…'` 작은따옴표 한 겹**으로 인라인 실행한다(f-string과 `{` 직후 따옴표는 피한다)",
   `:54` heredoc 금지 → "**Windows 호스트에서만** 막힌다. macOS에서는 `python3 - <<'EOF'`가 동작한다(`75cd4368` 21:26·21:34·21:43)",
   `:39` `command -v python3 jq` → 승인 프롬프트를 부르지 않는 **`python3 -c "print(1)"` 단일 명령**으로 교체.
   이 세 줄이 그대로면 다음 회차도 45건짜리 에러를 다시 만듭니다.

2. **`tasks.md`를 구현 커밋에 묶는 것을 커밋 직전 점검에 넣습니다.** `git status --short`에
   `openspec/changes/*/tasks.md`가 `M`으로 남아 있으면 커밋을 만들지 않습니다.
   지금 당장은 3장 체크 10개를 다음 커밋에 함께 올립니다.

3. **`add-multiturn-qa`를 이번 세션 안에 커밋합니다.** 내용을 더 손대지 말고 있는 그대로 올려
   168시간 창 안에 committer date를 박습니다.

4. **`src/app/config.py:91`을 지금 고칩니다.** 인라인 주석 3줄을 2줄로 줄이거나 근거를 `ARCHITECTURE.md`로
   옮깁니다. 한 줄짜리 위반이 네 회차째 살아 있는 것은 게이트를 신뢰하지 않게 만듭니다.

5. **`tests/` 484건은 별도 change로 분리합니다.** 매 회고가 숫자만 갱신하며 이월하는 중이고 늘고 있습니다.
   지금 손댈 수 없다면 "이 창에서는 안 한다"를 명시적으로 적어 이월 항목에서 뺍니다.

6. **캐시 4장(오케스트레이션) 전에 Codex 검토를 한 번 겁니다.** 아홉 세션 연속 외부 검토가 없었고,
   4장은 `/qa` 경로에 직접 붙는 곳이라 되돌리기 비용이 가장 큽니다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 08-03 21:48 | `4f55c9d8` | LLM 5개 파일 작업 (Bash 55, 에러 0) |
| 08-03 22:23 | `8ccc760d` | `/opsx:apply` — SSE 스트리밍 구현 시작 |
| 08-03 22:45 | `8ccc760d` | "스트리밍은 어떤 방식 쓰고 있지?" |
| 08-03 22:47 | `724d2218` | `add-demo-ui` 제안 → 서브에이전트 3인 조사 (API 계약·리포 관습·UI 설계) |
| 08-03 22:57 | `724d2218` | "POST + SSE라서 EventSource 못쓰는 이유는?" |
| 08-03 23:44 | `724d2218` | SSE change 완료 → 프런트엔드 착수 |
| 08-04 00:04 | `724d2218` | Claude in Chrome 연결, 브라우저로 업로드·삭제 실측 |
| 08-04 00:31 | `58b22a1f` | `add-multiturn-qa` 5아티팩트 (에러 0) |
| 08-04 00:54 | `2d412d02` | `add-rrf-algorithm-spec` 7아티팩트 (에러 0) |
| 08-04 20:15 | `45f0bdd9` | `add-response-cache` 4아티팩트, 사용자 검토 4연속으로 3건 뒤집힘 |
| 08-04 20:49 | `75cd4368` | 1장 캐시 도메인 → `23acd3a` |
| 08-04 21:14 | `75cd4368` | 2장 캐시 저장소 → `a466d72` (+732/-27) |
| 08-04 21:22 | `75cd4368` | `scope` 도입 스펙 수정 → `3eda547` (코드 0줄) |
| 08-04 21:37 | `75cd4368` | 전체 검증 `861 passed, 23 deselected` / `-m redis` `21 passed` |
| 08-04 21:45 | `75cd4368` | "23건 테스트 왜 생략된거지?" → `README.md:505` 낡은 숫자 정정 |
| 08-04 21:46 | `75cd4368` | 3장 Redis 어댑터 → `b07c66d` (13파일 +1,128/-9) |
