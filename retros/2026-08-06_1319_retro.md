# Retrobot 회고 — 2026-08-06 13:19 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 7 (실작업 4 · Retrobot 훅 3) |
| 총 프롬프트 | 22 (실질 요청 11 · 인터럽트 8 · 훅 지침 3) |
| 에러 발생 | 25 (그중 14건 = 56% 가 Retrobot 자신) |

직전 회고가 13:10 이므로 **증분은 9분**입니다. 그 9분에 커밋 하나(`56341eb`)가 났고,
24시간 집계는 아래에 그대로 둡니다.

## 세션별 활동

### 세션 `bc5f4779` (13:07 ~ 13:18 KST) — 이번 증분

**프롬프트:**
1. `demo-ui` 수정 — QA 응답에서 검색된 청크가 그대로 나오는데, LLM 답변이 먼저 오고
   "인용한 근거 N건" 아래에 청크가 오도록, 클릭하면 근거 내용이 펼쳐지도록
2. (선택지 중) "C로 진행해주세요"

**산출:** `56341eb "fix: demo ui 프론트엔드 화면 수정"` — 6파일 `+205/-31`

**발생한 에러:** 1건 (`cd demo-ui` 실패 — 이미 그 디렉터리에 있었음)

### 세션 `8f673272` (10:43 ~ 13:07 KST)

PDF 파서 회차. 13:10 회고에서 이미 다뤘고, 마지막 2분에 "프론트엔드 UI도 켜주세요"가
들어와 다음 세션으로 이어졌습니다. 에러 3건.

### 세션 `f13eeb41` (00:07 ~ 00:49) · `c69d384c` (23:09 ~ 00:07)

`pymupdf4llm` 파라미터 확인과 스펙 작성. 에러 7건 중 4건이 도구 사용 거절
(인터럽트로 방향을 되돌린 흔적).

### 세션 `e1686297` · `e76cada6` · `0e8a88f5` — Retrobot 훅 3회

프롬프트는 셋 다 `SKILL.md` 전문. 셋 다 같은 세 지점에서 막혔습니다.
에러 14건.

## KPT 회고

### Keep (좋았던 것)

- **검증이 실물까지 갔습니다.** `bc5f4779` 는 `npm run build` → `docker compose up -d
  --build --wait` → 실물 `/qa` SSE 를 `curl -N` 으로 받아 페이로드를 눈으로 확인하고 →
  `check_comments.py` 까지 밟은 뒤 커밋했습니다. `CLAUDE.md` 가 함정으로 못 박은
  `--build` 를 이번에도 지켰습니다(열 세션 연속).
- **`joinCitations` 가 없는 값을 지어내지 않습니다.** `done` 은 인용을 싣고 본문은 안
  싣고, `sources` 는 본문을 싣습니다. 둘을 `(document_id, chunk_index)` 로 맞붙이면서
  짝을 못 찾은 경우를 빈 문자열이 아니라 `null` 로 두고 주석에 이유를 남겼습니다 —
  화면이 "본문이 비어 있다"고 말하는 것과 "본문을 못 찾았다"는 다른 사실입니다.
- **캐시 히트 경로에서도 그 맞붙임이 성립합니다.** `qa.py:81` 의
  `SourcesEvent.cached(entry)` 가 저장된 `ScoredChunk` 를 그대로 실어 보내므로,
  캐시로 답이 나가도 청크 본문이 함께 갑니다. UI 가 캐시 히트에서만 근거를 못 펴는
  경로는 없습니다(코드로 확인).
- **선택지를 묻고 갔습니다.** `AskUserQuestion` 2회로 표현 방식을 확정한 뒤 손을 댔고,
  9분 만에 커밋까지 닫혔습니다.

### Problem (나빴던 것)

- **`joinCitations` 는 이 커밋이 추가한 유일한 로직인데 테스트가 없습니다.**
  `demo-ui/package.json` 에 테스트 러너 자체가 없고(`dev`·`build`·`preview` 뿐,
  devDependencies 에 vitest·jest 없음) 게이트는 `tsc -b` 하나입니다. 타입은
  키 조합이 어긋나는 것을 잡지 못합니다.
- **`add-multiturn-qa` 4아티팩트가 여전히 untracked — 열한 번째 계수, 60.5시간째**
  (08-04 00:48 이후 손 안 댐). 168시간 창에서 커밋 안 된 작업은 없는 작업입니다.
- **브랜치가 계속 갈라집니다.** `main` 은 `9ba55cd`(08-04 23:48)에 멈춰 있고,
  `add-cross-encoder` 는 main 대비 15커밋 `+5,748/-80`,
  `fix-pdf-parser` 는 7커밋 `+2,174/-79`. 둘의 공통 조상이 `main` 이라
  리랭킹 하루치를 못 본 채 PDF 작업이 쌓이는 구도가 그대로입니다.
- **`ruff format --check` 13건.** 직전 16건에서 줄긴 했지만 같은 뿌리(88자 손수
  줄바꿈 vs `pyproject.toml` `line-length = 100`)라 `ruff check` 의 "All checks passed!"
  는 여전히 이 13건을 못 봅니다. 닫는 비용은 `.venv/bin/ruff format src tests` 한 줄.
- **Retrobot 이 24시간 에러의 56%(14/25) — 24회차.** 훅 3세션이 전부 같은 순서로
  죽었습니다: `command -v python3 jq` 거부 → `command -v python3` 단독도 거부 →
  `/tmp` Write 거부 → `> /tmp/...` 리다이렉트 거부. `retrobot/SKILL.md` 는
  `afa9794`(08-03 22:23) 이후 **63시간 무수정**이라 회고 열한 편이 같은 진단만 반복합니다.
- **미푸시 2건**(`de13748`·`56341eb`). 08-04 의 36건에 비하면 작지만, `origin/fix-pdf-parser`
  는 `9eb2976` 에 멈춰 있어 방금 만든 근거 펼치기 UI 는 원격에 없습니다.
- **08-05 회고 6편이 이 타임라인에 없습니다 — 4회차.** `add-cross-encoder` 에만 있어
  `main` 을 여는 사람에게 08-05 는 여전히 없는 하루입니다.

### Try (개선해야 할 것)

1. `demo-ui` 에 vitest 를 넣고 `joinCitations` 만이라도 3케이스(정상 짝·짝 없음·`sources`
   자체가 `null`)를 덮습니다. 러너가 없으면 다음 로직도 같은 이유로 안 덮입니다.
2. `add-multiturn-qa` 를 이번 push 에 함께 올립니다 — 커밋 비용은 한 줄이고,
   열한 회차 이월된 유일한 이유는 아무도 `git add` 를 안 쳤다는 것입니다.
3. `fix-pdf-parser` 를 `add-cross-encoder` 위로 리베이스하거나 둘 다 `main` 에
   병합합니다. 격차가 `+5,748` 대 `+2,174` 인 지금이 가장 쌉니다.
4. `.venv/bin/ruff format src tests` 를 한 번 돌리고 커밋합니다(10초).
5. `retrobot/SKILL.md` 를 고칩니다 — 세 줄이면 됩니다.
   - "먼저 `command -v python3 jq` 를 쳐라" 삭제 (이 호스트에서 8/8 거부, 매 회차 첫 두 에러)
   - "하지 말 것"의 heredoc 금지를 **Windows 한정**으로 표시 (macOS 에서는
     `python3 - <<'PYEOF'` 가 유일하게 통하는 경로이고 이 회고도 그것으로 집계됐습니다)
   - `/tmp` 대신 **세션 스크래치패드**를 쓰라고 적기. 이번 창에서 `bc5f4779` 가
     `/private/tmp/claude-501/<인코딩된-프로젝트>/<세션-id>/scratchpad` 아래에
     실제로 파일을 쓰고 읽었습니다 — 막히는 것은 `/tmp` 가 아니라 **허용 디렉터리 밖**입니다.
6. 08-05 회고 6편을 `main` 계열 타임라인으로 옮깁니다(#3 이 닫히면 함께 닫힙니다).

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 23:09~00:07 | `c69d384c` | `pymupdf4llm` 스펙 작성, 인터럽트 2회 |
| 00:07~00:49 | `f13eeb41` | `to_markdown` 파라미터 확인(`ocr_language` 유무), 인터럽트 4회 |
| 00:07~00:23 | `e1686297`·`e76cada6` | Retrobot 훅 2회 — 같은 3지점에서 막힘 |
| 10:43~13:07 | `8f673272` | OCR 도입 → 전면 철회 → `pymupdf4llm.to_markdown()` 단독, `9eb2976` |
| 13:05~13:12 | `0e8a88f5` | Retrobot 훅 — `Grep` 29회 폴백으로 집계 |
| 13:07~13:18 | `bc5f4779` | 근거 펼치기 UI, 실물 `/qa` 확인 후 `56341eb` |
| 13:19 | (이 회고) | heredoc 으로 집계 |
