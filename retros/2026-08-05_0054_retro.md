# Retrobot 회고 — 2026-08-05 00:54 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 18 (지정 19개 중 활동 있음 / `58b22a1f` 는 창 밖) |
| 총 프롬프트 | 37 (실질 작업 요청 24 · Retrobot 자기 호출 13) |
| 에러 발생 | 61 (그중 **50건(82%)이 Retrobot 훅 자신**) |
| 창 안 커밋 | 43 (기능·문서 26 · 회고 17) |
| 미푸시 | 1 (`0145a27`) — `add-cross-encoder` 브랜치가 origin 에 없음 |

## 세션별 활동

### 작업 세션

#### `2d412d02` (08-04 00:54 ~ 19:24) — RRF 제안의 잔여

`/opsx:propose add-rrf-algorithm-spec`(하이브리드 검색·BM25·인덱싱/캐싱까지). 19:24 `/exit` 로 닫힘. 에러 0건.

#### `45f0bdd9` (08-04 20:13 ~ 20:48) — L1/L2 캐시 설계

`/opsx:propose` 로 "L1 Exact 히트 / L2 Semantic 히트" 방향을 세운 뒤, **사용자 지적이 설계를 네 번 고쳤습니다.**

1. 20:31 "사용자가 질문했을 때의 캐시를 설명하는 게 맞나요?" — 캐시 대상의 정의 확인
2. 20:34 운영자가 껐다 / Redis 없이 테스트가 돈다 / Redis 가 죽었다 — **세 상황의 원하는 동작 표**를 직접 제시
3. 20:43 "`REPLACED` 가 no-evidence 미삭제하는 것 확인" · "`insufficient_evidence` 무효화 누락 확인"

세 번째 지적이 두 시간 뒤 6장 무효화 설계의 축이 됐습니다. 에러 0건, Edit 22회.

#### `75cd4368` (08-04 20:49 ~ 23:35) — `add-response-cache` 71/71 완주

창 전체에서 가장 무거운 세션(1447 엔트리, Bash 207 · Edit 96 · Write 16). 프롬프트 14건이 전부 장 단위 `/opsx:apply` 였고, 2장→7장을 2시간 45분에 닫고 `/opsx:archive` 까지 갔습니다.

- 21:45 **"23건 테스트 왜 생략된거지?"** — `deselected` 를 지나치지 않고 물은 것이 이 세션의 값이 가장 큰 한 줄입니다(Redis 마커 분리 확인).
- 21:16 `/opsx:update ... scope 를 반영해주세요` — 코드가 붙기 전에 스펙을 고쳐 수정 비용이 0인 시점에 들어갔습니다.
- 22:18 "CLI 실행해서 기본 모델 측정해 설정값에 넣으면 안 되나요" — 컨테이너에서 실측까지 한 뒤 **거절**(빈 값을 어댑터가 `thread/start` 에 넘기므로 적는 순간 관측이 지시가 됨).
- 22:55 "나중에 작업 진행하겠습니다. 명시해주세요" — 미완을 완료로 칠하지 않고 남겼습니다.

에러 10건은 전부 게이트가 잡은 것(`E501`, 주석 위반 `config.py:91`, `ruff format` 차이)이고 단언 완화가 아니라 코드 수정으로 닫혔습니다.

#### `7656b215` (08-04 23:41 ~ 23:46) — 파이프라인 점검 + 아카이빙

"현재까지 RAG 파이프라인 어떤 로직 구현되어있는지 확인" → `/opsx:archive add-answer-generation`. 구현 현황을 먼저 읽고 아카이빙한 순서가 맞습니다.

#### `34e9f10c` (08-05 00:16 ~ 00:51) — 크로스인코더 제안

`/opsx:propose add-cross-encoder` 가 **745줄 스펙 7파일**(`0145a27`)로 나왔습니다. 축은 "지금 순위를 정하는 신호는 RRF 합의도 하나뿐이고, *이 청크가 이 질문에 실제로 답하는가* 는 아무도 보지 않는다".

- 밀집과 어휘가 **같은 방향으로 틀리면** 융합 점수가 정확히 `1.0` 이 되고 융합은 구조적으로 그것을 구별하지 못한다 — 기존 `ARCHITECTURE.md` 의 관측(`"연차를 팔 수 있나요?"`)을 근거로 씀
- 모델을 `Alibaba-NLP/gte-multilingual-reranker-base` 로 고른 이유를 **"다국어여야 한다는 것이 선택의 전부"** 로 좁혀 적음(영어 전용 리랭커는 한국어에서 무작위 순위)
- **범위 밖을 먼저 못 박음** — 크로스인코더 점수 기반 하한은 정답 라벨이 없어 값을 잴 수 없으므로 넣지 않는다
- 리랭커 실패는 검색을 세우지 않고 `ordered_by: "fusion"` 으로 강등, 리랭커 구성은 LLM 캐시 키에 들어감

00:51 `/opsx:apply add-cross-encoder 1장` 이 이 회고와 **동시에 진행 중**입니다 — 회고 집계 시점(00:54)에 `core/reranking.py` 49줄과 `test_reranking_core.py` 141줄이 워킹트리에 있고 `core/retrieval.py` 가 `+5/-1`. `tasks.md` 체크는 **65개 중 0개**라 진행분이 아직 태스크에 반영되지 않았습니다.

### Retrobot 훅 세션 (13개)

`e4260f90` `a2542476` `115a7e25` `e27b60d4` `7a93d660` `4e480d75` `8486165b` `25644f92` `cf9d24e5` `3f5b5605` `62743af5` `ed436f8b` `5db6209d` — 모두 프롬프트 1건(`SKILL.md` 자신), 모두 같은 두 줄에서 막혔습니다.

**발생한 에러 (창 전체 61건 중 50건):**

- `command -v python3 jq` 승인 거부 — **15건**. 단독으로 써도 거부됩니다
- `/tmp` 에 파서 스크립트 `Write` 거부 — **12건**. `retrobot_agg.py` · `retro_agg.py` · `retro_analyze.py` 이름만 바뀌며 반복
- `python3 -c` 에 `;` 나 `>` 를 붙인 복합 명령 거부
- `62743af5` 23:33 — heredoc 안에 한글을 넣어 `SyntaxError: Non-UTF-8 code starting with '\xec'` (이 호스트 `python3` 은 3.9.6)

## KPT 회고

### Keep (좋았던 것)

- **설계 수정이 전부 코드 앞에서 일어났습니다.** 20:31·20:34·20:43·21:16 네 번의 지적이 모두 스펙 단계에서 들어갔고, `3eda547` 은 코드 0줄에 문서만 바꾼 커밋입니다. 캐시 무효화(핵심 3경로 중 하나)가 구현 후 재설계 없이 한 번에 닫힌 이유가 여기 있습니다.
- **"왜 23건이 생략됐지"를 물었습니다.** 초록불의 숫자가 아니라 **빠진 숫자**를 본 질문입니다. 테스트 수를 늘리는 것보다 무엇이 안 돌고 있는지 아는 쪽이 10% 비중의 테스트 항목에서 실제로 평가되는 것입니다.
- **실측하고도 채택하지 않은 판단.** 22:18 의 모델 이름 하드코딩은 `gpt-5.6-sol` 을 눈으로 확인한 뒤에 거절됐습니다. 되는 것을 확인하는 일과 넣어야 하는지 판단하는 일을 분리했고, 남는 구멍(CLI 업그레이드 시 키가 `""` 인 채로 옛 모델의 답이 남음)을 범위·수명까지 문서에 남겼습니다.
- **주석 게이트가 지켜졌습니다.** `python3 scripts/check_comments.py` → **112개 파일, 위반 0건**. 일곱 회차 이월됐던 `config.py:91` 이 회수된 뒤 이번 창에서 새 위반이 들어오지 않았습니다.
- **크로스인코더 제안이 범위 밖을 먼저 적었습니다.** 리랭커에 하한을 안 거는 이유를 "정답 라벨이 없어 값을 잴 수 없다"로 명시한 것은, 재지 않은 임계값을 환각 억제 경로에 얹지 않겠다는 같은 원칙의 재적용입니다.

### Problem (나빴던 것)

- **Retrobot 이 이 창 에러의 82%(50/61)이고, 이번이 16회차입니다.** 훅 13세션이 **전부** `command -v python3 jq`(15건 거부) → `/tmp` Write(12건 거부) 두 줄에서 죽었습니다. `retrobot/SKILL.md` 는 `afa9794`(08-03 22:23) 이후 **26시간 무수정**이고, 회고 열 편이 같은 진단만 반복했습니다. **회고 도구가 회고 대상보다 많은 에러를 냅니다.**
- **`SKILL.md:54` 의 heredoc 금지가 이 호스트에서 유일하게 되는 경로를 막습니다.** 같은 창에서 `<<` 를 쓴 Bash 호출이 **110회 성공**했습니다. 금지는 Windows 에서 참인 사실인데 표에 호스트 구분이 없어 macOS 세션이 매번 되는 수단을 피해 우회로를 다시 찾습니다.
- **`ruff format --check` 가 16건으로 늘었습니다 — 12번째 재현이자 13 → 16 증가.** 같은 시점 `ruff check` 는 통과합니다(88자 손수 줄바꿈 vs `line-length = 100`). 즉 **초록불이 거짓**이고, `75cd4368` 의 `E501` 에러도 같은 뿌리입니다. 닫는 비용은 명령 한 줄입니다.
- **`0145a27` 이 미푸시이고 `add-cross-encoder` 브랜치가 origin 에 아예 없습니다.** 745줄 스펙 전체가 로컬에만 있습니다. 직전 회차가 `git push` 한 줄로 다섯 회차 이월을 닫았는데, 브랜치가 바뀌자마자 같은 모양이 다시 시작됐습니다.
- **`add-multiturn-qa` 가 여전히 untracked — 아홉 번째 계수입니다.** 24시간을 넘겼고 그 사이 change 셋이 제안→구현→아카이빙을 마쳤습니다.
- **외부 적대적 검토 0회 — 열여섯 세션 연속.** 이 창에서만 설계가 사람 지적으로 네 번 뒤집혔고 `validate --strict` 는 한 건도 잡지 못했습니다. 지금 붙일 크로스인코더는 상위 K의 **내용을 바꾸는** 단계라, 틀리면 답변 품질이 조용히 나빠지고 테스트가 그것을 잡지 못합니다.
- **1장 구현 190줄이 워킹트리에만 있고 `tasks.md` 는 65개 중 0개 체크입니다.** 진행분과 태스크 표시가 어긋난 상태이고, 이 창에서 이미 세 번 재발한 "구현은 커밋되고 문서·체크는 남는" 패턴이 시작되는 자리입니다.

### Try (개선해야 할 것)

1. **`retrobot/SKILL.md` 세 줄을 지금 고칩니다** — 26시간·10편·16회차의 유일한 원인입니다.
   - "집계 도구 확인" 절의 `command -v python3 jq` 를 지우고, **첫 시도를 `python3 -c '...'` 단일 호출로 못 박습니다**(단일 인용부호 안 여러 줄은 통과합니다 — 이 회고 전체가 그 방식 4회로 집계됐습니다).
   - `/tmp` Write 절차를 삭제합니다. 12/12 거부라 시도 자체가 손해입니다.
   - heredoc 금지에 **호스트 조건을 붙입니다** — `uname -s` 가 `Darwin` 이면 허용(같은 창 110회 성공), Windows 에서만 금지.
2. **`ruff format .` 한 줄을 다음 커밋에 넣습니다.** 12회차 이월이고 16건이며, 방치하면 `ruff check` 초록불이 계속 거짓 신호를 냅니다.
3. **`git push -u origin add-cross-encoder` 를 크로스인코더 1장 착수 **전에** 칩니다.** 스펙만이라도 원격에 있어야 다음 창이 끊겨도 745줄이 평가 대상으로 남습니다.
4. **`add-multiturn-qa` 를 이번 창에 처분합니다** — 커밋하거나 지우거나. 아홉 번 센 항목은 더 세도 값이 없습니다.
5. **크로스인코더 1장 구현 전에 Codex 검토를 한 번 겁니다.** 열여섯 세션 연속 0회이고, 이 change 는 검색 결과의 순서가 아니라 **프롬프트에 들어가는 근거 자체**를 바꾸므로 지금이 반영 비용이 가장 싼 구간입니다.
6. **새로 확정된 셸 제약을 `CLAUDE.md` 에 적습니다** — `@{u}` 같은 중괄호가 든 git 인자는 brace expansion 가드에 걸려 거부됩니다(`git rev-list --count HEAD --not --remotes` 로 대체).

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 08-04 00:54 | `2d412d02` | `/opsx:propose add-rrf-algorithm-spec` — 하이브리드·BM25 |
| 08-04 01:15 | `e4260f90` | Retrobot 훅 — `command -v` · `/tmp` Write 거부 |
| 08-04 20:15 | `45f0bdd9` | `/opsx:propose` L1/L2 캐시 설계 |
| 08-04 20:31~20:43 | `45f0bdd9` | 사용자 지적 3건 — 캐시 대상·3상황 표·`no_evidence` 무효화 누락 |
| 08-04 20:50 | `75cd4368` | `/opsx:apply` 1장 캐시 도메인 |
| 08-04 21:16 | `75cd4368` | `/opsx:update` scope 반영 — 코드 0줄, 문서만 |
| 08-04 21:45 | `75cd4368` | "23건 테스트 왜 생략된거지?" |
| 08-04 22:18 | `75cd4368` | CLI 모델 실측 → 하드코딩 거절 |
| 08-04 22:23~22:58 | `75cd4368` | 6장 무효화 · 7장 계측 |
| 08-04 23:30 | `75cd4368` | `/opsx:archive add-response-cache` (71/71) |
| 08-04 23:36 | `ed436f8b` | 회고 — 직후 `git push` 39커밋 |
| 08-04 23:41 | `7656b215` | RAG 파이프라인 현황 점검 → `add-answer-generation` 아카이빙 |
| 08-05 00:25 | `34e9f10c` | `/opsx:propose add-cross-encoder` — 745줄 스펙 |
| 08-05 00:51 | `34e9f10c` | `/opsx:apply` 1장 착수 — `reranking.py` 49줄 + 테스트 141줄이 회고 시점에 워킹트리 |
