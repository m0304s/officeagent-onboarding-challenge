# Retrobot 회고 — 2026-08-03 14:16 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 6 (메인 5 + 서브에이전트 1) |
| 총 프롬프트 | 13 (typed 12 + 슬래시 명령 1) |
| 도구 호출 | 308 (`a58ce1f7` 153 · `10c7baad` 78 · `02b371b1` 53 · `076cba3d` 18 · `0d343c07` 5 · 서브에이전트 1) |
| 에러 발생 | 34 (`10c7baad` 17 · `a58ce1f7` 13 · `02b371b1` 4 · 나머지 **0**) |
| 커밋 | `bd59e24` docs: ARCHITECTURE 문서 업데이트 (+4/-4, 1파일) |

> `a58ce1f7`·`02b371b1` 두 세션은 직전 회고([12:31](./2026-08-03_1231_retro.md))가 이미 상세히 다뤘습니다. 이번 회고는 **그 이후에 새로 열린 네 세션**을 중심으로 씁니다.

## 세션별 활동

### 세션 `076cba3d` (13:57 ~ 14:10 KST) — Codex 적대적 리뷰 → ARCHITECTURE 정정

**프롬프트:**
1. (13:57) codex 모델을 사용하여 `add-answer-generation` change에 대한 적대적 리뷰 진행해주세요.
2. (14:07) **Minor1은 진짜입니다. 수정 진행해주세요.** Major2는 절반만 맞습니다 — 캐싱이 PRD 필수이지만 해당 change에선 일부러 제외했고 추후 진행할 예정입니다. "answer-generation 기능이 브랜치에 통째로 없다" → 아직 Spec에 대한 내용입니다, 직접 구현은 안 했습니다. "새 동작에 자동화 커버리지가 없다" → 아직 구현 안 했습니다.

**주요 도구:** Read 6, Grep 5, Edit 3, Bash 3, Skill 1(`codex:rescue`), Agent 1(`codex:codex-rescue` 서브에이전트)

**Codex가 낸 지적 4건과 판정:**

| # | 지적 | 판정 | 처리 |
|---|------|------|------|
| Major | Answer-generation feature is entirely absent from the branch | **오해** — 스펙 단계이므로 당연 | 기각 |
| Major 1 | No automated coverage exists for any new answer-generation behavior | **오해** — 미구현 | 기각 |
| Major 2 | Caching and document-driven cache invalidation are explicitly deferred, despite being PRD requirements | **절반** — PRD 필수는 맞지만 이 change에서 의도적 제외 | 기각(추후 change) |
| Minor 1 | **Architecture documentation contradicts the current code about retrieval** | **진짜** | 수정 |

**발생한 에러:** 없음 (0건)

**결과:** `ARCHITECTURE.md` 4줄 수정 후 커밋 `bd59e24`.
- 헤더 "현재 구현 범위"에 **검색 서비스와 검색 API(`POST /search`)** 추가
- "그 위의 검색 서비스와 `POST /search`는 아직 없습니다" → "`RetrievalService`(질의 임베딩 → 대상 집합 → 저장소 질의 → 현재성 재검증 → 하한)와 `POST /search`가 모두 있습니다"로 교체
- 헤더 밖 2줄도 함께 정정 — 부모-자식 청킹 유예 근거("아직 검색 질의 자체가 없습니다" → "그 청크를 소비하는 답변 생성이 아직 없습니다"), 토큰 가드 위치("같은 change가 뒤이어 붙입니다" → 이미 붙어 있음)

### 세션 `agent-a7c4bdc33d39068c3` (13:59 ~ 14:02 KST) — codex-rescue 서브에이전트

**프롬프트:** (Agent 도구가 주입) adversarial review of add-answer-generation change — 발견마다 파일 경로·설명·왜 중요한지·수정 방향을 붙이고 Major/Minor로 분류, 마지막에 RAG 파이프라인·스트리밍·환각 억제 PRD 요구 충족 여부 총평.

**주요 도구:** Bash 1 (`codex exec`)

**발생한 에러:** 없음 (0건). 3분 07초에 리뷰 반환 — 직전 세션에서 겪은 `stream disconnected ... Reconnecting 5/5` 재발 없음.

### 세션 `0d343c07` (12:26 ~ 12:32 KST) — Retrobot 훅 동작 확인

**프롬프트:**
1. (12:26) `git config core.hooksPath .githooks` (Retrobot 활성화) 되어있나요?
2. (12:32) 지금 커밋 했는데, 회고작업 진행되고 있는지 확인해주세요.

**주요 도구:** PowerShell 4, Read 1 / **에러 0건**

**결과:** `core.hookspath=.githooks` 등록 확인, `.githooks/post-commit`(4,448 bytes) 존재 확인. 두 번째 질문에는 `.git/retrobot/lock`(12:26:26 생성)과 `last-run` 로그로 **실행 중**임을 확인해 답변.

### 세션 `10c7baad` (12:26 ~ 12:36 KST) — Retrobot 회고 생성(직전 회차)

훅이 자동 기동. `SKILL.md`를 프롬프트로 받아 12:31 회고를 생성하고 `TIMELINE.md`에 링크 추가.

**주요 도구:** Grep **34**, PowerShell 15, Bash 10, Read 12, Write 1, Edit 1

**발생한 에러 (17) — 전부 샌드박스·권한 벽:**
- `/tmp/retro_parse.py` 출력 리다이렉션 차단 (워킹 디렉토리 밖)
- `Python was not found` — 호스트에 python3 미설치
- `py -3 --version` 승인 필요 (Bash·PowerShell 각 1건)
- `Command contains expandable strings with embedded expressions`
- 글로브 경로 정적 검증 불가 1건, 셸 확장 구문 승인 필요 2건
- `get-item` / `out-file` / `set-content` 경로 정적 검증 실패 3건
- `cwd-first resolution` 그림자 경고 1건, 스크립트 블록 차단 1건
- `git add ...; git status --short` 다중 명령 승인 필요

**결과:** 스크립트를 포기하고 `Grep` 34회 + `Read` 12회로 우회해 회고 생성 성공.

### 세션 `a58ce1f7` (09:52 ~ 11:24) · `02b371b1` (11:29 ~ 12:26)

직전 회고([12:31](./2026-08-03_1231_retro.md))에서 전체를 다뤘습니다. 요약만 남깁니다 — Make 제거·Codex SDK 전환으로 `docker compose up` 한 줄 실행 완성(에러 13건, 418 passed), `add-answer-generation` 스펙 4개 아티팩트 870줄 `--strict` 통과(에러 4건).

## KPT 회고

### Keep (좋았던 것)

- **직전 회고의 첫 번째 Try를 다음 세션 첫 작업으로 이행했다.** 12:31 회고의 Try 1은 "다음 세션 첫 작업으로 `add-answer-generation` 스펙에 Codex 적대적 리뷰 1회. 리뷰 없이 `tasks.md`를 열지 않는다"였다. 13:57에 열린 다음 세션의 **첫 프롬프트가 정확히 그것**이었고, `tasks.md`는 열리지 않았다. 여덟 번 이월된 항목(동점 결정성)이 있는 이 프로젝트에서, Try가 24시간이 아니라 90분 만에 실행된 것은 처음이다.
- **리뷰를 받되 판정 주체를 넘기지 않았다.** Codex의 지적 4건 중 실제로 고친 것은 1건이다. 나머지 3건은 "구현 전 단계라 코드·테스트가 없는 것이 정상"과 "캐싱은 이 change의 의도적 범위 밖"이라는 **사실로 기각**했다. 적대적 리뷰어는 브랜치 상태만 보고 "기능이 통째로 없다"고 말할 수 있고, 그것을 그대로 받으면 스펙 단계에서 구현을 시작하게 된다. 리뷰의 값어치는 4건 중 1건에 있었고, 그 1건을 골라낸 것은 사람이었다.
- **고친 1건이 하필 불합격 조항에 걸리는 항목이었다.** `CLAUDE.md`의 불합격 기준에 "문서에 구현했다고 적었으나 해당 코드가 없음(허위 기재)"이 있다. 이번 건은 반대 방향 — 코드는 있는데 문서가 "아직 없습니다"라고 적고 있었다. 감점 조항은 아니지만 "문서-코드 일치" 감점 요인에는 그대로 해당한다. 4줄짜리 수정이 평가 항목 하나를 되돌린 셈이다.
- **헤더만 고치고 끝내지 않았다.** 리뷰가 짚은 것은 문서 상단의 "현재 구현 범위" 한 문장이다. 그런데 본문 두 곳(부모-자식 청킹 유예 근거, 토큰 가드가 붙는 자리)이 **"검색이 아직 없다"를 전제로 쓰인 문장**이었다. 상단만 고쳤으면 문서 안에서 서로 모순되는 상태가 됐다. 4줄 중 2줄이 리뷰가 지적하지 않은 자리다 — 지적의 뿌리를 찾아 같이 뽑았다.
- **리뷰 세션이 에러 0건으로 13분에 끝났다.** 18 tool_use로 리뷰 요청 → 서브에이전트 위임 → 결과 판정 → 근거 확인(Read 6 · Grep 5) → 수정 3건 → 커밋. 직전 세션에서 Codex 스트림이 5회 재접속 끝에 끊겼던 것과 달리 이번 `codex exec`는 3분 07초에 한 번에 반환됐다.

### Problem (나빴던 것)

- **문서가 코드보다 4시간 뒤처진 것을 사람도 에이전트도 아닌 외부 리뷰어가 발견했다.** `POST /search`와 `RetrievalService`는 `3c04680`(09:50 "feat: add-qa-retrieval 로직 구현")에 들어갔다. `ARCHITECTURE.md`는 13:57 리뷰 시점까지 "그 위의 검색 서비스와 `POST /search`는 아직 없습니다"라고 적고 있었다. 그 사이 `49c2064`·`8450c0c`·`4a29a0e` 세 커밋이 지나갔고, 그중 `4a29a0e`는 **답변 생성 스펙 870줄을 쓰면서** 검색 위에 무엇을 올릴지 논한 커밋이다. 더 나쁜 것은 8/2 21:19 회고가 **"문서 갱신을 같은 커밋에 포함(허위 기재 방지)"을 Keep으로 이미 적었다는 점**이다 — 한 번 지킨 규율이 이틀 만에 풀렸다.
- **Retrobot 샌드박스 벽이 또 막았다 — 열네 번째.** 직전 회차(`10c7baad`)에서 17건이 거부됐고, **이 회고를 만드는 이번 회차에서도 같은 벽이 재발했다**: `/tmp` 및 `%TEMP%` 파일 쓰기 거부, `python3` 미설치, `py -3` 승인 필요, 워킹 디렉토리 밖 `ls`/`wc` 차단, PowerShell `$()` 부분식·`.NET` 메서드·`ForEach-Object` 스크립트 블록 차단. 결국 이번에도 `Grep` 30여 회로 우회했다. 직전 회고 Try 6이 "`retrobot/SKILL.md`에 통과하는 방법을 절차로 적는다"였는데, `retrobot/` 안에는 여전히 `README.md`와 `SKILL.md` 둘뿐이다 — **`parse_sessions.py`는 8/3 00:40 회고 이후 네 회차째 없다.**
- **README `/search` 예시의 손으로 쓴 값이 네 번째 이월이다.** `README.md:169`의 `"score":0.8734`, `README.md:167`의 `"chunk_index":1`, `README.md:209`의 `"top_score":0.8734`가 그대로다. 12:31 회고는 이 항목의 Try를 **"확인한다가 아니라 붙여넣는다"**로까지 구체화했고, 같은 질의의 실측값 `0.861`이 이미 화면에 찍혔다는 사실까지 적어뒀다. 그 뒤 두 세션이 지나는 동안 `README.md`는 열리지 않았다. 청크 1개짜리 문서에서 `chunk_index: 1`은 여전히 불가능한 값이다.
- **훅이 도는지를 사용자가 물어봐야 알 수 있었다.** 12:26에 세션이 **두 개 동시에** 떴다 — 훅이 띄운 `10c7baad`(회고 생성)와, 사용자가 "회고작업 진행되고 있는지 확인해달라"고 연 `0d343c07`. 6분 뒤 `.git/retrobot/lock`을 확인해 "돌고 있다"고 답했다. `post-commit`이 조용히 백그라운드로 도는 탓에 **상태 확인용 세션이 하나 더 필요했다**. 그 세션은 코드를 한 줄도 바꾸지 않았다.
- **적대적 리뷰의 정확도가 4건 중 1건이었다.** 서브에이전트에 넘긴 프롬프트가 "add-answer-generation change의 적대적 리뷰"였을 뿐, **지금이 스펙 단계이고 구현·테스트는 아직 없다**는 상태를 알리지 않았다. 그래서 Major 2건이 "코드가 없다 / 테스트가 없다"로 나왔고, 이를 되받아 설명하는 데 프롬프트 한 번이 더 들었다. 리뷰어에게 무엇을 리뷰하는지가 아니라 **어느 단계를 리뷰하는지**를 말하지 않은 비용이다.

### Try (개선해야 할 것)

- **`ARCHITECTURE.md`의 "현재 구현 범위"를 사람이 관리하지 않게 만든다.** 라우터 등록 목록(`app.routes`)과 문서 헤더의 API 목록을 대조하는 테스트를 `tests/test_docs_consistency.py`에 신설한다. `POST /search`가 앱에 등록되어 있는데 문서 헤더에 없으면 실패한다. 이번 4시간 격차는 사람이 잊어서 생긴 것이고, 잊는 것은 다음에도 반복된다 — 검사가 없으면 다음 리뷰까지 또 열려 있다.
- **`retrobot/SKILL.md`에 "이 환경에서 실제로 통과하는 도구는 Grep과 Read뿐"을 못 박고, 검증된 정규식 4개를 그대로 박는다.** 스크립트 파일(`parse_sessions.py`)은 **네 회차 미작성이므로 포기한다** — 호스트에 python3가 없다는 것이 이번에 확정됐다. 대신 SKILL.md 2단계에 넣을 것: 프롬프트는 `"role":"user","content":"[^"]{1,500}`, 도구 집계는 `"type":"tool_use","id":"[^"]*","name":"[^"]*"`, 에러는 `.{0,320}"is_error":true`, 타임스탬프 범위는 `"timestamp":"[^"]*"`를 `count` 모드로 센 뒤 `offset=N-1`로 마지막 하나. 그리고 "`python3`·`/tmp`·PowerShell 스크립트 블록은 **시도하지 마라**" 한 줄. 열네 번 막혔으므로 열다섯 번째는 시도 자체를 지운다.
- **README `/search` 예시 교체를 다음 세션의 첫 다섯 줄 안에 넣는다.** 이번 회차에서 확인된 것은 "값이 없어서"가 아니라 "README를 열지 않아서"다. 그러니 액션은 값 측정이 아니라 **파일 열기**다 — `docker compose up` 후 `curl` 한 번, 출력을 `README.md:165~169`에 통째로 붙여넣고 `:209`의 `top_score`를 같은 실행 값으로. 다섯 번째 이월이 되면 그때는 예시 블록 자체를 지우는 쪽이 정직하다.
- **서브에이전트 리뷰 프롬프트에 "현재 단계"를 한 줄 넣는다.** `codex:rescue`를 부를 때 "이 change는 **스펙 확정 단계**이며 구현·테스트는 의도적으로 아직 없다. 리뷰 대상은 `openspec/changes/<name>/` 아래 아티팩트뿐이다"를 앞에 붙인다. 이번엔 4건 중 2건이 그 한 줄로 사라졌을 지적이다.
- **`post-commit` 훅이 시작·종료를 한 줄씩 stdout에 찍게 한다.** `.git/retrobot/lock`을 사람이 뒤져야 상태를 아는 지금 구조가 확인용 세션을 하나 만들어냈다. `[retrobot] started (pid …)` / `[retrobot] wrote retros/<file>` 두 줄이면 세션이 필요 없다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 (KST) | 세션 | 주요 활동 |
|------|------|----------|
| 09:52 ~ 11:24 | `a58ce1f7` | Make 제거·Codex SDK 전환, `docker compose up` 한 줄 실행 완성 (직전 회고에서 상술) |
| 11:29 ~ 12:26 | `02b371b1` | `/opsx:propose add-answer-generation` — 스펙 4개 아티팩트 870줄 (직전 회고에서 상술) |
| 12:26 | — | 커밋 `4a29a0e` docs: add-answer-generation 스펙 분석 → post-commit 훅 기동 |
| 12:26 | `0d343c07` | "`core.hooksPath` 활성화되어 있나요?" → `.githooks` 등록·훅 파일 존재 확인 |
| 12:26 ~ 12:36 | `10c7baad` | 훅이 자동 기동 — 샌드박스 거부 17건을 Grep 34회로 우회해 12:31 회고 생성 |
| 12:32 | `0d343c07` | "회고 돌고 있나요?" → `.git/retrobot/lock`(12:26:26)으로 실행 중 확인 |
| 13:57 | `076cba3d` | `codex` 적대적 리뷰 요청 → `codex:rescue` 스킬 → 서브에이전트 위임 |
| 13:59 ~ 14:02 | `agent-a7c4bdc` | `codex exec` 1회 3분 07초 — Major 3 / Minor 1 반환, 에러 0건 |
| 14:07 | `076cba3d` | 지적 4건 판정 — Minor1만 수용, Major 3건은 "스펙 단계"·"의도적 범위 제외"로 기각 |
| 14:08 | `076cba3d` | `ARCHITECTURE.md` 4줄 수정 — 헤더 2줄 + 전제가 낡은 본문 2줄 |
| 14:09 | — | 커밋 `bd59e24` docs: ARCHITECTURE 문서 업데이트 (+4/-4) |
