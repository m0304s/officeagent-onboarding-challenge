# Retrobot 회고 — 2026-08-04 23:36 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 19 (실작업 6 · Retrobot 훅 13) |
| 총 프롬프트 | 44 (실작업 31 · 훅 13) |
| 에러 발생 | 62 (그중 **49건(79%)이 Retrobot 자신**) |
| 창 내 커밋 | 40 (feat/docs 20 · 회고 20) |
| **origin 미푸시** | **0건** — `git log origin/add-response-cache..HEAD` 가 비었습니다 |

분석 범위는 08-03 23:36 ~ 08-04 23:36 입니다. 직전 회고(23:31) 이후의 **증분은 5분**이라,
아래 KPT 는 그 5분에서 실제로 바뀐 것과 직전 Try 6건의 이행 여부를 중심으로 씁니다.

## 직전 회고(23:31) Try 이행 점검

| # | 항목 | 결과 |
|---|---|---|
| 1 | `git push -u origin add-response-cache` | **이행** — 23:36:17 푸시, 39커밋 전부 |
| 2 | `ruff format src tests` | 미이행 — 이 회차 실측 `13 files would be reformatted` |
| 3 | `add-multiturn-qa` 커밋 또는 삭제 | 미이행 — 여전히 untracked |
| 4 | `tasks.md` 71/71 을 회고 커밋 전에 분리 커밋 | **미이행 — 예측대로 회고 커밋에 딸려 들어갔습니다** |
| 5 | `retrobot/SKILL.md` 3곳 수정 | 미이행 — `afa9794`(08-03 22:23) 이후 25시간 무수정 |
| 6 | 다음 change 전 Codex 검토 | 미이행 |

6건 중 1건 이행. 다만 이행된 그 1건이 이 창에서 가장 값이 큰 항목이었습니다.

## 세션별 활동

### 실작업 세션

| 세션 | 시각 | 프롬프트 | 에러 | 주요 도구 |
|---|---|---:|---:|---|
| `8ccc760d` | 08-03 23:36~23:42 | 0 (이월) | 0 | Bash 8 · Edit 4 |
| `724d2218` | 23:43~00:31 | 6 | 3 | Write 52 · Bash 46 · Edit 19 · chrome MCP 10 |
| `58b22a1f` | 00:31~00:51 | 2 | 0 | Bash 17 · Write 6 |
| `2d412d02` | 00:51~19:24 | 2 | 0 | Bash 24 · Write 7 |
| `45f0bdd9` | 20:13~20:48 | 7 | 0 | Bash 30 · Edit 22 |
| `75cd4368` | 20:49~**23:35** | 14 | 10 | **Bash 207 · Edit 96 · Read 29 · Write 16** |

### 증분 구간 (23:29~23:36) — `75cd4368` 의 마지막 7분

| 시각 | 활동 |
|---|---|
| 23:29 | `/opsx:apply add-response-cache` — 71/71 확인 |
| 23:30 | `/opsx:archive add-response-cache` 착수 |
| 23:31~23:32 | 델타 3개(`response-cache`·`answer-generation`·`document-ingestion`)가 메인 스펙에 반영됐는지 대조 |
| 23:33 | **`AskUserQuestion` — "델타 스펙을 메인으로 동기화한 뒤 아카이빙할까요?"** |
| 23:34~23:35 | 동기화 → `openspec/changes/archive/2026-08-04-add-response-cache` 로 이동 → `1ed96c8` |
| **23:36:17** | **`git push` — `origin/add-response-cache` 생성, 39커밋** |

`1ed96c8` 은 change 디렉토리 7개를 아카이브로 옮기고 `openspec/specs/` 에 **437줄을 신설**했습니다
(`response-cache` 303 · `answer-generation` 69 · `document-ingestion` 65).

## KPT 회고

### Keep (좋았던 것)

- **다섯 회차 이월된 미푸시가 닫혔습니다 — 23:36:17, 39커밋.**
  직전 회고가 23:31 에 "**평가자가 지금 리포를 열면 캐싱은 착수조차 안 한 것으로 보입니다**"
  라고 적은 지 **5분 만**에 `git push` 가 들어갔습니다. 원격 reflog 항목은 단 하나
  (`1ed96c8 … update by push`) — 브랜치가 이번에 처음 생겼다는 뜻입니다. 미푸시는
  7 → 13 → 32 → 34 → 36 으로 다섯 회차를 커지기만 했고, 회고가 매번 같은 지적을 반복했는데
  이번엔 **읽고 5분 안에 실행**됐습니다. 비중 18% 의 캐싱과 168시간 창 안의 산출물 39건이
  이 한 줄로 평가 대상이 됐습니다.

- **아카이빙 직전에 손을 멈추고 물었습니다 — 되돌릴 수 없는 연산 앞에서.**
  23:33 의 `AskUserQuestion` 이 이 7분의 핵심입니다. `/opsx:archive` 는 change 디렉토리를
  `archive/` 로 옮기는 연산이라, 델타 스펙이 메인(`openspec/specs/`)에 반영되지 않은 채
  실행하면 **`response-cache` 303줄을 포함한 437줄이 아카이브 안으로 사라집니다.**
  물어보기 전에 23:31~23:32 를 대조에 썼습니다 — `openspec/specs/` 목록을 뽑고, 델타 3개의
  연산 유형과 요구사항을 하나씩 메인과 맞춰보고, `answer-generation` 이 실제로 어느 디렉토리에
  있는지 `find` 로 확인한 뒤에야 질문을 만들었습니다. 근거 없이 물은 게 아니라 **답이 갈리는
  지점을 특정한 뒤** 물은 것이고, 이 순서가 뒤집혔으면 손실은 커밋 이후에야 드러났습니다.

- **change 하나가 제안부터 아카이빙까지 완결됐습니다.**
  20:15 `/opsx:propose` → 20:49~23:28 구현 8커밋 → 23:29 `/opsx:apply` 71/71 → 23:35 아카이브.
  3시간 20분 안에 한 사이클이 끊기지 않고 돌았고, 각 단계가 별도 커밋으로 남아 `git log` 만으로
  읽힙니다. `add-rrf-algorithm` 때는 아카이빙이 다음 날로 밀렸던 것과 대비됩니다.

### Problem (나빴던 것)

- **`tasks.md` 71/71 이 회고 커밋에 실려 갔습니다 — 직전 회고가 예측한 그대로.**
  `git log -S'- [x] 7.7'` 의 답은 `62f8904 docs(retro): 자동 회고 생성 by Retrobot` 입니다.
  직전 회고는 이것을 Problem 으로 적고 Try #4 에 분리 커밋 명령까지 한 줄로 써 뒀는데,
  **회고를 읽고 push 는 했으나 그 위의 항목은 지나갔습니다.** 결과적으로 `add-response-cache`
  완주 표시가 기능 커밋이 아니라 Retrobot 커밋에 붙어, 진행 이력을 `git log` 로 읽으면
  "회고 봇이 71개 태스크를 끝냈다"로 보입니다. 회고가 스스로 예고한 오염을 스스로 일으킨
  경우라, 다음 커밋 전에 훅이 무엇을 삼킬지 확인하는 절차가 없다는 뜻입니다.

- **`ruff format --check` 13건 — 열한 번째 재현, 수치가 한 건도 안 줄었습니다.**
  이 회차 실측: `13 files would be reformatted, 100 files already formatted`.
  같은 순간 `ruff check` 는 통과입니다. 직전 회고에 적힌 닫는 비용은 `.venv/bin/ruff format
  src tests` **한 줄 10초**였고, 열한 회차 동안 누적 방치 시간이 그 비용을 몇 자리수 넘겼습니다.
  보고된 예시(`src/…:309` 의 docstring 첫 줄)를 보면 손으로 접은 줄과 `line-length = 100`
  설정이 어긋나는 그 뿌리 그대로입니다.

- **`add-multiturn-qa` 4아티팩트가 24시간째 untracked — 여덟 번째 계수.**
  00:37 에 만든 이후 그 사이에 change 둘이 완주하고 하나가 아카이빙됐는데 이것만
  워킹트리에 남아 있습니다. `git status` 의 유일한 항목이라 다음 세션이 리포를 열면
  가장 먼저 보이는 것이 이 미결입니다.

- **Retrobot 이 이 창 에러의 79%(49/62) — 15회차. 이번 실행도 같은 세 줄에서 막혔습니다.**
  `retrobot/SKILL.md` 는 `afa9794`(08-03 22:23) 이후 **25시간 무수정**이고, 회고 열 편이
  같은 진단을 반복했습니다. **이번 실행이 직접 겪은 것**을 그대로 적습니다:

  | SKILL.md 지시 | 이 호스트에서 실제 |
  |---|---|
  | 맨 먼저 `command -v python3 jq` 를 쳐라 | **거부** — "multiple operations … requires approval" |
  | 스크립트는 `/tmp` 아래에 써라 | **거부** — Write 권한 미승인 |
  | "하지 말 것: heredoc" | **통과** — 이 회고의 집계 전부가 heredoc |

  덕분에 직전 회고가 확정해 둔 우회로(heredoc)를 알고 있었는데도 **금지 항목이라 두 번 막힌
  뒤에야** 그리로 갔습니다. 문서가 되는 길을 막고 안 되는 길을 지시하는 상태가 유지되고 있습니다.

- **새로 확정된 사실 — python3.9 heredoc 에 한글을 넣으면 죽습니다.**
  훅 세션 `62743af5` 가 남긴 에러입니다:
  `SyntaxError: Non-UTF-8 code starting with '\xec' in file <stdin> on line 3, but no encoding declared`.
  이 호스트의 `python3` 은 **3.9.6** 이라 `<stdin>` 경로에서는 PEP 263 인코딩 선언이 필요합니다.
  파일로 실행할 때는 안 나고 heredoc 일 때만 나므로, 우회로를 권장 경로로 바꾸는 순간
  바로 밟게 되는 함정입니다. 회피는 둘 중 하나입니다 — 스크립트를 **ASCII 로만** 쓰거나,
  첫 줄에 `# -*- coding: utf-8 -*-` 를 넣습니다.

- **외부 적대적 검토 0회 — 열다섯 세션 연속.**
  직전 회고가 "다섯 번 뒤집힌 것이 전부 사람의 눈이었고 `validate --strict` 는 0건"이라고
  적었고, 이번 창에서도 실제로 손을 멈추게 한 것은 **자체 판단(23:33)** 이었지 검증 도구가
  아니었습니다. `add-multiturn-qa` 는 세션 상태가 캐시 키에 섞이는 구조라 같은 계열의 결함이
  나올 자리인데, 코드가 붙기 전인 지금이 반영 비용이 가장 쌉니다.

### Try (개선해야 할 것)

1. **`retrobot/SKILL.md` 를 실측대로 고칩니다 — 열한 번째 지적, 네 곳.**
   에러의 79% 를 만드는 단일 원인이고, 고치는 데 필요한 사실은 전부 확정돼 있습니다.
   - "먼저 `command -v python3 jq` 를 쳐라" → **삭제**. `python3 -c "print(1)"` **단독** 호출로 판정.
   - "`/tmp` 에 스크립트를 쓴다" → **삭제**. heredoc 으로 인라인 처리.
   - "하지 말 것: heredoc" → **`uname -s` 로 호스트 분기**. Windows 에서만 금지, macOS 는 권장.
   - **신규**: "heredoc 스크립트는 ASCII 로 쓰거나 첫 줄에 `# -*- coding: utf-8 -*-` 를 넣는다
     (이 호스트 `python3` 은 3.9.6)."

2. **`ruff format` 을 열한 번째 회고에서 끝냅니다.**
   ```bash
   .venv/bin/ruff format src tests && .venv/bin/ruff format --check src tests
   ```
   그리고 `docker-compose.yml` 의 `test` 서비스 검증에 `ruff format --check` 를 넣어,
   `ruff check` 만 보고 초록으로 오판하는 경로를 막습니다. 그러지 않으면 열두 번째가 옵니다.

3. **`add-multiturn-qa` 를 커밋하거나 지웁니다.** 여덟 회차 이월은 "나중에" 가 아니라 "안 함"
   입니다. 착수 의사가 있으면 지금 커밋하고, 없으면 삭제해 `git status` 를 비웁니다.

4. **완료 표시는 훅이 돌기 전에 커밋합니다.** `tasks.md` 가 회고 커밋에 실린 것은 회고를
   기다렸기 때문입니다. 기능 작업이 끝나면 문서·체크박스까지 **한 커밋에 같이** 올리고,
   그다음에 훅이 돌게 둡니다. 커밋 직전 `git status --short` 로 훅이 무엇을 삼킬지 한 번 봅니다.

5. **`add-multiturn-qa` 착수 전에 Codex 검토를 한 번 겁니다.** 열다섯 세션 연속 0회이고,
   이 창에서도 손을 멈추게 한 것은 도구가 아니라 판단이었습니다. 판단이 한 번 어긋나면
   23:33 같은 지점이 그냥 지나갑니다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 08-03 23:36 | `8ccc760d` | SSE 로직 테스트 마무리 → `dcd6bd7` |
| 23:43 | `724d2218` | 데모 UI 착수, chrome MCP 로 실동작 확인 |
| 08-04 00:18 | `724d2218` | UI 통한 업로드·삭제 검증 → `f392b24`·`2d215cf` |
| 00:37 | `58b22a1f` | `/opsx:propose add-multiturn-qa` (이후 24시간 untracked) |
| 00:54 | `2d412d02` | `/opsx:propose add-rrf-algorithm-spec` → `ff7a5ad` |
| 09:42~17:29 | *(로그 미제공)* | RRF 구현 6커밋 — `35b9c3a` ~ `d320d8d` |
| 20:15 | `45f0bdd9` | `/opsx:propose` L1/L2 캐시 설계 |
| 20:43 | `45f0bdd9` | `no_evidence` 무효화 누락 지적 → `f07ed41` |
| 20:50~23:28 | `75cd4368` | 캐시 1~7장 8커밋 (`23acd3a` ~ `4425f5f`) |
| 23:11~23:27 | `75cd4368` | 주석 정리 187건 → 0건, `tests/README.md` 831줄 → `1638473` |
| 23:29 | `75cd4368` | `/opsx:apply` — 71/71 확인 |
| 23:31 | `75cd4368` | 델타 3개와 메인 스펙 대조 |
| 23:33 | `75cd4368` | **아카이빙 전 동기화 여부를 사용자에게 질의** |
| 23:35 | `75cd4368` | 메인 스펙 437줄 신설 + 아카이브 이동 → `1ed96c8` |
| **23:36:17** | — | **`git push` — 39커밋이 origin 에 처음 올라감** |
