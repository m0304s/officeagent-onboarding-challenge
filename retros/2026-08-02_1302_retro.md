# Retrobot 회고 — 2026-08-02 13:02 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 2 (직전 회고 이후 활동 기준) |
| 총 프롬프트 | 4 (실작업 요청) + `/compact` 2회 + 사용자 중단 1회 |
| 에러 발생 | 5 |

직전 회고(21:25) 이후 구간만 다룹니다. `f2564d3d` 세션의 16:04~21:25 구간은
[17:40](./2026-08-01_1740_retro.md)·[18:25](./2026-08-01_1825_retro.md)·[21:25](./2026-08-01_2125_retro.md)
회고에서 이미 보고했습니다.

## 세션별 활동

### 세션 `64d2c67f` (08-01 21:25 ~ 21:30)

post-commit 훅이 실행한 Retrobot 회고 생성(`d660a64`). 사용자 프롬프트 없음, 에러 없음.

### 세션 `f2564d3d` (신규 구간 08-01 21:26 ~ 08-02 13:03)

`add-document-ingestion`의 **섹션 4~7을 전부 끝냈습니다** — 임베딩 어댑터, 레지스트리·벡터
스토어, 수집 파이프라인, API 계층. 21:25 커밋 이후 두 블록(08-01 밤 / 08-02 낮)으로 나뉘며
중간에 약 14시간 공백이 있습니다.

**프롬프트:**
1. (08-01 21:29) `/opsx:apply` — "임베딩 어뎁터 구현하고, 이후 벡터 저장로직까지 완성해주세요."
2. (08-01 21:53) "수집 파이프라인까지 이어서 구현해주세요"
3. (08-02 12:35) `/opsx:apply` — "섹션 7 구성해주세요."
4. (08-02 12:47) "실수로 작업을 중단했습니다. 이어서 작업 진행해주세요"

**테스트 증가 추이:** 187 → 236(임베딩·저장소) → 270 → 271(수집 파이프라인) → 302 → **303 passed**

**발생한 에러:**
- (21:30) exit 127 — `command not found: python` ×4. 같은 명령을 네 번 연속 같은 방식으로 시도
- (21:59) `File has been modified since read` — 린터가 건드린 파일을 다시 읽지 않고 Edit 시도
- (08-02 12:37) exit 1
- (12:41) 테스트 2건 실패 (같은 블록 내에서 수정 후 12:43 33 passed)
- (12:47) `User rejected tool use` — 사용자가 실수로 중단, 다음 프롬프트로 복구 요청
- (12:52) exit 1 — pytest는 **303 passed**인데 뒤이은 `ruff`가 `I001 Import block is un-sorted` 로 실패

**커밋:** `8466509 feat: Ingest API 구현` (28 파일, **+3943/-265**)

| 영역 | 파일 |
|------|------|
| 임베딩 | `adapters/embedding/local.py`(184) — `multilingual-e5-small`, `passage:`/`query:` 접두사 캡슐화, `signature` 노출 |
| 저장소 | `adapters/registry/sqlite.py`(178), `adapters/vector_store/chroma.py`(219) |
| 서비스 | `services/ingestion.py`(+492) — 배치 임베딩, 리비전 교체·되돌리기, `document_id` 잠금, 기동 정리 |
| API | `api/routes/documents.py`(+193), `main.py`(+63), `adapters/protocols.py`(+131) |
| 테스트 | `test_ingestion_pipeline.py`(558) `test_documents_crud_api.py`(330) `test_vector_store.py`(234) `test_embedding.py`(212) `test_registry.py`(156) `stubs.py`(+274) `conftest.py`(+94) `api_harness.py`(27) `ingestion_harness.py`(44) |
| 문서 | `ARCHITECTURE.md`(+294) `README.md`(+74) |

`tasks.md` 기준 **섹션 1~7 완료, 섹션 8(동시성 검증)·9(컨테이너)·10(마무리) 미착수**입니다.

## KPT 회고

### Keep (좋았던 것)

- **직전 회고의 Try를 실제로 이행했다.** 21:25 회고에 "임시 검증 스크립트 대신 `tests/`에 실행 경로
  테스트를 추가한다"고 적었고, 이번 커밋에 `tests/api_harness.py`·`tests/ingestion_harness.py`가
  신설되고 `stubs.py`가 +274줄 늘었습니다. 어제는 curl 결과를 `json.load(sys.stdin)`에 물려 두 번
  깨졌는데, 이번엔 같은 검증이 테스트로 들어가 재실행이 공짜가 됐습니다. 회고가 문서로만 남지 않은
  첫 사례입니다.
- **`index_signature`가 설계 문서에서 끝나지 않고 코드를 관통했다.** 어제 도입한 개념이 오늘
  `unchanged` vs `reindexed` 분기(6.5), 기동 시 서명 불일치 문서의 `stale` 처리(6.10), 청크 id에
  서명을 넣어 구/신 모델 벡터가 서로 덮어쓰지 않게 하는 규칙(5.5), API 응답 필드와 그 테스트(7.14)
  까지 이어졌습니다. 설계 결정이 어디까지 파급되는지 미리 적어둔 것이 회수됐습니다.
- **실패 경로를 주입 가능한 스텁으로 테스트했다.** 벡터 스토어의 `add_chunks`·`delete_document`
  실패를 각각 주입해, 저장 실패 시 503과 **이전 리비전 보존**을, 되돌리기까지 실패한 경우에도 같은
  결과를 단언했습니다(6.13·6.14). 정상 경로만 덮은 테스트는 배치 저장의 부분 기록을 잡지 못합니다.
- **테스트가 구독·네트워크 없이 도는 구조를 유지했다.** 실제 모델 통합 테스트는 가중치가 없으면
  스킵(`needs_weights`)되고, 나머지는 결정론적 페이크 임베더로 돕니다. 과제 채점에서 "구독 없이
  실행되면 가산점" 항목에 직접 대응합니다.
- **문서를 구현과 같은 커밋에 넣었다.** `ARCHITECTURE.md` +294 / `README.md` +74. 문서-코드
  불일치가 불합격 사유인 과제에서, 미루지 않는 습관이 유지되고 있습니다.

### Problem (나빴던 것)

- **또 한 덩어리 커밋(+3943/-265, 28 파일, 15시간, 4개 섹션).** 21:25 회고에 "작업 블록 단위로
  커밋을 쪼갠다"고 적었지만 이행되지 않았습니다. 심지어 자연 경계가 로그에 다섯 번 찍혀 있습니다 —
  236(섹션 4·5) / 270 / 271(섹션 6) / 302 / 303(섹션 7). Retrobot이 커밋 단위로 회고를 만드는
  구조라, 하룻밤을 넘긴 두 작업 블록이 한 점으로 뭉개졌습니다.
- **`/compact`가 두 번 더 발생했다(21:28, 12:32).** 같은 회고에서 "커밋 직후 새 세션으로 넘어간다"를
  Try로 적었는데, 21:25 커밋 직후에도 세션을 유지해 3분 뒤 compact를 맞았습니다. `f2564d3d`
  한 세션이 이제 누적 5회입니다.
- **테스트는 통과하는데 린트에서 걸리는 패턴이 반복된다.** 어제는 `E501`, 오늘은 `I001`. 둘 다
  커밋 직전에야 드러났습니다. pytest와 ruff를 따로 돌리는 습관 때문이며, 통과 확인 후 다시 수정이
  들어가면 앞의 통과 확인이 무효가 됩니다.
- **같은 명령을 네 번 반복해 실패했다(21:30, `command not found: python`).** 첫 실패에서 실행
  방식(`python3`·`uv run`·venv 활성화)을 확인했으면 끝날 일이었습니다. 실패한 명령을 그대로 다시
  던지는 것은 정보를 얻지 못하는 반복입니다.
- **섹션 9(컨테이너·실행)가 미착수인데 커밋 메시지는 "Ingest API 구현"이다.** 임베딩 런타임과
  PDF 파서 의존성이 `Dockerfile`에 반영되지 않았고, 모델 가중치 굽기(9.2)·`docker compose up`
  실동작 확인(9.4)도 남아 있습니다. **"명시된 방법으로 실행 불가"는 이 과제의 1번 불합격 기준**
  이므로, 로컬 pytest 303개가 통과한다는 사실이 안전을 보장하지 않습니다.

### Try (개선해야 할 것)

- **`tasks.md`의 섹션 번호를 그대로 커밋 경계로 쓴다.** 섹션 체크박스가 전부 `[x]`가 되고 테스트가
  통과하면 그 자리에서 커밋합니다. 이번이라면 섹션 4·5 완료(236 passed) / 섹션 6 완료(271) /
  섹션 7 완료(303) 세 개였습니다. 커밋을 쪼개면 회고 해상도도 함께 올라갑니다.
- **`make verify` 같은 단일 타깃에 `pytest` + `ruff check` + `ruff format --check`를 묶는다.**
  이틀 연속 린트가 마지막에 터진 것은 검증 명령이 하나가 아니기 때문입니다. 이미 `Makefile`이 있으니
  타깃 하나만 추가하면 되고, 커밋 전 실행 대상도 이 하나로 고정됩니다.
- **명령이 두 번 같은 방식으로 실패하면 실행 환경부터 확인한다.** 세션 시작 시 파이썬 실행 진입점을
  한 번 확정하고(`uv run` / `.venv/bin/python`), 이후 그 형태만 씁니다. 세 번째 재시도는 금지.
- **섹션 9를 섹션 8보다 먼저 당긴다.** 동시성 검증(8.1~8.6)은 정확성 보강이지만, 컨테이너 실행은
  불합격 방어입니다. 특히 임베딩 모델 가중치가 이미지에 없으면 컨테이너 첫 업로드가 네트워크
  다운로드에 막혀 평가자 환경에서 그대로 실패합니다. 마침 커밋 직후(13:03) "ChromaDB를 임베디드가
  아닌 docker-compose 서버 모드로" 요청이 들어왔으니, 그 작업과 9.1~9.4를 한 블록으로 묶는 것이
  자연스럽습니다.
- **커밋 직후에는 세션을 끊는다.** 21:25에도 같은 걸 적었으므로, 이번엔 post-commit 시점을 세션
  종료 신호로 삼습니다. compact 요약을 통과시키는 것보다 커밋 메시지·`tasks.md`에서 문맥을 다시
  읽는 편이 손실이 적습니다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 08-01 21:25 | `f2564d3d` | 파서·업로드 커밋 (`80e99ce`) |
| 08-01 21:25~21:30 | `64d2c67f` | Retrobot 회고 자동 생성 (`d660a64`) |
| 08-01 21:28 | `f2564d3d` | `/compact` (누적 4회) |
| 08-01 21:29 | `f2564d3d` | `/opsx:apply` — 임베딩 어댑터 + 벡터 저장 로직 |
| 08-01 21:30 | `f2564d3d` | `command not found: python` ×4 (⚠ exit 127) |
| 08-01 21:46 | `f2564d3d` | 임베딩 테스트 16 passed |
| 08-01 21:47 | `f2564d3d` | 섹션 4·5 완료 → **236 passed** |
| 08-01 21:53 | `f2564d3d` | "수집 파이프라인까지 이어서 구현해주세요" |
| 08-01 21:59 | `f2564d3d` | `File has been modified since read` (⚠) |
| 08-01 22:09 | `f2564d3d` | **270 passed** |
| 08-01 22:19~22:20 | `f2564d3d` | 섹션 6 완료 → **271 passed**, 이후 약 14시간 공백 |
| 08-02 12:32 | `f2564d3d` | `/compact` (누적 5회) |
| 08-02 12:35 | `f2564d3d` | `/opsx:apply` — 섹션 7(API 계층) |
| 08-02 12:37~12:43 | `f2564d3d` | exit 1 · 테스트 2건 실패 → 수정 후 33 passed (⚠) |
| 08-02 12:47 | `f2564d3d` | 사용자 실수로 중단 → "이어서 작업 진행해주세요" (⚠) |
| 08-02 12:49 | `f2564d3d` | **302 passed** |
| 08-02 12:52 | `f2564d3d` | **303 passed** 이나 `ruff I001` 실패 (⚠ exit 1) |
| 08-02 13:01 | `f2564d3d` | **303 passed** · ruff 통과 재확인 |
| 08-02 13:02 | `f2564d3d` | 커밋 `8466509 feat: Ingest API 구현` (+3943/-265) |
| 08-02 13:03 | `f2564d3d` | 다음 지시 — ChromaDB를 임베디드에서 docker-compose 서버 모드로 전환 |
