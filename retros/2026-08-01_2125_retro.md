# Retrobot 회고 — 2026-08-01 21:25 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 2 (직전 회고 이후 활동 기준) |
| 총 프롬프트 | 2 (실작업 요청) + `/compact` 2회 + 사용자 중단 2회 |
| 에러 발생 | 3 |

직전 회고(18:25) 이후 구간만 상세히 다룹니다. `f2564d3d` 세션의 16:04~18:20 구간은
[17:40](./2026-08-01_1740_retro.md)·[18:25](./2026-08-01_1825_retro.md) 회고에서 이미 보고했습니다.

## 세션별 활동

### 세션 `f2564d3d` (16:04 ~ 21:26, 신규 구간 20:32 ~ 21:26)

18:20 커밋·push 이후 약 2시간 공백, 20:32 `/compact`로 재개해 `add-document-ingestion`의
파서·서비스·라우터 계층을 한 번에 끝냈습니다.

**프롬프트:**
1. (20:50) "service단까지 구현해서, 실제로 PDF 및 TXT/MD 파일 업로드 시 파싱 및 청킹이 진행되는지 확인해보고 싶습니다."
2. (21:14) "UploadFile을 사용하도록 해주세요. 다만, `MAX_UPLOAD_BYTES = 20 * 1024 * 1024` / `if file.size and file.size > MAX_UPLOAD_BYTES: raise AppException(ErrorCode.FILE_TOO_LARGE)` 위 형태로 파일 크기 제한을 하고, 직접 바이트로 읽는 형식은 피해주세요."

두 프롬프트 모두 **진행 중이던 도구 호출을 중단시키고** 들어온 방향 수정이었습니다(20:50, 21:13).

**테스트 증가 추이:** 113 → 150(파서) → 163(ingestion 서비스) → 186 → **187 passed**

**발생한 에러:**
- (20:50) `User rejected tool use` — 사용자가 진행 중 작업을 끊고 service 계층 요구를 추가
- (20:53) 샘플 문서 실측 스크립트 종료 코드 1 — `company-policy.txt`는 `format=txt chunks=1`로 정상 출력됐으나 스크립트 자체는 실패로 종료
- (21:22) 종료 코드 7 — 업로드 응답을 `json.load(sys.stdin)`으로 파싱하는 검증 스크립트가 Traceback

**커밋:** `80e99ce [BE] feat: 파서 구현 및 업로드 구현` (21개 파일, +1637/-37)

| 영역 | 파일 |
|------|------|
| 파서 | `adapters/parsers/{pdf,text,registry,normalization}.py` |
| 서비스 | `services/ingestion.py` |
| 라우터 | `api/routes/documents.py`, `api/errors.py` |
| 테스트 | `test_parsers.py`(298) `test_documents_api.py`(281) `test_ingestion.py`(167) `pdf_fixtures.py`(45) |
| 문서 | `ARCHITECTURE.md`(+178) `README.md`(+35) |

### 세션 `b57ce982` (18:20 ~ 18:27)

post-commit 훅이 실행한 Retrobot 회고 생성. 사용자 프롬프트 없음, 에러 없음.

## KPT 회고

### Keep (좋았던 것)

- **"실제로 되는지 확인"을 사용자가 명시적으로 요구했고, 그게 통했다.** 20:50 프롬프트는 코드를 더 써달라는 게
  아니라 *실물 PDF·TXT·MD를 넣어 파싱·청킹이 도는지 보자*는 요청이었습니다. 단위 테스트 187개가 통과해도
  end-to-end 경로가 막혀 있을 수 있는데, 이 요구 덕에 라우터까지 한 번에 이어졌습니다. 과제 불합격 기준 1번이
  "명시된 방법으로 실행 불가"인 만큼 실행 확인을 습관으로 두는 편이 안전합니다.
- **예외 경계를 계층에서 끊었다.** 파서가 `pymupdf.FileDataError`를 잡아 도메인 예외로 변환하고, 원인은
  로그로만 남깁니다. 라이브러리 예외가 라우터까지 새면 계층 분리가 무의미해지고 내부 메시지가 응답에
  노출되는데, 그 지점을 코드 주석으로 남겨 의도가 보존됐습니다. BE 설계 비중 18%에 직접 대응합니다.
- **테스트를 구현과 같은 커밋에 넣었다.** 신규 프로덕션 코드 대비 테스트가 791줄로, 파서 3종·API·서비스가
  모두 커버됐습니다. `pdf_fixtures.py`로 PDF를 코드에서 생성해 바이너리 픽스처를 리포에 넣지 않은 것도 좋습니다.
- **`ARCHITECTURE.md`를 구현과 동시에 갱신했다(+178줄).** 문서-코드 불일치는 이 과제에서 감점이 아니라
  불합격 사유이므로, 나중에 몰아 쓰지 않은 판단이 옳습니다.

### Problem (나빴던 것)

- **방향 수정이 두 번 다 "중단(interrupt)"으로 들어왔다.** 20:50과 21:13, 진행 중이던 도구 호출을 끊고
  요구가 추가됐습니다. UploadFile·20MB 상한 같은 제약은 라우터를 쓰기 *전에* 정해졌어야 할 인터페이스
  결정인데, 구현이 시작된 뒤 들어와 이미 쓴 코드를 되돌렸습니다.
- **검증 스크립트가 두 번 깨졌다(20:53 exit 1, 21:22 exit 7).** 구현 코드가 아니라 **확인용 임시
  스크립트**가 실패한 것입니다. 특히 21:22는 업로드 응답을 `json.load(sys.stdin)`에 그대로 물려 파싱이
  터진 케이스로, 응답이 JSON이 아닐 때(에러 페이지·빈 응답)를 가정하지 않았습니다. 검증하려다 검증
  도구를 디버깅하는 데 시간이 들어갔습니다.
- **`/compact`가 세션당 3회 발생했다.** 16:04~21:26 한 세션이 컨텍스트 한계를 세 번 넘겼습니다. 그때마다
  요약 재구성 비용이 들고, 요약 과정에서 세부 결정이 유실될 위험이 있습니다.
- **20:32~21:25에 커밋이 한 개뿐이다.** 파서·서비스·라우터·테스트·문서가 한 커밋(+1637)에 뭉쳐
  들어갔습니다. Retrobot이 커밋 시점으로 회고를 만드는 구조라, 계층별 진행 과정이 기록으로 남지 않습니다.

### Try (개선해야 할 것)

- **라우터·핸들러를 쓰기 전에 "인터페이스 제약 확인" 단계를 명시적으로 둔다.** 업로드 방식(UploadFile vs
  bytes), 크기 상한, 에러 코드 매핑처럼 되돌리기 비싼 결정은 `tasks.md`의 해당 항목에 선행 질문으로
  적어두고 구현 전에 한 번 확인합니다.
- **임시 검증 스크립트 대신 `tests/`에 실행 경로 테스트를 추가한다.** `TestClient`로 실제 업로드 →
  파싱 → 청킹을 검증하면 스크립트가 깨질 일도, 매번 다시 쓸 일도 없습니다. curl+파이프 파싱을 쓸 땐
  최소한 `-f`와 응답 본문 출력을 함께 둡니다.
- **작업 블록(파서 / 서비스 / 라우터) 단위로 커밋을 쪼갠다.** 각 블록이 테스트 통과 지점에 도달할 때
  커밋하면(150 passed → 163 → 187이 그 자연스러운 경계였습니다) 회고 타임라인의 해상도가 올라갑니다.
- **긴 세션은 블록 경계에서 의도적으로 끊는다.** `/compact`를 세 번 맞기 전에, 커밋 직후 새 세션으로
  넘어가면 요약 손실 없이 컨텍스트가 리셋됩니다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 18:20 | `f2564d3d` | 청킹 구현 커밋·push (`0e9391c`) |
| 18:20~18:27 | `b57ce982` | Retrobot 회고 자동 생성 (`eb97570`) |
| 18:27~20:32 | — | 작업 공백 |
| 20:32 | `f2564d3d` | `/compact` 후 `/opsx:apply` 재개 |
| 20:35~20:50 | `f2564d3d` | 파서 3종(PDF·TXT·MD)·레지스트리·정규화 구현 → **150 passed** |
| 20:50 | `f2564d3d` | 사용자 중단 — "service단까지 구현, 실제 업로드 파싱·청킹 확인" |
| 20:53 | `f2564d3d` | 샘플 문서 실측 스크립트 exit 1 (⚠) |
| 20:58 | `f2564d3d` | `services/ingestion.py` 구현 → **163 passed** |
| 21:13 | `f2564d3d` | 사용자 중단 — UploadFile + `MAX_UPLOAD_BYTES` 20MB 요구 |
| 21:14~21:20 | `f2564d3d` | 라우터를 UploadFile 기반으로 재작성, ruff E501 수정 → **186 passed** |
| 21:22 | `f2564d3d` | 업로드 응답 JSON 파싱 검증 스크립트 exit 7 (⚠) |
| 21:24 | `f2564d3d` | **187 passed** · ruff 통과 · `openspec validate --strict` 통과 |
| 21:25 | `f2564d3d` | 커밋 `80e99ce [BE] feat: 파서 구현 및 업로드 구현` (+1637/-37) |
