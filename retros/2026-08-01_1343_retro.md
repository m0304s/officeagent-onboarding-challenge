# Retrobot 회고 — 2026-08-01 13:43 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

트리거 커밋: `198870e` — *feat: 도커 컨테이너 및 SDK 자격증명 세팅*
(생성 직후 작성자 이메일 정정 리베이스로 해시가 `5c1c668` → `198870e`로 바뀌었습니다. 아래 Problem 참고.)

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 6 (+ 서브에이전트 7) |
| 신규 세션 | 1 (`41a00411` 후반부) + Retrobot 자기 실행 1 (`a90b7e8c`) |
| 총 프롬프트 | 34 (직전 회고 이후 신규 7) |
| 에러 발생 | 0 (`is_error` 0건) |

직전 회고([13:32](./2026-08-01_1332_retro.md)) 이후 새로 쌓인 로그는 **`41a00411` 세션의 13:32~13:42 구간**과
그 회고를 만든 훅 실행 `a90b7e8c`입니다. `03c55037`·`c292ef71`·`9c66fe8a`·`165045ea`와 서브에이전트 7개는
[12:35](./2026-08-01_1235_retro.md)·[13:22](./2026-08-01_1322_retro.md)·[13:32](./2026-08-01_1332_retro.md) 회고가 이미 다루었습니다.

## 세션별 활동

### 세션 `41a00411` (13:32 ~ 13:42) — 신규 구간

`bootstrap-runtime-skeleton`의 **그룹 6(컨테이너와 실행)** 을 끝내고 커밋했습니다. 35/52 → **47/52**.

**프롬프트:**
1. `/opsx:apply bootstrap-runtime-skeleton의 컨테이너화 블록 진행해주세요.`
2. (도구 사용 중 인터럽트 1회)
3. `git commit -m ...` — 직접 실행, 스테이징 누락으로 무동작
4. `git status`
5. `git config --global user.email cms8955@naver.com`
6. 커밋 작성자 정정 요청 (미푸시 7개 커밋 리라이트)

**만든 것:** `Dockerfile`(Python 3.11 + Node + `@anthropic-ai/claude-code`, 비루트 `app` UID 1000),
`docker-compose.yml`, `Makefile`, `scripts/sync-credentials.sh`, `.dockerignore`, `tests/test_boot.py`,
`README.md`·`ARCHITECTURE.md` 갱신 — 10개 파일 +462/-20.

**진행 경과:**

- 호스트 자격증명 형태를 먼저 확인 (토큰 값은 출력하지 않음) → 6.1부터 순차 진행
- 이미지 빌드가 오래 걸려 **백그라운드로 돌리면서 나머지 태스크를 병행**
- 빌드 성공 후 자기 검증에 그치던 테스트를 발견 — "마지막 테스트가 자기 자신을 검증하는 형태라 실제 커버리지가 없습니다"
  → 동기화 스크립트를 실제 실행하는 테스트로 교체 (출력 경로에 이음매 추가)
- 비공개 속성 `_probes`에 접근하는 테스트 대신 **관찰 가능한 동작**(외부 서비스 없이 `/health` 응답)으로 단언 전환
- 46개 테스트 통과 → 6.8 검증: **자격증명 없는 상태로 실제 기동** → `/health` 200,
  CLI 2.1.220 이미지에 설치 확인, 비루트 마운트 확인 → 자격증명 복원 후 0600·`app` 소유 읽기·쓰기 확인
- 문서에 `make up`을 한 줄 실행 명령으로 못박은 뒤 **그 명령이 실제로 도는지 확인하고** 종료
- `.secrets/`가 git에 잡히지 않는 것까지 확인

### 세션 `a90b7e8c` (13:32 ~ 13:36) — Retrobot 자기 실행

수정된 훅이 백그라운드에서 13:32 회고를 생성한 실행입니다. 트리거 커밋(작성자 날짜 13:32:24)에 대해
**커밋 시각 13:36:45** — 약 4분 20초. 직전 6분 20초보다 짧아졌고, 훅 자체는 즉시 반환했습니다.

**발생한 에러:** 0건. 이번 구간 전체에서 `is_error` tool_result가 하나도 없습니다.

## KPT 회고

### Keep (좋았던 것)

- **"문서에 적은 명령이 실제로 도는지"를 매번 마지막에 확인했습니다.** README에 `make up`을 한 줄 실행
  명령으로 적은 직후 그 명령을 실제로 돌렸습니다. CLAUDE.md의 불합격 기준 1번이 *명시된 방법으로 실행 불가*이고
  평가자는 단 1회만 시도합니다. 문서를 쓴 세션에서 바로 검증하지 않으면 어긋남을 알 방법이 없습니다.
- **자기 자신을 검증하는 테스트를 스스로 잡아냈습니다.** 빌드가 통과한 뒤 "마지막 테스트가 자기 자신을
  검증하는 형태라 실제 커버리지가 없다"고 판단해 동기화 스크립트를 실제 실행하는 테스트로 바꿨습니다.
  통과하는 테스트를 그대로 두는 게 가장 쉬운 선택인데 거기서 멈추지 않았습니다.
- **비공개 속성 접근 대신 관찰 가능한 동작으로 단언했습니다.** `_probes`를 노출하는 접근자를 만들 수도
  있었지만, "외부 서비스 없이도 헬스에 응답하는가"라는 외부 관찰로 바꿨습니다. 테스트가 내부 구조에
  묶이지 않아 이후 change에서 프로브 구현이 바뀌어도 깨지지 않습니다.
- **긴 빌드를 백그라운드로 빼고 나머지를 병행했습니다.** 도커 빌드를 기다리며 놀지 않고 다른 태스크를
  진행했습니다 — 13:22 회고에서 훅을 백그라운드로 뺀 것과 같은 패턴이 작업 진행에도 적용됐습니다.
- **비밀정보를 다루면서 값을 로그에 남기지 않았습니다.** 자격증명 형태 확인 시 "토큰 값은 출력하지 않습니다"를
  명시하고 진행했고, `.secrets/`가 git에 잡히지 않는 것까지 확인했습니다. 세션 로그가 그대로 `retros/`에
  요약되는 구조라 이 습관이 없으면 회고 파일로 토큰이 새어 나갑니다.
- **훅 수정이 효과를 유지했습니다.** 13:32 회고가 4분 20초 만에 자동 커밋까지 도달했습니다. 직전 회고에서
  검증한 백그라운드 분리가 일회성이 아니었음이 두 번째 실행으로 확인됐습니다.

### Problem (나빴던 것)

- **커밋 작성자 이메일을 7개 커밋을 쌓은 뒤에야 발견했습니다.** `최민석 <minseok@choeminseog-ui-MacBookPro...>`
  라는 로컬 호스트명 기반 이메일로 전부 커밋됐습니다. 저장소 초기화 직후 `git config user.email`을 확인했다면
  1분이면 끝날 일이 미푸시 커밋 7개 전체 리라이트가 됐습니다.
- **그 리라이트가 앞선 회고들의 커밋 참조를 전부 끊었습니다.** 12:35·13:22·13:32 회고에 적힌
  `b6dfbde`·`9543347`·`ff0223f`·`02e7392`·`5d4592f`가 이제 존재하지 않는 해시입니다.
  이 회고의 트리거 커밋 `5c1c668`도 생성 직후 `198870e`가 됐습니다. 다행히 미푸시 상태라 남의 히스토리는
  깨지 않았지만, 회고가 커밋 해시를 유일 식별자로 쓰는 이상 같은 일이 또 생기면 또 끊깁니다.
- **직접 실행한 `git commit`이 스테이징 누락으로 헛돌았습니다.** 세션 안에서 `git add` 없이 커밋을 시도해
  "Changes not staged for commit"만 받고 `git status` → 재시도로 한 왕복을 썼습니다.
- **`.retro_parse.py`가 리포 루트에 남아 린트를 깨뜨렸습니다.** 13:32 회고가 만든 임시 스크립트가
  이번 세션의 린트 실패 원인이 됐습니다. 직전 회고에서 "권한 규칙이 쓰기 범위를 강제하지 못한다"고
  지적한 바로 그 문제가 **다음 세션의 실제 비용으로 돌아왔습니다.** 지적만 하고 가드를 넣지 않은 결과입니다.
- **Codex 검토 실패는 결국 모델 이름 문제였고, 6번을 찍어본 뒤에야 캐시를 봤습니다.** (`41a00411` 전반부)
  계정 모델 캐시에 `gpt-5.4`·`gpt-5.4-mini`·`gpt-5.5`가 있었고 `gpt-5.5-codex`가 아니라 `gpt-5.5`가 맞는
  이름이었습니다. 캐시를 먼저 봤다면 6회가 1회였습니다. 직전 회고의 Try("모델 탐색을 그만두고 경로를 바꾼다")가
  옳은 방향이었고, 실제로 그 방향으로 풀렸습니다.

### Try (개선해야 할 것)

- **회고는 커밋 해시 대신 커밋 메시지 + 작성자 날짜를 1차 식별자로 쓰고, 해시는 보조로 병기합니다.**
  `retrobot/SKILL.md`의 트리거 커밋 표기 형식을 `<message>` (`<date>`, `<hash>` 당시 기준)으로 바꿉니다.
  리베이스·amend는 앞으로도 일어납니다.
- **훅에 워킹 트리 가드를 넣습니다 — 이번엔 실제로.** 자동 커밋 직전 `git status --porcelain`이 `retros/`
  밖을 건드렸으면 중단하고, 회고 실행이 만든 임시 파일은 종료 시 스스로 지웁니다. 직전 회고에서 같은 Try를
  적고 넘어간 탓에 `.retro_parse.py`가 다음 세션의 린트를 깼습니다.
- **저장소 초기 설정 체크리스트를 `ARCHITECTURE.md` 또는 `README.md`에 한 줄로 남깁니다.**
  최소 `git config user.name/user.email` + `git config core.hooksPath .githooks`. hooksPath는 12:35에,
  이메일은 13:42에 각각 사후 발견됐습니다 — 같은 종류의 누락이 두 번 났습니다.
- **`git add` → `git status` 확인 → `git commit` 순서를 고정합니다.** 세션 안에서 커밋할 때 스테이징
  결과를 눈으로 확인하고 넘어갑니다. 한 줄 추가로 왕복 하나가 사라집니다.
- **남은 그룹 7(마무리 검증) 5개를 다음 세션에서 끝내고 `openspec` 아카이브까지 갑니다.** 47/52에서
  멈춰 있고, 여기서 다른 change로 넘어가면 미완 change가 쌓입니다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 13:32 | `03c55037` | 훅 수정 커밋 (`ff0223f` → 현 `0a5c196`) |
| 13:32–13:36 | `a90b7e8c` | Retrobot 백그라운드 실행 — 13:32 회고 생성 (4분 20초) → 현 `22ac983` |
| 13:32–13:38 | `41a00411` | 그룹 6 착수 — 자격증명 형태 확인, Dockerfile·compose·sync 스크립트, 빌드 백그라운드 병행 |
| ~13:38 | `41a00411` | 자기 검증 테스트를 실제 실행 테스트로 교체, `_probes` 접근 대신 관찰 가능한 단언으로 전환 (46 통과) |
| ~13:39 | `41a00411` | 6.8 — 자격증명 없이 기동 `/health` 200, CLI 설치·비루트 마운트·0600 권한 확인 |
| ~13:40 | `41a00411` | README·ARCHITECTURE 갱신 후 `make up` 실동작 확인 → 47/52 |
| 13:41 | `41a00411` | 커밋 `5c1c668 feat: 도커 컨테이너 및 SDK 자격증명 세팅` (10파일 +462/-20) |
| 13:42 | `41a00411` | 커밋 작성자 이메일 문제 발견 → 미푸시 7개 커밋 작성자 리라이트 (전 해시 변경) |
