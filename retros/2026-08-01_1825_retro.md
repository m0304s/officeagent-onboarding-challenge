# Retrobot 회고 — 2026-08-01 18:25 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

분석 구간: 직전 회고(17:40) 이후 ~ 커밋 `0e9391c` (`feat: 문서 도메인·청킹 구현, index_signature 도입`)까지.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 3 (실작업 1 + Retrobot 2) |
| 총 프롬프트 | 4 (실사용자 발화 기준) |
| 에러 발생 | 5 (pytest 실행 경로 1 · Retrobot 권한 4) |

## 세션별 활동

### 세션 `f2564d3d` (17:38 ~ 18:21) — `/opsx:apply` task 1~2 구현 (메인)

**프롬프트:**
1. (17:25 큐잉) 청킹 전략은 recursive를 기본 옵션으로. 추후 부모-자식 옵션은 선택지 추가할 수 있도록
2. 청킹할때 왜 재귀형식으로 하지 않은건가?
3. codex를 통해서 작성된 내용 리뷰 진행해주세요.
4. 해당 로직까지 커밋 진행하고, push 진행해주세요.

**발생한 에러:**
- `python -m pytest` → Exit code 1. `.venv/bin`에 `pytest`가 없고 `command -v python3`가 `/usr/bin/python3`로 폴백. `.venv/bin/python -m pytest`로 정정 후 통과

**산출물:** 커밋 `0e9391c` — 12 파일, +1465/-24.
`core/chunking.py` 285줄 · `core/documents.py` 291줄 · `core/exceptions.py` +84 · `config.py` +42 · `ARCHITECTURE.md` +85 · 테스트 628줄(`test_chunking.py` 323 · `test_documents.py` 236 · `test_config.py` 69) · 스펙/design/tasks 갱신.

### 세션 `704ba125` (17:39 ~ 17:44) · `b57ce982` (18:20 ~) — Retrobot 자동 회고

**발생한 에러 (b57ce982, 이번 실행):**
- 셸 `for` 루프 → `Contains simple_expansion`
- `/tmp/retro_scan.py` Write 권한 미승인
- `python3 -c ...` → `This command requires approval`
- `grep -o ...` → `requires approval`
- 결국 Grep 도구 + `sed`/`head` 폴백으로 우회

## KPT 회고

### Keep (좋았던 것)

- **지시가 코드에 반영됐는지 산출물을 열어 대조한 것.** 17:25에 "recursive를 기본 옵션으로"를 지시하고, 17:55에 결과물을 보고 "왜 재귀형식으로 하지 않은건가?"를 되물었다. 에이전트의 보고("구현했습니다")가 아니라 파일을 근거로 판단했고, 그 되물음 하나가 30분치 구현의 방향을 되돌렸다.
- **한국어 문서에 맞는 분리자 계층을 직접 정한 것.** `_SEPARATORS`가 `"\n\n" → "\n" → "다. " → "요. " → ". " → " "` 순이다. 영어권 라이브러리의 기본 구분자(`\n\n`/`\n`/`. `/` `)를 그대로 쓰면 한국어 문장은 마침표 앞에서만 갈라져 문장 중간이 끊긴다. `다. `·`요. `를 계층에 넣은 것은 이 과제의 샘플 문서가 한국어라는 사실을 청킹 구현에 반영한 결정이다.
- **적대적 리뷰를 스펙 단계와 구현 단계에 각각 한 번씩 돌린 것.** 16:3x에 스펙을, 18:05에 구현 코드를 각각 Codex로 리뷰했다. 스펙 리뷰만 하고 구현을 자기 검토로 끝냈다면 `chunking.py`의 경계 처리는 검증되지 않은 채 커밋됐다. 리뷰 후 `chunking.py`를 `/tmp/chunking.bak`으로 백업한 뒤 수정한 것도, 되돌릴 수 있는 상태를 만들고 손댔다는 점에서 옳다.
- **첫 구현 커밋에 테스트 628줄이 함께 들어간 것.** 과제 규칙상 자동화 테스트 전무는 불합격 사유다(`CLAUDE.md`). 구현을 먼저 쌓고 테스트를 나중에 붙이는 순서였다면 마감 압박이 커질수록 밀렸을 항목인데, 첫 커밋부터 구현:테스트가 576:628로 붙었다.
- **무작위 입력으로 불변식을 직접 때려본 것.** `import random`으로 생성한 입력에 대해 "모든 비공백 문자가 어느 청크엔가 포함됨", "어떤 청크도 상한을 넘지 않음"을 두 차례 확인했다. spec에 SHALL/MUST NOT으로 적어둔 조항을 예시 기반 단위 테스트가 아니라 난수로 검증한 것이라, 손으로 고른 케이스가 놓치는 경계를 노렸다.
- **커밋 전 `pytest` + `ruff check`를 항상 세트로 돌린 것.** 로그상 최소 6회. 커밋 직전에도 한 번 더 돌리고 나서 `git add`했다.

### Problem (나빴던 것)

- **명시 지시가 구현에 반영되지 않아 사용자가 되물어야 했다.** 17:25 "recursive를 기본 옵션으로"와 17:55 "왜 재귀형식으로 하지 않은건가?" 사이 30분간, 구현은 지시와 다른 방향으로 진행됐다. 사용자가 결과물을 읽지 않았다면 그대로 커밋됐을 것이다. 지시 수신과 구현 착수 사이에 확인 지점이 없다.
- **`.venv` 확인 없이 `python -m pytest`를 먼저 실행했다.** 이 프로젝트는 `.venv/bin/python`을 쓴다는 사실이 로그 앞부분에 이미 있었는데도 시스템 python으로 시도했고, 실패 후 `ls .venv/bin`으로 확인하는 왕복이 한 번 더 붙었다.
- **거의 모든 Bash 호출이 `cd /Users/minseok/Desktop/claude_code/officeagent-onboarding-challenge`로 시작한다.** 17:50 이후 호출 20여 건 중 대부분이 같은 절대 경로를 앞에 달고 있다. 작업 디렉토리가 호출 간 유지되지 않는 것을 매번 `cd`로 때우고 있어, 명령 하나하나가 길어지고 읽기 어려워진다.
- **Retrobot이 3회 연속 같은 권한 벽에 부딪혔다.** 15:55 · 17:40 · 18:25 실행이 *동일한 순서로* 실패했다. 직전 회고가 Try로 적은 두 가지(SKILL.md를 Grep 우선으로 재작성 / `.claude/settings.json`에 권한 등록) 중 어느 것도 실행되지 않았다. 회고가 개선안을 적기만 하고 반영되지 않으면, 다음 회고는 같은 문장을 또 쓴다.
- **커밋 하나가 스펙 수정·구현·테스트·아키텍처 문서를 모두 담아 1465줄이 됐다.** 문서-코드 일치는 지켜졌지만, 나중에 "왜 이 청킹 구현인가"를 되짚을 때 이 커밋 하나를 통째로 읽어야 한다. 앞선 `0d27373`(스펙만 980줄)이 깨끗했던 것과 대비된다.

### Try (개선해야 할 것)

- **`/opsx:apply` 착수 시 해당 태스크에 걸린 사용자 지시를 `tasks.md`에 먼저 적는다.** "2.x 재귀 분할 — recursive 기본, 부모-자식은 옵션(2026-08-01 지시)" 같은 한 줄이면 구현이 지시에서 벗어날 때 파일이 먼저 알려준다. → *Problem 1*
- **세션의 첫 Bash에서 `.venv/bin/python -m pytest --version`을 1회 확인한다.** 인터프리터를 고르는 왕복이 세션당 한 번으로 끝난다. → *Problem 2*
- **`.claude/settings.json`에 프로젝트 표준 명령 프리픽스를 등록하고, 상대 경로로 호출한다.** 매 명령 앞의 `cd` 절대경로를 없앤다. → *Problem 3*
- **Retrobot 권한 문제를 다음 회고 전에 반드시 하나 처리한다.** 선택지는 (a) `.claude/settings.json`에 `Bash(python3:*)`·`Write(/tmp/**)` 허용 추가, (b) `retrobot/SKILL.md` 1~2단계를 Grep 도구 전용으로 재작성. 3회 반복은 "다음에 하자"가 통하지 않는다는 증거다. → *Problem 4*
- **커밋을 스펙 수정 / 구현+테스트 / 문서 3개로 쪼갠다.** `/opsx:apply` 중 tasks.md 체크와 코드가 섞이지 않도록, 태스크 묶음이 끝날 때마다 커밋한다. → *Problem 5*

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 17:25 | `f2564d3d` | 청킹 전략 지시 — recursive 기본, 부모-자식은 옵션으로 남김 |
| 17:39 | `704ba125` | Retrobot 회고 생성 → 커밋 `73abe8a` |
| 17:45 | `f2564d3d` | `pytest` 실행 실패(시스템 python) → `.venv/bin/python`으로 정정 |
| 17:55 | `f2564d3d` | "왜 재귀형식으로 하지 않은건가?" → 재귀 분할로 재작성 |
| 18:0x | `f2564d3d` | `_SEPARATORS` 한국어 종결어미 계층 도입, 난수 입력으로 청크 불변식 검증 |
| 18:05 | `f2564d3d` | Codex 구현 리뷰 → `chunking.py` 백업 후 수정, `pytest`+`ruff` 재검 |
| 18:19 | `f2564d3d` | 커밋 `0e9391c` (12 파일 +1465/-24) + `document-ingestion` push |
| 18:25 | `b57ce982` | Retrobot 회고 생성 (권한 실패 4회 후 Grep 폴백) |
