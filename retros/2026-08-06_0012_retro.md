# Retrobot 회고 — 2026-08-06 00:12 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 8 (실작업 3 · 훅 4 · 잔여 1) |
| 총 엔트리 | 1,589 |
| 도구 호출 | 422 (Bash 191 · Edit 103 · Read 47 · Grep 16 · Write 20) |
| 실질 프롬프트 | 11 |
| 에러 발생 | 20 (그중 16건 = 80% 가 Retrobot 자신) |
| 창 내 커밋 | 17 (실작업 11 · 회고 6) |

이 창(08-05 00:07 ~ 08-06 00:08)의 로그는 **macOS 호스트분만** 있습니다.
10:00~17:53 커밋 5건은 다른 호스트(세션 `f2287e68`)에서 났고, 그쪽 회고 4편
(`_1005`·`_1432`·`_1455`·`_1656`)이 이미 덮었습니다. 아래 분석은 로컬 로그가 있는
00:08~02:13 · 08:01~08:19 · 23:09~00:08 구간입니다.

## 세션별 활동

### `5db6209d` (00:08 ~ 00:08) — 7엔트리

직전 회고(`bfb8f5d`) 커밋 마무리. 에러 0건.

### `34e9f10c` "add-cross-encoder" (00:16 ~ 01:21) — 538엔트리 / 도구 133회 / 에러 1

**프롬프트:**
1. `/opsx:propose add-cross-encoder` — 리랭킹 단계 신설. "RRF 합의도만으로는 *몇 개의
   목록이 이 청크를 위에 뒀는가*만 보고 *이 청크가 이 질문에 실제로 답하는가*는 아무도
   보지 않는다"
2. `/opsx:apply` 1장 → 2장
3. "bge-reranker-v2-m3가 된다면, 해당 모델로 교체해주세요" (01:11)

`AskUserQuestion` 3문항이 제안 12분 만에 나왔고 전부 되돌리기 비싼 축입니다 — 굽는 모델,
배포 기본값, 리랭킹 점수를 `score` 자리에 놓을지. 커밋 `0145a27`(00:51) → `ba4e9d6`(01:21).

01:11 모델 교체 요청은 실측으로 처리됐습니다: HF API 로 `config.json`·`tokenizer_config.json`
을 직접 받아 크기를 재고, 컨테이너에서 `sentence_transformers.CrossEncoder` 시그니처를
`inspect` 로 확인한 뒤 바꿨습니다. 문서에 박혀 있던 `1.2GB`·`306M` 같은 낡은 수치도 같은
자리에서 정정.

**에러:** 1건 — `sleep 90; tail …` 이 도구에서 차단(복합 명령). 백그라운드 태스크로 우회.

### `408546e5` (00:51 ~ 00:57) · `518ba2d1` (01:21 ~ 01:28) — Retrobot 성공

각각 6분·7분. 회고 `cf9152e`·`37c3856` 커밋. 에러 4·5건은 전부 `SKILL.md` 첫 두 줄
(`command -v python3 jq` 거부 → `/tmp` Write 거부)에서 났고, 그 뒤 `python3 -c` 로 통과.

### `ac8941c4` (01:22 ~ 01:42) — 339엔트리 / 도구 91회 / 에러 1

`/opsx:apply` 3·4장 — 설정·배선·테스트를 20분에. `--build` 를 붙인 재검증이 살아 있고
(`docker compose run --build --rm test`), 01:32 `ruff format --check` 위반을 그 자리에서
발견해 닫았습니다. 커밋 `8000c6b`(01:39).

### `f20abf8a` (01:39 ~ 02:13) · `f2a13c41` (08:01 ~ 08:19) — Retrobot 실패

**둘 다 분석을 끝내고 쓰기 직전에 죽었습니다.**

| 세션 | 마지막 텍스트 | 죽은 시각 | 간격 |
|---|---|---|---|
| `f20abf8a` | "Analysis complete. Writing the retro." (01:58) | 02:13 | **15분** |
| `f2a13c41` | "모든 데이터를 확보했습니다. 회고를 작성합니다." (08:04) | 08:19 | **15분** |

둘 다 `API Error: Connection closed mid-response`. 회고 파일도, 커밋도 없습니다.
`f20abf8a` 는 34분간 Bash 29·Grep 16 회를 태우고 산출물 0.

### `c69d384c` (23:09 ~ 00:07) — 349엔트리 / 도구 82회 / 에러 2

**프롬프트 6건** — `/opsx:propose` (pymupdf4llm 병존) → `/opsx:propose` 재호출(23:23,
사용자가 중단) → **`/opsx:update` 로 교정**(같은 change 를 고쳐야 하는 자리에 새 change 를
만들 뻔함) → `/superpowers:brainstorming`(서명+인덱싱 정합 검토) → `/opsx:update` 0.3.4
고정(23:54, 중단) → **`/opsx:update` 1.28.0 정정**(23:58).

`AskUserQuestion` 6블록이 전부 되돌리기 비싼 축에 걸렸습니다. 그중 23:13 질문이 이 세션의
값입니다 — **"`index_signature` 에 파서 정체성이 없어서, 백엔드를 바꾸고 같은 파일을
재업로드해도 `unchanged` 로 끝나고 옛 청크가 남는다."** 설계는 이것을 재료 한 칸
(`pdf_extraction_signature`)으로 닫았고, 그 덕에 **무효화 코드를 한 줄도 새로 쓰지 않습니다**
— 기존 경로(`_purge_previous` · `derive_cache_key` · `derive_cache_scope` · `_verify`)가
어디까지 닿는지 따라가 표로 확인했습니다.

버전 조회는 PyPI JSON 을 직접 쳤고(`0.3.4` → `1.28.0`), `to_markdown` 계약은 pymupdf4llm
공식 API 문서를 받아 확인했습니다. 계층형 인덱싱은 "된다/안 된다"가 아니라 **서명 축이
겹친다**는 이유로 후속 change 로 분리(`CHUNK_STRATEGY_VERSION` 상승이 따라옴).

**에러:** 2건 — 둘 다 사용자의 의도적 중단(23:24 · 23:57)이고, 둘 다 방향 교정으로
이어졌습니다.

## KPT 회고

### Keep

- **`add-cross-encoder` 한 브랜치가 하루에 닫혔고, origin 에 올라가 있습니다** — 15커밋 ·
  63파일 · **+5,748/-80**, `origin/add-cross-encoder`(`431b5d2`)가 로컬과 같은 커밋입니다.
  08-04 창 내내 "미푸시 36건 / 브랜치가 origin 에 아예 없음"으로 다섯 회차 이월됐던 항목이
  이번 창에서는 **한 번도 재발하지 않았습니다.** 미푸시는 지금 **1건**(`88dc607`).
- **모델·버전·라이브러리 계약을 전부 실측으로 확정했습니다** — HF API 로 모델 크기와
  토크나이저, 컨테이너 `inspect` 로 `CrossEncoder` 시그니처, PyPI JSON 으로 의존성 그래프.
  "된다고 하니 쓴다"가 한 건도 없고, 문서의 낡은 수치(`1.2GB`·`306M`)도 같은 자리에서
  정정됐습니다.
- **되돌리기 비싼 축에서 먼저 물었습니다** — `AskUserQuestion` 7블록이 전부 코드가 붙기
  전에 나왔습니다(모델 선택 · 배포 기본값 · `score` 자리 · 파서 기본값 · 서명 재료 · 계층형
  인덱싱 범위 · 라이선스). 특히 서명 질문은 **비용 0 시점**에 나와 설계 축이 됐습니다.
- **`/opsx:propose` 를 `/opsx:update` 로 두 번 고쳐 잡았습니다** — 기존 change 를 고쳐야 하는
  자리에 새 change 를 만들면 스펙이 둘로 갈립니다. 사용자 중단 2회가 모두 그 교정이었고,
  둘 다 손해 없이 끝났습니다.
- **주석 게이트 `112개 파일, 위반 0건` 유지** — 08-04 에 187건 → 0건으로 닫힌 뒤 그대로입니다.

### Problem

1. **훅 두 실행이 15분씩 멈췄다가 산출물 0으로 죽었습니다.** 분석은 끝났고 "이제 씁니다"
   직후에 `Connection closed mid-response`. 두 번 다 **정확히 15분**이라 우연이 아닙니다.
   직전 회고들의 `TIMELINE.md` 한 행이 3,000~4,000자이고 파일 자체가 **59줄에 32,000토큰**
   이라(`_1455` 회고가 `Read` 한도 초과로 이미 관측), 회고 하나를 큰 `Write` 한 번으로 쏟는
   모양이 임계에 닿았습니다. 대가는 08-05 새벽·아침 두 구간이 **기록에서 통째로 비는 것**.
2. **이 브랜치의 `retros/TIMELINE.md` 에 08-05 가 한 줄도 없습니다.** `fix-pdf-parser` 는
   `main`(08-04 23:48)에서 갈라졌고, 08-05 회고 6편은 `add-cross-encoder` 에만 있습니다.
   지금 `main` 을 여는 사람에게 **08-05 는 존재하지 않는 하루**입니다.
3. **`pymupdf` 의 라이선스 기록이 오히려 지워졌습니다.** `pyproject.toml` 에서
   `# AGPL-3.0 — 상용 배포로 가려면 pypdf(BSD) 교체가 선행되어야 한다` 두 줄이 삭제되고
   버전 고정 근거로 대체됐습니다. 그런데 이번에 들어오는 `pymupdf_layout` 은 **비상용
   라이선스**이고, 00:00 `AskUserQuestion` 이 정확히 그것을 물었는데 — **커밋된
   아티팩트 어디에도 "라이선스"라는 단어가 없습니다.** `design.md:197` 에 남은 것은 크기와
   네트워크 위험뿐이고, `ARCHITECTURE.md:20`·`:498`·`README.md:666` 은 지금도 "AGPL 하나만
   조심하면 되고 교체 범위는 파일 하나"라고 말합니다. 질문은 했는데 **답의 근거가 증발**했고,
   상황은 더 나빠진 쪽입니다.
4. **`pyproject.toml` 이 워킹트리에만 있습니다.** `88dc607` 은 스펙 5파일만 담았는데
   의존성 교체(`pymupdf==1.28.0` · `pymupdf4llm==1.28.0`)는 이미 편집돼 있고 `tasks.md`
   의 `1.1` 은 `[ ]` 입니다. 08-04 에 세 번 반복된 "구현과 문서가 커밋에서 갈림" 패턴의
   거울상이고, `1.1a`(의존성이 실제로 무엇을 끌고 오는지 컨테이너에서 확인)를 밟기 전에
   핀이 먼저 들어갔습니다.
5. **`ruff format --check` 16건 — 13 → 16 으로 늘었습니다.** 08-04 창에서 열한 번 재현되고
   한 건도 안 줄었던 항목이 이번엔 증가했습니다. 같은 순간 `ruff check` 는 통과하므로
   초록불이 거짓이고, 닫는 비용은 여전히 `.venv/bin/ruff format src tests` 한 줄입니다.
6. **`add-multiturn-qa` 가 아직 untracked 입니다** — 08-04 23:36 회고가 "여덟 번째 계수"로
   적은 그 디렉터리이고, 그 사이 change 가 둘 더 지나갔습니다.
7. **외부 적대적 검토 0회가 이어집니다.** 이번 창에서 들어온 것은 `+5,748` 줄의 리랭킹
   파이프라인과, 무효화 코드를 새로 쓰지 않겠다고 선언한 서명 설계입니다. 후자는 **틀리면
   조용히 옛 청크가 남는** 종류인데, 검증한 것은 자기 자신뿐입니다.

### Try

1. **회고를 한 번의 큰 `Write` 로 쏟지 않습니다.** 본문 파일을 먼저 쓰고 `TIMELINE.md` 는
   `Edit` 한 줄 추가로 분리하며, 타임라인 행은 요약 2~3문장으로 제한합니다(현재 행 평균
   3,500자 → 목표 600자 이하). 상세는 회고 파일 안에 있고, 링크가 그리로 데려갑니다.
   *— Problem 1*
2. **`TIMELINE.md` 를 브랜치가 아니라 `main` 에서 관리합니다.** 최소한 이번 회차에
   `add-cross-encoder` 의 08-05 6행을 이 브랜치로 회수하거나, 두 브랜치를 `main` 으로
   합쳐 타임라인 구멍을 닫습니다. *— Problem 2*
3. **라이선스 사실을 아티팩트에 되돌려 놓습니다.** `design.md` 위험 절에 `pymupdf_layout`
   의 라이선스 조건을 한 줄, `tasks.md 7.1` 에 `ARCHITECTURE.md:20`·`:498`·`README.md:666`
   갱신(“교체 범위는 파일 하나”가 더 이상 참이 아니고, 조심할 라이선스가 AGPL 하나가
   아니다)을 명시적으로 넣습니다. 이 리포에서 문서-코드 불일치는 감점이 아니라
   **불합격**입니다. *— Problem 3*
4. **`1.1a` 를 `1.1` 앞으로 옮기거나, 최소한 핀 커밋과 같은 커밋에서 밟습니다.**
   `docker compose build` 가 통과하고 첫 추출이 네트워크 없이 끝나는지 확인하기 전에는
   `pyproject.toml` 을 커밋하지 않습니다. *— Problem 4*
5. **`.venv/bin/ruff format src tests` 를 다음 커밋 전에 한 번 돌립니다** — 10초, 열두 번째
   재현을 여기서 끊습니다. *— Problem 5*
6. **`add-multiturn-qa` 를 커밋하거나 지웁니다.** 아홉 번째 계수를 만들지 않습니다.
   *— Problem 6*
7. **`add-pymupdf4llm-parser` 구현 착수 전에 Codex 검토를 한 번 겁니다.** 대상은 결정 2
   (파서와 서명 재료가 한 객체에서 나온다)와 결정 5(재료 한 칸으로 무효화를 대신한다) —
   지금은 스펙 텍스트라 반영 비용이 가장 쌉니다. *— Problem 7*

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 00:08 | `5db6209d` | 직전 회고 커밋 마무리 (`bfb8f5d`) |
| 00:16 | `34e9f10c` | `add-cross-encoder` 착수 — 리랭킹 단계 신설 제안 |
| 00:27 | `34e9f10c` | `AskUserQuestion` 3문항 (모델 · 기본값 · `score` 자리) |
| 00:51 | `34e9f10c` | `0145a27` 스펙 커밋 → 1장 착수 |
| 00:51 | `408546e5` | 훅 회고 성공 (`cf9152e`, 6분) |
| 01:11 | `34e9f10c` | bge-reranker-v2-m3 교체 — HF API·컨테이너 `inspect` 실측 |
| 01:21 | `34e9f10c` | `ba4e9d6` 크로스인코더 어댑터 |
| 01:22 | `518ba2d1` | 훅 회고 성공 (`37c3856`, 7분) |
| 01:22 | `ac8941c4` | 3·4장 — 설정·배선·테스트, `--build` 재검증 |
| 01:39 | `ac8941c4` | `8000c6b` 검색 파이프라인 배선 |
| 01:39 | `f20abf8a` | 훅 회고 **실패** — 01:58 "쓰겠다" → 02:13 연결 끊김 |
| 08:01 | — | `7dd258e` 리랭킹 설정 (macOS 마지막 커밋) |
| 08:01 | `f2a13c41` | 훅 회고 **실패** — 08:04 "쓰겠다" → 08:19 연결 끊김 |
| 10:00~17:53 | *(타 호스트)* | 리랭킹 API 노출 · 크로스인코더 실행 · LLM-Judge 스펙 · 평가 |
| 23:09 | `c69d384c` | `add-pymupdf4llm-parser` 제안 — 어댑터 병존 |
| 23:13 | `c69d384c` | 서명에 파서 정체성이 없다는 발견 → 설계 축 |
| 23:24 | `c69d384c` | `/opsx:propose` → `/opsx:update` 교정 |
| 23:31 | `c69d384c` | brainstorming — 서명·인덱싱 정합 검토 |
| 23:58 | `c69d384c` | 버전 고정 정정 (0.3.4 → 1.28.0), PyPI 실측 |
| 00:07 | `c69d384c` | `88dc607` 스펙 5파일 커밋 |
