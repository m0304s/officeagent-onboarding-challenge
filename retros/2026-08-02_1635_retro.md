# Retrobot 회고 — 2026-08-02 16:35 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 11 (실작업 3 + Retrobot 자동 실행 6 + 서브에이전트 2) |
| 총 프롬프트 | 3 (창 내 **신규** 지시 기준) |
| 에러 발생 | 1 (도구 실패 1) |
| 커밋 | `0b63e3f feat: Ingestion 스펙 확정 및 동작 확인` (+475/-18, 11 파일) |

이번 창(08-01 16:35 ~ 08-02 16:35)에서 **직전 회고([16:07](./2026-08-02_1607_retro.md)) 이후의 신규 활동은 마지막 17분**이다 —
`f2564d3d` 세션이 `add-document-ingestion` change의 마지막 두 섹션(9. 컨테이너와 실행 · 10. 마무리)을 끝내고 스펙을 동기화·아카이브했다.
그 앞 시간대는 [13:02](./2026-08-02_1302_retro.md) · [15:34](./2026-08-02_1534_retro.md) · [16:07](./2026-08-02_1607_retro.md) 회고에서 이미 다뤘으므로 아래 KPT는 이 17분에 집중한다.

## 세션별 활동

### 세션 `f2564d3d` (16:09 ~ 16:26 KST) — 섹션 9·10 완료, 스펙 동기화·아카이브

**프롬프트:**
1. `/opsx:apply` — 섹션 9(컨테이너와 실행) · 10(마무리)
2. `/opsx:archive`
3. (선택 응답) "스펙 동기화 — 지금 동기화 (권장)"

**도구 사용:** Bash 26 · Read 7 · AskUserQuestion 1

**9. 컨테이너와 실행**

- **9.1 의존성 고정** — `chromadb>=0.5` → `chromadb==1.5.9`. 서버 이미지와 버전이 어긋나면 "기동은 되는데 첫 쓰기에서 터진다"는 근거를 `pyproject.toml` 주석으로 남김
- **9.2 Dockerfile에 임베딩 런타임·가중치 굽기 (+28줄)**
  - torch를 CPU 전용 휠로. `--index-url`이 아니라 `--extra-index-url`인 이유를 주석에 명시 — CPU 인덱스에 aarch64 휠이 없어 인덱스를 통째로 갈아끼우면 **애플 실리콘 빌드가 아예 실패**한다
  - 모델 가중치 굽기를 `COPY src` **앞** 레이어로. 코드를 고칠 때마다 수백 MB 재다운로드를 막고, 기동 선로딩(4.8)의 런타임 네트워크 의존을 없앤다
  - `APP_EMBEDDING_MODEL`을 환경변수 하나로 둬 굽는 모델과 런타임 모델의 출처를 통일
- **9.7 `docker compose up` 실제 1회 실행 확인** — `make sync-credentials` → `docker compose up -d --wait` → 컨테이너 안에서 `/health` → txt 업로드 → `/documents` 목록 → 상세 → 삭제까지 **실제 요청으로** 확인
- **열화 상태 실측** — 벡터 스토어를 내린 뒤 확인한 결과가 README 서술과 달라 문구를 실측대로 정정:

  | 상황 | 실제 |
  |---|---|
  | 새 내용 업로드 | `503 storage_unavailable` |
  | 같은 바이트 재업로드 | `200 unchanged` (저장할 게 없어 벡터 스토어에 닿지 않음) |
  | 목록·상세 조회 | 정상 (레지스트리만 봄) |

- **9.8 문서-코드 대조** — README 설정 표를 눈으로 훑지 않고 `Settings`를 실제로 import해 대조하는 스크립트로 확인

**10. 마무리**

- **10.1** 외부 서비스·네트워크 없이 `pytest` → **308 passed, 19 skipped** / `make test-all`(벡터 스토어 포함) → **327 passed**
- **10.3 spec 시나리오 ↔ 테스트 대조** — `#### Scenario:` 목록과 `tests/test_*.py`의 테스트 함수 목록을 뽑아 맞춰 봄. 여기서 **미커버 시나리오 1건 발견** → `test_a_storage_failure_leaves_the_previous_revision_intact` 추가(+33). 배치 크기를 1로 낮춰 "첫 배치는 쓰이고 그다음이 실패"하는 부분 기록 상태를 만들어, 실패한 교체가 이전 리비전을 건드리지 않는지 단언
- **10.4** `openspec validate --strict` 통과 → **90/90 완료**

**바인드 마운트 전환에 따른 문서 정정** (직전 커밋 `8f48961`이 네임드 볼륨 → `./data/chroma` 바인드 마운트로 바꿨음)

- `ARCHITECTURE.md` 스택 표: "볼륨 — 각각 네임드 볼륨" → "영속화 — 호스트 바인드 마운트(`./data/chroma`·`./data/registry`)" + 리눅스 UID 1000 불일치 시 쓰기 실패 가능성 명시
- `README.md`: 서비스 표에 `vector-store-ui`(**기본 기동에 미포함, `--profile gui`**) 행 추가
- `README.md`: "`docker-compose.yml`의 `user:` 줄 **주석을 푸세요**" → "`user: "${DOCKER_UID:-1000}:${DOCKER_GID:-1000}"` 를 **추가하세요**". 해당 주석 줄은 직전 커밋에서 삭제돼 이미 존재하지 않았다

**아카이브 (16:22)**

- `AskUserQuestion` — "데이터 스펙을 메인 스펙으로 동기화한 뒤 아카이브할까요?" → **지금 동기화** 선택
- ADDED 14건을 `openspec/specs/document-ingestion/spec.md`(390줄)로 병합, change를 `openspec/changes/archive/2026-08-02-add-document-ingestion/`으로 이동
- 메인 스펙이 `service-health` 하나에서 **둘**로 늘어남

**발생한 에러:**

- `Exit code 7` — `docker compose exec api python -c "import torch..."` → `service "api" is not running`, `health=000`. 벡터 스토어를 내린 채 검증을 돌린 뒤라 스택이 남아 있지 않았다. `docker compose up -d --wait`로 재기동한 뒤 바인드 마운트 구성에서 9.7을 다시 확인(`/tmp/verify.txt` 업로드 → 목록 조회)

### 그 밖 (창 내)

- Retrobot 자동 실행 6회 — `704ba125`(17:39) · `b57ce982`(18:20) · `64d2c67f`(21:25) · `abf1f969`(13:02) · `ad268012`(15:34) · `5720fb57`(16:07)
- 실작업 세션 `18b09f4b`(15:30~15:50) · `1679c0c5`(15:59~16:07) — [16:07 회고](./2026-08-02_1607_retro.md)에서 다룸
- 서브에이전트 2건 — `agent-abc4a4b5`(Explore) · `agent-a63d8f3c`, 08-01 스펙 리뷰 반영 단계

## KPT 회고

### Keep (좋았던 것)

- **네 번 미룬 섹션 9를 세션의 첫 작업으로 했다.** [13:02](./2026-08-02_1302_retro.md) · [15:34](./2026-08-02_1534_retro.md) · [16:07](./2026-08-02_1607_retro.md) 세 회고 연속으로 Try에 적히고 세 번 미뤄진 항목이다. 게다가 이건 취향 문제가 아니라 CLAUDE.md 불합격 기준 **두 개**("명시된 방법으로 실행 불가", "문서에 있다고 적은 코드가 없음")를 직접 막는 작업이었다. 회고의 Try가 실제로 다음 세션의 순서를 바꾼 첫 사례.
- **"떴다"가 아니라 컨테이너 안에서 요청으로 증명했다.** `docker compose up --wait`만 보고 넘어가지 않고 health → 업로드 → 목록 → 상세 → 삭제를 실제로 돌렸다. 9.7의 문구가 "실행 확인"인데 확인의 정의를 스스로 올려 잡았다.
- **정상 경로만이 아니라 열화 경로를 실측하고 그 결과로 문서를 고쳤다.** 벡터 스토어를 내린 상태에서 "새 업로드는 503, **같은 바이트 재업로드는 200 unchanged**, 조회는 정상"이라는 세 갈래를 확인했다. README에는 "업로드는 503"이라고만 적혀 있었다 — 틀린 건 아니지만 부정확했고, 평가자가 재업로드로 시험하면 문서와 다르게 동작했을 것이다.
- **문서-코드 대조를 눈이 아니라 스크립트로 했고, 그게 실제로 결함을 찾았다.** README 설정 표는 `Settings`를 import해 대조했고, spec 시나리오는 `#### Scenario:` 목록과 테스트 함수 목록을 뽑아 맞췄다. 후자가 미커버 시나리오 1건을 찾아내 테스트가 늘었다(327 passed). 대조가 형식적 체크가 아니었다는 증거다.
- **직전 회고의 "설계 근거 소실" 우려가 대부분 기우였음이 확인됐다.** 지목된 5건 중 4건(캐시 헬스체크 순서·`:ro` 금지·워커 1개와 `asyncio.Lock`·리눅스 UID)은 `ARCHITECTURE.md` 51·66·67행에 이미 살아 있었고, 남은 볼륨 항목을 이번에 바인드 마운트 실체로 정정했다. 근거를 코드 주석이 아니라 문서에 이중으로 둔 구조가 주석 유실을 흡수했다.
- **평가자가 그대로 따라 하면 막혔을 문장을 잡았다.** README의 "`user:` 줄 주석을 푸세요"는 그 줄이 삭제된 뒤라 따라갈 수 없는 지시였다. 실제로 추가할 한 줄을 적어 두었다.
- **arm64 빌드가 깨지는 함정을 주석으로 남겼다.** `--index-url`로 갈아끼우고 싶은 유혹이 큰 자리에서 왜 `--extra-index-url`인지를 적어 뒀다. 다음 사람이 "CPU 전용인데 왜 보조 인덱스지?"로 되돌리는 걸 막는다.

### Problem (나빴던 것)

- **`docker compose exec api`를 스택 상태 확인 없이 쐈다가 `service "api" is not running`으로 실패했다.** 바로 앞 단계에서 벡터 스토어를 내린 채 검증을 돌린 뒤였다. `docker compose ps` 한 줄이면 실패 대신 사실을 먼저 봤을 자리다.
- **커밋 하나에 성격이 다른 변경 다섯 종류가 들어갔다** — Dockerfile 신규 레이어, 의존성 버전 고정, 문서 정정 3건, 테스트 추가, 스펙 동기화·아카이브(+475/-18, 11 파일). 커밋 분할은 [13:02](./2026-08-02_1302_retro.md) 회고부터 네 번째 미이행이다. 특히 "스펙 390줄 신규 + change 디렉터리 이동"은 diff를 읽는 사람 입장에서 나머지를 통째로 가린다.
- **직전 회고 Try 두 건이 그대로 남았다.**
  - `docker-compose.yml` 끝 개행 미복구 (`\ No newline at end of file`)
  - **최상위 `volumes:` 선언이 삭제된 뒤 기존 네임드 볼륨(`officeagent_*`)의 이관·폐기 결정이 여전히 없다.** 현재 compose에 `volumes:` 블록은 없다. 바인드 마운트로 갈아탄 지금 이전 볼륨에 남은 벡터·레지스트리는 참조되지 않는 고아다
- **registry 볼륨을 왜 분리하는지의 근거가 읽히지 않는 곳으로 옮겨 갔다.** "컨테이너를 다시 만들어도 지금 유효한 리비전이 무엇인가가 남아야 한다"는 tasks 9.5에만 있고, `ARCHITECTURE.md`의 영속화 행에는 "각자의 데이터를 갖습니다"까지만 있다. 그 tasks.md는 방금 아카이브로 들어갔다.
- **과제 본체 기준으로 여기까지가 전부 ingestion이다.** RAG 질의응답·**스트리밍(SSE)**·LLM 응답 캐싱·`PROMPT_DESIGN.md`가 통째로 남았다. 평가 비중으로 **72%**다. 하드 마감(08-09)까지 일주일이지만 남은 쪽이 미지의 영역(claude-code-sdk 실호출·스트리밍 계약·유사 질문 캐시)이라는 점이 지금까지와 다르다.

### Try (개선해야 할 것)

- **다음 change 착수 전에 `docker volume ls | grep officeagent`로 남은 네임드 볼륨을 확인하고 이관/삭제를 결정한다. 동시에 `docker-compose.yml` 끝 개행을 복구한다.** 두 항목 모두 세 번째 이월이며, 어느 쪽도 5분이 넘지 않는다. 이월되는 이유는 어려워서가 아니라 "다음에"라고 적기만 해서다 — 다음 세션의 **첫 커밋에 포함**한다.
- **컨테이너를 대상으로 하는 명령(`exec`·`logs`·`cp`) 앞에는 `docker compose ps`를 붙인다.** 이번처럼 직전 단계에서 스택을 건드린 뒤에는 특히.
- **registry 분리 근거 한 줄을 `ARCHITECTURE.md` 영속화 행에 옮긴다.** 아카이브된 tasks.md는 다음 사람이 열지 않는다.
- **다음 change는 "RAG 질의응답 + 스트리밍"으로 잡고, 스펙 단계에서 SSE 계약을 먼저 못 박는다** — 이벤트 종류(토큰·출처·완료·에러), 출처를 언제 흘리는지(첫 토큰 전 vs 후), 클라이언트 중단 시 서버가 무엇을 정리하는지, 캐시 히트일 때도 스트리밍 형태를 유지하는지. CLAUDE.md는 "답변 전체를 모아 한 번에 반환하는 방식만 있으면 감점"이라고 명시하므로, 논스트리밍을 먼저 만들고 나중에 감싸는 순서는 피한다.
- **커밋을 change 단위가 아니라 작업 단위로 자른다.** 이번이라면 ① Dockerfile·의존성 ② 문서 정정 ③ 테스트 추가 ④ 스펙 동기화·아카이브. 전부는 못 지키더라도 **"스펙 동기화·아카이브"만은 분리**한다 — 이건 코드 변경이 0줄인데 diff의 대부분을 차지한다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 (KST) | 세션 | 주요 활동 |
|------|------|----------|
| 08-01 16:35~21:25 | `f2564d3d` + 서브에이전트 2 | 스펙 리뷰 반영, 청킹·파서·ingestion 서비스 — [17:40](./2026-08-01_1740_retro.md) · [18:25](./2026-08-01_1825_retro.md) · [21:25](./2026-08-01_2125_retro.md) 회고 |
| 08-02 12:35~15:14 | `f2564d3d` | API 계층, Chroma 서버 모드, 동시성 검증, 임베딩 선로딩 — [13:02](./2026-08-02_1302_retro.md) · [15:34](./2026-08-02_1534_retro.md) 회고 |
| 08-02 15:30~16:07 | `18b09f4b` · `1679c0c5` | ChromaDB 데이터 미표시 조사, GUI compose 연결 — [16:07](./2026-08-02_1607_retro.md) 회고 |
| 08-02 16:09 | `f2564d3d` | `/opsx:apply` — 섹션 9 착수 (세 회고 연속 미룬 항목) |
| 08-02 16:10~16:13 | `f2564d3d` | `chromadb==1.5.9` 고정, Dockerfile에 CPU torch + 모델 가중치 굽기(+28), `docker compose build api` |
| 08-02 16:13~16:14 | `f2564d3d` | spec Scenario ↔ 테스트 대조 → 미커버 1건 발견, 리비전 보존 테스트 추가(+33) |
| 08-02 16:14~16:16 | `f2564d3d` | `up -d --wait` → 컨테이너 안에서 health·업로드·목록·상세·삭제 실행. 벡터 스토어 정지 상태 3갈래 실측 |
| 08-02 16:16~16:18 | `f2564d3d` | 외부 서비스 없이 308 passed / 19 skipped, 포함 시 327 passed |
| 08-02 16:18 | `f2564d3d` | `docker compose exec api` → `service "api" is not running` (health=000) |
| 08-02 16:19~16:21 | `f2564d3d` | 재기동 후 바인드 마운트 구성에서 9.7 재확인, README·ARCHITECTURE 볼륨/GUI/`user:` 문구 정정 |
| 08-02 16:22~16:24 | `f2564d3d` | `/opsx:archive` → 스펙 동기화 선택 → `openspec/specs/document-ingestion/spec.md` 390줄 생성, change 아카이브. **90/90** |
| 08-02 16:26 | — | `0b63e3f feat: Ingestion 스펙 확정 및 동작 확인` 커밋 (+475/-18) |
