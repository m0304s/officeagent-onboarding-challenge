# Retrobot 회고 — 2026-08-03 17:10 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 2 (작업 1 + Retrobot 훅 1) |
| 총 프롬프트 | 1 |
| 에러 발생 | 4 (작업 세션) + 13 (Retrobot 훅) |

직전 회고(15:52) 이후 새로 들어온 구간만 다룹니다. 목록의 나머지 로그 파일은 이전 회고에
이미 반영되어 있습니다.

## 세션별 활동

### 세션 `4ddf86a5` (15:51 ~ 17:03 KST, 72분) — `add-answer-generation` 2장

**프롬프트:**
1. `/opsx:apply` **"add-answer-generation 에서 2장 작성 진행해주세요."**

인터럽트 0회. 도구 44회 — Bash 13 · Read 11 · PowerShell 9 · Write 5 · Edit 5 · Skill 1.

**결과:** `371350c` (+1184/-8, 5파일) — `core/answers.py`(152) · `core/prompting.py`(331) ·
`tests/test_prompting.py`(490) · `PROMPT_DESIGN.md`(195) · `tasks.md`. **418 → 549 passed**
(신규 131건), `ruff check`·`format --check`·`openspec validate --strict` 통과.

**발생한 에러:**
- `Exit code 49` — `Python was not found; run without arguments to install from the Microsoft Store`
  (호스트 `python` 이 Windows Store alias) → `py -3`(3.15.0b3)로 전환
- `NativeCommandError` 2건 — PowerShell 에서 `docker compose run ... 2>&1 | Select-Object` .
  두 번 다 명령 자체는 돌아 `131 passed` · `549 passed` 를 얻었다
- Edit 실패 1건 — `String to replace not found in file` (`tasks.md` 2.8) → Read 후 재시도

**정지:** 16:00:23 도구 호출 → 16:47:44 결과. **47분**. 그 사이 다른 엔트리가 없다.

### 세션 `cfcd577b` (15:50 ~ 15:58 KST) — Retrobot 훅

직전 회고(`1637006`) 생성. 에러 13건, 권한 거부 11건.

### 세션 `b752f9e1` (~15:52 KST)

15:29 세션의 꼬리. 15:52 회고에 이미 반영됨.

> 참고: 17:04 에 `20a4b397`("3장 LLM 어댑터구현")이 시작됐습니다. 이 훅의 분석 대상 목록
> 밖이라 다음 회고에서 다룹니다.

## KPT 회고

### Keep (좋았던 것)

- **`PROMPT_DESIGN.md` 의 프롬프트 전문을 손으로 옮기지 않고 `build_prompt` 를 실제로 실행해
  받은 문자열로 붙였다.** 옮겨 적는 순간 문서와 코드가 갈리고, `CLAUDE.md` 는 그 갈림을
  감점이 아니라 **불합격**(허위 기재)으로 규정한다. 더 중요한 것은 그 위에 붙인 범위 노트다 —
  "재시도 정책·이벤트 시퀀스·인용 조립은 3~6장의 동작이고 지금 확인 가능한 것은 프롬프트와
  파서까지". 없는 코드를 있다고 적을 자리를 **쓰기 전에** 닫았다. README `/search` 예시가
  일곱 번 이월되게 만든 행동(측정하지 않은 값을 그럴듯하게 적기)의 정확한 반대다.

- **프롬프트와 파서가 갈리는 것을 테스트로 잡았다.** 2.5 는 목록에 없던 단언을 하나 더했다 —
  프롬프트 문자열에서 판정 줄을 정규식으로 뽑아 `parse_answer` 에 먹인다. 이 불일치는 오류를
  내지 않는다. 모델은 시킨 대로 쓰는데 서버가 못 알아듣고, 그 증상은 "판정 줄 없음" 경로로
  조용히 흡수돼 **`INSUFFICIENT` 가 영원히 안 나오는 형태로만** 드러난다. 테스트가 없으면
  관측 자체가 불가능한 결함이었다.

- **`VerdictSplitter` ↔ `parse_answer` 동치를 임의 분할로 단언했다.** 출력 12종 × 1·2·3조각
  모든 분할에서 이어 붙인 본문이 `parse_answer(...).body` 와 같음을 직접 확인한다. 스펙이
  "`answer` 조각을 이어 붙인 것 = `done.answer`" 를 요구하는데, 그 불변식이 **말이 아니라
  실행되는 표**로 남았다. 두 경로가 판정 줄 인식 규칙을 상수 하나(`_VERDICT_LINES`)로
  공유하게 만든 것이 그 단언을 가능하게 한 설계다.

- **설계에 없던 `finish()` 를 구현 중에 찾아 더했다.** 개행 없이 끝나는 출력(판정 줄만 낸
  회차, 짧은 한 줄 답변)이 `feed` 만으로는 버퍼에 갇힌다. 설계 문서를 그대로 코드로 옮기지
  않고 반례를 먼저 찾았다 — 이 change 에서 가장 틀리기 쉽다고 스스로 지목한 부품에서.

- **커밋 전에 전체를 실제로 돌렸다.** `549 passed in 47.19s` (16:00:57) → 커밋 (17:03).
  신규 131건이 기존 418건에 무영향임을 숫자로 확인했다. 직전 회고 Try #3 이 요구한 "재고 나서
  적기" 중 재는 쪽은 이번에도 했다.

- **호스트에 `pytest` 가 없자 README 에 적힌 컨테이너 경로로 갈아탔다.** 부수적으로 문서에
  적힌 테스트 실행 방법이 실제로 돈다는 것이 확인됐다 — 평가자가 1회 수행해 확인하는 바로 그
  경로다.

### Problem (나빴던 것)

- **직전 회고 Try 6건 중 이행 0건.** #1(`CLAUDE.md` 셸 규칙) 미이행 → 셸 혼용이 **여섯 세션
  연속** 재발. #3(`tasks.md` 3장 머리에 기준선) 미이행 — 549 를 재고도 3장 머리에 적지
  않았다(**네 회차째**). #4(`retrobot/SKILL.md` 우회 절차) 미이행 → 샌드박스 벽 18번째 재발.
  #6(회고의 Try 를 다음 세션 첫 프롬프트에) 미이행 — 첫 프롬프트는 "2장 작성 진행해주세요."
  한 줄이었다. 직전 회고가 **"이행된 Try 는 `tasks.md` 에 적혀 있던 것뿐"** 이라고 진단했는데,
  이번 세션에는 `tasks.md` 밖 Try 가 하나도 실리지 않았고 이행률은 정확히 0이 됐다.
  **진단은 맞았고 조치만 없었다.**

- **72분 중 47분(65%)이 도구 호출 하나를 기다린 시간이다.** `cd "C:\...\" && python -c ...`
  형태가 승인 프롬프트를 띄우는 것은 도구 설명에 적힌 알려진 동작이고, 이 세션은 그 형태를
  **11번** 썼다. 그렇게 47분을 기다려 받은 결과가 `Python was not found` 였다 — 대기 비용과
  실패가 겹쳤다. 다음 호출부터는 즉시 돌아왔으므로 승인이 한 번에 풀린 것으로 보이지만,
  그 한 번이 세션의 3분의 2를 먹었다.

- **PowerShell `2>&1` 실패를 5분 간격으로 두 번 냈다.** 16:53 에 `NativeCommandError` 를
  받고도 16:57 에 같은 형태를 다시 썼다. 고쳐지지 않은 이유가 분명하다 — **두 번 다 명령은
  돌았고 원하는 숫자(131·549)를 얻었다.** 실패가 아프지 않았다. 이것이 셸 혼용이 여섯 세션을
  사는 메커니즘이다. 규칙을 문서에 박지 않으면, 결과가 나오는 한 매 세션 다시 발명된다.

- **1,184줄 커밋에 외부 검토 0회.** 그 안에 필수 산출물 `PROMPT_DESIGN.md`(평가 13%)와,
  스스로 "이 change 에서 가장 틀리기 쉬운 로직"이라 적은 `VerdictSplitter` 가 있다. 14:16
  회고가 세운 Codex 적대적 리뷰 관행은 **스펙 단계에만** 붙고 구현 단계에는 한 번도 붙지
  않았다. 스펙 리뷰는 매번 설계를 바꾸는 지적을 냈는데, 정작 그 설계를 코드로 옮기는 단계는
  아무도 안 본다.

- **README `/search` 예시의 손으로 쓴 `0.8734`·`chunk_index: 1` 이 여덟 번째 이월.**
  이번엔 README 를 **열었는데도** 지나쳤다(`sed -n '285,315p' README.md`, 16:51). 테스트 실행
  방법을 찾으러 연 것이라 예시 블록이 시야 밖이었다 — 파일을 여는 것으로는 안 고쳐진다는 것이
  이번에 증명됐다.

- **Retrobot 샌드박스 벽 18번째 재발.** 이 회고를 만드는 동안 Bash `ls`(로그 디렉터리)·
  PowerShell 글롭·Bash `sed` 3건이 거부됐다. 직전 회고가 "포기가 확정된 이상 이제 미완 과제가
  아니라 **상시 비용**" 이라 규정하고 우회 절차를 `SKILL.md` 에 적기로 했는데, 적히지 않아
  이번에도 Grep/Glob 조합을 처음부터 다시 찾았다.

### Try (개선해야 할 것)

회고에만 적힌 Try 는 여덟 번 연속 이행되지 않았고 `tasks.md` 에 적힌 것만 이행됐다.
**따라서 아래 항목은 전부 "어느 파일의 어느 줄에 적을 것인가"까지 지정한다.**

1. **`CLAUDE.md` 에 셸 규칙 네 줄을 다음 커밋 전에 박는다.** 회고에 두 번 적었고 두 번 다
   실패했다 — 다음 세션이 자동으로 읽는 파일은 `CLAUDE.md` 뿐이다.
   - Bash 는 POSIX(Git Bash), PowerShell 은 5.1 — 한 명령에 섞지 않는다
   - Bash 에서 `cd` 접두사 금지 (승인 프롬프트 → 이번에 47분)
   - PowerShell 에서 네이티브 exe 에 `2>&1` 금지 (`NativeCommandError`)
   - 컨테이너 경로 인자에는 `MSYS_NO_PATHCONV=1`

   Problem 1·2·3 을 한 번에 친다.

2. **README `/search` 예시 교체를 `tasks.md` 3장에 태스크로 추가한다** (3.16 등). 여덟 번
   이월된 유일한 이유는 그것이 회고에만 있었기 때문이다. 이월을 끊는 방법은 다른 작업의 합격
   조건으로 만드는 것뿐이다.

3. **`tasks.md` 3장 머리에 `549 passed (2026-08-03 17:03, 371350c)` 한 줄.** 네 회차째
   미이행. 재는 것은 매번 하는데 적는 것만 안 한다 — 3장은 기존 테스트 무영향이 합격 조건인
   장이라 기준선이 없으면 그 조건을 판정할 수 없다.

4. **3장 구현 직후 Codex 적대적 리뷰 1회를 `tasks.md` 3.17 로 못 박는다.** 대상은
   `session.py` 의 수명·반납 불변식(3.6·3.7·3.14) — 죽은 세션 반납과 stderr 미소비는 테스트가
   초록인 채로 통과할 수 있는 종류의 결함이다. 리뷰어에게 **"구현 단계"** 임을 먼저 말한다
   (14:16 회고에서 이걸 안 말해 지적 2건이 헛돌았다).

5. **`retrobot/SKILL.md` 에 "이 환경의 우회 절차" 절을 신설한다.** Windows + 샌드박스에서는
   Bash·PowerShell·python3·node 가 전부 막히므로 **Glob 으로 파일을 찾고 Grep 의 `-o`·
   `head_limit` 으로만 집계한다**. 18회 반복된 탐색 비용을 지침 한 절로 끝낸다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 15:50~15:58 | `cfcd577b` | Retrobot 훅 — 직전 회고 생성 (`1637006`), 권한 거부 11건 |
| 15:51 | `4ddf86a5` | `/opsx:apply` 2장 작성 요청 (프롬프트 1건, 이후 인터럽트 0) |
| 15:52~15:54 | `4ddf86a5` | 기존 코드 정찰 — `core/retrieval.py`·`documents.py`·`services/retrieval.py`·`api/routers/search.py` |
| 15:55~15:57 | `4ddf86a5` | 2.1 `core/answers.py` — `FinishReason`·`Citation`·`Answer` 불변식 |
| 15:57~16:00 | `4ddf86a5` | 2.2~2.4 `core/prompting.py` — `PROMPT_VERSION="qa-ko-1"`, `build_prompt`, `parse_answer`, `VerdictSplitter` |
| 16:00 | `4ddf86a5` | 호스트 `python -c` 스모크 호출 → **47분 정지** |
| 16:47 | `4ddf86a5` | `Python was not found` (exit 49) → `py -3`(3.15.0b3)로 전환, 스모크 성공 |
| 16:49~16:51 | `4ddf86a5` | 2.5~2.7 `tests/test_prompting.py` 490줄 — 분리기↔파서 동치 포함 |
| 16:51 | `4ddf86a5` | 호스트에 `pytest` 없음 → README 의 컨테이너 실행 경로로 전환 |
| 16:52~16:53 | `4ddf86a5` | `docker compose run --build --rm test pytest` → **131 passed in 0.14s** |
| 16:53 | `4ddf86a5` | ruff 검사 — PowerShell `2>&1` `NativeCommandError` (1회차) |
| 16:54~16:57 | `4ddf86a5` | 2.8 — `build_prompt` 를 실제 실행해 프롬프트 전문 채취, `PROMPT_DESIGN.md` 195줄 |
| 16:57~17:00 | `4ddf86a5` | 전체 스위트 — `NativeCommandError` (2회차) → **549 passed in 47.19s** |
| 17:01 | `4ddf86a5` | `tasks.md` 갱신 (Edit 실패 1건 후 재시도), `openspec validate --strict` 통과 |
| 17:03 | — | 커밋 `371350c` "feat: 응답 도메인 세팅" (+1184/-8) |
| 17:04 | `20a4b397` | 3장 LLM 어댑터 구현 착수 (다음 회고 대상) |
