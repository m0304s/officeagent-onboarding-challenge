# Retrobot 회고 — 2026-08-04 11:45 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 2 (이번 창 10:59~11:36) |
| 총 프롬프트 | 1 (슬래시 1) — 작업 세션 기준 |
| 에러 발생 | 10 (작업 세션 3 · Retrobot 훅 세션 7) |
| 커밋 | `a4be966` (19파일 +919/-99) |
| push | **완료** — `origin/add-rrf-algorithm` == `a4be966`, ahead 0 |
| 도구 | Edit 43 · Bash 37 · Read 18 · Write 7 · Skill 1 |
| 검증 | 컨테이너 `772 passed, 2 deselected (72.53s)` · `ruff check .` 통과 · `check_comments.py` src 0건 |
| 진행 | `add-rrf-algorithm-spec` **24/55 → 36/55** |

직전 회고(11:03) 이후의 창입니다. 세션 `a449f215` 는 그 회고를 만든 Retrobot 훅이고,
실제 작업은 `c4df5f7c` 한 세션(10:59~11:35, 36분)입니다.

## 세션별 활동

### 세션 `a449f215` (10:59 ~ 11:06, 7분) — Retrobot 훅

`4d370fc` 커밋 후 자동 실행되어 `2026-08-04_1103_retro.md` 를 만들고 `c0e65ea` 로 커밋했습니다.

**발생한 에러:** 7건 — 전부 승인 거부입니다.

- `command -v python3 jq` (반복), `command -v py`, `ls -lt --time-style=...` 이 모두 `requires approval`

직전 회고가 Try 4번으로 "`retrobot/SKILL.md` 의 첫 지시를 이 호스트 기준으로 고친다"고 적었지만
고쳐지지 않았고, 같은 두 줄에서 다시 막혔습니다.

### 세션 `c4df5f7c` (10:59 ~ 11:35, 36분) — 브랜치 `add-rrf-algorithm`

**프롬프트:**

1. `/opsx:apply add-rrf-algorithm` — "Retriever 계약과 구현 과 검색 서비스 팬아웃 로직 구현해주세요." (10:59)

**발생한 에러:** 3건.

- `openspec status --change add-rrf-algorithm` → `not found. Available changes:` — **2회 연속**
  (`instructions apply` 도 같은 인자로 한 번 더). 실제 이름은 `add-rrf-algorithm-spec` 입니다
- `python - <<'EOF'` → **Exit code 49** (Windows Store 별칭). `CLAUDE.md` 호스트 표에 적힌 함정입니다

**흐름:** `add-rrf-algorithm-spec` 의 5·6장을 구현해 진행률을 **24/55 → 36/55** 로 올렸습니다.

`adapters/retrievers/` 를 새로 만들어 `dense.py`·`lexical.py` 를 `Retriever` 계약 뒤로 넣고,
`registry.py` 가 이름으로 인스턴스를 만듭니다. `services/retrieval.py` 는 `asyncio.gather` 로
활성 retriever 전부에 같은 질의를 겹쳐 던지고(`_fan_out`), 성공한 목록만 `fuse()` 에 넘긴 뒤
재검증→절단 순으로 상위 K를 만듭니다. `config.py` 에는 `RetrieverSettings`(이름·가중치·
후보 깊이·필수 여부) 목록과 `retrieval_rrf_k` 가 붙었고, 기동 시 활성 retriever 0개와
`candidate_depth < top_k 상한` 을 거부합니다. 기본 구성은 `dense`(required) + `lexical`(선택)입니다.

검증은 컨테이너에서 `docker compose run --build --rm test` → **772 passed, 2 deselected
(72.53s)**, `ruff check .` 통과, `check_comments.py` 는 위반 1건이 나와 고친 뒤 **0건**으로
재확인했습니다. 커밋 `a4be966` (11:36) 이후 **push 까지 됐습니다.**

## KPT 회고

### Keep (좋았던 것)

- **push 를 했습니다 — 세 회고 연속 Try 1번이던 항목입니다.** `origin/add-rrf-algorithm` 이
  `a4be966` 로 올라가 ahead 0 입니다. `dfbdb8d`(02:08)에 멈춰 쌓이던 미push 5건이 이번 커밋과
  함께 전부 원격에 있습니다. 168시간 창 판정은 committer date 기준이라 로컬에만 있던 동안은
  평가자가 볼 수 없는 상태였고, 그 위험이 닫혔습니다.
- **깨진 테스트를 "고장"이 아니라 "설계가 바뀐 결과"로 규명하고, 이유를 코드에 남겼습니다.**
  `test_health_answers_while_a_query_is_being_embedded` 는 문서를 올리지 않은 채 질의 임베딩
  지연을 재던 테스트입니다. 이번 리팩터로 질의 임베딩이 밀집 retriever 안으로 들어가
  **대상 집합 확정 뒤**가 되면서, 문서가 없으면 임베딩 자체가 일어나지 않아 재려던 지연이
  발동하지 않습니다. 지연 값을 늘리거나 단언을 느슨하게 하는 대신 문서를 먼저 올리도록 고치고,
  왜 그래야 하는지를 docstring 에 적었습니다.
- **부분 실패 정책을 코드 계약으로 굳혔습니다.** `_dispose_of` 는 필수 retriever 실패는 올리고
  (도메인 예외가 아니면 `StorageUnavailable` 로 바꿔 `503` 보증을 지킴), 선택 retriever 실패는
  경고 로그와 함께 융합 입력에서 **뺍니다 — 빈 목록으로 위장하지 않습니다.** 전부 실패면
  장애로 올립니다. `CancelledError` 는 처분 대상에서 제외해 그대로 전파합니다.
- **세대 혼입을 구조로 막았습니다.** 대상 문서 목록을 한 번 만들어 모든 retriever 에 같은 것을
  넘깁니다(각자 레지스트리를 읽으면 시점이 갈려 융합 결과에 두 세대가 섞임). 절단(`top_k`)은
  재검증 **뒤**에 둡니다(앞에 두면 밀려난 리비전이 자리를 차지한 만큼 결과가 비어 돌아옴).
  둘 다 주석에 "왜"만 한 줄로 붙어 있습니다.
- **설정에 실제 유효성 검사를 넣고 테스트로 덮었습니다.** `test_config.py` 가 113줄 늘었습니다.
  기본값만 검증하면 기동을 통과한 구성에서 큰 `top_k` 요청 하나가 곧바로 깊이를 넘어선다는
  것을 짚고, 기본값이 아니라 **상한**과 비교하도록 했습니다.
- **커밋 전에 주석 규칙 위반 1건을 잡아 0으로 되돌렸습니다.** 통과한 뒤 다시 재서 확인했습니다.

### Problem (나빴던 것)

- **`LLM 응답 캐싱` 은 다섯 회고 연속 없습니다 — 불합격 기준 항목입니다.**
  `services/qa.py` 의 `cache` **0회**, `adapters/cache/` 는 아직 `probe.py` 하나.
  직전 회고 Try **2번**이 "`add-rrf-algorithm-spec` 구현을 여기서 멈추고 남은 시간을 캐싱에 쓴다"
  였고, 이번 36분은 전부 RRF 하이브리드 검색에 갔습니다. 하이브리드 검색은 요구사항 밖 확장이고
  캐싱은 요구사항 3번이자 평가 비중 **18%** 입니다.
- **168시간 창까지 약 3시간 반입니다.** 첫 커밋 `1313b0a`(07-28 15:11) 기준 마감은
  **오늘 15:11 KST**. 남은 19개 태스크를 마저 밀면 캐싱은 시작조차 못 합니다.
- **직전 회고가 Keep 으로 올렸던 change 이름 실수가 그대로 재발했습니다.** 11:03 회고는
  "`openspec list` 로 먼저 확인해 정정했다 — 직전 창에서 두 번 실패했던 지점"이라고 적었는데,
  이번 창에서 `add-rrf-algorithm` 으로 **다시 2회 연속** 실패했습니다. 회고에 Keep 으로 적는 것이
  다음 세션의 행동을 바꾸지 못한다는 증거입니다. 이 사실은 `CLAUDE.md` 에 없습니다.
- **`--build` 없는 컨테이너 실행이 2회입니다.** `docker compose run --rm test` 와
  `docker compose run --rm test ruff check .`. `CLAUDE.md` 검증 절차의 **첫 항목**입니다.
  이번에는 직전 빌드 이후 소스 편집이 없어 결과가 유효했지만, 규칙 이탈 자체가 반복입니다.
- **계획에 적어 둔 실물 기동 검증을 실행하지 않았습니다.** 세션 계획에는
  "`docker compose up -d --build --wait` 후 `./data` 를 비우고 `sample-docs/` 두 건을 올려
  `/search`·`/qa` 를 눈으로 확인 — 기여 retriever 목록까지"가 있었는데, 이 세션의 Bash 명령
  37건에 `docker compose up` 이 **한 번도 없습니다.** 팬아웃·부분 실패·기여 목록은 이번 커밋의
  핵심 변경인데 실물 요청으로는 한 번도 지나가지 않았습니다. 직전 창에서는 했던 검증입니다.
- **`retrieval_cache_size` 가 config 에만 있고 소비처가 없습니다.** `src` 전체에서 선언 1회뿐이고
  `README.md`·`ARCHITECTURE.md`·`docker-compose.yml`·`openspec/` 어디에도 없습니다. 문서에 없으니
  허위 기재는 아니지만, 이번 커밋이 넣은 죽은 손잡이입니다.
- **Windows venv 경로에서 시행착오가 있었습니다.** `./.venv/bin/ruff` 가 실패해 `ls .venv/Scripts`
  로 확인해야 했습니다. `CLAUDE.md` 호스트 표의 Windows 행에 이 경로가 없습니다.
- **열린 change 는 그대로 셋이고**(`add-answer-generation` 86/87 · `add-demo-ui` ·
  `add-rrf-algorithm-spec` 36/55), **외부 검토 0회, 열두 세션 연속**입니다.

### Try (개선해야 할 것)

1. **여기서 `add-rrf-algorithm-spec` 을 멈추고 남은 시간을 캐싱에 씁니다.** 5·6장까지 커밋·push
   됐으니 중단점으로 깨끗합니다. 나머지 19개 태스크는 "미구현 부분은 설계 문서로 대체 가능"에
   기댑니다. 최소 구현 다섯 단계 — 질문 정규화 키 → `adapters/cache/` 저장소 어댑터 →
   `services/qa.py` 조회·기록 → 응답의 캐시 히트 필드 → 문서 변경 시 무효화.
   직전 회고 Try 2번과 같은 문장이며, 이번에는 남은 시간이 3시간 반입니다.
2. **알아낸 사실을 `CLAUDE.md` 에 적습니다 — 회고에만 적으면 다음 세션이 읽지 않습니다.**
   이번 창이 그 증거입니다. 넣을 것 둘: change 이름은 `add-rrf-algorithm-spec` 이고
   `add-rrf-algorithm` 은 브랜치명이라는 것, Windows 의 venv 실행 파일은 `.venv/Scripts/` 라는 것.
3. **마감 전 마지막 검증은 `docker compose up -d --build --wait` 실물 기동 1회로 합니다.**
   이번 창이 계획해 놓고 건너뛴 항목이고, 평가자가 1회 수행하는 것과 같은 경로입니다.
   `./data` 를 비우고 `sample-docs/` 두 건을 올려 `/search` 응답의 기여 retriever 목록까지 봅니다.
4. **`retrieval_cache_size` 를 쓰거나 지웁니다.** 캐싱 구현에서 실제로 쓸 값이면 소비처를 붙이고,
   아니면 이번 커밋에서 들어온 대로 되돌립니다.
5. **`retrobot/SKILL.md` 의 첫 단계를 내장 도구(`Glob`·`Grep`) 집계로 바꿉니다.**
   직전 회고 Try 4번 미이행이고, 이번 훅 세션의 에러 7건이 전부 그 한 줄에서 나왔습니다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 (KST) | 세션 | 주요 활동 |
|------|------|----------|
| 10:59~11:06 | `a449f215` | Retrobot 훅 — 11:03 회고 생성, 승인 거부 7건 우회, `c0e65ea` 커밋 |
| 10:59 | `c4df5f7c` | `/opsx:apply add-rrf-algorithm` — "Retriever 계약과 구현, 검색 서비스 팬아웃" |
| 11:00 | `c4df5f7c` | `openspec status/instructions --change add-rrf-algorithm` → `not found` **2회 연속**, `-spec` 으로 정정 |
| 11:01~11:08 | `c4df5f7c` | 아티팩트·구현 정독 — `chroma.py` 질의 절, `ScoredChunk` 사용처, 하네스·conftest (Read 18) |
| 11:09 | `c4df5f7c` | `python - <<'EOF'` → **Exit code 49** (Windows Store 별칭) |
| 11:10~11:18 | `c4df5f7c` | `adapters/retrievers/` 신설 — `dense.py`·`lexical.py`·`registry.py`, `Retriever` 프로토콜 |
| 11:19~11:24 | `c4df5f7c` | `services/retrieval.py` 팬아웃 — `asyncio.gather`, `_dispose_of` 부분 실패 정책, 재검증 후 절단 |
| 11:25~11:27 | `c4df5f7c` | `config.py` — `RetrieverSettings`(가중치·깊이·필수), `retrieval_rrf_k`, 기동 유효성 2건 |
| 11:28 | `c4df5f7c` | `./.venv/bin/ruff` 실패 → `.venv/Scripts` 확인, 컨테이너 `ruff check .` 로 전환 |
| 11:29~11:31 | `c4df5f7c` | `--build` 전체 스위트 → 재실행(`--build` 누락)에서 **1 failed, 771 passed** |
| 11:31~11:33 | `c4df5f7c` | 실패 원인 규명 — 질의 임베딩이 대상 집합 확정 뒤로 이동, 테스트가 문서를 먼저 올리도록 수정 |
| 11:33 | `c4df5f7c` | `--build` 재실행 → **772 passed, 2 deselected (72.53s)** |
| 11:34 | `c4df5f7c` | `check_comments.py` 위반 1건 → 수정 후 **0건**, `ruff check .` 통과 |
| 11:35 | `c4df5f7c` | `tasks.md` 5·6장 체크 (24/55 → **36/55**) |
| 11:36 | — | `a4be966` 커밋 (19파일 +919/-99) **및 push** — `origin/add-rrf-algorithm` ahead 0 |
