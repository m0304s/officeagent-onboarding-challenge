# Retrobot 회고 — 2026-08-06 00:20 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 9 |
| 총 프롬프트 | 24 |
| 총 도구 호출 | 486 |
| 에러 발생 | 25 (그중 20건 = 80% 가 Retrobot 자신) |
| 창 내 커밋 | 16 (add-cross-encoder 13 + fix-pdf-parser 3) |

분석 창: 2026-08-05 00:20 ~ 2026-08-06 00:20 KST.

## 세션별 활동

### 세션 `34e9f10c` (08-05 00:25 ~ 01:21) — 크로스인코더 제안·착수

Bash 62 · Edit 32 · Read 25 · Write 11 · AskUserQuestion 1 · Monitor 1 · ToolSearch 1.

**프롬프트:**
1. `/opsx:propose` — `add-cross-encoder` 4아티팩트 생성
2. `/opsx:apply` (00:51)
3. `/opsx:apply` (00:58)
4. `bge-reranker-v2-m3가 된다면, 해당 모델로 교체해주세요.` — 모델 선택을 사용자가 직접 뒤집음

**발생한 에러:** 1건
- `Blocked: sleep 90 followed by: tail -6 ...` — 백그라운드 태스크 진행 확인용 `sleep` + `tail` 복합 명령이 거부. `Monitor` 로 전환해 해결.

### 세션 `408546e5` (08-05 00:51 ~ 00:57) — Retrobot

Bash 19 · Edit 4 · Read 2 · Skill 1 · Write 2. 에러 4건.

### 세션 `ac8941c4` (08-05 01:22 ~ 01:42) — 크로스인코더 어댑터 구현

Bash 45 · Edit 36 · Read 10. 프롬프트 1 (`/opsx:apply`). 에러 1건.
- `ruff format` 미적용 — `src/app/adapters/lexical/sqlite.py:65`. 포맷 게이트가 잡아냈고 즉시 수정.

### 세션 `518ba2d1` (08-05 01:21 ~ 01:28) — Retrobot

Bash 21 · Write 2. 에러 5건 (창 내 최다).

### 세션 `f20abf8a` (08-05 01:39 ~ 02:13) — Retrobot

Bash 29 · Grep 16 · Write 1. 에러 3건. `/tmp` 쓰기가 막히자 `Grep` 16회로 우회.

### 세션 `f2a13c41` (08-05 08:01 ~ 08:19) — Retrobot

Bash 14 · Glob 1 · Write 2. 에러 4건.

### 세션 `c69d384c` (08-05 23:09 ~ 08-06 00:07) — PDF 파서 교체 설계

AskUserQuestion 6 · Bash 34 · Edit 30 · WebFetch 5 · WebSearch 1 · Write 4 · Skill 1 · ToolSearch 1.

**프롬프트:** 10건. `/opsx:propose` 3회 → 사용자 중단 → `/opsx:update` 3회 → `brainstorming` 스킬 → 다시 `/opsx:update`.

**발생한 에러:** 2건 — 둘 다 사용자의 도구 사용 거부(`Edit` 반려). 즉 런타임 실패가 아니라 **사용자가 방향을 두 번 되돌린 것**이다.

이 세션이 창 안에서 유일하게 **웹을 뒤진** 세션이다 (WebFetch 5 + WebSearch 1). pymupdf4llm 의 버전 요구(`pymupdf4llm 1.28.0` ↔ `pymupdf==1.28.0` 정확 일치)를 실제 패키지 메타에서 확인했고, 그 근거가 `pyproject.toml` 주석으로 그대로 남았다.

### 세션 `e1686297` (08-06 00:07 ~ 00:15) — Retrobot

Bash 25 · Edit 1 · Write 2. 에러 4건.

### 세션 `f13eeb41` (08-06 00:07 ~ 00:18) — PDF 픽스처 구현

Bash 21 · Edit 9 · Read 7 · Write 1. 프롬프트 3 (`/opsx:apply` 2회 + 사용자 중단 1회).

**발생한 에러:** 1건
- `E501 Line too long (101 > 100)` — `tests/pdf_fixtures.py:95`. 같은 실행에서 주석 게이트(`112개 파일, 위반 0건`)는 통과.

## KPT 회고

### Keep (좋았던 것)

- **설계를 사용자가 두 번 되돌렸고, 되돌린 자리가 전부 구현 이전이었다.** `34e9f10c` 에서 리랭커 모델을 `bge-reranker-v2-m3` 로 교체하라는 지시가 어댑터 구현(`ba4e9d6`) **직전**에 들어왔고, `c69d384c` 에서 `Edit` 반려 2건이 `/opsx:propose` → `/opsx:update` 전환을 강제했다. 코드가 아니라 아티팩트를 고쳤으므로 되돌림 비용이 0이다. `add-response-cache` 회차에서 확립한 "구현 전에 뒤집는다" 패턴이 두 회차 연속 재현됐다.

- **PDF 픽스처를 바이너리 대신 생성 코드로 만들었다.** `tests/pdf_fixtures.py` 는 테스트용 PDF 를 그 자리에서 그린다. 이유가 파일 docstring 에 있다 — 바이너리를 커밋하면 "텍스트 레이어가 없다"는 성질을 **파일 이름으로만 주장**하게 되고 diff 로 확인할 수 없다. 한글 글리프 깨짐을 막으려고 내장 CJK 폰트(`korea`)를 명시한 것까지 픽스처 탓의 실패를 선제 차단한 판단이다.

- **의존성 고정 근거가 코드 옆에 남았다.** `pymupdf==1.28.0` 정확 고정의 이유가 "추출 결과가 저장물이고 저장물이 `index_signature` 의 축이라, 버전이 흔들리면 같은 설정에서 같은 PDF 가 다른 청크가 된다"로 주석에 적혔다. 범위 지정(`>=1.24`)에서 정확 고정으로 옮긴 결정이 나중에 "왜 이렇게 빡빡하지"로 되물어지지 않는다.

- **포맷·린트·주석 3게이트가 이번에도 전부 자기 몫을 잡았다.** `ruff format`(`sqlite.py:65`), `E501`(`pdf_fixtures.py:95`), 주석 게이트(112파일 0건). 사람이 눈으로 볼 필요가 없었던 지적이 3건이다.

### Problem (나빴던 것)

- **`main` 이 24시간 넘게 멈춰 있고, 브랜치가 10개로 쌓였다.** `main` 은 여전히 `9ba55cd`(08-04 23:48)다. `add-cross-encoder` 는 `main` 대비 **15커밋 / 63파일 / +5,748 -80** 으로 벌어져 있고, 08-05 하루치 작업(리랭킹 설정 → API 노출 → 크로스인코더 실행 → llm-judge 스펙 → 크로스인코더 테스트 → LLM 평가, 6개 실질 커밋)이 통째로 그 안에 있다. 그런데 **오늘 새로 판 `fix-pdf-parser` 는 `add-cross-encoder` 가 아니라 낡은 `main` 에서 갈라졌다.** 즉 PDF 파서 교체 작업은 하루치 리랭킹 코드를 못 본 채 진행 중이고, 두 브랜치는 나중에 5,748줄과 811줄을 들고 만나게 된다. 병합을 미룰수록 이 숫자만 커진다.

- **회고 기록 자체가 브랜치에 갈라져 하루가 사라졌다.** `retros/` 에 `2026-08-05_*` 파일이 **한 건도 없다.** 실제로는 6건(`0054`·`0122`·`1005`·`1432`·`1455`·`1656`)이 만들어졌지만 전부 `add-cross-encoder` 브랜치에만 있다. 그래서 이 브랜치의 `TIMELINE.md` 는 **08-04 23:36 → 08-06 00:12 로 하루를 건너뛴다.** 직전 회고(`2026-08-06_0012`)가 이미 "`main` 을 여는 사람에게 08-05 는 없는 하루"라고 적었는데 **8분 뒤인 지금도 그대로다** — 신호는 있었고 아무도 받지 않았다.

- **Retrobot 훅 두 실행이 분석만 끝내고 죽었다.** `f20abf8a`(01:39~02:13)·`f2a13c41`(08:01~08:19) 은 `Grep` 16회·`Bash` 29회까지 돌고 회고 파일을 못 남겼다. 직전 회고가 원인을 `Connection closed mid-response`(분석 직후 정확히 15분 뒤)로 지목했고, `TIMELINE.md` 한 행이 평균 3,500자·전체 32,000토큰이라 큰 `Write` 한 번이 임계에 닿는다고 적었다. **그런데 그 회고 자신이 3,500자짜리 행을 또 붙였다.** 진단과 행동이 같은 파일 안에서 어긋난다.

- **08-05 10:00~17:53 구간의 작업 로그가 이 호스트에 없다.** 그 시간대 커밋 6건은 존재하는데 `~/.claude/projects/` 아래 대응하는 `*.jsonl` 이 없다 — 다른 호스트(Windows)에서 작업한 것으로 보인다. 이 회고는 **그 반나절에 대해 커밋 메시지 이상을 말할 수 없다.** 호스트가 둘인 프로젝트에서 회고는 구조적으로 반쪽이 된다.

- **커밋 메시지가 커밋 내용과 어긋난다.** `da889dc "feat: pymupdf4llm 동작 확인"` 이 담은 것은 `tasks.md` 체크 8줄과 `tests/pdf_fixtures.py` 94줄이고, 이 픽스처는 `pymupdf` 만 쓴다. 정작 **`pymupdf4llm` 을 의존성에 넣는 `pyproject.toml` 변경은 커밋되지 않고 워킹트리에 남아 있다.** 커밋을 체크아웃해서는 "동작 확인"을 재현할 수 없다. `openspec/changes/add-multiturn-qa/` 도 통째로 미추적 상태다.

- **Retrobot 이 24시간 에러의 80%(20/25)를 만들었다 — 23회차.** 5개 Retrobot 세션이 **매번 같은 두 벽**에 부딪혔다: ① `command -v python3 jq` 가 multi-operation 판정으로 거부(진단하라는 줄이 진단을 막는다) ② `/tmp` 쓰기 거부. 5회 전부 재생산됐다. 직전 회고(`2026-08-06_0012`)가 22회차로 같은 진단을 적었는데 **`retrobot/SKILL.md` 는 여전히 안 고쳐졌다** — 회고가 원인을 정확히 짚고도 원인 파일을 건드리지 않으니 다음 회차가 똑같이 걸린다.

- **직전 회고의 우회로 결론이 틀렸다.** `2026-08-06_0012` 는 "`python3 -c '...'` 단독 호출은 통하고, 뒤에 `>`·파이프·`;` 를 붙이는 순간 거부된다"고 확정했다. **이번 회차에 `cd ... && python3 -c '...' 2>&1 | head -200` 이 그대로 통했다.** `&&` 도 파이프도 문제가 아니었다. 잘못된 제약을 물려주면 다음 회차가 쓸 수 있는 도구를 스스로 봉인한다.

### Try (개선해야 할 것)

- **`retrobot/SKILL.md` 를 이번 회차에 고친다.** 세 줄이다. ① `command -v python3 jq` → `command -v python3` 와 `command -v jq` **두 명령으로 분리** ② "`/tmp` 아래에 쓴다" → "**스크립트 파일을 만들지 말고 `python3 -c '...'` 인라인으로 집계한다**" ③ `2026-08-06_0012` 의 "파이프·`;` 금지" 서술 삭제. 근거는 이번 회차의 실측 오류 메시지다 — `Claude Code may only write to files in the allowed working directories for this session: '<repo>'`. **`/tmp` 는 이 호스트에서 영구히 못 쓴다.** 조건부가 아니라 확정 사실이므로 그렇게 적는다.

- **`add-cross-encoder` 를 `main` 에 먼저 병합하고 `fix-pdf-parser` 를 리베이스한다.** 지금 `fix-pdf-parser` 는 3커밋·811줄이라 리베이스 비용이 가장 싸다. 이 순서를 뒤집으면 5,748줄 대 811줄의 충돌을 나중에 치른다. 병합 전에 `docker compose run --build --rm test` 로 두 브랜치가 합쳐진 상태를 한 번 돌린다.

- **브랜치를 팔 때 기준을 명시적으로 고른다.** `git checkout -b <new> main` 이 기본이 되어 있는데, 직전 작업 위에 쌓아야 할 때가 있다. 새 change 를 시작할 때 "이 작업은 직전 브랜치의 코드를 필요로 하는가"를 한 번 묻고, 필요하면 `main` 이 아니라 그 브랜치에서 판다. PDF 파서는 리랭킹과 독립이라 이번엔 사고가 안 났지만, 판단을 한 게 아니라 기본값을 따랐을 뿐이다.

- **커밋 전에 `git status` 를 읽고, 메시지가 주장하는 것이 그 커밋 안에 있는지 확인한다.** `pyproject.toml` 의 `pymupdf4llm` 추가는 `da889dc` 에 들어갔어야 한다. "동작 확인"을 주장하는 커밋은 **체크아웃만으로 그 확인이 재현돼야** 한다. 미추적 `openspec/changes/add-multiturn-qa/` 도 같은 회차에 정리한다.

- **`TIMELINE.md` 한 행을 두 문장으로 제한한다.** 직전 회고가 임계 도달을 진단하고도 3,500자 행을 붙였다. 상세는 회고 파일이 들고 있으므로 인덱스 행은 **"무엇이 닫혔고 무엇이 남았는가" 두 문장이면 충분**하다. 이 회차부터 실제로 그렇게 쓴다.

- **`retros/TIMELINE.md` 병합 규칙을 정한다.** 브랜치별로 회고가 갈라지는 것은 막을 수 없으니(각 브랜치에서 커밋 훅이 돈다), 병합 시 `TIMELINE.md` 는 **날짜순으로 합치는 것이 정답**임을 `CLAUDE.md` 에 한 줄 적는다. 지금처럼 하루가 통째로 사라진 것을 아무도 눈치채지 못하는 상태를 반복하지 않는다.

- **호스트가 둘이라는 사실을 회고 머리말에 적는다.** 로그가 없는 구간을 "활동 없음"으로 읽으면 회고가 거짓말을 한다. 창 안에 커밋은 있는데 대응 세션 로그가 없으면 **"다른 호스트 작업, 로그 미확보"로 명시**한다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 08-05 00:25 | `34e9f10c` | `/opsx:propose add-cross-encoder` — 4아티팩트 생성 |
| 08-05 00:51 | `408546e5` | Retrobot (에러 4) · `0145a27` 크로스 인코더 스펙 분석 |
| 08-05 00:58 | `34e9f10c` | `/opsx:apply` 착수 |
| 08-05 01:11 | `34e9f10c` | 사용자 지시로 리랭커 모델을 `bge-reranker-v2-m3` 로 교체 |
| 08-05 01:21 | `34e9f10c` / `518ba2d1` | `ba4e9d6` 크로스인코더 어댑터 구현 → Retrobot (에러 5) |
| 08-05 01:22 | `ac8941c4` | `/opsx:apply` — Edit 36회, `ruff format` 위반 1건 수정 |
| 08-05 01:39 | `f20abf8a` | `8000c6b` 검색 파이프라인 배선 → Retrobot (`Grep` 16회 우회) |
| 08-05 08:01 | `f2a13c41` | `7dd258e` 리랭킹 설정 추가 → Retrobot (에러 4) |
| 08-05 10:00~17:53 | — | `173779b`·`816d04d`·`4904c19`·`cface34`·`431b5d2` — **다른 호스트, 로그 미확보** |
| 08-05 23:09 | `c69d384c` | PDF 파서 교체 설계 시작 — WebFetch 5·WebSearch 1 로 pymupdf4llm 버전 제약 확인 |
| 08-05 23:24 | `c69d384c` | 사용자가 `Edit` 반려 → `/opsx:propose` 에서 `/opsx:update` 로 전환 |
| 08-05 23:31 | `c69d384c` | `brainstorming` 스킬로 설계 재정리 |
| 08-06 00:07 | `c69d384c` / `e1686297` | `88dc607` pymupdf4llm 모델 스펙 정의(524줄) → Retrobot (에러 4) |
| 08-06 00:08 | `f13eeb41` | `/opsx:apply` — `tests/pdf_fixtures.py` 작성, `E501` 1건 수정 |
| 08-06 00:17 | `f13eeb41` | `da889dc` "pymupdf4llm 동작 확인" — 단 `pyproject.toml` 변경은 미커밋 |
