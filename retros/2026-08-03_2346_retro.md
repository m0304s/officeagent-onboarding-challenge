# Retrobot 회고 — 2026-08-03 23:46 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 4 (+ 서브에이전트 3) |
| 총 프롬프트 | 9 |
| 에러 발생 | 14 (그중 **10건이 Retrobot 자신**) |

분석 창은 직전 회고(22:47) 이후인 **22:40 ~ 23:46**입니다.
두 change가 **동시에** 돌았습니다 — `add-answer-generation` 마감(세션 A)과
`add-demo-ui` 제안(세션 B).

## 세션별 활동

### 세션 `8ccc760d` (22:40 ~ 23:42) — `add-answer-generation` 6·7·8장 마감

**프롬프트:**
1. [22:45] 스트리밍은 어떤 방식 쓰고 있지?
2. [23:06] `/opsx:apply add-answer-generation` — 6장 남은 것, 그리고 7·8장 작업 진행해주세요

**한 일:**

- 22:41~22:45 5장 뒷정리 — `1 failed, 639 passed` → **640 passed**, 컨테이너 린트 통과.
  `--build`를 붙인 검증만 씀.
- **6장**(설정·기동 경로) — `tests/test_config.py` +97, `tests/test_boot.py` +56, `49 passed`.
- **7장**(문서 정합) — `ARCHITECTURE.md` +227, `README.md` +154, `PROMPT_DESIGN.md` +24,
  `openspec/config.yaml` +17. 스택 표에 남아 있던 마지막 `claude-code-sdk` 문장을 23:31에 제거.
- **7.3에서 결함을 회수** — "실물 CLI 테스트를 포함해 돌리는 명령을 README에 적어라"는
  태스크인데 **그 층의 코드가 없었습니다.** design 결정 14가 다섯 번째 겹으로 약속해 두고
  태스크가 없던 자리입니다. 없는 것을 적으면 허위 기재(불합격 기준)라 문서를 약하게 쓰는 대신
  `tests/test_llm_live.py` 101줄을 만들고 마커로 기본 수집에서 뺐습니다 →
  **`660 passed, 2 deselected`**, 실물 층만 돌리면 `2 passed, 660 deselected`.
- 실물 확인 — 인증 있는 상태에서 `/qa` SSE 스트림 캡처, 무근거 질의 `no_evidence` 캡처,
  자격증명을 뺀 상태로 재기동해 **`/qa`만 실패하고 `/health`·`/documents`·`/search`는 정상**임을
  관측한 뒤 복원. 검색 crowding을 보려고 문서를 3건으로 늘렸다가 sample-docs 2건으로 되돌림.
- `PROMPT_DESIGN.md`에 인용된 프롬프트를 실제 `build_prompt` 출력과 diff하고,
  그 문서가 적어 둔 실행 명령을 실제로 돌려 `131 passed` 확인.
- 23:37 `src 위반: 1` → 23:39 원인 확인 → 23:41 **`src 위반 0`**.
- 커밋 `dcd6bd7` (+719/-44, 13파일). 640 → **660 passed**, 진행 73 → **86/87**.

**발생한 에러:** 2건 — `.secrets/codex/` 부재와 `openspec/project.md` 부재를 한 명령에서 확인
(`config.yaml`이 맞는 경로), `docker compose up` 직후 `/health`가 `000`(exit 56)이라 기다렸다 재시도.

### 세션 `724d2218` (22:47 ~ 23:44) — `add-demo-ui` 제안

**프롬프트:**
1. [22:48] `/opsx:propose` — 질의-응답 결과를 확인할 프론트엔드 데모 페이지 (브랜드·UI 가이드 동봉)
2. [22:57] POST + SSE라서 EventSource 못쓰는 이유는?
3. [23:00] `/opsx:apply add-demo-ui` — 전체 작업 진행해주세요
4. [23:04] 자, 그렇다면 다른 세션에서 change를 완료하고 나서 프론트엔드 데모 UI를 붙여보겠습니다
5. [23:44] 현재 change 완성했습니다(SSE). 이어서 프론트엔드 데모 UI 완성해주세요

**한 일:**

- `superpowers:brainstorming` → `AskUserQuestion` 3문(**실행 방법**·화면 범위·프레임워크).
  첫 질문에 "평가자가 실행 불가하면 불합격 기준에 걸립니다"를 붙여 물음.
- proposal · `specs/demo-console/spec.md` · design · tasks 4아티팩트 작성, `validate` 통과.
- 23:00 서브에이전트 3개 — 백엔드 계약 literal 추출 / 리포 관습 조사 / 구현 설계.
- **23:04 사용자가 중단.** 설계 에이전트 `TaskStop` ×2 (23:05), 산출물 폐기.
- 23:44 재개, `.gitignore`·`.dockerignore`에 demo-ui 항목 추가 (**미커밋**).

**에러:** 0건.

### 세션 `9d06bafd` (22:46 ~ 22:52) · `d0c01226` (23:43 ~ ) — Retrobot 두 회차

에러 10건. 내용은 아래 Problem 4.

## KPT 회고

### Keep (좋았던 것)

- **적을 것이 없을 때 문서를 약하게 쓰는 대신 코드를 만들었습니다.** 7.3은 README에
  실물 CLI 테스트 실행법을 적으라는 태스크였고, 그 층이 없다는 것을 그 자리에서 발견했습니다.
  선택지는 (가) 태스크를 넘긴다 (나) "지원 예정"이라 적는다 (다) 만든다 — 셋 중 (나)만이
  이 리포에서 **감점이 아니라 불합격**(허위 기재)입니다. 101줄을 만들고 마커로 기본 수집에서
  빼서, 한 줄 명령의 무구독 실행(가산점 조건)을 깨지 않고 닫았습니다.
- **직전 회고가 지적한 줄을 같은 회차에 닫았습니다.** 22:47 회고는 "610→0을 만든 커밋의
  바로 다음 커밋이 `src/`를 다시 열었다(`sse.py:146`)"를 적었고, 이번 세션이 23:37에 `1`을
  재고 23:41에 `0`으로 만들었습니다. `--build`를 붙인 검증은 이번에도 전부 준수 — **세 세션 연속**.
- **문서의 주장을 관측으로 바꿨습니다.** "자격증명이 없으면 `/qa`만 실패한다"를 문장으로
  두지 않고 자격증명 없이 재기동해 네 엔드포인트를 각각 찔러 확인했고, `PROMPT_DESIGN.md`가
  인용한 프롬프트는 `build_prompt` 실제 출력과 diff했으며, 그 문서에 적힌 실행 명령도
  실제로 돌려 `131 passed`를 봤습니다. `0.8734`를 손으로 써서 열 회차를 끈 행동의 정반대입니다.
- **change 충돌을 사람이 알아채고 멈췄습니다.** 23:04에 사용자가 demo-ui 구현을 중단하고
  "다른 세션에서 change를 완료한 뒤"로 미뤘습니다. 그 판단이 옳았습니다 — 40분 뒤
  세션 A가 `sse.py`를 고치고 API 계약 문서를 227줄 고쳤고, demo-ui가 그때 코드를 짰다면
  갈라진 계약 위에 짰을 것입니다. 순서 판단이 에이전트가 아니라 사람에게서 나왔습니다.

### Problem (나빴던 것)

1. **`ruff format --check .` 실패 파일 5 → 6개.** 다섯 번째 재현이고 매 회차 악화 중입니다
   (17:41 → 1, 20:31 → 2, 22:23 → 2, 22:47 → 5, 지금 **6**). 이번 세션은 `ruff check .`(린트)를
   여섯 번 돌렸지만 `format --check`는 **한 번도** 돌리지 않았습니다. 새로 들어온
   `test_llm_live.py`·`test_config.py`·`test_boot.py`가 후보입니다. 이 명령은
   **`README.md:319`가 평가자에게 시키는 명령**이고, 평가자는 1회만 수행합니다.
   호스트 실측: `6 files would be reformatted, 146 files already formatted`.
2. **`tests/` 주석 위반 422 → 432.** 규칙 도입 후 세 번째 증가입니다. 이번에 들어온 테스트
   파일 3개가 다시 규칙 밖에서 태어났습니다. `CLAUDE.md:74`는 "판정은
   `python3 scripts/check_comments.py`가 한다"고만 적고 **범위가 `src/`라는 문장이 없어서**,
   그 파일에 적힌 대로 인자 없이 돌리면 지금도 exit 1(432건)입니다. 문서에 적힌 명령이
   실패하는 두 번째 자리입니다.
3. **외부 검토 0회 — 여섯 세션 연속.** 미검토 누적 4,321 + 2,543 + 1,032 + 719줄.
   다만 이번엔 성격이 다릅니다: 23:36에 `4B.1`이 유일한 미완 태스크임을 확인하고
   **"이번 요청 범위 밖"이라고 명시**한 뒤 86/87로 닫았습니다. 처음으로 보류에 이유가 붙었지만,
   그 이유가 다음 세션에 전달되는 자리는 회고뿐입니다.
4. **Retrobot 샌드박스 벽 23번째 — 이번엔 원인이 `SKILL.md` 자신입니다.** 전체 에러 14건 중
   10건(71%)이 Retrobot 두 회차이고, 둘이 **완전히 같은 순서로** 실패했습니다.
   - 22:23 회고가 우회로를 확정했습니다: "`/tmp`에 파일을 만드는 것이 막히고 `python3 -`
     stdin은 통한다." 그런데 `SKILL.md` 본문은 여전히 "파싱 스크립트가 필요하면 `/tmp` 아래에
     쓴다"고 지시하고, 런타임이 읽는 것은 회고가 아니라 **커밋된 `SKILL.md`**입니다.
     두 회차 모두 `/tmp/retro_agg.py` Write로 시작해 거부됐습니다.
   - 더 나쁜 것은, 20:32에 이 문제를 줄이려고 `SKILL.md`에 넣은 진단 한 줄
     **`command -v python3 jq`가 그 자체로 거부**된다는 것입니다("multiple operations").
     개선책이 새 실패 지점이 됐고, 두 회차 모두 그 줄에서 먼저 걸렸습니다.
     **`which python3 jq`는 한 번에 통합니다**(이번 회차 실측 — `/usr/bin/python3`·`/usr/bin/jq`).
5. **`add-demo-ui` 제안이 낡은 컨텍스트 위에서 쓰였습니다(미수).** 22:49에 읽은
   `openspec/config.yaml`의 스택 표는 LLM SDK를 **`claude-code-sdk` (pip)**로,
   근거를 "파이썬 네이티브라 subprocess 래핑 없이"로 적고 있었습니다. 그 문장을 지운 것은
   42분 뒤 **다른 세션**(23:31)이고, 아티팩트 4건은 그 사이(22:51~22:56)에 쓰였습니다.
   결과적으로 새지는 않았지만(`grep` 0건), 이 리포에서 문서-코드 불일치는 불합격 기준입니다.
6. **미커밋 잔여물.** `.gitignore`·`.dockerignore` 수정(+7)과 `openspec/changes/add-demo-ui/`
   4아티팩트가 작업 트리에만 있습니다. 23:43 커밋 직후에 생겼습니다.

### Try (개선해야 할 것)

1. **지금 `ruff format .`을 돌리고 `README.md:319`를 통과시켜라.** 다섯 회차 이월이고
   호스트에서 6개가 재져 있습니다. 커밋 전 검증 묶음에 `ruff check`와 나란히
   `ruff format --check .`를 넣어라 — 지금은 린트만 돌아서 이 숫자가 안 보입니다.
2. **`CLAUDE.md:74`에 판정 범위를 적어라.** "판정은 `python3 scripts/check_comments.py src/`가
   한다"로 명령 자체를 고치거나, `tests/`를 언제 닫을지를 태스크로 만들어라.
   지금은 그 파일에 적힌 명령이 실패하는데 그것이 정상인지 아닌지가 어디에도 없습니다.
3. **`retrobot/SKILL.md`를 고쳐라 — 회고에 적는 것으로는 24번째가 옵니다.** 세 곳:
   (가) "`/tmp` 아래에 쓴다"를 지우고 "**스크립트 파일을 만들지 마라**"로,
   (나) `command -v python3 jq` → **`which python3 jq`**,
   (다) 통하는 형태를 본문에 박아라 — `python3 -c '...'`(셸은 **작은따옴표**, 파이썬 코드
   안에서는 **큰따옴표만**, f-string 백슬래시 금지) 또는 `python3 -` stdin 파이프.
   이번 회차가 그 형태로 전부 통과했습니다.
4. **`4B.1`을 demo-ui보다 먼저 붙일지 지금 결정하고 `tasks.md`에 적어라.** 미검토가 8,600줄을
   넘었고 그 안에 좀비 회수·세션 반납 불변식처럼 **테스트가 초록인 채로 통과할 수 있는**
   결함이 있습니다. 보류라면 보류를 `tasks.md`에 쓰라 — 회고에만 쓰면 다음 세션이 못 봅니다.
5. **`add-demo-ui` 착수 전 `openspec/config.yaml`과 `README.md`를 다시 읽어라.**
   22:49에 읽은 판은 이미 낡았습니다. 아티팩트가 백엔드 계약을 인용하고 있다면
   23:43 커밋 기준으로 대조하라.
6. **`.gitignore`·`.dockerignore`와 demo-ui 아티팩트를 커밋하라.** 하루 1회 이상 커밋 규칙이
   있고, 지금 상태는 다음 세션이 "이게 왜 여기 있지"로 시작하게 만듭니다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 22:40 | `8ccc760d` | 5장 뒷정리 — `1 failed, 639 passed` → **640 passed**, 컨테이너 린트 통과 |
| 22:46 | `9d06bafd` | Retrobot 회차 — `/tmp` Write·`command -v` 거부 7건 |
| 22:47 | `724d2218` | `/opsx:propose add-demo-ui` — brainstorming → 실행 방법·범위·프레임워크 3문 |
| 22:49 | `724d2218` | `openspec/config.yaml` 읽음 — 스택 표가 아직 `claude-code-sdk` |
| 22:51 | `724d2218` | proposal · spec · design · tasks 4아티팩트 (~22:56), `validate` 통과 |
| 22:57 | `724d2218` | 사용자 질문 — POST + SSE라서 EventSource를 못 쓰는 이유 |
| 23:00 | `724d2218` | 서브에이전트 3 fan-out — 백엔드 계약 / 리포 관습 / 구현 설계 |
| 23:04 | `724d2218` | **사용자가 중단** — "다른 세션에서 change 완료 후 붙이겠다", `TaskStop` ×2 |
| 23:06 | `8ccc760d` | `/opsx:apply add-answer-generation` — 6장 잔여 + 7·8장 |
| 23:09 | `8ccc760d` | 6장 설정·기동 테스트 — `49 passed` |
| 23:10 | `8ccc760d` | 7장 — `ARCHITECTURE.md` 227줄 정합, 낡은 단언 색출 |
| 23:19 | `8ccc760d` | **`660 passed, 2 deselected`** — 실물 CLI 층 신설(`test_llm_live.py` 101줄) |
| 23:20 | `8ccc760d` | 실물 `/qa` SSE 스트림 캡처, 검색 crowding 확인 후 sample-docs 2건으로 복원 |
| 23:24 | `8ccc760d` | 자격증명 제거 후 재기동 — `/qa`만 실패, 나머지 엔드포인트 정상 확인 |
| 23:31 | `8ccc760d` | `config.yaml`에서 마지막 `claude-code-sdk` 참조 제거 |
| 23:32 | `8ccc760d` | `PROMPT_DESIGN.md` 인용 프롬프트를 실제 출력과 diff, 문서 명령 실행 → `131 passed` |
| 23:37 | `8ccc760d` | `src 위반: 1`(`sse.py` `_pump`) 발견 |
| 23:41 | `8ccc760d` | **`src 위반 0`**, `660 passed` 재확인 |
| 23:43 | `8ccc760d` | 커밋 `dcd6bd7` — feat: SSE 로직 테스트 (+719/-44, 13파일), 진행 **86/87** |
| 23:43 | `d0c01226` | Retrobot 회차 — 같은 자리에서 같은 거부 3건 |
| 23:44 | `724d2218` | 재개 — `.gitignore`·`.dockerignore`에 demo-ui 항목 (미커밋) |
