# Retrobot 회고 — 2026-08-04 01:18 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 3 (이번 창 00:30~01:18) / 17 (최근 24시간, 서브에이전트 4 포함) |
| 총 프롬프트 | 2 (이번 창의 사용자 프롬프트) |
| 에러 발생 | 7 (이번 창) — 그중 **7건 전부가 Retrobot 두 회차** |
| 커밋 | `ff7a5ad` (+1,065 / 8파일) — 스펙만, 코드 0줄 |
| 미커밋 | `openspec/changes/add-multiturn-qa/` (untracked) |

## 세션별 활동

### 세션 `cc79ede2` (00:30 ~ 00:37)

Retrobot 회고 생성(`2026-08-04_0030_retro.md`, 커밋 `65305d9`).

**발생한 에러:** 5건

- `command -v python3 jq` — "multiple operations" 승인 거부
- `/tmp/retro_agg.py` · `/tmp/retro_files.txt` Write 권한 거부 (2건)
- `demo-ui/src/**/*.test.*` no matches — 그 시점 브랜치에 `demo-ui`가 없었음

### 세션 `58b22a1f` (00:31 ~ 00:51, 20분)

**프롬프트:**
1. `git checkout add-answer-generation`

이 한 줄에서 `add-multiturn-qa` change 제안 5개 아티팩트가 나왔다. 코드를 먼저 읽고
(`services/qa.py` · `core/prompting.py` · `api/queries.py` · `adapters/protocols.py`,
`config.py`·`main.py`의 캐시·와이어링 grep) 스펙을 썼다.

- `proposal.md`
- `specs/conversation-session/spec.md` · `specs/query-rewriting/spec.md` · `specs/answer-generation/spec.md`
- `design.md` · `tasks.md`
- `openspec validate --strict` 통과

`AskUserQuestion` 1회 — "`/qa`는 SSE 전용인데 '스트리밍 응답 범위 밖'을 어떻게 읽을까"를
스펙 확정 **전에** 사람에게 물었다.

**발생한 에러:** 0건

### 세션 `2d412d02` (00:51 ~ 01:12, 21분)

**프롬프트:**
1. `/opsx:propose add-rrf-algorithm-spec` — "하이브리드 검색 단계를 넣는다. BM25를 고려하고,
   인덱싱 및 캐싱 단계까지 고려한다"

- `proposal.md` (72줄)
- `specs/rank-fusion/spec.md` (137) · `specs/lexical-index/spec.md` (179) ·
  `specs/retrieval/spec.md` (316) · `specs/document-ingestion/spec.md` (116)
- `design.md` (161) · `tasks.md` (82)
- `openspec validate --strict` 통과 → `ff7a5ad` 01:15 커밋

여기서도 순서가 같다 — `services/retrieval.py` · `core/retrieval.py` · `config.py` ·
`chroma.py` · `routes/search.py` · 기존 `openspec/specs/retrieval/spec.md`와
`document-ingestion/spec.md`의 해당 절을 읽은 뒤 델타를 썼다.

**발생한 에러:** 0건

### 세션 `현재` (01:15 ~)

Retrobot 회고 생성.

**발생한 에러:** 2건

- `command -v python3 jq` — 승인 거부 (직전 회고 Try #1이 지목한 바로 그 줄)
- `/tmp/retrobot_agg.py` Write 거부

## KPT 회고

### Keep (좋았던 것)

- **두 change 제안 41분, 에러 0건, 둘 다 `validate --strict` 통과.** 아티팩트 12개
  (스펙 델타 7개 포함)를 쓰면서 셸 에러도 재실행도 없었다. 스펙 단계의 속도가 이 정도로
  안정된 것은 이번이 처음이다.

- **낡은 컨텍스트 위에서 스펙을 쓰지 않았다.** 23:46 회고가 지적한 실패
  (`add-demo-ui` proposal이 42분 전에 지워진 `claude-code-sdk` 문장 위에서 쓰인 것)가
  이번엔 재발하지 않았다. 두 세션 모두 **코드를 먼저 읽고 그다음 스펙**이라는 같은 순서를
  밟았고, `2d412d02`는 기존 `openspec/specs/` 본문의 해당 줄 범위(`sed -n '143,265p'`)까지
  열어 델타의 앵커를 실제 문장에 맞췄다. 스펙이 코드보다 뒤처지는 것(8/3 14:16 회고의
  `ARCHITECTURE.md` 4시간 지연)의 반대 방향이다.

- **모호한 계약을 스펙 확정 전에 사람에게 넘겼다.** `58b22a1f`의 `AskUserQuestion`은
  "멀티턴이 기존 SSE 이벤트 어휘 4종을 건드리는가"를 물었다. 이 질문을 안 하고 썼다면
  `sources`/`answer`/`done`/`error` 순서 계약(5장에서 `queries.py`로 import 강제한 것)이
  스펙 문서 한 줄로 조용히 흔들렸을 자리다.

### Problem (나빴던 것)

- **열려 있는 change가 넷이 됐다.** `add-answer-generation`(86/87) ·
  `add-demo-ui`(48/55) · `add-multiturn-qa`(0/N) · `add-rrf-algorithm-spec`(0/N).
  23:46 회고가 "두 change가 겹친 첫 회차"를 문제로 적었고, 그로부터 90분 만에 넷이다.
  더 나쁜 것은 **닫는 쪽에 손이 가지 않았다는 것** — `add-answer-generation`은 1태스크
  남았는데 이번 창에서 건드리지 않았고, 그 대신 새 change 둘이 열렸다.

- **`add-multiturn-qa` 20분치가 커밋되지 않았다.** `git status`에 untracked로 남아 있다.
  `CLAUDE.md`가 명시한 168시간 창 판정은 `git log`의 committer date 기준이고, Retrobot도
  커밋 시점의 작업 로그로 회고를 만든다. 지금 상태로는 이 20분이 어느 쪽에도 안 잡힌다.
  같은 창에서 `add-rrf-algorithm-spec`은 커밋됐으므로 "잊었다"가 아니라 **change 하나를
  끝내고 다음으로 넘어갈 때 커밋 경계를 안 그은 것**이다.

- **이번 창의 산출물이 전부 스펙이고 코드는 0줄이다.** +1,065줄이 전부 `openspec/` 아래다.
  평가 비중은 RAG 파이프라인 23% · BE 설계 18% · LLM 활용 18% · 캐싱 18%인데, 이번에 쓴
  두 스펙(멀티턴 · 하이브리드 검색+RRF)은 **둘 다 요구사항에 없는 확장**이다. 미구현은
  설계 문서로 대체 가능하다는 규정이 있으므로 무의미하진 않지만, 캐싱(18%)은 요구사항
  본문에 있고 아직 change조차 없다.

- **`ruff format --check .` 6 files — 일곱 번째 이월.** 이번 두 세션 모두 실행 0회.
  실측(호스트, `.venv/bin/ruff`):

  ```
  6 files would be reformatted, 149 files already formatted
  ```

  `README.md:319`가 평가자에게 시키는 명령이고 평가자는 1회만 수행한다. 직전 회고 Try #2가
  "`ruff format .` 한 번이면 닫힌다"고 적었는데 그 한 번이 또 안 돌았다. 이번 창의 작업이
  `.md`뿐이라 파이썬 게이트가 통째로 건너뛰어진 것 — 8/4 00:30 회고가 프런트엔드 작업에
  대해 적은 것과 **같은 모양**이다.

- **`tests/` 주석 위반 432건 그대로.** `python3 scripts/check_comments.py`를 인자 없이
  돌리면 지금도 exit 1(`86개 파일, 위반 432건`). `src`만 주면 0건이다.
  `CLAUDE.md:74`는 인자 없는 형태를 판정 기준으로 적어 두었으므로, **문서에 적힌 판정
  명령이 실패한다**. 22:23 이후 네 회차째 같은 자리다.

- **직전 회고 Try 이행 0/5.** (#6 서브에이전트 `sleep`은 이번 창에 해당 없음)
  - #1 `SKILL.md` 3곳 수정 → 미이행. `SKILL.md:25`는 여전히 "`/tmp` 아래에 쓴다",
    `:35`는 여전히 `command -v python3 jq`, `:54`는 heredoc을 **금지 목록에** 두고 있다.
  - #2 커밋 전 검증 묶음 스크립트 → 미이행 (위 6 files가 증거)
  - #3 `demo-ui/src/api/sse.ts` 테스트 → 미이행 (`demo-ui/src`에 `*.test.*` 0건)
  - #4 `4B.1` 결정 → 미이행. `tasks.md:148`은 여전히 `[ ]`이고 보류 표기도 없다.
  - #5 남은 7태스크의 검증 주체 명시 → 미이행

- **외부 검토(`4B.1`) 0회 — 여덟 세션 연속.** 미검토 누적에 이번 1,065줄이 더해졌다.
  다만 성격을 구분해 둘 필요는 있다 — 이번 것은 스펙이고, 미검토가 아픈 쪽은 여전히
  3·4장 구현 코드(좀비 회수 · stderr 미소비 · 반납 불변식)다.

- **Retrobot 샌드박스 벽 25번째 — 직전 회고가 "25번째를 막을 마지막 기회"라고 쓴 그 회차.**
  이번 창 에러 7건이 **전부** Retrobot 두 회차이고, 둘 다 같은 두 줄에서 시작해 같이 막혔다:
  `command -v python3 jq`(복합 명령 가드) → `/tmp` Write(권한 거부). 런타임이 읽는 파일은
  회고가 아니라 `SKILL.md`이고, 그 파일은 25회차째 안 고쳐졌다.
  **이번에 통한 형태(실측):** `python3 -c '…'` 를 **작은따옴표 한 겹**으로 감싸고
  f-string과 `{` 직후 따옴표를 피하면 통과한다. 큰 스크립트도 이 형태로 한 번에 들어간다.

### Try (개선해야 할 것)

1. **`add-multiturn-qa`를 지금 커밋하라.** 20분치 아티팩트 5개가 untracked다.
   `git add openspec/changes/add-multiturn-qa && git commit -m "docs: 멀티턴 QA 스펙 정의"`
   — 168시간 창 판정이 committer date 기준이므로 이건 미루면 사라지는 종류다.

2. **`retrobot/SKILL.md`를 이 커밋에 같이 넣어라.** 회고에 26번째를 적지 않는 유일한 방법이다.
   - `:25` "파싱 스크립트가 필요하면 `/tmp` 아래에 쓴다" → **"스크립트 파일을 만들지 마라.
     `/tmp` 쓰기는 이 샌드박스에서 차단된다"**
   - `:35` `command -v python3 jq` → **`which python3 jq`** (복합 명령 가드에 안 걸림)
   - `:54` 금지 목록에서 heredoc을 빼고, 통하는 형태를 본문에 박아라 —
     **`python3 -c '…'` 작은따옴표 한 겹, f-string·`{"` 회피.** 이번 회차의 모든 집계가
     이 형태로 통과했다.

3. **`ruff format .` 를 지금 한 번 돌려라.** 일곱 회차 이월, 6 files, 명령 하나.
   그다음 Try #2의 검증 묶음(`ruff check` · `ruff format --check .` ·
   `check_comments.py src` · `pytest`)을 `scripts/verify.sh` 하나로 묶고 `README.md:319`가
   그것을 가리키게 하라. **회고에 여덟 번째로 적기보다 지금 4줄짜리 파일을 만드는 쪽이 싸다.**

4. **`CLAUDE.md:74`의 판정 범위를 `src/`로 명시하라.** 지금 문서는 인자 없는 실행을
   판정 기준으로 적어 두었는데 그 형태는 exit 1이다. `tests/` 432건을 정리할 생각이 없다면
   문서를 실제 기준에 맞추는 쪽이 맞고, 정리할 생각이라면 그것을 태스크로 세워라.
   **둘 중 하나를 고르지 않는 상태가 네 회차째다.**

5. **열린 change 넷의 순서를 `openspec/` 밖 한 곳에 적어라.** 무엇을 먼저 닫는지가
   지금 어디에도 없다. 최소한 `add-answer-generation`의 남은 1태스크가 무엇이고 왜
   안 닫혔는지는 `tasks.md` 머리에 적혀야 한다. 넷이 동시에 열린 상태에서 새 change를
   또 여는 것이 다음 세션의 기본값이 된다.

6. **캐싱 change를 세워라.** 평가 비중 18%이고 `CLAUDE.md` 핵심 요구사항 3번이며
   불합격 기준("캐시 DB 미구현")에도 걸려 있는데, 열린 change 넷 중 어느 것도 이걸
   다루지 않는다. `add-rrf-algorithm-spec`이 "인덱싱 및 캐싱 단계까지 고려"라고
   쓰였지만 그건 인덱스 캐시이지 **LLM 응답 캐시가 아니다.**

7. **`4B.1`을 `[~] 보류 — 사유`로 마킹하든 붙이든 이번에 끝내라.** 6회차째 `[ ]`이고
   보류 판단은 여전히 회고에만 있다. 직전 회고 Try #4를 글자 그대로 반복한다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 00:30 | `cc79ede2` | Retrobot 회고 생성 → `65305d9`. 에러 5건(모두 샌드박스 벽) |
| 00:31 | `58b22a1f` | `git checkout add-answer-generation` — 브랜치 복귀 후 `add-multiturn-qa` 착수 |
| 00:38 | `58b22a1f` | `AskUserQuestion` — 멀티턴이 기존 SSE 이벤트 어휘를 건드리는가 |
| 00:40~00:48 | `58b22a1f` | proposal · 델타 스펙 3 · design · tasks 작성, `validate --strict` 통과 |
| 00:51 | `2d412d02` | `/opsx:propose add-rrf-algorithm-spec` — BM25 + RRF 하이브리드 검색 |
| 00:55~00:57 | `2d412d02` | 기존 코드·스펙 정독(`retrieval` 2겹 · `chroma.py` · `search.py` · 기존 델타 앵커) |
| 00:58~01:12 | `2d412d02` | 아티팩트 7개 1,065줄, `validate --strict` 통과 |
| 01:15 | — | `ff7a5ad` 커밋 (rrf 스펙만) — `add-multiturn-qa`는 untracked로 남음 |
| 01:15 | `현재` | Retrobot 회고 생성. 에러 2건 — 25번째 샌드박스 벽 |
