# Retrobot 회고 — 2026-08-06 13:10 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 5 (구현 3 · Retrobot 훅 2) |
| 총 프롬프트 | 33 (사람) / 578 (user 엔트리 전체) |
| 도구 호출 | Bash 314 · Edit 111 · Write 37 · Read 17 · Grep·Glob 0 |
| 에러 발생 | 19 (이 중 9건 = 47% 가 Retrobot 자신) |
| 커밋 | 1건 (`9eb2976 feat: pymupdf4llm 구현`, 25파일 `+938/-92`) |
| 테스트 | 974 → 979 → **983 passed, 23 deselected** |

## 세션별 활동

### 세션 `c69d384c` (08-05 23:09 ~ 08-06 00:07)

`add-pymupdf4llm-parser` 제안·설계. 프롬프트 7건.

1. `/opsx:propose` — 기존 PyMuPDF 를 pymupdf4llm 으로, **어댑터만 갈아끼우는 방식**으로
2. `/opsx:propose` — txt 가 아니라 `to_markdown()` 으로 마크다운 + 레이아웃 추출
3. `/superpowers:brainstorming` — 두 파서를 어댑터 교체로 두는 설계 검토
4. `/opsx:update` — 버전 `0.3.4` 고정
5. `/opsx:update` — **정정, `1.28.0` 으로 고정**

### 세션 `f13eeb41` (08-06 00:07 ~ 00:49)

1~2장 구현. 프롬프트 6건.

1. `/opsx:apply 1장`, `/opsx:apply 2장`
2. `/superpowers:brainstorming` — PDF 에 이미지가 있을 때의 처리
3. `ocr_language=…` 파라미터 문의
4. **`https://pymupdf.readthedocs.io/…/pymupdf4llm/api.html` 링크를 주며 `to_markdown` 파라미터를 직접 확인시킴**

**발생한 에러:**
- `ruff` Exit code 1 — `E501 Line too long (101 > 100) tests/pdf_fixtures.py:95` (주석 검사는 112개 파일 위반 0건 통과)

### 세션 `8f673272` (08-06 10:43 ~ 13:05) — 이번 창의 본체

프롬프트 18건, Bash 177회, Edit 66회. 3~8장 구현 + 아카이브.

| 시각 | 프롬프트 |
|---|---|
| 10:44 | `/opsx:update` — **텍스트 감지에는 RapidOCR 만 사용. 경계 상자마다 Tesseract 로 인식** |
| 10:53 | `…/최민석_카카오뱅크 통장사본.pdf` 로 테스트 |
| 11:03 | 이 파일 맞아? |
| 11:05 | **하이브리드 OCR 을 하지 않나요? 왜 `to_markdown()` 하면 OCR 된 내용까지 추출된 영역이 없나요** |
| 11:08 | `/opsx:apply 2장 및 3장` |
| 11:19 | `/opsx:update` — OCR 통합 진행 |
| 11:30 | `/opsx:apply 4장 OCR 인식` |
| 11:48 | 내장 OCR 을 진행하고, pymupdf4llm 의 경우에만. pymupdf 는 OCR X |
| 12:06 | `/opsx:update` — 텍스트+이미지는 텍스트 레이아웃만. **OCR 은 추후 change 로** |
| **12:09** | **OCR 모두 제거하겠습니다. `pymupdf4llm.to_markdown()` 만 해주세요** |
| 12:16 | `/opsx:apply` **3장** (재작업) |
| 12:24 | `…/KDBC_2023_논문-5.pdf` 로 결과 확인 |
| 12:27 / 12:38 | `/opsx:apply` **4장**, **4장 및 5장** (재작업) |
| 12:45 | `/opsx:apply 6장 재색인` |
| 12:51 | `/opsx:apply 7장` + 도커로 컨테이너 기동 |
| 12:57 | 프론트엔드 UI 도 켜주세요 |
| 13:01 | `/opsx:apply 8장` + archive |

**발생한 에러:** 3건
- `ModuleNotFoundError: No module named 'tests'` — 호스트에서 픽스처를 직접 돌리려다 실패
- `2 failed` / `1 failed` — 아래 TDD 항목 참조 (의도된 RED)

### 세션 `e1686297` (00:07~00:15) · `e76cada6` (00:17~00:23)

Retrobot 훅 자체 실행 2회. 에러 4 + 5 = 9건.

## KPT 회고

### Keep (좋았던 것)

- **RED 를 네 번 통과시키고 GREEN 으로 닫았습니다.** `test_extension_selects_the_format_and_the_parser[…PdfParser]` → `[…PdfMarkdownParser]` → `test_a_previous_document_does_not_change_the_next_extraction[markdown]` → `test_the_extraction_mode_reaches_the_cache_through_the_app_wiring`. 각각 먼저 빨갛게 만든 뒤 구현이 따라갔고, 테스트 수가 974 → 979 → 983 으로 **파서를 갈아끼우는 변경에서 오히려 늘었습니다.** 어댑터 교체가 계약을 지켰다는 증거가 숫자로 남습니다.
- **어댑터 경계를 린트로 못박았습니다.** `src/app/core/.ruff.toml` 의 `banned-api` 에 `pymupdf4llm` 을 더해, PDF 파싱이 `core/` 로 새는 것을 기존 `pymupdf` 와 같은 방식으로 막습니다. 규칙을 문서가 아니라 도구가 지킵니다.
- **버전 핀이 이번엔 커밋에 들어갔습니다.** `pyproject.toml` 의 `pymupdf==1.28.0` · `pymupdf4llm==1.28.0` 과 "pymupdf4llm 1.28.0 이 pymupdf==1.28.0 을 정확히 요구하므로 두 값이 함께 움직인다"는 근거 주석이 `9eb2976` 에 함께 있습니다. 08-06 00:20 회고가 지적한 "`da889dc` 는 핀이 미커밋이라 체크아웃으로 재현되지 않는다"가 **닫혔습니다.**
- **추측 대신 상류 문서를 폈습니다.** 00:35 에 `to_markdown` 파라미터를 공식 API 문서 링크로 확인했고, 12:09 의 OCR 철회는 그 확인 위에서 나온 결정입니다. 되돌린 것 자체는 비용이지만, 되돌린 근거는 추측이 아니었습니다.
- **아카이브까지 한 세션에서 닫았습니다.** `openspec/changes/add-pymupdf4llm-parser` → `archive/2026-08-06-add-pymupdf4llm-parser`, 메인 스펙 `document-ingestion/spec.md` `+138`. 8장 실물 확인(도커 기동 + UI)까지 밟고 나서 옮겼습니다.
- **이번 Retrobot 실행은 금지 항목을 밟지 않았습니다.** `/tmp` Write 가 거부된 직후 heredoc 으로 우회하지 않고 `Grep`·`Glob`·`head`/`tail` 조합(SKILL.md 가 명시한 폴백 경로)으로 전량 집계했습니다. 08-06 00:12 회고가 "금지 항목인 heredoc 으로 우회하고서야 통했다"고 적은 실패를 이번엔 반복하지 않았습니다.

### Problem (나빴던 것)

- **세션의 61% 가 코드 0줄을 남기고 되돌아갔습니다.** 10:44 "RapidOCR 만 사용하십시오"부터 12:09 "OCR 모두 제거하겠습니다"까지 86분 / 총 142분. 그 사이에 `/opsx:update` 2회 · `/opsx:apply 4장 OCR 인식` 1회가 실제로 실행됐고, 지금 `src/` 에 남은 OCR 흔적은 **"OCR 을 수행하지 않는다"는 기존 에러 메시지 3줄이 전부**입니다. 되돌림의 대가로 3장은 11:08 과 12:16 에, 4장은 11:30 과 12:27·12:38 에 **두 번씩 다시 밟았습니다.**
- **되돌림의 신호는 11:05 에 이미 나왔는데 64분을 더 갔습니다.** "왜 `to_markdown()` 하면 OCR 된 내용까지 추출된 영역이 없나요" — 이 질문이 곧 "이 라이브러리 경로엔 OCR 이 없다"는 관측이었습니다. 그런데 그 뒤로 11:19 "OCR 통합", 11:30 "4장 OCR 인식", 11:48 "내장 OCR" 로 세 번 더 밀었습니다. 관측과 계획이 어긋났을 때 **계획을 먼저 검증하지 않고 구현으로 밀었습니다.**
- **`fix-pdf-parser` 와 `add-cross-encoder` 의 격차가 더 벌어졌습니다.** 둘 다 `main` 에서 갈라져 있고 지금 **15 커밋 대 5 커밋**, 각각 `main` 대비 `+5,748/-80`(63파일)과 `+1,844/-48`(28파일). `main` 은 여전히 `9ba55cd` 에서 멈춰 있습니다. 08-06 00:20 회고가 "병합·리베이스는 지금이 가장 싸다"고 적은 시점보다 이번 창에서 **`+1,033` 만큼 더 비싸졌습니다.**
- **08-05 회고 6편이 이 브랜치 타임라인에 없습니다.** `retros/` 는 `2026-08-04_2336` 다음이 `2026-08-06_0012` 로, 크로스인코더 하루(`816d04d` 14:31 · `4904c19` 14:49 · `cface34` 16:51 · `431b5d2` 17:53)가 통째로 빕니다. 그 회고들은 `add-cross-encoder` 에만 있어, `main` 을 여는 사람에게 08-05 는 **없는 하루**입니다. 세 회차 연속 이월.
- **`add-multiturn-qa` 가 61시간째 untracked 입니다.** 08-04 00:48 에 마지막으로 손댄 `design.md`·`proposal.md`·`tasks.md`·`specs/` 가 여전히 `git status` 의 `??`. **열 번째 계수.**
- **`ruff format --check` 16건이 그대로입니다.** 13 → 16 으로 늘어난 뒤 이번 창에서 줄지 않았습니다(189 파일 통과, 16 파일 재포맷 필요). `ruff check` 는 통과하므로 CI 를 깨지 않고, 그래서 계속 남습니다.
- **`retrobot/SKILL.md` 가 08-03 `afa9794` 이후 3일째 무수정입니다.** 24 회차 넘게 같은 항목이 회고에 적혔는데 스킬 본문은 바뀌지 않았습니다. 그 결과 이번 실행도 `command -v python3 jq`(다중 연산 거부) → `command -v python3`(승인 필요) → `/tmp` Write(거부) 를 **순서대로 다 밟은 뒤에야** 폴백 경로로 갔습니다. 00:20 회고가 실측으로 정정한 "막히는 건 워킹 디렉토리 밖 쓰기뿐"이 문서에 반영되지 않아, 알아낸 사실이 다음 실행에 전달되지 않습니다.
- **에러 19건 중 9건(47%)이 Retrobot 자신입니다.** 24 회차 연속으로 이 비율이 회고에 오릅니다.
- **외부 적대적 검토 0회.** 열여섯 세션 연속. 파서 백엔드를 통째로 바꾸고 `+1,844` 를 쌓았는데 다른 눈이 한 번도 보지 않았습니다.

### Try (개선해야 할 것)

- **`/opsx:apply` 로 구현에 들어가기 전에 "라이브러리가 이걸 하는가"를 한 번 확인합니다.** 00:35 에 `to_markdown` API 문서를 편 그 행동을 11:05 의 질문 직후에 했다면 86분이 아니라 10분에 끝났습니다. 구체적으로 — **변경 계획에 외부 라이브러리의 능력을 전제한 항목이 있으면, `tasks.md` 의 해당 장 첫 줄을 "상류 문서·시그니처로 그 능력을 확인한다"로 둡니다.**
- **`main` 을 먼저 정리한 뒤 다음 change 를 엽니다.** `add-cross-encoder`(15커밋) → `main` 머지, 그 다음 `fix-pdf-parser` 리베이스. 지금 두 브랜치가 만나는 파일은 검색·인덱싱 경로라 늦출수록 충돌 면적이 커집니다. 이번 창에서만 `+1,033` 늘었습니다.
- **08-05 회고 6편을 `main` 으로 가져옵니다.** `add-cross-encoder` 머지에 자동으로 딸려오므로, 위 항목 하나로 함께 닫힙니다.
- **`add-multiturn-qa` 를 이번 커밋에 넣거나 지웁니다.** 열 번 세는 동안 아무 일도 일어나지 않았다는 것은 "나중에"가 답이 아니라는 뜻입니다.
- **`retrobot/SKILL.md` 의 "집계 도구가 없거나 막힌 환경" 절을 이번 실측으로 고칩니다.** macOS 호스트 항목을 신설하고 — *`/tmp` 쓰기는 거부된다. `command -v` 는 승인이 필요하다. `Grep`(`output_mode: count`) + `head -c`/`tail -c` + `grep -o` 조합이 전량 집계에 충분하다* — 를 적습니다. 이번 실행이 그 조합만으로 578 엔트리·5 세션을 다 셌으므로 근거가 있습니다. **이 항목이 닫히기 전까지 매 회차 같은 3단계 거부를 반복합니다.**
- **`ruff format` 을 한 번에 돌리고 별도 커밋으로 분리합니다.** 16건이 다음 기능 diff 에 섞이면 리뷰가 어려워집니다. 세 회차 이월 중.
- **파서 교체 diff 를 외부 검토에 겁니다.** `codex:rescue` 든 `/code-review` 든, `+1,844` 는 혼자 판단하기에 큰 표면입니다. 특히 `PDF_EXTRACTION_VERSION` 과 `index_signature` 의 결합 — 백엔드를 바꿔도 재색인이 안 도는 결함은 08-05 23:13 에 한 번 발견된 종류입니다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 08-05 23:09 | `c69d384c` | `/opsx:propose` — 어댑터 교체 방식으로 pymupdf4llm 도입 |
| 23:5x | `c69d384c` | 버전 핀 `0.3.4` → **`1.28.0` 정정** |
| 08-06 00:07 | — | `88dc607 docs: pymupdf4llm 모델 스펙 정의` |
| 00:07 | `f13eeb41` | 1~2장 구현, 이미지 PDF 브레인스토밍 |
| 00:17 | — | `da889dc feat: pymupdf4llm 동작 확인` |
| 00:35 | `f13eeb41` | **공식 API 문서로 `to_markdown` 파라미터 확인** |
| 10:44 | `8f673272` | **RapidOCR + Tesseract 지시** — 여기서 되돌려질 86분 시작 |
| 11:05 | `8f673272` | "왜 OCR 된 내용까지 추출된 영역이 없나요" — 신호는 여기 |
| 11:19~11:48 | `8f673272` | OCR 통합 · 4장 OCR 인식 · 내장 OCR (전량 폐기됨) |
| **12:09** | `8f673272` | **OCR 전면 철회, `to_markdown()` 만** |
| 12:16~12:38 | `8f673272` | 3·4·5장 **재작업** |
| 12:45~12:57 | `8f673272` | 6장 재색인 · 7장 · 도커 기동 + UI 실물 확인 |
| 13:01 | `8f673272` | 8장 + 아카이브 |
| 13:05 | — | `9eb2976 feat: pymupdf4llm 구현` — 983 passed |
