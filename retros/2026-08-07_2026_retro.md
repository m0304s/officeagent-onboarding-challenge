# Retrobot 회고 — 2026-08-07 20:26 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 10 (실작업 3 · 서브에이전트 3 · 회고 4) |
| 총 프롬프트 | 18 (사용자 실작업 11 · 서브에이전트 지시 3 · 회고 4) |
| 에러 발생 | 27 (**회고 도구 자신이 21건, 78%**) |
| 커밋 | 5 (`df8f0ce` `1fcf833` `4b13644` `a419fa7` `34da339`) |
| 테스트 | **1,154건 전부 통과** (기본 1,121 + 마커 33) |

분석 창은 2026-08-06 20:26 ~ 08-07 20:26 KST입니다.

## 세션별 활동

### 세션 `f4deea06` (08-06 21:05 ~ 23:30)

`fix-retrieval-cache-defects` 마무리 구간. Bash 253 · Edit 106 · Write 7로 이 창에서 가장 무거운
세션인데 **사용자 프롬프트가 0건**입니다 — 직전 회고가 다룬 세션의 꼬리라 지시 없이 이어서 돈
구간입니다.

**발생한 에러:** 3건

- `cd: no such file or directory: openspec/specs` — 아카이빙 전이라 아직 없던 디렉터리
- `jq: parse error: Invalid numeric literal at line 1, column 5` (동일/유사 질문 캐시 확인 스크립트)
- `AssertionError` (인라인 python 검증)

### 세션 `82dfb17a` (08-06 23:31 ~ 23:57)

**프롬프트:**
1. `현재 add-cross-encoder 브랜치 및 main 브랜치에 충돌이 존재합니다. 목표 : main 브랜치에 add-cross-encoder 스펙 및 코드 병합`
2. (중단)

Bash 80 · Read 19 · Edit 14. 병합 후 `df8f0ce`로 `add-cross-encoder`를 아카이브하고
`add-multiturn-qa` 7개 아티팩트(+623줄)를 함께 커밋했습니다.

**발생한 에러:** 1건 — 아카이브 전 `openspec/specs/` 경로 조회 실패(글롭 미매치)

### 세션 `b9bfeeec` (08-07 18:47 ~ 20:25) — 이 창의 중심

**프롬프트:**
1. `현재 전체 소스코드를 분석하고, 문서-코드 불일치 되는 부분 확인 해주세요. 이때 PRD.md 파일 기반으로 과제 평가를 진행해주세요.`
2. `수정 진행해주세요`
3. `나머지 테스트도 모두 진행해주세요`
4. `수정 진행해주세요`
5. (중단)
6. `docker compose test 전체 진행해주세요`
7. `작업 진행 중인가?`
8. `끝나면 결과를 정리해주세요`
9. `작업 진행 중인가?`

Bash 42 · Edit 34 · Read 18 · **Agent 3**. 감사 대상을 문서 3종으로 쪼개 서브에이전트 3개를
동시에 붙였습니다.

- `agent-a3216c47` — README.md의 모든 사실 주장 대조 (Bash 38, 에러 0)
- `agent-a6132837` — ARCHITECTURE.md 675줄 대조 (Bash 45, 에러 0)
- `agent-a6c4db29` — PROMPT_DESIGN.md 341줄 + tests/README.md 대조 (Bash 42, 에러 1)

**발생한 에러:** 2건 (본세션 1 + 서브에이전트 1)

- `AttributeError: type object 'DocumentFormat' has no attribute 'TEXT'. Did you mean: 'TXT'?`
- 인라인 검증 스크립트 exit 1

### 회고 세션 `a60a7617`(08-06 21:38) · `13315412`(23:24) · `fc108055`(23:31) · `d1363bb4`(23:50)

프롬프트는 넷 다 `retrobot/SKILL.md` 전문으로 동일합니다. 넷이 합쳐 **에러 21건**을 만들었고,
내용도 동일합니다.

- `command -v python3 jq` 승인 거부 (4세션 전부)
- `/tmp/*.py` 쓰기 차단 (4세션 전부)
- `Contains brace with quote character` · `file_redirect` · `backslash-escaped whitespace` 정적 분석 거부

## KPT 회고

### Keep (좋았던 것)

- **PRD의 불합격 기준을 그대로 겨눈 감사였습니다.** 「문서에 구현했다고 적었으나 해당 코드가
  없음」은 감점이 아니라 **불합격** 항목인데, 이 창에서 실제로 그 자리에 있던 문장이 하나
  발견됐습니다 — **"레이어 방향을 린트로 강제한다"**. `core/.ruff.toml`은 import 금지 목록만
  갖고 있고 레이어 방향을 강제하는 장치는 없습니다. 세 문서에서 정정하고 "리뷰로 지키는
  중이며 import-linter 도입이 남은 빚"이라고 명시했습니다. **없는 장치를 있다고 적은 유일한
  자리를 마감 이틀 전에 걷어냈습니다.**
- **감사를 3분할해 병렬로 붙인 것이 유효했습니다.** 문서 3종(1,000줄 이상)을 한 컨텍스트에서
  읽으면 뒤로 갈수록 대조가 성기어집니다. 서브에이전트 3개가 각자 Bash 38~45회로 **코드를 직접
  열어** 대조했고, 셋 중 둘은 에러 0건으로 끝났습니다. 감사자와 수정자를 분리했기 때문에
  "내가 쓴 문서를 내가 읽는" 자기확인 편향이 한 단계 끊겼습니다.
- **자기가 새로 심을 뻔한 불일치를 자기가 잡았습니다.** 「K와 후보 깊이」 절을 새로 쓰면서
  어휘 과다 인출 창을 `top_k × 10`으로 적었는데, 코드를 되짚어 파라미터 이름만 `top_k`이고
  실제로는 `candidate_depth`(100)가 넘어간다는 것을 확인해 **후보 깊이 × 10 = 1000줄**로
  정정했습니다. 바로 앞 표가 두 값을 구분해 둔 터라 독자가 50으로 계산할 자리였습니다.
  **불일치를 고치는 작업이 불일치를 생산하는 것이 이 종류 작업의 기본 실패 모드**인데,
  커밋 전에 자체 검증 단계를 한 번 더 돌려 막았습니다.
- **`--build`를 또 빠뜨리지 않았습니다 — 일곱 세션 연속.** `docker compose run --build --rm test`
  두 번 모두 정확한 형태였고, 기본 1,121건과 마커 33건(`llm or redis or slow`)을 **둘 다**
  돌려 저장소의 테스트 1,154건 전부를 통과시켰습니다. 실물 `codex` CLI 골든셋 채점·실물 Redis
  계약·크로스인코더 리랭킹 품질이 여기 포함됩니다.
- **`add-multiturn-qa`의 아홉 회차 이월이 끝났습니다.** 73시간 untracked였던 7개 아티팩트가
  `df8f0ce`로 들어갔고, `add-cross-encoder`도 같은 커밋에서 아카이브됐습니다. 직전 회고 Try가
  또 한 번 실행됐습니다.
- **49분짜리 테스트를 기다리는 동안 판단 재료를 먼저 냈습니다.** 20:09의 "여전히 돌고 있습니다"
  응답이 단순 상태 보고가 아니라 — `codex app-server` 자식 프로세스가 사라진 것에서 LLM 구간
  종료를, CPU 99%에서 크로스인코더 구간을 추론하고 — **"지금 멈춰도 검증 공백은 생기지 않는다"**
  는 근거(변경분은 문서 3종 + `anyio` 선언 + 주석 한 줄, 코드 영향분은 이미 1,121건으로 검증됨)를
  붙여 중단 결정을 사용자에게 넘겼습니다.

### Problem (나빴던 것)

- **Retrobot이 또 하루 에러의 78%(21/27)를 혼자 만들었고, 이것이 17회차입니다. 그리고 이
  회고를 쓰는 지금 세션이 18회차입니다.** `retrobot/SKILL.md`는 `afa9794`(08-03 22:23) 이후
  **94시간 무수정**입니다. 네 세션이 각자 처음부터 같은 세 개의 벽에 부딪혔고, 문서가 안내하는
  첫 명령(`command -v python3 jq`)은 이 호스트에서 **반드시 거부되며**, 문서가 지정한 스크립트
  위치(`/tmp`)에는 **쓸 수 없고**, 문서가 "하지 말 것"으로 금지한 heredoc이 **유일하게 통하는
  길**입니다. 세 문장 전부 현실과 반대인 채로 나흘째입니다. 직전 회고가 이것을 정확히 같은
  문장으로 지적했고, **그 Try는 실행되지 않았습니다.**
- **49분 33초를 진행률 없이 태웠습니다.** 마커 테스트를 백그라운드로 돌리면서 출력을 `tail`
  뒤에 두는 바람에 남은 건수를 볼 수 없었고, 사용자 프롬프트 9건 중 **2건(19:41·20:09)이
  "작업 진행 중인가?"** 였습니다. 진행 상황을 알아내려고 `docker top`·`docker stats`·`wc -c`로
  **간접 추정**을 해야 했습니다 — CPU 점유율로 테스트 단계를 역추론하는 것은 영리하지만,
  애초에 `pytest -v`나 파일 tee로 해결될 문제였습니다.
- **문서-코드 일치 검사만 자동화가 없습니다.** 이 저장소는 주석 규칙을 `scripts/check_comments.py`로,
  린트를 `ruff`로, 동작을 테스트 1,154건으로 강제합니다. 그런데 **PRD가 유일하게 "불합격"으로
  분류한 항목**(허위 기재)에 대응하는 검사는 사람이 18:48에 프롬프트를 쳐야 도는 수동
  절차입니다. 게이트가 붙은 것은 감점 항목이고, 게이트가 없는 것이 불합격 항목입니다.
- **`ruff format` 미정리가 17건 → 19건으로 늘었습니다.** 열세 회차째 세고 있고, 한 번도 줄지
  않았습니다. `ruff check`는 게이트에 걸려 있고 `format`은 아니라, **집계만 하고 방치하는 지표**가
  됐습니다. 고치거나 세기를 그만두거나 둘 중 하나입니다.
- **이 창의 최대 문서 변경(`4b13644`, 872 삽입 / 2,443 삭제)을 회고가 설명하지 못합니다.**
  08-07 오전 세 커밋(10:41 `1fcf833`, 11:47 `4b13644`, 15:55 `a419fa7`)에 해당하는 세션 로그가
  훅이 넘긴 파일 목록에 **없습니다.** ARCHITECTURE.md가 2,117줄 규모로 재작성된 회차인데
  회고에는 커밋 통계만 남습니다. **회고가 볼 수 있는 것은 훅이 고른 파일뿐**이라는 구조적 한계가
  이번에 처음 실제 공백으로 드러났습니다.
- **병합 완료 브랜치 9개가 그대로입니다** (`add-answer-generation`·`add-cross-encoder`·`add-demo-ui`·
  `add-qa-retrieval`·`add-rrf-algorithm`·`bootstrap-runtime-skeleton`·`debugging`·`document-ingestion`·
  `fix-pdf-parser`). 미병합은 `add-response-cache`·`fix-demo-ui-citation` 둘. 직전 회고와 **한 건도
  달라지지 않았습니다** — 살아 있는 브랜치와 끝난 브랜치가 목록에서 여전히 구분되지 않습니다.
- **외부(Codex) 적대적 검토는 여전히 0회, 열일곱 세션 연속.** 다만 이번 회차는 성질이
  달랐습니다 — 서브에이전트 3개가 "불일치를 찾아라"는 적대적 지시로 붙었고 실제로 결함을
  건져냈습니다. **같은 모델의 다른 컨텍스트**라는 한계는 남지만, 열여섯 회차 동안 0이던 자리에
  처음 무언가가 들어왔습니다.

### Try (개선해야 할 것)

1. **`retrobot/SKILL.md`의 「집계 도구가 없거나 막힌 환경」 절을 지금 세션의 사실로 교체한다.**
   18회차 반복이고, 항목이 실행 가능하지 않아 이월된 것이 아니라 **아무도 손대지 않아서**
   이월됐습니다. 넣을 문장은 이미 확정돼 있습니다 (이 회고 자신이 검증했습니다).
   - macOS 호스트: `command -v python3 jq`는 **거부된다**. `python3 --version`을 단독으로 쓴다.
   - `/tmp` 쓰기는 **차단된다**. Write 도구도, `>` 리다이렉션도 안 된다.
   - **통하는 유일한 길은 `python3 - <<'PYEOF' … PYEOF` (stdin 파이프)** 다. 현재 문서의
     "하지 말 것"에서 heredoc을 빼고 여기로 옮긴다.
   - 금지 목록에 남길 것: `python -c` 인라인(따옴표 정적 분석 거부), 브레이스+따옴표 확장.
2. **긴 테스트는 출력을 파일로 남긴다.** `docker compose run --build --rm test pytest -m "llm or redis or slow" -v`
   로 건별 진행을 내보내거나, 백그라운드 실행 시 `tail` 대신 전체 출력을 파일에 받아
   `wc -l`로 진행 건수를 센다. 49분 동안 `docker stats`로 단계를 추론하는 일이 반복되지 않게 한다.
3. **문서-코드 일치 검사를 `scripts/`에 넣는다.** 전면 감사는 자동화가 어렵지만, **수치와 고유명사**는
   가능합니다 — 문서에 적힌 청크 크기·오버랩·`top_k`·`candidate_depth`·RRF `k`·캐시 임계값·리랭커
   리비전(`953dc6f6…`)·지원 포맷 목록을 `src/app/core/config.py`의 실제 기본값과 대조하는 스크립트.
   이번에 사람이 잡은 불일치 중 절반 이상이 이 유형(네 겹→세 겹, 넷→다섯, 118MB, `top_k × 10`)이었습니다.
4. **`ruff format`을 결정한다.** `docker compose run --build --rm test ruff format src tests`로 19건을
   한 번에 정리하고 `ruff format --check`를 게이트에 넣거나, **회고에서 이 지표를 삭제**한다.
   열세 회차 동안 세기만 한 숫자는 추세가 아니라 소음입니다.
5. **병합 완료 브랜치 9개를 지운다.** `git branch -d`로 한 줄이고, 남기면 `add-response-cache`·
   `fix-demo-ui-citation` 둘이 **살아 있는 브랜치**로 즉시 보입니다. 마감(08-09 23:59)까지
   이틀이라, 지금 목록이 정리돼 있어야 남은 작업의 범위가 눈에 들어옵니다.
6. **훅이 넘기는 파일 목록에 당일 커밋과 대응하는 세션이 다 있는지 회고 첫 단계에서 확인한다.**
   `git log --since`의 커밋 시각과 세션 창을 대조해, 설명되지 않는 커밋이 있으면 **회고 안에
   공백으로 명시**한다(이번처럼). 조용히 빠지면 타임라인이 거짓말을 합니다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 08-06 21:05 | `f4deea06` | `fix-retrieval-cache-defects` 마무리 (Bash 253 · Edit 106, 프롬프트 0건) |
| 08-06 21:38 | `a60a7617` | 회고 생성 — 에러 5건(승인 거부 2 · `/tmp` 차단 1 · 정적 분석 2) |
| 08-06 23:24 | `13315412` | 회고 생성 — 에러 6건 |
| 08-06 23:31 | `fc108055` | 회고 생성 — 에러 3건 |
| 08-06 23:31 | `82dfb17a` | `add-cross-encoder` → `main` 병합, 스펙 아카이빙 (`4a6ce35`·`df8f0ce`) |
| 08-06 23:50 | `d1363bb4` | 회고 생성 — 에러 7건 (하루 최다) |
| 08-07 10:41 | *(로그 없음)* | `1fcf833` 문서-코드 불일치 수정 (감사 설계 문서 + `tests/README.md`) |
| 08-07 11:47 | *(로그 없음)* | `4b13644` 제출 문서 재작성 (**+872 / −2,443**, ARCHITECTURE 2,117줄 정리) |
| 08-07 15:55 | *(로그 없음)* | `a419fa7` 테스트 결함 수정 (`conftest`·골든판정·하네스·리랭킹 품질) |
| 08-07 18:48 | `b9bfeeec` | PRD 기반 전면 감사 착수 — 서브에이전트 3개 병렬(README·ARCHITECTURE·PROMPT_DESIGN) |
| 08-07 18:56 | 서브에이전트 3 | 감사 완료, 둘은 에러 0건 |
| 08-07 19:03 | `b9bfeeec` | `check_comments.py` 124파일 위반 0 · `ruff check` 통과 |
| 08-07 19:06 | `b9bfeeec` | `docker compose run --build --rm test` → **1121 passed** (1분 23초) |
| 08-07 19:12 | `b9bfeeec` | 마커 테스트 시작 (`-m "llm or redis or slow"`) |
| 08-07 19:44 | `b9bfeeec` | 자신이 새로 쓴 `top_k × 10`을 **후보 깊이 × 10**으로 자체 정정 |
| 08-07 20:09 | `b9bfeeec` | 진행 상황을 `docker top`·`stats`로 간접 추정, 중단 판단 근거 제시 |
| 08-07 20:21 | `b9bfeeec` | 마커 테스트 **33 passed** (49분 33초) — 총 1,154건 전부 통과 |
| 08-07 20:25 | — | `34da339` 문서-코드 일치화 커밋 (문서 3종 + `anyio` 선언 + 주석 1줄) |
