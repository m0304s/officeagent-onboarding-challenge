# PROMPT DESIGN

> **이 문서는 현재 구현된 것만 기술합니다.** 프롬프트 조립·모델 출력 파싱·판정 줄 분리는 [`src/app/core/prompting.py`](./src/app/core/prompting.py)에 있고, [`tests/test_prompting.py`](./tests/test_prompting.py)가 LLM 없이 문자열 단언으로 고정합니다.
>
> **이 프롬프트를 실제로 모델에 보내는 경로는 아직 없습니다.** LLM 어댑터(`adapters/llm/`)와 QA 서비스(`services/qa.py`), `POST /qa`는 `add-answer-generation` change의 3~6장이 붙입니다. 아래에서 "서비스가 …한다"로 적힌 것 중 **재시도 정책·이벤트 시퀀스·인용 조립**은 그 시점의 동작이고, 지금 코드로 확인할 수 있는 것은 프롬프트 문자열과 파서까지입니다.

## 한 장으로 보는 설계

프롬프트가 하는 일은 하나입니다 — **모델이 지어낼 여지를 줄이고, 지어냈을 때 서버가 그것을 잡아낼 수 있는 형태로 출력을 받는 것.** 구성 요소마다 막으려는 것이 다릅니다.

| 구성 요소 | 무엇을 막는가 | 실패해도 남는 방어선 |
|---|---|---|
| `<근거>` 블록 — 번호·파일명·위치와 함께 | 모델이 근거를 **어느 문서에서 왔는지 모른 채** 인용하는 것 | 인용 항목의 값은 검색 결과에서 복사하므로 모델이 파일명을 잘못 적어도 응답은 정확 |
| 규칙 1 — "근거에 없는 내용을 쓰지 마십시오" | 학습된 지식(파라메트릭 지식)이 답을 채우는 것 | 마커 검증 — 근거를 가리키지 못하는 문장은 인용이 붙지 않는다 |
| 규칙 2 — "제공된 근거 밖을 조회하지 마십시오" | **에이전트가 컨테이너 파일시스템을 뒤지러 가는 것** | 빈 작업 디렉터리 · 읽기 전용 샌드박스 · 최소 환경변수 (3장) |
| 규칙 3 — "지어내지 말고 무엇이 부족한지 밝히십시오" | 무관한 근거로 그럴듯한 답을 만드는 것 | 없음. **이 지시가 `INSUFFICIENT` 판정의 유일한 원천입니다** |
| 규칙 4 — `[n]` 마커 | 출처 없는 주장 | 서버가 마커를 검증하고 범위 밖을 버린다 |
| 출력 형식 — 첫 줄 판정 | 거절이 **산문 속에 숨는 것** | 판정 줄이 없으면 `ANSWERABLE`로 보고 로그로 남긴다 |
| 출력 형식 — "본문을 비워 두지 마십시오" | 형식만 지키고 내용을 못 낸 회차가 성공으로 집계되는 것 | 빈 본문은 생성 실패로 분류해 재시도에 태운다 |
| 질문을 **마지막에** 배치 | 문서 본문에 섞인 문장이 지시를 덮어쓰는 것 | 없음 |

## 프롬프트 전문

아래는 `build_prompt("교육비 지원 한도가 얼마인가요?", [company-policy.txt 청크, benefits-2026.pdf 청크])`가 실제로 만드는 문자열 **그대로**입니다.

```text
당신은 사내 문서 질의응답 어시스턴트입니다. 아래 <근거>에 실린 내용만으로 <질문>에 답하십시오.

규칙
1. 근거에 없는 내용을 쓰지 마십시오. 이미 알고 있는 사실이라도 근거에 없으면 쓰지 않습니다.
2. **제공된 근거 밖을 조회하지 마십시오.** 파일을 열거나 명령을 실행하거나 웹을 검색하지 않습니다 — 답하는 데 필요한 것은 모두 아래에 있습니다.
3. 근거가 질문에 답하기에 충분하지 않으면 지어내지 말고, 무엇이 부족한지 밝히십시오.
4. 근거에서 가져온 문장은 **끝에 그 근거의 번호를 `[n]` 형태로** 답니다. 여러 근거를 함께 썼으면 `[1][2]` 처럼 이어 붙입니다. 근거 목록에 없는 번호는 쓰지 마십시오.

출력 형식
첫 줄에 판정을 아래 둘 중 하나로 **그대로** 적습니다. 앞뒤에 다른 말을 붙이지 않습니다.

VERDICT: ANSWERABLE
VERDICT: INSUFFICIENT

둘째 줄부터 본문을 한국어로 씁니다. ANSWERABLE 이면 답변을, INSUFFICIENT 이면 근거의 무엇이 부족한지를 씁니다. **어느 쪽이든 본문을 비워 두지 마십시오.**

<근거>
[1] company-policy.txt (문자 120–540)
교육비는 1인당 연 200만원까지 지원합니다. 신청은 부서장 승인 후 인사팀에 제출합니다.

[2] benefits-2026.pdf (3쪽)
자격증 응시료는 교육비 예산과 별도로 처리합니다.
</근거>

<질문>
교육비 지원 한도가 얼마인가요?
</질문>
```

기대하는 응답:

```text
VERDICT: ANSWERABLE
교육비는 1인당 연 200만원까지 지원됩니다 [1]. 신청은 부서장 승인 후 인사팀에 제출합니다 [1]. 자격증 응시료는 이 예산과 별도로 처리됩니다 [2].
```

## 환각 억제 — 세 겹으로 답니다

환각을 "막는" 단일 지시는 없다고 보고 설계했습니다. 대신 **성격이 다른 세 층**을 겹칩니다. 하나가 뚫려도 나머지가 남고, 무엇보다 **뚫렸다는 사실이 관측 가능해집니다.**

### 1. 근거 밖을 보지 못하게 한다 (입력 차단)

두 종류의 "밖"이 있습니다.

- **모델의 기억** — 규칙 1이 막습니다. 그리고 **근거가 0건이면 아예 호출하지 않습니다**(아래 "거절 두 갈래"). 문맥이 빈 프롬프트에서 모델이 쓸 수 있는 재료는 학습된 지식뿐이라, "답하지 마라"고 지시하는 것보다 묻지 않는 편이 확실합니다.
- **컨테이너의 파일** — 규칙 2가 막습니다. 이 줄이 특별한 이유는 **상대가 텍스트 생성기가 아니라 에이전트**이기 때문입니다. Codex CLI는 도구를 실행하고 파일을 읽을 수 있는 실행체라, RAG 프롬프트를 받고 소스 코드를 뒤지러 갈 수 있습니다. 그래서 이 지시는 **네 겹 중 가장 바깥이자 가장 약한 겹**입니다 — 실제 차단은 빈 작업 디렉터리·읽기 전용 샌드박스·비대화형 승인·최소 환경변수가 하고(3장), 프롬프트는 그 전부가 실패했을 때 남는 마지막 줄입니다.

### 2. 거절을 산문이 아니라 형식으로 만든다 (판정 줄)

모델이 "죄송하지만 제공된 문서에는…"이라고 쓰면 그것은 거절이지만, **서버는 그것이 거절인지 답변인지 알 수 없습니다.** 문자열을 뒤져 거절 문구 패턴을 찾는 방법은 언어·톤·모델 판본에 따라 흔들립니다.

그래서 판정을 **첫 줄에 기계가 읽을 값으로** 받습니다.

```
VERDICT: ANSWERABLE
VERDICT: INSUFFICIENT
```

이 값이 `finish_reason`이 되고, `INSUFFICIENT`이면 인용을 비웁니다 — "답할 수 없다"면서 근거를 인용하는 출력은 모순이고, 인용을 남기면 거절문에 출처가 붙어 답변처럼 보이기 때문입니다.

**판정 줄을 첫 줄에 두는 것이 스트리밍과 양립하기 위한 조건입니다.** 판정이 출력 어디에 있어도 되게 두면 판정을 알기 위해 전체를 기다려야 하고, 그러면 스트리밍이 사라집니다. 첫 줄이면 **그 한 줄만 버퍼링**하고 나머지는 그대로 흘려보낼 수 있습니다. 실측에서 판정 줄 하나가 토큰 델타 일곱 조각(`VER`·`DICT`·`:`·`' ANSW'`·`ER`·`ABLE`·`\n`)에 걸쳐 왔고, 그것을 조립하는 상태 기계가 `VerdictSplitter`입니다.

판정 줄은 **`answer` 이벤트에도 `done.answer`에도 실리지 않습니다.** 사용자에게 보일 답변의 일부가 아니라 서버와 모델 사이의 표시이기 때문입니다.

### 3. 출처를 강제하고, 서버가 검증한다 (마커)

규칙 4가 `[n]` 마커를 요구하고, 서버가 그 마커를 **검증해서** 구조화된 `citations`로 조립합니다.

- 등장 순서대로 모으고, 중복은 한 번만 싣고, `1..근거 수` 밖은 **버립니다.**
- 버린 개수를 응답에 싣습니다(`dropped_markers`).
- **본문의 마커는 지우지 않습니다.** 지우면 스트림으로 흘러간 문장과 최종본이 달라져, 둘 다 받은 클라이언트가 무엇을 표시할지 알 수 없게 됩니다.

**없는 근거를 가리키는 마커가 환각의 가장 그럴듯한 형태입니다.** 번호가 붙어 있으니 검증된 것처럼 보이는데 가리키는 곳이 없습니다. 조용히 통과시키면 출처 표기가 오히려 신뢰를 만들어 내는 장치가 됩니다. 그래서 버리고, **버린 수를 응답에 노출합니다** — 그 수가 늘어나는 것이 프롬프트가 나빠졌다는 가장 이른 신호입니다.

중복 제거를 범위 검증보다 **먼저** 하는 이유도 그 신호 때문입니다. 같은 없는 번호를 세 번 쓴 답변의 `dropped_markers`가 `3`이면 이 수가 답변 길이에 비례해 흔들려 지표로 쓸 수 없습니다. 세는 대상은 "잘못 가리킨 근거"이지 "잘못 적은 글자"가 아닙니다.

## 거절 두 갈래 — 누가 판정하는가

"답할 수 없다"는 **서로 다른 두 사건**이고, 판정 주체가 다릅니다. 프롬프트가 맡는 것은 그중 하나뿐입니다.

| 사건 | 판정 주체 | 모델 호출 | `finish_reason` | 답변 문자열 |
|---|---|---|---|---|
| 근거가 0건 | **서비스** (검색 결과 개수만 보면 된다) | **하지 않는다** | `no_evidence` | **빈 문자열** |
| 근거는 있으나 질문을 뒷받침하지 않음 | **모델** (근거 본문을 읽어야 한다) | 한다 | `insufficient_evidence` | 모델이 쓴 사유 |
| 답변 | 모델 | 한다 | `stop` | 모델이 쓴 답변 |

**둘을 나누는 이유는 사용자가 할 일이 다르기 때문입니다** — 앞은 문서를 올려야 하고 뒤는 질문을 바꿔야 합니다. 유사도 점수만으로는 뒤를 판정할 수 없습니다. 점수가 높아도 내용이 질문과 무관할 수 있고, 그 판정에는 본문을 읽는 일이 필요합니다. 그래서 검색 계층은 "이 청크를 보여줄 가치가 있는가"(유사도 하한)까지만 판정하고 거절 문구를 만들지 않으며, 그 경계가 프롬프트에게 규칙 3의 자리를 남깁니다.

**근거가 0건일 때 서비스가 대신 문구를 쓰지도 않습니다.** `answer`는 빈 문자열이고 `answer` 이벤트는 한 번도 나가지 않습니다. 근거가 없다는 **사실**은 `finish_reason`이 나르고, 사용자에게 보일 **표현**은 소비자가 정합니다.

이 선택으로 얻는 것이 프롬프트 설계 문서에 적을 만한 종류입니다 — **답변 문자열을 만드는 곳이 모델뿐이라는 규칙에 예외가 없어집니다.** 과제의 불합격 기준에 "LLM 호출 없이 하드코딩된 응답"이 있는데, 서버가 거절 문구를 한 줄이라도 갖는 순간 그 경계가 해석 문제가 됩니다. 문구가 서버에 있으면 언어·톤·제품 문맥에 따라 변형이 늘고, 그때마다 서버가 답변 작성자에 가까워집니다. 그 미끄러짐이 시작될 자리를 없앴습니다.

## 왜 구조화 출력(JSON Schema)을 쓰지 않는가

Codex CLI는 JSON Schema로 출력을 강제하는 기능이 있고, 그것을 쓰면 판정과 인용을 파싱 없이 받을 수 있습니다. 쓰지 않았습니다.

- **JSON은 스트리밍과 원리적으로 충돌합니다.** 부분 JSON은 파싱할 수 없으므로 답변 문자열을 꺼내려면 전체를 받아야 합니다. 스키마를 쓰는 순간 스트리밍은 **형식 때문에** 불가능해집니다. 스트리밍은 과제의 하드 요구사항이고 구조화 출력은 그렇지 않습니다.
- **스키마가 검증 단계를 없애 주지 못합니다.** 스키마는 `cited_chunks: [7]`의 *형식*은 보증하지만 근거가 2건뿐일 때 `7`이 존재하지 않는다는 *사실*은 보증하지 않습니다. 어느 형식을 쓰든 범위 검증은 우리가 합니다. 그렇다면 스키마가 대신해 주는 일은 괄호 세기뿐이고, 그건 정규식 한 줄입니다.

**구조화 출력을 포기한 것이 아니라 위치를 옮긴 것입니다.** 구조를 보증하는 곳이 모델이 아니라 서버입니다 — `citations`는 검증을 통과한 항목만 담긴 배열이고 그 모양은 응답 모델이 강제합니다. 모델이 형식을 어겨도 소비자가 받는 구조는 흔들리지 않습니다. **모델이 지키는 형식에 응답 구조를 의존시키는 쪽이 오히려 약합니다.**

## 형식을 어겼을 때 무슨 일이 일어나는가

모델은 형식을 어깁니다. 그때의 동작을 정해 두지 않으면 같은 위반이 배포마다 다르게 나타납니다.

| 모델 출력 | 서버의 처리 | 왜 |
|---|---|---|
| 첫 줄 `VERDICT: ANSWERABLE` | 나머지가 답변. 마커 검증 진행 | — |
| 첫 줄 `VERDICT: INSUFFICIENT` | 나머지가 거절 사유. **인용을 비운다** | 판정이 마커를 이긴다 |
| 판정 줄 없음 | `ANSWERABLE`로 보고 **출력 전체를 본문으로** 쓴다 + 경고 로그 | 그 본문도 근거만 주어진 프롬프트에서 나왔다 — 형식 위반이 곧 환각은 아니다. 마커 검증은 그대로 돌아 출처 보증도 그대로다 |
| 판정 줄만 있고 본문이 없음 | **생성 실패**로 분류해 재시도. 소진되면 `error` | 빈 답변으로 끝나는 경로는 "근거 없음" 하나뿐이어야 한다. 둘이면 소비자 화면에서 정상과 장애가 같아 보인다 |
| 범위 밖 마커 | 버리고 개수를 응답에 싣는다 | 없는 근거를 가리키는 인용은 환각이다 |
| 마커가 하나도 없음 | 유효한 답변. `citations`가 빈다 | "답은 했는데 근거를 특정하지 못했다"는 별개의 상태다 |

판정 줄 인식은 **정확히 일치**할 때만 성립합니다 — `verdict: answerable`도, `VERDICT:ANSWERABLE`도, 뒤에 공백이 붙은 것도 판정 줄이 아닙니다. 관대하게 받으면 스트리밍 분리기가 "판정 줄이 아직 안 끝났을 가능성"을 판정할 수 없어(뒤에 공백이 몇 개 더 올지 모릅니다) 버퍼 상한이 사라지고, 그 순간 **형식을 어긴 회차에서만 스트리밍이 죽습니다.** 엄격하게 받는 대가는 그 회차에서 판정 줄이 본문에 남는 것뿐이고, 그쪽이 훨씬 쌉니다.

## 문맥 블록의 모양

```
[1] company-policy.txt (문자 120–540)
<청크 본문>

[2] benefits-2026.pdf (3쪽)
<청크 본문>
```

문맥에 들어가는 값은 **검색 결과가 이미 들고 있는 것뿐입니다.** 프롬프트를 만들려고 문서를 다시 조회하지 않습니다.

- **번호**가 마커의 정의역입니다. `1`부터 빠짐없이 매기며, 이 범위 밖을 가리키는 마커가 버려집니다.
- **파일명**을 넣는 이유는 둘입니다 — 모델이 "어느 문서에서 왔는가"를 답변 문장에 녹일 수 있고, 마커가 가리키는 대상이 사람이 읽어도 확인 가능해집니다.
- **위치**는 PDF면 쪽(`3쪽`), 텍스트·마크다운이면 원문 문자 구간(`문자 120–540`)입니다. PDF에 문자 오프셋을 적지 않는 이유는 그 값이 **쪽 안의** 오프셋이라(수집이 문서 전역 오프셋을 만들지 않습니다) 문서에서 그 자리를 찾는 데 쓸 수 없고, 적어 두면 문서 전체 기준으로 오해되기 때문입니다.

**질문은 문맥 뒤에 옵니다.** 긴 문맥 뒤에 질문을 두면 모델이 지시를 잊는다는 반대 방향의 우려가 있지만, 지시문이 문맥 **앞**에 따로 있으므로 그 위험은 이미 나뉘어 있습니다. 질문을 마지막에 두어 얻는 것은 문서 본문에 섞여 들어온 명령형 문장이 지시를 덮어쓰기 어려워진다는 점입니다.

## `PROMPT_VERSION`

```python
PROMPT_VERSION = "qa-ko-1"
```

**출력 문자열이 바뀌면 반드시 올립니다.**

다음 change가 붙일 **캐시 키에 들어가야 할 값**입니다. 프롬프트가 바뀌면 같은 질문의 답이 달라지는데, 버전이 키에 없으면 프롬프트를 개선해도 낡은 캐시가 옛 답을 계속 내보냅니다. 그러면 개선이 관측되지 않을 뿐 아니라, **개선했다고 믿는 상태에서 실제로는 아무것도 바뀌지 않습니다** — 가장 조용한 종류의 실패입니다.

여기서는 값만 만들어 두고 키 설계는 캐싱 change에 넘깁니다. 지금 키를 정하면 아직 존재하지 않는 캐시를 가정한 구조가 남습니다.

## 프롬프트를 어떻게 시험하는가 — LLM 없이

프롬프트 조립과 출력 파싱을 `core/`의 **순수 함수**로 둔 이유가 이것입니다. `tests/test_prompting.py`가 구독·네트워크·비동기 없이 문자열 단언만으로 돕니다.

세 묶음입니다.

1. **프롬프트 회귀** — 모든 근거가 번호·파일명·위치와 함께 들어가는가, 지시문이 환각을 막는 문장들을 갖고 있는가, 질문이 근거 뒤에 오는가.
2. **파서 경계** — 위 "형식을 어겼을 때" 표의 모든 행.
3. **판정 줄 분리** — 델타가 판정 줄을 쪼개는 모든 경우. 여기에는 **분리기와 파서가 같은 본문을 내놓는가**를 출력 12종 × 모든 분할 방식으로 확인하는 테스트가 포함됩니다.

그중 하나가 다른 것들보다 중요합니다 — **프롬프트가 지시한 판정 줄을 파서가 실제로 인식하는가.** 프롬프트에서 정규식으로 판정 줄을 뽑아 파서에 먹여 확인합니다. 둘이 갈리면 모델은 시킨 대로 쓰는데 서버가 못 알아듣고, 그 증상은 오류가 아니라 "판정 줄 없음" 경로로 **조용히 흡수되어** `INSUFFICIENT`가 영원히 나오지 않는 형태로만 드러납니다. 거절이 사라지는 것이라 환각 억제가 통째로 무력해지는데 어디에도 실패가 남지 않습니다.

실행:

```bash
docker compose run --build --rm test pytest tests/test_prompting.py
```

## 아직 정하지 않은 것

- **문맥 총량 상한.** `top_k` 상한 50 × 청크 600자 ≈ 30KB가 최악의 프롬프트 크기입니다. 모델 입력 창을 넘겼을 때 CLI가 오류를 내는지 조용히 절단하는지 아직 모릅니다. 기본 `top_k`가 5라 정상 경로에서는 닿지 않으므로 지금 상한을 두지 않았고, 필요해지면 설정 항목 하나가 붙습니다.
- **소수 언어 응답.** 프롬프트가 한국어 답변을 지시합니다. 영어 문서에 영어로 물었을 때 어떻게 답하는 것이 옳은지는 정하지 않았습니다 — 사내 문서 Q&A라는 이 과제의 문맥에서는 한국어 고정이 예측 가능성 쪽으로 기울어 있다고 보았습니다.
- **인용의 문장 단위 정렬.** 마커가 어느 문장에 붙었는지까지 구조화하지 않습니다. 근거 청크 단위가 이 과제에서 검증 가능한 최소 단위입니다.
