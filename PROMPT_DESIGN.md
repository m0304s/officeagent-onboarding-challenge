# PROMPT DESIGN

RAG 파이프라인에서 쓰는 프롬프트의 설계 의도와 환각 억제 전략을 정리한 문서.

프롬프트가 하는 일은 하나입니다 — **모델이 지어낼 여지를 줄이고, 지어냈을 때 서버가 그것을 잡아낼 수 있는 형태로 출력을 받는 것.** 프롬프트 하나에 환각 억제를 몰아주지 않았습니다. 프롬프트는 다섯 겹 방어선 중 **세 번째**일 뿐입니다 — 앞의 둘(검색·근거 0건이면 호출하지 않음)은 프롬프트에 닿기 전에 걸러 내고, 뒤의 둘(판정 줄·마커 검증)은 프롬프트가 실패했을 때를 위해 있습니다.

> **구현 위치** — 프롬프트 조립·출력 파싱·판정 줄 분리는 [`src/app/core/prompting.py`](./src/app/core/prompting.py)에 있고, [`tests/test_prompting.py`](./tests/test_prompting.py)가 **LLM 없이 문자열 단언으로** 고정합니다. 실제 전송은 [`src/app/adapters/llm/`](./src/app/adapters/llm/) → [`src/app/services/qa.py`](./src/app/services/qa.py) → `POST /qa` 경로입니다.

---

## 1. 설계 원칙

세 가지입니다.

1. **모델을 신뢰하지 않는다.** 모델은 형식을 어깁니다. 어겼을 때의 동작을 정해 두지 않으면 같은 위반이 배포마다 다르게 나타납니다.
2. **구조는 모델이 아니라 서버가 보증한다.** 응답의 모양이 모델의 형식 준수에 의존하면, 모델이 규칙을 어긴 회차에서 소비자가 받는 구조까지 흔들립니다.
3. **답변 문자열을 만드는 곳은 모델뿐이다.** 서버가 거절 문구를 한 줄이라도 갖는 순간, 과제 불합격 기준인 "LLM 호출 없이 하드코딩된 응답"과의 경계가 해석 문제가 됩니다.

### 한 장으로 보는 설계

| 구성 요소 | 무엇을 막는가 | 실패해도 남는 방어선 |
|---|---|---|
| `<근거>` 블록 — 번호·파일명·위치 | 모델이 **어느 문서에서 왔는지 모른 채** 인용하는 것 | 인용 값을 검색 결과에서 **복사**하므로 모델이 파일명을 틀려도 응답은 정확 |
| 규칙 1 — "근거에 없는 내용을 쓰지 마십시오" | 학습된 지식이 답을 채우는 것 | 마커 검증 — 근거를 못 가리키는 문장에는 인용이 붙지 않음 |
| 규칙 2 — "제공된 근거 밖을 조회하지 마십시오" | **에이전트가 컨테이너 파일시스템을 뒤지러 가는 것** | 빈 작업 디렉터리 · 읽기 전용 샌드박스 · 비대화형 승인 · 세션 비영속 · 최소 환경변수 |
| 규칙 3 — "지어내지 말고 무엇이 부족한지 밝히십시오" | 무관한 근거로 그럴듯한 답을 만드는 것 | **없음 — 이 지시가 `INSUFFICIENT` 판정의 유일한 원천입니다** |
| 규칙 4 — `[n]` 마커 | 출처 없는 주장 | 서버가 마커를 검증하고 범위 밖을 버림 |
| 출력 형식 — 첫 줄 판정 | 거절이 **산문 속에 숨는 것** | 판정 줄이 없으면 `ANSWERABLE` 로 보고 경고 로그 |
| 출력 형식 — "본문을 비워 두지 마십시오" | 형식만 지키고 내용을 못 낸 회차가 성공으로 집계되는 것 | 빈 본문은 **생성 실패**로 분류해 재시도에 태움 |
| 질문을 **마지막**에 배치 | 문서 본문에 섞인 명령형 문장이 지시를 덮어쓰는 것 | 없음 |

**규칙 3에 "실패해도 남는 방어선: 없음"이라고 적은 것이 이 표에서 가장 중요한 줄입니다.** 다른 규칙은 서버가 사후에 검증할 수 있지만, "이 근거로 답할 수 있는가"는 **본문을 읽어야 하는 판정**이라 서버가 대신할 수 없습니다. 그래서 이 규칙에만 백스톱이 없고, 그 사실을 적어 두어야 다음 사람이 여기를 함부로 고치지 않습니다.

---

## 2. 프롬프트 전문

아래는 `build_prompt("교육비 지원 한도가 얼마인가요?", [...])` 가 **실제로 만드는 문자열 그대로**입니다. 문서를 쓰면서 손으로 옮겨 적은 것이 아니라 함수를 실행해 받은 값입니다.

```text
당신은 사내 문서 질의응답 어시스턴트입니다. 아래 <근거>에 실린 내용만으로 <질문>에 답하십시오.

규칙
1. 근거에 없는 내용을 쓰지 마십시오. 이미 알고 있는 사실이라도 근거에 없으면 쓰지 않습니다.
2. **제공된 근거 밖을 조회하지 마십시오.** 파일을 열거나 명령을 실행하거나 웹을 검색하지 않습니다 — 답하는 데 필요한 것은 모두 아래에 있습니다.
3. 근거가 질문에 답하기에 충분하지 않으면 지어내지 말고, 무엇이 부족한지 밝히십시오.
4. 근거에서 가져온 문장은 **끝에 그 근거의 번호를 `[n]` 형태로** 답니다. 여러 근거를 함께 썼으면 `[1][2]` 처럼 이어 붙입니다. 근거 목록에 없는 번호는 쓰지 마십시오.

출력 형식
첫 줄에 판정을 아래 둘 중 하나로 **그대로** 적습니다. 앞뒤에 다른 말을 붙이지 않습니다.

VERDICT: ANSWERABLE
VERDICT: INSUFFICIENT

둘째 줄부터 본문을 한국어로 씁니다. ANSWERABLE 이면 답변을, INSUFFICIENT 이면 근거의 무엇이 부족한지를 씁니다. **어느 쪽이든 본문을 비워 두지 마십시오.**

<근거>
[1] company-policy.txt (문자 120–540)
교육비는 1인당 연 200만원까지 지원합니다. 신청은 부서장 승인 후 인사팀에 제출합니다.

[2] benefits-2026.pdf (3쪽)
자격증 응시료는 교육비 예산과 별도로 처리합니다.
</근거>

<질문>
교육비 지원 한도가 얼마인가요?
</질문>
```

기대하는 응답:

```text
VERDICT: ANSWERABLE
교육비는 1인당 연 200만원까지 지원됩니다 [1]. 신청은 부서장 승인 후 인사팀에 제출합니다 [1]. 자격증 응시료는 이 예산과 별도로 처리됩니다 [2].
```

### 규칙 2가 이 프로젝트 고유인 이유

대부분의 RAG 프롬프트에는 없는 줄입니다. **생성기가 파일을 열 수 있는 에이전트(`codex app-server`)** 이기 때문에 필요합니다. 다른 LLM API였다면 "웹을 검색하지 마세요"는 무의미한 지시지만, 여기서는 에이전트가 실제로 컨테이너 파일시스템을 뒤지러 갈 수 있습니다.

**그리고 이 규칙은 프롬프트에만 맡기지 않았습니다.** 파일 접근을 막는 것은 지시 하나에 **프로세스 격리 다섯**을 겹친 것입니다 — 빈 임시 작업 디렉터리, `sandbox: "read-only"`, `approvalPolicy: "never"`, 세션 파일을 남기지 않는 `ephemeral`(이상 `adapters/llm/codex.py`), 상속하지 않는 최소 환경변수(`adapters/llm/session.py`). **지시로 막고 구조로도 막습니다.**

> 이 격리 다섯은 4절의 환각 억제 다섯 겹과 **다른 축**입니다. 방어선 다섯 겹은 "근거 없는 말을 하는 것"을, 격리 다섯은 "근거 밖을 읽으러 가는 것"을 막습니다. 규칙 2가 두 축에 걸쳐 있는 유일한 항목입니다.

### 문맥 블록의 모양

```
[1] company-policy.txt (문자 120–540)
<청크 본문>

[2] benefits-2026.pdf (3쪽)
<청크 본문>
```

문맥에 들어가는 값은 **검색 결과가 이미 들고 있는 것뿐입니다.** 프롬프트를 만들려고 문서를 다시 조회하지 않습니다.

- **번호**가 마커의 정의역입니다. `1` 부터 빠짐없이 매기며, 이 범위 밖을 가리키는 마커가 버려집니다.
- **파일명**을 넣는 이유는 둘입니다 — 모델이 "어느 문서에서 왔는가"를 답변 문장에 녹일 수 있고, 마커가 가리키는 대상이 사람이 읽어도 확인 가능해집니다.
- **위치**는 PDF면 쪽(`3쪽`), 텍스트·마크다운이면 원문 문자 구간(`문자 120–540`)입니다. PDF에 문자 오프셋을 적지 않는 이유는 그 값이 **쪽 안의** 오프셋이라 문서에서 그 자리를 찾는 데 쓸 수 없고, 적어 두면 문서 전체 기준으로 오해되기 때문입니다.

**질문은 문맥 뒤에 옵니다.** 긴 문맥 뒤에 질문을 두면 모델이 지시를 잊는다는 반대 방향의 우려가 있지만, **지시문이 문맥 앞에 따로 있으므로 그 위험은 이미 나뉘어 있습니다.** 질문을 마지막에 두어 얻는 것은 문서 본문에 섞여 들어온 명령형 문장이 지시를 덮어쓰기 어려워진다는 점입니다.

---

## 3. 왜 구조화 출력(JSON Schema)을 쓰지 않는가

Codex CLI는 JSON Schema로 출력을 강제하는 기능이 있고, 그것을 쓰면 판정과 인용을 파싱 없이 받을 수 있습니다. **쓰지 않았습니다.**

- **JSON은 스트리밍과 원리적으로 충돌합니다.** 부분 JSON은 파싱할 수 없으므로 답변 문자열을 꺼내려면 전체를 받아야 합니다. 스키마를 쓰는 순간 스트리밍은 **형식 때문에** 불가능해집니다. 스트리밍은 과제의 하드 요구사항이고 구조화 출력은 그렇지 않습니다.
- **스키마가 검증 단계를 없애 주지 못합니다.** 스키마는 `cited_chunks: [7]` 의 *형식*은 보증하지만, 근거가 2건뿐일 때 `7` 이 **존재하지 않는다는 사실**은 보증하지 않습니다. 어느 형식을 쓰든 범위 검증은 우리가 합니다. 그렇다면 스키마가 대신해 주는 일은 괄호 세기뿐이고, 그건 정규식 한 줄입니다.

**구조화 출력을 포기한 것이 아니라 위치를 옮긴 것입니다.** 구조를 보증하는 곳이 모델이 아니라 서버입니다 — `citations` 는 검증을 통과한 항목만 담긴 배열이고 그 모양은 응답 모델이 강제합니다. **모델이 형식을 어겨도 소비자가 받는 구조는 흔들리지 않습니다.**

### 대신 쓰는 것 — "첫 줄 판정" 규약

판정 줄 인식은 **정확히 일치**할 때만 성립합니다. `verdict: answerable` 도, `VERDICT:ANSWERABLE` 도, 뒤에 공백이 붙은 것도 판정 줄이 아닙니다.

관대하게 받으면 스트리밍 분리기가 "판정 줄이 아직 안 끝났을 가능성"을 판정할 수 없어(뒤에 공백이 몇 개 더 올지 모릅니다) **버퍼 상한이 사라지고, 그 순간 형식을 어긴 회차에서만 스트리밍이 죽습니다.** 엄격하게 받는 대가는 그 회차에서 판정 줄이 본문에 남는 것뿐이고, 그쪽이 훨씬 쌉니다.

엄격하게 받으므로 **버퍼 상한이 `21자`로 증명됩니다**(가장 긴 판정 줄의 길이). 무한정 자라는 경로가 없다는 것이 코드에 상수로 남아 있습니다.

---

## 4. 환각 억제 다중 방어선

RAG에서 환각을 하나의 수단으로 막는 것은 불가능합니다. **다섯 겹**을 두고, 각 겹이 앞 겹의 실패를 전제로 동작하게 했습니다.

### 방어선 1 — 검색 (근거가 애초에 도착하는가)

각 retriever가 자기 척도의 하한을 융합 **전에** 판정합니다(밀집 코사인 `0.82`, 어휘 희소도 `0.3` — 둘 다 계측값). 하이브리드라 한쪽이 놓친 것을 다른 쪽이 구제합니다.

**그러나 검색은 근거 없음을 통보하지 않습니다.** 빈 목록으로 드러낼 뿐이고 **거절 문구를 만들지 않습니다.** 검색이 거절 문구를 만들면 프롬프트가 그 판정을 뒤집을 수 없게 되고, 과제의 "하드코딩된 응답" 경계에도 닿습니다.

**임계값 하나에 환각 억제를 몰아주지 않는 이유는 둘입니다.** 임계값은 의미가 아니라 **벡터 거리**를 봅니다 — 질문의 어휘가 문서와 겹치면 답이 없어도 점수가 높습니다("연차를 팔 수 있나요?"는 연차 항목과 가깝습니다). 그리고 임계값이 유일한 방어선이면 모델이나 도메인이 바뀌어 점수 분포가 이동하는 순간 억제 전체가 함께 무너집니다.

### 방어선 2 — 근거 0건이면 모델을 부르지 않는다

```python
async def _no_evidence(self, context):
    """근거 0건 — 생성기를 부르지 않고, 문구도 만들지 않고 done 으로 끝낸다."""
    answer = Answer.no_evidence()      # text = ""
    ...
```

**빈 문맥에서 모델이 쓸 재료는 학습된 지식뿐이라 애초에 묻지 않습니다.** 이것이 가장 강한 방어선입니다 — 호출이 없으면 환각도 없습니다.

`build_prompt` 는 근거가 비면 **예외를 던집니다.** 정책이 아니라 타입 수준의 불변식이라, 이 경로를 우회하는 코드를 쓸 수 없습니다.

### 방어선 3 — 프롬프트 (규칙 1·2·3)

2절의 프롬프트 전문. 특히 **규칙 3이 `INSUFFICIENT` 판정의 유일한 원천**이라, 이 방어선이 뚫리면 뒤에서 잡을 방법이 없습니다.

### 방어선 4 — 판정 줄 (거절이 산문에 숨지 못하게)

모델이 "~인 것 같습니다", "명확하지 않지만 아마도" 같은 애매한 산문으로 답하면 **소비자는 그것이 거절인지 답변인지 알 수 없습니다.** 첫 줄 판정이 그것을 형식으로 만듭니다.

`INSUFFICIENT` 판정은 **마커를 이깁니다** — 본문에 `[1]` 이 있어도 인용을 비웁니다. 거절에 인용이 붙으면 "근거를 인용한 거절"이라는 자기모순이 됩니다. 이 규칙은 `Answer.__post_init__` 의 불변식이라 코드가 강제합니다.

### 방어선 5 — 마커 검증 (서버가 출처를 확정한다)

```python
def _validate_markers(body, source_count):
    """마커를 등장 순서로 모으고, 중복을 지우고, 범위 밖을 버린다."""
    seen = list(dict.fromkeys(int(m) for m in _MARKER.findall(body)))
    kept = tuple(m for m in seen if 1 <= m <= source_count)
    return kept, len(seen) - len(kept)
```

- **범위 밖 마커는 버리고 개수를 응답에 싣습니다**(`dropped_markers`). 없는 근거를 가리키는 인용은 환각입니다.
- **인용의 값은 검색 결과에서 복사합니다.** 모델은 번호만 고르고, 파일명·리비전·오프셋·쪽은 서버가 채웁니다. **모델이 파일명을 잘못 적어도 응답의 출처는 정확합니다.**
- 중복 제거가 범위 검증보다 **먼저**입니다 — 세는 대상이 "잘못 가리킨 근거"이지 "잘못 적은 글자"가 아니기 때문입니다.
- **마커가 하나도 없는 것은 유효한 답변입니다.** `citations` 가 빌 뿐입니다 — "답은 했는데 근거를 특정하지 못했다"는 별개의 상태이고, 이것을 실패로 처리하면 정상 답변이 사라집니다.

`dropped_markers` 는 응답에 실리고 로그에도 남습니다. **이 값이 늘어나는 것이 프롬프트 회귀의 가장 이른 신호입니다.**

### 방어선이 함께 작동하는 시나리오

질문: `"파이썬으로 웹 크롤러 만드는 방법 알려줘"` / 문서: 사내 정책·개발 가이드

1. **방어선 1** — 밀집 코사인이 `0.82` 미만, 어휘 희소도가 `0.3` 미만 → 채택 0건
2. **방어선 2** — 근거 0건이므로 **모델을 부르지 않음**
3. 응답: `finish_reason: "no_evidence"`, `answer: ""`, `citations: []`, `answer` 이벤트 0회

근거가 하한을 **넘긴** 경우(예: `"연차를 팔 수 있나요?"` — 연차 항목과 어휘가 겹침)라면 방어선 3~5가 순서대로 작동합니다.

1. **방어선 3** — 규칙 3이 "지어내지 말고 무엇이 부족한지 밝히라"
2. **방어선 4** — 모델이 `VERDICT: INSUFFICIENT` + 사유
3. **방어선 5** — 판정이 마커를 이겨 `citations` 를 비움
4. 응답: `finish_reason: "insufficient_evidence"`, 사유 본문, `citations: []`

---

## 5. 거절 두 갈래 — 누가 판정하는가

"답할 수 없다"는 **서로 다른 두 사건**이고 판정 주체가 다릅니다. 프롬프트가 맡는 것은 그중 하나뿐입니다.

| 사건 | 판정 주체 | 모델 호출 | `finish_reason` | 답변 문자열 |
|---|---|---|---|---|
| 근거가 0건 | **서비스** (개수만 보면 됨) | **하지 않음** | `no_evidence` | **빈 문자열** |
| 근거는 있으나 질문을 뒷받침하지 않음 | **모델** (본문을 읽어야 함) | 함 | `insufficient_evidence` | 모델이 쓴 사유 |
| 답변 | 모델 | 함 | `stop` | 모델이 쓴 답변 |

**둘을 나누는 이유는 사용자가 할 일이 다르기 때문입니다** — 앞은 문서를 올려야 하고 뒤는 질문을 바꿔야 합니다. 유사도 점수만으로는 뒤를 판정할 수 없습니다.

**근거가 0건일 때 서비스가 대신 문구를 쓰지도 않습니다.** `answer` 는 빈 문자열이고 `answer` 이벤트는 한 번도 나가지 않습니다. 근거가 없다는 **사실**은 `finish_reason` 이 나르고, 사용자에게 보일 **표현**은 소비자가 정합니다.

이 선택으로 얻는 것: **답변 문자열을 만드는 곳이 모델뿐이라는 규칙에 예외가 없어집니다.** 문구가 서버에 있으면 언어·톤·제품 문맥에 따라 변형이 늘고, 그때마다 서버가 답변 작성자에 가까워집니다. **그 미끄러짐이 시작될 자리를 없앴습니다.**

### 형식을 어겼을 때의 동작표

모델은 형식을 어깁니다. 정해 두지 않으면 같은 위반이 배포마다 다르게 나타납니다.

| 모델 출력 | 서버의 처리 | 왜 |
|---|---|---|
| 첫 줄 `VERDICT: ANSWERABLE` | 나머지가 답변. 마커 검증 진행 | — |
| 첫 줄 `VERDICT: INSUFFICIENT` | 나머지가 거절 사유. **인용을 비움** | 판정이 마커를 이긴다 |
| 판정 줄 없음 | `ANSWERABLE` 로 보고 **출력 전체를 본문으로** + 경고 로그 | 그 본문도 근거만 주어진 프롬프트에서 나왔다 — **형식 위반이 곧 환각은 아니다.** 마커 검증은 그대로 돌아 출처 보증도 그대로 |
| 판정 줄만 있고 본문 없음 | **생성 실패**로 분류해 재시도. 소진되면 `error` | 빈 답변으로 끝나는 경로는 "근거 없음" 하나뿐이어야 한다. 둘이면 소비자 화면에서 정상과 장애가 같아 보인다 |
| 범위 밖 마커 | 버리고 개수를 응답에 실음 | 없는 근거를 가리키는 인용은 환각 |
| 마커가 하나도 없음 | 유효한 답변. `citations` 가 빔 | "답은 했는데 근거를 특정하지 못했다"는 별개의 상태 |

**형식 위반은 응답이 아니라 로그로 잡습니다.** 판정 줄이 없어도 사용자에게는 정상 답변이 나가고, 경고만 남습니다. 이 경고가 늘어나는 것이 프롬프트 회귀의 신호입니다.

---

## 6. `PROMPT_VERSION`

```python
PROMPT_VERSION = "qa-ko-1"
```

**출력 문자열이 바뀌면 반드시 올립니다.**

**이 값은 응답 캐시 키의 재료 여섯 중 하나입니다** — 정규화된 질의, `top_k`, `prompt_version`, `index_signature`, 모델 식별자, `rerank_signature`([`src/app/core/cache.py`](./src/app/core/cache.py)의 `derive_cache_key`).

프롬프트가 바뀌면 같은 질문의 답이 달라지는데, 버전이 키에 없으면 프롬프트를 개선해도 낡은 캐시가 옛 답을 계속 내보냅니다. 그러면 개선이 관측되지 않을 뿐 아니라 **개선했다고 믿는 상태에서 실제로는 아무것도 바뀌지 않습니다** — 가장 조용한 종류의 실패입니다.

**이 값을 올리는 것만으로 캐시가 저절로 갈립니다.** 옛 항목은 새 키로 조회되지 않아 닿을 수 없게 되고 TTL로 사라집니다. 캐시를 비우는 별도 절차도, 배포 순서에 대한 주의사항도 없습니다.

> **다만 대가가 하나 있습니다.** `ARCHITECTURE.md` 의 골든셋 판정표(융합 45/47, 리랭킹 49/50)는 `qa-ko-1` 에서 나온 값입니다. 프롬프트를 고치고 버전을 올리면 **그 표가 출하되지 않는 프롬프트를 설명하게 됩니다.** 출력 문자열을 건드렸다면 같은 커밋에서 버전을 올리고, 표에 어느 버전의 값인지를 함께 적으십시오.

---

## 7. 프롬프트를 어떻게 시험하는가 — LLM 없이

프롬프트 품질을 LLM 없이 검증할 수는 없습니다. 그러나 **프롬프트와 파서가 서로 어긋나는 것**은 LLM 없이 검증할 수 있고, 그것이 가장 흔한 회귀입니다.

| 무엇을 | 어떻게 | 왜 LLM 없이 되는가 |
|---|---|---|
| 프롬프트가 지시한 판정 줄을 파서가 인식하는가 | 지시문에서 정규식으로 판정 줄을 뽑아 `parse_answer` 에 넣어 인식되는지 왕복 확인 | 파서가 순수 함수라 문자열만으로 돎 |
| 스트리밍 분리기와 완성본 파서가 같은 본문을 내는가 | 출력 12종 × 1·2·3조각 **전 분할**로 동치 단언 | 조각 경계는 우리가 만들 수 있음 |
| 판정 줄이 델타로 쪼개져 와도(`VER` / `DICT` / `: ANSW`) 복원되는가 | 같은 표 | 상태 기계가 순수 함수 |
| 범위 밖 마커가 버려지고 개수가 세어지는가 | 입출력 표 | 파서가 순수 함수 |
| 프롬프트에 근거 0건이 들어갈 수 없는가 | 예외 단언 | 불변식 |

**이 불일치는 오류를 내지 않습니다.** 프롬프트가 `VERDICT:` 를 지시하는데 파서가 `판정:` 을 찾고 있으면, 모든 응답이 `ANSWERABLE` 로 분류되고 `INSUFFICIENT` 가 **영원히 나오지 않습니다.** 테스트는 초록이고 응답은 `200` 입니다.

**동치 단언이 스펙 불변식을 떠받칩니다** — "`answer` 조각들을 이어 붙이면 `done.answer` 와 같다"는 계약이 성립하려면 스트리밍 경로와 완성본 경로가 같은 규칙으로 판정 줄을 걷어내야 합니다. 그래서 세 부품(`build_prompt`·`parse_answer`·`VerdictSplitter`)이 판정 줄 정의를 **상수 하나로 공유**합니다.

### 실물에서 확인한 것

| 확인 | 결과 |
|---|---|
| 델타를 이어 붙인 것이 완성본과 일치하는가 | **바이트 단위로 일치** (77자, 델타 38건) |
| 모델이 판정 줄 형식을 지키는가 | 정상 회차에서 지킴. 안 지킨 회차는 경고 로그로 관측 |
| 근거 없는 질문에서 무엇으로 끝나는가 | `no_evidence` — 생성기 호출 0회 |
| 골든셋 50문항 채점 | 기준 답변과 일치 **49**, 불일치 1, 판정불가 0 |

---

## 8. 두 번째 프롬프트 — 판정용 (LLM-Judge)

리랭킹 효과를 재기 위해, 생성된 답변이 골든셋의 `reference_answer` 와 같은 말을 하는지 판정하는 **두 번째 프롬프트**가 있습니다. 평가 전용이라 서비스 경로에는 없습니다(`-m "llm and slow"` 뒤).

설계에서 다른 점 셋:

1. **`JUDGE_PROMPT_VERSION` 을 `PROMPT_VERSION` 과 별개로 둡니다.** 채점 기준이 바뀌는 것과 답변 프롬프트가 바뀌는 것은 다른 사건이고, 합치면 채점자를 고칠 때마다 응답 캐시가 날아갑니다.
2. **판정자에게 근거 청크를 넘기지 않습니다.** 근거가 왔는지는 이미 앞 단계(검색 게이트)의 판정이라, 판정자에게 주면 같은 것을 두 번 세게 됩니다.
3. **도구 조회를 금지합니다.** 생성기가 파일을 열 수 있는 에이전트라 **채점자도 같은 도구를 갖습니다.** 채점자가 원문을 찾아 읽으면 채점 대상이 "답변"이 아니라 "답변 + 채점자가 찾아낸 것"이 됩니다.

### 채점 순서와 합산하지 않는 이유

```
① 검색 게이트 (근거가 왔는가 — 결정적)
② 문자열 검사 (expected_spans — 결정적)
③ LLM-Judge (reference_answer 와 같은 말인가 — 비결정적)

②와 ③을 합산하지 않는다
```

**합치면 흔들리는 쪽이 결정적인 쪽을 오염시킵니다.** 실제로 어긋난 문항이 7건 나왔고, 그중 6건은 `expected_spans` 가 **맞는 답을 다른 표현으로 썼다고 떨어뜨린 것**이었습니다. 나머지 1건은 방향이 반대라 사람이 봐야 합니다. **이 표의 값어치는 봐야 할 문항을 일곱 개로 좁혀 준다는 데 있습니다.**

`판정불가` 는 오답으로 세지 않습니다 — 판정자가 흔들린 것이 품질 저하로 보이면 안 되기 때문입니다. 과반이면 그 회차의 통과 수를 읽지 않습니다.

### 판정자 자신은 검증되지 않았다

**판정이 사람의 판정과 얼마나 맞는지 잰 적이 없습니다.** 그래서 통과 수는 "우리 판정자가 그렇게 봤다"까지입니다. 그리고 **판정자와 답변 생성기가 같은 CLI라 같은 실패를 공유할 수 있습니다.**

같은 구성을 두 회차 돌렸을 때 **집계는 같았지만 어긋난 문항의 목록이 달랐습니다.** 문자열 통과 수가 40으로 같은데 구성원이 바뀌었으므로 **생성기가 회차마다 다른 문장을 썼다**는 뜻입니다. 그래서 이 측정에서 읽어도 되는 것은 집계까지이고, **문항 하나의 결과를 근거로 무언가를 결정해서는 안 됩니다.**

---

## 9. 트레이드오프와 아직 정하지 않은 것

### 프롬프트 인젝션

질문을 문맥 **뒤**에 두어 문서 본문의 명령형 문장이 지시를 덮어쓰기 어렵게 했고, 질의 길이에 문자·토큰 두 겹 상한이 있습니다. **그러나 완전한 방어는 아닙니다.**

이 프로젝트에서 인젝션의 결과가 특히 나쁠 수 있는 이유는 생성기가 **에이전트**이기 때문입니다. 그래서 방어를 프롬프트가 아니라 **프로세스 격리**(빈 작업 디렉터리·읽기 전용 샌드박스·비대화형 승인·세션 비영속·최소 환경변수)에 걸었습니다. 인젝션이 성공해도 **뒤질 파일이 없고 실행할 권한이 없습니다.**

운영 이식 시: 알려진 패턴 탐지, 문맥과 질문의 구분자 강화, 시스템 프롬프트 제약 반복.

### 프롬프트가 한국어 고정

샘플 문서가 한국어이고 답변도 한국어라 지시문을 한국어로 고정했습니다. 질문 언어를 따라가게 하는 것은 **응답 언어 정책이 하나 더 생기는 일**이라 지금 정하지 않았습니다. 다국어 요구가 실제로 생기면 지시문·판정 줄·판정 프롬프트 셋을 함께 옮겨야 합니다.

### Few-shot 예시를 넣지 않았다

프롬프트에 입출력 예시를 넣으면 형식 준수율이 올라갈 수 있습니다. 넣지 않은 이유는 둘입니다 — 예시가 문맥 토큰을 먹어 근거가 들어갈 자리가 줄고, **형식 위반의 처리가 이미 정의되어 있어**(5절 표) 위반이 서비스를 깨지 않기 때문입니다. 형식 준수율이 실제로 문제가 되는 것이 관측되면(경고 로그의 빈도) 그때 넣습니다. **지금 넣으면 무엇을 개선했는지 잴 기준이 없습니다.**

### 근거 순서가 답변에 미치는 영향을 재지 않았다

프롬프트는 상위 K개를 순서대로 넣습니다. 모델이 앞쪽 근거에 더 큰 가중치를 둔다면 리랭킹의 효과가 답변에 반영되는 경로가 여기일 텐데, **재지 않았습니다.** 실제로 리랭킹 실측에서 `@1` 이 27→36으로 크게 움직이는 동안 답변 일치는 45→49에 그쳤는데, 그 간격의 원인이 "프롬프트가 다섯 개를 다 읽어서"인지 "순서가 실제로는 영향이 없어서"인지 이 측정으로는 가릴 수 없습니다.

### 규칙 3에 백스톱이 없다

1절 표에 적은 그대로입니다. `INSUFFICIENT` 판정의 정확성은 **전적으로 모델에 달려 있고**, 서버가 검증할 방법이 없습니다. 골든셋에서 근거 없음이 정답인 6문항으로 간접 확인하는 것이 지금 할 수 있는 전부입니다.

---

## 요약

이 프로젝트의 프롬프트 설계는 **"모델이 규칙을 지키게 만든다"** 가 아니라 **"모델이 규칙을 어겨도 서비스가 무너지지 않게 만든다"** 에서 출발했습니다.

- 형식 강제를 스트리밍과 맞바꾸지 않았고(줄 규약),
- 구조 보증의 위치를 모델에서 서버로 옮겼고(마커 검증 + 응답 모델),
- 거절을 산문이 아니라 형식으로 만들었고(판정 줄),
- 답변 문자열을 만드는 곳을 모델 하나로 유지했으며(빈 문자열 + `finish_reason`),
- 프롬프트가 유일한 방어선인 자리(규칙 3)를 **명시적으로 표시**했습니다.

프롬프트가 실패했을 때 무슨 일이 일어나는지가 이 문서의 절반입니다.
