## ADDED Requirements

### Requirement: 문서 변경은 그 문서를 근거로 삼은 캐시된 답변을 무효화한다

문서의 수집·교체·삭제가 성공적으로 확정되면, **그 문서를 인용한 캐시된 답변이 모두 무효화되어야 한다** (**MUST**). 무효화 대상은 그 문서를 인용한 항목으로 한정되어야 하며 (**MUST**), 관계없는 문서를 인용한 항목이 함께 지워져서는 안 된다 (**MUST NOT**).

청크를 실제로 저장한 수집은, **답하지 못했다고 판정되어 캐시된 답변도 무효화해야 한다** (**MUST**) — 근거를 찾지 못한 종료와 근거로 답할 수 없다고 판정된 종료 **둘 다**다. 그 판정의 전제가 "그때 저장되어 있던 내용"이라, 내용이 더해지거나 바뀌면 전제가 깨진다.

이 무효화는 **새 문서의 수집뿐 아니라 기존 문서의 교체와 재색인에서도** 일어나야 한다 (**MUST**) — 조건은 문서의 개수가 아니라 내용이다. 교체는 개수를 그대로 둔 채 내용을 바꾸고, 재색인은 검색 대상이 되는 청크를 바꾼다.

이 무효화의 범위는 문서 단위가 아니다 (**MUST NOT**) — 답하지 못한 판정을 뒤집는 문서는 그 판정이 참조한 문서와 다른 문서이므로, 문서 태그로는 닿지 않는다.

아무것도 저장하지 않고 끝난 수집(`unchanged`)은 이 판정을 무효화해서는 안 된다 (**MUST NOT**).

무효화는 레지스트리 커밋이 **끝난 뒤**에 일어나야 한다 (**MUST**). 수집이 실패해 롤백되거나 요청이 거부되면 캐시는 그대로여야 한다 (**MUST**).

무효화 실패가 수집·삭제 요청을 실패시켜서는 안 된다 (**MUST NOT**) — 문서는 이미 바뀌었고, 그것을 되돌리는 것이 더 큰 손해다. 다만 무효화가 실패했다는 사실은 관측 가능해야 한다 (**MUST**).

내용이 같아 아무것도 하지 않고 끝난 수집(`unchanged`)은 캐시를 무효화해서는 안 된다 (**MUST NOT**) — 바뀐 것이 없으면 캐시된 답변도 여전히 옳다.

> 이 요구사항이 수집 쪽 스펙에 있는 이유는 무효화의 **시점**이 수집의 순서 계약에 속하기 때문이다. 커밋 전에 지우면 실패한 수집이 멀쩡한 캐시를 날리고, 커밋 후에 지우지 않으면 바뀐 문서의 옛 답변이 그대로 나간다.

#### Scenario: 재업로드가 그 문서를 인용한 답변을 무효화한다

- **WHEN** 어떤 문서를 인용한 답변이 캐시된 상태에서 그 문서를 내용이 다른 버전으로 재업로드한다
- **THEN** 수집이 `replaced` 로 끝나고, 그 문서에 관한 같은 질문이 이후 캐시 미스로 처리된다

#### Scenario: 삭제가 그 문서를 인용한 답변을 무효화한다

- **WHEN** 어떤 문서를 인용한 답변이 캐시된 상태에서 그 문서를 삭제한다
- **THEN** 삭제가 성공하고, 그 문서에 관한 같은 질문이 이후 캐시 미스로 처리된다

#### Scenario: 새 문서 수집이 근거 없음 판정을 무효화한다

- **WHEN** 어떤 질문이 근거 없음으로 캐시된 상태에서 새 문서를 수집한다
- **THEN** 같은 질문이 이후 캐시 미스로 처리되어 검색이 다시 돌아간다

#### Scenario: 문서 교체가 근거 없음 판정을 무효화한다

- **WHEN** 어떤 질문이 근거 없음으로 캐시된 상태에서 **기존 문서를** 다른 내용으로 교체한다
- **THEN** 수집이 `replaced` 로 끝나고, 같은 질문이 이후 캐시 미스로 처리된다

#### Scenario: 수집이 "답할 수 없음" 판정도 무효화한다

- **WHEN** 어떤 질문이 문서 A 를 근거로 답할 수 없다고 판정되어 캐시된 상태에서 문서 B 를 수집한다
- **THEN** 문서 A 를 건드리지 않았음에도 같은 질문이 이후 캐시 미스로 처리된다

#### Scenario: 관계없는 문서의 수집은 다른 답변을 건드리지 않는다

- **WHEN** 문서 A 를 인용한 답변이 캐시된 상태에서 관계없는 문서 B 를 재업로드한다
- **THEN** 문서 A 에 관한 같은 질문이 여전히 캐시 히트로 처리된다

#### Scenario: 거부된 업로드는 캐시를 건드리지 않는다

- **WHEN** 캐시된 답변이 있는 상태에서 지원하지 않는 포맷이나 내용이 없는 문서를 업로드한다
- **THEN** 업로드가 거부되고, 이후 같은 질문이 여전히 캐시 히트로 처리된다

#### Scenario: 내용이 같은 재업로드는 캐시를 지우지 않는다

- **WHEN** 어떤 문서를 인용한 답변이 캐시된 상태에서 같은 내용의 같은 파일을 다시 업로드한다
- **THEN** 수집이 `unchanged` 로 끝나고, 같은 질문이 여전히 캐시 히트로 처리된다

#### Scenario: 캐시 저장소가 죽어 있어도 수집이 성공한다

- **WHEN** 캐시 저장소에 닿을 수 없는 상태에서 문서를 수집한다
- **THEN** 수집이 정상적으로 성공하고 문서가 검색 가능해진다
